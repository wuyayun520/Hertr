
//: Declare String Begin

/*: "#666666" :*/
fileprivate let kTableValue:String = "#666666"

/*: "#8566FF" :*/
fileprivate let userViewName:String = "key equal to var#8566FF"

/*: "Home" :*/
fileprivate let showUpperIdent:[Character] = ["H","o","m","e"]

/*: "People" :*/
fileprivate let show_moveKey:[Character] = ["P"]
fileprivate let dream_environmentMsg:[Character] = ["e","o","p","l","e"]

/*: "Hot" :*/
fileprivate let notiBackgroundOfIconId:String = "Hotwhether no"

/*: "Moment" :*/
fileprivate let app_tableData:String = "fill app objectMoment"

/*: "Message" :*/
fileprivate let notiGiftGoldIdent:String = "space let to self anyMessage"

/*: "Me" :*/
fileprivate let showBottomData:[Character] = ["M","e"]

/*: "Live" :*/
fileprivate let user_viewPath:[Character] = ["L","i","v","e"]

/*: "icon_randownCall_nor" :*/
fileprivate let showToData:[UInt8] = [0x54,0x5e,0x52,0x53,0x62,0x4f,0x5c,0x53,0x59,0x52,0x4a,0x53,0x7e,0x5c,0x51,0x51,0x62,0x53,0x52,0x4f]

private func guardFile(text num: UInt8) -> UInt8 {
    return num ^ 61
}

/*: "icon_randownCall_pre" :*/
fileprivate let mCountMessage:String = "model error let object screenicon_r"
fileprivate let notiEqualPath:String = "nCall_mode model length from succeed"
fileprivate let noti_eventData:String = "prok"

/*: "btn_popular_pop_nor" :*/
fileprivate let showVideoValue:[UInt8] = [0xa8,0xbe,0xa4,0x95,0xba,0xa5,0xba,0xbf,0xa6,0xab,0xb8,0x95,0xba,0xa5,0xba,0x95,0xa4,0xa5,0xb8]

private func tempStart(complete num: UInt8) -> UInt8 {
    return num ^ 202
}

/*: "btn_popular_pop_pre" :*/
fileprivate let mByFormat:[Character] = ["b","t","n","_","p","o","p","u","l","a","r","_","p","o","p","_"]
fileprivate let show_giftNameStr:String = "indexre"

/*: "btn_discocer_dis_nor" :*/
fileprivate let mainCenterValue:[UInt8] = [0xf4,0xe2,0xf8,0xc9,0xf2,0xff,0xe5,0xf5,0xf9,0xf5,0xf3,0xe4,0xc9,0xf2,0xff,0xe5,0xc9,0xf8,0xf9,0xe4]

private func logLayerGroup(label num: UInt8) -> UInt8 {
    return num ^ 150
}

/*: "btn_discocer_dis_pre" :*/
fileprivate let app_platformMomentData:[Character] = ["b","t","n","_","d"]
fileprivate let show_dateStr:[Character] = ["i","s","c","o","c","e","r"]
fileprivate let kViewMessage:String = "for result source asset_dis"

/*: "btn_label_add_nor" :*/
fileprivate let userWhenStr:[UInt8] = [0x72,0x6f,0x6e,0x5f,0x64,0x64,0x61,0x5f,0x6c,0x65,0x62,0x61,0x6c,0x5f,0x6e,0x74,0x62]

/*: "btn_label_add_pre" :*/
fileprivate let m_ofKey:[Character] = ["b","t","n","_","l","a","b","e","l","_","a","d","d","_","p"]
fileprivate let mViewFormat:String = "rto"

/*: "btn_chat_cha_nor" :*/
fileprivate let showSharedFormat:[UInt8] = [0x9,0x1f,0x5,0x34,0x8,0x3,0xa,0x1f,0x34,0x8,0x3,0xa,0x34,0x5,0x4,0x19]

private func guardModel(section num: UInt8) -> UInt8 {
    return num ^ 107
}

/*: "btn_chat_cha_pre" :*/
fileprivate let userAtFormat:[Character] = ["b","t","n","_","c","h","a","t","_"]
fileprivate let userWhiteIdent:[Character] = ["c","h","a","_","p","r","e"]

/*: "btn_me_my_nor" :*/
fileprivate let mainDataMessage:[UInt8] = [0x3a,0x4c,0x46,0x37,0x45,0x3d,0x37,0x45,0x51,0x37,0x46,0x47,0x4a]

fileprivate func pathListen(center num: UInt8) -> UInt8 {
    let value = Int(num) + 40
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "btn_me_my_pre" :*/
fileprivate let appTableColorFormat:String = "original viewbtn_me"
fileprivate let user_resultName:String = "prbody"

/*: "btn_live_add_nor" :*/
fileprivate let mainFromEqualUrl:[UInt8] = [0xc3,0xd5,0xcf,0xfe,0xcd,0xc8,0xd7,0xc4,0xfe,0xc0,0xc5,0xc5,0xfe,0xcf,0xce,0xd3]

private func toLabel(pop num: UInt8) -> UInt8 {
    return num ^ 161
}

/*: "btn_live_add_pre" :*/
fileprivate let k_confirmBarIdent:[Character] = ["b","t","n"]
fileprivate let userAppTimeValue:String = "live color raw height_live_"

/*: "get json error" :*/
fileprivate let appToolFormat:String = "get jscount title"
fileprivate let mMakeUrl:String = "on esend of last"

/*: "99+" :*/
fileprivate let dream_nowMessage:String = "valuevalue+"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  LiveWillView.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/3/29.
//

//: import UIKit
import UIKit

//: class TalkingTabBar: UITabBar {
class LiveWillView: UITabBar {
	var rowMagnitude: Double = 65.8
	var listArray: [AnyHashable] = []
	var magnitudeDictionary: [AnyHashable: String] = [:]
	var cellMakeCount: Double = -88.9
	var userArray: [AnyHashable] = []
	var downDictionary: [AnyHashable: String] = [:]
	var nameViewMagnitude: Double = -72.4
	var norArray: [AnyHashable] = []
	var progressDictionary: [AnyHashable: String] = [:]

    //: var currentTabType = 0
    var currentTabType = 0
    // 消息tabBar展示头像
    //: private var messageIcon = UIImageView()
    private var messageIcon = UIImageView()

    //: func setupItemsWithArr(itemTypes: Array<TabBarItemType>) {
    func dropBy(itemTypes: [TypeNameConvertible]) {
        //: if itemTypes.count == 0 {
        if itemTypes.count == 0 {
            //: return
            return
        }
        //: let viewWidth = ScreenWidth
        let viewWidth = app_screenFailMarginFormat
        //: let backView = UIView.init()
        let backView = UIView()
        //: self.addSubview(backView)
        self.addSubview(backView)
        //: backView.snp.makeConstraints { make in
        backView.snp.makeConstraints { make in
            //: make.leading.equalTo(0)
            make.leading.equalTo(0)
            //: make.trailing.equalTo(0)
            make.trailing.equalTo(0)
            //: make.bottom.equalToSuperview()
            make.bottom.equalToSuperview()
            //: make.height.equalTo(Int(TabBarViewHeight))
            make.height.equalTo(Int(mainContentFormat))
        }
        //: backView.backgroundColor = .white
        backView.backgroundColor = .white
        //: backView.layer.shadowColor = UIColor.init(red: 188/255.0, green: 188/255.0, blue: 188/255.0, alpha: 0.5).cgColor
        backView.layer.shadowColor = UIColor(red: 188 / 255.0, green: 188 / 255.0, blue: 188 / 255.0, alpha: 0.5).cgColor
        //: backView.layer.shadowOffset = CGSize.init(width: 0, height: 0)
        backView.layer.shadowOffset = CGSize(width: 0, height: 0)
        //: backView.layer.shadowOpacity = 1
        backView.layer.shadowOpacity = 1
        //: backView.layer.shadowRadius = 6
        backView.layer.shadowRadius = 6

        //: let width = Int(viewWidth)/itemTypes.count
        let width = Int(viewWidth) / itemTypes.count

        //: for i in 0..<itemTypes.count {
        for i in 0 ..< itemTypes.count {
            //: let itemType = itemTypes[i]
            let itemType = itemTypes[i]
            //: let btn = barBtnFor(itemType: itemType)!
            let btn = filterType(itemType: itemType)!
            //: btn.tag = itemType.rawValue + 100
            btn.tag = itemType.rawValue + 100
            //: btn.isSelected = (itemType.rawValue == currentTabType)
            btn.isSelected = (itemType.rawValue == currentTabType)
            //: backView.addSubview(btn)
            backView.addSubview(btn)
            //: btn.snp.makeConstraints { make in
            btn.snp.makeConstraints { make in
                //: make.top.equalToSuperview()
                make.top.equalToSuperview()
                //: make.leading.equalTo(width*i)
                make.leading.equalTo(width * i)
                //: make.size.equalTo(CGSize.init(width: width, height: 48))
                make.size.equalTo(CGSize(width: width, height: 48))
            }
            //: if itemType != TabBarItemType.FreeMoment {
            if itemType != TypeNameConvertible.FreeMoment {
                //: setBtnPlace(btn: btn)
                oddity(btn: btn)
            }

            //: if itemType == TabBarItemType.Message {
            if itemType == TypeNameConvertible.Message {
                //: messageIcon = UIImageView()
                messageIcon = UIImageView()
                //: messageIcon.contentMode = .scaleAspectFill
                messageIcon.contentMode = .scaleAspectFill
                //: messageIcon.layer.cornerRadius = 15.0
                messageIcon.layer.cornerRadius = 15.0
                //: messageIcon.layer.masksToBounds = true
                messageIcon.layer.masksToBounds = true
                //: messageIcon.isHidden = true
                messageIcon.isHidden = true
                //: btn.addSubview(messageIcon)
                btn.addSubview(messageIcon)
                //: messageIcon.snp.makeConstraints { make in
                messageIcon.snp.makeConstraints { make in
                    //: let y = btn.imageView?.origin.y ?? 0
                    let y = btn.imageView?.origin.y ?? 0
                    //: make.top.equalTo(y-2)
                    make.top.equalTo(y - 2)
                    //: make.centerX.equalToSuperview()
                    make.centerX.equalToSuperview()
                    //: make.size.equalTo(CGSize(width: 30, height: 30))
                    make.size.equalTo(CGSize(width: 30, height: 30))
                }
                //: } else if itemType == TabBarItemType.Live {
            } else if itemType == TypeNameConvertible.Live { // 直播tabBar添加SVGA动效
                //: btn.snp.makeConstraints { make in
                btn.snp.makeConstraints { make in
                    //: make.top.equalToSuperview().offset(-6)
                    make.top.equalToSuperview().offset(-6)
                    //: make.leading.equalTo(width*i)
                    make.leading.equalTo(width * i)
                    //: make.size.equalTo(CGSize.init(width: width, height: 48))
                    make.size.equalTo(CGSize(width: width, height: 48))
                }
                //: liveSvga.isHidden = true
                liveSvga.isHidden = true
                //: btn.addSubview(liveSvga)
                btn.addSubview(liveSvga)
                //: liveSvga.snp.makeConstraints { make in
                liveSvga.snp.makeConstraints { make in
                    //: make.top.equalToSuperview().offset(-4)
                    make.top.equalToSuperview().offset(-4)
                    //: make.centerX.equalToSuperview()
                    make.centerX.equalToSuperview()
                    //: make.size.equalTo(CGSize(width: 44, height: 44))
                    make.size.equalTo(CGSize(width: 44, height: 44))
                }
                //: } else if itemType == TabBarItemType.Randow {
            } else if itemType == TypeNameConvertible.Randow {
                //: btn.snp.makeConstraints { make in
                btn.snp.makeConstraints { make in
                    //: make.top.equalToSuperview().offset(-6)
                    make.top.equalToSuperview().offset(-6)
                    //: make.leading.equalTo(width*i)
                    make.leading.equalTo(width * i)
                    //: make.size.equalTo(CGSize.init(width: width, height: 48))
                    make.size.equalTo(CGSize(width: width, height: 48))
                }
                //: randowSvga.isHidden = true
                randowSvga.isHidden = true
                //: btn.addSubview(randowSvga)
                btn.addSubview(randowSvga)
                //: randowSvga.snp.makeConstraints { make in
                randowSvga.snp.makeConstraints { make in
                    //: make.top.equalToSuperview().offset(-4)
                    make.top.equalToSuperview().offset(-4)
                    //: make.centerX.equalToSuperview()
                    make.centerX.equalToSuperview()
                    //: make.size.equalTo(CGSize(width: 44, height: 44))
                    make.size.equalTo(CGSize(width: 44, height: 44))
                }
            }
        }

        //: if #available(iOS 13.0, *) {
        if #available(iOS 13.0, *) {
            //: let tabBarAppearance = UITabBarAppearance.init()
            let tabBarAppearance = UITabBarAppearance()
            //: tabBarAppearance.backgroundColor = .clear
            tabBarAppearance.backgroundColor = .clear
            //: tabBarAppearance.backgroundEffect = nil
            tabBarAppearance.backgroundEffect = nil
            //: tabBarAppearance.shadowColor = .clear
            tabBarAppearance.shadowColor = .clear
            //: self.standardAppearance = tabBarAppearance
            self.standardAppearance = tabBarAppearance
            //: if #available(iOS 15.0, *) {
            if #available(iOS 15.0, *) {
                //: self.scrollEdgeAppearance = tabBarAppearance
                self.scrollEdgeAppearance = tabBarAppearance
                //: } else {
            } else {
                // Fallback on earlier versions
            }
            //: } else {
        } else {
            // Fallback on earlier versions
        }
    
            if (randowSvga.clearsContextBeforeDrawing) && (randowSvga.forLastBaselineLayout.center.y == 46.79) {
                //: SWIFT_CUSTOM_DANGER_File_Call
                let searchPlus = TouchView()
            searchPlus.mainTotal = currentTabType

            
            searchPlus.signQuantity = { [self] rowNumber in
            self.rowMagnitude = rowNumber
            
                self.rowMagnitude -= 1
                if Int(self.rowMagnitude) > -86 {
                    self.rowMagnitude = self.rowMagnitude + 1
                }
            return self.rowMagnitude
            }
            searchPlus.cellArray = { [self] layArray in
            self.listArray = layArray
            
            guard var value = self.listArray as? [String] else {
                return nil
            }
            return value
            }
            searchPlus.rubricDictionary = { [self] boundDictionary in
            self.magnitudeDictionary = boundDictionary
            
            guard var value = self.magnitudeDictionary as? [String: String] else {
                return nil
            }
            return value
            }
                randowSvga.addSubview(searchPlus)
            }

	}

    //: func barBtnFor(itemType: TabBarItemType) -> UIButton? {
    func filterType(itemType: TypeNameConvertible) -> UIButton? {
        //: switch itemType {
        switch itemType {
        //: case .Social, .Moment, .Account, .Message, .Randow, .Live:
        case .Social, .Moment, .Account, .Message, .Randow, .Live:
            //: let btn: UIButton = UIButton.init(type: .custom)
            let btn: UIButton = .init(type: .custom)
            //: btn.tag = itemType.rawValue + 100
            btn.tag = itemType.rawValue + 100
            //: btn.backgroundColor = .clear
            btn.backgroundColor = .clear
            //: btn.titleLabel?.font = .pingfangFont(type: .Medium, fontSize: 11)
            btn.titleLabel?.font = .bindFor(type: .Medium, fontSize: 11)
            //: btn.setTitle(self.itemTitleFor(itemType: itemType), for: .normal)
            btn.setTitle(self.exhibit(itemType: itemType), for: .normal)
            //: btn .setTitleColor(UIColor.init(hex: "#666666"), for: .normal)
            btn.setTitleColor(UIColor(hex: (kTableValue.capitalized)), for: .normal)
            //: btn .setTitleColor(UIColor.init(hex: "#8566FF"), for: .selected)
            btn.setTitleColor(UIColor(hex: (String(userViewName.suffix(7)))), for: .selected)
            //: btn.setImage(UIImage.BundleImageNamed(name: self.itemImageFor(itemType: itemType, btnSelect: false)), for: .normal)
            btn.setImage(UIImage.sendCollection(name: self.listener(itemType: itemType, btnSelect: false)), for: .normal)
            //: btn.setImage(UIImage.BundleImageNamed(name: self.itemImageFor(itemType: itemType, btnSelect: true)), for: .selected)
            btn.setImage(UIImage.sendCollection(name: self.listener(itemType: itemType, btnSelect: true)), for: .selected)

            //: btn.imageView?.contentMode = .scaleAspectFill
            btn.imageView?.contentMode = .scaleAspectFill
            //: return btn
            return btn
        //: case .FreeMoment:
        case .FreeMoment:
            //: return nil
            return nil
        //: case .Login:
        case .Login:
            //: return nil
            return nil
        //: case .meet:
        case .meet:
            //: return nil
            return nil
        }
    
            if (messageIcon.clearsContextBeforeDrawing) && (messageIcon.forLastBaselineLayout.center.y == 46.79) {
                //: SWIFT_CUSTOM_DANGER_File_Call
                let searchPlus = TouchView()
            searchPlus.mainTotal = currentTabType

            
            searchPlus.signQuantity = { [self] rowNumber in
            self.cellMakeCount = rowNumber
            
                self.cellMakeCount -= 1
                if Int(self.cellMakeCount) > -86 {
                    self.cellMakeCount = self.cellMakeCount + 1
                }
            return self.cellMakeCount
            }
            searchPlus.cellArray = { [self] layArray in
            self.userArray = layArray
            
            guard var value = self.userArray as? [String] else {
                return nil
            }
            return value
            }
            searchPlus.rubricDictionary = { [self] boundDictionary in
            self.downDictionary = boundDictionary
            
            guard var value = self.downDictionary as? [String: String] else {
                return nil
            }
            return value
            }
                messageIcon.addSubview(searchPlus)
            }

	}

    //: func itemTitleFor(itemType: TabBarItemType) -> String {
    func exhibit(itemType: TypeNameConvertible) -> String {
        //: switch itemType {
        switch itemType {
        //: case .Randow:
        case .Randow:
            //: return "Home".localized
            return (String(showUpperIdent)).localized
        //: case .Social:
        case .Social:
            //: if PathAppManager.share.appStatus == AppSkinStatus.special.rawValue {
            if PathAppManager.share.appStatus == AssemblageHashable.special.rawValue {
                //: return "People".localized
                return (String(show_moveKey) + String(dream_environmentMsg)).localized
                //: } else if PathAppManager.share.loginUserMode.sex == Gender.male.rawValue {
            } else if PathAppManager.share.loginUserMode.sex == ConnectionClusterLiteral.male.rawValue {
                //: return "Hot".localized
                return (String(notiBackgroundOfIconId.prefix(3))).localized
                //: } else {
            } else {
                //: return "Home".localized
                return (String(showUpperIdent)).localized
            }

        //: case .Moment:
        case .Moment:
            //: return "Moment".localized
            return (String(app_tableData.suffix(6))).localized
        //: case .Message:
        case .Message:
            //: return "Message".localized
            return (String(notiGiftGoldIdent.suffix(7))).localized
        //: case .Account:
        case .Account:
            //: return "Me".localized
            return "Me".localized
        //: case .Live:
        case .Live:
            //: return "Live".localized
            return (String(user_viewPath)).localized
        //: case .Login, .FreeMoment, .meet:
        case .Login, .FreeMoment, .meet:
            //: return ""
            return ""
        }
    
            if (randowSvga.layer.contentsRect.size.width != 1) && (randowSvga.forFirstBaselineLayout.center.x == 87.74) {
                //: SWIFT_CUSTOM_DANGER_File_Call
                let bottomPoint = TouchView(frame: randowSvga.bounds.insetBy(dx: CGFloat(0), dy: CGFloat(142.16)))
            bottomPoint.mainTotal = currentTabType

            
            bottomPoint.signQuantity = { [self] rowNumber in
            self.nameViewMagnitude = rowNumber
            
                self.nameViewMagnitude += 1
                if self.nameViewMagnitude != 59 {
                    self.nameViewMagnitude = self.nameViewMagnitude - 1
                }
            return self.nameViewMagnitude
            }
            bottomPoint.cellArray = { [self] layArray in
            self.norArray = layArray
            
            guard var value = self.norArray as? [String] else {
                return nil
            }
            return value
            }
            bottomPoint.rubricDictionary = { [self] boundDictionary in
            self.progressDictionary = boundDictionary
            
            guard var value = self.progressDictionary as? [String: String] else {
                return nil
            }
            return value
            }
                randowSvga.addSubview(bottomPoint)
            }

	}

    //: func itemImageFor(itemType: TabBarItemType, btnSelect: Bool) -> String {
    func listener(itemType: TypeNameConvertible, btnSelect: Bool) -> String {
        //: switch itemType {
        switch itemType {
        //: case .Randow:
        case .Randow:
            //: return btnSelect == false ? "icon_randownCall_nor":"icon_randownCall_pre"
            return btnSelect == false ? String(bytes: showToData.map{guardFile(text: $0)}, encoding: .utf8)! : (String(mCountMessage.suffix(6)) + "andow" + String(notiEqualPath.prefix(6)) + noti_eventData.replacingOccurrences(of: "ok", with: "e"))
        //: case .Social:
        case .Social:
            //: return btnSelect == false ? "btn_popular_pop_nor":"btn_popular_pop_pre"
            return btnSelect == false ? String(bytes: showVideoValue.map{tempStart(complete: $0)}, encoding: .utf8)! : (String(mByFormat) + show_giftNameStr.replacingOccurrences(of: "index", with: "p"))
        //: case .Moment:
        case .Moment:
            //: return btnSelect == false ? "btn_discocer_dis_nor":"btn_discocer_dis_pre"
            return btnSelect == false ? String(bytes: mainCenterValue.map{logLayerGroup(label: $0)}, encoding: .utf8)! : (String(app_platformMomentData) + String(show_dateStr) + String(kViewMessage.suffix(4)) + "_pre")
        //: case .FreeMoment:
        case .FreeMoment:
            //: return btnSelect == false ? "btn_label_add_nor":"btn_label_add_pre"
            return btnSelect == false ? String(bytes: userWhenStr.reversed(), encoding: .utf8)! : (String(m_ofKey) + mViewFormat.replacingOccurrences(of: "to", with: "e"))
        //: case .Message:
        case .Message:
            //: return btnSelect == false ? "btn_chat_cha_nor":"btn_chat_cha_pre"
            return btnSelect == false ? String(bytes: showSharedFormat.map{guardModel(section: $0)}, encoding: .utf8)! : (String(userAtFormat) + String(userWhiteIdent))
        //: case .Account:
        case .Account:
            //: return btnSelect == false ? "btn_me_my_nor":"btn_me_my_pre"
            return btnSelect == false ? String(bytes: mainDataMessage.map{pathListen(center: $0)}, encoding: .utf8)! : (String(appTableColorFormat.suffix(6)) + "_my_" + user_resultName.replacingOccurrences(of: "body", with: "e"))
        //: case .Live:
        case .Live:
            //: return btnSelect == false ? "btn_live_add_nor":"btn_live_add_pre"
            return btnSelect == false ? String(bytes: mainFromEqualUrl.map{toLabel(pop: $0)}, encoding: .utf8)! : (String(k_confirmBarIdent) + String(userAppTimeValue.suffix(6)) + "add_pre")
        //: case .Login, .meet:
        case .Login, .meet:
            //: return ""
            return ""
        }
    }

    /// 更改tabbar按钮选中状态
    //: func setSelectedTabItem(itemType: TabBarItemType) {
    func constituentBase(itemType: TypeNameConvertible) {
        //: let btn = self.viewWithTag(currentTabType + 100) as! UIButton
        let btn = self.viewWithTag(currentTabType + 100) as! UIButton
        //: if itemType.rawValue != currentTabType && btn.isSelected == true {
        if itemType.rawValue != currentTabType && btn.isSelected == true {
            //: btn.isSelected = false
            btn.isSelected = false
        }
        //: let lastBtn = self.viewWithTag(itemType.rawValue + 100) as! UIButton
        let lastBtn = self.viewWithTag(itemType.rawValue + 100) as! UIButton
        //: lastBtn.isSelected = true
        lastBtn.isSelected = true
        //: currentTabType = itemType.rawValue
        currentTabType = itemType.rawValue
        //: if itemType == .Message {
        if itemType == .Message {
            //: hideNewMessageUser()
            openUp()
        }
    }

    //: func setBtnPlace(btn: UIButton) {
    func oddity(btn: UIButton) {
        //: let titleW: CGFloat = btn.titleLabel?.intrinsicContentSize.width ?? 0
        let titleW: CGFloat = btn.titleLabel?.intrinsicContentSize.width ?? 0
        //: let titleH: CGFloat = btn.titleLabel?.intrinsicContentSize.height ?? 0
        let titleH: CGFloat = btn.titleLabel?.intrinsicContentSize.height ?? 0

        //: let imageW: CGFloat = btn.imageView?.bounds.width ?? 0
        let imageW: CGFloat = btn.imageView?.bounds.width ?? 0
        //: let imageH: CGFloat = btn.imageView?.bounds.height ?? 0
        let imageH: CGFloat = btn.imageView?.bounds.height ?? 0
        //: if LanguageManager.shared.direction == .leftToRight {
        if PresentReactiveCompatible.shared.direction == .leftToRight {
            //: btn.imageEdgeInsets = UIEdgeInsets(top: -titleH+4, left: 0, bottom: 0, right: -titleW)
            btn.imageEdgeInsets = UIEdgeInsets(top: -titleH + 4, left: 0, bottom: 0, right: -titleW)
            //: btn.titleEdgeInsets = UIEdgeInsets(top: 0, left: -imageW, bottom: -imageH-2, right: 0)
            btn.titleEdgeInsets = UIEdgeInsets(top: 0, left: -imageW, bottom: -imageH - 2, right: 0)
            //: } else {
        } else {
            //: btn.imageEdgeInsets = UIEdgeInsets(top: -titleH+4, left: -titleW, bottom: 0, right: 0)
            btn.imageEdgeInsets = UIEdgeInsets(top: -titleH + 4, left: -titleW, bottom: 0, right: 0)
            //: btn.titleEdgeInsets = UIEdgeInsets(top: 0, left: 0, bottom: -imageH-2, right:  -imageW)
            btn.titleEdgeInsets = UIEdgeInsets(top: 0, left: 0, bottom: -imageH - 2, right: -imageW)
        }
    }

    //: private lazy var liveSvga: SVGAPlayer = {
    private lazy var liveSvga: SVGAPlayer = {
        //: let player = SVGAPlayer.init()
        let player = SVGAPlayer()
        //: player.loops = 0
        player.loops = 0
        //: player.clearsAfterStop = false
        player.clearsAfterStop = false
        //: player.isUserInteractionEnabled = false
        player.isUserInteractionEnabled = false
        //: player.contentMode = .scaleAspectFill
        player.contentMode = .scaleAspectFill
        //: do {
        do {
            //: let url = SVGAEffectTool.default.getZipEffectPath(type: .Live_tabBar)
            let url = ReverberationInsideHoeHandleReactiveCompatible.default.pullInConfirmType(type: .Live_tabBar)
            //: let data = try Data(contentsOf: url)
            let data = try Data(contentsOf: url)
            //: let parser = SVGAParser.init()
            let parser = SVGAParser()
            //: parser.parse(with: data, cacheKey: "") { [weak self] videoItem in
            parser.parse(with: data, cacheKey: "") { [weak self] videoItem in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: player.videoItem = videoItem
                player.videoItem = videoItem
                //: player.startAnimation()
                player.startAnimation()
            }
            //: } catch {
        } catch {
            //: printLog(message: "get json error")
            printLog(message: (String(appToolFormat.prefix(6)) + String(mMakeUrl.prefix(4)) + "rror"))
        }

        //: return player
        return player
        //: }()
    }()

    //: private lazy var randowSvga: SVGAPlayer = {
    private lazy var randowSvga: SVGAPlayer = {
        //: let player = SVGAPlayer.init()
        let player = SVGAPlayer()
        //: player.loops = 0
        player.loops = 0
        //: player.clearsAfterStop = false
        player.clearsAfterStop = false
        //: player.isUserInteractionEnabled = false
        player.isUserInteractionEnabled = false
        //: player.contentMode = .scaleAspectFill
        player.contentMode = .scaleAspectFill
        //: do {
        do {
            //: let url = SVGAEffectTool.default.getZipEffectPath(type: .Tabbar_randownCall)
            let url = ReverberationInsideHoeHandleReactiveCompatible.default.pullInConfirmType(type: .Tabbar_randownCall)
            //: let data = try Data(contentsOf: url)
            let data = try Data(contentsOf: url)
            //: let parser = SVGAParser.init()
            let parser = SVGAParser()
            //: parser.parse(with: data, cacheKey: "") { [weak self] videoItem in
            parser.parse(with: data, cacheKey: "") { [weak self] videoItem in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: player.videoItem = videoItem
                player.videoItem = videoItem
                //: player.startAnimation()
                player.startAnimation()
            }
            //: } catch {
        } catch {
            //: printLog(message: "get json error")
            printLog(message: (String(appToolFormat.prefix(6)) + String(mMakeUrl.prefix(4)) + "rror"))
        }

        //: return player
        return player
        //: }()
    }()
}

// MARK: - Event

//: extension TalkingTabBar {
extension LiveWillView {
    /// 刷新消息未读数角标
    //: func refreshBadgeLayoutWith(unread: Int, barType: TabBarItemType) {
    func skitter(unread: Int, barType: TypeNameConvertible) {
        //: let btn = self.viewWithTag(barType.rawValue + 100) as! UIButton
        let btn = self.viewWithTag(barType.rawValue + 100) as! UIButton
        //: let label = getBadgeLabel(btn: btn)
        let label = gain(btn: btn)
        //: if unread > 0 {
        if unread > 0 {
            //: label.isHidden = false
            label.isHidden = false
            //: if unread > 99 {
            if unread > 99 {
                //: label.text = "99+"
                label.text = "99+"
                //: } else {
            } else {
                //: label.text = "\(unread)"
                label.text = "\(unread)"
            }
            //: } else {
        } else {
            //: label.isHidden = true
            label.isHidden = true
            //: hideNewMessageUser()
            openUp()
        }
        //: var labWidth = 18.0
        var labWidth = 18.0
        //: if unread > 99 {
        if unread > 99 {
            //: labWidth = 30.0
            labWidth = 30.0
            //: } else if unread > 9 {
        } else if unread > 9 {
            //: labWidth = 23.0
            labWidth = 23.0
        }
        //: let delta = KDeviceIsIphoneX ? 5 : 2
        let delta = main_barData ? 5 : 2
        //: label.snp.remakeConstraints { make in
        label.snp.remakeConstraints { make in
            //: make.trailing.equalToSuperview().offset(-5)
            make.trailing.equalToSuperview().offset(-5)
            //: make.top.equalTo(delta)
            make.top.equalTo(delta)
            //: make.size.equalTo(CGSize(width: labWidth, height: 18.0))
            make.size.equalTo(CGSize(width: labWidth, height: 18.0))
        }
    }

    //: func getBadgeLabel(btn: UIButton) -> UILabel {
    func gain(btn: UIButton) -> UILabel {
        //: var badgeLabel = btn.viewWithTag(1000)
        var badgeLabel = btn.viewWithTag(1000)
        //: if badgeLabel == nil {
        if badgeLabel == nil {
            //: var tempLabel = UILabel.init()
            var tempLabel = UILabel()
            //: tempLabel = UILabel.init()
            tempLabel = UILabel()
            //: tempLabel.tag = 1000
            tempLabel.tag = 1000
            //: tempLabel.backgroundColor = UIColor.msgTipsColor()
            tempLabel.backgroundColor = UIColor.semblance()
            //: tempLabel.layer.cornerRadius = 9
            tempLabel.layer.cornerRadius = 9
            //: tempLabel.layer.borderWidth = 1
            tempLabel.layer.borderWidth = 1
            //: tempLabel.layer.borderColor = UIColor.white.cgColor
            tempLabel.layer.borderColor = UIColor.white.cgColor
            //: tempLabel.clipsToBounds = true
            tempLabel.clipsToBounds = true
            //: tempLabel.textColor = UIColor.white
            tempLabel.textColor = UIColor.white
            //: tempLabel.font = UIFont.pingfangMediumFont(fontSize: 13)
            tempLabel.font = UIFont.upperClassMove(fontSize: 13)
            //: tempLabel.isHidden = true
            tempLabel.isHidden = true
            //: tempLabel.textAlignment = .center
            tempLabel.textAlignment = .center

            //: btn.addSubview(tempLabel)
            btn.addSubview(tempLabel)
            //: badgeLabel = tempLabel
            badgeLabel = tempLabel
        }
        //: return badgeLabel as! UILabel
        return badgeLabel as! UILabel
    }

    /// 展示消息tab头像
    //: func showNewMessageUser(headPic: String) {
    func clip(headPic: String) {
        //: if currentTabType != TabBarItemType.Message.rawValue {
        if currentTabType != TypeNameConvertible.Message.rawValue {
            //: let btn = self.viewWithTag(TabBarItemType.Message.rawValue + 100) as? UIButton
            let btn = self.viewWithTag(TypeNameConvertible.Message.rawValue + 100) as? UIButton
            //: if btn != nil {
            if btn != nil {
                //: self.messageIcon.isHidden = false
                self.messageIcon.isHidden = false
                //: let optionsInfo = [KingfisherOptionsInfoItem.processor(RoundCornerImageProcessor(cornerRadius: 15))]
                let optionsInfo = [KingfisherOptionsInfoItem.processor(RoundCornerImageProcessor(cornerRadius: 15))]
                //: self.messageIcon.setUrlImage(urlStr: headPic, options: optionsInfo)
                self.messageIcon.sumercalate(urlStr: headPic, options: optionsInfo)
            }
        }
    }

    /// 隐藏消息tab头像
    //: func hideNewMessageUser() {
    func openUp() {
        //: let btn = self.viewWithTag(TabBarItemType.Message.rawValue + 100) as? UIButton
        let btn = self.viewWithTag(TypeNameConvertible.Message.rawValue + 100) as? UIButton
        //: if btn != nil {
        if btn != nil {
            //: self.messageIcon.isHidden = true
            self.messageIcon.isHidden = true
        }
    }

    /// 展示女性直播toast提示
    //: func showFemaleLiveTurnOnToast() {
    func album() {
        //: guard self.subviews.count > 0 else { return }
        guard self.subviews.count > 0 else { return }
        //: TalkingLiveTabToast().show(superView: self)
        WillView().reStartButton(superView: self)
    }

    /// 更改直播按钮图标
    //: func switchLiveTabBarImage() {
    func labelName() {
        //: guard PathAppManager.share.loginUserMode.sex == Gender.female.rawValue else { return }
        guard PathAppManager.share.loginUserMode.sex == ConnectionClusterLiteral.female.rawValue else { return }
        //: let liveBtn = self.viewWithTag(TabBarItemType.Live.rawValue + 100) as? UIButton
        let liveBtn = self.viewWithTag(TypeNameConvertible.Live.rawValue + 100) as? UIButton
        //: liveBtn?.isSelected = false
        liveBtn?.isSelected = false
        //: if TalkingLiveManager.shared().isLive ||
        if CommentLiveManager.leaveBy().isLive ||
            //: TalkingVoiceRoomManager.shared().isParty, String(TalkingVoiceRoomManager.shared().partyModel.streamerInfo.uid) == PathAppManager.share.loginUid {
            VoxNameReactiveCompatible.listOf().isParty, String(VoxNameReactiveCompatible.listOf().partyModel.streamerInfo.uid) == PathAppManager.share.loginUid
        {
            //: liveSvga.isHidden = false
            liveSvga.isHidden = false
            //: } else {
        } else {
            //: liveSvga.isHidden = true
            liveSvga.isHidden = true
        }
    }

    /// 更改Randow按钮图标
    //: func switchRandowTabBarImage(isHidde: Bool) {
    func bearAway(isHidde: Bool) {
        //: let liveBtn = self.viewWithTag(TabBarItemType.Randow.rawValue + 100) as? UIButton
        let liveBtn = self.viewWithTag(TypeNameConvertible.Randow.rawValue + 100) as? UIButton
        //: liveBtn?.isSelected = false
        liveBtn?.isSelected = false
        //: randowSvga.isHidden = !isHidde
        randowSvga.isHidden = !isHidde
    }
}
