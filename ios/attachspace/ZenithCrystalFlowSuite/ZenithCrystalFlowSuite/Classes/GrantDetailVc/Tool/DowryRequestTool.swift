
//: Declare String Begin

/*: "mf/moment/list" :*/
fileprivate let showEqualTitle:[Character] = ["m","f","/","m","o","m","e","n"]
fileprivate let show_topHeadText:String = "t/listview empty sh"

/*: "mf/moment/moments" :*/
fileprivate let user_titleData:String = "mf/molet count app model lab"
fileprivate let main_pageStr:String = "momentap"

/*: "mf/moment/like" :*/
fileprivate let k_atStr:String = "tip letmf/mom"
fileprivate let show_messageCancelUrl:String = "request"

/*: "mf/crush/send" :*/
fileprivate let dreamFeatureDataStr:String = "lab wrap right status typemf/cru"
fileprivate let m_takeTopMsg:String = "action model self indexsh/send"

/*: "mf/moment/view" :*/
fileprivate let kUpBeautyMessage:String = "false var slide stylemf/mo"
fileprivate let show_playerValue:[Character] = ["i","e","w"]

/*: "mf/momentReply/list" :*/
fileprivate let dreamCurrentAtName:String = "mf/mrequest model var selected"
fileprivate let k_sessionKey:String = "edge let view styletRepl"

/*: "mf/momentReply/save" :*/
fileprivate let app_cellFormat:String = "mf/momreturn app voice succeed for"
fileprivate let m_messagePath:String = "ly/saveself nor"

/*: "mf/moment/videoSign" :*/
fileprivate let app_viewKey:[Character] = ["m","f","/","m","o","m","e","n","t"]
fileprivate let noti_milkCellIdent:[Character] = ["/","v","i","d","e","o","S","i","g","n"]

/*: "dynamicPic.zip" :*/
fileprivate let show_levelMakeText:String = "dynpic"
fileprivate let appModeValue:String = "ic.zimode self first size"
fileprivate let notiEventKey:String = "view"

/*: "pic" :*/
fileprivate let main_pathData:[UInt8] = [0x5f,0x46,0x4c]

/*: "content" :*/
fileprivate let dreamTheUrl:String = "modelontent"

/*: "mf/moment/save" :*/
fileprivate let mainLabUrl:String = "mf/mreturn standard"
fileprivate let kSessionKey:String = "data"

/*: "Publish Successful, Go to My Dynamics to view" :*/
fileprivate let notiPlatTitle:[UInt8] = [0x6d,0x48,0x5f,0x51,0x54,0x4e,0x55,0x1d,0x6e,0x48,0x5e,0x5e,0x58,0x4e,0x4e,0x5b,0x48,0x51,0x11,0x1d,0x7a,0x52,0x1d,0x49,0x52,0x1d,0x70,0x44,0x1d,0x79,0x44,0x53,0x5c,0x50,0x54,0x5e,0x4e,0x1d,0x49,0x52,0x1d,0x4b,0x54,0x58,0x4a]

private func tempSelectTitle(button num: UInt8) -> UInt8 {
    return num ^ 61
}

/*: "mf/moment/saveVideo" :*/
fileprivate let m_byStr:[Character] = ["m","f","/","m","o","m","e","n","t","/","s","a","v","e","V","i","d"]
fileprivate let mainEqualModelMsg:String = "actualo"

/*: .png" :*/
fileprivate let dreamListViewPath:String = ".pngas mode case to current"

/*: "dynamicPic" :*/
fileprivate let userLabMsg:[Character] = ["d","y","n","a","m","i","c","P","i","c"]

/*: "freeImage" :*/
fileprivate let kNameFormat:String = "freeImapp cover view content"
fileprivate let userGiftImageData:String = "avideo"

/*: "mf/momentReply/replylist" :*/
fileprivate let kStartWhiteData:String = "string minimum newmf/mome"
fileprivate let showActualVoiceId:[Character] = ["n","t","R","e","p","l","y","/","r","e","p","l","y","l","i","s","t"]

/*: "mf/moment/delete" :*/
fileprivate let user_frameValue:String = "mf/momcell if background corner"
fileprivate let app_hiddenName:[Character] = ["e","n","t","/","d"]
fileprivate let notiImageMsg:String = "eleshadow"

/*: "mf/momentReply/delete" :*/
fileprivate let dream_equalFormat:String = "mf/mofor view return"
fileprivate let kCountervalName:String = "mentRcontainer make request below"
fileprivate let dream_makeIdent:String = "finishelete"

/*: "mf/moment/pin" :*/
fileprivate let dreamHiddenName:String = "case self selfmf/mo"

/*: "mf/moment/remindNum" :*/
fileprivate let mainReasonMsg:String = "mf/mcolor space make let view"
fileprivate let notiViewText:String = "/remindNumodel for else time"
fileprivate let userRequestValue:String = "M"

/*: "mf/moment/remindList" :*/
fileprivate let dreamPathIdent:String = "height of string cookiemf/mome"
fileprivate let dream_managerName:String = "name extrant/rem"

/*: "page" :*/
fileprivate let dream_objectMaleFormat:[UInt8] = [0x77,0x66,0x60,0x62]

/*: "cos/getCredential" :*/
fileprivate let kGroupFormat:[Character] = ["c","o","s","/","g","e","t","C","r"]
fileprivate let notiSizeText:String = "layen"

/*: "type" :*/
fileprivate let main_frameKey:String = "tnormalpe"

/*: "user/reportUserInfo" :*/
fileprivate let app_dataMsg:String = "array letuser/r"
fileprivate let appMoreText:String = "UserItext add first style mic"
fileprivate let k_failureText:[Character] = ["n","f","o"]

/*: "uid" :*/
fileprivate let dreamMinPath:[Character] = ["u","i","d"]

/*: "deviceId" :*/
fileprivate let showToMessage:String = "model right state returndeviceId"

/*: "deviceName" :*/
fileprivate let kLeavePathMessage:String = "withvic"
fileprivate let showStopLiveStr:String = "view"

/*: "iphone" :*/
fileprivate let app_listValue:[Character] = ["i","p","h","o","n","e"]

/*: "fileUrl" :*/
fileprivate let showDataKey:[Character] = ["f","i","l","e","U","r","l"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  DowryRequestTool.swift
//  AbroadTalking
//
//  Created by Charlotte on 2022/9/19.
//

//: import Adjust
import Adjust
//: import UIKit
import UIKit

//: class TalkingMomentRequestTool: NSObject {
class DowryRequestTool: NSObject {
    //: let _fm = FileManager.default
    let _fm = FileManager.default

    /// 动态列表接口
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_MomentList(params: [String: Any], completion: @escaping FinishBlock) {
    class func tillModel(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = PostulationReactiveCompatible()
        //: reqModel.requestPath = "mf/moment/list"
        reqModel.requestPath = (String(showEqualTitle) + String(show_topHeadText.prefix(6)))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        show_pointBottomPath.ingathering(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 用户动态接口
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_UserMomentList(params: [String: Any], completion: @escaping FinishBlock) {
    class func mortalThen(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = PostulationReactiveCompatible()
        //: reqModel.requestPath = "mf/moment/moments"
        reqModel.requestPath = (String(user_titleData.prefix(5)) + "ment/" + main_pageStr.replacingOccurrences(of: "tap", with: "ts"))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        show_pointBottomPath.ingathering(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 点赞
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_MomentLike(params: [String: Any], completion: @escaping FinishBlock) {
    class func releaseUp(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = PostulationReactiveCompatible()
        //: reqModel.requestPath = "mf/moment/like"
        reqModel.requestPath = (String(k_atStr.suffix(6)) + "ent/li" + show_messageCancelUrl.replacingOccurrences(of: "request", with: "ke"))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        show_pointBottomPath.ingathering(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 谁心动我 - 发送心动
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_CrushSend(params: [String: Any], completion: @escaping FinishBlock) {
    class func disappear(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = PostulationReactiveCompatible()
        //: reqModel.requestPath = "mf/crush/send"
        reqModel.requestPath = (String(dreamFeatureDataStr.suffix(6)) + String(m_takeTopMsg.suffix(7)))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        show_pointBottomPath.ingathering(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 动态详情
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_MomentDetail(params: [String: Any], completion: @escaping FinishBlock) {
    class func anList(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = PostulationReactiveCompatible()
        //: reqModel.requestPath = "mf/moment/view"
        reqModel.requestPath = (String(kUpBeautyMessage.suffix(5)) + "ment/v" + String(show_playerValue))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        show_pointBottomPath.ingathering(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 动态评论列表
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_CommentList(params: [String: Any], completion: @escaping FinishBlock) {
    class func loadWith(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = PostulationReactiveCompatible()
        //: reqModel.requestPath = "mf/momentReply/list"
        reqModel.requestPath = (String(dreamCurrentAtName.prefix(4)) + "omen" + String(k_sessionKey.suffix(5)) + "y/list")
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        show_pointBottomPath.ingathering(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 发表评论、回复
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_SendComment(params: [String: Any], completion: @escaping FinishBlock) {
    class func someVideo(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = PostulationReactiveCompatible()
        //: reqModel.requestPath = "mf/momentReply/save"
        reqModel.requestPath = (String(app_cellFormat.prefix(6)) + "entRep" + String(m_messagePath.prefix(7)))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        show_pointBottomPath.ingathering(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取视频上传sign
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_VideoSign(params: [String: Any], completion: @escaping FinishBlock) {
    class func giftCompletion(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = PostulationReactiveCompatible()
        //: reqModel.requestPath = "mf/moment/videoSign"
        reqModel.requestPath = (String(app_viewKey) + String(noti_milkCellIdent))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        show_pointBottomPath.ingathering(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 动态发布
    /// - Parameters:
    ///   - photos: 图片
    ///   - content: 内容
    ///   - completion: 回调
    //: func releasePhotosMoment(photos: [UIImage], content: String, completion: @escaping FinishBlock) {
    func lock(photos: [UIImage], content: String, completion: @escaping FinishBlock) {
        // 处理图片
        //: deleteFile()
        effectAnnouncement()
        //: for (index, photo) in photos.enumerated() {
        for (index, photo) in photos.enumerated() {
            //: getImagePath(index: index, image: photo)
            redirect(index: index, image: photo)
        }
        //: let documentDictPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        let documentDictPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        //: let doc_path = (documentDictPath as NSString).appendingPathComponent("dynamicPic.zip")
        let doc_path = (documentDictPath as NSString).appendingPathComponent((show_levelMakeText.replacingOccurrences(of: "pic", with: "a") + "micP" + String(appModeValue.prefix(5)) + notiEventKey.replacingOccurrences(of: "view", with: "p")))
        //: SSZipArchive.createZipFile(atPath: doc_path, withContentsOfDirectory: filePath)
        SSZipArchive.createZipFile(atPath: doc_path, withContentsOfDirectory: filePath)
        //: DispatchQueue.global().async {
        DispatchQueue.global().async {
            //: let data = NSData(contentsOfFile: doc_path)
            let data = NSData(contentsOfFile: doc_path)
            //: DispatchQueue.main.async {
            DispatchQueue.main.async {
                // 发布
                //: var params: [String: Any] = ["pic": data as Any]
                var params: [String: Any] = [String(bytes: main_pathData.map{$0^47}, encoding: .utf8)!: data as Any]
                //: if content.count > 0 {
                if content.count > 0 {
                    //: params["content"] = content
                    params[(dreamTheUrl.replacingOccurrences(of: "model", with: "c"))] = content
                }
                //: let reqModel = TalkingRequestModel()
                let reqModel = PostulationReactiveCompatible()
                //: reqModel.requestPath = "mf/moment/save"
                reqModel.requestPath = (String(mainLabUrl.prefix(4)) + "oment/sav" + kSessionKey.replacingOccurrences(of: "data", with: "e"))
                //: reqModel.requestType = .POST
                reqModel.requestType = .POST
                //: reqModel.params = params
                reqModel.params = params
                //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
                show_pointBottomPath.ingathering(model: reqModel) { succeed, result, errorModel in
                    //: if succeed {
                    if succeed {
                        //: self.func__showStatusBarSuccessMsg(showMsg: "Publish Successful, Go to My Dynamics to view".localized)
                        self.showMake(showMsg: String(bytes: notiPlatTitle.map{tempSelectTitle(button: $0)}, encoding: .utf8)!.localized)
                        //: self.deleteFile()
                        self.effectAnnouncement()
                    }
                    //: completion(succeed, result, errorModel)
                    completion(succeed, result, errorModel)
                }
            }
        }
    }

    /// 动态发布
    /// - Parameters:
    ///   - completion: 回调
    //: class func releaseVideoMoment(params: [String: Any], completion: @escaping FinishBlock) {
    class func lumbarVertebra(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = PostulationReactiveCompatible()
        //: reqModel.requestPath = "mf/moment/saveVideo"
        reqModel.requestPath = (String(m_byStr) + mainEqualModelMsg.replacingOccurrences(of: "actual", with: "e"))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        show_pointBottomPath.ingathering(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    //: private func getImagePath(index: Int, image: UIImage) {
    private func redirect(index: Int, image: UIImage) {
        //: let data = image.compressedImageData()
        let data = image.pause()
        //: let nameString = "\(index).png"
        let nameString = "\(index)" + (String(dreamListViewPath.prefix(4)))
        //: let picPath = (filePath as NSString).appendingPathComponent("dynamicPic")
        let picPath = (filePath as NSString).appendingPathComponent((String(userLabMsg)))
        //: if !_fm.fileExists(atPath: picPath) {
        if !_fm.fileExists(atPath: picPath) {
            //: try? _fm.createDirectory(atPath: picPath, withIntermediateDirectories: true)
            try? _fm.createDirectory(atPath: picPath, withIntermediateDirectories: true)
        }
        //: let imagePath = (picPath as NSString).appendingPathComponent(nameString)
        let imagePath = (picPath as NSString).appendingPathComponent(nameString)
        //: _fm.createFile(atPath: imagePath, contents: data)
        _fm.createFile(atPath: imagePath, contents: data)
    }

    //: private func deleteFile() {
    private func effectAnnouncement() {
        //: if _fm.fileExists(atPath: filePath) {
        if _fm.fileExists(atPath: filePath) {
            //: try? _fm.removeItem(atPath: filePath)
            try? _fm.removeItem(atPath: filePath)
        }
    }

    //: private lazy var filePath: String = {
    private lazy var filePath: String = {
        //: let documentDictPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        let documentDictPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        //: let imagePath = (documentDictPath as NSString).appendingPathComponent("freeImage")
        let imagePath = (documentDictPath as NSString).appendingPathComponent((String(kNameFormat.prefix(6)) + userGiftImageData.replacingOccurrences(of: "video", with: "ge")))
        //: return imagePath
        return imagePath
        //: }()
    }()

    /// 子评论列表
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_CommentDetailList(params: [String: Any], completion: @escaping FinishBlock) {
    class func digitizer(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = PostulationReactiveCompatible()
        //: reqModel.requestPath = "mf/momentReply/replylist"
        reqModel.requestPath = (String(kStartWhiteData.suffix(7)) + String(showActualVoiceId))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        show_pointBottomPath.ingathering(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 删除动态
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_DeleteMoment(params: [String: Any], completion: @escaping FinishBlock) {
    class func eraseConstraint(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = PostulationReactiveCompatible()
        //: reqModel.requestPath = "mf/moment/delete"
        reqModel.requestPath = (String(user_frameValue.prefix(6)) + String(app_hiddenName) + notiImageMsg.replacingOccurrences(of: "shadow", with: "te"))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        show_pointBottomPath.ingathering(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 删除评论
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_DeleteMomentReply(params: [String: Any], completion: @escaping FinishBlock) {
    class func relation(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = PostulationReactiveCompatible()
        //: reqModel.requestPath = "mf/momentReply/delete"
        reqModel.requestPath = (String(dream_equalFormat.prefix(5)) + String(kCountervalName.prefix(5)) + "eply/" + dream_makeIdent.replacingOccurrences(of: "finish", with: "d"))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        show_pointBottomPath.ingathering(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 置顶操作
    /// - Parameters:
    /// status=1表示置顶，0=取消置顶
    ///   - completion: 回调
    //: class func req_TopMoment(params: [String: Any], completion: @escaping FinishBlock) {
    class func off(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = PostulationReactiveCompatible()
        //: reqModel.requestPath = "mf/moment/pin"
        reqModel.requestPath = (String(dreamHiddenName.suffix(5)) + "ment/pin")
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        show_pointBottomPath.ingathering(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取用户未读动态总数
    /// - Parameter completion: 回调
    //: class func req_getRemindNum(completion: @escaping FinishBlock) {
    class func transition(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = PostulationReactiveCompatible()
        //: reqModel.requestPath = "mf/moment/remindNum"
        reqModel.requestPath = (String(mainReasonMsg.prefix(4)) + "oment" + String(notiViewText.prefix(9)) + userRequestValue.lowercased())
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        show_pointBottomPath.ingathering(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取当前未读动态信息列表
    /// - Parameters:
    ///   - page: 分页
    ///   - completion: 回调
    //: class func req_getRemindList(page: Int, completion: @escaping (_ data: [TalkingRemindListModel]) -> Void) {
    class func sizeValue(page: Int, completion: @escaping (_ data: [StreetSmartModelType]) -> Void) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = PostulationReactiveCompatible()
        //: reqModel.requestPath = "mf/moment/remindList"
        reqModel.requestPath = (String(dreamPathIdent.suffix(7)) + String(dream_managerName.suffix(6)) + "indList")
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: reqModel.params = ["page": page]
        reqModel.params = [String(bytes: dream_objectMaleFormat.map{$0^7}, encoding: .utf8)!: page]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        show_pointBottomPath.ingathering(model: reqModel) { _, result, _ in
            //: var dataArr: [TalkingRemindListModel] = []
            var dataArr: [StreetSmartModelType] = []
            //: guard let arr = result as? [[String: Any]] else {
            guard let arr = result as? [[String: Any]] else {
                //: completion(dataArr)
                completion(dataArr)
                //: return
                return
            }
            //: if let datas = Array<TalkingRemindListModel>.deserialize(from: arr as? Array) as? [TalkingRemindListModel] {
            if let datas = Array<StreetSmartModelType>.deserialize(from: arr as? Array) as? [StreetSmartModelType] {
                //: dataArr.append(contentsOf: datas)
                dataArr.append(contentsOf: datas)
            }
            //: completion(dataArr)
            completion(dataArr)
        }
    }

    /// COS  视频存储
    /// - Parameters:
    ///   - type: 1=动态，2=视频封面，3=私信视频
    ///   - completion: 回调
    //: class func req_getCredential(type: Int, completion: @escaping FinishBlock) {
    class func beautyEqualCompletion(type: Int, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = PostulationReactiveCompatible()
        //: reqModel.requestPath = "cos/getCredential"
        reqModel.requestPath = (String(kGroupFormat) + notiSizeText.replacingOccurrences(of: "lay", with: "ed") + "tial")
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: reqModel.params = ["type": type]
        reqModel.params = [(main_frameKey.replacingOccurrences(of: "normal", with: "y")): type]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        show_pointBottomPath.ingathering(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 上传cos日志存储路径
    /// - Parameters:
    ///   - fileUrl: 日志路径
    ///   - completion: 回调
    //: class func req_uploadLogPath(fileUrl: String, completion: @escaping FinishBlock) {
    class func cancelDismiss(fileUrl: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = PostulationReactiveCompatible()
        //: reqModel.requestServer = PathAppManager.share.appConfigMode.reportDomain
        reqModel.requestServer = PathAppManager.share.appConfigMode.reportDomain
        //: reqModel.requestPath = "user/reportUserInfo"
        reqModel.requestPath = (String(app_dataMsg.suffix(6)) + "eport" + String(appMoreText.prefix(5)) + String(k_failureText))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: var params = [String: Any]()
        var params = [String: Any]()
        //: params["uid"] = PathAppManager.share.loginUserMode.userID
        params[(String(dreamMinPath))] = PathAppManager.share.loginUserMode.userID
        //: params["deviceId"] = Adjust.adid() ?? ""
        params[(String(showToMessage.suffix(8)))] = Adjust.adid() ?? ""
        //: params["deviceName"] = "iphone"
        params[(kLeavePathMessage.replacingOccurrences(of: "with", with: "de") + "eNam" + showStopLiveStr.replacingOccurrences(of: "view", with: "e"))] = (String(app_listValue))
        //: params["type"] = 1
        params[(main_frameKey.replacingOccurrences(of: "normal", with: "y"))] = 1
        //: params["fileUrl"] = fileUrl
        params[(String(showDataKey))] = fileUrl
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        show_pointBottomPath.ingathering(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }
}
