
//: Declare String Begin

/*: "http://" :*/
fileprivate let m_containerFormat:[Character] = ["h","t","t","p",":","/","/"]

/*: "https://" :*/
fileprivate let showNameMessage:String = "HTTP"

/*: "http:// :*/
fileprivate let userSnapMsg:[Character] = ["h","t"]
fileprivate let dream_normalFormat:[Character] = ["t","p",":","/","/"]

/*: "serverSelctIndex =  :*/
fileprivate let showManagerFormatDataKey:String = "segenderv"
fileprivate let appPriceFormat:String = "dex = share self count"

/*: "serverUrlStr =  :*/
fileprivate let k_okTitle:String = "torve"
fileprivate let mainZoneFormat:String = "model range addrUrlSt"

/*: "app. :*/
fileprivate let noti_currentEmptyTransformFormat:String = "view extension string height contentapp."

/*: .com" :*/
fileprivate let userColorData:String = ".comleft private button in"

/*: "IM/getMyBlackList" :*/
fileprivate let k_shouldNameData:String = "every modelIM/get"
fileprivate let user_valueId:[Character] = ["M","y","B"]
fileprivate let show_viewId:String = "lacimage"

/*: "mf/business/sendMsg" :*/
fileprivate let dream_languageStr:String = "mf/busmake sum color self"
fileprivate let notiEndAngleTitle:String = "to collection error namesendM"
fileprivate let dreamNameFormat:String = "SG"

/*: "mf/business/sendGift" :*/
fileprivate let notiBindIdent:[Character] = ["m","f","/","b","u","s","i","n"]
fileprivate let notiInfoValue:String = "user else result labess/"

/*: "mf/chatRoom/sendMsg" :*/
fileprivate let dreamIconData:String = "mf/chareturn tap self"
fileprivate let mainFrameDoingIdent:[Character] = ["n","d","M","s","g"]

/*: "mf/chatRoom/sendGift" :*/
fileprivate let notiValueClickRecommendUrl:[Character] = ["m","f","/","c","h","a","t","R","o","o","m","/","s","e","n","d","G"]
fileprivate let appIndexTitle:[Character] = ["i","f","t"]

/*: "recharge/applePayNotify" :*/
fileprivate let kPathStr:[Character] = ["r","e","c","h","a","r","g","e","/","a","p","p"]
fileprivate let kMediumStr:String = "lePayNdata return input type"
fileprivate let dreamLeadingStr:String = "otsharedfy"

/*: "user/appleLogin" :*/
fileprivate let app_commitFormat:String = "user/apequal normal let view view"
fileprivate let notiImageTitle:String = "manager leading extrapleLogin"

/*: "en" :*/
fileprivate let mViewPath:[Character] = ["e","n"]

/*: "ar" :*/
fileprivate let noti_dataAId:String = "aaccept"

/*: "es" :*/
fileprivate let kObjectStr:String = "eleading"

/*: "pt" :*/
fileprivate let userMicMessage:[Character] = ["p","t"]

/*: "GJ21FB" :*/
fileprivate let notiLeaveData:String = "GJ2bindFB"

/*: "完成了检测,请求状态正常" :*/
fileprivate let dreamTableData:[Character] = ["完","成","\u{4e86}","检","测",",","\u{8bf7}","求","状","态","正","常"]

/*: "接口请求失败" :*/
fileprivate let userViewInputPath:String = "\u{63a5}\u{53e3}请求\u{5931}败"

/*: "完成了检测,需要更换server url" :*/
fileprivate let mainManagerName:String = "\u{5b8c}成了检测"
fileprivate let app_skipKey:String = "rver urlif save"

/*: "HTTPProxy" :*/
fileprivate let notiTitleSizePath:String = "HTTflowflow"
fileprivate let dreamModelIdent:String = "roaspecty"

/*: "__SCOPED__" :*/
fileprivate let notiPresentFormat:String = "__SCOPEend voice image at else"
fileprivate let mainDetailClearMsg:[Character] = ["D","_","_"]

/*: "tap" :*/
fileprivate let app_pathUrl:String = "tto"

/*: "tun" :*/
fileprivate let dream_fromMessage:String = "TUN"

/*: "ipsec" :*/
fileprivate let show_labData:String = "IPSEC"

/*: "ppp" :*/
fileprivate let dream_statusMessage:String = "layerlayerlayer"

/*: "func__updateTalkingCookies =  :*/
fileprivate let mainTransitionSucceedData:String = "video whitefunc__"
fileprivate let showLabelWrapId:String = "teTabar direction resume back"
fileprivate let appProfileModelIdent:String = "image to selflkingC"
fileprivate let notiNameIdent:[Character] = ["e","s"," ","="," "]

/*: "https:// :*/
fileprivate let dream_sendValue:String = "hquotep"

/*: "func__deleteSameCookiesWithUrl =  :*/
fileprivate let dream_rawPath:[UInt8] = [0xba,0xc9,0xc2,0xb7,0xb3,0xb3,0xb8,0xb9,0xc0,0xb9,0xc8,0xb9,0xa7,0xb5,0xc1,0xb9,0x97,0xc3,0xc3,0xbf,0xbd,0xb9,0xc7,0xab,0xbd,0xc8,0xbc,0xa9,0xc6,0xc0,0x74,0x91,0x74]

fileprivate func nameContainer(head num: UInt8) -> UInt8 {
    let value = Int(num) + 172
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "UID" :*/
fileprivate let appFirstTapMsg:[Character] = ["U","I","D"]

/*: "PHPSESSID" :*/
fileprivate let mainNameViewLetStr:String = "PHPSESSIvalue"

/*: "func__saveAllCookies =  :*/
fileprivate let mainPopFormat:String = "func__scell view"
fileprivate let dream_textFormat:String = "view nextaveAl"
fileprivate let main_sizeData:[Character] = ["i","e","s"," ","="," "]

/*: "func__saveAllCookies  error" :*/
fileprivate let kMakeData:[Character] = ["f","u","n","c","_","_","s"]
fileprivate let noti_pairName:String = "bar versionaveAl"
fileprivate let app_nameFormat:String = "ies  all player pop"

/*: "🍪 删除 :*/
fileprivate let main_labelName:[Character] = ["🍪"," ","删","除"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  LiveAddrThen.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/8/23.
//

//: import UIKit
import UIKit
//: import WebKit
import WebKit

//: enum LangType: String {
enum ViewTermConvertible: String {
    //: case en = "en"
    case en
    //: case ar = "ar"
    case ar
    //: case es = "es"
    case es
    //: case pt = "pt"
    case pt
}

//: public class TalkingRequestAddrTool: NSObject {
public class LiveAddrThen: NSObject {
    //: @objc static public let share = TalkingRequestAddrTool()
    @objc public static let share = LiveAddrThen()
    //: private override init() {
    override private init() {
        //: super.init()
        super.init()
        //: setUp()
        recess()
    }

    //: private var timerDispose: Disposable?
    private var timerDispose: Disposable?

    //: var serverSelctIndex = 0
    var serverSelctIndex = 0
    //: var hasFinishCheck   = false
    var hasFinishCheck = false
    //: var hostCheckNum     = 0
    var hostCheckNum = 0

    //: var domainStr: String?
    var domainStr: String? // 用户登录的信息
    //: var _serverUrlStr: String = ""       //
    var _serverUrlStr: String = "" //
    
    private let string = (String(dream_languageStr.prefix(6)) + "iness/" + String(notiEndAngleTitle.suffix(5)) + dreamNameFormat.lowercased())
    private let newStr = (String(notiBindIdent) + String(notiInfoValue.suffix(4)) + "sendGift")
    private let roomStr = (String(dreamIconData.prefix(6)) + "tRoom/se" + String(mainFrameDoingIdent))
    private let goodStr = (String(notiValueClickRecommendUrl) + String(appIndexTitle))
    private let tStr = (String(kPathStr) + String(kMediumStr.prefix(6)) + dreamLeadingStr.replacingOccurrences(of: "shared", with: "i"))
    
    //: @objc public var serverUrlStr: String {
    @objc public var serverUrlStr: String {
        //: get {
        get {
            //: return _serverUrlStr
            return _serverUrlStr
        }
        //: set {
        set {
            //: var newServerUrl: String = ""
            var newServerUrl: String = ""
            //: if newValue.hasPrefix("http://") || newValue.hasPrefix("https://") {
            if newValue.hasPrefix((String(m_containerFormat))) || newValue.hasPrefix((showNameMessage.lowercased() + "s://")) {
                //: newServerUrl = newValue
                newServerUrl = newValue
                //: } else {
            } else {
                //: if Environment_Formal && !Encryption_Request {
                if app_warnKey && !mainHomePath {
                    //: newServerUrl = "http://\(newValue )"
                    newServerUrl = (String(userSnapMsg) + String(dream_normalFormat)) + "\(newValue)"
                    //: } else {
                } else {
                    //: newServerUrl = "http://\(newValue )"
                    newServerUrl = (String(userSnapMsg) + String(dream_normalFormat)) + "\(newValue)"
                }
            }
            //: if NSURL.init(string: newServerUrl) == nil {
            if NSURL(string: newServerUrl) == nil {
                //: return
                return
            }
            //: let oldServerUrl: String = _serverUrlStr
            let oldServerUrl: String = _serverUrlStr

            //: if oldServerUrl.isEmptyString || oldServerUrl != newServerUrl {
            if oldServerUrl.isEmptyString || oldServerUrl != newServerUrl {
                //: _serverUrlStr = newServerUrl
                _serverUrlStr = newServerUrl
                //: if oldServerUrl.count > 0 {
                if oldServerUrl.count > 0 {
                    //: let oldCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL.init(string: oldServerUrl)!)!
                    let oldCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL(string: oldServerUrl)!)!
                    //: func__updateTalkingCookies(cookies: oldCookies)
                    performData(cookies: oldCookies)
                }
            }
        }
    }

    //: func setUp() {
    func recess() {
        //: serverSelctIndex = Defaults.integer(forKey: TalkingAddrServerUrlSelectIndexKey)
        serverSelctIndex = dreamBottomMsg.integer(forKey: k_errPathKey)
        //: printLog(message: "serverSelctIndex = \(serverSelctIndex)")
        printLog(message: (showManagerFormatDataKey.replacingOccurrences(of: "gender", with: "r") + "erSelctIn" + String(appPriceFormat.prefix(6))) + "\(serverSelctIndex)")
        //: let serverDomain = func__getInitialServerUrl()
        let serverDomain = referUrl()
        //: var serverPrefix = "http://"
        var serverPrefix = (String(m_containerFormat))
        //: if Environment_Formal && !Encryption_Request {
        if app_warnKey && !mainHomePath {
            //: serverPrefix = "http://"
            serverPrefix = (String(m_containerFormat))
        }
        //: serverUrlStr = serverPrefix.appending(serverDomain)
        serverUrlStr = serverPrefix.appending(serverDomain)
        //: printLog(message: "serverUrlStr = \(serverUrlStr)")
        printLog(message: (k_okTitle.replacingOccurrences(of: "to", with: "se") + String(mainZoneFormat.suffix(6)) + "r = ") + "\(serverUrlStr)")
    }

    //: func func__getInitialServerUrl() -> String {
    func referUrl() -> String {
        //: var appServerUrl = ""
        var appServerUrl = ""
        //: appServerUrl = serverUrlArr[serverSelctIndex]
        appServerUrl = serverUrlArr[serverSelctIndex]
        //: return appServerUrl
        return appServerUrl
    }

    //: func func__saveNewTalkingServerUrl(serverStr: String) {
    func imageEnter(serverStr: String) {
        //: serverUrlStr = serverStr
        serverUrlStr = serverStr
    }

    //: private lazy var serverUrlArr: Array = {
    private lazy var serverUrlArr: Array = //: return ["app.\(ReplaceUrlDomain).com", "app.\(ReplaceUrlDomain).com"]
        [(String(noti_currentEmptyTransformFormat.suffix(4))) + "\(dreamRecordPath)" + (String(userColorData.prefix(4))), (String(noti_currentEmptyTransformFormat.suffix(4))) + "\(dreamRecordPath)" + (String(userColorData.prefix(4)))]
    //: }()

    //: lazy var slowPathArr: Array = {
    lazy var slowPathArr: Array = //: return ["IM/getMyBlackList",
        [(String(k_shouldNameData.suffix(6)) + String(user_valueId) + show_viewId.replacingOccurrences(of: "image", with: "k") + "List"),
         //: "mf/business/sendMsg",
         string,
         //: "mf/business/sendGift",
         newStr,
         //: "mf/chatRoom/sendMsg",
         roomStr,
         //: "mf/chatRoom/sendGift",
         goodStr,
         //: "recharge/applePayNotify",
         tStr,
         //: "user/appleLogin"]
         (String(app_commitFormat.prefix(7)) + String(notiImageTitle.suffix(8)))]
    //: }()

    /// 获取接口语言
    //: lazy var interfaceLang: String = {
    lazy var interfaceLang: String = {
        //: let lang = FacultyThen.getSystemLangCode()
        let lang = FacultyThen.underListSend()
        //: if ["en", "ar", "es", "pt"].contains(lang) {
        if ["en", "ar", "es", "pt"].contains(lang) {
            //: return lang
            return lang
        }
        //: return "en"
        return "en"
        //: }()
    }()

    //: lazy var randomStr: String = {
    lazy var randomStr: String = //: return String.randomString(length: 16)
        .buttonLength(length: 16)
    //: }()

    //: lazy var encryKeyStr: String = {
    lazy var encryKeyStr: String = {
        //: let sufStr = randomStr.suffix(6)
        let sufStr = randomStr.suffix(6)
        //: let reversedStr: String = String(sufStr.reversed())
        let reversedStr = String(sufStr.reversed())
        //: return "GJ21FB" + reversedStr
        return (notiLeaveData.replacingOccurrences(of: "bind", with: "1")) + reversedStr
        //: }()
    }()

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }
}

//: extension TalkingRequestAddrTool {
extension LiveAddrThen {
    //: func checkServerUrl() {
    func popEvent() {
        //: if PathAppManager.share.networkStatus != .Unavailable {
        if PathAppManager.share.networkStatus != .Unavailable {
            //: func_requestHost()
            publish()
            //: } else {
        } else {
            //: func_starCodeTime()
            dataSkip()
        }
    }

    //: func func_starCodeTime() {
    func dataSkip() {
        //: if self.hasFinishCheck {
        if self.hasFinishCheck {
            //: return
            return
        }
        //: if timerDispose != nil {
        if timerDispose != nil {
            //: timerDispose?.dispose()
            timerDispose?.dispose()
        }
        //: timerDispose = Observable<Int>.interval(.seconds(3), scheduler: MainScheduler.instance)
        timerDispose = Observable<Int>.interval(.seconds(3), scheduler: MainScheduler.instance)
            //: .subscribe(onNext: { [weak self] num in
            .subscribe(onNext: { [weak self] _ in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.func_requestHost()
                self.publish()
                //: })
            })
    }

    //: func func_endCodeTime() {
    func sendMoreTime() {
        //: timerDispose?.dispose()
        timerDispose?.dispose()
    }

    //: func func_requestHost() {
    func publish() {
        //: if PathAppManager.share.networkStatus == .Unavailable {
        if PathAppManager.share.networkStatus == .Unavailable {
            //: return
            return
        }
        //: func_endCodeTime()
        sendMoreTime()
        //: AppManagerRequest.func__initRequestHost { [weak self] succeed, result, errorModel in
        PlayerReactiveCompatible.indexFrom { [weak self] succeed, _, _ in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: if succeed {
            if succeed {
                //: self.hasFinishCheck = true
                self.hasFinishCheck = true
                //: printLog(message: "完成了检测,请求状态正常")
                printLog(message: (String(dreamTableData)))
                //: } else {
            } else {
                //: printLog(message: "接口请求失败")
                printLog(message: (userViewInputPath))
                //: if self.hasFinishCheck {return}
                if self.hasFinishCheck { return }
                //: self.hostCheckNum += 1
                self.hostCheckNum += 1
                //: if self.hostCheckNum < 3 {
                if self.hostCheckNum < 3 {
                    //: self.func_starCodeTime()
                    self.dataSkip()
                    //: } else {
                } else {
                    //: printLog(message: "完成了检测,需要更换server url")
                    printLog(message: (mainManagerName + ",\u{9700}要更换se" + String(app_skipKey.prefix(8))))
                    //: self.func__changeHostUrl()
                    self.viewIcon()
                }
            }
        }
    }

    //: func func__changeHostUrl() {
    func viewIcon() {
        //: hostCheckNum = 0
        hostCheckNum = 0
        //: hasFinishCheck = true
        hasFinishCheck = true
        //: func_endCodeTime()
        sendMoreTime()

        //: serverSelctIndex = (serverSelctIndex == 0) ? 1 : 0
        serverSelctIndex = (serverSelctIndex == 0) ? 1 : 0
        //: Defaults.set(serverSelctIndex, forKey: TalkingAddrServerUrlSelectIndexKey)
        dreamBottomMsg.set(serverSelctIndex, forKey: k_errPathKey)
        //: let serverDomain = serverUrlArr[serverSelctIndex]
        let serverDomain = serverUrlArr[serverSelctIndex]
        //: var serverPrefix = "http://"
        var serverPrefix = (String(m_containerFormat))
        //: if Environment_Formal && !Encryption_Request {
        if app_warnKey, !mainHomePath {
            //: serverPrefix = "http://"
            serverPrefix = (String(m_containerFormat))
        }
        //: serverUrlStr = serverPrefix.appending(serverDomain)
        serverUrlStr = serverPrefix.appending(serverDomain)
    }
}

// MARK: - 判断用户设备是否开启VPN

//: extension TalkingRequestAddrTool {
public extension LiveAddrThen {
    /// 判断用户设备是否开启代理
    /// - Returns: false: 未开启  true: 开启
    //: public class func isUsedProxy() -> Bool {
    class func mix() -> Bool {
        //: guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        //: guard let dict = proxy as? [String: Any] else { return false }
        guard let dict = proxy as? [String: Any] else { return false }
        //: guard let HTTPProxy = dict["HTTPProxy"] as? String else { return false }
        guard let HTTPProxy = dict[(notiTitleSizePath.replacingOccurrences(of: "flow", with: "P") + dreamModelIdent.replacingOccurrences(of: "aspect", with: "x"))] as? String else { return false }
        //: if (HTTPProxy.count > 0) {
        if HTTPProxy.count > 0 {
            //: return true
            return true
        }
        //: return false
        return false
    }

    /// 判断用户设备是否开启代理VPN
    /// - Returns: false: 未开启  true: 开启
    //: public class func isUsedVPN() -> Bool {
    class func tight() -> Bool {
        //: guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        //: guard let dict = proxy as? [String: Any] else { return false }
        guard let dict = proxy as? [String: Any] else { return false }
        //: guard let scopedDic = dict["__SCOPED__"] as? [String: Any] else { return false }
        guard let scopedDic = dict[(String(notiPresentFormat.prefix(7)) + String(mainDetailClearMsg))] as? [String: Any] else { return false }
        //: for keyStr in scopedDic.keys {
        for keyStr in scopedDic.keys {
            //: if keyStr.contains("tap") || keyStr.contains("tun") || keyStr.contains("ipsec") || keyStr.contains("ppp"){
            if keyStr.contains((app_pathUrl.replacingOccurrences(of: "to", with: "ap"))) || keyStr.contains((dream_fromMessage.lowercased())) || keyStr.contains((show_labData.lowercased())) || keyStr.contains((dream_statusMessage.replacingOccurrences(of: "layer", with: "p"))) {
                //: return true
                return true
            }
        }
        //: return false
        return false
    }
}

//: extension TalkingRequestAddrTool {
extension LiveAddrThen {
    //: func func__updateTalkingCookies(cookies: Array<HTTPCookie>) {
    func performData(cookies: [HTTPCookie]) {
        //: printLog(message: "func__updateTalkingCookies = \(cookies)")
        printLog(message: (String(mainTransitionSucceedData.suffix(6)) + "upda" + String(showLabelWrapId.prefix(4)) + String(appProfileModelIdent.suffix(6)) + "ooki" + String(notiNameIdent)) + "\(cookies)")
        //: var tempCookies: Array<HTTPCookie> = cookies
        var tempCookies: [HTTPCookie] = cookies
        //: if cookies.count == 0 {
        if cookies.count == 0 {
            //: tempCookies = func__getCookiesFromOldServerUrl()
            tempCookies = taskCreate()
        }
        //: if tempCookies.count == 0 {
        if tempCookies.count == 0 {
            //: return
            return
        }
        //: var tempUrlArr: Array<URL> = []
        var tempUrlArr: [URL] = []

        //: if !self.serverUrlStr.isEmptyString {
        if !self.serverUrlStr.isEmptyString {
            //: tempUrlArr.append(URL.init(string: self.serverUrlStr)!)
            tempUrlArr.append(URL(string: self.serverUrlStr)!)
        }

        //: var urlDomain: String = PathAppManager.share.appConfigMode.urlH5Domain
        var urlDomain: String = PathAppManager.share.appConfigMode.urlH5Domain
        //: urlDomain = urlDomain.replacingOccurrences(of: "http://", with: "https://")
        urlDomain = urlDomain.replacingOccurrences(of: (String(m_containerFormat)), with: (showNameMessage.lowercased() + "s://"))
        //: if urlDomain.hasPrefix("https://") {
        if urlDomain.hasPrefix((showNameMessage.lowercased() + "s://")) {
            //: tempUrlArr.append(URL.init(string: urlDomain)!)
            tempUrlArr.append(URL(string: urlDomain)!)
            //: } else {
        } else {
            //: tempUrlArr.append(URL.init(string: "https://\(urlDomain)")!)
            tempUrlArr.append(URL(string: (dream_sendValue.replacingOccurrences(of: "quote", with: "tt") + "s://") + "\(urlDomain)")!)
        }

        //: var addCookieArr: Array<HTTPCookie> = []
        var addCookieArr: [HTTPCookie] = []
        //: for cookies in tempCookies {
        for cookies in tempCookies {
            //: var cookieInfo: [HTTPCookiePropertyKey: Any] = cookies.properties!
            var cookieInfo: [HTTPCookiePropertyKey: Any] = cookies.properties!
            //: for url in tempUrlArr {
            for url in tempUrlArr {
                //: cookieInfo[HTTPCookiePropertyKey.domain] = url.host
                cookieInfo[HTTPCookiePropertyKey.domain] = url.host
                //: cookieInfo.removeValue(forKey: HTTPCookiePropertyKey.discard)
                cookieInfo.removeValue(forKey: HTTPCookiePropertyKey.discard)
                //: let newCookie = HTTPCookie.init(properties: cookieInfo)
                let newCookie = HTTPCookie(properties: cookieInfo)
                //: if newCookie != nil {
                if newCookie != nil {
                    //: addCookieArr.append(newCookie!)
                    addCookieArr.append(newCookie!)
                }
            }
        }
        //: for cookies in addCookieArr {
        for cookies in addCookieArr {
            //: HTTPCookieStorage.shared.setCookie(cookies)
            HTTPCookieStorage.shared.setCookie(cookies)
        }

        //: for url in tempUrlArr {
        for url in tempUrlArr {
            //: func__deleteSameCookiesWithUrl(url: url)
            streetwiseDetail(url: url)
        }
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: WKWebsiteDataStore.default().fetchDataRecords(ofTypes: [WKWebsiteDataTypeCookies]) { _ in
            WKWebsiteDataStore.default().fetchDataRecords(ofTypes: [WKWebsiteDataTypeCookies]) { _ in
            }
        }

        //: func__saveAllCookies()
        self.cookies()
    }

    //: func func__deleteSameCookiesWithUrl(url: URL) {
    func streetwiseDetail(url: URL) {
        //: let cookiesArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies(for: url)!
        let cookiesArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: url)!
        //: var saveDic: Dictionary<String, String> = [:]
        var saveDic: [String: String] = [:]
        //: for cookies in cookiesArr {
        for cookies in cookiesArr {
            //: if !cookies.name.isEmptyString {
            if !cookies.name.isEmptyString {
                //: let saveKey = cookies.name
                let saveKey = cookies.name
                //: if saveDic.keys.contains(saveKey) {
                if saveDic.keys.contains(saveKey) {
                    //: printLog(message: "func__deleteSameCookiesWithUrl = \(cookies.name)")
                    printLog(message: String(bytes: dream_rawPath.map{nameContainer(head: $0)}, encoding: .utf8)! + "\(cookies.name)")
                    //: HTTPCookieStorage.shared.deleteCookie(cookies)
                    HTTPCookieStorage.shared.deleteCookie(cookies)
                    //: } else {
                } else {
                    //: saveDic[saveKey] = "1"
                    saveDic[saveKey] = "1"
                }
            }
        }
    }

    //: func func__getCookiesFromOldServerUrl() -> Array<HTTPCookie> {
    func taskCreate() -> [HTTPCookie] {
        //: var serverPrefix = "http://"
        var serverPrefix = (String(m_containerFormat))
        //: if Environment_Formal && !Encryption_Request {
        if app_warnKey, !mainHomePath {
            //: serverPrefix = "https://"
            serverPrefix = (showNameMessage.lowercased() + "s://")
        }
        //: let fullDomain = serverPrefix.appending("app.\(ReplaceUrlDomain).com")
        let fullDomain = serverPrefix.appending((String(noti_currentEmptyTransformFormat.suffix(4))) + "\(dreamRecordPath)" + (String(userColorData.prefix(4))))
        //: let oldCookies = HTTPCookieStorage.shared.cookies(for: URL.init(string: fullDomain)!)
        let oldCookies = HTTPCookieStorage.shared.cookies(for: URL(string: fullDomain)!)
        //: return oldCookies!
        return oldCookies!
    }

    //: @objc func cookieChanged(note: Notification) {
    @objc func changedScriptBack(note _: Notification) {
        //: if PathAppManager.share.loginUid.isEmptyString {
        if PathAppManager.share.loginUid.isEmptyString {
            //: return
            return
        }
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: checkHtmlCookieIfNeedCurrentVersion()
            account()
            //: } else {
        } else {
            //: checkHtmlCookieIfNeedEarlyVersion()
            belowStatus()
        }
    }

    //: func checkHtmlCookieIfNeedCurrentVersion() {
    func account() {
        //: var htmlDomain: String = PathAppManager.share.appConfigMode.urlH5Domain
        var htmlDomain: String = PathAppManager.share.appConfigMode.urlH5Domain

        //: if !htmlDomain.isEmptyString {
        if !htmlDomain.isEmptyString {
            //: let url = URL.init(string: htmlDomain)
            let url = URL(string: htmlDomain)
            //: htmlDomain = url?.host ?? ""
            htmlDomain = url?.host ?? ""
            //: let cookieArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
            let cookieArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
            //: var appCookie: Dictionary<String, HTTPCookie> =  Dictionary()
            var appCookie: [String: HTTPCookie] = Dictionary()
            //: var h5Cookie: Dictionary<String, HTTPCookie> =  Dictionary()
            var h5Cookie: [String: HTTPCookie] = Dictionary()

            //: for cookie in cookieArr {
            for cookie in cookieArr {
                //: let tempServerUrlStr = self.serverUrlStr
                let tempServerUrlStr = self.serverUrlStr
                //: let temphtmlDomainStr = htmlDomain
                let temphtmlDomainStr = htmlDomain
                //: if tempServerUrlStr.contains(cookie.domain) {
                if tempServerUrlStr.contains(cookie.domain) {
                    //: if cookie.name.uppercased() == "UID" {
                    if cookie.name.uppercased() == (String(appFirstTapMsg)) {
                        //: appCookie["UID"] = cookie
                        appCookie[(String(appFirstTapMsg))] = cookie
                    }
                    //: if cookie.name.uppercased() == "PHPSESSID" {
                    if cookie.name.uppercased() == (mainNameViewLetStr.replacingOccurrences(of: "value", with: "D")) {
                        //: appCookie["PHPSESSID"] = cookie
                        appCookie[(mainNameViewLetStr.replacingOccurrences(of: "value", with: "D"))] = cookie
                    }

                    //: } else if temphtmlDomainStr.contains(cookie.domain) {
                } else if temphtmlDomainStr.contains(cookie.domain) {
                    //: if cookie.name.uppercased() == "UID" {
                    if cookie.name.uppercased() == (String(appFirstTapMsg)) {
                        //: h5Cookie["UID"] = cookie
                        h5Cookie[(String(appFirstTapMsg))] = cookie
                    }
                    //: if cookie.name.uppercased() == "PHPSESSID" {
                    if cookie.name.uppercased() == (mainNameViewLetStr.replacingOccurrences(of: "value", with: "D")) {
                        //: h5Cookie["PHPSESSID"] = cookie
                        h5Cookie[(mainNameViewLetStr.replacingOccurrences(of: "value", with: "D"))] = cookie
                    }
                }
                //: var cookieEqual = true
                var cookieEqual = true
                //: if appCookie.keys.count != h5Cookie.keys.count {
                if appCookie.keys.count != h5Cookie.keys.count {
                    //: cookieEqual = false
                    cookieEqual = false
                    //: } else {
                } else {
                    //: if appCookie.keys.contains("UID") && h5Cookie.keys.contains("UID") {
                    if appCookie.keys.contains((String(appFirstTapMsg))), h5Cookie.keys.contains((String(appFirstTapMsg))) {
                        //: let appUid: HTTPCookie = appCookie["UID"]!
                        let appUid: HTTPCookie = appCookie[(String(appFirstTapMsg))]!
                        //: let h5Uid: HTTPCookie = h5Cookie["UID"]!
                        let h5Uid: HTTPCookie = h5Cookie[(String(appFirstTapMsg))]!

                        //: if h5Uid.value != appUid.value {
                        if h5Uid.value != appUid.value {
                            //: cookieEqual = false
                            cookieEqual = false
                        }
                    }
                    //: if appCookie.keys.contains("PHPSESSID") && h5Cookie.keys.contains("PHPSESSID") {
                    if appCookie.keys.contains((mainNameViewLetStr.replacingOccurrences(of: "value", with: "D"))), h5Cookie.keys.contains((mainNameViewLetStr.replacingOccurrences(of: "value", with: "D"))) {
                        //: let appPHPSESSID: HTTPCookie = appCookie["PHPSESSID"]!
                        let appPHPSESSID: HTTPCookie = appCookie[(mainNameViewLetStr.replacingOccurrences(of: "value", with: "D"))]!
                        //: let h5PHPSESSID: HTTPCookie = h5Cookie["PHPSESSID"]!
                        let h5PHPSESSID: HTTPCookie = h5Cookie[(mainNameViewLetStr.replacingOccurrences(of: "value", with: "D"))]!
                        //: if appPHPSESSID.value != h5PHPSESSID.value {
                        if appPHPSESSID.value != h5PHPSESSID.value {
                            //: cookieEqual = false
                            cookieEqual = false
                        }
                    }
                }
                //: if !cookieEqual {
                if !cookieEqual {
                    //: for (key, _) in appCookie {
                    for (key, _) in appCookie {
                        //: let cookie: HTTPCookie = appCookie[key]!
                        let cookie: HTTPCookie = appCookie[key]!
                        //: var cookiePro: Dictionary<HTTPCookiePropertyKey, Any> = cookie.properties!
                        var cookiePro: [HTTPCookiePropertyKey: Any] = cookie.properties!
                        //: cookiePro[HTTPCookiePropertyKey.domain] = htmlDomain
                        cookiePro[HTTPCookiePropertyKey.domain] = htmlDomain
                        //: cookiePro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                        cookiePro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                        //: HTTPCookieStorage.shared.setCookie(HTTPCookie.init(properties: cookiePro)!)
                        HTTPCookieStorage.shared.setCookie(HTTPCookie(properties: cookiePro)!)
                    }
                }
            }
        }
    }

    //: func checkHtmlCookieIfNeedEarlyVersion() {
    func belowStatus() {
        //: let htmlDomain: String = PathAppManager.share.appConfigMode.urlH5Domain
        let htmlDomain: String = PathAppManager.share.appConfigMode.urlH5Domain
        //: let url: URL = URL.init(string: htmlDomain)!
        let url = URL(string: htmlDomain)!
        //: let cookieArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies(for: url)!
        let cookieArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: url)!
        //: var hasUID = false
        var hasUID = false
        //: var hasPHPSSID = false
        var hasPHPSSID = false
        //: for cookie in cookieArr {
        for cookie in cookieArr {
            //: if cookie.name == "UID" && cookie.value == PathAppManager.share.loginUserMode.userID {
            if cookie.name == (String(appFirstTapMsg)), cookie.value == PathAppManager.share.loginUserMode.userID {
                //: hasUID = true
                hasUID = true
            }
            //: if cookie.name == "PHPSESSID" {
            if cookie.name == (mainNameViewLetStr.replacingOccurrences(of: "value", with: "D")) {
                //: hasPHPSSID = true
                hasPHPSSID = true
            }
        }
        //: if !hasUID || !hasPHPSSID {
        if !hasUID || !hasPHPSSID {
            //: let cookieArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
            let cookieArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
            //: var appCookie: Dictionary<String, Any> =  Dictionary()
            var appCookie: [String: Any] = Dictionary()
            //: for cookie in cookieArr {
            for cookie in cookieArr {
                //: let tempServerUrlStr = self.serverUrlStr
                let tempServerUrlStr = self.serverUrlStr

                //: if tempServerUrlStr.contains(cookie.domain) {
                if tempServerUrlStr.contains(cookie.domain) {
                    //: if cookie.name.uppercased() == "UID" {
                    if cookie.name.uppercased() == (String(appFirstTapMsg)) {
                        //: appCookie["UID"] = cookie
                        appCookie[(String(appFirstTapMsg))] = cookie
                    }
                    //: if cookie.name.uppercased() == "PHPSESSID" {
                    if cookie.name.uppercased() == (mainNameViewLetStr.replacingOccurrences(of: "value", with: "D")) {
                        //: appCookie["PHPSESSID"] = cookie
                        appCookie[(mainNameViewLetStr.replacingOccurrences(of: "value", with: "D"))] = cookie
                    }
                }
                //: if !hasUID && appCookie.keys.contains("UID") {
                if !hasUID, appCookie.keys.contains((String(appFirstTapMsg))) {
                    //: let uidCookie: HTTPCookie = appCookie["UID"] as! HTTPCookie
                    let uidCookie: HTTPCookie = appCookie[(String(appFirstTapMsg))] as! HTTPCookie
                    //: var uidPro: Dictionary<HTTPCookiePropertyKey, Any> = uidCookie.properties!
                    var uidPro: [HTTPCookiePropertyKey: Any] = uidCookie.properties!
                    //: uidPro[HTTPCookiePropertyKey.domain] = url.host
                    uidPro[HTTPCookiePropertyKey.domain] = url.host
                    //: uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    //: HTTPCookieStorage.shared.setCookie(HTTPCookie.init(properties: uidPro)!)
                    HTTPCookieStorage.shared.setCookie(HTTPCookie(properties: uidPro)!)
                }
                //: if !hasPHPSSID && appCookie.keys.contains("PHPSESSID") {
                if !hasPHPSSID, appCookie.keys.contains((mainNameViewLetStr.replacingOccurrences(of: "value", with: "D"))) {
                    //: let uidCookie: HTTPCookie = appCookie["PHPSESSID"] as! HTTPCookie
                    let uidCookie: HTTPCookie = appCookie[(mainNameViewLetStr.replacingOccurrences(of: "value", with: "D"))] as! HTTPCookie
                    //: var uidPro: Dictionary<HTTPCookiePropertyKey, Any> = uidCookie.properties!
                    var uidPro: [HTTPCookiePropertyKey: Any] = uidCookie.properties!
                    //: uidPro[HTTPCookiePropertyKey.domain] = url.host
                    uidPro[HTTPCookiePropertyKey.domain] = url.host
                    //: uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    //: HTTPCookieStorage.shared.setCookie(HTTPCookie.init(properties: uidPro)!)
                    HTTPCookieStorage.shared.setCookie(HTTPCookie(properties: uidPro)!)
                }
            }
        }
    }

    //: func func__saveAllCookies() {
    func cookies() {
        //: do {
        do {
            //: let cookies: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
            let cookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
            //: printLog(message: "func__saveAllCookies = \(cookies)")
            printLog(message: (String(mainPopFormat.prefix(7)) + String(dream_textFormat.suffix(5)) + "lCook" + String(main_sizeData)) + "\(cookies)")
            //: let cookieData = try NSKeyedArchiver.archivedData(withRootObject: cookies)
            let cookieData = try NSKeyedArchiver.archivedData(withRootObject: cookies)
            //: Defaults.set(cookieData, forKey: TalkingUserCookiesKey)
            dreamBottomMsg.set(cookieData, forKey: main_useStr)
            //: } catch _ {
        } catch _ {
            //: printLog(message: "func__saveAllCookies  error")
            printLog(message: (String(kMakeData) + String(noti_pairName.suffix(5)) + "lCook" + String(app_nameFormat.prefix(5)) + "error"))
        }
    }

    //: func func__setAppCookies() {
    func toOf() {
        //: let data = Defaults.object(forKey: TalkingUserCookiesKey)
        let data = dreamBottomMsg.object(forKey: main_useStr)
        //: if data != nil {
        if data != nil {
            //: let data2: NSArray = NSKeyedUnarchiver.unarchiveObject(with: data as! Data) as! NSArray
            let data2: NSArray = NSKeyedUnarchiver.unarchiveObject(with: data as! Data) as! NSArray
            //: for cookie in data2 {
            for cookie in data2 {
                //: HTTPCookieStorage.shared.setCookie(cookie as! HTTPCookie)
                HTTPCookieStorage.shared.setCookie(cookie as! HTTPCookie)
            }
        }
    }

    //: func func__removeAllCookies() {
    func calorieChart() {
        //: Defaults.removeObject(forKey: TalkingUserCookiesKey)
        dreamBottomMsg.removeObject(forKey: main_useStr)
        //: let cookiesArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
        let cookiesArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
        //: for cookies in cookiesArr {
        for cookies in cookiesArr {
            //: printLog(message: "🍪 删除\(cookies.name)")
            printLog(message: "🍪 删除\(cookies.name)")
            //: HTTPCookieStorage.shared.deleteCookie(cookies)
            HTTPCookieStorage.shared.deleteCookie(cookies)
        }
    }
}
