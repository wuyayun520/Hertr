
//: Declare String Begin

/*: / :*/
fileprivate let show_managerVoiceAddMsg:String = "/"

/*:  ( :*/
fileprivate let showAddValue:[Character] = [" ","("]

/*: ; build: :*/
fileprivate let user_messageTopEdgeName:[Character] = [";"," ","b","u","i","l","d",":"]

/*: ; iOS  :*/
fileprivate let main_liveId:String = "; iOSsection guard"
fileprivate let user_filterUrl:[Character] = [" "]

/*: ;  :*/
fileprivate let show_extentTitle:[Character] = [";"," "]

/*: )" :*/
fileprivate let show_statusStr:String = "center"

/*: "audio" :*/
fileprivate let show_withId:String = "arowdio"

/*: + :*/
fileprivate let main_sizeMsg:[Character] = ["+"]

/*: .wav" :*/
fileprivate let user_showIdent:[Character] = [".","w","a","v"]

/*: "audio/wav" :*/
fileprivate let userFrontPath:String = "AUD"
fileprivate let show_beginText:String = "io/wavextension control"

/*: .jpg" :*/
fileprivate let appPointIdent:[Character] = [".","j","p","g"]

/*: "image/jpeg" :*/
fileprivate let showSizeMsg:[Character] = ["i","m","a"]
fileprivate let userColorMessage:String = "ge/jpeglevel path"

/*: "://app." :*/
fileprivate let user_namePath:[Character] = [":","/","/","a","p","p","."]

/*: "data" :*/
fileprivate let main_availableData:[Character] = ["d","a","t","a"]

/*: ":null" :*/
fileprivate let dreamAddListData:String = "equal to:null"

/*: ":{}" :*/
fileprivate let mResultUrl:String = ":{equal"

/*: "Request failed, Try again later" :*/
fileprivate let notiLocationViewTruePath:[UInt8] = [0x72,0x65,0x74,0x61,0x6c,0x20,0x6e,0x69,0x61,0x67,0x61,0x20,0x79,0x72,0x54,0x20,0x2c,0x64,0x65,0x6c,0x69,0x61,0x66,0x20,0x74,0x73,0x65,0x75,0x71,0x65,0x52]

/*: "json error" :*/
fileprivate let m_countKey:[Character] = ["j","s","o","n"," ","e","r","r","o","r"]

/*: "platform=iphone&version= :*/
fileprivate let showSectionStr:String = "presultatf"
fileprivate let kIndicatorPath:String = "ipafteron"
fileprivate let m_againPath:[Character] = ["e","&","v","e","r","s","i","o","n","="]

/*: &packageId= :*/
fileprivate let dreamToId:String = "min var center from&pack"

/*: &bundleId= :*/
fileprivate let notiKeyValue:[Character] = ["&","b","u","n","d","l","e","I","d","="]

/*: &lang= :*/
fileprivate let main_countIdent:String = "image beauty&lang="

/*: "?!@#$^&%*+,:;='" :*/
fileprivate let kMagnitudeervalId:String = "?!@#$^"
fileprivate let noti_equalFormat:String = ":;=\'"

/*: "`<>()[]{}/\\| " :*/
fileprivate let appKitTitle:String = "`<top()["
fileprivate let dream_leadingStr:[Character] = ["]","{","}","/","\\","|"," "]

/*: "/route/slowindex" :*/
fileprivate let dream_ofKey:[Character] = ["/","r","o","u","t","e","/","s","l","o","w","i","n","d","e","x"]

/*: "/route/index" :*/
fileprivate let dreamNameResponseIdent:String = "/routetype from"
fileprivate let notiToPicHeightUrl:String = "equal model self type transform/index"

/*: "?path= :*/
fileprivate let mVoiceViewStr:String = "quick index temp description object?path="

/*: "&GJNonceStr= :*/
fileprivate let userSexValue:String = "leading pair list&GJNon"
fileprivate let mEqualStr:[Character] = ["c","e","S","t","r","="]

/*: "& :*/
fileprivate let kBarModelTextStr:String = "up"

/*: = :*/
fileprivate let k_rowId:[Character] = ["="]

/*: "&params= :*/
fileprivate let user_appPath:String = "style to movie view return&p"
fileprivate let app_locationTitle:String = "arams=add for seek catch"

/*: "/ :*/
fileprivate let mLabelMaleTimeIdent:String = "lab"

/*: "? :*/
fileprivate let userIndexMessage:[Character] = ["?"]

/*: "token" :*/
fileprivate let noti_popStr:[Character] = ["t","o","k","e","n"]

/*: "请求成功 :*/
fileprivate let showPlayerValue:[Character] = ["\u{8bf7}","求","成","功"]

/*: "AAAAAAAA" :*/
fileprivate let dream_textStr:String = "challengechallengechallenge"
fileprivate let userFieldBitData:[Character] = ["A","A"]

/*: "AA" :*/
fileprivate let userStartKey:[Character] = ["A","A"]

/*: "BB" :*/
fileprivate let mShareName:[Character] = ["B","B"]

/*: "下载完成 -  :*/
fileprivate let notiByText:String = "isolate载"
fileprivate let app_modelKey:String = "完value - "

/*: "/Documents/" :*/
fileprivate let kEqualId:String = "/Doculist view in all begin"

/*: ".mp4" :*/
fileprivate let app_managerStr:String = ".mp4for game let for"

/*: "移动地址: :*/
fileprivate let kAppUrl:String = "移\u{52a8}地址:"

/*: "后台任务下载回来" :*/
fileprivate let kSendIdent:String = "\u{540e}台任\u{52a1}下\u{8f7d}\u{56de}来"

/*: "下载成功 : :*/
fileprivate let m_centerData:String = "下载成功 :"

/*: "Net Error" :*/
fileprivate let mainWindowKey:String = "Net Eview path text"
fileprivate let dream_aboutFormat:String = "requestor"

/*: "下载失败 : :*/
fileprivate let mainObjectMsg:[Character] = ["\u{4e0b}","载","失","\u{8d25}"," ",":"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  PointRequestTool.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/1/28.
//

//: import Alamofire
import Alamofire
//: import CoreMedia
import CoreMedia
//: import UIKit
import UIKit

// 默认请求超时时间
//: let REQUEST_TIMEOUT_INTERVAL = 10.0
let kDismissIdent = 10.0

//: let userAgent = "\(AppName)/\(AppVersion) (\(AppBundle); build:\(AppBuildNumber); iOS \(UIDevice.current.systemVersion); \(UIDevice.modelName))"
let notiViewValue = "\(mVideoIdent)/\(notiUserPath) (\(userDeviceFormat)" + (String(user_messageTopEdgeName)) + "\(kViewFormat)" + (String(main_liveId.prefix(5)) + String(user_filterUrl)) + "\(UIDevice.current.systemVersion); \(UIDevice.modelName))"

//: typealias FinishBlock = (_ succeed: Bool, _ result: Any?, _ errorModel: TalkingErrorResponse?) -> Void
typealias FinishBlock = (_ succeed: Bool, _ result: Any?, _ errorModel: DataErrorResponse?) -> Void

//: let GJ = TalkingRequestTool.init()
let show_pointBottomPath = PointRequestTool()

//: @objc class TalkingRequestTool: NSObject, URLSessionDownloadDelegate {
@objc class PointRequestTool: NSObject, URLSessionDownloadDelegate {
    //: public func startRequest(model: TalkingRequestModel, completion: @escaping FinishBlock) {
    public func ingathering(model: PostulationReactiveCompatible, completion: @escaping FinishBlock) {
        //: if model.paramsContainBinaryObject() {
        if model.rarity() {
            //: self.startUploadDataRequest(model: model, completion: completion)
            self.totaleractionUpload(model: model, completion: completion)
            //: } else {
        } else {
            //: self.startNormalRequest(model: model, completion: completion)
            self.underServer(model: model, completion: completion)
        }
    }

    //: public func startUploadDataRequest(model: TalkingRequestModel, completion: @escaping FinishBlock) {
    public func totaleractionUpload(model: PostulationReactiveCompatible, completion: @escaping FinishBlock) {
        //: let serverUrl = self.buildServerUrl(model: model)
        let serverUrl = self.outputItem(model: model)
        //: let headers = self.getRequestHeader(model: model)
        let headers = self.viewModel(model: model)

        //: if model.requestType == .GET {
        if model.requestType == .GET {
            //: AF.request(serverUrl, method: .get, parameters: model.params, headers: headers).responseData { [self] responseData in
            AF.request(serverUrl, method: .get, parameters: model.params, headers: headers).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    principalIcon(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, DataErrorResponse(errorCode: ManagerColumnConvertible.NetError.rawValue, errorMsg: user_netTitle))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        colorGift(showMsg: user_netTitle)
                    }

                    //: break
                }
            }
            //: } else {
        } else {
            //: AF.upload(multipartFormData: { (multipartFormData) in
            AF.upload(multipartFormData: { multipartFormData in
                //: for (key, value) in model.params {
                for (key, value) in model.params {
                    //: if value is Data {
                    if value is Data {
                        //: if key == "audio" {
                        if key == (show_withId.replacingOccurrences(of: "row", with: "u")) {
                            //: multipartFormData.append(value as! Data, withName: key, fileName: "\(NSDate.getCurrentTimeStamp())+\(PackageID).wav", mimeType: "audio/wav")
                            multipartFormData.append(value as! Data, withName: key, fileName: "\(NSDate.rentGiftStamp())+\(dreamAgeKey)" + (String(user_showIdent)), mimeType: (userFrontPath.lowercased() + String(show_beginText.prefix(6))))
                            //: } else {
                        } else {
                            //: multipartFormData.append(value as! Data, withName: key, fileName: "\(key).jpg", mimeType: "image/jpeg")
                            multipartFormData.append(value as! Data, withName: key, fileName: "\(key)" + (String(appPointIdent)), mimeType: (String(showSizeMsg) + String(userColorMessage.prefix(7))))
                        }
                        //: } else {
                    } else {
                        //: let data_ = "\(value)".data(using: String.Encoding.utf8)!
                        let data_ = "\(value)".data(using: String.Encoding.utf8)!
                        //: multipartFormData.append(data_, withName: key)
                        multipartFormData.append(data_, withName: key)
                    }
                }
                //: }, to: serverUrl, method: .post, headers: headers).responseData { [self] responseData in
            }, to: serverUrl, method: .post, headers: headers).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    principalIcon(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, DataErrorResponse(errorCode: ManagerColumnConvertible.NetError.rawValue, errorMsg: user_netTitle))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        colorGift(showMsg: user_netTitle)
                    }

                    //: break
                }
            }
        }
    }

    //: public func startNormalRequest(model: TalkingRequestModel, completion: @escaping FinishBlock) {
    public func underServer(model: PostulationReactiveCompatible, completion: @escaping FinishBlock) {
        //: let serverUrl = self.buildServerUrl(model: model)
        let serverUrl = self.outputItem(model: model)
        //: let headers = self.getRequestHeader(model: model)
        let headers = self.viewModel(model: model)

        //: if model.requestType == .GET {
        if model.requestType == .GET {
            //: AF.request(serverUrl, method: .get, parameters: (Encryption_Request ? nil : model.params), headers: headers, requestModifier: { $0.timeoutInterval = REQUEST_TIMEOUT_INTERVAL }).responseData { [self] responseData in
            AF.request(serverUrl, method: .get, parameters: mainHomePath ? nil : model.params, headers: headers, requestModifier: { $0.timeoutInterval = kDismissIdent }).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    principalIcon(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, DataErrorResponse(errorCode: ManagerColumnConvertible.NetError.rawValue, errorMsg: user_netTitle))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        colorGift(showMsg: user_netTitle)
                    }
                    //: break
                }
            }
            //: } else {
        } else {
            //: AF.request(serverUrl, method: .post, parameters: model.params, headers: headers, requestModifier: { $0.timeoutInterval = REQUEST_TIMEOUT_INTERVAL }).responseData { [self] responseData in
            AF.request(serverUrl, method: .post, parameters: model.params, headers: headers, requestModifier: { $0.timeoutInterval = kDismissIdent }).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    principalIcon(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, DataErrorResponse(errorCode: ManagerColumnConvertible.NetError.rawValue, errorMsg: user_netTitle))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        colorGift(showMsg: user_netTitle)
                    }

                    //: break
                }
            }
        }
    }

    //: func func__requestSucess(model: TalkingRequestModel, response: HTTPURLResponse, responseData: Data, completion: @escaping FinishBlock) {
    func principalIcon(model: PostulationReactiveCompatible, response: HTTPURLResponse, responseData: Data, completion: @escaping FinishBlock) {
        //: let httpCookies = HTTPCookie.cookies(withResponseHeaderFields: response.allHeaderFields as! [String: String], for: response.url!)
        let httpCookies = HTTPCookie.cookies(withResponseHeaderFields: response.allHeaderFields as! [String: String], for: response.url!)
        //: if httpCookies.count > 0 {
        if httpCookies.count > 0 {
            //: TalkingRequestAddrTool.share.func__updateTalkingCookies(cookies: httpCookies)
            LiveAddrThen.share.performData(cookies: httpCookies)
        }

        //: var responseJson = String(data: responseData, encoding: .utf8)
        var responseJson = String(data: responseData, encoding: .utf8)

        //: if Encryption_Request, model.requestServer.contains("://app.") {
        if mainHomePath, model.requestServer.contains((String(user_namePath))) {
            //: responseJson = responseJson?.aes256Decrypt(key: TalkingRequestAddrTool.share.encryKeyStr)
            responseJson = responseJson?.device(key: LiveAddrThen.share.encryKeyStr)
        }

        //: responseJson = responseJson?.replacingOccurrences(of: "\"data\":null", with: "\"data\":{}")
        responseJson = responseJson?.replacingOccurrences(of: "\"" + (String(main_availableData)) + "\"" + (String(dreamAddListData.suffix(5))), with: "" + "\"" + (String(main_availableData)) + "\"" + ":{}")
        //: if !Encryption_Request {
        if !mainHomePath {
            //: responseJson = responseJson?.replacingOccurrences(of: ":null", with: ":\"\"")
            responseJson = responseJson?.replacingOccurrences(of: (String(dreamAddListData.suffix(5))), with: ":" + "\"" + "\"")
        }
        //: if let responseModel = JSONDeserializer<TalkingBaseResponse>.deserializeFrom(json: responseJson) {
        if let responseModel = JSONDeserializer<SunProtectionFactorModelType>.deserializeFrom(json: responseJson) {
            //: if responseModel.errno == RequestResultCode.Normal.rawValue {
            if responseModel.errno == ManagerColumnConvertible.Normal.rawValue {
                //: completion(true, responseModel.data, nil)
                completion(true, responseModel.data, nil)
                //: } else {
            } else {
                //: completion(false, responseModel.data, TalkingErrorResponse.init(errorCode: responseModel.errno, errorMsg: responseModel.msg ?? ""))
                completion(false, responseModel.data, DataErrorResponse(errorCode: responseModel.errno, errorMsg: responseModel.msg ?? ""))
                //: if model.showErrorStatusBar {
                if model.showErrorStatusBar {
                    //: func__showStatusBarErrorMsg(showMsg: responseModel.msg ?? "Request failed, Try again later".localized)
                    colorGift(showMsg: responseModel.msg ?? String(bytes: notiLocationViewTruePath.reversed(), encoding: .utf8)!.localized)
                }
                //: switch responseModel.errno {
                switch responseModel.errno {
                //: case RequestResultCode.NeedReLogin.rawValue:
                case ManagerColumnConvertible.NeedReLogin.rawValue:
                    //: NotificationCenter.default.post(name: DID_LOGIN_OUT_SUCCESS_NOTIFICATION, object: nil, userInfo: nil)
                    NotificationCenter.default.post(name: mDetailId, object: nil, userInfo: nil)
                //: default:
                default:
                    //: break
                    break
                }
            }
            //: } else {
        } else {
            //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: "json error".localized))
            completion(false, nil, DataErrorResponse(errorCode: ManagerColumnConvertible.NetError.rawValue, errorMsg: (String(m_countKey)).localized))
        }
    }

    //: func buildServerUrl(model: TalkingRequestModel) -> String {
    func outputItem(model: PostulationReactiveCompatible) -> String {
        //: var serverUrl: String = model.requestServer
        var serverUrl: String = model.requestServer

        //: var otherParams = "platform=iphone&version=\(AppNetVersion)&packageId=\(PackageID)&bundleId=\(AppBundle)&lang=\(TalkingRequestAddrTool.share.interfaceLang)"
        var otherParams = (showSectionStr.replacingOccurrences(of: "result", with: "l") + "orm=" + kIndicatorPath.replacingOccurrences(of: "after", with: "h") + String(m_againPath)) + "\(m_saltIdent)" + (String(dreamToId.suffix(5)) + "ageId=") + "\(dreamAgeKey)" + (String(notiKeyValue)) + "\(userDeviceFormat)" + (String(main_countIdent.suffix(6))) + "\(LiveAddrThen.share.interfaceLang)"
        //: if Encryption_Request, serverUrl.contains("://app.") {
        if mainHomePath, serverUrl.contains((String(user_namePath))) {
            //: let allowCharSet: CharacterSet = CharacterSet.init(charactersIn: "?!@#$^&%*+,:;='\"`<>()[]{}/\\| ").inverted
            let allowCharSet = CharacterSet(charactersIn: "?!@#$^&%*+,:;='" + "\"" + "`<>()[]{}/\\| ").inverted
            //: if TalkingRequestAddrTool.share.slowPathArr.contains(model.requestPath) {
            if LiveAddrThen.share.slowPathArr.contains(model.requestPath) {
                //: serverUrl.append("/route/slowindex")
                serverUrl.append((String(dream_ofKey)))
                //: } else {
            } else {
                //: serverUrl.append("/route/index") // 如果使用IP需要加上app/
                serverUrl.append((String(dreamNameResponseIdent.prefix(6)) + String(notiToPicHeightUrl.suffix(6)))) // 如果使用IP需要加上app/
            }

            //: let pathStr = model.requestPath.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
            let pathStr = model.requestPath.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
            //: serverUrl.append("?path=\(pathStr)")
            serverUrl.append((String(mVoiceViewStr.suffix(6))) + "\(pathStr)")
            //: serverUrl.append("&GJNonceStr=\(TalkingRequestAddrTool.share.randomStr)")
            serverUrl.append((String(userSexValue.suffix(6)) + String(mEqualStr)) + "\(LiveAddrThen.share.randomStr)")

            //: if model.requestType == .GET {
            if model.requestType == .GET {
                //: for (key, value) in model.params {
                for (key, value) in model.params {
                    //: otherParams.append("&\(key)=\(value)")
                    otherParams.append("&\(key)=\(value)")
                }
            }
            //: if let encryStr = otherParams.aes256Encrypt(key: TalkingRequestAddrTool.share.encryKeyStr) {
            if let encryStr = otherParams.someLab(key: LiveAddrThen.share.encryKeyStr) {
                //: let paramsStr = encryStr.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
                let paramsStr = encryStr.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
                //: serverUrl.append("&params=\(paramsStr)")
                serverUrl.append((String(user_appPath.suffix(2)) + String(app_locationTitle.prefix(6))) + "\(paramsStr)")
            }
            //: printLog(message: serverUrl)
            printLog(message: serverUrl)
            //: } else {
        } else {
            //: if !model.requestPath.isEmptyString {
            if !model.requestPath.isEmptyString {
                //: serverUrl.append("/\(model.requestPath)")
                serverUrl.append("/\(model.requestPath)")
            }
            //: serverUrl.append("?\(otherParams)")
            serverUrl.append("?\(otherParams)")
        }
        //: return serverUrl
        return serverUrl
    }

    /// 获取请求头参数
    /// - Parameter model: 请求模型
    /// - Returns: 请求头参数
    //: func getRequestHeader(model: TalkingRequestModel) -> HTTPHeaders {
    func viewModel(model: PostulationReactiveCompatible) -> HTTPHeaders {
        //: var headers = [HTTPHeader.userAgent(userAgent)]
        var headers = [HTTPHeader.userAgent(notiViewValue)]
        //: if model.addHeaderToken.isEmpty == false {
        if model.addHeaderToken.isEmpty == false {
            //: let token = HTTPHeader(name: "token", value: model.addHeaderToken)
            let token = HTTPHeader(name: (String(noti_popStr)), value: model.addHeaderToken)
            //: headers.append(token)
            headers.append(token)
        }
        //: return HTTPHeaders(headers)
        return HTTPHeaders(headers)
    }

    // MARK: - 系统后台下载

    //: func urlSessionBackgroundDownLoad(model: TalkingRequestModel) {
    func beyondExecutiveSession(model: PostulationReactiveCompatible) {
        //: let serverUrl = self.buildServerUrl(model: model)
        let serverUrl = self.outputItem(model: model)
        //: URLSession.shared.dataTask(with: URL.init(string: serverUrl)!) {(data, response, error) in
        URLSession.shared.dataTask(with: URL(string: serverUrl)!) { _, response, error in
            //: if error == nil {
            if error == nil {
                //: printLog(message: "请求成功\(String(describing: response))" )
                printLog(message: (String(showPlayerValue)) + "\(String(describing: response))")
            }
        }

//        let configuration1 = URLSessionConfiguration.default    // default模式下系统会创建一个持久化的缓存并在用户的钥匙串中存储证书
//        let configuration2 = URLSessionConfiguration.ephemeral  // 所有内容的生命周期都与session相同，当session无效时，所有内容自动释放。

        // 后台下载
        //: let configuration3 = URLSessionConfiguration.background(withIdentifier: "AAAAAAAA")  // 后台甚至APP已经关闭的时候仍然在传输数据的会话
        let configuration3 = URLSessionConfiguration.background(withIdentifier: (dream_textStr.replacingOccurrences(of: "challenge", with: "AA") + String(userFieldBitData))) // 后台甚至APP已经关闭的时候仍然在传输数据的会话
        //: configuration3.httpAdditionalHeaders = ["AA": "AA", "BB": "BB"] // 与请求一起发送的附加头文件的字典
        configuration3.httpAdditionalHeaders = ["AA": "AA", "BB": "BB"] // 与请求一起发送的附加头文件的字典
        //: configuration3.networkServiceType = .default
        configuration3.networkServiceType = .default // 网络服务的类型
        //: configuration3.allowsCellularAccess = true
        configuration3.allowsCellularAccess = true // 一个布尔值，用于确定是否应通过蜂窝网络进行连接
        //: configuration3.timeoutIntervalForRequest = 15
        configuration3.timeoutIntervalForRequest = 15
        //: configuration3.timeoutIntervalForResource = 15
        configuration3.timeoutIntervalForResource = 15
        //: let session = URLSession.init(configuration: configuration3, delegate: self, delegateQueue: OperationQueue.main)
        let session = URLSession(configuration: configuration3, delegate: self, delegateQueue: OperationQueue.main)
        //: session.downloadTask(with: URL.init(string: serverUrl)!).resume()
        session.downloadTask(with: URL(string: serverUrl)!).resume()
    }

    //: func urlSession(_ session: URLSession, downloadTask: URLSessionDownloadTask, didFinishDownloadingTo location: URL) {
    func urlSession(_: URLSession, downloadTask _: URLSessionDownloadTask, didFinishDownloadingTo location: URL) {
        //: printLog(message: "下载完成 - \(location)")
        printLog(message: (notiByText.replacingOccurrences(of: "isolate", with: "下") + app_modelKey.replacingOccurrences(of: "value", with: "成")) + "\(location)")
        //: let locationPath = location.path
        let locationPath = location.path
        //: let documnets = NSHomeDirectory() + "/Documents/" + NSDate.getCurrentTimeStamp() + ".mp4"
        let documnets = NSHomeDirectory() + (String(kEqualId.prefix(5)) + "ments/") + NSDate.rentGiftStamp() + (String(app_managerStr.prefix(4)))
        //: printLog(message: "移动地址:\(documnets)")
        printLog(message: (kAppUrl) + "\(documnets)")
        //: let fileManager = FileManager.default
        let fileManager = FileManager.default
        //: do {
        do {
            //: try fileManager.moveItem(atPath: locationPath, toPath: documnets)
            try fileManager.moveItem(atPath: locationPath, toPath: documnets)
            //: } catch {
        } catch {
            // catch 中默认提供error信息, 当序列化不成功是, 返回error
            //: printLog(message: error)
            printLog(message: error)
        }
    }

    // 监听下载进度
    //: func urlSession(_ session: URLSession, downloadTask: URLSessionDownloadTask, didWriteData bytesWritten: Int64, totalBytesWritten: Int64, totalBytesExpectedToWrite: Int64) {
    func urlSession(_: URLSession, downloadTask _: URLSessionDownloadTask, didWriteData _: Int64, totalBytesWritten _: Int64, totalBytesExpectedToWrite _: Int64) {}

    //: func urlSessionDidFinishEvents(forBackgroundURLSession session: URLSession) {
    func urlSessionDidFinishEvents(forBackgroundURLSession _: URLSession) {
        //: printLog(message: "后台任务下载回来")
        printLog(message: (kSendIdent))
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: guard let backgroundHandle = AppDelegateHelper.shared.backgroundSessionCompletionHandler else { return }
            guard let backgroundHandle = ShareDelegateHelper.shared.backgroundSessionCompletionHandler else { return }
            //: backgroundHandle()
            backgroundHandle()
        }
    }

    // MARK: - 动画下载

    //: public func startGiftZipRequest(giftUrl: String, path: String, completion: @escaping FinishBlock) {
    public func bodyOfWater(giftUrl: String, path: String, completion: @escaping FinishBlock) {
        //: let destination: DownloadRequest.Destination = { _, _ in
        let destination: DownloadRequest.Destination = { _, _ in
            //: let pathUrl = URL(fileURLWithPath: path)
            let pathUrl = URL(fileURLWithPath: path)
            //: return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
            return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
        }
        //: let giftUrlStr = giftUrl.replacingOccurrences(of: UrlDomin.urlAes256Decrypt(), with: ReplaceUrlDomain)
        let giftUrlStr = giftUrl.replacingOccurrences(of: user_topData.disobey(), with: dreamRecordPath)
        //: AF.download(giftUrlStr, to: destination).downloadProgress { speed in
        AF.download(giftUrlStr, to: destination).downloadProgress { _ in
        }
        //: .responseData { response in
        .responseData { response in
            //: switch response.result {
            switch response.result {
            //: case .success(let json):
            case let .success(json):
                //: printLog(message: "下载成功 :\(json)")
                printLog(message: (m_centerData.capitalized) + "\(json)")
                //: completion(true, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.Normal.rawValue, errorMsg: "Net Error".localized))
                completion(true, nil, DataErrorResponse(errorCode: ManagerColumnConvertible.Normal.rawValue, errorMsg: (String(mainWindowKey.prefix(5)) + dream_aboutFormat.replacingOccurrences(of: "request", with: "rr")).localized))
            //: break
            //: case .failure(let errMsg):
            case let .failure(errMsg):
                //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: "Net Error".localized))
                completion(false, nil, DataErrorResponse(errorCode: ManagerColumnConvertible.NetError.rawValue, errorMsg: (String(mainWindowKey.prefix(5)) + dream_aboutFormat.replacingOccurrences(of: "request", with: "rr")).localized))

                //: printLog(message: "下载失败 :\(errMsg)")
                printLog(message: (String(mainObjectMsg)) + "\(errMsg)")
            }
        }
    }

    //: public func startVoiceRequest(voiceUrl: String, path: String, completion: @escaping FinishBlock) {
    public func streamObserver(voiceUrl: String, path: String, completion: @escaping FinishBlock) {
        //: let destination: DownloadRequest.Destination = { _, _ in
        let destination: DownloadRequest.Destination = { _, _ in
            //: let pathUrl = URL(fileURLWithPath: path)
            let pathUrl = URL(fileURLWithPath: path)
            //: return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
            return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
        }

        //: let voiceUrlStr = voiceUrl.replacingOccurrences(of: UrlDomin.urlAes256Decrypt(), with: ReplaceUrlDomain)
        let voiceUrlStr = voiceUrl.replacingOccurrences(of: user_topData.disobey(), with: dreamRecordPath)
        //: AF.download(voiceUrlStr, to: destination).downloadProgress { speed in
        AF.download(voiceUrlStr, to: destination).downloadProgress { _ in
        }
        //: .responseData { response in
        .responseData { response in
            //: switch response.result {
            switch response.result {
            //: case .success(let json):
            case let .success(json):
                //: printLog(message: "下载成功 :\(json)")
                printLog(message: (m_centerData.capitalized) + "\(json)")
                //: completion(true, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.Normal.rawValue, errorMsg: "Net Error".localized))
                completion(true, nil, DataErrorResponse(errorCode: ManagerColumnConvertible.Normal.rawValue, errorMsg: (String(mainWindowKey.prefix(5)) + dream_aboutFormat.replacingOccurrences(of: "request", with: "rr")).localized))
            //: break
            //: case .failure(let errMsg):
            case let .failure(errMsg):
                //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: "Net Error".localized))
                completion(false, nil, DataErrorResponse(errorCode: ManagerColumnConvertible.NetError.rawValue, errorMsg: (String(mainWindowKey.prefix(5)) + dream_aboutFormat.replacingOccurrences(of: "request", with: "rr")).localized))

                //: printLog(message: "下载失败 :\(errMsg)")
                printLog(message: (String(mainObjectMsg)) + "\(errMsg)")
            }
        }
    }
}
