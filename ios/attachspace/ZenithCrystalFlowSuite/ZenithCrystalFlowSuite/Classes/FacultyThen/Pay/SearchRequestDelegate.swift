
//: Declare String Begin

/*: "APPLE_IAP_PAY_SUCCEED_NOTIFICATION" :*/
fileprivate let main_messageUrl:[UInt8] = [0xf9,0x8,0x8,0x4,0xfd,0x17,0x1,0xf9,0x8,0x17,0x8,0xf9,0x11,0x17,0xb,0xd,0xfb,0xfb,0xfd,0xfd,0xfc,0x17,0x6,0x7,0xc,0x1,0xfe,0x1,0xfb,0xf9,0xc,0x1,0x7,0x6]

fileprivate func saveName(draft num: UInt8) -> UInt8 {
    let value = Int(num) + 72
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "APPLE_IAP_SUBSCRIBE_SUCCEED_NOTIFICATION" :*/
fileprivate let dreamPriceKey:[UInt8] = [0x52,0x43,0x43,0x5f,0x56,0x4c,0x5a,0x52,0x43,0x4c,0x40,0x46,0x51,0x40,0x50,0x41,0x5a,0x51,0x56,0x4c,0x40,0x46,0x50,0x50,0x56,0x56,0x57,0x4c,0x5d,0x5c,0x47,0x5a,0x55,0x5a,0x50,0x52,0x47,0x5a,0x5c,0x5d]

private func pointName(head num: UInt8) -> UInt8 {
    return num ^ 19
}

/*: "mf/recharge/createApplePay" :*/
fileprivate let main_sItemName:String = "mf/rview tap"
fileprivate let dreamFrameNativeId:String = "e/creabody system"
fileprivate let noti_iconName:String = "view clearplePay"

/*: "productId" :*/
fileprivate let showSizeMessage:String = "productIdcolor bottom right"

/*: "source" :*/
fileprivate let show_lastIdent:String = "identityource"

/*: "IAP充值创建订单失败: :*/
fileprivate let kSessionBorderDismissPath:[Character] = ["I"]
fileprivate let k_arrayLabelStr:String = "point super inAP充值创"

/*: , 参数: :*/
fileprivate let noti_deadlineName:String = ", 参\u{6570}:"

/*: ." :*/
fileprivate let dreamObjectRangeModelUrl:[Character] = ["."]

/*: "orderNum" :*/
fileprivate let kLeadingTopTitle:[Character] = ["o","r","d","e","r","N","u","m"]

/*: "mf/recharge/applePayNotify" :*/
fileprivate let main_officialCurIdent:String = "mf/rnor background table data"
fileprivate let showAtText:String = "rge/apindicator enable true shadow transform"
fileprivate let showMessageIdent:String = "as tap true actualyNotify"

/*: "IAP充值校验失败: :*/
fileprivate let dreamTableLockFormat:[Character] = ["I"]
fileprivate let app_touchMsg:String = "AP充\u{503c}校"

/*: "transactionId" :*/
fileprivate let app_valueKey:String = "tstatuteansa"
fileprivate let appFormatId:String = "save"

/*: "reportMoney" :*/
fileprivate let app_whiteId:String = "server at valuereportMon"
fileprivate let mTransformName:String = "emanager"

/*: "mf/AutoSub/AppleCreateOrder" :*/
fileprivate let showFieldName:String = "mf/Aunor as fatal live"
fileprivate let main_failKey:String = "file nameb/Appl"
fileprivate let showRequestData:String = "ateOrdeif selected section"
fileprivate let noti_spendMoreUrl:String = "collection"

/*: "IAP订阅创建订单失败: :*/
fileprivate let mainAppFormat:[Character] = ["I","A","P","订","阅","创","建","订","单","\u{5931}","败",":"]

/*: "orderId" :*/
fileprivate let mDetailUrl:[UInt8] = [0xfc,0xff,0xf1,0xf2,0xff,0xd6,0xf1]

fileprivate func imageTask(bring num: UInt8) -> UInt8 {
    let value = Int(num) + 115
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "mf/AutoSub/ApplePaySuccess" :*/
fileprivate let dreamIndexData:[Character] = ["m","f","/","A","u","t","o","S","u","b","/","A","p","p","l"]
fileprivate let noti_readIdent:[Character] = ["e","P","a","y","S","u","c","c","e","s","s"]

/*: "IAP订阅校验失败: :*/
fileprivate let noti_momentText:String = "IAP订阅center return let make self"
fileprivate let showFormatColorMessage:[Character] = ["校","\u{9a8c}","失","败",":"]

/*: "App" :*/
fileprivate let app_dataPath:[Character] = ["A","p","p"]

/*: "OrderTransactionInfo_ :*/
fileprivate let k_translatePath:String = "true any playerOrder"
fileprivate let mMaximumMessage:String = "let return status tableTransact"
fileprivate let mainBackName:[Character] = ["i","o","n","I","n","f","o","_"]

/*: "OrderTransactionInfo_Subscribe_ :*/
fileprivate let mainMakeId:[UInt8] = [0x82,0xbf,0xa9,0xa8,0xbf,0x99,0xbf,0xac,0xa3,0xbe,0xac,0xae,0xb9,0xa4,0xa2,0xa3,0x84,0xa3,0xab,0xa2,0x92,0x9e,0xb8,0xaf,0xbe,0xae,0xbf,0xa4,0xaf,0xa8,0x92]

private func rareEarthElement(intimate num: UInt8) -> UInt8 {
    return num ^ 205
}

/*: "verifyData" :*/
fileprivate let app_toUrl:[UInt8] = [0x3c,0x2f,0x38,0x23,0x2c,0x33,0xe,0x2b,0x3e,0x2b]

private func titleTop(section num: UInt8) -> UInt8 {
    return num ^ 74
}

/*: "IAP查询内购商品失败, productId: :*/
fileprivate let showMakeMessage:String = "IAP\u{67e5}"
fileprivate let user_sizeFormat:String = "position mode失败, p"
fileprivate let m_pageUrl:String = "tId:view list self"

/*: "IAP查询内购商品失败, products.count为0." :*/
fileprivate let appRecordName:[Character] = ["I","A","P","查","询","内","\u{8d2d}","商","\u{54c1}","失","败",","," ","p","r","o","d","u"]
fileprivate let dream_sectionText:String = "cts.cstatus item to"
fileprivate let mainByName:String = "ount\u{4e3a}0."

/*: "IAP查询内购商品失败, Error: :*/
fileprivate let mainNameVoiceFormat:String = "IAP\u{67e5}\u{8be2}"
fileprivate let main_sinceIdent:String = "active,"
fileprivate let k_workTitleToUrl:[Character] = [" ","E","r","r","o","r",":"]

/*: "IAP-purchased: :*/
fileprivate let kProfileFormat:String = "Itemp"
fileprivate let kControlRangeUrl:[Character] = ["P","-","p","u","r","c","h","a","s","e","d",":"]

/*: , 订单号: :*/
fileprivate let showReplyName:[Character] = [","," ","\u{8ba2}","\u{5355}","号",":"]

/*: , 商品Id: :*/
fileprivate let kProgressTableData:String = ", 商\u{54c1}I"
fileprivate let appAcceptMsg:String = "if viewd:"

/*: , 苹果transId: :*/
fileprivate let userDayListName:[Character] = [","," ","苹","\u{679c}","t","r","a","n","s","I","d",":"]

/*: "IAP-failed: :*/
fileprivate let main_recordTitle:[Character] = ["I","A","P","-","f","a","i","l","e","d",":"]

/*: "IAP-deferred: :*/
fileprivate let app_toolKey:String = "IAP-defself status lab male"
fileprivate let main_modifyMessageLocationMsg:[Character] = ["e"]
fileprivate let notiSectionStr:[Character] = ["r","r","e","d",":"]

/*: "IAP未知交易类型: :*/
fileprivate let mainAvailablePath:String = "IAP\u{672a}知"
fileprivate let mMarginFoodUrl:String = "交易类content:"

/*: " 未知的交易类型" :*/
fileprivate let noti_quoteMessage:String = " 未知的"
fileprivate let mainBotMsg:[Character] = ["\u{4ea4}","易","类","型"]

/*: "IAP获取本地收据数据失败, transactionId: :*/
fileprivate let notiUserContent:[Character] = ["I","A","P","获","取","本","地","收","据","\u{6570}","据","失","败",","," ","t","r","a","n","s","a","c","t","i"]
fileprivate let kTargetTitle:String = "onId:break add time window"

/*: , orderId: :*/
fileprivate let mainImageTitle:String = "area empty return, ord"

/*: , payType: :*/
fileprivate let mAddMsg:[Character] = [","," ","p","a","y","T","y","p","e"]
fileprivate let show_femaleText:[Character] = [":"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  SearchRequestDelegate.swift
//  AbroadTalking
//
//  Created by young on 2022/9/2.
//

//: import StoreKit
import StoreKit
//: import SwiftyJSON
import SwiftyJSON
//: import UIKit
import UIKit

// 苹果支付成功通知
//: public let APPLE_IAP_PAY_SUCCEED_NOTIFICATION = NSNotification.Name(rawValue: "APPLE_IAP_PAY_SUCCEED_NOTIFICATION")
public let show_detailText = NSNotification.Name(rawValue: String(bytes: main_messageUrl.map{saveName(draft: $0)}, encoding: .utf8)!)
// 苹果订阅成功通知
//: public let APPLE_IAP_SUBSCRIBE_SUCCEED_NOTIFICATION = NSNotification.Name(rawValue: "APPLE_IAP_SUBSCRIBE_SUCCEED_NOTIFICATION")
public let m_statusBarValue = NSNotification.Name(rawValue: String(bytes: dreamPriceKey.map{pointName(head: $0)}, encoding: .utf8)!)
// 最大失败重试次数
//: let APPLE_IAP_MAX_RETRY_COUNT = 9
let mKeyPingContent = 9

/// 支付类型
//: enum ApplePayType {
enum GiftType {
    //: case Pay
    case Pay // 支付
    //: case Subscribe
    case Subscribe // 订阅
}

/// 支付状态
//: enum AppleIAPStatus: String {
enum CharterTermConvertible: String {
    //: case unknow            = "未知类型"
    case unknow = "未知类型"
    //: case createOrderFail   = "创建订单失败"
    case createOrderFail = "创建订单失败"
    //: case notArrow          = "设备不允许"
    case notArrow = "设备不允许"
    //: case noProductId       = "缺少产品Id"
    case noProductId = "缺少产品Id"
    //: case failed            = "交易失败/取消"
    case failed = "交易失败/取消"
    //: case restored          = "已购买过该商品"
    case restored = "已购买过该商品"
    //: case deferred          = "交易延期"
    case deferred = "交易延期"
    //: case verityFail        = "服务器验证失败"
    case verityFail = "服务器验证失败"
    //: case veritySucceed     = "服务器验证成功"
    case veritySucceed = "服务器验证成功"
    //: case renewSucceed      = "自动续订成功"
    case renewSucceed = "自动续订成功"
}

//: typealias IAPcompletionHandle = (AppleIAPStatus, Double, ApplePayType) -> Void
typealias IAPcompletionHandle = (CharterTermConvertible, Double, GiftType) -> Void

//: class AppleIAPManager: NSObject {
class SearchRequestDelegate: NSObject {
    //: var completionHandle: IAPcompletionHandle?
    var completionHandle: IAPcompletionHandle?
    //: private var productInfoReq: SKProductsRequest?
    private var productInfoReq: SKProductsRequest?
    //: private var reqRetryCountDict = [String: Int]()
    private var reqRetryCountDict = [String: Int]() // 记录每个交易请求重试次数
    //: private var payCacheList = [[String: String]]()
    private var payCacheList = [[String: String]]() // 【购买】缓存数据
    //: private var subscribeCacheList = [[String: String]]()
    private var subscribeCacheList = [[String: String]]() // 【订阅】缓存数据
    //: private var createOrderId: String?
    private var createOrderId: String? // 当前支付服务端创建的订单id
    //: private var currentPayType: ApplePayType = .Pay
    private var currentPayType: GiftType = .Pay // 当前支付类型

    // singleton
    //: static let shared = AppleIAPManager()
    static let shared = SearchRequestDelegate()
    //: override func copy() -> Any { return self }
    override func copy() -> Any { return self }
    //: override func mutableCopy() -> Any { return self }
    override func mutableCopy() -> Any { return self }
    //: private override init() {
    override private init() {
        //: super.init()
        super.init()
        //: SKPaymentQueue.default().add(self as SKPaymentTransactionObserver)
        SKPaymentQueue.default().add(self as SKPaymentTransactionObserver)
        // 监听应用将要销毁
        //: NotificationCenter.default.addObserver(self, selector: #selector(appWillTerminate),
        NotificationCenter.default.addObserver(self, selector: #selector(pushTerminate),
                                               //: name: UIApplication.willTerminateNotification,
                                               name: UIApplication.willTerminateNotification,
                                               //: object: nil)
                                               object: nil)
    }

    // MARK: - NotificationCenter

    //: @objc func appWillTerminate() {
    @objc func pushTerminate() {
        //: SKPaymentQueue.default().remove(self as SKPaymentTransactionObserver)
        SKPaymentQueue.default().remove(self as SKPaymentTransactionObserver)
    }
}

// MARK: - 【苹果购买】业务接口

//: extension AppleIAPManager {
private extension SearchRequestDelegate {
    /// 【购买】创建业务订单
    /// - Parameters:
    ///   - productId: 产品Id
    ///   - block: 回调
    //: fileprivate func req_pay_createAppleOrder(productId: String, source: Int, handle: @escaping (String?, Bool) -> Void) {
    func priceHandle(productId: String, source: Int, handle: @escaping (String?, Bool) -> Void) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = PostulationReactiveCompatible()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/recharge/createApplePay"
        reqModel.requestPath = (String(main_sItemName.prefix(4)) + "echarg" + String(dreamFrameNativeId.prefix(6)) + "teAp" + String(noti_iconName.suffix(6)))
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["productId"] = productId
        dict[(String(showSizeMessage.prefix(9)))] = productId
        //: dict["source"] = source
        dict[(show_lastIdent.replacingOccurrences(of: "identity", with: "s"))] = source
        //: reqModel.params = dict
        reqModel.params = dict
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        show_pointBottomPath.ingathering(model: reqModel) { succeed, result, errorModel in
            //: guard succeed == true else {
            guard succeed == true else {
                //: UploadLogTool.writeLog(msg: "IAP充值创建订单失败:\(String(describing: errorModel?.errorMsg)), 参数:\(dict).")
                LabelLogTool.fillInLine(msg: (String(kSessionBorderDismissPath) + String(k_arrayLabelStr.suffix(5)) + "建订单失败:") + "\(String(describing: errorModel?.errorMsg)), 参数:\(dict).")
                //: handle(nil, succeed)
                handle(nil, succeed)
                //: return
                return
            }

            //: let json = JSON(result!)
            let json = JSON(result!)
            //: let orderId = json["orderNum"].string
            let orderId = json[(String(kLeadingTopTitle))].string
            //: handle(orderId, succeed)
            handle(orderId, succeed)
        }
    }

    /// 【购买】上传支付信息到服务器验证
    /// - Parameters:
    ///   - transaction: 交易信息
    ///   - params: 接口参数
    //: fileprivate func req_pay_uploadAppletransaction(_ transactionId: String, params: [String: String]) {
    func buttonParams(_ transactionId: String, params: [String: String]) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = PostulationReactiveCompatible()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/recharge/applePayNotify"
        reqModel.requestPath = (String(main_officialCurIdent.prefix(4)) + "echa" + String(showAtText.prefix(6)) + "plePa" + String(showMessageIdent.suffix(7)))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        show_pointBottomPath.ingathering(model: reqModel) { succeed, result, errorModel in
            //: guard succeed == true || errorModel?.errorCode == 405 else {
            guard succeed == true || errorModel?.errorCode == 405 else { // 验证接口失败，重试接口
                //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 2) {
                DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 2) {
                    //: UploadLogTool.writeLog(msg: "IAP充值校验失败:\(String(describing: errorModel?.errorCode)), 参数:\(params).")
                    LabelLogTool.fillInLine(msg: (String(dreamTableLockFormat) + app_touchMsg + "\u{9a8c}失败:") + "\(String(describing: errorModel?.errorCode)), 参数:\(params).")
                    //: self.transcationPurchasedToCheck(transactionId, .Pay)
                    self.makeText(transactionId, .Pay)
                }
                //: return
                return
            }

            //: let result = JSON(result ?? [:])
            let result = JSON(result ?? [:])

            // 过滤已验证成功的订单数据
            //: let newPayCacheList = self.payCacheList.filter({$0["transactionId"] != transactionId})
            let newPayCacheList = self.payCacheList.filter { $0[(app_valueKey.replacingOccurrences(of: "statute", with: "r") + "ctionI" + appFormatId.replacingOccurrences(of: "save", with: "d"))] != transactionId }
            //: let diskPath = self.getPayCachePath()
            let diskPath = self.kickBackEqual()
            //: NSKeyedArchiver.archiveRootObject(newPayCacheList, toFile: diskPath)
            NSKeyedArchiver.archiveRootObject(newPayCacheList, toFile: diskPath)

            // 成功通知
            //: NotificationCenter.default.post(name: APPLE_IAP_PAY_SUCCEED_NOTIFICATION, object: nil)
            NotificationCenter.default.post(name: show_detailText, object: nil)
            // 成功回调
            //: self.completionHandle?(.veritySucceed, result["reportMoney"].doubleValue, .Pay)
            self.completionHandle?(.veritySucceed, result[(String(app_whiteId.suffix(9)) + mTransformName.replacingOccurrences(of: "manager", with: "y"))].doubleValue, .Pay)
        }
    }
}

// MARK: - 【苹果订阅】业务接口

//: extension AppleIAPManager {
private extension SearchRequestDelegate {
    /// 【订阅】创建业务订单
    /// - Parameters:
    ///   - productId: 产品Id
    ///   - block: 回调
    //: fileprivate func req_subscribe_createAppleOrder(productId: String, source: Int, handle: @escaping (String?, Bool) -> Void) {
    func product(productId: String, source: Int, handle: @escaping (String?, Bool) -> Void) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = PostulationReactiveCompatible()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/AutoSub/AppleCreateOrder"
        reqModel.requestPath = (String(showFieldName.prefix(5)) + "toSu" + String(main_failKey.suffix(6)) + "eCre" + String(showRequestData.prefix(7)) + noti_spendMoreUrl.replacingOccurrences(of: "collection", with: "r"))
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["productId"] = productId
        dict[(String(showSizeMessage.prefix(9)))] = productId
        //: dict["source"] = source
        dict[(show_lastIdent.replacingOccurrences(of: "identity", with: "s"))] = source
        //: reqModel.params = dict
        reqModel.params = dict
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        show_pointBottomPath.ingathering(model: reqModel) { succeed, result, errorModel in
            //: guard succeed == true else {
            guard succeed == true else {
                //: UploadLogTool.writeLog(msg: "IAP订阅创建订单失败:\(String(describing: errorModel?.errorMsg)), 参数:\(dict).")
                LabelLogTool.fillInLine(msg: (String(mainAppFormat)) + "\(String(describing: errorModel?.errorMsg)), 参数:\(dict).")
                //: handle(nil, succeed)
                handle(nil, succeed)
                //: return
                return
            }

            //: let json = JSON(result!)
            let json = JSON(result!)
            //: let orderId = json["orderId"].string
            let orderId = json[String(bytes: mDetailUrl.map{imageTask(bring: $0)}, encoding: .utf8)!].string
            //: handle(orderId, succeed)
            handle(orderId, succeed)
        }
    }

    /// 【订阅】上传支付信息到服务器验证
    /// - Parameters:
    ///   - transaction: 交易信息
    ///   - params: 接口参数
    //: fileprivate func req_subscribe_uploadAppletransaction(_ transactionId: String, params: [String: String]) {
    func mediumApp(_ transactionId: String, params: [String: String]) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = PostulationReactiveCompatible()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/AutoSub/ApplePaySuccess"
        reqModel.requestPath = (String(dreamIndexData) + String(noti_readIdent))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        show_pointBottomPath.ingathering(model: reqModel) { succeed, result, errorModel in
            //: guard succeed == true || errorModel?.errorCode == 405 else {
            guard succeed == true || errorModel?.errorCode == 405 else { // 验证接口失败，重试接口
                //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 3) {
                DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 3) {
                    //: UploadLogTool.writeLog(msg: "IAP订阅校验失败:\(String(describing: errorModel?.errorCode)), 参数:\(params).")
                    LabelLogTool.fillInLine(msg: (String(noti_momentText.prefix(5)) + String(showFormatColorMessage)) + "\(String(describing: errorModel?.errorCode)), 参数:\(params).")
                    //: self.transcationPurchasedToCheck(transactionId, .Subscribe)
                    self.makeText(transactionId, .Subscribe)
                }
                //: return
                return
            }

            //: let result = JSON(result ?? [:])
            let result = JSON(result ?? [:])

            // 过滤已验证成功的订单数据
            //: let newSubscribeCacheList = self.subscribeCacheList.filter({$0["transactionId"] != transactionId})
            let newSubscribeCacheList = self.subscribeCacheList.filter { $0[(app_valueKey.replacingOccurrences(of: "statute", with: "r") + "ctionI" + appFormatId.replacingOccurrences(of: "save", with: "d"))] != transactionId }
            //: let diskPath = self.getSubscribeCachePath()
            let diskPath = self.paragraph()
            //: NSKeyedArchiver.archiveRootObject(newSubscribeCacheList, toFile: diskPath)
            NSKeyedArchiver.archiveRootObject(newSubscribeCacheList, toFile: diskPath)

            // 成功通知
            //: NotificationCenter.default.post(name: APPLE_IAP_SUBSCRIBE_SUCCEED_NOTIFICATION, object: nil)
            NotificationCenter.default.post(name: m_statusBarValue, object: nil)
            // 成功回调
            //: self.completionHandle?(.veritySucceed, result["reportMoney"].doubleValue, .Subscribe)
            self.completionHandle?(.veritySucceed, result[(String(app_whiteId.suffix(9)) + mTransformName.replacingOccurrences(of: "manager", with: "y"))].doubleValue, .Subscribe)
        }
    }
}

// MARK: - Event

//: extension AppleIAPManager {
extension SearchRequestDelegate {
    /// 初始化数据
    //: private func iap_initData() {
    private func cellData() {
        //: self.payCacheList = getLocalPayCacheList(payType: .Pay)
        self.payCacheList = note(payType: .Pay)
        //: self.subscribeCacheList = getLocalPayCacheList(payType: .Subscribe)
        self.subscribeCacheList = note(payType: .Subscribe)
        //: self.createOrderId = nil
        self.createOrderId = nil
    }

    /// 获取缓存列表
    /// - Parameter payType: 支付类型
    /// - Returns: 缓存列表
    //: private func getLocalPayCacheList(payType: ApplePayType) -> [[String: String]] {
    private func note(payType: GiftType) -> [[String: String]] {
        //: var list: [[String: String]]?
        var list: [[String: String]]?
        //: var diskPath = ""
        var diskPath = ""
        //: if payType == .Pay {
        if payType == .Pay {
            //: diskPath = getPayCachePath()
            diskPath = kickBackEqual()
            //: } else {
        } else {
            //: diskPath = getSubscribeCachePath()
            diskPath = paragraph()
        }

        //: if FileManager.default.fileExists(atPath: diskPath) {
        if FileManager.default.fileExists(atPath: diskPath) {
            //: list = NSKeyedUnarchiver.unarchiveObject(withFile: diskPath) as? [[String: String]]
            list = NSKeyedUnarchiver.unarchiveObject(withFile: diskPath) as? [[String: String]]
            //: if list == nil {
            if list == nil {
                //: try? FileManager.default.removeItem(atPath: diskPath)
                try? FileManager.default.removeItem(atPath: diskPath)
            }
        }
        //: if list == nil {
        if list == nil {
            //: list = [[String: String]]()
            list = [[String: String]]()
        }
        //: return list!
        return list!
    }

    /// 获取【购买】缓存路径【和uid关联】
    /// - Returns: 缓存路径
    //: private func getPayCachePath() -> String {
    private func kickBackEqual() -> String {
        //: let documentDirectoryPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        let documentDirectoryPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        //: let appDirectoryPath = (documentDirectoryPath as NSString).appendingPathComponent("App")
        let appDirectoryPath = (documentDirectoryPath as NSString).appendingPathComponent((String(app_dataPath)))

        //: let fileManager = FileManager.default
        let fileManager = FileManager.default
        //: if fileManager.fileExists(atPath: appDirectoryPath) == false {
        if fileManager.fileExists(atPath: appDirectoryPath) == false {
            //: try? fileManager.createDirectory(atPath: appDirectoryPath, withIntermediateDirectories: true)
            try? fileManager.createDirectory(atPath: appDirectoryPath, withIntermediateDirectories: true)
        }

        //: let uid = Defaults.string(forKey: TalkingLoginUidCacheKey) ?? ""
        let uid = dreamBottomMsg.string(forKey: k_messageStr) ?? ""
        //: let filePath = (appDirectoryPath as NSString).appendingPathComponent("OrderTransactionInfo_\(uid)")
        let filePath = (appDirectoryPath as NSString).appendingPathComponent((String(k_translatePath.suffix(5)) + String(mMaximumMessage.suffix(8)) + String(mainBackName)) + "\(uid)")
        //: return filePath
        return filePath
    }

    /// 获取【订阅】缓存路径【和uid关联】
    /// - Returns: 缓存路径
    //: private func getSubscribeCachePath() -> String {
    private func paragraph() -> String {
        //: let documentDirectoryPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        let documentDirectoryPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        //: let appDirectoryPath = (documentDirectoryPath as NSString).appendingPathComponent("App")
        let appDirectoryPath = (documentDirectoryPath as NSString).appendingPathComponent((String(app_dataPath)))

        //: let fileManager = FileManager.default
        let fileManager = FileManager.default
        //: if fileManager.fileExists(atPath: appDirectoryPath) == false {
        if fileManager.fileExists(atPath: appDirectoryPath) == false {
            //: try? fileManager.createDirectory(atPath: appDirectoryPath, withIntermediateDirectories: true)
            try? fileManager.createDirectory(atPath: appDirectoryPath, withIntermediateDirectories: true)
        }

        //: let uid = Defaults.string(forKey: TalkingLoginUidCacheKey) ?? ""
        let uid = dreamBottomMsg.string(forKey: k_messageStr) ?? ""
        //: let filePath = (appDirectoryPath as NSString).appendingPathComponent("OrderTransactionInfo_Subscribe_\(uid)")
        let filePath = (appDirectoryPath as NSString).appendingPathComponent(String(bytes: mainMakeId.map{rareEarthElement(intimate: $0)}, encoding: .utf8)! + "\(uid)")
        //: return filePath
        return filePath
    }

    /// 获取本地收据数据
    /// - Parameters:
    ///   - transactionId: 收据标识符
    ///   - payType: 支付类型
    /// - Returns: 收据数据
    //: fileprivate func getVerifyData(_ transactionId: String, _ payType: ApplePayType) -> String? {
    fileprivate func toData(_ transactionId: String, _ payType: GiftType) -> String? {
        // 有未完成的订单，先取缓存
        //: var paramsArr = [[String: String]]()
        var paramsArr = [[String: String]]()
        //: switch(payType) {
        switch payType {
        //: case .Pay:
        case .Pay:
            //: paramsArr = self.payCacheList.filter({$0["transactionId"] == transactionId})
            paramsArr = self.payCacheList.filter { $0[(app_valueKey.replacingOccurrences(of: "statute", with: "r") + "ctionI" + appFormatId.replacingOccurrences(of: "save", with: "d"))] == transactionId }
        //: case .Subscribe:
        case .Subscribe:
            //: paramsArr = self.subscribeCacheList.filter({$0["transactionId"] == transactionId})
            paramsArr = self.subscribeCacheList.filter { $0[(app_valueKey.replacingOccurrences(of: "statute", with: "r") + "ctionI" + appFormatId.replacingOccurrences(of: "save", with: "d"))] == transactionId }
        }
        //: if paramsArr.count > 0 && paramsArr.first!["verifyData"] != nil {
        if paramsArr.count > 0, paramsArr.first![String(bytes: app_toUrl.map{titleTop(section: $0)}, encoding: .utf8)!] != nil {
            //: return paramsArr.first!["verifyData"]
            return paramsArr.first![String(bytes: app_toUrl.map{titleTop(section: $0)}, encoding: .utf8)!]
        }

        // 取本地
        //: guard let receiptUrl = Bundle.main.appStoreReceiptURL else { return nil }
        guard let receiptUrl = Bundle.main.appStoreReceiptURL else { return nil }
        //: let data = NSData(contentsOf: receiptUrl)
        let data = NSData(contentsOf: receiptUrl)
        //: let receiptStr = data?.base64EncodedString(options: NSData.Base64EncodingOptions(rawValue: 0))
        let receiptStr = data?.base64EncodedString(options: NSData.Base64EncodingOptions(rawValue: 0))
        //: return receiptStr
        return receiptStr
    }
}

// MARK: - 失败重试流程

//: extension AppleIAPManager {
extension SearchRequestDelegate {
    /// 检测未完成的苹果支付【只会重试当前登录用户】
    //: func iap_checkUnfinishedTransactions() {
    func peek() {
        //: iap_initData()
        cellData()

        // 【购买】失败重试
        //: for dict in self.payCacheList {
        for dict in self.payCacheList {
            //: iap_failedRetry(dict["transactionId"], .Pay)
            retry(dict[(app_valueKey.replacingOccurrences(of: "statute", with: "r") + "ctionI" + appFormatId.replacingOccurrences(of: "save", with: "d"))], .Pay)
        }

        // 【订阅】失败重试
        //: for dict in self.subscribeCacheList {
        for dict in self.subscribeCacheList {
            //: iap_failedRetry(dict["transactionId"], .Subscribe)
            retry(dict[(app_valueKey.replacingOccurrences(of: "statute", with: "r") + "ctionI" + appFormatId.replacingOccurrences(of: "save", with: "d"))], .Subscribe)
        }
    }

    /// 失败重试
    /// - Parameters:
    ///   - transactionId: Id
    ///   - payType: 支付类型
    //: private func iap_failedRetry(_ transactionId: String?, _ payType: ApplePayType) {
    private func retry(_ transactionId: String?, _ payType: GiftType) {
        //: guard let transactionId = transactionId else { return }
        guard let transactionId = transactionId else { return }
        // 初始化每个交易请求次数
        //: reqRetryCountDict[transactionId] = 0
        reqRetryCountDict[transactionId] = 0
        // 3. 服务端校验流程
        //: transcationPurchasedToCheck(transactionId, payType)
        makeText(transactionId, payType)
    }
}

// MARK: - 苹果正常支付流程

//: extension AppleIAPManager {
extension SearchRequestDelegate {
    /// 发起苹果支付【1.创建订单； 2.发起苹果支付； 3.服务端校验】
    /// - Parameters:
    ///   - purchID: 产品ID
    ///   - payType: 支付类型
    ///   - handle: 回调
    ///   - source: 0 常规充值 1 观看视频后充值或订阅
    //: func iap_startPurchase(productId: String, payType: ApplePayType, source: Int = 0, handle: @escaping IAPcompletionHandle) {
    func shouldAdd(productId: String, payType: GiftType, source: Int = 0, handle: @escaping IAPcompletionHandle) {
        //: iap_initData()
        cellData()
        //: self.completionHandle = handle
        self.completionHandle = handle
        //: self.currentPayType = payType
        self.currentPayType = payType

        // 1. 根据类型创建订单
        //: switch(payType) {
        switch payType {
        //: case .Pay:
        case .Pay:
            //: req_pay_createAppleOrder(productId: productId, source: source) { [weak self] orderId, succeed in
            priceHandle(productId: productId, source: source) { [weak self] orderId, succeed in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: guard succeed == true && orderId != nil else {
                guard succeed == true && orderId != nil else { // 订单创建失败
                    //: self.completionHandle?(.createOrderFail, 0, .Pay)
                    self.completionHandle?(.createOrderFail, 0, .Pay)
                    //: return
                    return
                }

                //: self.createOrderId = orderId
                self.createOrderId = orderId
                //: self.requestProductInfo(productId)
                self.requestInfo(productId)
            }

        //: case .Subscribe:
        case .Subscribe:
            //: req_subscribe_createAppleOrder(productId: productId, source: source) { [weak self] orderId, succeed in
            product(productId: productId, source: source) { [weak self] orderId, succeed in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: guard succeed == true && orderId != nil else {
                guard succeed == true && orderId != nil else { // 订单创建失败
                    //: self.completionHandle?(.createOrderFail, 0, .Subscribe)
                    self.completionHandle?(.createOrderFail, 0, .Subscribe)
                    //: return
                    return
                }

                //: self.createOrderId = orderId
                self.createOrderId = orderId
                //: self.requestProductInfo(productId)
                self.requestInfo(productId)
            }
        }
    }

    // 2 发起苹果支付，查询apple内购商品
    //: fileprivate func requestProductInfo(_ productId: String) {
    fileprivate func requestInfo(_ productId: String) {
        //: guard SKPaymentQueue.canMakePayments() else {
        guard SKPaymentQueue.canMakePayments() else {
            //: UploadLogTool.writeLog(msg: "IAP查询内购商品失败, productId:\(productId).")
            LabelLogTool.fillInLine(msg: (showMakeMessage + "\u{8be2}\u{5185}购商品" + String(user_sizeFormat.suffix(5)) + "roduc" + String(m_pageUrl.prefix(4))) + "\(productId).")
            //: self.completionHandle?(.notArrow, 0, currentPayType)
            self.completionHandle?(.notArrow, 0, currentPayType)
            //: return
            return
        }

        // 销毁当前请求
        //: self.clearProductInfoRequest()
        self.receiveAdd()
        // 查询apple内购商品
        //: let identifiers: Set<String> = [productId]
        let identifiers: Set<String> = [productId]
        //: productInfoReq = SKProductsRequest(productIdentifiers: identifiers)
        productInfoReq = SKProductsRequest(productIdentifiers: identifiers)
        //: productInfoReq?.delegate = self
        productInfoReq?.delegate = self
        //: productInfoReq?.start()
        productInfoReq?.start()
    }

    // 销毁当前请求
    //: fileprivate func clearProductInfoRequest() {
    fileprivate func receiveAdd() {
        //: guard productInfoReq != nil else { return }
        guard productInfoReq != nil else { return }
        //: productInfoReq?.delegate = nil
        productInfoReq?.delegate = nil
        //: productInfoReq?.cancel()
        productInfoReq?.cancel()
        //: productInfoReq = nil
        productInfoReq = nil
    }
}

// MARK: - SKProductsRequestDelegate【商品查询】

//: extension AppleIAPManager: SKProductsRequestDelegate {
extension SearchRequestDelegate: SKProductsRequestDelegate {
    // 查询apple内购商品成功回调
    //: func productsRequest(_ request: SKProductsRequest, didReceive response: SKProductsResponse) {
    func productsRequest(_: SKProductsRequest, didReceive response: SKProductsResponse) {
        //: guard response.products.count > 0 else {
        guard response.products.count > 0 else {
            //: UploadLogTool.writeLog(msg: "IAP查询内购商品失败, products.count为0.")
            LabelLogTool.fillInLine(msg: (String(appRecordName) + String(dream_sectionText.prefix(5)) + mainByName))
            //: self.completionHandle?( .noProductId, 0, currentPayType)
            self.completionHandle?(.noProductId, 0, currentPayType)
            //: return
            return
        }

        //: let payment = SKPayment(product: response.products.first!)
        let payment = SKPayment(product: response.products.first!)
        //: SKPaymentQueue.default().add(payment)
        SKPaymentQueue.default().add(payment)
    }

    // 查询apple内购商品失败
    //: func request(_ request: SKRequest, didFailWithError error: Error) {
    func request(_: SKRequest, didFailWithError error: Error) {
        //: UploadLogTool.writeLog(msg: "IAP查询内购商品失败, Error:\(error).")
        LabelLogTool.fillInLine(msg: (mainNameVoiceFormat + "内购\u{5546}品失" + main_sinceIdent.replacingOccurrences(of: "active", with: "败") + String(k_workTitleToUrl)) + "\(error).")
        //: self.completionHandle?( .noProductId, 0, currentPayType)
        self.completionHandle?(.noProductId, 0, currentPayType)
    }

    // 查询apple内购商品完成
    //: func requestDidFinish(_ request: SKRequest) {
    func requestDidFinish(_: SKRequest) {}
}

// MARK: - SKPaymentTransactionObserver【支付回调】

//: extension AppleIAPManager: SKPaymentTransactionObserver {
extension SearchRequestDelegate: SKPaymentTransactionObserver {
    /// 2.2 apple内购完成回调
    //: func paymentQueue(_ queue: SKPaymentQueue, updatedTransactions transactions: [SKPaymentTransaction]) {
    func paymentQueue(_: SKPaymentQueue, updatedTransactions transactions: [SKPaymentTransaction]) {
        //: for transaction in transactions {
        for transaction in transactions {
            //: switch transaction.transactionState {
            switch transaction.transactionState {
            //: case .purchasing:
            case .purchasing: // 交易中
                //: break
                break
            //: case .purchased:
            case .purchased: // 交易成功
                /**
                 original.transactionIdentifier 首次订阅时为nil，transaction.transactionIdentifier有值；
                 后续自动订阅、续订时，original.transactionIdentifier为首次订阅时生成的transaction.transactionIdentifier，值固定不变；
                 每次订阅transaction.transactionIdentifier都不一样，为当前交易的标识；
                 */
                //: if transaction.original != nil && createOrderId == nil {
                if transaction.original != nil && createOrderId == nil { // 启动自动续订时，不需要调用服务端验证接口
                    //: self.completionHandle?(.renewSucceed, 0, currentPayType)
                    self.completionHandle?(.renewSucceed, 0, currentPayType)
                    //: } else {
                } else { // 普通购买和订阅
                    //: UploadLogTool.writeLog(msg: "IAP-purchased:\(currentPayType), 订单号:\(String(describing: createOrderId)), 商品Id:\(transaction.payment.productIdentifier), 苹果transId:\(String(describing: transaction.transactionIdentifier)).")
                    LabelLogTool.fillInLine(msg: (kProfileFormat.replacingOccurrences(of: "temp", with: "A") + String(kControlRangeUrl)) + "\(currentPayType)" + (String(showReplyName)) + "\(String(describing: createOrderId))" + (kProgressTableData + String(appAcceptMsg.suffix(2))) + "\(transaction.payment.productIdentifier)" + (String(userDayListName)) + "\(String(describing: transaction.transactionIdentifier)).")
                    // 初始化每个交易请求次数
                    //: reqRetryCountDict[transaction.transactionIdentifier!] = 0
                    reqRetryCountDict[transaction.transactionIdentifier!] = 0
                    // 3. 服务端校验流程
                    //: transcationPurchasedToCheck(transaction.transactionIdentifier!, self.currentPayType)
                    makeText(transaction.transactionIdentifier!, self.currentPayType)
                }
                // 移除苹果支付系统缓存
                //: SKPaymentQueue.default().finishTransaction(transaction)
                SKPaymentQueue.default().finishTransaction(transaction)
                //: createOrderId = nil
                createOrderId = nil
            //: case .failed:
            case .failed: // 交易失败/取消
                //: SKPaymentQueue.default().finishTransaction(transaction)
                SKPaymentQueue.default().finishTransaction(transaction)
                //: UploadLogTool.writeLog(msg: "IAP-failed:\(currentPayType), 订单号:\(String(describing: createOrderId)), 商品Id:\(transaction.payment.productIdentifier), 苹果transId:\(String(describing: transaction.transactionIdentifier)).")
                LabelLogTool.fillInLine(msg: (String(main_recordTitle)) + "\(currentPayType)" + (String(showReplyName)) + "\(String(describing: createOrderId))" + (kProgressTableData + String(appAcceptMsg.suffix(2))) + "\(transaction.payment.productIdentifier)" + (String(userDayListName)) + "\(String(describing: transaction.transactionIdentifier)).")
                //: self.completionHandle?(.failed, 0, currentPayType)
                self.completionHandle?(.failed, 0, currentPayType)
                //: createOrderId = nil
                createOrderId = nil
            //: case .restored:
            case .restored: // 已购买过该商品
                //: SKPaymentQueue.default().finishTransaction(transaction)
                SKPaymentQueue.default().finishTransaction(transaction)
                //: self.completionHandle?(.restored, 0, currentPayType)
                self.completionHandle?(.restored, 0, currentPayType)
                //: createOrderId = nil
                createOrderId = nil
            //: case .deferred:
            case .deferred: // 交易延期
                //: SKPaymentQueue.default().finishTransaction(transaction)
                SKPaymentQueue.default().finishTransaction(transaction)
                //: UploadLogTool.writeLog(msg: "IAP-deferred:\(currentPayType), 订单号:\(String(describing: createOrderId)), 商品Id:\(transaction.payment.productIdentifier), 苹果transId:\(String(describing: transaction.transactionIdentifier)).")
                LabelLogTool.fillInLine(msg: (String(app_toolKey.prefix(7)) + String(main_modifyMessageLocationMsg) + String(notiSectionStr)) + "\(currentPayType)" + (String(showReplyName)) + "\(String(describing: createOrderId))" + (kProgressTableData + String(appAcceptMsg.suffix(2))) + "\(transaction.payment.productIdentifier)" + (String(userDayListName)) + "\(String(describing: transaction.transactionIdentifier)).")
                //: self.completionHandle?(.deferred, 0, currentPayType)
                self.completionHandle?(.deferred, 0, currentPayType)
                //: createOrderId = nil
                createOrderId = nil
            //: @unknown default:
            @unknown default:
                //: SKPaymentQueue.default().finishTransaction(transaction)
                SKPaymentQueue.default().finishTransaction(transaction)
                //: self.completionHandle?(.unknow, 0, currentPayType)
                self.completionHandle?(.unknow, 0, currentPayType)
                //: createOrderId = nil
                createOrderId = nil
                //: UploadLogTool.writeLog(msg: "IAP未知交易类型:\(currentPayType), 订单号:\(String(describing: createOrderId)), 商品Id:\(transaction.payment.productIdentifier), 苹果transId:\(String(describing: transaction.transactionIdentifier)).")
                LabelLogTool.fillInLine(msg: (mainAvailablePath + mMarginFoodUrl.replacingOccurrences(of: "content", with: "型")) + "\(currentPayType)" + (String(showReplyName)) + "\(String(describing: createOrderId))" + (kProgressTableData + String(appAcceptMsg.suffix(2))) + "\(transaction.payment.productIdentifier)" + (String(userDayListName)) + "\(String(describing: transaction.transactionIdentifier)).")
                //: fatalError(" 未知的交易类型")
                fatalError((noti_quoteMessage.capitalized + String(mainBotMsg)))
            }
        }
    }

    /// 3. 服务端校验流程
    /// - Parameters:
    ///   - transactionId: 交易唯一标识符
    ///   - payType: 支付类型
    //: fileprivate func transcationPurchasedToCheck(_ transactionId: String, _ payType: ApplePayType) {
    fileprivate func makeText(_ transactionId: String, _ payType: GiftType) {
        //: guard let receiptStr = getVerifyData(transactionId, payType) else {
        guard let receiptStr = toData(transactionId, payType) else {
            //: UploadLogTool.writeLog(msg: "IAP获取本地收据数据失败, transactionId:\(transactionId), orderId:\(String(describing: createOrderId)), payType:\(payType).")
            LabelLogTool.fillInLine(msg: (String(notiUserContent) + String(kTargetTitle.prefix(5))) + "\(transactionId)" + (String(mainImageTitle.suffix(5)) + "erId:") + "\(String(describing: createOrderId))" + (String(mAddMsg) + String(show_femaleText)) + "\(payType).")
            //: self.completionHandle?(.verityFail, 0, payType)
            self.completionHandle?(.verityFail, 0, payType)
            //: return
            return
        }

        // 缓存支付成功信息，防止接口校验失败
        //: if createOrderId != nil {
        if createOrderId != nil { // 正常支付流程
            //: switch(payType) {
            switch payType {
            //: case .Pay:
            case .Pay:
                //: if self.payCacheList.filter({$0["transactionId"] == transactionId || $0["orderId"] == createOrderId}).count == 0 {  // 防止重复添加缓存数据

                if self.payCacheList.filter({ $0[(app_valueKey.replacingOccurrences(of: "statute", with: "r") + "ctionI" + appFormatId.replacingOccurrences(of: "save", with: "d"))] == transactionId || $0[String(bytes: mDetailUrl.map{imageTask(bring: $0)}, encoding: .utf8)!] == createOrderId }).count == 0 { // 防止重复添加缓存数据
                    //: let cacheDict = ["transactionId": transactionId,
                    let cacheDict = [(app_valueKey.replacingOccurrences(of: "statute", with: "r") + "ctionI" + appFormatId.replacingOccurrences(of: "save", with: "d")): transactionId,
                                     //: "orderId": createOrderId!,
                                     String(bytes: mDetailUrl.map{imageTask(bring: $0)}, encoding: .utf8)!: createOrderId!,
                                     //: "verifyData": receiptStr]
                                     String(bytes: app_toUrl.map{titleTop(section: $0)}, encoding: .utf8)!: receiptStr]
                    //: self.payCacheList.append(cacheDict)
                    self.payCacheList.append(cacheDict)
                    //: let diskPath = self.getPayCachePath()
                    let diskPath = self.kickBackEqual()
                    //: NSKeyedArchiver.archiveRootObject(self.payCacheList, toFile: diskPath)
                    NSKeyedArchiver.archiveRootObject(self.payCacheList, toFile: diskPath)
                }

            //: case .Subscribe:
            case .Subscribe:
                //: if self.subscribeCacheList.filter({$0["transactionId"] == transactionId || $0["orderId"] == createOrderId}).count == 0 { // 防止重复添加缓存数据

                if self.subscribeCacheList.filter({ $0[(app_valueKey.replacingOccurrences(of: "statute", with: "r") + "ctionI" + appFormatId.replacingOccurrences(of: "save", with: "d"))] == transactionId || $0[String(bytes: mDetailUrl.map{imageTask(bring: $0)}, encoding: .utf8)!] == createOrderId }).count == 0 { // 防止重复添加缓存数据
                    //: let cacheDict = ["transactionId": transactionId,
                    let cacheDict = [(app_valueKey.replacingOccurrences(of: "statute", with: "r") + "ctionI" + appFormatId.replacingOccurrences(of: "save", with: "d")): transactionId,
                                     //: "orderId": createOrderId!,
                                     String(bytes: mDetailUrl.map{imageTask(bring: $0)}, encoding: .utf8)!: createOrderId!,
                                     //: "verifyData": receiptStr]
                                     String(bytes: app_toUrl.map{titleTop(section: $0)}, encoding: .utf8)!: receiptStr]
                    //: self.subscribeCacheList.append(cacheDict)
                    self.subscribeCacheList.append(cacheDict)
                    //: let diskPath = self.getSubscribeCachePath()
                    let diskPath = self.paragraph()
                    //: NSKeyedArchiver.archiveRootObject(self.subscribeCacheList, toFile: diskPath)
                    NSKeyedArchiver.archiveRootObject(self.subscribeCacheList, toFile: diskPath)
                }
            }
        }

        // 限制交易重试最大次数
        //: var reqCount = reqRetryCountDict[transactionId] ?? 0
        var reqCount = reqRetryCountDict[transactionId] ?? 0
        //: reqCount += 1
        reqCount += 1
        //: reqRetryCountDict[transactionId] = reqCount
        reqRetryCountDict[transactionId] = reqCount
        //: if reqCount > APPLE_IAP_MAX_RETRY_COUNT {
        if reqCount > mKeyPingContent {
            //: self.completionHandle?(.verityFail, 0, payType)
            self.completionHandle?(.verityFail, 0, payType)
            //: return
            return
        }

        // 3.服务端校验，根据transactionId从缓存中取
        //: switch(payType) {
        switch payType {
        //: case .Pay:
        case .Pay:
            //: let paramsArr = self.payCacheList.filter({$0["transactionId"] == transactionId})
            let paramsArr = self.payCacheList.filter { $0[(app_valueKey.replacingOccurrences(of: "statute", with: "r") + "ctionI" + appFormatId.replacingOccurrences(of: "save", with: "d"))] == transactionId }
            //: guard paramsArr.count > 0 else { return }
            guard paramsArr.count > 0 else { return }
            //: req_pay_uploadAppletransaction(transactionId, params: paramsArr.first!)
            buttonParams(transactionId, params: paramsArr.first!)

        //: case .Subscribe:
        case .Subscribe:
            //: let paramsArr = self.subscribeCacheList.filter({$0["transactionId"] == transactionId})
            let paramsArr = self.subscribeCacheList.filter { $0[(app_valueKey.replacingOccurrences(of: "statute", with: "r") + "ctionI" + appFormatId.replacingOccurrences(of: "save", with: "d"))] == transactionId }
            //: guard paramsArr.count > 0 else { return }
            guard paramsArr.count > 0 else { return }
            //: req_subscribe_uploadAppletransaction(transactionId, params: paramsArr.first!)
            mediumApp(transactionId, params: paramsArr.first!)
        }
    }
}