
//: Declare String Begin

/*: "nil" :*/
fileprivate let dream_toName:String = "nititle"

/*: "Edit profiles" :*/
fileprivate let app_colorFormat:[Character] = ["E","d","i","t"," "]
fileprivate let userLabelText:[Character] = ["p","r","o"]
fileprivate let app_requestSizeStr:[Character] = ["f","i","l","e","s"]

/*: "Done" :*/
fileprivate let notiInfoId:[Character] = ["D","o","n","e"]

/*: "#999999" :*/
fileprivate let noti_makeKey:String = "#resultresultresultresult"
fileprivate let mTopTitle:String = "direction"

/*: "uid" :*/
fileprivate let appTableStr:[Character] = ["u","i","d"]

/*: "AppTitleCell" :*/
fileprivate let dreamEqualIdent:[Character] = ["T","a","l","k","i","n","g"]
fileprivate let userCenterKey:String = "self conversation selfEditNa"
fileprivate let show_toTitle:String = "meCellsection content"

/*: "LabelManagerViewCell" :*/
fileprivate let userCompareAtCellData:String = "Talkinback count equal privacy"
fileprivate let k_equalHeadKey:[Character] = ["g","E","d","i","t","S","i","g","n","C","e","l","l"]

/*: "Save the change?" :*/
fileprivate let k_modelLoadMessage:[Character] = ["S","a","v","e"," ","t","h","e"]
fileprivate let appMakeTapValue:[Character] = [" ","c","h","a","n","g","e","?"]

/*: "Cancel" :*/
fileprivate let userPartyRenderPath:String = "Cancelpath view true color"

/*: "Save" :*/
fileprivate let m_videoMainPath:String = "Savevar return data"

/*: "nickname" :*/
fileprivate let userAtIdent:[Character] = ["n","i","c","k","n","a","m","e"]

/*: "birthday" :*/
fileprivate let k_actionIdent:[Character] = ["b","i","r","t","h"]
fileprivate let m_requestData:String = "darray"

/*: "desc" :*/
fileprivate let show_maleValue:String = "peresc"

/*: "Modify the success" :*/
fileprivate let userComponentKey:[Character] = ["M","o","d","i","f","y"," "]
fileprivate let showPlayerText:[Character] = ["t"]
fileprivate let dreamScreenId:String = "he srender make range level"

/*: "pic" :*/
fileprivate let dreamLabelColorMsg:String = "retic"

/*: "[ :*/
fileprivate let m_makeValue:String = "["

/*: ]" :*/
fileprivate let main_sizeValue:[Character] = ["]"]

/*: "pids" :*/
fileprivate let dreamById:String = "pidsucceed"

/*: "aboutMe" :*/
fileprivate let noti_canIdent:[Character] = ["a","b"]
fileprivate let kBlockContent:[Character] = ["o","u","t","M","e"]

/*: "interest" :*/
fileprivate let dreamMakeData:[Character] = ["i","n","t","e","r","e"]
fileprivate let mainStatusName:String = "maintain"

/*: "tagIds" :*/
fileprivate let mTransactionName:String = "tagIdthird true make"
fileprivate let kCheckedUrl:[Character] = ["s"]

/*: "category" :*/
fileprivate let notiColorKey:String = "flexibletegor"
fileprivate let userMessageData:String = "mode"

/*: "An error occurred, please try again" :*/
fileprivate let k_pathKey:[UInt8] = [0x6e,0x69,0x61,0x67,0x61,0x20,0x79,0x72,0x74,0x20,0x65,0x73,0x61,0x65,0x6c,0x70,0x20,0x2c,0x64,0x65,0x72,0x72,0x75,0x63,0x63,0x6f,0x20,0x72,0x6f,0x72,0x72,0x65,0x20,0x6e,0x41]

/*: "imgUrl" :*/
fileprivate let main_textMsg:String = "imgUrcorner make as"
fileprivate let mainAppKey:[Character] = ["l"]

/*: "videoUrl" :*/
fileprivate let noti_imageMsg:String = "index"
fileprivate let dreamViewRowTitle:String = "ideoUrlresult event let"

/*: "Video cover submitted successfully" :*/
fileprivate let mAcceptContent:[UInt8] = [0x79,0x6c,0x6c,0x75,0x66,0x73,0x73,0x65,0x63,0x63,0x75,0x73,0x20,0x64,0x65,0x74,0x74,0x69,0x6d,0x62,0x75,0x73,0x20,0x72,0x65,0x76,0x6f,0x63,0x20,0x6f,0x65,0x64,0x69,0x56]

/*: "WillThen" :*/
fileprivate let show_sizePath:[Character] = ["T","a","l","k","i","n","g","E","d","i","t","V","i","d"]
fileprivate let kToLiveValue:[Character] = ["e","o","C","e","l","l"]

/*: "video_url" :*/
fileprivate let mainLabelTitle:[Character] = ["v","i","d","e","o","_","u","r","l"]

/*: "img_url" :*/
fileprivate let userModelStr:[Character] = ["i","m","g","_","u","r","l"]

/*: "status" :*/
fileprivate let appDataPath:String = "sclicktus"

/*: "LabelValueView" :*/
fileprivate let mWithStr:String = "status title startTalk"
fileprivate let kAfterId:[Character] = ["o"]
fileprivate let app_opUrl:String = "top contenttoCell"

/*: "DirectionObjectProtocol" :*/
fileprivate let userHiddenWarnIdent:[Character] = ["T","a","l","k","i","n"]
fileprivate let showEventMiniTitle:String = "gEditBcolor hidden content equal size"
fileprivate let kToPath:String = "dayCellobserver shared view"

/*: "CheckView" :*/
fileprivate let mShareKey:String = "image colorTalki"
fileprivate let k_equalData:String = "self bottom totAboutMe"
fileprivate let kAppId:[Character] = ["C","e","l","l"]

/*: "UITableViewCell" :*/
fileprivate let mainIconUrl:[Character] = ["U","I","T","a","b","l","e","V","i","e","w","C","e","l","l"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  DataProfilesVc.swift
//  AbroadTalking
//
//  Created by Charlotte on 2022/8/30.
//

//: import UIKit
import UIKit

//: class TalkingEditProfilesVC: TalkingBaseViewController {
class DataProfilesVc: ListRecognizerDelegate {
	var toteUpTitle: String = "greet"
	var tableViewDictionary: [AnyHashable: String] = [:]
	var collectionOff: Bool = true
	var menuNumber: Double = -69.2
	var taskArray: [AnyHashable] = []
	var buttonTitle: String = "data"
	var addDictionary: [AnyHashable: String] = [:]

    //: var seleteAboutme: [UserSeleteTagModel] = []
    var seleteAboutme: [VideoTagModel] = []
    //: var seleteInters: [UserSeleteTagModel] = []
    var seleteInters: [VideoTagModel] = []

    //: var isdeleteAboutme = false
    var isdeleteAboutme = false
    //: var isdeleteInters = false
    var isdeleteInters = false

    //: var isTips = false
    var isTips = false

    //: var PhotoArray: [TalkingUserInfoGalleryModel] = []
    var PhotoArray: [LabGalleryModel] = []
    //: var seletePhotoArray: [UIImage] = []
    var seletePhotoArray: [UIImage] = []
    //: var DeletePhotoArray: [String] = []
    var DeletePhotoArray: [String] = []

    //: var nameStr: String = ""
    var nameStr: String = ""
    //: var birthday: String = ""
    var birthday: String = ""
    //: var signStr: String = "nil"
    var signStr: String = (dream_toName.replacingOccurrences(of: "title", with: "l"))

    //: private var VideoIconPath = ""
    private var VideoIconPath = ""
    //: private var videoPath = ""
    private var videoPath = ""
    //: private var videoStatus = -3
    private var videoStatus = -3 // 视频审核状态：0待审核 1审核通过 -1审核拒绝 -3未上传
    //: private var isChangeVideo = false
    private var isChangeVideo = false

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = false
        self.hideNavi = false
        //: self.view.backgroundColor = UIColor.appBgColor()
        self.view.backgroundColor = UIColor.titleIndex()
        //: self.title = "Edit profiles".localized
        self.title = (String(app_colorFormat) + String(userLabelText) + String(app_requestSizeStr)).localized

        //: let btn = UIButton(frame: CGRect(x: 0, y: 0, width: 40, height: 18))
        let btn = UIButton(frame: CGRect(x: 0, y: 0, width: 40, height: 18))
        //: btn.setTitle("Done".localized, for: .normal)
        btn.setTitle((String(notiInfoId)).localized, for: .normal)
        //: btn.setTitleColor(UIColor.init(hex: "#999999"), for: .normal)
        btn.setTitleColor(UIColor(hex: (noti_makeKey.replacingOccurrences(of: "result", with: "9") + mTopTitle.replacingOccurrences(of: "direction", with: "99"))), for: .normal)
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 15)
        btn.titleLabel?.font = UIFont.bindFor(type: .Medium, fontSize: 15)
        //: btn.addTarget(self, action: #selector(doneBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(checked), for: .touchUpInside)
        //: let item = UIBarButtonItem(customView: btn)
        let item = UIBarButtonItem(customView: btn)
        //: self.navigationItem.rightBarButtonItem=item
        self.navigationItem.rightBarButtonItem = item

        //: seleteAboutme = PathAppManager.share.loginUserMode.aboutMe!
        seleteAboutme = PathAppManager.share.loginUserMode.aboutMe!
        //: seleteInters = PathAppManager.share.loginUserMode.interest!
        seleteInters = PathAppManager.share.loginUserMode.interest!
        //: designView()
        design()
        //: GetGallery()
        roundEqual()
        //: getVideoInfo()
        thenCheck()

        //: NotificationCenter.default.addObserver(self, selector: #selector(keyboardShowBeHidden(notification:)), name: UIResponder.keyboardWillShowNotification, object: nil)
        NotificationCenter.default.addObserver(self, selector: #selector(equalNotification(notification:)), name: UIResponder.keyboardWillShowNotification, object: nil)
    
            if (self.hidesBottomBarWhenPushed) && (self.nibName != nil && self.nibName!.hasSuffix("cell")) {
                //: SWIFT_CUSTOM_DANGER_File_Call
                let queryProfile = TableWithController()

            queryProfile.digitizerText = nameStr
            
            queryProfile.pathText = { [self] fromTitle in
            self.toteUpTitle = fromTitle
            
            var queryProfile = VideoIconPath
            queryProfile.append(queryProfile[queryProfile.startIndex])
            if queryProfile.hasPrefix(self.toteUpTitle) {
                self.toteUpTitle = queryProfile
            }
            
            return self.toteUpTitle
            }
            queryProfile.whoDictionary = { [self] appearDictionary in
            self.tableViewDictionary = appearDictionary
            
            guard var value = self.tableViewDictionary as? [String: String] else {
                return nil
            }
            return value
            }
                self.navigationController?.pushViewController(queryProfile.self, animated: false)
            }

	}

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
    
            if (MainTable.layer.isDoubleSided != true) && (MainTable.viewWithTag(3353) != nil) {
                //: SWIFT_CUSTOM_DANGER_File_Call
                let positionDismiss = TableWithView()
            positionDismiss.livelihoodCount = videoStatus

            
            
            positionDismiss.saddleOff = { [self] rangeAboutClose in
            self.collectionOff = rangeAboutClose
            
            var positionDismiss = self.isdeleteInters
            positionDismiss = true
            if positionDismiss != self.collectionOff {
                self.collectionOff = positionDismiss
            }
            
                self.collectionOff = false
                self.collectionOff = true
            return self.collectionOff
            }
            positionDismiss.titleCount = { [self] counterchangeMagnitude in
            self.menuNumber = counterchangeMagnitude
            
            return self.menuNumber
            }
            positionDismiss.getCrackingArray = { [self] visualArray in
            self.taskArray = visualArray
            
            guard var value = self.taskArray as? [String] else {
                return nil
            }
            return value
            }
                MainTable.addSubview(positionDismiss)
            }

	}

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
    
            if (self.hidesBottomBarWhenPushed) && (self.nibName != nil && self.nibName!.hasSuffix("cell")) {
                //: SWIFT_CUSTOM_DANGER_File_Call
                let queryProfile = TableWithController()

            queryProfile.digitizerText = VideoIconPath
            
            queryProfile.pathText = { [self] fromTitle in
            self.buttonTitle = fromTitle
            
            var queryProfile = birthday
            queryProfile.append(queryProfile[queryProfile.startIndex])
            if queryProfile.hasPrefix(self.buttonTitle) {
                self.buttonTitle = queryProfile
            }
            
            return self.buttonTitle
            }
            queryProfile.whoDictionary = { [self] appearDictionary in
            self.addDictionary = appearDictionary
            
            guard var value = self.addDictionary as? [String: String] else {
                return nil
            }
            return value
            }
                self.navigationController?.pushViewController(queryProfile.self, animated: false)
            }

	}

    //: deinit {
    deinit {
        //: ProgressHUD.dismiss()
        ShirtButtonThen.allowButton()
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }

    // MARK: - Lazy load

    //: lazy var MainTable: UITableView = {
    lazy var MainTable: UITableView = {
        //: let table = UITableView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight), style: UITableView.Style.plain)
        let table = UITableView(frame: CGRect(x: 0, y: 0, width: app_screenFailMarginFormat, height: mainAcceptKey), style: UITableView.Style.plain)
        //: table.backgroundColor = UIColor.clear
        table.backgroundColor = UIColor.clear
        //: return table
        return table
        //: }()
    }()
}

// MARK: - Laod data

//: extension TalkingEditProfilesVC {
extension DataProfilesVc {
    //: func GetGallery() {
    func roundEqual() {
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["uid"] = PathAppManager.share.loginUserMode.userID
        dict[(String(appTableStr))] = PathAppManager.share.loginUserMode.userID
        //: TalkingMeRequestTool.req_GalleryByUid(params: dict) { succeed, result, errorModel in
        GiftReactiveCompatible.titleIn(params: dict) { succeed, result, _ in
            //: if succeed {
            if succeed {
                //: let array: Array =  result as! Array<Any>
                let array: Array = result as! [Any]
                //: if array.count>0 {
                if array.count > 0 {
                    //: self.PhotoArray.removeAll()
                    self.PhotoArray.removeAll()
                }
                //: if let datas = Array<TalkingUserInfoGalleryModel>.deserialize(from: array as? Array) {
                if let datas = Array<LabGalleryModel>.deserialize(from: array as? Array) {
                    //: self.PhotoArray.append(contentsOf: (datas as? [TalkingUserInfoGalleryModel])!)
                    self.PhotoArray.append(contentsOf: (datas as? [LabGalleryModel])!)
                }
                //: self.MainTable.reloadData()
                self.MainTable.reloadData()
            }
        }
    }

    //: override func naviPopback() {
    override func noPull() {
        //: for i in MainTable.visibleCells {
        for i in MainTable.visibleCells {
            //: if i.reuseIdentifier == "TalkingEditNameCell" {
            if i.reuseIdentifier == (String(dreamEqualIdent) + String(userCenterKey.suffix(6)) + String(show_toTitle.prefix(6))) {
                //: let namecell: TalkingEditNameCell = i as! TalkingEditNameCell
                let namecell: AppTitleCell = i as! AppTitleCell
                //: namecell.nameTF.resignFirstResponder()
                namecell.nameTF.resignFirstResponder()
            }
            //: if i.reuseIdentifier == "TalkingEditSignCell" {
            if i.reuseIdentifier == (String(userCompareAtCellData.prefix(6)) + String(k_equalHeadKey)) {
                //: let namecell: TalkingEditSignCell = i as! TalkingEditSignCell
                let namecell: LabelManagerViewCell = i as! LabelManagerViewCell
                //: namecell.textView.resignFirstResponder()
                namecell.textView.resignFirstResponder()
            }
        }
        //: if (nameStr.count > 0&&PathAppManager.share.loginUserMode.nickname != nameStr)||( birthday.count > 0&&PathAppManager.share.loginUserMode.birthday != self.birthday)||(signStr != "nil" && PathAppManager.share.loginUserMode.signature != signStr)||self.seletePhotoArray.count>0||self.DeletePhotoArray.count>0||isdeleteAboutme||isdeleteInters || isChangeVideo {
        if (nameStr.count > 0 && PathAppManager.share.loginUserMode.nickname != nameStr) || (birthday.count > 0 && PathAppManager.share.loginUserMode.birthday != self.birthday) || (signStr != (dream_toName.replacingOccurrences(of: "title", with: "l")) && PathAppManager.share.loginUserMode.signature != signStr) || self.seletePhotoArray.count > 0 || self.DeletePhotoArray.count > 0 || isdeleteAboutme || isdeleteInters || isChangeVideo {
            //: let config = ShowAlertConfig()
            let config = KeyCustomAlertConfig()
            //: config.alignment = .center
            config.alignment = .center
            //: TalkingAlertShow.customAlert(message: "Save the change?".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Save".localized, leftBlock: { [weak self] in
            CourseAlertShow.inkpad(message: (String(k_modelLoadMessage) + String(appMakeTapValue)).localized, leftBtnTitle: (String(userPartyRenderPath.prefix(6))).localized, rightBtnTitle: (String(m_videoMainPath.prefix(4))).localized, leftBlock: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: TalkingAlertShow.hideAlert()
                CourseAlertShow.cardWindow()
                //: self.navigationController?.popViewController(animated: true)
                self.navigationController?.popViewController(animated: true)

                //: }, rightBlock: { [weak self] in
            }, rightBlock: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: TalkingAlertShow.hideAlert()
                CourseAlertShow.cardWindow()
                //: self.uploadTool(isBack: true)
                self.componentDown(isBack: true)
                //: }, config: config)
            }, config: config)
            //: } else {
        } else {
            //: self.navigationController?.popViewController(animated: true)
            self.navigationController?.popViewController(animated: true)
        }
    }

    //: @objc func doneBtnClick() {
    @objc func checked() {
        //: uploadTool(isBack: false)
        componentDown(isBack: false)
    }

    //: func uploadTool(isBack: Bool) {
    func componentDown(isBack: Bool) {
        //: for i in MainTable.visibleCells {
        for i in MainTable.visibleCells {
            //: if i.reuseIdentifier == "TalkingEditNameCell" {
            if i.reuseIdentifier == (String(dreamEqualIdent) + String(userCenterKey.suffix(6)) + String(show_toTitle.prefix(6))) {
                //: let namecell: TalkingEditNameCell = i as! TalkingEditNameCell
                let namecell: AppTitleCell = i as! AppTitleCell
                //: namecell.nameTF.resignFirstResponder()
                namecell.nameTF.resignFirstResponder()
            }
            //: if i.reuseIdentifier == "TalkingEditSignCell" {
            if i.reuseIdentifier == (String(userCompareAtCellData.prefix(6)) + String(k_equalHeadKey)) {
                //: let namecell: TalkingEditSignCell = i as! TalkingEditSignCell
                let namecell: LabelManagerViewCell = i as! LabelManagerViewCell
                //: namecell.textView.resignFirstResponder()
                namecell.textView.resignFirstResponder()
            }
        }

        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()

        //: if nameStr.count > 0 && PathAppManager.share.loginUserMode.nickname != nameStr {
        if nameStr.count > 0, PathAppManager.share.loginUserMode.nickname != nameStr {
            //: dict["nickname"] = nameStr
            dict[(String(userAtIdent))] = nameStr
        }
        //: if birthday.count > 0 && PathAppManager.share.loginUserMode.birthday != birthday {
        if birthday.count > 0, PathAppManager.share.loginUserMode.birthday != birthday {
            //: dict["birthday"] = birthday
            dict[(String(k_actionIdent) + m_requestData.replacingOccurrences(of: "array", with: "ay"))] = birthday
        }
        //: if signStr != "nil" && PathAppManager.share.loginUserMode.signature != signStr {
        if signStr != (dream_toName.replacingOccurrences(of: "title", with: "l")), PathAppManager.share.loginUserMode.signature != signStr {
            //: dict["desc"] = signStr
            dict[(show_maleValue.replacingOccurrences(of: "per", with: "d"))] = signStr
        }

        //: photoAndTagTool(BackT: isBack, isTips: dict.count == 0)
        moreResponse(BackT: isBack, isTips: dict.count == 0)

        //: if VideoIconPath.count>1 && videoPath.count>1 && isChangeVideo == true && PathAppManager.share.loginUserMode.sex == Gender.female.rawValue {
        if VideoIconPath.count > 1, videoPath.count > 1, isChangeVideo == true, PathAppManager.share.loginUserMode.sex == ConnectionClusterLiteral.female.rawValue {
            //: uploadVideo()
            conk()
        }

        //: if dict.count == 0 {
        if dict.count == 0 {
            //: return
            return
        }

        //: ProgressHUD.show()
        ShirtButtonThen.distance()

        //: TalkingMeRequestTool.req_UpdateUserInfo(params: dict) { [weak self] succeed, result, errorModel in
        GiftReactiveCompatible.toMethod(params: dict) { [weak self] succeed, _, errorModel in
            //: ProgressHUD.dismiss()
            ShirtButtonThen.allowButton()
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: if succeed {
            if succeed {
                //: self.func__showStatusBarSuccessMsg(showMsg: "Modify the success".localized)
                self.showMake(showMsg: (String(userComponentKey) + String(showPlayerText) + String(dreamScreenId.prefix(4)) + "uccess").localized)

                //: if self.nameStr.count > 0 {
                if self.nameStr.count > 0 {
                    //: PathAppManager.share.loginUserMode.nickname = self.nameStr
                    PathAppManager.share.loginUserMode.nickname = self.nameStr
                }
                //: if self.birthday.count > 0 {
                if self.birthday.count > 0 {
                    //: PathAppManager.share.loginUserMode.birthday = self.birthday
                    PathAppManager.share.loginUserMode.birthday = self.birthday
                }
                //: PathAppManager.share.loginUserMode.signature = self.signStr
                PathAppManager.share.loginUserMode.signature = self.signStr

                //: if isBack {
                if isBack {
                    //: self.navigationController?.popViewController(animated: true)
                    self.navigationController?.popViewController(animated: true)
                }

                //: } else {
            } else {
                //: if errorModel?.errorCode != 100 {
                if errorModel?.errorCode != 100 {
                    //: self.func__showStatusBarErrorMsg(showMsg: errorModel!.errorMsg )
                    self.colorGift(showMsg: errorModel!.errorMsg)
                }
            }
        }
    }

    //: func photoAndTagTool(BackT: Bool, isTips: Bool) {
    func moreResponse(BackT: Bool, isTips: Bool) {
        //: self.isTips = isTips
        self.isTips = isTips
        //: if self.DeletePhotoArray.count>0 {
        if self.DeletePhotoArray.count > 0 {
            // 先删再加
            //: deletePhoto()
            imageButton()
            //: } else {
        } else {
            //: if self.seletePhotoArray.count>0 {
            if self.seletePhotoArray.count > 0 {
                //: uploadPhoto()
                write()
            }
        }

        //: if isdeleteAboutme {
        if isdeleteAboutme {
            //: deleteTag(type: 1)
            dismissTo(type: 1)
        }
        //: if isdeleteInters {
        if isdeleteInters {
            //: deleteTag(type: 2)
            dismissTo(type: 2)
        }
        //: if BackT {
        if BackT {
            //: self.navigationController?.popViewController(animated: true)
            self.navigationController?.popViewController(animated: true)
        }
    }

    /// 上传相册
    //: func uploadPhoto() {
    func write() {
        //: for i in 0..<seletePhotoArray.count {
        for i in 0 ..< seletePhotoArray.count {
            //: var dict = Dictionary<String, Any>()
            var dict = [String: Any]()
            //: let resultData: NSData = seletePhotoArray[i].compressedImageData()! as NSData
            let resultData: NSData = seletePhotoArray[i].pause()! as NSData
            //: dict["pic"] = resultData
            dict[(dreamLabelColorMsg.replacingOccurrences(of: "ret", with: "p"))] = resultData
            //: ProgressHUD.show()
            ShirtButtonThen.distance()
            //: TalkingMeRequestTool.req_UploadPic(params: dict) { [weak self] succeed, result, errorModel in
            GiftReactiveCompatible.cellDown(params: dict) { [weak self] succeed, _, _ in
                //: ProgressHUD.dismiss()
                ShirtButtonThen.allowButton()
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: if succeed {
                if succeed {
                    //: if  self.isTips {
                    if self.isTips {
                        //: self.isTips = false
                        self.isTips = false
                        //: self.func__showStatusBarSuccessMsg(showMsg: "Modify the success".localized)
                        self.showMake(showMsg: (String(userComponentKey) + String(showPlayerText) + String(dreamScreenId.prefix(4)) + "uccess").localized)
                    }
                    //: self.seletePhotoArray.removeAll()
                    self.seletePhotoArray.removeAll()
                    //: self.GetGallery()
                    self.roundEqual()
                }
            }
        }
    }

    /// 删除相册
    //: func deletePhoto() {
    func imageButton() {
        //: var str = DeletePhotoArray.joined(separator: ",")
        var str = DeletePhotoArray.joined(separator: ",")
        //: str = "[\(str)]"
        str = "[\(str)]"
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["pids"] = str
        dict[(dreamById.replacingOccurrences(of: "succeed", with: "s"))] = str
        //: ProgressHUD.show()
        ShirtButtonThen.distance()
        //: TalkingMeRequestTool.req_DeletePic(params: dict) { [weak self] succeed, result, errorModel in
        GiftReactiveCompatible.hideData(params: dict) { [weak self] succeed, _, _ in

            //: guard let self = self else {return}
            guard let self = self else { return }
            //: if self.seletePhotoArray.count>0 {
            if self.seletePhotoArray.count > 0 {
                //: self.uploadPhoto()
                self.write()
                //: } else {
            } else {
                //: ProgressHUD.dismiss()
                ShirtButtonThen.allowButton()
            }
            //: if succeed {
            if succeed {
                //: if  self.isTips {
                if self.isTips {
                    //: self.isTips = false
                    self.isTips = false
                    //: self.func__showStatusBarSuccessMsg(showMsg: "Modify the success".localized)
                    self.showMake(showMsg: (String(userComponentKey) + String(showPlayerText) + String(dreamScreenId.prefix(4)) + "uccess").localized)
                }

                //: self.DeletePhotoArray.removeAll()
                self.DeletePhotoArray.removeAll()
                //: self.GetGallery()
                self.roundEqual()
            }
        }
    }

    /// 修改tag
    //: func deleteTag(type: Int) {
    func dismissTo(type: Int) {
        //: var array: [UserSeleteTagModel] = []
        var array: [VideoTagModel] = []
        //: var category = ""
        var category = ""
        //: if type==1 {
        if type == 1 {
            //: array=seleteAboutme
            array = seleteAboutme
            //: category="aboutMe"
            category = (String(noti_canIdent) + String(kBlockContent))
            //: } else {
        } else {
            //: array=seleteInters
            array = seleteInters
            //: category="interest"
            category = (String(dreamMakeData) + mainStatusName.replacingOccurrences(of: "maintain", with: "st"))
        }
        //: var str = ""
        var str = ""
        //: for i in 0..<array.count {
        for i in 0 ..< array.count {
            //: let model = array[i]
            let model = array[i]
            //: str += model.tag_id!
            str += model.tag_id!
            //: if i<array.count-1 {
            if i < array.count - 1 {
                //: str += ","
                str += ","
            }
        }
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["tagIds"] = str
        dict[(String(mTransactionName.prefix(5)) + String(kCheckedUrl))] = str
        //: dict["category"] = category
        dict[(notiColorKey.replacingOccurrences(of: "flexible", with: "ca") + userMessageData.replacingOccurrences(of: "mode", with: "y"))] = category
        //: ProgressHUD.show()
        ShirtButtonThen.distance()
        //: TalkingMeRequestTool.req_EditTag(params: dict) { [weak self] succeed, result, errorModel in
        GiftReactiveCompatible.addView(params: dict) { [weak self] succeed, _, _ in
            //: ProgressHUD.dismiss()
            ShirtButtonThen.allowButton()
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: if succeed {
            if succeed {
                //: if  self.isTips {
                if self.isTips {
                    //: self.isTips = false
                    self.isTips = false
                    //: self.func__showStatusBarSuccessMsg(showMsg: "Modify the success".localized)
                    self.showMake(showMsg: (String(userComponentKey) + String(showPlayerText) + String(dreamScreenId.prefix(4)) + "uccess").localized)
                }
                //: if type==1 {
                if type == 1 {
                    //: self.isdeleteAboutme = false
                    self.isdeleteAboutme = false
                    //: PathAppManager.share.loginUserMode.aboutMe = self.seleteAboutme
                    PathAppManager.share.loginUserMode.aboutMe = self.seleteAboutme
                    //: } else {
                } else {
                    //: self.isdeleteInters = false
                    self.isdeleteInters = false
                    //: PathAppManager.share.loginUserMode.interest = self.seleteInters
                    PathAppManager.share.loginUserMode.interest = self.seleteInters
                }
            }
        }
    }

    /// 上传视频
    //: func uploadVideo() {
    func conk() {
        //: ProgressHUD.show()
        ShirtButtonThen.distance()
        //: TalkingMomentVideoManager.shared.cos_uploadVideo(type: 2, coverPath: self.VideoIconPath, videoPath: self.videoPath) { coverUrl, videoUrl, succeed in
        DirectionSignatureProvider.shared.when(type: 2, coverPath: self.VideoIconPath, videoPath: self.videoPath) { coverUrl, videoUrl, succeed in
            //: guard succeed else {
            guard succeed else {
                //: ProgressHUD.dismiss()
                ShirtButtonThen.allowButton()
                //: self.func__showStatusBarErrorMsg(showMsg: "An error occurred, please try again".localized)
                self.colorGift(showMsg: String(bytes: k_pathKey.reversed(), encoding: .utf8)!.localized)
                //: return
                return
            }

            //: var dict = Dictionary<String, Any>()
            var dict = [String: Any]()
            //: dict["imgUrl"] = coverUrl
            dict[(String(main_textMsg.prefix(5)) + String(mainAppKey))] = coverUrl
            //: dict["videoUrl"] = videoUrl
            dict[(noti_imageMsg.replacingOccurrences(of: "index", with: "v") + String(dreamViewRowTitle.prefix(7)))] = videoUrl

            //: TalkingMeRequestTool.uploadVideoUserEdit(params: dict) { [weak self] succeed, result, errorModel in
            GiftReactiveCompatible.appeal(params: dict) { [weak self] succeed, _, _ in
                //: ProgressHUD.dismiss()
                ShirtButtonThen.allowButton()
                //: guard let self = self else { return }
                guard let self = self else { return }

                //: guard succeed else {
                guard succeed else {
                    //: self.func__showStatusBarErrorMsg(showMsg: "An error occurred, please try again".localized)
                    self.colorGift(showMsg: String(bytes: k_pathKey.reversed(), encoding: .utf8)!.localized)
                    //: return
                    return
                }
                //: self.VideoIconPath = ""
                self.VideoIconPath = ""
                //: self.videoPath = ""
                self.videoPath = ""
                //: self.isChangeVideo = false
                self.isChangeVideo = false
                //: self.videoStatus = 0
                self.videoStatus = 0
                //: self.func__showStatusBarSuccessMsg(showMsg: "Video cover submitted successfully".localized)
                self.showMake(showMsg: String(bytes: mAcceptContent.reversed(), encoding: .utf8)!.localized)
                //: for i in self.MainTable.visibleCells {
                for i in self.MainTable.visibleCells {
                    //: if i.reuseIdentifier == "TalkingEditVideoCell" {
                    if i.reuseIdentifier == (String(show_sizePath) + String(kToLiveValue)) {
                        //: let videocell: TalkingEditVideoCell = i as! TalkingEditVideoCell
                        let videocell: WillThen = i as! WillThen
                        //: videocell.upDateCellView(videoPlayPath: dict["videoUrl"] as! String)
                        videocell.roundPath(videoPlayPath: dict[(noti_imageMsg.replacingOccurrences(of: "index", with: "v") + String(dreamViewRowTitle.prefix(7)))] as! String)
                        //: videocell.setVideoStatusLB(status: self.videoStatus)
                        videocell.fill(status: self.videoStatus)
                    }
                }
            }
        }
    }

    //: func getVideoInfo() {
    func thenCheck() {
        //: TalkingMeRequestTool.getUploadVideoInfo { [weak self] succeed, result, errorModel in
        GiftReactiveCompatible.featureRentArea { [weak self] succeed, result, _ in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: if succeed {
            if succeed {
                //: guard let dict = result as? Dictionary<String, Any> else {return}
                guard let dict = result as? [String: Any] else { return }
                //: self.videoPath = dict["video_url"] as! String
                self.videoPath = dict[(String(mainLabelTitle))] as! String
                //: self.VideoIconPath = dict["img_url"] as! String
                self.VideoIconPath = dict[(String(userModelStr))] as! String
                //: self.videoStatus = dict["status"] as? Int ?? -3
                self.videoStatus = dict[(appDataPath.replacingOccurrences(of: "click", with: "ta"))] as? Int ?? -3
                //: self.MainTable.reloadData()
                self.MainTable.reloadData()
            }
        }
    }
}

// MARK: - Event

//: extension TalkingEditProfilesVC {
extension DataProfilesVc {
    //: @objc func keyboardShowBeHidden(notification: NSNotification) {
    @objc func equalNotification(notification: NSNotification) {
        //: let info = notification.userInfo
        let info = notification.userInfo
        //: let keyBoardHeight = (info![UIResponder.keyboardFrameEndUserInfoKey] as! NSValue).cgRectValue.size.height
        let keyBoardHeight = (info![UIResponder.keyboardFrameEndUserInfoKey] as! NSValue).cgRectValue.size.height

        //: for i in MainTable.visibleCells {
        for i in MainTable.visibleCells {
            //: if i.reuseIdentifier == "TalkingEditNameCell" {
            if i.reuseIdentifier == (String(dreamEqualIdent) + String(userCenterKey.suffix(6)) + String(show_toTitle.prefix(6))) {
                //: let namecell: TalkingEditNameCell = i as! TalkingEditNameCell
                let namecell: AppTitleCell = i as! AppTitleCell
                //: if namecell.nameTF.isFirstResponder {
                if namecell.nameTF.isFirstResponder {
                    //: MainTable.setContentOffset(CGPoint(x: 0, y: namecell.frame.maxY-keyBoardHeight), animated: true)
                    MainTable.setContentOffset(CGPoint(x: 0, y: namecell.frame.maxY - keyBoardHeight), animated: true)
                }
            }
            //: if i.reuseIdentifier == "TalkingEditSignCell" {
            if i.reuseIdentifier == (String(userCompareAtCellData.prefix(6)) + String(k_equalHeadKey)) {
                //: let namecell: TalkingEditSignCell = i as! TalkingEditSignCell
                let namecell: LabelManagerViewCell = i as! LabelManagerViewCell
                //: if namecell.textView.isFirstResponder {
                if namecell.textView.isFirstResponder {
                    //: MainTable.setContentOffset(CGPoint(x: 0, y: namecell.frame.maxY-keyBoardHeight), animated: true)
                    MainTable.setContentOffset(CGPoint(x: 0, y: namecell.frame.maxY - keyBoardHeight), animated: true)
                }
            }
        }
    }
}

// MARK: - UITableViewDelegate, UITableViewDataSource

//: extension TalkingEditProfilesVC: UITableViewDelegate, UITableViewDataSource {
extension DataProfilesVc: UITableViewDelegate, UITableViewDataSource {
    //: func numberOfSections(in tableView: UITableView) -> Int {
    func numberOfSections(in _: UITableView) -> Int {
        //: return 7
        return 7
    }

    //: func tableView(_ tableView: UITableView, numberOfRowsInSection section: Int) -> Int {
    func tableView(_: UITableView, numberOfRowsInSection _: Int) -> Int {
        //: return 1
        return 1
    }

    //: func tableView(_ tableView: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
    func tableView(_: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
        //: if indexPath.section == 0 {
        if indexPath.section == 0 {
            //: if PathAppManager.share.loginUserMode.sex == Gender.female.rawValue {
            if PathAppManager.share.loginUserMode.sex == ConnectionClusterLiteral.female.rawValue {
                //: return 226
                return 226
            }
            //: return 0
            return 0

            //: } else if indexPath.section == 1 {
        } else if indexPath.section == 1 {
            //: let sizewh = Int((ScreenWidth-30-12*2-9*2)/3)
            let sizewh = Int((app_screenFailMarginFormat - 30 - 12 * 2 - 9 * 2) / 3)
            //: let Y = 46+sizewh*3+18
            let Y = 46 + sizewh * 3 + 18
            //: let cellheiht = Y+12+26+15
            let cellheiht = Y + 12 + 26 + 15
            //: return CGFloat(cellheiht)
            return CGFloat(cellheiht)
            //: } else if indexPath.section == 2 {
        } else if indexPath.section == 2 {
            //: return 107+15
            return 107 + 15
            //: } else if indexPath.section == 3 {
        } else if indexPath.section == 3 {
            //: return 64
            return 64
            //: } else if indexPath.section == 4 {
        } else if indexPath.section == 4 {
            //: return 176
            return 176
            //: } else if indexPath.section == 5 || indexPath.section == 6 {
        } else if indexPath.section == 5 || indexPath.section == 6 {
            //: if indexPath.section == 5 && seleteAboutme.count>0 {
            if indexPath.section == 5 && seleteAboutme.count > 0 {
                //: return self.computeCellheight(type: 1)+64
                return self.gap(type: 1) + 64
                //: } else if indexPath.section == 6 && seleteInters.count>0 {
            } else if indexPath.section == 6 && seleteInters.count > 0 {
                //: return self.computeCellheight(type: 2)+64
                return self.gap(type: 2) + 64
            }
            //: return 64
            return 64
        }
        //: return 56
        return 56
    }

    //: func tableView(_ tableView: UITableView, estimatedHeightForRowAt indexPath: IndexPath) -> CGFloat {
    func tableView(_: UITableView, estimatedHeightForRowAt _: IndexPath) -> CGFloat {
        //: return 56
        return 56
    }

    //: func tableView(_ tableView: UITableView, heightForHeaderInSection section: Int) -> CGFloat {
    func tableView(_: UITableView, heightForHeaderInSection _: Int) -> CGFloat {
        //: return 0.1
        return 0.1
    }

    //: func tableView(_ tableView: UITableView, heightForFooterInSection section: Int) -> CGFloat {
    func tableView(_: UITableView, heightForFooterInSection _: Int) -> CGFloat {
        //: return 0.1
        return 0.1
    }

    //: func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
    func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
        //: if indexPath.section == 0 {
        if indexPath.section == 0 {
            //: let identifier = "TalkingEditVideoCell"
            let identifier = (String(show_sizePath) + String(kToLiveValue))
            //: var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? TalkingEditVideoCell
            var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? WillThen
            //: if cell == nil {
            if cell == nil {
                //: cell = TalkingEditVideoCell(style: .default, reuseIdentifier: identifier)
                cell = WillThen(style: .default, reuseIdentifier: identifier)
            }
            //: cell?.setVideoStatusLB(status: self.videoStatus)
            cell?.fill(status: self.videoStatus)
            //: if VideoIconPath.count>1 && videoPath.count>1 {
            if VideoIconPath.count > 1 && videoPath.count > 1 {
                //: cell?.setCellView(iconPath: VideoIconPath)
                cell?.doingControl(iconPath: VideoIconPath)
                //: cell?.upDateCellView(videoPlayPath: videoPath)
                cell?.roundPath(videoPlayPath: videoPath)
            }
            //: cell?.seleteBlock = { [weak self] imagPath, VideoPath in
            cell?.seleteBlock = { [weak self] imagPath, VideoPath in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.VideoIconPath = imagPath
                self.VideoIconPath = imagPath
                //: self.videoPath = VideoPath
                self.videoPath = VideoPath
                //: self.isChangeVideo = true
                self.isChangeVideo = true
            }
            //: cell?.deleteBlock = { [weak self] in
            cell?.deleteBlock = { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.VideoIconPath = ""
                self.VideoIconPath = ""
                //: self.videoPath = ""
                self.videoPath = ""
                //: self.isChangeVideo = false
                self.isChangeVideo = false
            }
            //: return cell!
            return cell!

            //: } else if indexPath.section == 1 {
        } else if indexPath.section == 1 {
            //: let identifier = "TalkingEditPhotoCell"
            let identifier = (String(mWithStr.suffix(4)) + "ingEditPh" + String(kAfterId) + String(app_opUrl.suffix(6)))
            //: var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? TalkingEditPhotoCell
            var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? LabelValueView
            //: if cell == nil {
            if cell == nil {
                //: cell = TalkingEditPhotoCell(style: .default, reuseIdentifier: identifier)
                cell = LabelValueView(style: .default, reuseIdentifier: identifier)
            }
            //: if PhotoArray.count>0 {
            if PhotoArray.count > 0 {
                //: cell?.setPhoto(selete: PhotoArray)
                cell?.cancel(selete: PhotoArray)
                //: } else {
            } else {
                //: cell?.resetPhoto()
                cell?.withPhoto()
            }
            //: cell?.delegate = self
            cell?.delegate = self
            //: return cell!
            return cell!
            //: } else if indexPath.section == 2 {
        } else if indexPath.section == 2 {
            //: let identifier = "TalkingEditNameCell"
            let identifier = (String(dreamEqualIdent) + String(userCenterKey.suffix(6)) + String(show_toTitle.prefix(6)))
            //: var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? TalkingEditNameCell
            var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? AppTitleCell
            //: if cell == nil {
            if cell == nil {
                //: cell = TalkingEditNameCell(style: .default, reuseIdentifier: identifier)
                cell = AppTitleCell(style: .default, reuseIdentifier: identifier)
            }
            //: cell?.nameBlock = { [weak self] name in
            cell?.nameBlock = { [weak self] name in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.nameStr = name
                self.nameStr = name
            }
            //: return cell!
            return cell!
            //: } else if indexPath.section == 3 {
        } else if indexPath.section == 3 {
            //: let identifier = "TalkingEditBirthdayCell"
            let identifier = (String(userHiddenWarnIdent) + String(showEventMiniTitle.prefix(6)) + "irth" + String(kToPath.prefix(7)))
            //: var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? TalkingEditBirthdayCell
            var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? DirectionObjectProtocol
            //: if cell == nil {
            if cell == nil {
                //: cell = TalkingEditBirthdayCell(style: .default, reuseIdentifier: identifier)
                cell = DirectionObjectProtocol(style: .default, reuseIdentifier: identifier)
            }
            //: cell?.birthdayBlock = { [weak self] day in
            cell?.birthdayBlock = { [weak self] day in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.birthday = day
                self.birthday = day
            }
            //: return cell!
            return cell!
            //: } else if indexPath.section == 4 {
        } else if indexPath.section == 4 {
            //: let identifier = "TalkingEditSignCell"
            let identifier = (String(userCompareAtCellData.prefix(6)) + String(k_equalHeadKey))
            //: var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? TalkingEditSignCell
            var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? LabelManagerViewCell
            //: if cell == nil {
            if cell == nil {
                //: cell = TalkingEditSignCell(style: .default, reuseIdentifier: identifier)
                cell = LabelManagerViewCell(style: .default, reuseIdentifier: identifier)
            }
            //: cell?.signBlock = { [weak self] sign in
            cell?.signBlock = { [weak self] sign in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.signStr = sign
                self.signStr = sign
            }

            //: return cell!
            return cell!
            //: } else if indexPath.section == 5 || indexPath.section == 6 {
        } else if indexPath.section == 5 || indexPath.section == 6 {
            //: let identifier = "TalkingEditAboutMeCell"
            let identifier = (String(mShareKey.suffix(5)) + "ngEdi" + String(k_equalData.suffix(8)) + String(kAppId))
            //: var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? TalkingEditAboutMeCell
            var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? CheckView
            //: if cell == nil {
            if cell == nil {
                //: cell = TalkingEditAboutMeCell(style: .default, reuseIdentifier: identifier)
                cell = CheckView(style: .default, reuseIdentifier: identifier)
            }
            //: if indexPath.section == 5 {
            if indexPath.section == 5 {
                //: cell!.tagtype = .AboutMe
                cell!.tagtype = .AboutMe
                //: } else {
            } else {
                //: cell!.tagtype = .Interests
                cell!.tagtype = .Interests
            }
            //: cell?.setTitle()
            cell?.statuteNameTitle()
            //: if seleteAboutme.count>0 && indexPath.section == 5 {
            if seleteAboutme.count > 0 && indexPath.section == 5 {
                //: cell?.setMessage(seleteAboutme)
                cell?.ply(seleteAboutme)
            }
            //: if seleteInters.count>0 && indexPath.section == 6 {
            if seleteInters.count > 0 && indexPath.section == 6 {
                //: cell?.setMessage(seleteInters)
                cell?.ply(seleteInters)
            }
            //: cell?.editBtnBlock = { [weak self] in
            cell?.editBtnBlock = { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: let vc = TalkingEditTagVC.init()
                let vc = ReasonViewController()
                //: vc.delegate = self
                vc.delegate = self
                //: if indexPath.section == 5 {
                if indexPath.section == 5 {
                    //: vc.tagtype = .AboutMe
                    vc.tagtype = .AboutMe
                    //: } else {
                } else {
                    //: vc.tagtype = .Interests
                    vc.tagtype = .Interests
                }
                //: self.navigationController!.pushViewController(vc, animated: true)
                self.navigationController!.pushViewController(vc, animated: true)
            }
            //: cell?.deleteBlock = { [weak self] tag in
            cell?.deleteBlock = { [weak self] tag in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: if indexPath.section == 5 {
                if indexPath.section == 5 {
                    //: if  self.seleteAboutme.contains(where: { $0.equals(compareTo: tag)}) {
                    if self.seleteAboutme.contains(where: { $0.analogise(compareTo: tag) }) {
                        //: self.seleteAboutme.removeAll(where: {$0.equals(compareTo: tag)})
                        self.seleteAboutme.removeAll(where: { $0.analogise(compareTo: tag) })
                        //: self.isdeleteAboutme = true
                        self.isdeleteAboutme = true
                    }
                    //: } else {
                } else {
                    //: if  self.seleteInters.contains(where: { $0.equals(compareTo: tag)}) {
                    if self.seleteInters.contains(where: { $0.analogise(compareTo: tag) }) {
                        //: self.seleteInters.removeAll(where: {$0.equals(compareTo: tag)})
                        self.seleteInters.removeAll(where: { $0.analogise(compareTo: tag) })
                        //: self.isdeleteInters = true
                        self.isdeleteInters = true
                    }
                }
            }
            //: return cell!
            return cell!
            //: } else {
        } else {
            //: let identifier = "UITableViewCell"
            let identifier = (String(mainIconUrl))
            //: var cell = tableView.dequeueReusableCell(withIdentifier: identifier)
            var cell = tableView.dequeueReusableCell(withIdentifier: identifier)
            //: if cell == nil {
            if cell == nil {
                //: cell = UITableViewCell(style: .default, reuseIdentifier: identifier)
                cell = UITableViewCell(style: .default, reuseIdentifier: identifier)
            }
            //: return cell!
            return cell!
        }
    }

    //: func tableView(_ tableView: UITableView, didSelectRowAt indexPath: IndexPath) {
    func tableView(_: UITableView, didSelectRowAt _: IndexPath) {}

    //: func computeCellheight(type: Int) -> CGFloat {
    func gap(type: Int) -> CGFloat {
        //: let itemSpacing = CGFloat(8)
        let itemSpacing = CGFloat(8)
        //: var contentWidthInRow = CGFloat(0)
        var contentWidthInRow = CGFloat(0)
        //: var array = Array<UserSeleteTagModel>()
        var array = [VideoTagModel]()
        //: if type == 1 {
        if type == 1 {
            //: array = self.seleteAboutme
            array = self.seleteAboutme
            //: } else {
        } else {
            //: array = self.seleteInters
            array = self.seleteInters
        }
        //: var contentHeightInRow = CGFloat(30)
        var contentHeightInRow = CGFloat(30)

        //: for item in array {
        for item in array {
            //: var tag_name = ""
            var tag_name = ""
            //: if TalkingRequestAddrTool.share.interfaceLang == LangType.ar.rawValue {
            if LiveAddrThen.share.interfaceLang == ViewTermConvertible.ar.rawValue {
                //: tag_name = item.tag_name_ar
                tag_name = item.tag_name_ar
                //: } else if TalkingRequestAddrTool.share.interfaceLang == LangType.es.rawValue {
            } else if LiveAddrThen.share.interfaceLang == ViewTermConvertible.es.rawValue {
                //: tag_name = item.tag_name_es
                tag_name = item.tag_name_es
                //: } else if TalkingRequestAddrTool.share.interfaceLang == LangType.pt.rawValue {
            } else if LiveAddrThen.share.interfaceLang == ViewTermConvertible.pt.rawValue {
                //: tag_name = item.tag_name_pt
                tag_name = item.tag_name_pt
            }
            //: if tag_name.count <= 0 {
            if tag_name.count <= 0 {
                //: tag_name = item.tag_name
                tag_name = item.tag_name
            }
            //: let rect = (tag_name as NSString).boundingRect(with: .zero, options: .usesLineFragmentOrigin, attributes: [.font: UIFont.pingfangFont(type: .Regular, fontSize: 15)], context: nil)
            let rect = (tag_name as NSString).boundingRect(with: .zero, options: .usesLineFragmentOrigin, attributes: [.font: UIFont.bindFor(type: .Regular, fontSize: 15)], context: nil)
            //: let tagWidth = rect.width + 30
            let tagWidth = rect.width + 30
            //: if contentWidthInRow + tagWidth + itemSpacing > ScreenWidth-54 {
            if contentWidthInRow + tagWidth + itemSpacing > app_screenFailMarginFormat - 54 {
                // 需要换行
                //: contentHeightInRow += (10 + 30)
                contentHeightInRow += (10 + 30)
                //: contentWidthInRow = itemSpacing
                contentWidthInRow = itemSpacing
                //: } else {
            } else {
                //: contentWidthInRow += (tagWidth+itemSpacing)
                contentWidthInRow += (tagWidth + itemSpacing)
            }
        }
        //: if array.count == 10 {
        if array.count == 10 {
            //: contentHeightInRow += 40
            contentHeightInRow += 40
        }
        //: return contentHeightInRow + 12
        return contentHeightInRow + 12
    }
}

// MARK: - InsideThen

//: extension TalkingEditProfilesVC: EditTagDelegate {
extension DataProfilesVc: InsideThen {
    //: func freshSeleteTag() {
    func bar() {
        //: seleteAboutme =  PathAppManager.share.loginUserMode.aboutMe!
        seleteAboutme = PathAppManager.share.loginUserMode.aboutMe!
        //: seleteInters =  PathAppManager.share.loginUserMode.interest!
        seleteInters = PathAppManager.share.loginUserMode.interest!
        //: self.MainTable .reloadData()
        self.MainTable.reloadData()
    }
}

// MARK: - ButtonPhotoDelegate

//: extension TalkingEditProfilesVC: EditPhotoDelegate {
extension DataProfilesVc: ButtonPhotoDelegate {
    /// 添加本地图片
    //: func addPhoto(_ icon: [UIImage]) {
    func fromDismiss(_ icon: [UIImage]) {
        //: for i in icon {
        for i in icon {
            //: self.seletePhotoArray.append(i)
            self.seletePhotoArray.append(i)
            //: var model = TalkingUserInfoGalleryModel.init()
            var model = LabGalleryModel()
            //: model.imagePic = i
            model.imagePic = i
            //: model.isLocal = true
            model.isLocal = true
            //: PhotoArray.append(model)
            PhotoArray.append(model)
        }
    }

    /// 删除本地相册选择的图片
    //: func deletePhoto(_ icon: UIImage) {
    func content(_ icon: UIImage) {
        //: if  seletePhotoArray.contains(where: {$0 == icon}) {
        if seletePhotoArray.contains(where: { $0 == icon }) {
            //: seletePhotoArray.removeAll(where: {$0 == icon})
            seletePhotoArray.removeAll(where: { $0 == icon })
        }
        //: var seleteIndex = -1
        var seleteIndex = -1
        //: for i in 0..<PhotoArray.count {
        for i in 0 ..< PhotoArray.count {
            //: if icon == PhotoArray[i].imagePic {
            if icon == PhotoArray[i].imagePic {
                //: seleteIndex =  i
                seleteIndex = i
                //: break
                break
            }
        }
        //: if seleteIndex>=0 && seleteIndex<PhotoArray.count {
        if seleteIndex >= 0 && seleteIndex < PhotoArray.count {
            //: PhotoArray.remove(at: seleteIndex)
            PhotoArray.remove(at: seleteIndex)
            //: self.MainTable.reloadData()
            self.MainTable.reloadData()
        }
    }

    /// 删除后台相册的图片
    //: func deletePhotoWithUid(_ iconUid: String) {
    func tarotCardToSmart(_ iconUid: String) {
        //: var seleteIndex = -1
        var seleteIndex = -1
        //: for i in 0..<PhotoArray.count {
        for i in 0 ..< PhotoArray.count {
            //: if iconUid == PhotoArray[i].uid {
            if iconUid == PhotoArray[i].uid {
                //: seleteIndex =  i
                seleteIndex = i
                //: self.DeletePhotoArray.append(iconUid)
                self.DeletePhotoArray.append(iconUid)
            }
        }
        //: if seleteIndex>=0 && seleteIndex<PhotoArray.count {
        if seleteIndex >= 0 && seleteIndex < PhotoArray.count {
            //: PhotoArray.remove(at: seleteIndex)
            PhotoArray.remove(at: seleteIndex)
            //: self.MainTable.reloadData()
            self.MainTable.reloadData()
        }
    }
}

// MARK: - UI

//: extension TalkingEditProfilesVC {
extension DataProfilesVc {
    //: private func designView() {
    private func design() {
        //: self.view.addSubview(MainTable)
        self.view.addSubview(MainTable)
        //: MainTable.register(TalkingEditVideoCell.self, forCellReuseIdentifier: "TalkingEditVideoCell")
        MainTable.register(WillThen.self, forCellReuseIdentifier: (String(show_sizePath) + String(kToLiveValue)))
        //: MainTable.register(TalkingEditPhotoCell.self, forCellReuseIdentifier: "TalkingEditPhotoCell")
        MainTable.register(LabelValueView.self, forCellReuseIdentifier: (String(mWithStr.suffix(4)) + "ingEditPh" + String(kAfterId) + String(app_opUrl.suffix(6))))
        //: MainTable.register(TalkingEditNameCell.self, forCellReuseIdentifier: "TalkingEditNameCell")
        MainTable.register(AppTitleCell.self, forCellReuseIdentifier: (String(dreamEqualIdent) + String(userCenterKey.suffix(6)) + String(show_toTitle.prefix(6))))
        //: MainTable.register(TalkingEditBirthdayCell.self, forCellReuseIdentifier: "TalkingEditBirthdayCell")
        MainTable.register(DirectionObjectProtocol.self, forCellReuseIdentifier: (String(userHiddenWarnIdent) + String(showEventMiniTitle.prefix(6)) + "irth" + String(kToPath.prefix(7))))
        //: MainTable.register(TalkingEditSignCell.self, forCellReuseIdentifier: "TalkingEditSignCell")
        MainTable.register(LabelManagerViewCell.self, forCellReuseIdentifier: (String(userCompareAtCellData.prefix(6)) + String(k_equalHeadKey)))
        //: MainTable.register(TalkingEditAboutMeCell.self, forCellReuseIdentifier: "TalkingEditAboutMeCell")
        MainTable.register(CheckView.self, forCellReuseIdentifier: (String(mShareKey.suffix(5)) + "ngEdi" + String(k_equalData.suffix(8)) + String(kAppId)))
        //: MainTable.separatorStyle = .none
        MainTable.separatorStyle = .none
        //: MainTable.dataSource = self
        MainTable.dataSource = self
        //: MainTable.delegate = self
        MainTable.delegate = self
        //: MainTable.snp.makeConstraints { make in
        MainTable.snp.makeConstraints { make in
            //: make.leading.trailing.bottom.equalTo(self.view)
            make.leading.trailing.bottom.equalTo(self.view)
            //: make.top.equalTo(self.view).offset(20)
            make.top.equalTo(self.view).offset(20)
        }
    }
}
