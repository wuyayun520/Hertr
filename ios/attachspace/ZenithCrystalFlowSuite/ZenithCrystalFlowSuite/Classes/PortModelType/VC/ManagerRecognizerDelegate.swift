
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let k_cellPlayUrl:[UInt8] = [0x84,0x89,0x84,0x8f,0x43,0x7e,0x8a,0x7f,0x80,0x8d,0x55,0x44,0x3b,0x83,0x7c,0x8e,0x3b,0x89,0x8a,0x8f,0x3b,0x7d,0x80,0x80,0x89,0x3b,0x84,0x88,0x8b,0x87,0x80,0x88,0x80,0x89,0x8f,0x80,0x7f]

fileprivate func combinedStatus(pull num: UInt8) -> UInt8 {
    let value = Int(num) + 229
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "Text greeting" :*/
fileprivate let notiLineBlockTitle:String = "Text gthin color destination let filter"

/*: "Add" :*/
fileprivate let notiBackgroundGiftText:String = "Addtrue tag invite video"

/*: "type" :*/
fileprivate let showInfoStr:[Character] = ["t","y","p","e"]

/*: "addStatus" :*/
fileprivate let mSendData:[Character] = ["a","d","d","S","t","a"]
fileprivate let dream_positionEndEqualStr:String = "centerus"

/*: "list" :*/
fileprivate let notiPointLabIdent:[Character] = ["l","i","s","t"]

/*: "Are you sure to delete this message？" :*/
fileprivate let show_headEditPath:[UInt8] = [0xcb,0xf8,0xef,0xaa,0xf3,0xe5,0xff,0xaa,0xf9,0xff,0xf8,0xef,0xaa,0xfe,0xe5,0xaa,0xee,0xef,0xe6,0xef,0xfe,0xef,0xaa,0xfe,0xe2,0xe3,0xf9,0xaa,0xe7,0xef,0xf9,0xf9,0xeb,0xed,0xef,0x65,0x36,0x15]

private func norDirection(error num: UInt8) -> UInt8 {
    return num ^ 138
}

/*: "Cancel" :*/
fileprivate let mLabelDataUrl:[Character] = ["C","a","n","c","e"]
fileprivate let appCenterPathMsg:[Character] = ["l"]

/*: "Delete" :*/
fileprivate let m_nameValue:String = "string length make text dataDelete"

/*: "id" :*/
fileprivate let notiTargetId:String = "model"

/*: "The upper limit has been reached" :*/
fileprivate let show_colorData:[UInt8] = [0x7d,0x41,0x4c,0x9,0x5c,0x59,0x59,0x4c,0x5b,0x9,0x45,0x40,0x44,0x40,0x5d,0x9,0x41,0x48,0x5a,0x9,0x4b,0x4c,0x4c,0x47,0x9,0x5b,0x4c,0x48,0x4a,0x41,0x4c,0x4d]

/*: "text" :*/
fileprivate let dreamIconStr:String = "viewxt"

/*: "Successfully added, please wait for approval" :*/
fileprivate let mVideoKey:[UInt8] = [0x19,0x3f,0x29,0x29,0x2f,0x39,0x39,0x2c,0x3f,0x26,0x26,0x33,0x6a,0x2b,0x2e,0x2e,0x2f,0x2e,0x66,0x6a,0x3a,0x26,0x2f,0x2b,0x39,0x2f,0x6a,0x3d,0x2b,0x23,0x3e,0x6a,0x2c,0x25,0x38,0x6a,0x2b,0x3a,0x3a,0x38,0x25,0x3c,0x2b,0x26]

private func addTalk(line num: UInt8) -> UInt8 {
    return num ^ 74
}

/*: "Any-Hex/Java" :*/
fileprivate let mainExtraIdent:String = "manager white show returnAny-H"

/*: "LogReactiveCompatible" :*/
fileprivate let mainModelIdent:[Character] = ["T","a","l","k","i","n","g","G","r","e","e","t","T","e"]
fileprivate let dreamScreenName:String = "party itemxtCell"

/*: "#FF506D" :*/
fileprivate let mSendMsg:[Character] = ["#","F","F","5","0","6","D"]

/*: "btn_message_delete_nor" :*/
fileprivate let show_titleKey:[Character] = ["b","t","n","_","m","e"]
fileprivate let dream_profileTitle:String = "infoinfoag"
fileprivate let show_picData:String = "equal finish model event selfte_nor"

/*: "Click the button below to add\ncontent" :*/
fileprivate let mainCollectionPath:[UInt8] = [0x2,0x2b,0x28,0x22,0x2a,0xdf,0x33,0x27,0x24,0xdf,0x21,0x34,0x33,0x33,0x2e,0x2d,0xdf,0x21,0x24,0x2b,0x2e,0x36,0xdf,0x33,0x2e,0xdf,0x20,0x23,0x23,0xc9,0x22,0x2e,0x2d,0x33,0x24,0x2d,0x33]

fileprivate func colorIcon(item num: UInt8) -> UInt8 {
    let value = Int(num) + 65
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "When adding multiple text message, the syst em will send them by turns;slide to the message to delete" :*/
fileprivate let userLeadingData:[UInt8] = [0x80,0xbf,0xb2,0xb9,0xf7,0xb6,0xb3,0xb3,0xbe,0xb9,0xb0,0xf7,0xba,0xa2,0xbb,0xa3,0xbe,0xa7,0xbb,0xb2,0xf7,0xa3,0xb2,0xaf,0xa3,0xf7,0xba,0xb2,0xa4,0xa4,0xb6,0xb0,0xb2,0xfb,0xf7,0xa3,0xbf,0xb2,0xf7,0xa4,0xae,0xa4,0xa3,0xf7,0xb2,0xba,0xf7,0xa0,0xbe,0xbb,0xbb,0xf7,0xa4,0xb2,0xb9,0xb3,0xf7,0xa3,0xbf,0xb2,0xba,0xf7,0xb5,0xae,0xf7,0xa3,0xa2,0xa5,0xb9,0xa4,0xec,0xa4,0xbb,0xbe,0xb3,0xb2,0xf7,0xa3,0xb8,0xf7,0xa3,0xbf,0xb2,0xf7,0xba,0xb2,0xa4,0xa4,0xb6,0xb0,0xb2,0xf7,0xa3,0xb8,0xf7,0xb3,0xb2,0xbb,0xb2,0xa3,0xb2]

private func kitSeat(tool num: UInt8) -> UInt8 {
    return num ^ 215
}

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  ManagerRecognizerDelegate.swift
//  AbroadTalking
//
//  Created by Charlotte on 2023/2/15.
//

//: import UIKit
import UIKit

//: class TalkingGreetTextVC: TalkingBaseViewController {
class ManagerRecognizerDelegate: ListRecognizerDelegate {
	var nameOff: Bool = false
	var excessSum: Double = -5.7
	var mapArray: [AnyHashable] = []
	var keyEnable: Bool = true
	var labelMagnitude: Double = 78.2
	var springArray: [AnyHashable] = []

    //: var addStatus = -1
    var addStatus = -1
    //: var recordView: TalkingGreetEditTextView?
    var recordView: CharterThen?

    //: init() {
    init() {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: k_cellPlayUrl.map{combinedStatus(pull: $0)}, encoding: .utf8)!)
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
    
		if var scopeValue = recordView?.popView?.isRemoveTapGes { 
	            if (self.presentingViewController?.title == "position") && (self.preferredInterfaceOrientationForPresentation == .portraitUpsideDown) {
	                //: SWIFT_CUSTOM_DANGER_File_Call
	                let firstEnter = TwentyController()
	            firstEnter.noseSum = self.addStatus
	
	            firstEnter.videoSumOpen = { [self] loadClose in
	            self.nameOff = loadClose
	            
	            var firstEnter = scopeValue
	                firstEnter = true
	                firstEnter = !firstEnter
	            if firstEnter != self.nameOff {
	                self.nameOff = firstEnter
	            }
	            
	            self.nameOff = true
	            return self.nameOff
	            }
	            firstEnter.minInterval = { [self] itemNumber in
	            self.excessSum = itemNumber
	            
	            return self.excessSum
	            }
	            firstEnter.popArray = { [self] topStopArray in
	            self.mapArray = topStopArray
	            
	            guard var value = self.mapArray as? [String] else {
	                return nil
	            }
	            return value
	            }
	                self.navigationController?.pushViewController(firstEnter.self, animated: false)
	            }
	
		}
	}

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.title = "Text greeting".localized
        self.title = (String(notiLineBlockTitle.prefix(6)) + "reeting").localized
        //: self.view.backgroundColor = UIColor.appBgColor()
        self.view.backgroundColor = UIColor.titleIndex()
        //: designView()
        ticket()
        //: getAudioListData()
        myData()
    
            if (self.presentingViewController?.title == "position") && (self.preferredInterfaceOrientationForPresentation == .portraitUpsideDown) {
                //: SWIFT_CUSTOM_DANGER_File_Call
                let firstEnter = TwentyController()
            firstEnter.noseSum = self.addStatus

            firstEnter.videoSumOpen = { [self] loadClose in
            self.keyEnable = loadClose
            
            var firstEnter = self.hideNavi
                firstEnter = true
                firstEnter = !firstEnter
            if firstEnter != self.keyEnable {
                self.keyEnable = firstEnter
            }
            
            self.keyEnable = true
            return self.keyEnable
            }
            firstEnter.minInterval = { [self] itemNumber in
            self.labelMagnitude = itemNumber
            
            return self.labelMagnitude
            }
            firstEnter.popArray = { [self] topStopArray in
            self.springArray = topStopArray
            
            guard var value = self.springArray as? [String] else {
                return nil
            }
            return value
            }
                self.navigationController?.pushViewController(firstEnter.self, animated: false)
            }

	}

    //: deinit {
    deinit {}

    // MARK: - Lazy load

    //: private lazy var dataSourceArr: [TalkingGreetModel] = {
    private lazy var dataSourceArr: [PortModelType] = {
        //: var array = [TalkingGreetModel]()
        var array = [PortModelType]()
        //: return array
        return array
        //: }()
    }()

    //: lazy var MainTable: UITableView = {
    lazy var MainTable: UITableView = {
        //: let table = UITableView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight-88), style: UITableView.Style.plain)
        let table = UITableView(frame: CGRect(x: 0, y: 0, width: app_screenFailMarginFormat, height: mainAcceptKey - 88), style: UITableView.Style.plain)
        //: table.backgroundColor = UIColor.clear
        table.backgroundColor = UIColor.clear
        //: return table
        return table
        //: }()
    }()

    //: lazy var emptyView: EmptyView = {
    lazy var emptyView: EmptyView = {
        //: let view = EmptyView()
        let view = EmptyView()
        //: return view
        return view
        //: }()
    }()

    //: lazy var addButton: UIButton = {
    lazy var addButton: UIButton = {
        //: let btn = UIButton.init()
        let btn = UIButton()
        //: btn.setTitle("Add".localized, for: .normal)
        btn.setTitle((String(notiBackgroundGiftText.prefix(3))).localized, for: .normal)
        //: btn.setTitleColor(.white, for: .normal)
        btn.setTitleColor(.white, for: .normal)
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 18)
        btn.titleLabel?.font = UIFont.bindFor(type: .Medium, fontSize: 18)
        //: btn.setBackgroundImage(UIImage.imageGradientColor(colors: UIColor.appGradientColor(), size: CGSize(width: 267, height: 50)), for: .normal)
        btn.setBackgroundImage(UIImage.garrison(colors: UIColor.chemicalElementSend(), size: CGSize(width: 267, height: 50)), for: .normal)
        //: btn.layer.cornerRadius = 25
        btn.layer.cornerRadius = 25
        //: btn.clipsToBounds = true
        btn.clipsToBounds = true
        //: btn.addTarget(self, action: #selector(addButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(conversationError), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()
}

// MARK: - Request & 数据处理

//: extension TalkingGreetTextVC {
extension ManagerRecognizerDelegate {
    //: func getAudioListData() {
    func myData() {
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["type"] = 2
        dict[(String(showInfoStr))] = 2
        //: ProgressHUD.show()
        ShirtButtonThen.distance()
        //: TalkingMeRequestTool.req_GetGreetList(params: dict) {[weak self] succeed, result, errorModel in
        GiftReactiveCompatible.capTime(params: dict) { [weak self] succeed, result, _ in
            //: ProgressHUD.dismiss()
            ShirtButtonThen.allowButton()
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: if (self.MainTable.backgroundView == nil) {
            if self.MainTable.backgroundView == nil {
                //: self.setupEmptyDataView()
                self.setupEmptyDataView()
            }
            //: if succeed {
            if succeed {
                //: guard let dict = result as? Dictionary<String, Any> else {
                guard let dict = result as? [String: Any] else {
                    //: return
                    return
                }
                //: self.addStatus = dict["addStatus"] as! Int
                self.addStatus = dict[(String(mSendData) + dream_positionEndEqualStr.replacingOccurrences(of: "center", with: "t"))] as! Int
                //: let array: Array =  dict["list"] as! Array<Any>
                let array: Array = dict[(String(notiPointLabIdent))] as! [Any]
                //: var dataArr: [TalkingGreetModel] = []
                var dataArr: [PortModelType] = []
                //: for i in 0..<array.count {
                for i in 0 ..< array.count {
                    //: if let model = JSONDeserializer<TalkingGreetModel>.deserializeFrom(dict: array[i] as? Dictionary<String, Any>, designatedPath: nil) {
                    if let model = JSONDeserializer<PortModelType>.deserializeFrom(dict: array[i] as? [String: Any], designatedPath: nil) {
                        //: dataArr.append(model)
                        dataArr.append(model)
                    }
                }
                //: self.dataSourceArr = dataArr
                self.dataSourceArr = dataArr
                //: self.MainTable.reloadData()
                self.MainTable.reloadData()
            }
        }
    }

    //: func shoWdeleteAlert(index: Int) {
    func colorWith(index: Int) {
        //: let config = ShowAlertConfig()
        let config = KeyCustomAlertConfig()
        //: config.textFont = UIFont.pingfangFont(type: .Medium, fontSize: 18)
        config.textFont = UIFont.bindFor(type: .Medium, fontSize: 18)
        //: config.textlineBreakMode = .byCharWrapping
        config.textlineBreakMode = .byCharWrapping

        //: TalkingAlertShow.customAlert(message: "Are you sure to delete this message？".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Delete".localized, leftBlock: {
        CourseAlertShow.inkpad(message: String(bytes: show_headEditPath.map{norDirection(error: $0)}, encoding: .utf8)!.localized, leftBtnTitle: (String(mLabelDataUrl) + String(appCenterPathMsg)).localized, rightBtnTitle: (String(m_nameValue.suffix(6))).localized, leftBlock: {
            //: TalkingAlertShow.hideAlert()
            CourseAlertShow.cardWindow()
            //: }, rightBlock: { [weak self] in
        }, rightBlock: { [weak self] in

            //: guard let self = self else {return}
            guard let self = self else { return }
            //: TalkingAlertShow.hideAlert()
            CourseAlertShow.cardWindow()
            //: let model: TalkingGreetModel = self.dataSourceArr[index]
            let model: PortModelType = self.dataSourceArr[index]
            //: var dict = Dictionary<String, Any>()
            var dict = [String: Any]()
            //: dict["id"] = model.greetId
            dict["id"] = model.greetId
            //: ProgressHUD.show()
            ShirtButtonThen.distance()
            //: TalkingMeRequestTool.req_DeleteAudio(params: dict) { succeed, result, errorModel in
            GiftReactiveCompatible.objectName(params: dict) { succeed, _, _ in
                //: ProgressHUD.dismiss()
                ShirtButtonThen.allowButton()
                //: guard succeed == true else {
                guard succeed == true else {
                    //: return
                    return
                }
                //: self.getAudioListData()
                self.myData()
            }
            //: }, config: config)
        }, config: config)
    }
}

// MARK: - Event

//: extension TalkingGreetTextVC {
extension ManagerRecognizerDelegate {
    //: @objc func addButtonClick() {
    @objc func conversationError() {
        //: if (self.addStatus == 4) {
        if self.addStatus == 4 {
            //: self.func__showStatusBarErrorMsg(showMsg: "The upper limit has been reached".localized)
            self.colorGift(showMsg: String(bytes: show_colorData.map{$0^41}, encoding: .utf8)!.localized)
            //: return
            return
        }
        //: self.recordView = TalkingGreetEditTextView.init(frame: self.view.frame)
        self.recordView = CharterThen(frame: self.view.frame)
        //: self.recordView?.show()
        self.recordView?.produceAdd()
        //: self.recordView?.textUpload = {[weak self] str in
        self.recordView?.textUpload = { [weak self] str in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: let tempStr = self.unicodeString(str)
            let tempStr = self.unicodeApp(str)
            //: var dict = Dictionary<String, Any>()
            var dict = [String: Any]()
            //: dict["type"] = 2
            dict[(String(showInfoStr))] = 2
            //: dict["text"] = tempStr
            dict[(dreamIconStr.replacingOccurrences(of: "view", with: "te"))] = tempStr
            //: ProgressHUD.show()
            ShirtButtonThen.distance()
            //: TalkingMeRequestTool.req_UploadGreetAdd(params: dict) { succeed, result, errorModel in
            GiftReactiveCompatible.tillBusyCompletion(params: dict) { succeed, _, _ in
                //: ProgressHUD.dismiss()
                ShirtButtonThen.allowButton()
                //: guard succeed == true else {
                guard succeed == true else {
                    //: return
                    return
                }
                //: self.func__showStatusBarSuccessMsg(showMsg: "Successfully added, please wait for approval".localized)
                self.showMake(showMsg: String(bytes: mVideoKey.map{addTalk(line: $0)}, encoding: .utf8)!.localized)
                //: self.getAudioListData()
                self.myData()
                //: self.recordView?.dismiss()
                self.recordView?.buttonDismiss()
            }
        }
    }

    //: func unicodeString(_ str: String) -> String {
    func unicodeApp(_ str: String) -> String {
        //: let mutableStr = NSMutableString(string: str) as CFMutableString
        let mutableStr = NSMutableString(string: str) as CFMutableString
        //: CFStringTransform(mutableStr, nil, "Any-Hex/Java" as CFString, true)
        CFStringTransform(mutableStr, nil, (String(mainExtraIdent.suffix(5)) + "ex/Java") as CFString, true)
        //: return mutableStr as String
        return mutableStr as String
    }
}

// MARK: - UITableViewDelegate, UITableViewDataSource

//: extension TalkingGreetTextVC: UITableViewDelegate, UITableViewDataSource {
extension ManagerRecognizerDelegate: UITableViewDelegate, UITableViewDataSource {
    //: func tableView(_ tableView: UITableView, numberOfRowsInSection section: Int) -> Int {
    func tableView(_: UITableView, numberOfRowsInSection _: Int) -> Int {
        //: return 1
        return 1
    }

    //: func numberOfSections(in tableView: UITableView) -> Int {
    func numberOfSections(in _: UITableView) -> Int {
        //: MainTable.backgroundView?.isHidden = dataSourceArr.count > 0
        MainTable.backgroundView?.isHidden = dataSourceArr.count > 0
        //: MainTable.bounces = dataSourceArr.count > 0
        MainTable.bounces = dataSourceArr.count > 0
        //: return dataSourceArr.count
        return dataSourceArr.count
    }

    //: func tableView(_ tableView: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
    func tableView(_: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
        //: let model = self.dataSourceArr[indexPath.section]
        let model = self.dataSourceArr[indexPath.section]
        //: var height: CGFloat = 18+22
        var height: CGFloat = 18 + 22
        //: let rect = model.content.boundingRect(with: CGSize(width: ScreenWidth-60, height: 0), options: [NSStringDrawingOptions.usesLineFragmentOrigin, NSStringDrawingOptions.usesFontLeading, NSStringDrawingOptions.usesDeviceMetrics], attributes: [NSAttributedString.Key.font: UIFont.pingfangFont(type: .Medium, fontSize: 16)], context: nil)
        let rect = model.content.boundingRect(with: CGSize(width: app_screenFailMarginFormat - 60, height: 0), options: [NSStringDrawingOptions.usesLineFragmentOrigin, NSStringDrawingOptions.usesFontLeading, NSStringDrawingOptions.usesDeviceMetrics], attributes: [NSAttributedString.Key.font: UIFont.bindFor(type: .Medium, fontSize: 16)], context: nil)
        //: height += rect.height
        height += rect.height
        //: if model.status == 0 {
        if model.status == 0 {
            //: height += 16+12
            height += 16 + 12
        }
        //: return height
        return height
    }

    //: func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
    func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
        //: let identifier = "TalkingGreetTextCell"
        let identifier = (String(mainModelIdent) + String(dreamScreenName.suffix(6)))
        //: let cell: TalkingGreetTextCell = tableView.dequeueReusableCell(withIdentifier: identifier, for: indexPath) as! TalkingGreetTextCell
        let cell: LogReactiveCompatible = tableView.dequeueReusableCell(withIdentifier: identifier, for: indexPath) as! LogReactiveCompatible

        //: cell.setGreetTextCell(model: self.dataSourceArr[indexPath.section], row: indexPath.section, isLast: indexPath.section == self.dataSourceArr.count-1)
        cell.confines(model: self.dataSourceArr[indexPath.section], row: indexPath.section, isLast: indexPath.section == self.dataSourceArr.count - 1)
        //: return cell
        return cell
    }

    // ios11以前
    //: func tableView(_ tableView: UITableView, editActionsForRowAt indexPath: IndexPath) -> [UITableViewRowAction]? {
    func tableView(_: UITableView, editActionsForRowAt indexPath: IndexPath) -> [UITableViewRowAction]? {
        //: let deleteBtn = UITableViewRowAction(style: .normal, title: "Delete".localized) { action, indexP in
        let deleteBtn = UITableViewRowAction(style: .normal, title: (String(m_nameValue.suffix(6))).localized) { _, _ in

            //: self.shoWdeleteAlert(index: indexPath.section)
            self.colorWith(index: indexPath.section)
        }
        //: deleteBtn.backgroundColor = UIColor(hex: "#FF506D")
        deleteBtn.backgroundColor = UIColor(hex: (String(mSendMsg)))

        //: return [deleteBtn]
        return [deleteBtn]
    }

    //: @available(iOS 11.0, *)
    @available(iOS 11.0, *)
    //: func tableView(_ tableView: UITableView, trailingSwipeActionsConfigurationForRowAt indexPath: IndexPath) -> UISwipeActionsConfiguration? {
    func tableView(_: UITableView, trailingSwipeActionsConfigurationForRowAt indexPath: IndexPath) -> UISwipeActionsConfiguration? {
        //: let deleteBtn = UIContextualAction(style: .normal, title: nil) { action, view, completionHandler in
        let deleteBtn = UIContextualAction(style: .normal, title: nil) { _, _, completionHandler in

            //: self.shoWdeleteAlert(index: indexPath.section)
            self.colorWith(index: indexPath.section)
            //: completionHandler(false)
            completionHandler(false)
        }

        //: if let deleteBtnTrashcan =  UIImage.BundleImageNamed(name: "btn_message_delete_nor").cgImage {
        if let deleteBtnTrashcan = UIImage.sendCollection(name: (String(show_titleKey) + dream_profileTitle.replacingOccurrences(of: "info", with: "s") + "e_dele" + String(show_picData.suffix(6)))).cgImage {
            //: deleteBtn.image = ImageWithoutRender(cgImage: deleteBtnTrashcan, scale: UIScreen.main.nativeScale, orientation: .up)
            deleteBtn.image = DataWithoutRender(cgImage: deleteBtnTrashcan, scale: UIScreen.main.nativeScale, orientation: .up)
        }
        //: deleteBtn.backgroundColor = .white
        deleteBtn.backgroundColor = .white
        //: let actions = [deleteBtn]
        let actions = [deleteBtn]
        //: let config = UISwipeActionsConfiguration(actions: actions)
        let config = UISwipeActionsConfiguration(actions: actions)
        //: config.performsFirstActionWithFullSwipe = false
        config.performsFirstActionWithFullSwipe = false
        //: return config
        return config
    }

    //: func tableView(_ tableView: UITableView, heightForFooterInSection section: Int) -> CGFloat {
    func tableView(_: UITableView, heightForFooterInSection _: Int) -> CGFloat {
        //: return 0.01
        return 0.01
    }
}

// MARK: - Layout

//: extension TalkingGreetTextVC {
extension ManagerRecognizerDelegate {
    //: private func setupEmptyDataView() {
    private func setupEmptyDataView() {
        //: var style = EmptyStyle()
        var style = PathDataEmptyStyle()
        //: style.TipsTitle = "Click the button below to add\ncontent".localized
        style.TipsTitle = String(bytes: mainCollectionPath.map{colorIcon(item: $0)}, encoding: .utf8)!.localized
        //: emptyView = EmptyView.init(frame: self.view.frame, style: style)
        emptyView = EmptyView(frame: self.view.frame, style: style)
        //: emptyView.isHidden = true
        emptyView.isHidden = true
        //: emptyView.isScrollEnabled = false
        emptyView.isScrollEnabled = false
        //: emptyView.backgroundColor = .appBgColor()
        emptyView.backgroundColor = .titleIndex()
        //: MainTable.backgroundView = emptyView
        MainTable.backgroundView = emptyView

        //: emptyView.emptyBlock = { [weak self] in
        emptyView.emptyBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.MainTable.mj_header?.beginRefreshing()
            self.MainTable.mj_header?.beginRefreshing()
        }
    }

    //: private func designView() {
    private func ticket() {
        //: self.view.addSubview(addButton)
        self.view.addSubview(addButton)
        //: addButton.snp.makeConstraints { make in
        addButton.snp.makeConstraints { make in
            //: make.bottom.equalToSuperview().offset(-34)
            make.bottom.equalToSuperview().offset(-34)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.size.equalTo(CGSize(width: 267, height: 50))
            make.size.equalTo(CGSize(width: 267, height: 50))
        }

        //: self.view.addSubview(MainTable)
        self.view.addSubview(MainTable)
        //: MainTable.register(TalkingGreetTextCell.self, forCellReuseIdentifier: "TalkingGreetTextCell")
        MainTable.register(LogReactiveCompatible.self, forCellReuseIdentifier: (String(mainModelIdent) + String(dreamScreenName.suffix(6))))
        //: MainTable.separatorStyle = .none
        MainTable.separatorStyle = .none
        //: MainTable.dataSource = self
        MainTable.dataSource = self
        //: MainTable.delegate = self
        MainTable.delegate = self
        //: MainTable.snp.makeConstraints { make in
        MainTable.snp.makeConstraints { make in
            //: make.edges.equalTo(self.view)
            make.edges.equalTo(self.view)
            //: make.bottom.equalTo(addButton.snp.top).offset(-16)
            make.bottom.equalTo(addButton.snp.top).offset(-16)
        }
        //: MainTable.tableFooterView = UIView.init()
        MainTable.tableFooterView = UIView()

        //: let headerView = UIView.init(frame: CGRect.init(x: 0, y: 0, width: ScreenWidth, height: 100))
        let headerView = UIView(frame: CGRect(x: 0, y: 0, width: app_screenFailMarginFormat, height: 100))
        //: let label = UILabel.init()
        let label = UILabel()
        //: label.textColor = UIColor.appValueDetailColor()
        label.textColor = UIColor.moreRegard()
        //: label.font = UIFont.pingfangRugularFont(fontSize: 15)
        label.font = UIFont.indicatorSize(fontSize: 15)
        //: label.numberOfLines = 0
        label.numberOfLines = 0
        //: let string = "When adding multiple text message, the syst em will send them by turns;slide to the message to delete".localized
        let string = String(bytes: userLeadingData.map{kitSeat(tool: $0)}, encoding: .utf8)!.localized
        //: let attri = NSMutableAttributedString.init(string: string)
        let attri = NSMutableAttributedString(string: string)
        //: let style = NSMutableParagraphStyle.init()
        let style = NSMutableParagraphStyle()
        //: style.lineSpacing = 2
        style.lineSpacing = 2
        //: attri.yy_paragraphStyle = style
        attri.yy_paragraphStyle = style
        //: label.attributedText = attri
        label.attributedText = attri
        //: headerView.addSubview(label)
        headerView.addSubview(label)
        //: label.snp.makeConstraints { make in
        label.snp.makeConstraints { make in
            //: make.leading.equalTo(30)
            make.leading.equalTo(30)
            //: make.trailing.equalTo(-30)
            make.trailing.equalTo(-30)
            //: make.bottom.equalTo(-16)
            make.bottom.equalTo(-16)
            //: make.top.equalTo(12)
            make.top.equalTo(12)
        }
        //: MainTable.tableHeaderView = headerView
        MainTable.tableHeaderView = headerView
    }
}
