
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let dream_countryText:[UInt8] = [0xb1,0xb6,0xb1,0xac,0xf0,0xbb,0xb7,0xbc,0xbd,0xaa,0xe2,0xf1,0xf8,0xb0,0xb9,0xab,0xf8,0xb6,0xb7,0xac,0xf8,0xba,0xbd,0xbd,0xb6,0xf8,0xb1,0xb5,0xa8,0xb4,0xbd,0xb5,0xbd,0xb6,0xac,0xbd,0xbc]

private func indexReturn(frame num: UInt8) -> UInt8 {
    return num ^ 216
}

/*: "Voice greeting" :*/
fileprivate let user_giftCellId:[Character] = ["V","o","i","c","e"," ","g","r","e","e","t","i","n","g"]

/*: "Add" :*/
fileprivate let show_viewWhiteUserId:String = "m cell insideAdd"

/*: "type" :*/
fileprivate let dream_giftBorderReplyText:[Character] = ["t","y","p","e"]

/*: "addStatus" :*/
fileprivate let m_indexLabStr:String = "addStaimage run model view"
fileprivate let mainDataValue:String = "errors"

/*: "list" :*/
fileprivate let user_actualEqualCenterIdent:String = "licellt"

/*: "audio" :*/
fileprivate let appBindStr:String = "autoo"

/*: "length" :*/
fileprivate let dreamEventId:String = "lerequestgth"

/*: "url" :*/
fileprivate let main_headMessage:String = "ucamera"

/*: "Successfully added, please wait for approval" :*/
fileprivate let notiSharedMsg:[UInt8] = [0x6c,0x61,0x76,0x6f,0x72,0x70,0x70,0x61,0x20,0x72,0x6f,0x66,0x20,0x74,0x69,0x61,0x77,0x20,0x65,0x73,0x61,0x65,0x6c,0x70,0x20,0x2c,0x64,0x65,0x64,0x64,0x61,0x20,0x79,0x6c,0x6c,0x75,0x66,0x73,0x73,0x65,0x63,0x63,0x75,0x53]

/*: "Are you sure to delete this message？" :*/
fileprivate let kValueBottomName:[UInt8] = [0x83,0xb0,0xa7,0xe2,0xbb,0xad,0xb7,0xe2,0xb1,0xb7,0xb0,0xa7,0xe2,0xb6,0xad,0xe2,0xa6,0xa7,0xae,0xa7,0xb6,0xa7,0xe2,0xb6,0xaa,0xab,0xb1,0xe2,0xaf,0xa7,0xb1,0xb1,0xa3,0xa5,0xa7,0x2d,0x7e,0x5d]

private func hiddenMake(row num: UInt8) -> UInt8 {
    return num ^ 194
}

/*: "Cancel" :*/
fileprivate let appMoreIdent:String = "Cancelhave transition guard"

/*: "Delete" :*/
fileprivate let mTitleContent:String = "in model return targetDelete"

/*: "id" :*/
fileprivate let userColorIdent:String = "iequal"

/*: "Please wait for the audit result" :*/
fileprivate let notiManagerMsg:[UInt8] = [0x97,0xab,0xa2,0xa6,0xb4,0xa2,0xe7,0xb0,0xa6,0xae,0xb3,0xe7,0xa1,0xa8,0xb5,0xe7,0xb3,0xaf,0xa2,0xe7,0xa6,0xb2,0xa3,0xae,0xb3,0xe7,0xb5,0xa2,0xb4,0xb2,0xab,0xb3]

private func modelInfo(text num: UInt8) -> UInt8 {
    return num ^ 199
}

/*: "The upper limit has been reached" :*/
fileprivate let notiMakeText:[UInt8] = [0x33,0x47,0x44,0xff,0x54,0x4f,0x4f,0x44,0x51,0xff,0x4b,0x48,0x4c,0x48,0x53,0xff,0x47,0x40,0x52,0xff,0x41,0x44,0x44,0x4d,0xff,0x51,0x44,0x40,0x42,0x47,0x44,0x43]

fileprivate func refRange(photo num: UInt8) -> UInt8 {
    let value = Int(num) - 223
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "filePath" :*/
fileprivate let dream_viewName:[Character] = ["f","i","l","e","P","a","t","h"]

/*: "time" :*/
fileprivate let dreamColorName:String = "timdaily"

/*: "EncephalonViewCell" :*/
fileprivate let main_thenIdent:String = "for noneTalkin"
fileprivate let notiModePath:String = "tap equaletAu"
fileprivate let m_logTitle:String = "playeri"
fileprivate let m_resultReportMessage:[Character] = ["o","C","e","l","l"]

/*: "#FF506D" :*/
fileprivate let dreamCurTitle:String = "import view lab cookie#FF506D"

/*: "btn_message_delete_nor" :*/
fileprivate let dream_viewUrl:String = "hidden index add toolbtn_me"
fileprivate let dreamRegularNameId:String = "dtalke"
fileprivate let main_videoStr:[Character] = ["t","e","_","n","o","r"]

/*: "Currently mute" :*/
fileprivate let app_toolName:[Character] = ["C","u","r","r","e","n","t","l","y"," ","m","u","t","e"]

/*: "Edit Title" :*/
fileprivate let show_shareMakeContent:String = "Edit tap cell"
fileprivate let user_withMessage:String = "life"

/*: "OK" :*/
fileprivate let userLabelData:String = "Ostate"

/*: "remark" :*/
fileprivate let m_findUrl:[Character] = ["r","e","m","a","r","k"]

/*: "Click the button below to add\ncontent" :*/
fileprivate let main_resultContent:[UInt8] = [0xdc,0xf3,0xf6,0xfc,0xf4,0xbf,0xeb,0xf7,0xfa,0xbf,0xfd,0xea,0xeb,0xeb,0xf0,0xf1,0xbf,0xfd,0xfa,0xf3,0xf0,0xe8,0xbf,0xeb,0xf0,0xbf,0xfe,0xfb,0xfb,0x95,0xfc,0xf0,0xf1,0xeb,0xfa,0xf1,0xeb]

private func titleMode(back num: UInt8) -> UInt8 {
    return num ^ 159
}

/*: "Add multiple voice greetings, and the system will send voice messages by turns;\nClick the bubble to play the voice, slide to the voice area to delete it, and the voice title can be edited." :*/
fileprivate let kInfoFeeId:[UInt8] = []

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  AudioDataThen.swift
//  AbroadTalking
//
//  Created by Charlotte on 2023/2/9.
//

//: import UIKit
import UIKit

//: class TalkingGreetAudioVC: TalkingBaseViewController {
class AudioDataThen: ListRecognizerDelegate {
	var startTextEnable: Bool = true
	var rowIndexSum: Double = -6.3
	var errorArray: [AnyHashable] = []
	var topDictionary: [AnyHashable: String] = [:]
	var cellBorderDoing: Bool = true
	var backNumber: Double = 58.1
	var noArray: [AnyHashable] = []
	var infoDictionary: [AnyHashable: String] = [:]

    //: var addStatus = -1
    var addStatus = -1 // 1：可以添加；2：已有正在审核中得音频，不能添加；3：已有20条审核通过的音频，不能添加；4：已有20条审核通过的文字，不能添加；5：已有20条审核中或审核通过的图片，不能添加；
    //: var recordView: TalkingGreetRecordAudioView?
    var recordView: ConstraintAudioView?
    //: var isPlaying = false
    var isPlaying = false
    //: var playIndex: IndexPath?
    var playIndex: IndexPath?
    //: var player: TalkingAudioPlayTool?
    var player: StreetSmartThen?
    //: var isMute = false
    var isMute = false
    //: var currenCell: TalkingGreetAudioCell?
    var currenCell: EncephalonViewCell?
    //: init() {
    init() {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: dream_countryText.map{indexReturn(frame: $0)}, encoding: .utf8)!)
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
    
		if var equalValue = self.recordView?.filePath { 
	            if (emptyView.tintColor != nil && emptyView.tintColor.cgColor == UIColor(cgColor: UIColor.lightGray.cgColor).cgColor) && (emptyView.restorationIdentifier != nil) {
	                //: SWIFT_CUSTOM_DANGER_File_Call
	                let typeCell = CurRequireView(frame: emptyView.bounds.integral)
	            typeCell.playerTitle = equalValue
	            typeCell.sizeListOn = { [self] streamOn in
	            self.cellBorderDoing = streamOn
	            
	            var typeCell = isPlaying
	                typeCell = true
	                typeCell = !typeCell
	            if typeCell != self.cellBorderDoing {
	                self.cellBorderDoing = typeCell
	            }
	            
	            return self.cellBorderDoing
	            }
	            typeCell.libraryLabMagnitude = { [self] pushMagnitude in
	            self.backNumber = pushMagnitude
	            
	            return self.backNumber
	            }
	            typeCell.increaseArray = { [self] genreSumArray in
	            self.noArray = genreSumArray
	            
	            guard var value = self.noArray as? [String] else {
	                return nil
	            }
	            return value
	            }
	            typeCell.atDictionary = { [self] rowDictionary in
	            self.infoDictionary = rowDictionary
	            
	            guard var value = self.infoDictionary as? [String: String] else {
	                return nil
	            }
	            return value
	            }
	                emptyView.addSubview(typeCell)
	            }
	
		}
	}

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
    
		if var padSexValue = recordView?.audioTool?.filePath { 
			if var featureValue = self.currenCell?.backView { 
		            if (featureValue.tintColor != nil && featureValue.tintColor.cgColor == UIColor(cgColor: UIColor.lightGray.cgColor).cgColor) && (featureValue.restorationIdentifier != nil) {
		                //: SWIFT_CUSTOM_DANGER_File_Call
		                let typeCell = CurRequireView(frame: featureValue.bounds.integral)
		            typeCell.playerTitle = padSexValue
		            typeCell.sizeListOn = { [self] streamOn in
		            self.startTextEnable = streamOn
		            
		            var typeCell = isPlaying
		                typeCell = true
		                typeCell = !typeCell
		            if typeCell != self.startTextEnable {
		                self.startTextEnable = typeCell
		            }
		            
		            return self.startTextEnable
		            }
		            typeCell.libraryLabMagnitude = { [self] pushMagnitude in
		            self.rowIndexSum = pushMagnitude
		            
		            return self.rowIndexSum
		            }
		            typeCell.increaseArray = { [self] genreSumArray in
		            self.errorArray = genreSumArray
		            
		            guard var value = self.errorArray as? [String] else {
		                return nil
		            }
		            return value
		            }
		            typeCell.atDictionary = { [self] rowDictionary in
		            self.topDictionary = rowDictionary
		            
		            guard var value = self.topDictionary as? [String: String] else {
		                return nil
		            }
		            return value
		            }
		                featureValue.addSubview(typeCell)
		            }
		
			}
		}
	}

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.title = "Voice greeting".localized
        self.title = (String(user_giftCellId)).localized
        //: self.view.backgroundColor = UIColor.appBgColor()
        self.view.backgroundColor = UIColor.titleIndex()
        //: designView()
        from()
        //: getAudioListData()
        metadataData()
        //: setMutedDetection()
        showTip()
        //: NotificationCenter.default.addObserver(self, selector: #selector(cancelPlayerAudio(notification:)), name: SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION, object: nil)
        NotificationCenter.default.addObserver(self, selector: #selector(heatherMixture(notification:)), name: dreamPartyIdent, object: nil)

        //: NotificationCenter.default.addObserver(self, selector: #selector(interruptionChange(notification:)), name: AVAudioSession.interruptionNotification, object: nil)
        NotificationCenter.default.addObserver(self, selector: #selector(upwards(notification:)), name: AVAudioSession.interruptionNotification, object: nil)

        // 添加接收到音视频通话通知
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(stopPlay),
                                               selector: #selector(characteristicRootOfASquareMatrix),
                                               //: name: SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION,
                                               name: dreamPartyIdent,
                                               //: object: nil)
                                               object: nil)
    }

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
        //: if isPlaying {
        if isPlaying {
            //: self.player?.stopPlay()
            self.player?.gestureFit()
        }
    }

    // MARK: - Lazy load

    //: private lazy var dataSourceArr: [TalkingGreetModel] = {
    private lazy var dataSourceArr: [PortModelType] = {
        //: var array = [TalkingGreetModel]()
        var array = [PortModelType]()
        //: return array
        return array
        //: }()
    }()

    //: lazy var MainTable: UITableView = {
    lazy var MainTable: UITableView = {
        //: let table = UITableView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight-88), style: UITableView.Style.plain)
        let table = UITableView(frame: CGRect(x: 0, y: 0, width: app_screenFailMarginFormat, height: mainAcceptKey - 88), style: UITableView.Style.plain)
        //: table.backgroundColor = UIColor.clear
        table.backgroundColor = UIColor.clear
        //: return table
        return table
        //: }()
    }()

    //: lazy var emptyView: EmptyView = {
    lazy var emptyView: EmptyView = {
        //: let view = EmptyView()
        let view = EmptyView()
        //: return view
        return view
        //: }()
    }()

    //: lazy var addButton: UIButton = {
    lazy var addButton: UIButton = {
        //: let btn = UIButton.init()
        let btn = UIButton()
        //: btn.setTitle("Add".localized, for: .normal)
        btn.setTitle((String(show_viewWhiteUserId.suffix(3))).localized, for: .normal)
        //: btn.setTitleColor(.white, for: .normal)
        btn.setTitleColor(.white, for: .normal)
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 18)
        btn.titleLabel?.font = UIFont.bindFor(type: .Medium, fontSize: 18)
        //: btn.setBackgroundImage(UIImage.imageGradientColor(colors: UIColor.appGradientColor(), size: CGSize(width: 267, height: 50)), for: .normal)
        btn.setBackgroundImage(UIImage.garrison(colors: UIColor.chemicalElementSend(), size: CGSize(width: 267, height: 50)), for: .normal)
        //: btn.layer.cornerRadius = 25
        btn.layer.cornerRadius = 25
        //: btn.clipsToBounds = true
        btn.clipsToBounds = true
        //: btn.addTarget(self, action: #selector(addButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(small), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()
}

// MARK: - Request & 数据处理

//: extension TalkingGreetAudioVC {
extension AudioDataThen {
    //: func getAudioListData() {
    func metadataData() {
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["type"] = 1
        dict[(String(dream_giftBorderReplyText))] = 1
        //: ProgressHUD.show()
        ShirtButtonThen.distance()
        //: TalkingMeRequestTool.req_GetGreetList(params: dict) {[weak self] succeed, result, errorModel in
        GiftReactiveCompatible.capTime(params: dict) { [weak self] succeed, result, _ in

            //: ProgressHUD.dismiss()
            ShirtButtonThen.allowButton()
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: if (self.MainTable.backgroundView == nil) {
            if self.MainTable.backgroundView == nil {
                //: self.setupEmptyDataView()
                self.makeView()
            }
            //: if succeed {
            if succeed {
                //: guard let dict = result as? Dictionary<String, Any> else {
                guard let dict = result as? [String: Any] else {
                    //: return
                    return
                }
                //: self.addStatus = dict["addStatus"] as! Int
                self.addStatus = dict[(String(m_indexLabStr.prefix(6)) + mainDataValue.replacingOccurrences(of: "error", with: "tu"))] as! Int
                //: let array: Array =  dict["list"] as! Array<Any>
                let array: Array = dict[(user_actualEqualCenterIdent.replacingOccurrences(of: "cell", with: "s"))] as! [Any]
                //: var dataArr: [TalkingGreetModel] = []
                var dataArr: [PortModelType] = []
                //: for i in 0..<array.count {
                for i in 0 ..< array.count {
                    //: if let model = JSONDeserializer<TalkingGreetModel>.deserializeFrom(dict: array[i] as? Dictionary<String, Any>, designatedPath: nil) {
                    if let model = JSONDeserializer<PortModelType>.deserializeFrom(dict: array[i] as? [String: Any], designatedPath: nil) {
                        //: dataArr.append(model)
                        dataArr.append(model)
                    }
                }
                //: self.player = nil
                self.player = nil
                //: self.dataSourceArr = dataArr
                self.dataSourceArr = dataArr
                //: self.MainTable.reloadData()
                self.MainTable.reloadData()
            }
        }
    }

    //: func requestUploadAudioFileData(data: NSData, time: String) {
    func postulationButtonTime(data: NSData, time: String) {
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["type"] = 1
        dict[(String(dream_giftBorderReplyText))] = 1
        //: dict["audio"] = data
        dict[(appBindStr.replacingOccurrences(of: "to", with: "di"))] = data
        //: dict["length"] = Int(time)
        dict[(dreamEventId.replacingOccurrences(of: "request", with: "n"))] = Int(time)

        //: ProgressHUD.show()
        ShirtButtonThen.distance()
        //: TalkingMeRequestTool.req_UploadGreetAdd(params: dict) {[weak self] succeed, result, errorModel in
        GiftReactiveCompatible.tillBusyCompletion(params: dict) { [weak self] _, result, _ in

            //: ProgressHUD.dismiss()
            ShirtButtonThen.allowButton()
            //: guard let self = self else {return}
            guard let self = self else { return }

            //: guard let dict = result as? Dictionary<String, Any> else {
            guard let dict = result as? [String: Any] else {
                //: return
                return
            }
            //: let url: String = dict["url"] as! String
            let url: String = dict[(main_headMessage.replacingOccurrences(of: "camera", with: "rl"))] as! String
            //: if !url.isEmptyString {
            if !url.isEmptyString {
                //: self.func__showStatusBarSuccessMsg(showMsg: "Successfully added, please wait for approval".localized)
                self.showMake(showMsg: String(bytes: notiSharedMsg.reversed(), encoding: .utf8)!.localized)
                //: self.getAudioListData()
                self.metadataData()
                //: self.recordView?.dismiss()
                self.recordView?.indicator()
                //: self.recordView = nil
                self.recordView = nil
            }
        }
    }

    //: func shoWdeleteAlert(index: Int) {
    func viewWdelete(index: Int) {
        //: let config = ShowAlertConfig()
        let config = KeyCustomAlertConfig()
        //: config.textFont = UIFont.pingfangFont(type: .Medium, fontSize: 18)
        config.textFont = UIFont.bindFor(type: .Medium, fontSize: 18)
        //: config.textlineBreakMode = .byCharWrapping
        config.textlineBreakMode = .byCharWrapping

        //: TalkingAlertShow.customAlert(message: "Are you sure to delete this message？".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Delete".localized, leftBlock: {
        CourseAlertShow.inkpad(message: String(bytes: kValueBottomName.map{hiddenMake(row: $0)}, encoding: .utf8)!.localized, leftBtnTitle: (String(appMoreIdent.prefix(6))).localized, rightBtnTitle: (String(mTitleContent.suffix(6))).localized, leftBlock: {
            //: TalkingAlertShow.hideAlert()
            CourseAlertShow.cardWindow()
            //: }, rightBlock: { [weak self] in
        }, rightBlock: { [weak self] in

            //: guard let self = self else {return}
            guard let self = self else { return }
            //: TalkingAlertShow.hideAlert()
            CourseAlertShow.cardWindow()
            //: self.stopPlay()
            self.characteristicRootOfASquareMatrix()

            //: let model: TalkingGreetModel = self.dataSourceArr[index]
            let model: PortModelType = self.dataSourceArr[index]
            //: var dict = Dictionary<String, Any>()
            var dict = [String: Any]()
            //: dict["id"] = model.greetId
            dict["id"] = model.greetId
            //: TalkingMeRequestTool.req_DeleteAudio(params: dict) { succeed, result, errorModel in
            GiftReactiveCompatible.objectName(params: dict) { succeed, _, _ in
                //: guard succeed == true else {
                guard succeed == true else {
                    //: return
                    return
                }
                //: self.getAudioListData()
                self.metadataData()
            }
            //: }, config: config)
        }, config: config)
    }
}

// MARK: - Event

//: extension TalkingGreetAudioVC {
extension AudioDataThen {
    //: @objc func cancelPlayerAudio(notification: NSNotification) -> Void {
    @objc func heatherMixture(notification _: NSNotification) {
        //: stopPlay()
        characteristicRootOfASquareMatrix()
    }

    /// 播放过程中中断
    //: @objc func interruptionChange(notification: NSNotification) -> Void {
    @objc func upwards(notification: NSNotification) {
        //: let dic = notification.userInfo as![String: AnyObject]
        let dic = notification.userInfo as! [String: AnyObject]

        //: let changeReason = dic[AVAudioSessionInterruptionTypeKey]
        let changeReason = dic[AVAudioSessionInterruptionTypeKey]
        //: if changeReason?.intValue ?? 0 == AVAudioSession.InterruptionType.began.rawValue {
        if changeReason?.intValue ?? 0 == AVAudioSession.InterruptionType.began.rawValue {
            //: stopPlay()
            characteristicRootOfASquareMatrix()
        }
    }

    //: @objc func addButtonClick() {
    @objc func small() {
        //: guard TalkingSocketManager.shared.isData == false else {
        guard PublishDataSocketDelegate.shared.isData == false else {
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.colorGift(showMsg: app_successShowMsg)
            //: return
            return
        }

        //: TalkingPermissionTool.func__openRecordServiceWithBlock(true) { isOpen in
        EqualReactiveCompatible.utility(true) { isOpen in
            //: guard isOpen == true else {
            guard isOpen == true else {
                //: self.func__showStatusBarErrorMsg(showMsg: kMessage_permissions_error)
                self.colorGift(showMsg: showMeBarReasonPath)
                //: return
                return
            }
            //: if self.addStatus == 2 {
            if self.addStatus == 2 {
                //: self.func__showStatusBarErrorMsg(showMsg: "Please wait for the audit result".localized)
                self.colorGift(showMsg: String(bytes: notiManagerMsg.map{modelInfo(text: $0)}, encoding: .utf8)!.localized)
                //: return
                return
                    //: }else if (self.addStatus == 3) {
            } else if self.addStatus == 3 {
                //: self.func__showStatusBarErrorMsg(showMsg: "The upper limit has been reached".localized)
                self.colorGift(showMsg: String(bytes: notiMakeText.map{refRange(photo: $0)}, encoding: .utf8)!.localized)
                //: return
                return
            }
            //: self.stopPlay()
            self.characteristicRootOfASquareMatrix()
            //: self.recordView = TalkingGreetRecordAudioView.init(frame: self.view.frame)
            self.recordView = ConstraintAudioView(frame: self.view.frame)
            //: self.recordView?.show()
            self.recordView?.motion()
            //: self.recordView?.audioFileUpload = {[weak self] dict in
            self.recordView?.audioFileUpload = { [weak self] dict in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: let data = NSData.init(contentsOfFile: dict["filePath"] as! String)
                let data = NSData(contentsOfFile: dict[(String(dream_viewName))] as! String)
                //: guard data != nil else {
                guard data != nil else {
                    //: return
                    return
                }
                //: self.requestUploadAudioFileData(data: data!, time: dict["time"] as! String)
                self.postulationButtonTime(data: data!, time: dict[(dreamColorName.replacingOccurrences(of: "daily", with: "e"))] as! String)
            }
        }
    }
}

//: extension TalkingGreetAudioVC {
extension AudioDataThen {
    //: func setMutedDetection() {
    func showTip() {
        //: RBDMuteSwitch.shared.detectMuteSwitch()
        ListReactiveCompatible.shared.gop()
        //: RBDMuteSwitch.shared.isMutedBlock = { [weak self] mute in
        ListReactiveCompatible.shared.isMutedBlock = { [weak self] mute in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.isMute = mute
            self.isMute = mute
        }
    }
}

// MARK: - UITableViewDelegate, UITableViewDataSource

//: extension TalkingGreetAudioVC: UITableViewDelegate, UITableViewDataSource {
extension AudioDataThen: UITableViewDelegate, UITableViewDataSource {
    //: func tableView(_ tableView: UITableView, numberOfRowsInSection section: Int) -> Int {
    func tableView(_: UITableView, numberOfRowsInSection _: Int) -> Int {
        //: return 1
        return 1
    }

    //: func numberOfSections(in tableView: UITableView) -> Int {
    func numberOfSections(in _: UITableView) -> Int {
        //: MainTable.backgroundView?.isHidden = dataSourceArr.count > 0
        MainTable.backgroundView?.isHidden = dataSourceArr.count > 0
        //: MainTable.bounces = dataSourceArr.count > 0
        MainTable.bounces = dataSourceArr.count > 0
        //: return dataSourceArr.count
        return dataSourceArr.count
    }

    //: func tableView(_ tableView: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
    func tableView(_: UITableView, heightForRowAt _: IndexPath) -> CGFloat {
        //: return 101
        return 101
    }

    //: func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
    func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
        //: let identifier = "TalkingGreetAudioCell"
        let identifier = (String(main_thenIdent.suffix(6)) + "gGre" + String(notiModePath.suffix(4)) + m_logTitle.replacingOccurrences(of: "player", with: "d") + String(m_resultReportMessage))
        //: let cell: TalkingGreetAudioCell = tableView.dequeueReusableCell(withIdentifier: identifier, for: indexPath) as! TalkingGreetAudioCell
        let cell: EncephalonViewCell = tableView.dequeueReusableCell(withIdentifier: identifier, for: indexPath) as! EncephalonViewCell

        //: cell.setGreetAudioCell(model: self.dataSourceArr[indexPath.section], row: indexPath.section, isLast: indexPath.section == self.dataSourceArr.count-1)
        cell.titleWith(model: self.dataSourceArr[indexPath.section], row: indexPath.section, isLast: indexPath.section == self.dataSourceArr.count - 1)

        //: if isPlaying && playIndex?.row == indexPath.section {
        if isPlaying && playIndex?.row == indexPath.section {
            //: cell.syncPlayState(isPlaying: true)
            cell.count(isPlaying: true)
            //: }else {
        } else {
            //: cell.syncPlayState(isPlaying: false)
            cell.count(isPlaying: false)
        }

        //: cell.audioPlayActionBlock = { [weak self] in
        cell.audioPlayActionBlock = { [weak self] in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: RBDMuteSwitch.shared.detectMuteSwitch()
            ListReactiveCompatible.shared.gop()
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                //: self.playAudio(index: indexPath)
                self.session(index: indexPath)
            }
        }
        //: cell.editActionBlock = { [weak self] in
        cell.editActionBlock = { [weak self] in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: self.stopPlay()
            self.characteristicRootOfASquareMatrix()
            //: self.editTitle(index: indexPath.section)
            self.territorialDivision(index: indexPath.section)
        }
        //: return cell
        return cell
    }

    //: func tableView(_ tableView: UITableView, didSelectRowAt indexPath: IndexPath) {
    func tableView(_: UITableView, didSelectRowAt _: IndexPath) {}

    // ios11以前
    //: func tableView(_ tableView: UITableView, editActionsForRowAt indexPath: IndexPath) -> [UITableViewRowAction]? {
    func tableView(_: UITableView, editActionsForRowAt indexPath: IndexPath) -> [UITableViewRowAction]? {
        //: let deleteBtn = UITableViewRowAction(style: .normal, title: "Delete".localized) { action, indexP in
        let deleteBtn = UITableViewRowAction(style: .normal, title: (String(mTitleContent.suffix(6))).localized) { _, _ in

            //: self.shoWdeleteAlert(index: indexPath.section)
            self.viewWdelete(index: indexPath.section)
        }
        //: deleteBtn.backgroundColor = UIColor(hex: "#FF506D")
        deleteBtn.backgroundColor = UIColor(hex: (String(dreamCurTitle.suffix(7))))

        //: return [deleteBtn]
        return [deleteBtn]
    }

    //: @available(iOS 11.0, *)
    @available(iOS 11.0, *)
    //: func tableView(_ tableView: UITableView, trailingSwipeActionsConfigurationForRowAt indexPath: IndexPath) -> UISwipeActionsConfiguration? {
    func tableView(_: UITableView, trailingSwipeActionsConfigurationForRowAt indexPath: IndexPath) -> UISwipeActionsConfiguration? {
        //: let deleteBtn = UIContextualAction(style: .normal, title: nil) { action, view, completionHandler in
        let deleteBtn = UIContextualAction(style: .normal, title: nil) { _, _, completionHandler in

            //: self.shoWdeleteAlert(index: indexPath.section)
            self.viewWdelete(index: indexPath.section)
            //: completionHandler(false)
            completionHandler(false)
        }

        //: if let deleteBtnTrashcan =  UIImage.BundleImageNamed(name: "btn_message_delete_nor").cgImage {
        if let deleteBtnTrashcan = UIImage.sendCollection(name: (String(dream_viewUrl.suffix(6)) + "ssage_" + dreamRegularNameId.replacingOccurrences(of: "talk", with: "el") + String(main_videoStr))).cgImage {
            //: deleteBtn.image = ImageWithoutRender(cgImage: deleteBtnTrashcan, scale: UIScreen.main.nativeScale, orientation: .up)
            deleteBtn.image = DataWithoutRender(cgImage: deleteBtnTrashcan, scale: UIScreen.main.nativeScale, orientation: .up)
        }
        //: deleteBtn.backgroundColor = .white
        deleteBtn.backgroundColor = .white
        //: let actions = [deleteBtn]
        let actions = [deleteBtn]
        //: let config = UISwipeActionsConfiguration(actions: actions)
        let config = UISwipeActionsConfiguration(actions: actions)
        //: config.performsFirstActionWithFullSwipe = false
        config.performsFirstActionWithFullSwipe = false
        //: return config
        return config
    }

    //: func playAudio(index: IndexPath) {
    func session(index: IndexPath) {
        //: guard !TalkingSocketManager.shared.isData else {
        guard !PublishDataSocketDelegate.shared.isData else {
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.colorGift(showMsg: app_successShowMsg)
            //: return
            return
        }
        //: if isPlaying && playIndex == index {
        if isPlaying && playIndex == index {
            //: isPlaying = false
            isPlaying = false
            //: MainTable.reloadData()
            MainTable.reloadData()
            //: self.player?.stopPlay()
            self.player?.gestureFit()
            //: return
            return
        }
        //: if isPlaying {
        if isPlaying {
            //: currenCell = MainTable.cellForRow(at: playIndex ?? IndexPath()) as? TalkingGreetAudioCell
            currenCell = MainTable.cellForRow(at: playIndex ?? IndexPath()) as? EncephalonViewCell
            //: if currenCell != nil {
            if currenCell != nil {
                //: currenCell?.syncPlayState(isPlaying: false)
                currenCell?.count(isPlaying: false)
            }
        }

        //: playIndex = index
        playIndex = index
        //: let model = self.dataSourceArr[index.section]
        let model = self.dataSourceArr[index.section]

        //: if isMute || AVAudioSession.sharedInstance().outputVolume <= 0 {
        if isMute || AVAudioSession.sharedInstance().outputVolume <= 0 {
            //: self.func__showStatusBarErrorMsg(showMsg: "Currently mute".localized)
            self.colorGift(showMsg: (String(app_toolName)).localized)
            //: return
            return
        }
        //: if self.player == nil {
        if self.player == nil {
            //: self.player = TalkingAudioPlayTool.shared
            self.player = StreetSmartThen.shared
            //: self.player?.delegate = self
            self.player?.delegate = self
        }
        //: self.player?.stopPlay()
        self.player?.gestureFit()
        //: self.player?.play(urlStr: model.content)
        self.player?.eachBind(urlStr: model.content)
        //: isPlaying = true
        isPlaying = true
        //: currenCell = MainTable.cellForRow(at: playIndex ?? IndexPath()) as? TalkingGreetAudioCell
        currenCell = MainTable.cellForRow(at: playIndex ?? IndexPath()) as? EncephalonViewCell
        //: if currenCell != nil {
        if currenCell != nil {
            //: currenCell?.syncPlayState(isPlaying: true)
            currenCell?.count(isPlaying: true)
        }
    }

    //: func editTitle(index: Int) {
    func territorialDivision(index: Int) {
        //: let view = TalkingEditAlertView.init(title: "Edit Title".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "OK".localized, config: nil)
        let view = IsoclinicThen(title: (String(show_shareMakeContent.prefix(5)) + "Titl" + user_withMessage.replacingOccurrences(of: "life", with: "e")).localized, leftBtnTitle: (String(appMoreIdent.prefix(6))).localized, rightBtnTitle: "OK".localized, config: nil)
        //: view.show()
        view.even()

        //: view.rightBlock = { [weak self] text in
        view.rightBlock = { [weak self] text in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: ProgressHUD.show()
            ShirtButtonThen.distance()

            //: var model: TalkingGreetModel = self.dataSourceArr[index]
            var model: PortModelType = self.dataSourceArr[index]
            //: var dict = Dictionary<String, Any>()
            var dict = [String: Any]()
            //: dict["id"] = model.greetId
            dict["id"] = model.greetId
            //: dict["remark"] = text
            dict[(String(m_findUrl))] = text

            //: TalkingMeRequestTool.req_UpdateAudioRemark(params: dict) { succeed, result, errorModel in
            GiftReactiveCompatible.remarkSum(params: dict) { succeed, _, _ in
                //: ProgressHUD.dismiss()
                ShirtButtonThen.allowButton()
                //: if succeed {
                if succeed {
                    //: model.remark = text
                    model.remark = text
                    //: self.dataSourceArr[index] = model
                    self.dataSourceArr[index] = model
                    //: self.MainTable.reloadData()
                    self.MainTable.reloadData()
                }
            }
        }
    }

    //: @objc func stopPlay() {
    @objc func characteristicRootOfASquareMatrix() {
        //: if isPlaying {
        if isPlaying {
            //: isPlaying = false
            isPlaying = false
            //: self.player?.stopPlay()
            self.player?.gestureFit()
            //: if currenCell != nil {
            if currenCell != nil {
                //: currenCell?.syncPlayState(isPlaying: false)
                currenCell?.count(isPlaying: false)
            }
        }
    }
}

//: extension TalkingGreetAudioVC: TalkingAudioPlayToolDelegate {
extension AudioDataThen: CapThen {
    //: func playerStatusChange(status: AudioPlayerStatus) {
    func receive(status: NobilityExpressionConvertible) {
        //: switch status {
        switch status {
        //: case .PlayerFinish:
        case .PlayerFinish:
            //: stopPlay()
            characteristicRootOfASquareMatrix()
        //: break
        //: case .PlayerInterruption:
        case .PlayerInterruption:
            //: stopPlay()
            characteristicRootOfASquareMatrix()
        //: break
        //: default:
        default:
            //: break
            break
        }
    }
}

// MARK: - Layout

//: extension TalkingGreetAudioVC {
extension AudioDataThen {
    //: private func setupEmptyDataView() {
    private func makeView() {
        //: var style = EmptyStyle()
        var style = PathDataEmptyStyle()
        //: style.TipsTitle = "Click the button below to add\ncontent".localized
        style.TipsTitle = String(bytes: main_resultContent.map{titleMode(back: $0)}, encoding: .utf8)!.localized
        //: emptyView = EmptyView.init(frame: self.view.frame, style: style)
        emptyView = EmptyView(frame: self.view.frame, style: style)
        //: emptyView.isHidden = true
        emptyView.isHidden = true
        //: emptyView.isScrollEnabled = false
        emptyView.isScrollEnabled = false
        //: emptyView.backgroundColor = .appBgColor()
        emptyView.backgroundColor = .titleIndex()
        //: MainTable.backgroundView = emptyView
        MainTable.backgroundView = emptyView

        //: emptyView.emptyBlock = { [weak self] in
        emptyView.emptyBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.MainTable.mj_header?.beginRefreshing()
            self.MainTable.mj_header?.beginRefreshing()
        }
    }

    //: private func designView() {
    private func from() {
        //: self.view.addSubview(addButton)
        self.view.addSubview(addButton)
        //: addButton.snp.makeConstraints { make in
        addButton.snp.makeConstraints { make in
            //: make.bottom.equalToSuperview().offset(-34)
            make.bottom.equalToSuperview().offset(-34)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.size.equalTo(CGSize(width: 267, height: 50))
            make.size.equalTo(CGSize(width: 267, height: 50))
        }
        //: self.view.addSubview(MainTable)
        self.view.addSubview(MainTable)
        //: MainTable.register(TalkingGreetAudioCell.self, forCellReuseIdentifier: "TalkingGreetAudioCell")
        MainTable.register(EncephalonViewCell.self, forCellReuseIdentifier: (String(main_thenIdent.suffix(6)) + "gGre" + String(notiModePath.suffix(4)) + m_logTitle.replacingOccurrences(of: "player", with: "d") + String(m_resultReportMessage)))
        //: MainTable.separatorStyle = .none
        MainTable.separatorStyle = .none
        //: MainTable.dataSource = self
        MainTable.dataSource = self
        //: MainTable.delegate = self
        MainTable.delegate = self
        //: MainTable.snp.makeConstraints { make in
        MainTable.snp.makeConstraints { make in
            //: make.edges.equalTo(self.view)
            make.edges.equalTo(self.view)
            //: make.bottom.equalTo(addButton.snp.top).offset(-16)
            make.bottom.equalTo(addButton.snp.top).offset(-16)
        }
        //: MainTable.tableFooterView = UIView.init()
        MainTable.tableFooterView = UIView()

        //: let headerView = UIView.init(frame: CGRect.init(x: 0, y: 0, width: ScreenWidth, height: 145))
        let headerView = UIView(frame: CGRect(x: 0, y: 0, width: app_screenFailMarginFormat, height: 145))
        //: let label = UILabel.init()
        let label = UILabel()
        //: label.textColor = UIColor.appValueDetailColor()
        label.textColor = UIColor.moreRegard()
        //: label.font = UIFont.pingfangRugularFont(fontSize: 15)
        label.font = UIFont.indicatorSize(fontSize: 15)
        //: label.numberOfLines = 0
        label.numberOfLines = 0
        //: let string = "Add multiple voice greetings, and the system will send voice messages by turns;\nClick the bubble to play the voice, slide to the voice area to delete it, and the voice title can be edited.".localized
        let string = String(bytes: kInfoFeeId.map{$0^192}, encoding: .utf8)!.localized
        //: let attri = NSMutableAttributedString.init(string: string)
        let attri = NSMutableAttributedString(string: string)
        //: let style = NSMutableParagraphStyle.init()
        let style = NSMutableParagraphStyle()
        //: style.lineSpacing = 2
        style.lineSpacing = 2
        //: attri.yy_paragraphStyle = style
        attri.yy_paragraphStyle = style
        //: label.attributedText = attri
        label.attributedText = attri
        //: headerView.addSubview(label)
        headerView.addSubview(label)
        //: label.snp.makeConstraints { make in
        label.snp.makeConstraints { make in
            //: make.leading.equalTo(30)
            make.leading.equalTo(30)
            //: make.trailing.equalTo(-30)
            make.trailing.equalTo(-30)
            //: make.bottom.equalTo(-16)
            make.bottom.equalTo(-16)
            //: make.top.equalTo(12)
            make.top.equalTo(12)
        }
        //: MainTable.tableHeaderView = headerView
        MainTable.tableHeaderView = headerView
    }
}
