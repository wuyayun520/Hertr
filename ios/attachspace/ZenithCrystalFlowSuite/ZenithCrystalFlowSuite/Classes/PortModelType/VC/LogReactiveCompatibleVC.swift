
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let main_cellServerTitle:[UInt8] = [0x64,0x65,0x74,0x6e,0x65,0x6d,0x65,0x6c,0x70,0x6d,0x69,0x20,0x6e,0x65,0x65,0x62,0x20,0x74,0x6f,0x6e,0x20,0x73,0x61,0x68,0x20,0x29,0x3a,0x72,0x65,0x64,0x6f,0x63,0x28,0x74,0x69,0x6e,0x69]

/*: "Greeting Message Settings" :*/
fileprivate let notiButtonTitle:[Character] = ["G","r","e","e","t","i","n","g"," ","M","e","s","s","a","g","e"," ","S","e","t","t","i"]
fileprivate let mainMediumUrl:[Character] = ["n","g","s"]

/*: "SelectionTableCell" :*/
fileprivate let m_menuUrl:String = "Talkfail head array"
fileprivate let mDetailUrl:String = "REET"
fileprivate let show_titleKey:String = "tTabstring cell"
fileprivate let appShowMessage:String = "pointpoint"

/*: "Voice greeting" :*/
fileprivate let mUserFormat:String = "Voice grleft self skip self"
fileprivate let user_finishPath:String = "eetlabng"

/*: "icon_me_greet_vioce" :*/
fileprivate let m_scopeIdent:String = "frame handle close picicon_"
fileprivate let main_labelMakeUrl:String = "eet_view player voice"
fileprivate let user_sourceKey:String = "vioccollection"

/*: "Text greeting" :*/
fileprivate let user_imageId:[Character] = ["T","e","x","t"," ","g","r","e","e","t","i","n"]
fileprivate let appToPath:[Character] = ["g"]

/*: "icon_me_greet_text" :*/
fileprivate let show_centerIndexKitStr:[Character] = ["i","c","o","n","_","m","e","_","g","r","e","e","t"]
fileprivate let show_dataUrl:String = "_textcos now mobile center"

/*: "Photo greeting" :*/
fileprivate let mHiddenName:[Character] = ["P","h","o","t","o"," "]
fileprivate let appFillIdent:String = "grcomponent"
fileprivate let k_modelTitle:String = "etilocal"

/*: "icon_me_greet_photo" :*/
fileprivate let noti_dreamId:String = "icon_mblock text register stop image"
fileprivate let appLineNameMsg:String = "true list up let_photo"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  LogReactiveCompatible.swift
//  AbroadTalking
//
//  Created by Charlotte on 2023/2/9.
//

//: import UIKit
import UIKit

//: class TalkingAutoGreetSettingVC: TalkingBaseViewController {
class LogReactiveCompatibleVC: ListRecognizerDelegate {
	var giftLabSum: Int = 89
	var contentName: String = "user"
	var equalArray: [AnyHashable] = []
	var succeedDictionary: [AnyHashable: String] = [:]
	var useCount: Int = 46
	var skinPicContent: String = "at"
	var titleBackArray: [AnyHashable] = []
	var errorCardDictionary: [AnyHashable: String] = [:]
	var observerSum: Double = -61.1
	var picAllowArray: [AnyHashable] = []
	var showDictionary: [AnyHashable: String] = [:]

    //: var settingModel = TalkingSettingModel()
    var settingModel = TitleModelType()

    //: init() {
    init() {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: main_cellServerTitle.reversed(), encoding: .utf8)!)
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: getSettingData()
        loadBuild()
    
		if var greetValue = self.settingModel.textIsSet { 
			if var positionValue = self.settingModel.intimatePhotoIsSet { 
		            if (self.isMovingToParent) && (self.childForStatusBarStyle != nil && self.childForStatusBarStyle!.edgesForExtendedLayout == .left) {
		                //: SWIFT_CUSTOM_DANGER_File_Call
		                let leftThrough = TouchController()
		            leftThrough.moreOff = self.hideNavi
		            leftThrough.marginOfSafetyQuantity = positionValue
	
		            leftThrough.windowQuantity = { [self] groupTotal in
		            self.giftLabSum = groupTotal
		            
		            var leftThrough = greetValue
		            leftThrough = 0
		            if leftThrough > self.giftLabSum {
		                self.giftLabSum = leftThrough
		            }
		            
		            return self.giftLabSum
		            }
		            leftThrough.gestureSectionTitle = { [self] doingContent in
		            self.contentName = doingContent
		            
		            let coverText = self.contentName.suffix((self.contentName.isEmpty ? 3 : 9)).count
		            self.contentName.reserveCapacity(coverText + (self.contentName.isEmpty ? 0 : 9))
		            return self.contentName
		            }
		            leftThrough.matchArray = { [self] layArray in
		            self.equalArray = layArray
		            
		            guard var value = self.equalArray as? [String] else {
		                return nil
		            }
		            return value
		            }
		            leftThrough.topDictionary = { [self] boundDictionary in
		            self.succeedDictionary = boundDictionary
		            
		            guard var value = self.succeedDictionary as? [String: String] else {
		                return nil
		            }
		            return value
		            }
		                self.navigationController?.present(leftThrough.self, animated: true) { [self] in
		            let packthreadName = self.contentName.prefix(upTo: self.contentName.index(self.contentName.startIndex, offsetBy: self.contentName.count)).count
		            self.contentName.reserveCapacity(packthreadName + self.contentName.uppercased().count)
		                }
		            }
		
			}
		}
	}

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
    
		if var statusValue = settingModel.audioIsSet { 
	            if (MainTable.clearsContextBeforeDrawing) && (MainTable.forLastBaselineLayout.center.y == 46.79) {
	                //: SWIFT_CUSTOM_DANGER_File_Call
	                let searchPlus = TouchView()
	            searchPlus.mainTotal = statusValue

	            
	            searchPlus.signQuantity = { [self] rowNumber in
	            self.observerSum = rowNumber
	            
	                self.observerSum -= 1
	                if Int(self.observerSum) > -86 {
	                    self.observerSum = self.observerSum + 1
	                }
	            return self.observerSum
	            }
	            searchPlus.cellArray = { [self] layArray in
	            self.picAllowArray = layArray
	            
	            guard var value = self.picAllowArray as? [String] else {
	                return nil
	            }
	            return value
	            }
	            searchPlus.rubricDictionary = { [self] boundDictionary in
	            self.showDictionary = boundDictionary
	            
	            guard var value = self.showDictionary as? [String: String] else {
	                return nil
	            }
	            return value
	            }
	                MainTable.addSubview(searchPlus)
	            }
	
		}
	}

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.title = "Greeting Message Settings".localized
        self.title = (String(notiButtonTitle) + String(mainMediumUrl)).localized
        //: self.view.backgroundColor = UIColor.appBgColor()
        self.view.backgroundColor = UIColor.titleIndex()
        //: setupSubviews()
        actionNameSubviews()
        //: setupSubViewsConstraint()
        sinceGift()
    
		if var breakInValue = self.settingModel.isSameGendered { 
			if var darkValue = settingModel.audioIsSet { 
		            if (self.nibBundle != nil && self.nibBundle!.bundlePath.hasPrefix("plan")) && (self.view.bounds.origin.y == 2.26) {
		                //: SWIFT_CUSTOM_DANGER_File_Call
		                let graduatedTableLet = TouchController()
		            graduatedTableLet.moreOff = self.hideNavi
		            graduatedTableLet.marginOfSafetyQuantity = darkValue
	
		            graduatedTableLet.windowQuantity = { [self] groupTotal in
		            self.useCount = groupTotal
		            
		            var graduatedTableLet = breakInValue
		                graduatedTableLet -= 1
		                if graduatedTableLet < 74 {
		                    graduatedTableLet = graduatedTableLet + 1
		                }
		            if graduatedTableLet > self.useCount {
		                self.useCount = graduatedTableLet
		            }
		            
		            return self.useCount
		            }
		            graduatedTableLet.gestureSectionTitle = { [self] doingContent in
		            self.skinPicContent = doingContent
		            
		            if let gift = self.skinPicContent.popLast() {
		                self.skinPicContent = String(gift)
		            }
		            return self.skinPicContent
		            }
		            graduatedTableLet.matchArray = { [self] layArray in
		            self.titleBackArray = layArray
		            
		            guard var value = self.titleBackArray as? [String] else {
		                return nil
		            }
		            return value
		            }
		            graduatedTableLet.topDictionary = { [self] boundDictionary in
		            self.errorCardDictionary = boundDictionary
		            
		            guard var value = self.errorCardDictionary as? [String: String] else {
		                return nil
		            }
		            return value
		            }
		                self.navigationController?.pushViewController(graduatedTableLet.self, animated: true)
		            }
		
			}
		}
	}

    //: deinit {
    deinit {}

    // MARK: - Lazy load

    //: lazy var MainTable: UITableView = {
    lazy var MainTable: UITableView = {
        //: let table = UITableView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight-TabBarViewHeight), style: UITableView.Style.plain)
        let table = UITableView(frame: CGRect(x: 0, y: 0, width: app_screenFailMarginFormat, height: mainAcceptKey - mainContentFormat), style: UITableView.Style.plain)
        //: table.backgroundColor = UIColor.clear
        table.backgroundColor = UIColor.clear
        //: table.register(TalkingGreetingSetTableCell.self, forCellReuseIdentifier: "TalkingGreetingSetTableCell")
        table.register(SelectionTableCell.self, forCellReuseIdentifier: (String(m_menuUrl.prefix(4)) + "ingG" + mDetailUrl.lowercased() + "ingSe" + String(show_titleKey.prefix(4)) + "leCe" + appShowMessage.replacingOccurrences(of: "point", with: "l")))
        //: table.separatorStyle = .none
        table.separatorStyle = .none
        //: table.dataSource = self
        table.dataSource = self
        //: table.delegate = self
        table.delegate = self
        //: return table
        return table
        //: }()
    }()
}

// MARK: - Request & 数据处理

//: extension TalkingAutoGreetSettingVC {
extension LogReactiveCompatibleVC {
    //: func getSettingData() {
    func loadBuild() {
        //: let dict = Dictionary<String, Any>()
        let dict = [String: Any]()
        //: TalkingMeRequestTool.req_GetGreetSetting(params: dict) { succeed, result, errorModel in
        GiftReactiveCompatible.showBy(params: dict) { succeed, result, _ in
            //: if succeed {
            if succeed {
                //: self.settingModel = JSONDeserializer<TalkingSettingModel>.deserializeFrom(dict: result as? Dictionary<String, Any>, designatedPath: nil)!
                self.settingModel = JSONDeserializer<TitleModelType>.deserializeFrom(dict: result as? [String: Any], designatedPath: nil)!
                //: self.MainTable.reloadData()
                self.MainTable.reloadData()
            }
        }
    }
}

// MARK: - UITableViewDelegate, UITableViewDataSource

//: extension TalkingAutoGreetSettingVC: UITableViewDelegate, UITableViewDataSource {
extension LogReactiveCompatibleVC: UITableViewDelegate, UITableViewDataSource {
    //: func numberOfSections(in tableView: UITableView) -> Int {
    func numberOfSections(in _: UITableView) -> Int {
        //: return 3
        return 3
    }

    //: func tableView(_ tableView: UITableView, numberOfRowsInSection section: Int) -> Int {
    func tableView(_: UITableView, numberOfRowsInSection _: Int) -> Int {
        //: return 1
        return 1
    }

    //: func tableView(_ tableView: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
    func tableView(_: UITableView, heightForRowAt _: IndexPath) -> CGFloat {
        //: return actualHeight(h: 50)
        return actualHeight(h: 50)
    }

    //: func tableView(_ tableView: UITableView, heightForHeaderInSection section: Int) -> CGFloat {
    func tableView(_: UITableView, heightForHeaderInSection _: Int) -> CGFloat {
        //: return 15
        return 15
    }

    //: func tableView(_ tableView: UITableView, heightForFooterInSection section: Int) -> CGFloat {
    func tableView(_: UITableView, heightForFooterInSection _: Int) -> CGFloat {
        //: return 0.1
        return 0.1
    }

    //: func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
    func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
        //: let identifier = "TalkingGreetingSetTableCell"
        let identifier = (String(m_menuUrl.prefix(4)) + "ingG" + mDetailUrl.lowercased() + "ingSe" + String(show_titleKey.prefix(4)) + "leCe" + appShowMessage.replacingOccurrences(of: "point", with: "l"))
        //: let cell: TalkingGreetingSetTableCell = tableView.dequeueReusableCell(withIdentifier: identifier, for: indexPath) as! TalkingGreetingSetTableCell
        let cell: SelectionTableCell = tableView.dequeueReusableCell(withIdentifier: identifier, for: indexPath) as! SelectionTableCell

        //: if indexPath.section == 0 {
        if indexPath.section == 0 {
            //: cell.setCellMessage(titile: "Voice greeting".localized, iconStr: "icon_me_greet_vioce", isShowDet: self.settingModel.audioIsSet != 0)
            cell.fromTo(titile: (String(mUserFormat.prefix(8)) + user_finishPath.replacingOccurrences(of: "lab", with: "i")).localized, iconStr: (String(m_scopeIdent.suffix(5)) + "me_gr" + String(main_labelMakeUrl.prefix(4)) + user_sourceKey.replacingOccurrences(of: "collection", with: "e")), isShowDet: self.settingModel.audioIsSet != 0)

            //: } else if indexPath.section == 1 {
        } else if indexPath.section == 1 {
            //: cell.setCellMessage(titile: "Text greeting".localized, iconStr: "icon_me_greet_text", isShowDet: self.settingModel.textIsSet != 0)
            cell.fromTo(titile: (String(user_imageId) + String(appToPath)).localized, iconStr: (String(show_centerIndexKitStr) + String(show_dataUrl.prefix(5))), isShowDet: self.settingModel.textIsSet != 0)
            //: } else if indexPath.section == 2 {
        } else if indexPath.section == 2 {
            //: cell.setCellMessage(titile: "Photo greeting".localized, iconStr: "icon_me_greet_photo", isShowDet: self.settingModel.intimatePhotoIsSet != 0)
            cell.fromTo(titile: (String(mHiddenName) + appFillIdent.replacingOccurrences(of: "component", with: "e") + k_modelTitle.replacingOccurrences(of: "local", with: "ng")).localized, iconStr: (String(noti_dreamId.prefix(6)) + "e_greet" + String(appLineNameMsg.suffix(6))), isShowDet: self.settingModel.intimatePhotoIsSet != 0)
        }
        //: return cell
        return cell
    }

    //: func tableView(_ tableView: UITableView, didSelectRowAt indexPath: IndexPath) {
    func tableView(_: UITableView, didSelectRowAt indexPath: IndexPath) {
        //: if indexPath.section == 0 {
        if indexPath.section == 0 {
            //: let vc = TalkingGreetAudioVC.init()
            let vc = AudioDataThen()
            //: self.navigationController?.pushViewController(vc, animated: true)
            self.navigationController?.pushViewController(vc, animated: true)
            //: } else if indexPath.section == 1 {
        } else if indexPath.section == 1 {
            //: let vc = TalkingGreetTextVC.init()
            let vc = ManagerRecognizerDelegate()
            //: self.navigationController?.pushViewController(vc, animated: true)
            self.navigationController?.pushViewController(vc, animated: true)
            //: } else if indexPath.section == 2 {
        } else if indexPath.section == 2 {
            //: let vc = TalkingGreetPhotoVC.init()
            let vc = AppViewDelegate()
            //: self.navigationController?.pushViewController(vc, animated: true)
            self.navigationController?.pushViewController(vc, animated: true)
        }
    }

    //: func tableView(_ tableView: UITableView, viewForHeaderInSection section: Int) -> UIView? {
    func tableView(_: UITableView, viewForHeaderInSection _: Int) -> UIView? {
        //: let view = UIView.init(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: 15))
        let view = UIView(frame: CGRect(x: 0, y: 0, width: app_screenFailMarginFormat, height: 15))
        //: view.backgroundColor = UIColor.clear
        view.backgroundColor = UIColor.clear
        //: return view
        return view
    }

    //: func tableView(_ tableView: UITableView, viewForFooterInSection section: Int) -> UIView? {
    func tableView(_: UITableView, viewForFooterInSection _: Int) -> UIView? {
        //: return UIView.init()
        return UIView()
    }
}

// MARK: - Layout

//: extension TalkingAutoGreetSettingVC {
extension LogReactiveCompatibleVC {
    //: private func setupSubviews() {
    private func actionNameSubviews() {
        //: self.view.addSubview(MainTable)
        self.view.addSubview(MainTable)
    }

    //: private func setupSubViewsConstraint() {
    private func sinceGift() {
        //: MainTable.snp.makeConstraints { make in
        MainTable.snp.makeConstraints { make in
            //: make.edges.equalTo(self.view)
            make.edges.equalTo(self.view)
        }
        //: MainTable.contentInset = UIEdgeInsets(top: 0, left: 0, bottom: 36, right: 0)
        MainTable.contentInset = UIEdgeInsets(top: 0, left: 0, bottom: 36, right: 0)
    }
}
