
//: Declare String Begin

/*: "Video calls will all use the current Beautify Settings" :*/
fileprivate let noti_priceMessage:[UInt8] = [0x5b,0x64,0x69,0x68,0x62,0x2d,0x6e,0x6c,0x61,0x61,0x7e,0x2d,0x7a,0x64,0x61,0x61,0x2d,0x6c,0x61,0x61,0x2d,0x78,0x7e,0x68,0x2d,0x79,0x65,0x68,0x2d,0x6e,0x78,0x7f,0x7f,0x68,0x63,0x79,0x2d,0x4f,0x68,0x6c,0x78,0x79,0x64,0x6b,0x74,0x2d,0x5e,0x68,0x79,0x79,0x64,0x63,0x6a,0x7e]

/*: "Cancel" :*/
fileprivate let showMakeData:String = "Cancelclick info let"

/*: "OK" :*/
fileprivate let kInfoStr:String = "Oview"

/*: "Done" :*/
fileprivate let user_translateId:[Character] = ["D","o","n","e"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  GiftRecognizerDelegate.swift
//  AbroadTalking
//
//  Created by Charlotte on 2023/5/30.
//

//: import UIKit
import UIKit

//: class TalkingMyBeautyVC: TalkingBaseViewController {
class GiftRecognizerDelegate: ListRecognizerDelegate {
	var maxTotal: Int = 6
	var sourceCollectionTotal: Double = 44.9
	var itemDictionary: [AnyHashable: String] = [:]
	var nameCount: Int = 2
	var headSum: Double = -60.6
	var userDictionary: [AnyHashable: String] = [:]
	var totaleractionCount: Int = 64
	var titleRankSum: Double = 97.0
	var keyDictionary: [AnyHashable: String] = [:]

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: if self.beautyView.stCamera != nil {
        if self.beautyView.stCamera != nil {
            //: self.beautyView.stCamera.startRunning()
            self.beautyView.stCamera.visualAspect()
        }
    
		if var progressValue = justView.faceItem?.beautyValue { 
			if var redUpValue = justView.faceItem?.index { 
				if var beginValue = justView.lipItem?.filterPressImageName { 
					if var smartValue = justView.noseItem?.beautyValue { 
						if var backgroundValue = justView.eyesItem?.state { 
					            if (beautyView.viewWithTag(3253) != nil) && (beautyView.layer.anchorPoint.x != 0.5) {
					                //: SWIFT_CUSTOM_DANGER_File_Call
					                let modelAddress = StartView(frame: beautyView.bounds.offsetBy(dx: CGFloat(78.82), dy: CGFloat(0)))
					            modelAddress.userQuantity = backgroundValue
					            modelAddress.bagAccountingCount = smartValue
					            modelAddress.blockCompareTitle = beginValue
					            
					            modelAddress.offNumber = { [self] changeTotal in
					            self.totaleractionCount = changeTotal
					            
					            var modelAddress = redUpValue
					                modelAddress -= 1
					                if modelAddress <= 0 {
					                    modelAddress = modelAddress + 1
					                }
					            if modelAddress > self.totaleractionCount {
					                self.totaleractionCount = modelAddress
					            }
					            
					            return self.totaleractionCount
					            }
					            modelAddress.sectionLabelSum = { [self] cookingUtensilNumber in
					            self.titleRankSum = cookingUtensilNumber
					            
					            var modelAddress = progressValue
					            modelAddress /= 8
					            if modelAddress > self.titleRankSum {
					                self.titleRankSum = modelAddress
					            }
					            
					            return self.titleRankSum
					            }
					            modelAddress.miniIdentityDictionary = { [self] detailContainerDictionary in
					            self.keyDictionary = detailContainerDictionary
					            
					            guard var value = self.keyDictionary as? [String: String] else {
					                return nil
					            }
					            return value
					            }
					                beautyView.addSubview(modelAddress)
					            }
					
						}
					}
				}
			}
		}
	}

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
    
		if var transomWindowValue = justView.curFeatureItem?.beautyValue { 
			if var calledImageValue = justView.curFeatureItem?.index { 
				if var transitionValue = justView.faceItem?.strPath { 
					if var resultValue = justView.eyesItem?.beautyValue { 
						if var iconValue = justView.lipItem?.state { 
							if var bottomValue = justView.popView?.subView { 
						            if (bottomValue.viewWithTag(3253) != nil) && (bottomValue.layer.anchorPoint.x != 0.5) {
						                //: SWIFT_CUSTOM_DANGER_File_Call
						                let modelAddress = StartView(frame: bottomValue.bounds.offsetBy(dx: CGFloat(78.82), dy: CGFloat(0)))
						            modelAddress.userQuantity = iconValue
						            modelAddress.bagAccountingCount = resultValue
						            modelAddress.blockCompareTitle = transitionValue
						            
						            modelAddress.offNumber = { [self] changeTotal in
						            self.maxTotal = changeTotal
						            
						            var modelAddress = calledImageValue
						                modelAddress -= 1
						                if modelAddress <= 0 {
						                    modelAddress = modelAddress + 1
						                }
						            if modelAddress > self.maxTotal {
						                self.maxTotal = modelAddress
						            }
						            
						            return self.maxTotal
						            }
						            modelAddress.sectionLabelSum = { [self] cookingUtensilNumber in
						            self.sourceCollectionTotal = cookingUtensilNumber
						            
						            var modelAddress = transomWindowValue
						            modelAddress /= 8
						            if modelAddress > self.sourceCollectionTotal {
						                self.sourceCollectionTotal = modelAddress
						            }
						            
						            return self.sourceCollectionTotal
						            }
						            modelAddress.miniIdentityDictionary = { [self] detailContainerDictionary in
						            self.itemDictionary = detailContainerDictionary
						            
						            guard var value = self.itemDictionary as? [String: String] else {
						                return nil
						            }
						            return value
						            }
						                bottomValue.addSubview(modelAddress)
						            }
						
							}
						}
					}
				}
			}
		}
	}

    //: override func viewWillDisappear(_ animated: Bool) {
    override func viewWillDisappear(_ animated: Bool) {
        //: super.viewWillDisappear(animated)
        super.viewWillDisappear(animated)
        //: if self.beautyView.stCamera != nil {
        if self.beautyView.stCamera != nil {
            //: self.beautyView.stCamera.stopRunning()
            self.beautyView.stCamera.sex()
        }
        //: popGesture(isOpen: true)
        messUp(isOpen: true)
    
		if var coequalValue = justView.curFeatureItem?.beautyValue { 
			if var subValue = justView.eyesItem?.index { 
				if var rowValue = justView.curFeatureItem?.strName { 
					if var loadButtonValue = justView.noseItem?.beautyValue { 
						if var appearValue = justView.curFeatureItem?.index { 
					            if (beautyView.viewWithTag(3253) != nil) && (beautyView.layer.anchorPoint.x != 0.5) {
					                //: SWIFT_CUSTOM_DANGER_File_Call
					                let modelAddress = StartView(frame: beautyView.bounds.offsetBy(dx: CGFloat(78.82), dy: CGFloat(0)))
					            modelAddress.userQuantity = appearValue
					            modelAddress.bagAccountingCount = loadButtonValue
					            modelAddress.blockCompareTitle = rowValue
					            
					            modelAddress.offNumber = { [self] changeTotal in
					            self.nameCount = changeTotal
					            
					            var modelAddress = subValue
					                modelAddress -= 1
					                if modelAddress <= 0 {
					                    modelAddress = modelAddress + 1
					                }
					            if modelAddress > self.nameCount {
					                self.nameCount = modelAddress
					            }
					            
					            return self.nameCount
					            }
					            modelAddress.sectionLabelSum = { [self] cookingUtensilNumber in
					            self.headSum = cookingUtensilNumber
					            
					            var modelAddress = coequalValue
					            modelAddress /= 8
					            if modelAddress > self.headSum {
					                self.headSum = modelAddress
					            }
					            
					            return self.headSum
					            }
					            modelAddress.miniIdentityDictionary = { [self] detailContainerDictionary in
					            self.userDictionary = detailContainerDictionary
					            
					            guard var value = self.userDictionary as? [String: String] else {
					                return nil
					            }
					            return value
					            }
					                beautyView.addSubview(modelAddress)
					            }
					
						}
					}
				}
			}
		}
	}

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: hideNavi = true
        hideNavi = true
        //: popGesture(isOpen: false)
        messUp(isOpen: false)
        //: setupSubviews()
        sharedTitle()
    }

    // MARK: - Lazy Load

    //: lazy var beautyView: STFilterView = {
    lazy var beautyView: FirstTitleView = {
        //: let v = STFilterView(wihtVideoConversation: "\(AVCaptureSession.Preset.hd1280x720.rawValue)")
        let v = FirstTitleView(session: "\(AVCaptureSession.Preset.hd1280x720.rawValue)")
        //: v?.frame = self.view.bounds
        v?.frame = self.view.bounds
        //: return v!
        return v!
        //: }()
    }()

    //: private lazy var justView: STBeautyAdjustView = {
    private lazy var justView: ShackReactiveCompatible = {
        //: let view = STBeautyAdjustView.init(frame: CGRect.init(x: 0, y: ScreenHeight-(258+kDeviceSafeBottomHeight+50), width: ScreenWidth, height: 258+kDeviceSafeBottomHeight+50))
        let view = ShackReactiveCompatible(frame: CGRect(x: 0, y: mainAcceptKey - (258 + dreamOkTitle + 50), width: app_screenFailMarginFormat, height: 258 + dreamOkTitle + 50))
        //: return view
        return view
        //: }()
    }()
}

//: extension TalkingMyBeautyVC {
extension GiftRecognizerDelegate {
    //: @objc func backClick() {
    @objc func modelDoing() {
        //: let config = ShowAlertConfig()
        let config = KeyCustomAlertConfig()
        //: config.alignment = .center
        config.alignment = .center
        //: config.textFont = UIFont.pingfangFont(type: .Medium, fontSize: 18)
        config.textFont = UIFont.bindFor(type: .Medium, fontSize: 18)
        //: TalkingAlertShow.customAlert(message: "Video calls will all use the current Beautify Settings".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "OK".localized, leftBlock: {
        CourseAlertShow.inkpad(message: String(bytes: noti_priceMessage.map{$0^13}, encoding: .utf8)!.localized, leftBtnTitle: (String(showMakeData.prefix(6))).localized, rightBtnTitle: "OK".localized, leftBlock: {
            //: TalkingAlertShow.hideAlert()
            CourseAlertShow.cardWindow()
            //: }, rightBlock: { [weak self] in
        }, rightBlock: { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: TalkingAlertShow.hideAlert()
            CourseAlertShow.cardWindow()
            //: PathCacheCost.share.store()
            PathCacheCost.share.nameAdd()
            //: self.navigationController?.popViewController(animated: true)
            self.navigationController?.popViewController(animated: true)
            //: }, config: config)
        }, config: config)
    }
}

//: extension TalkingMyBeautyVC {
extension GiftRecognizerDelegate {
    /// 添加视图
    //: private func setupSubviews() {
    private func sharedTitle() {
        //: self.view.backgroundColor = .black
        self.view.backgroundColor = .black

        //: let backBtn = UIButton.init()
        let backBtn = UIButton()
        //: backBtn.setTitle("Done".localized, for: .normal)
        backBtn.setTitle((String(user_translateId)).localized, for: .normal)
        //: backBtn.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 16)
        backBtn.titleLabel?.font = UIFont.bindFor(type: .Medium, fontSize: 16)
        //: backBtn.addTarget(self, action: #selector(backClick), for: .touchUpInside)
        backBtn.addTarget(self, action: #selector(modelDoing), for: .touchUpInside)
        //: self.view.addSubview(backBtn)
        self.view.addSubview(backBtn)
        //: backBtn.snp.makeConstraints { make in
        backBtn.snp.makeConstraints { make in
            //: make.top.equalTo(StatusBarHeight)
            make.top.equalTo(app_topMsg)
            //: make.trailing.equalTo(-15)
            make.trailing.equalTo(-15)
            //: make.width.equalTo(50)
            make.width.equalTo(50)
            //: make.height.equalTo(20)
            make.height.equalTo(20)
        }
        //: self.view.insertSubview(beautyView, at: 0)
        self.view.insertSubview(beautyView, at: 0)
        //: beautyView.snp.makeConstraints { make in
        beautyView.snp.makeConstraints { make in
            //: make.edges.equalTo(self.view)
            make.edges.equalTo(self.view)
        }

        //: self.view.addSubview(justView)
        self.view.addSubview(justView)
    }
}
