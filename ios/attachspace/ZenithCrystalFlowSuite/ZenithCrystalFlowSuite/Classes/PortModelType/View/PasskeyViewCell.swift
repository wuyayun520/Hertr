
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let dream_labelEqualUrl:[UInt8] = [0x85,0x8a,0x85,0x90,0x44,0x7f,0x8b,0x80,0x81,0x8e,0x56,0x45,0x3c,0x84,0x7d,0x8f,0x3c,0x8a,0x8b,0x90,0x3c,0x7e,0x81,0x81,0x8a,0x3c,0x85,0x89,0x8c,0x88,0x81,0x89,0x81,0x8a,0x90,0x81,0x80]

fileprivate func streamLeading(size num: UInt8) -> UInt8 {
    let value = Int(num) - 28
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "icon_home_v" :*/
fileprivate let k_liveMessage:String = "icon_hompath top dream model round"
fileprivate let notiViewData:[Character] = ["e","_","v"]

/*: "Delete" :*/
fileprivate let mainEventPath:String = "icon i data heightDelete"

/*: "ion_blacklist_back" :*/
fileprivate let dream_arrayFormat:String = "ion_bbackground minimum let"
fileprivate let app_enableModelName:String = "type layer head data_back"

/*: "icon_home_boy" :*/
fileprivate let dream_systemMsg:String = "icon_homnormal bind view of bottom"
fileprivate let notiIndexName:String = "e_boyprepare if from"

/*: "icon_home_girl" :*/
fileprivate let k_imageId:[Character] = ["i","c","o","n","_","h","o"]
fileprivate let mainLightTakeSystemMsg:String = "data exception point model viewme_girl"

/*: "   " :*/
fileprivate let show_shadowNameWithPath:String = "   "

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  PasskeyViewCell.swift
//  AbroadTalking
//
//  Created by Charlotte on 2022/9/16.
//

//: import UIKit
import UIKit

//: typealias DelteBlckIconBlock = (Int) ->()
typealias DelteBlckIconBlock = (Int) -> Void

//: class TalkingBlacklistCell: UITableViewCell {
class PasskeyViewCell: UITableViewCell {
	var searchEnable: Bool = true
	var fileCount: Double = 21.5
	var quantityEnable: Bool = false
	var bestowmentMagnitude: Double = 63.0
	var areaOff: Bool = false
	var giftInterval: Double = 34.6

    //: var currenModel = TalkingBlacklistModel()
    var currenModel = StraddleHandyJSON()
    //: var index = 0
    var index = 0
    //: var deleteBlock: DelteBlckIconBlock!
    var deleteBlock: DelteBlckIconBlock!

    //: override func awakeFromNib() {
    override func awakeFromNib() {
        //: super.awakeFromNib()
        super.awakeFromNib()
    
		if var blockValue = currenModel.isTPAuth { 
	            if (!IconBtn.autoresizesSubviews) && (IconBtn.forLastBaselineLayout.center.y == 28.13) {
	                //: SWIFT_CUSTOM_DANGER_File_Call
	                let simultaneouslyViewLet = UserView()
	
	            
	            simultaneouslyViewLet.styleClose = { [self] labelDoing in
	            self.quantityEnable = labelDoing
	            
	            var simultaneouslyViewLet = blockValue
	                simultaneouslyViewLet = false
	                simultaneouslyViewLet = !simultaneouslyViewLet
	            if simultaneouslyViewLet != self.quantityEnable {
	                self.quantityEnable = simultaneouslyViewLet
	            }
	            
	                self.quantityEnable = true
	                self.quantityEnable = false
	            return self.quantityEnable
	            }
	            simultaneouslyViewLet.geneticMutationTotal = { [self] frameInterval in
	            self.bestowmentMagnitude = frameInterval
	            
	                self.bestowmentMagnitude -= 1
	                if self.bestowmentMagnitude < 77 {
	                    self.bestowmentMagnitude = self.bestowmentMagnitude + 1
	                }
	            return self.bestowmentMagnitude
	            }
	                IconBtn.addSubview(simultaneouslyViewLet)
	            }
	
		}
	}

    //: override func setSelected(_ selected: Bool, animated: Bool) {
    override func setSelected(_ selected: Bool, animated: Bool) {
        //: super.setSelected(selected, animated: animated)
        super.setSelected(selected, animated: animated)
    
		if var errorValue = self.currenModel.isTPAuth { 
	            if (IconBtn.layer.contentsRect.size.height != 1) && (IconBtn.layer.anchorPointZ != 0) {
	                //: SWIFT_CUSTOM_DANGER_File_Call
	                let listParty = UserView(frame: IconBtn.bounds)
	
	            
	            listParty.styleClose = { [self] labelDoing in
	            self.areaOff = labelDoing
	            
	            var listParty = errorValue
	                listParty = false
	                listParty = false
	            if listParty != self.areaOff {
	                self.areaOff = listParty
	            }
	            
	                self.areaOff = false
	                self.areaOff = false
	            return self.areaOff
	            }
	            listParty.geneticMutationTotal = { [self] frameInterval in
	            self.giftInterval = frameInterval
	            
	                self.giftInterval += 1
	                if self.giftInterval < 75 {
	                    self.giftInterval = self.giftInterval - 1
	                }
	            return self.giftInterval
	            }
	                IconBtn.addSubview(listParty)
	            }
	
		}
	}

    //: required init?(coder aDecoder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: dream_labelEqualUrl.map{streamLeading(size: $0)}, encoding: .utf8)!)
    }

    //: override func layoutSubviews() {
    override func layoutSubviews() {
        //: super.layoutSubviews()
        super.layoutSubviews()
    
		if var directionValue = currenModel.isTPAuth { 
	            if (IconBtn.layer.contentsRect.size.height != 1) && (IconBtn.layer.anchorPointZ != 0) {
	                //: SWIFT_CUSTOM_DANGER_File_Call
	                let listParty = UserView(frame: IconBtn.bounds)
	
	            
	            listParty.styleClose = { [self] labelDoing in
	            self.searchEnable = labelDoing
	            
	            var listParty = directionValue
	                listParty = false
	                listParty = false
	            if listParty != self.searchEnable {
	                self.searchEnable = listParty
	            }
	            
	                self.searchEnable = false
	                self.searchEnable = false
	            return self.searchEnable
	            }
	            listParty.geneticMutationTotal = { [self] frameInterval in
	            self.fileCount = frameInterval
	            
	                self.fileCount += 1
	                if self.fileCount < 75 {
	                    self.fileCount = self.fileCount - 1
	                }
	            return self.fileCount
	            }
	                IconBtn.addSubview(listParty)
	            }
	
		}
	}

    //: override init(style: UITableViewCell.CellStyle, reuseIdentifier: String?) {
    override init(style: UITableViewCell.CellStyle, reuseIdentifier: String?) {
        //: super.init(style: style, reuseIdentifier: reuseIdentifier)
        super.init(style: style, reuseIdentifier: reuseIdentifier)
        //: selectionStyle = UITableViewCell.SelectionStyle.none
        selectionStyle = UITableViewCell.SelectionStyle.none
        //: self.backgroundColor = UIColor.clear
        self.backgroundColor = UIColor.clear
        //: self.contentView.backgroundColor = UIColor.clear
        self.contentView.backgroundColor = UIColor.clear
        //: self.contentView.addSubview(IconBtn)
        self.contentView.addSubview(IconBtn)
        //: self.contentView.addSubview(nameLabel)
        self.contentView.addSubview(nameLabel)
        //: self.contentView.addSubview(cardImg)
        self.contentView.addSubview(cardImg)
        //: self.contentView.addSubview(sexBtn)
        self.contentView.addSubview(sexBtn)
        //: self.contentView.addSubview(RemoveBtn)
        self.contentView.addSubview(RemoveBtn)

        //: IconBtn.snp.makeConstraints { make in
        IconBtn.snp.makeConstraints { make in
            //: make.leading.equalTo(self).offset(15)
            make.leading.equalTo(self).offset(15)
            //: make.top.equalTo(self).offset(8)
            make.top.equalTo(self).offset(8)
            //: make.width.height.equalTo(40)
            make.width.height.equalTo(40)
        }
        //: nameLabel.snp.makeConstraints { make in
        nameLabel.snp.makeConstraints { make in
            //: make.leading.equalTo(IconBtn.snp.trailing).offset(12)
            make.leading.equalTo(IconBtn.snp.trailing).offset(12)
            //: make.top.equalTo(self).offset(17)
            make.top.equalTo(self).offset(17)
            //: make.height.equalTo(21)
            make.height.equalTo(21)
        }
        //: cardImg.snp.makeConstraints { make in
        cardImg.snp.makeConstraints { make in
            //: make.leading.equalTo(nameLabel.snp.trailing).offset(4)
            make.leading.equalTo(nameLabel.snp.trailing).offset(4)
            //: make.top.equalTo(self).offset(20)
            make.top.equalTo(self).offset(20)
            //: make.height.equalTo(16)
            make.height.equalTo(16)
            //: make.width.equalTo(16)
            make.width.equalTo(16)
        }
        //: sexBtn.snp.makeConstraints { make in
        sexBtn.snp.makeConstraints { make in
            //: make.leading.equalTo(cardImg.snp.trailing).offset(4)
            make.leading.equalTo(cardImg.snp.trailing).offset(4)
            //: make.top.equalTo(self).offset(20)
            make.top.equalTo(self).offset(20)
            //: make.height.equalTo(16)
            make.height.equalTo(16)
            //: make.width.equalTo(33)
            make.width.equalTo(33)
            //: make.trailing.lessThanOrEqualTo(RemoveBtn.snp.leading).offset(-5)
            make.trailing.lessThanOrEqualTo(RemoveBtn.snp.leading).offset(-5)
        }
        //: RemoveBtn.snp.makeConstraints { make in
        RemoveBtn.snp.makeConstraints { make in
            //: make.trailing.equalTo(self.snp.trailing).offset(-15)
            make.trailing.equalTo(self.snp.trailing).offset(-15)
            //: make.centerY.equalTo(self)
            make.centerY.equalTo(self)
            //: make.height.equalTo(34)
            make.height.equalTo(34)
            //: make.width.equalTo(89)
            make.width.equalTo(89)
        }
    }

    // MARK: - Lazy load

    //: lazy var IconBtn: UIButton = {
    lazy var IconBtn: UIButton = {
        //: let btn = UIButton.init()
        let btn = UIButton()
        //: btn.layer.cornerRadius = 5
        btn.layer.cornerRadius = 5
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        //: btn.addTarget(self, action: #selector(IconBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(dayClick), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    //: lazy var nameLabel: UILabel = {
    lazy var nameLabel: UILabel = {
        //: let label = UILabel.init()
        let label = UILabel()
        //: label.font = .pingfangFont(type: .Medium, fontSize: 18)
        label.font = .bindFor(type: .Medium, fontSize: 18)
        //: label.textColor = .appTitleColor()
        label.textColor = .statusBy()
        //: return label
        return label
        //: }()
    }()

    //: lazy var cardImg: UIImageView = {
    lazy var cardImg: UIImageView = {
        //: let img = UIImageView.init()
        let img = UIImageView()
        //: img.image = UIImage.BundleImageNamed(name: "icon_home_v")
        img.image = UIImage.sendCollection(name: (String(k_liveMessage.prefix(8)) + String(notiViewData)))
        //: img.contentMode = .scaleAspectFill
        img.contentMode = .scaleAspectFill
        //: return img
        return img
        //: }()
    }()

    //: lazy var sexBtn: UIButton = {
    lazy var sexBtn: UIButton = {
        //: let btn = UIButton.init()
        let btn = UIButton()
        //: btn.setTitleColor(UIColor.white, for: .normal)
        btn.setTitleColor(UIColor.white, for: .normal)
        //: btn.titleLabel?.font = .pingfangFont(type: .Medium, fontSize: 10)
        btn.titleLabel?.font = .bindFor(type: .Medium, fontSize: 10)
        //: return btn
        return btn
        //: }()
    }()

    //: lazy var RemoveBtn: UIButton = {
    lazy var RemoveBtn: UIButton = {
        //: let btn = UIButton.init()
        let btn = UIButton()
        //: btn.setTitle("Delete".localized, for: .normal)
        btn.setTitle((String(mainEventPath.suffix(6))).localized, for: .normal)
        //: btn.setTitleColor(UIColor.appThemeColor(), for: .normal)
        btn.setTitleColor(UIColor.theBeauty(), for: .normal)
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Regular, fontSize: 16)
        btn.titleLabel?.font = UIFont.bindFor(type: .Regular, fontSize: 16)
        //: btn.setBackgroundImage(UIImage.BundleImageNamed(name: "ion_blacklist_back"), for: .normal)
        btn.setBackgroundImage(UIImage.sendCollection(name: (String(dream_arrayFormat.prefix(5)) + "lacklist" + String(app_enableModelName.suffix(5)))), for: .normal)
        //: btn.addTarget(self, action: #selector(RemoveBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(constraint), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()
}

// MARK: - Event

//: extension TalkingBlacklistCell {
extension PasskeyViewCell {
    //: func setCell(model: TalkingBlacklistModel, index: Int) {
    func removeChildStatus(model: StraddleHandyJSON, index: Int) {
        //: currenModel = model
        currenModel = model
        //: self.index = index
        self.index = index
        //: IconBtn.setUrlImage(urlStr: model.headPic ?? "")
        IconBtn.lean(urlStr: model.headPic ?? "")
        //: nameLabel.text = model.nickname
        nameLabel.text = model.nickname

        //: if model.isTPAuth == false {
        if model.isTPAuth == false {
            //: cardImg.isHidden = true
            cardImg.isHidden = true
            //: cardImg.snp.updateConstraints { make in
            cardImg.snp.updateConstraints { make in
                //: make.width.equalTo(1)
                make.width.equalTo(1)
                //: make.leading.equalTo(nameLabel.snp.trailing).offset(0)
                make.leading.equalTo(nameLabel.snp.trailing).offset(0)
            }
            //: } else {
        } else {
            //: cardImg.isHidden = false
            cardImg.isHidden = false
            //: cardImg.snp.updateConstraints { make in
            cardImg.snp.updateConstraints { make in
                //: make.leading.equalTo(nameLabel.snp.trailing).offset(4)
                make.leading.equalTo(nameLabel.snp.trailing).offset(4)
                //: make.width.equalTo(16)
                make.width.equalTo(16)
            }
        }
        //: switch model.sex {
        switch model.sex {
        //: case 1:
        case 1:
            //: sexBtn.setBackgroundImage(UIImage.BundleImageNamed(name: "icon_home_boy"), for: .normal)
            sexBtn.setBackgroundImage(UIImage.sendCollection(name: (String(dream_systemMsg.prefix(8)) + String(notiIndexName.prefix(5)))), for: .normal)
        //: break
        //: case 2:
        case 2:
            //: sexBtn.setBackgroundImage(UIImage.BundleImageNamed(name: "icon_home_girl"), for: .normal)
            sexBtn.setBackgroundImage(UIImage.sendCollection(name: (String(k_imageId) + String(mainLightTakeSystemMsg.suffix(7)))), for: .normal)
        //: break
        //: default:
        default:
            //: break
            break
        }
        //: sexBtn.setTitle("   " + String(model.age!), for: .normal)
        sexBtn.setTitle("   " + String(model.age!), for: .normal)
    }

    //: @objc func IconBtnClick() {
    @objc func dayClick() {
        //: NamePushManager.share.func__pushToUserDetailVC(uid: currenModel.uid)
        NamePushManager.share.pressAway(uid: currenModel.uid)
    }

    //: @objc func RemoveBtnClick() {
    @objc func constraint() {
        //: if self.deleteBlock != nil {
        if self.deleteBlock != nil {
            //: self.deleteBlock(index)
            self.deleteBlock(index)
        }
    }
}
