
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let user_stateTextKey:[UInt8] = [0x3f,0x38,0x3f,0x22,0x7e,0x35,0x39,0x32,0x33,0x24,0x6c,0x7f,0x76,0x3e,0x37,0x25,0x76,0x38,0x39,0x22,0x76,0x34,0x33,0x33,0x38,0x76,0x3f,0x3b,0x26,0x3a,0x33,0x3b,0x33,0x38,0x22,0x33,0x32]

private func leadingCenter(of num: UInt8) -> UInt8 {
    return num ^ 86
}

/*: "Load..." :*/
fileprivate let appTaskStr:[Character] = ["L","o","a","d",".",".","."]

/*: "Skip" :*/
fileprivate let dream_requestLeaveMessage:String = "Skipmain gift string true format"

/*: "HttpTool.NativeToJs('recharge')" :*/
fileprivate let dreamTightToIdent:[UInt8] = [0x29,0x27,0x65,0x67,0x72,0x61,0x68,0x63,0x65,0x72,0x27,0x28,0x73,0x4a,0x6f,0x54,0x65,0x76,0x69,0x74,0x61,0x4e,0x2e,0x6c,0x6f,0x6f,0x54,0x70,0x74,0x74,0x48]

/*: "window.onPageShow&&onPageShow();" :*/
fileprivate let userOfficialPath:[UInt8] = [0x2f,0x31,0x36,0x3c,0x37,0x2f,0x76,0x37,0x36,0x8,0x39,0x3f,0x3d,0xb,0x30,0x37,0x2f,0x7e,0x7e,0x37,0x36,0x8,0x39,0x3f,0x3d,0xb,0x30,0x37,0x2f,0x70,0x71,0x63]

private func atomicSum85(value num: UInt8) -> UInt8 {
    return num ^ 88
}

/*: "jsEvent(onPageShow):  :*/
fileprivate let app_imageData:String = "value make bottomjsEven"
fileprivate let mEnterMessage:String = "type path custom letageSho"
fileprivate let appMakeCellKey:String = "w): empty video view cell label"

/*: --- :*/
fileprivate let m_modelUrl:String = "---"

/*: "window.onPageHide&&onPageHide();" :*/
fileprivate let main_productFileToMessage:[UInt8] = [0x3b,0x29,0x28,0x65,0x64,0x69,0x48,0x65,0x67,0x61,0x50,0x6e,0x6f,0x26,0x26,0x65,0x64,0x69,0x48,0x65,0x67,0x61,0x50,0x6e,0x6f,0x2e,0x77,0x6f,0x64,0x6e,0x69,0x77]

/*: "jsEvent(onPageHide):  :*/
fileprivate let dream_resignName:[Character] = ["j","s","E","v","e","n","t","(","o","n","P","a","g","e"]
fileprivate let dreamBottomBlockUrl:String = "image more of page letHide): "

/*: "title" :*/
fileprivate let main_willId:String = "titcamera"

/*: "canGoBack" :*/
fileprivate let notiFaceName:[Character] = ["c","a","n","G","o","B","a","c","k"]

/*: "PHPSESSID= :*/
fileprivate let dream_nameKey:[Character] = ["P","H","P"]
fileprivate let user_indexTitle:[Character] = ["S","E","S","S","I","D","="]

/*: ";uid= :*/
fileprivate let mainCellMsg:String = "class style selection;uid="

/*: "Cookie" :*/
fileprivate let showSendMessage:[Character] = ["C","o","o","k","i","e"]

/*: "nav_back_black_nor" :*/
fileprivate let notiTempMessage:[Character] = ["n","a","v","_","b","a"]
fileprivate let dreamPathMsg:String = "leading data day content appck_bla"
fileprivate let noti_eventId:String = "view make imageck_nor"

/*: "ico_intimate_list_sm" :*/
fileprivate let userInfoToolName:[Character] = ["i","c","o","_","i","n","t","i","m","a"]
fileprivate let app_createStr:[Character] = ["t","e","_","l","i","s","t","_","s","m"]

/*: "document.getElementsByTagName('body')[0].style.background='rgba(0,0,0,0)'" :*/
fileprivate let mainModelValue:[UInt8] = [0x27,0x29,0x30,0x2c,0x30,0x2c,0x30,0x2c,0x30,0x28,0x61,0x62,0x67,0x72,0x27,0x3d,0x64,0x6e,0x75,0x6f,0x72,0x67,0x6b,0x63,0x61,0x62,0x2e,0x65,0x6c,0x79,0x74,0x73,0x2e,0x5d,0x30,0x5b,0x29,0x27,0x79,0x64,0x6f,0x62,0x27,0x28,0x65,0x6d,0x61,0x4e,0x67,0x61,0x54,0x79,0x42,0x73,0x74,0x6e,0x65,0x6d,0x65,0x6c,0x45,0x74,0x65,0x67,0x2e,0x74,0x6e,0x65,0x6d,0x75,0x63,0x6f,0x64]

/*: "Alert" :*/
fileprivate let app_toValue:String = "let path row labelAlert"

/*: "OK" :*/
fileprivate let dreamByStr:[Character] = ["O","K"]

/*: "Cancle" :*/
fileprivate let userAsideTitle:String = "Canclemake self"

/*: "Done" :*/
fileprivate let dream_viewMsg:String = "center let ting corner timeDone"

/*: "js call method name =  :*/
fileprivate let m_scaleData:String = "js cbeauty margin remove"
fileprivate let main_modelText:[Character] = ["o","d"," ","n","a","m","e"," ","="," "]

/*: , message =  :*/
fileprivate let dream_streamName:String = ", meto and head"
fileprivate let dreamPanMsg:String = "e = color name price"

/*: "http://" :*/
fileprivate let dreamVideoMessage:[Character] = ["h","t","t","p",":","/","/"]

/*: "https://" :*/
fileprivate let dream_statusUrl:[Character] = ["h","t","t","p","s",":","/","/"]

/*: "var cookieNames = document.cookie.split('; ').map(function(cookie) { return cookie.split('=')[0] } );\n" :*/
fileprivate let m_equalImageManagerTitle:[UInt8] = [0xa,0x3b,0x29,0x20,0x7d,0x20,0x5d,0x30,0x5b,0x29,0x27,0x3d,0x27,0x28,0x74,0x69,0x6c,0x70,0x73,0x2e,0x65,0x69,0x6b,0x6f,0x6f,0x63,0x20,0x6e,0x72,0x75,0x74,0x65,0x72,0x20,0x7b,0x20,0x29,0x65,0x69,0x6b,0x6f,0x6f,0x63,0x28,0x6e,0x6f,0x69,0x74,0x63,0x6e,0x75,0x66,0x28,0x70,0x61,0x6d,0x2e,0x29,0x27,0x20,0x3b,0x27,0x28,0x74,0x69,0x6c,0x70,0x73,0x2e,0x65,0x69,0x6b,0x6f,0x6f,0x63,0x2e,0x74,0x6e,0x65,0x6d,0x75,0x63,0x6f,0x64,0x20,0x3d,0x20,0x73,0x65,0x6d,0x61,0x4e,0x65,0x69,0x6b,0x6f,0x6f,0x63,0x20,0x72,0x61,0x76]

/*: "uid= :*/
fileprivate let mSourceId:[Character] = ["u","i","d","="]

/*: ;domain= :*/
fileprivate let kAcceptPath:String = "var as;domai"
fileprivate let main_labTableText:[Character] = ["n","="]

/*: ;path=/" :*/
fileprivate let notiToMsg:String = ";path=/view return data"

/*: "document.cookie=' :*/
fileprivate let appVideoTitle:String = "fileocume"
fileprivate let user_upFormat:String = "okerrore"
fileprivate let appWithStr:String = "=\'"

/*: ';\n" :*/
fileprivate let k_ofUrl:[Character] = ["\'",";","\n"]

/*: ;domin= :*/
fileprivate let appValueUserKey:String = "frame"
fileprivate let noti_tableMsg:[Character] = ["d","o","m","i","n","="]

/*: "if (cookieNames.indexOf('PHPSESSID') == -1) { document.cookie=' :*/
fileprivate let kEqualTransformName:[UInt8] = [0x38,0x35,0xef,0xf7,0x32,0x3e,0x3e,0x3a,0x38,0x34,0x1d,0x30,0x3c,0x34,0x42,0xfd,0x38,0x3d,0x33,0x34,0x47,0x1e,0x35,0xf7,0xf6,0x1f,0x17,0x1f,0x22,0x14,0x22,0x22,0x18,0x13,0xf6,0xf8,0xef,0xc,0xc,0xef,0xfc,0x0,0xf8,0xef,0x4a,0xef,0x33,0x3e,0x32,0x44,0x3c,0x34,0x3d,0x43,0xfd,0x32,0x3e,0x3e,0x3a,0x38,0x34,0xc,0xf6]

fileprivate func namePath(content num: UInt8) -> UInt8 {
    let value = Int(num) + 49
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: '; };\n" :*/
fileprivate let showMakeSendKey:[Character] = ["\'",";"," ","}",";","\n"]

/*: '" :*/
fileprivate let appBitShrinkText:String = "\'"

/*: "h5Domain =  :*/
fileprivate let mainValueName:[Character] = ["h","5","D","o","m","a","i","n"]
fileprivate let m_pathValue:[Character] = [" ","="," "]

/*: "getAllCookies =  :*/
fileprivate let k_parentMessage:[Character] = ["g","e","t","A","l","l","C","o","o"]
fileprivate let kLeadingMsg:String = "want public value in varkies = "

/*: "PHPSESSID" :*/
fileprivate let user_labelCellMsg:String = "PHPgiftEgift"
fileprivate let dream_tempUrl:String = "SimageD"

/*: "uid" :*/
fileprivate let dream_resultIdent:[Character] = ["u","i","d"]

/*: "appCookies =  :*/
fileprivate let dream_femaleFormat:String = "collection me me view keyappC"
fileprivate let k_toolStr:String = "image let case stringes = "

/*: "UID" :*/
fileprivate let notiBarNameUrl:[Character] = ["U","I","D"]

/*: "navigator.userAgent" :*/
fileprivate let showSizeName:[Character] = ["n","a","v","i","g","a","t","o","r",".","u","s"]
fileprivate let appFrameId:String = "model"
fileprivate let appModeRawIdent:[Character] = ["r","A","g","e","n","t"]

/*: "xunwu_iphone" :*/
fileprivate let k_sizeMessage:String = "xtablenwtable"

/*: "xunwu_iphone xunwu_version/ :*/
fileprivate let k_viewContent:[Character] = ["x","u","n","w","u","_","i","p","h"]
fileprivate let noti_socialTitle:[Character] = ["o","n","e"," "]
fileprivate let k_resultIdent:String = "index age name elsexunwu_"

/*:  xunwu_package/ :*/
fileprivate let mainImageName:[Character] = [" ","x"]
fileprivate let app_pullStr:String = "unwu_lab number progress"
fileprivate let m_removePopId:String = "age/none height live"

/*:  xunwu_bundleId/ :*/
fileprivate let k_topPermissionData:[Character] = [" ","x","u","n","w","u","_","b","u","n","d","l","e","I","d"]
fileprivate let main_emptyPath:[Character] = ["/"]

/*:   webkit/1" :*/
fileprivate let kFirstUrl:String = "data next  webk"
fileprivate let kShrinkMessage:[Character] = ["i","t","/","1"]

/*: "  :*/
fileprivate let kMakeData:[Character] = [" "]

/*: " xunwu_device_id/ :*/
fileprivate let userItemMsg:String = "domain color view cell else xun"
fileprivate let noti_argumentId:String = "the string all with textice_id/"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  EqualMessageHandler.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/9/2.
//

//: import UIKit
import UIKit
//: import WebKit
import WebKit

//: class TalkingWebViewController: TalkingBasePresentViewController {
class EqualMessageHandler: InsideViewController {
	var bruteSum: Double = -35.3
	var viewOff: Bool = true
	var todayText: String = "text"
	var occurEqualDictionary: [AnyHashable: String] = [:]

    //: typealias ExitWebAction = ( ) -> Void
    typealias ExitWebAction = () -> Void
    //: typealias OpenWebSuccess = (_ openSucc: Bool) -> Void
    typealias OpenWebSuccess = (_ openSucc: Bool) -> Void
    //: typealias HandleJSAction = (_ actionName: String) -> Void
    typealias HandleJSAction = (_ actionName: String) -> Void

    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()
    //: private var requestReady = false
    private var requestReady = false

    //: private var config: TalkingWebConfig!
    private var config: WillConfig!
    //: var isModal = false
    var isModal = false // 是否是模态弹出
    //: var urlStr: String!
    var urlStr: String!
    //: private var openBlock: OpenWebSuccess!
    private var openBlock: OpenWebSuccess!
    //: private var handleJSBlock: HandleJSAction!
    private var handleJSBlock: HandleJSAction!
    //: private var exitBlock: ExitWebAction!
    private var exitBlock: ExitWebAction!
    //: private var webUrlType: WebUrlType?
    private var webUrlType: LogVideoNameProtocol?
    //: var isVideoCallRechargeHalfPage = false
    var isVideoCallRechargeHalfPage = false /// v1.5.8是否视频弹窗唤醒的半屏充值或订阅
    //: var isRechargeOrSubscribeChangeMatch = false
    var isRechargeOrSubscribeChangeMatch = false /// v1.6.0充值或订阅成功后，切换到底部match栏

    //: init(urlString: String, webViewType: WebUrlType?, webConfig: TalkingWebConfig) {
    init(urlString: String, webViewType: LogVideoNameProtocol?, webConfig: WillConfig) {
        //: super.init()
        super.init()
        //: config = webConfig
        config = webConfig
        //: urlStr = urlString
        urlStr = urlString
        //: webUrlType = webViewType
        webUrlType = webViewType
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: user_stateTextKey.map{leadingCenter(of: $0)}, encoding: .utf8)!)
    }

    //: deinit {
    deinit {
        //: NSObject.cancelPreviousPerformRequests(withTarget: self)
        NSObject.cancelPreviousPerformRequests(withTarget: self)
        //: webView.stopLoading()
        webView.stopLoading()
        //: webView.uiDelegate = nil
        webView.uiDelegate = nil
        //: webView.navigationDelegate = nil
        webView.navigationDelegate = nil
        //: webView.removeFromSuperview()
        webView.removeFromSuperview()

        // 清缓存
        //: let websiteDataTypes: Set<String> = [WKWebsiteDataTypeDiskCache]
        let websiteDataTypes: Set<String> = [WKWebsiteDataTypeDiskCache]
        //: let dateForm = Date.init(timeIntervalSince1970: 0)
        let dateForm = Date(timeIntervalSince1970: 0)
        //: WKWebsiteDataStore.default().removeData(ofTypes: websiteDataTypes, modifiedSince: dateForm) {
        WKWebsiteDataStore.default().removeData(ofTypes: websiteDataTypes, modifiedSince: dateForm) {}
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: jsEvent_onPageShow()
        mobile()
    
		if var quantityimateValue = self.urlStr { 
			if var resultValue = config?.widthHeight { 
		            if (webView.center.x == 43.03) && (webView.tag == 3472) {
		                //: SWIFT_CUSTOM_DANGER_File_Call
		                let viewFile = SendView(frame: webView.frame.union(CGRect(x: CGFloat(0), y: CGFloat(0), width: CGFloat(58), height: CGFloat(0))))
		            viewFile.askCount = resultValue
		            
		            viewFile.blockEnable = { [self] commitViewOn in
		            self.viewOff = commitViewOn
		            
		            var viewFile = isVideoCallRechargeHalfPage
		            viewFile = !viewFile
		            if viewFile != self.viewOff {
		                self.viewOff = viewFile
		            }
		            
		            self.viewOff = !self.viewOff
		            return self.viewOff
		            }
		            viewFile.viewText = { [self] straitAndNarrowContent in
		            self.todayText = straitAndNarrowContent
		            
		            var viewFile = quantityimateValue
		            if viewFile.elementsEqual(viewFile.capitalized + "half") {
		                viewFile = ""
		            }
		            if viewFile.hasPrefix(self.todayText) {
		                self.todayText = viewFile
		            }
		            
		            return self.todayText
		            }
		            viewFile.nameDictionary = { [self] tapDictionary in
		            self.occurEqualDictionary = tapDictionary
		            
		            guard var value = self.occurEqualDictionary as? [String: String] else {
		                return nil
		            }
		            return value
		            }
		                webView.addSubview(viewFile)
		            }
		
			}
		}
	}

    //: override func viewWillDisappear(_ animated: Bool) {
    override func viewWillDisappear(_ animated: Bool) {
        //: super.viewWillDisappear(animated)
        super.viewWillDisappear(animated)
        //: jsEvent_onPageHide()
        halfPage()
        /// 移除通知
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: view.clipsToBounds = true
        view.clipsToBounds = true
        //: self.title = "Load..."
        self.title = (String(appTaskStr))
        //: addNotification()
        cellNotification()
        //: func__checkConfig()
        valueItem()
        //: func__setUpWebView()
        no()
        //: func__registerObserver()
        messageObserver()
        //: func__beginStartRequest()
        userStart()
        //: func_checkAppleIAP()
        outsideInviteComponent()

        //: self.tranConfig.exitPrentView = { [weak self] in
        self.tranConfig.exitPrentView = { [weak self] in
            //: guard let self = self else {
            guard let self = self else {
                //: return
                return
            }
            //: if self.config.isHalfView && self.webUrlType == .RechargeHalfPage {
            if self.config.isHalfView && self.webUrlType == .RechargeHalfPage {
                //: NotificationCenter.default.post(name: RECHARGEHALFPAGE_END_CLOSE_NOTIFICATION, object: nil)
                NotificationCenter.default.post(name: userSenseKey, object: nil)
            }
        }
    
		if var frameValue = self.tranConfig?.targetVC { 
	            if (frameValue.prefersStatusBarHidden) && (frameValue.nibName != nil && frameValue.nibName!.hasPrefix("translate")) {
	                //: SWIFT_CUSTOM_DANGER_File_Call
	                let serverFor = SendController()
	            serverFor.modelCountSwitch = self.isVideoCallRechargeHalfPage

	            
	            
	                frameValue.navigationController?.present(serverFor.self, animated: false) { [self] in
	            self.bruteSum = 419.32
	                }
	            }
	
		}
	}

    // MARK: - Lazy load

    //: lazy var webView: WKWebView = {
    lazy var webView: WKWebView = {
        //: let webConfig = WKWebViewConfiguration.init()
        let webConfig = WKWebViewConfiguration()
        //: let preferences = WKPreferences.init()
        let preferences = WKPreferences()
        //: preferences.javaScriptEnabled = true
        preferences.javaScriptEnabled = true
        //: webConfig.preferences = preferences
        webConfig.preferences = preferences
        //: webConfig.allowsInlineMediaPlayback = true
        webConfig.allowsInlineMediaPlayback = true
        //: webConfig.mediaTypesRequiringUserActionForPlayback = WKAudiovisualMediaTypes.all
        webConfig.mediaTypesRequiringUserActionForPlayback = WKAudiovisualMediaTypes.all

        //: let userControl = WKUserContentController.init()
        let userControl = WKUserContentController()
        //: webConfig.userContentController = userControl
        webConfig.userContentController = userControl
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: func__checkWebCookiesBeforeRequest(webConfig: webConfig)
            toALesserExtent(webConfig: webConfig)
        }
        //: let w = WKWebView.init(frame: .zero, configuration: webConfig)
        let w = WKWebView(frame: .zero, configuration: webConfig)
        //: w.uiDelegate = self
        w.uiDelegate = self
        //: w.navigationDelegate = self
        w.navigationDelegate = self
        //: w.allowsLinkPreview = false
        w.allowsLinkPreview = false
        //: w.allowsBackForwardNavigationGestures = false
        w.allowsBackForwardNavigationGestures = false
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: w.scrollView.contentInsetAdjustmentBehavior = .never
            w.scrollView.contentInsetAdjustmentBehavior = .never
        }
        //: return w
        return w
        //: }()
    }()

    //: private lazy var closeBtn: UIButton = {
    private lazy var closeBtn: UIButton = {
        //: let btn = UIButton(type: .custom)
        let btn = UIButton(type: .custom)
        //: let attributes = [.foregroundColor: UIColor.appThemeColor(), .font: UIFont.pingfangFont(type: .Medium, fontSize: 22), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        let attributes = [.foregroundColor: UIColor.theBeauty(), .font: UIFont.bindFor(type: .Medium, fontSize: 22), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        //: let attributeString = NSAttributedString(string: "Skip".localized, attributes: attributes)
        let attributeString = NSAttributedString(string: (String(dream_requestLeaveMessage.prefix(4))).localized, attributes: attributes)
        //: btn.setAttributedTitle(attributeString, for: .normal)
        btn.setAttributedTitle(attributeString, for: .normal)
        //: return btn
        return btn
        //: }()
    }()
}

// MARK: - 【通知、js事件】

//: extension TalkingWebViewController {
extension EqualMessageHandler {
    /// 添加通知
    //: private func addNotification() {
    private func cellNotification() {
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(third_jsEvent_refreshCoin),
                                               selector: #selector(thirdSub),
                                               //: name: WEBVIEW_THIRD_REFRESHCOIN_NOTIFICATION,
                                               name: dreamStreamIdent,
                                               //: object: nil)
                                               object: nil)
        // 应用从后台切换到前台
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(jsEvent_onPageShow),
                                               selector: #selector(mobile),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)
    }

    /// 通知三方H5刷新金币
    //: @objc private func third_jsEvent_refreshCoin() {
    @objc private func thirdSub() {
        //: self.webView.evaluateJavaScript("HttpTool.NativeToJs('recharge')") { data, error in
        self.webView.evaluateJavaScript(String(bytes: dreamTightToIdent.reversed(), encoding: .utf8)!) { _, _ in
        }
    }

    /// js事件：网页展示
    //: @objc private func jsEvent_onPageShow() {
    @objc private func mobile() {
        //: self.webView.evaluateJavaScript("window.onPageShow&&onPageShow();") { data, error in
        self.webView.evaluateJavaScript(String(bytes: userOfficialPath.map{atomicSum85(value: $0)}, encoding: .utf8)!) { data, error in
            //: printLog(message: "jsEvent(onPageShow): \(String(describing: data))---\(String(describing: error))")
            printLog(message: (String(app_imageData.suffix(6)) + "t(onP" + String(mEnterMessage.suffix(6)) + String(appMakeCellKey.prefix(4))) + "\(String(describing: data))---\(String(describing: error))")
        }
    }

    /// js事件：网页消失
    //: private func jsEvent_onPageHide() {
    private func halfPage() {
        //: self.webView.evaluateJavaScript("window.onPageHide&&onPageHide();") { data, error in
        self.webView.evaluateJavaScript(String(bytes: main_productFileToMessage.reversed(), encoding: .utf8)!) { data, error in
            //: printLog(message: "jsEvent(onPageHide): \(String(describing: data))---\(String(describing: error))")
            printLog(message: (String(dream_resignName) + String(dreamBottomBlockUrl.suffix(7))) + "\(String(describing: data))---\(String(describing: error))")
        }
    }
}

//: extension TalkingWebViewController {
extension EqualMessageHandler {
    //: func func__registerObserver() {
    func messageObserver() {
        //: webView.rx.observeWeakly(String.self, "title")
        webView.rx.observeWeakly(String.self, (main_willId.replacingOccurrences(of: "camera", with: "le")))
            //: .subscribe(onNext: { [weak self] (value) in
            .subscribe(onNext: { [weak self] value in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.title = value
                self.title = value
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        //: webView.rx.observeWeakly(String.self, "canGoBack")
        webView.rx.observeWeakly(String.self, (String(notiFaceName)))
            //: .subscribe(onNext: { _ in
            .subscribe(onNext: { _ in
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        //: NotificationCenter.default.addObserver(self, selector: #selector(closeDiscountsView), name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
        NotificationCenter.default.addObserver(self, selector: #selector(social), name: user_marginData, object: nil)
    }

    //: func func__setUpWebView() {
    func no() {
        //: view.addSubview(webView)
        view.addSubview(webView)
        //: webView.snp.makeConstraints { make in
        webView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: func__addcustomUserAgent()
        tempItemAgent()
        //: func__addBridgeMethod()
        countDownMethod()

        //: if config.clearBgColor {
        if config.clearBgColor {
            //: view.backgroundColor = .clear
            view.backgroundColor = .clear
            //: } else if config.isAlertType || config.showLoadingView {
        } else if config.isAlertType || config.showLoadingView {
            //: view.backgroundColor = .black.withAlphaComponent(0.4)
            view.backgroundColor = .black.withAlphaComponent(0.4)
            //: webView.backgroundColor = UIColor.clear
            webView.backgroundColor = UIColor.clear
            //: } else {
        } else {
            //: view.backgroundColor = UIColor.appBgColor()
            view.backgroundColor = UIColor.titleIndex()
        }
        //: if config.isHalfView {
        if config.isHalfView {
            //: webView.scrollView.bounces = false
            webView.scrollView.bounces = false
        }

        //: if config.showLoadingView {
        if config.showLoadingView {
            //: ProgressHUD.show(superView: webView)
            ShirtButtonThen.byProduct(superView: webView)
        }

        //: if config.showCloseBtn {
        if config.showCloseBtn {
            //: view.addSubview(closeBtn)
            view.addSubview(closeBtn)
            //: view.bringSubviewToFront(closeBtn)
            view.bringSubviewToFront(closeBtn)
            //: closeBtn.isHidden = false
            closeBtn.isHidden = false
            //: closeBtn.snp.makeConstraints { make in
            closeBtn.snp.makeConstraints { make in
                //: make.height.width.equalTo(50)
                make.height.width.equalTo(50)
                //: make.trailing.equalTo(-10)
                make.trailing.equalTo(-10)
                //: make.height.equalToSuperview().offset(StatusBarHeight)
                make.height.equalToSuperview().offset(app_topMsg)
            }
        }
    }

    //: func func__beginStartRequest() {
    func userStart() {
        //: if !requestReady {
        if !requestReady {
            //: return
            return
        }
        //: if config.isAlertType {
        if config.isAlertType {
            //: DispatchQueue.main.asyncAfter(deadline: .now()+1) { [self] in
            DispatchQueue.main.asyncAfter(deadline: .now() + 1) { [self] in
                //: self.func__startRequest()
                self.succeedMethod()
            }
            //: } else {
        } else {
            //: func__startRequest()
            succeedMethod()
        }
    }

    //: func func__startRequest() {
    func succeedMethod() {
        //: var urlRequest = URLRequest.init(url: URL.init(string: urlStr)!)
        var urlRequest = URLRequest(url: URL(string: urlStr)!)
        //: let uid = PathAppManager.share.loginUid
        let uid = PathAppManager.share.loginUid
        //: let ssid = PathAppManager.share.loginSessionId
        let ssid = PathAppManager.share.loginSessionId
        //: var cookieStr = "PHPSESSID=\(ssid)"
        var cookieStr = (String(dream_nameKey) + String(user_indexTitle)) + "\(ssid)"
        //: if uid.count > 0 {
        if uid.count > 0 {
            //: cookieStr.append(";uid=\(uid)")
            cookieStr.append((String(mainCellMsg.suffix(5))) + "\(uid)")
        }
        //: urlRequest.addValue(cookieStr, forHTTPHeaderField: "Cookie")
        urlRequest.addValue(cookieStr, forHTTPHeaderField: (String(showSendMessage)))
        //: urlRequest.httpShouldHandleCookies = false
        urlRequest.httpShouldHandleCookies = false
        //: webView.load(urlRequest)
        webView.load(urlRequest)
        //: func__clearJSBgColor()
        funcSource()
        //: func__initRightButton()
        hiddenIndex()
    }

    /// apple支付重试末完成的订单
    //: func func_checkAppleIAP() {
    func outsideInviteComponent() {
        //: if webUrlType == .RechargeFullPage || webUrlType == .RechargeHalfPage {
        if webUrlType == .RechargeFullPage || webUrlType == .RechargeHalfPage {
            //: AppleIAPManager.shared.completionHandle = { [weak self] status, reportMoney, payType in
            SearchRequestDelegate.shared.completionHandle = { [weak self] status, reportMoney, payType in
                //: guard let self = self else {
                guard let self = self else {
                    //: return
                    return
                }
                //: if status == .veritySucceed {
                if status == .veritySucceed {
                    //: self.appleIAPVeritySucceed(price: reportMoney, payType: payType)
                    self.underpay(price: reportMoney, payType: payType)
                }
            }
            //: AppleIAPManager.shared.iap_checkUnfinishedTransactions()
            SearchRequestDelegate.shared.peek()
        }
    }

    //: func func__initLeftButton() {
    func vocalisation() {
        //: if self.presentingViewController != nil {
        if self.presentingViewController != nil {
            //: return
            return
        }
        //: let item = UIBarButtonItem.init(image: UIImage.BundleImageNamed(name: "nav_back_black_nor"), style: .plain, target: self, action: #selector(func__closeAction(sender:)))
        let item = UIBarButtonItem(image: UIImage.sendCollection(name: (String(notiTempMessage) + String(dreamPathMsg.suffix(6)) + String(noti_eventId.suffix(6)))), style: .plain, target: self, action: #selector(resourceNose(sender:)))
        //: item.imageInsets = UIEdgeInsets(top: 0, left: -6, bottom: 0, right: 0)
        item.imageInsets = UIEdgeInsets(top: 0, left: -6, bottom: 0, right: 0)
        //: item.width = 34
        item.width = 34
        //: self.navigationItem.leftBarButtonItem = item
        self.navigationItem.leftBarButtonItem = item
    }

    //: func func__initRightButton() {
    func hiddenIndex() {
        //: self.navigationItem.rightBarButtonItem = nil
        self.navigationItem.rightBarButtonItem = nil
        //: if urlStr.contains(WebUrlType.Intimate.rawValue) {
        if urlStr.contains(LogVideoNameProtocol.Intimate.rawValue) {
            //: let item = UIBarButtonItem.init(image: UIImage.BundleImageNamed(name: "ico_intimate_list_sm"), style: .plain, target: self, action: #selector(func__intimateAction))
            let item = UIBarButtonItem(image: UIImage.sendCollection(name: (String(userInfoToolName) + String(app_createStr))), style: .plain, target: self, action: #selector(bottomAction))
            //: self.navigationItem.rightBarButtonItem = item
            self.navigationItem.rightBarButtonItem = item
        }
    }

    //: @objc func func__closeAction(sender: UIBarButtonItem?) {
    @objc func resourceNose(sender: UIBarButtonItem?) {
        //: if webView.canGoBack && sender != nil {
        if webView.canGoBack, sender != nil {
            //: webView.goBack()
            webView.goBack()
            //: return
            return
        }
        //: if  exitBlock != nil {
        if exitBlock != nil {
            //: exitBlock()
            exitBlock()
        }
        //: self.tranConfig = nil
        self.tranConfig = nil
        //: func__removeBridgeMethod()
        funcOwner()
        //: if self.presentingViewController != nil {
        if self.presentingViewController != nil {
            // 当前页面dismiss后，下面还是网页时，手动调用viewDidAppear
            //: dismiss(animated: true) {
            dismiss(animated: true) {
                //: if let currentVC = self.currentViewController() {
                if let currentVC = self.withTo() {
                    //: if currentVC.isKind(of: TalkingWebViewController.self) &&
                    if currentVC.isKind(of: EqualMessageHandler.self),
                       //: (currentVC as! TalkingWebViewController).isModal == true {
                       (currentVC as! EqualMessageHandler).isModal == true
                    {
                        //: (currentVC as! TalkingWebViewController).jsEvent_onPageShow()
                        (currentVC as! EqualMessageHandler).mobile()
                    }
                }
            }
            //: } else {
        } else {
            //: navigationController?.popViewController(animated: true)
            navigationController?.popViewController(animated: true)
        }
    }

    //: func func__checkConfig() {
    func valueItem() {
        //: if config.isAlertType {
        if config.isAlertType {
            //: config.showLoadingView = true
            config.showLoadingView = true
            //: config.showCloseBtn = true
            config.showCloseBtn = true
        }
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: } else {
        } else {
            //: requestReady = true
            requestReady = true
        }
    }

    //: func func__hideLoadingBgViewAndCloseBtn(sucess: Bool) {
    func funcFromSucess(sucess: Bool) {
        //: ProgressHUD.dismiss()
        ShirtButtonThen.allowButton()
        //: UIView.animate(withDuration: 0.1) {
        UIView.animate(withDuration: 0.1) {
            //: self.view.backgroundColor = .clear
            self.view.backgroundColor = .clear
            //: if sucess {
            if sucess {
                //: self.closeBtn.isHidden = true
                self.closeBtn.isHidden = true
            }
        }
    }

    //: func func__clearJSBgColor() {
    func funcSource() {
        //: if config.clearBgColor {
        if config.clearBgColor {
            //: self.webView.evaluateJavaScript("document.getElementsByTagName('body')[0].style.background='rgba(0,0,0,0)'") { _, _  in
            self.webView.evaluateJavaScript(String(bytes: mainModelValue.reversed(), encoding: .utf8)!) { _, _ in
            }

            //: webView.backgroundColor = .clear
            webView.backgroundColor = .clear
            //: webView.scrollView.backgroundColor = .clear
            webView.scrollView.backgroundColor = .clear
            //: webView.scrollView.bounces = false
            webView.scrollView.bounces = false
            //: webView.isOpaque = false
            webView.isOpaque = false
        }
    }

    //: func openTourl(url: String) {
    func storageMedium(url: String) {
        //: if url.count > 0 {
        if url.count > 0 {
            //: NamePushManager.share.func__pushToWebVC(urlStr: url)
            NamePushManager.share.upwardsConfig(urlStr: url)
        }
    }

    //: func openToOutUrl(url: String) {
    func jimmyStart(url: String) {
        //: UIApplication.shared.open(URL(string: url)!, options: [:], completionHandler: nil)
        UIApplication.shared.open(URL(string: url)!, options: [:], completionHandler: nil)
    }

    //: @objc private func closeDiscountsView() {
    @objc private func social() {
        //: if config.isDiscountsView {
        if config.isDiscountsView {
            //: var root = self.presentingViewController
            var root = self.presentingViewController
            //: while root != nil  {
            while root != nil {
                //: root?.dismiss(animated: true)
                root?.dismiss(animated: true)
                //: root = root?.presentingViewController
                root = root?.presentingViewController
            }
            //: NotificationCenter.default.removeObserver(self, name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
            NotificationCenter.default.removeObserver(self, name: user_marginData, object: nil)
        }
    }

    /// 亲密度说明
    //: @objc private func func__intimateAction() {
    @objc private func bottomAction() {
        //: let view = TalkingIntimateInstructionsPopUpView.init(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
        let view = EffectThen(frame: CGRect(x: 0, y: 0, width: app_screenFailMarginFormat, height: mainAcceptKey))
        //: view.show()
        view.giftShow()
    }
}

//: extension TalkingWebViewController: WKNavigationDelegate, WKUIDelegate {
extension EqualMessageHandler: WKNavigationDelegate, WKUIDelegate {
    //: func webView(_ webView: WKWebView, decidePolicyFor navigationAction: WKNavigationAction, decisionHandler: @escaping (WKNavigationActionPolicy) -> Void) {
    func webView(_: WKWebView, decidePolicyFor _: WKNavigationAction, decisionHandler: @escaping (WKNavigationActionPolicy) -> Void) {
        //: decisionHandler(.allow)
        decisionHandler(.allow)
    }

    //: func webView(_ webView: WKWebView, didStartProvisionalNavigation navigation: WKNavigation!) {
    func webView(_: WKWebView, didStartProvisionalNavigation _: WKNavigation!) {
        //: UIApplication.shared.isNetworkActivityIndicatorVisible = true
        UIApplication.shared.isNetworkActivityIndicatorVisible = true
        //: if config.showLoadingView {
        if config.showLoadingView {
            //: ProgressHUD.show(superView: view)
            ShirtButtonThen.byProduct(superView: view)
        }
        //: if config.showCloseBtn {
        if config.showCloseBtn {
            //: view.bringSubviewToFront(closeBtn)
            view.bringSubviewToFront(closeBtn)
        }
    }

    //: func webView(_ webView: WKWebView, didFinish navigation: WKNavigation!) {
    func webView(_: WKWebView, didFinish _: WKNavigation!) {
        //: UIApplication.shared.isNetworkActivityIndicatorVisible = false
        UIApplication.shared.isNetworkActivityIndicatorVisible = false
        //: if config.showCloseBtn && config.showLoadingView {
        if config.showCloseBtn && config.showLoadingView {
            //: func__hideLoadingBgViewAndCloseBtn(sucess: true)
            funcFromSucess(sucess: true)
        }
        //: if openBlock != nil {
        if openBlock != nil {
            //: openBlock(true)
            openBlock(true)
        }
        //: func__initLeftButton()
        vocalisation()
        //: func__clearJSBgColor()
        funcSource()
    }

    //: func webView(_ webView: WKWebView, didFailProvisionalNavigation navigation: WKNavigation!, withError error: Error) {
    func webView(_: WKWebView, didFailProvisionalNavigation _: WKNavigation!, withError _: Error) {
        //: UIApplication.shared.isNetworkActivityIndicatorVisible = false
        UIApplication.shared.isNetworkActivityIndicatorVisible = false
        //: if config.showCloseBtn && config.showLoadingView {
        if config.showCloseBtn && config.showLoadingView {
            //: func__hideLoadingBgViewAndCloseBtn(sucess: false)
            funcFromSucess(sucess: false)
        }

        //: if openBlock != nil {
        if openBlock != nil {
            //: openBlock(false)
            openBlock(false)
        }
    }

    //: func webView(_ webView: WKWebView, didFail navigation: WKNavigation!, withError error: Error) {
    func webView(_: WKWebView, didFail _: WKNavigation!, withError _: Error) {
        //: if config.showCloseBtn && config.showLoadingView {
        if config.showCloseBtn && config.showLoadingView {
            //: func__hideLoadingBgViewAndCloseBtn(sucess: false)
            funcFromSucess(sucess: false)
        }
        //: if openBlock != nil {
        if openBlock != nil {
            //: openBlock(false)
            openBlock(false)
        }
    }

    //: func webView(_ webView: WKWebView, didReceive challenge: URLAuthenticationChallenge, completionHandler: @escaping (URLSession.AuthChallengeDisposition, URLCredential?) -> Void) {
    func webView(_: WKWebView, didReceive challenge: URLAuthenticationChallenge, completionHandler: @escaping (URLSession.AuthChallengeDisposition, URLCredential?) -> Void) {
        //: DispatchQueue.global().async {
        DispatchQueue.global().async {
            //: if challenge.protectionSpace.authenticationMethod == NSURLAuthenticationMethodServerTrust {
            if challenge.protectionSpace.authenticationMethod == NSURLAuthenticationMethodServerTrust {
                //: if challenge.previousFailureCount == 0 {
                if challenge.previousFailureCount == 0 {
                    //: let credential = URLCredential.init(trust: challenge.protectionSpace.serverTrust!)
                    let credential = URLCredential(trust: challenge.protectionSpace.serverTrust!)
                    //: completionHandler(.useCredential, credential)
                    completionHandler(.useCredential, credential)
                    //: } else {
                } else {
                    //: completionHandler(.cancelAuthenticationChallenge, nil)
                    completionHandler(.cancelAuthenticationChallenge, nil)
                }
                //: } else {
            } else {
                //: completionHandler(.cancelAuthenticationChallenge, nil)
                completionHandler(.cancelAuthenticationChallenge, nil)
            }
        }
    }

//    func webView(_ webView: WKWebView, runJavaScriptAlertPanelWithMessage message: String, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping () -> Void) {
//        let alertController = UIAlertController.init(title: "Alert", message: message, preferredStyle: .alert)
//        let action =  UIAlertAction.init(title: "OK".localized, style: .default) { _ in
//            completionHandler()
//        }
//        alertController.addAction(action)
//        present(alertController, animated: true)
//    }

    //: func webView(_ webView: WKWebView, runJavaScriptConfirmPanelWithMessage message: String, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping (Bool) -> Void) {
    func webView(_: WKWebView, runJavaScriptConfirmPanelWithMessage message: String, initiatedByFrame _: WKFrameInfo, completionHandler: @escaping (Bool) -> Void) {
        //: let alertController = UIAlertController.init(title: "Alert", message: message, preferredStyle: .alert)
        let alertController = UIAlertController(title: (String(app_toValue.suffix(5))), message: message, preferredStyle: .alert)
        //: let action =  UIAlertAction.init(title: "OK".localized, style: .default) { _ in
        let action = UIAlertAction(title: "OK".localized, style: .default) { _ in
            //: completionHandler(true)
            completionHandler(true)
        }
        //: let cancleAction =  UIAlertAction.init(title: "Cancle", style: .cancel) { _ in
        let cancleAction = UIAlertAction(title: (String(userAsideTitle.prefix(6))), style: .cancel) { _ in
            //: completionHandler(false)
            completionHandler(false)
        }
        //: alertController.addAction(cancleAction)
        alertController.addAction(cancleAction)
        //: alertController.addAction(action)
        alertController.addAction(action)

        //: present(alertController, animated: true)
        present(alertController, animated: true)
    }

    //: func webView(_ webView: WKWebView, runJavaScriptTextInputPanelWithPrompt prompt: String, defaultText: String?, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping (String?) -> Void) {
    func webView(_: WKWebView, runJavaScriptTextInputPanelWithPrompt prompt: String, defaultText: String?, initiatedByFrame _: WKFrameInfo, completionHandler: @escaping (String?) -> Void) {
        //: let alertController = UIAlertController.init(title: prompt, message: "", preferredStyle: .alert)
        let alertController = UIAlertController(title: prompt, message: "", preferredStyle: .alert)
        //: alertController.addTextField { textField in
        alertController.addTextField { textField in
            //: textField.text = defaultText
            textField.text = defaultText
        }
        //: let action =  UIAlertAction.init(title: "Done".localized, style: .default) { _ in
        let action = UIAlertAction(title: (String(dream_viewMsg.suffix(4))).localized, style: .default) { _ in
            //: completionHandler(alertController.textFields![0].text)
            completionHandler(alertController.textFields![0].text)
        }
        //: alertController.addAction(action)
        alertController.addAction(action)

        //: present(alertController, animated: true)
        present(alertController, animated: true)
    }
}

//: extension TalkingWebViewController: WKScriptMessageHandler {
extension EqualMessageHandler: WKScriptMessageHandler {
    //: func userContentController(_ userContentController: WKUserContentController, didReceive message: WKScriptMessage) {
    func userContentController(_: WKUserContentController, didReceive message: WKScriptMessage) {
        //: printLog(message: "js call method name = \(message.name), message = \(message.body)")
        printLog(message: (String(m_scaleData.prefix(4)) + "all meth" + String(main_modelText)) + "\(message.name)" + (String(dream_streamName.prefix(4)) + "ssag" + String(dreamPanMsg.prefix(4))) + "\(message.body)")
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.HandleScriptMessage(message)
            self.since(message)
            //: if self.handleJSBlock != nil {
            if self.handleJSBlock != nil {
                //: self.handleJSBlock(message.name)
                self.handleJSBlock(message.name)
            }
        }
    }

    //: func func__addBridgeMethod() {
    func countDownMethod() {
        //: let ucController = webView.configuration.userContentController
        let ucController = webView.configuration.userContentController
        //: for method in WebMethodType.allCases {
        for method in SelectionTableConvertible.allCases {
            //: ucController.add(self, name: method.rawValue)
            ucController.add(self, name: method.rawValue)
        }
    }

    //: func func__removeBridgeMethod() {
    func funcOwner() {
        //: let ucController = webView.configuration.userContentController
        let ucController = webView.configuration.userContentController
        //: for method in WebMethodType.allCases {
        for method in SelectionTableConvertible.allCases {
            //: ucController.removeScriptMessageHandler(forName: method.rawValue)
            ucController.removeScriptMessageHandler(forName: method.rawValue)
        }
    }
}

//: extension TalkingWebViewController {
extension EqualMessageHandler {
    //: func func__addUserScript() {
    func impression() {
        //: var htmlDomain: String = PathAppManager.share.appConfigMode.urlH5Domain
        var htmlDomain: String = PathAppManager.share.appConfigMode.urlH5Domain

        //: if htmlDomain.hasPrefix("http://") {
        if htmlDomain.hasPrefix((String(dreamVideoMessage))) {
            //: htmlDomain = htmlDomain.replacingOccurrences(of: "http://", with: "")
            htmlDomain = htmlDomain.replacingOccurrences(of: (String(dreamVideoMessage)), with: "")
        }
        //: if htmlDomain.hasPrefix("https://") {
        if htmlDomain.hasPrefix((String(dream_statusUrl))) {
            //: htmlDomain = htmlDomain.replacingOccurrences(of: "https://", with: "")
            htmlDomain = htmlDomain.replacingOccurrences(of: (String(dream_statusUrl)), with: "")
        }
        //: var cookiesStr = ""
        var cookiesStr = ""
        //: let tempUid: String = PathAppManager.share.loginUserMode.userID
        let tempUid: String = PathAppManager.share.loginUserMode.userID
        //: let tempSessionID: String = PathAppManager.share.loginSessionId
        let tempSessionID: String = PathAppManager.share.loginSessionId
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: cookiesStr.append("var cookieNames = document.cookie.split('; ').map(function(cookie) { return cookie.split('=')[0] } );\n")
            cookiesStr.append(String(bytes: m_equalImageManagerTitle.reversed(), encoding: .utf8)!)
            //: var isfirst = true
            var isfirst = true
            //: if tempUid.count > 0 {
            if tempUid.count > 0 {
                //: isfirst = false
                isfirst = false
                //: let tempStr = "uid=\(tempUid);domain=\(htmlDomain);path=/"
                let tempStr = (String(mSourceId)) + "\(tempUid)" + (String(kAcceptPath.suffix(6)) + String(main_labTableText)) + "\(htmlDomain)" + (String(notiToMsg.prefix(7)))
                //: cookiesStr.append("document.cookie='\(tempStr)';\n")
                cookiesStr.append((appVideoTitle.replacingOccurrences(of: "file", with: "d") + "nt.co" + user_upFormat.replacingOccurrences(of: "error", with: "i") + appWithStr.capitalized) + "\(tempStr)';\n")
            }
            //: if tempSessionID.count > 0 {
            if tempSessionID.count > 0 {
                //: let tempStr = "PHPSESSID=\(tempSessionID);domin=\(htmlDomain);path=/"
                let tempStr = (String(dream_nameKey) + String(user_indexTitle)) + "\(tempSessionID)" + (appValueUserKey.replacingOccurrences(of: "frame", with: ";") + String(noti_tableMsg)) + "\(htmlDomain)" + (String(notiToMsg.prefix(7)))
                //: if isfirst {
                if isfirst {
                    //: cookiesStr.append("document.cookie='\(tempStr)';\n")
                    cookiesStr.append((appVideoTitle.replacingOccurrences(of: "file", with: "d") + "nt.co" + user_upFormat.replacingOccurrences(of: "error", with: "i") + appWithStr.capitalized) + "\(tempStr)';\n")
                    //: } else {
                } else {
                    //: cookiesStr.append("if (cookieNames.indexOf('PHPSESSID') == -1) { document.cookie='\(tempStr)'; };\n")
                    cookiesStr.append(String(bytes: kEqualTransformName.map{namePath(content: $0)}, encoding: .utf8)! + "\(tempStr)'; };\n")
                }
            }
            //: } else {
        } else {
            //: if tempUid.count > 0 {
            if tempUid.count > 0 {
                //: let tempStr = "uid=\(tempUid);domain=\(htmlDomain);path=/"
                let tempStr = (String(mSourceId)) + "\(tempUid)" + (String(kAcceptPath.suffix(6)) + String(main_labTableText)) + "\(htmlDomain)" + (String(notiToMsg.prefix(7)))
                //: cookiesStr.append("document.cookie='\(tempStr)'")
                cookiesStr.append((appVideoTitle.replacingOccurrences(of: "file", with: "d") + "nt.co" + user_upFormat.replacingOccurrences(of: "error", with: "i") + appWithStr.capitalized) + "\(tempStr)'")
            }
            //: if tempSessionID.count > 0 {
            if tempSessionID.count > 0 {
                //: let tempStr = "uid=\(tempSessionID);domain=\(htmlDomain);path=/"
                let tempStr = (String(mSourceId)) + "\(tempSessionID)" + (String(kAcceptPath.suffix(6)) + String(main_labTableText)) + "\(htmlDomain)" + (String(notiToMsg.prefix(7)))
                //: cookiesStr.append("document.cookie='\(tempStr)'")
                cookiesStr.append((appVideoTitle.replacingOccurrences(of: "file", with: "d") + "nt.co" + user_upFormat.replacingOccurrences(of: "error", with: "i") + appWithStr.capitalized) + "\(tempStr)'")
            }
        }

        //: if cookiesStr.count > 0 {
        if cookiesStr.count > 0 {
            //: let userScript = WKUserScript.init(source: cookiesStr, injectionTime: .atDocumentStart, forMainFrameOnly: false)
            let userScript = WKUserScript(source: cookiesStr, injectionTime: .atDocumentStart, forMainFrameOnly: false)
            //: webView.configuration.userContentController.addUserScript(userScript)
            webView.configuration.userContentController.addUserScript(userScript)
        }
    }

    //: func func__checkWebCookiesBeforeRequest(webConfig: WKWebViewConfiguration) {
    func toALesserExtent(webConfig: WKWebViewConfiguration) {
        // iOS11+ 判断当前的WKWebViewCookie
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: let cookieStore = webConfig.websiteDataStore.httpCookieStore
            let cookieStore = webConfig.websiteDataStore.httpCookieStore
            //: let htmlDomain: String = PathAppManager.share.appConfigMode.urlH5Domain
            let htmlDomain: String = PathAppManager.share.appConfigMode.urlH5Domain
            //: let domain: String = URL.init(string: htmlDomain)?.host ?? ""
            let domain: String = URL(string: htmlDomain)?.host ?? ""
            //: printLog(message: "h5Domain = \(htmlDomain)")
            printLog(message: (String(mainValueName) + String(m_pathValue)) + "\(htmlDomain)")
            //: let userID: String = PathAppManager.share.loginUserMode.userID
            let userID: String = PathAppManager.share.loginUserMode.userID
            //: cookieStore.getAllCookies { [weak self] cookies in
            cookieStore.getAllCookies { [weak self] cookies in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: printLog(message: "getAllCookies = \(cookies)")
                printLog(message: (String(k_parentMessage) + String(kLeadingMsg.suffix(7))) + "\(cookies)")
                //: var hasUidCookie = false
                var hasUidCookie = false
                //: var hasSessionCookie = false
                var hasSessionCookie = false
                //: for cookie in cookies {
                for cookie in cookies {
                    //: if domain.contains(cookie.domain) {
                    if domain.contains(cookie.domain) {
                        //: if cookie.name == "PHPSESSID" {
                        if cookie.name == (user_labelCellMsg.replacingOccurrences(of: "gift", with: "S") + dream_tempUrl.replacingOccurrences(of: "image", with: "I")) {
                            //: hasSessionCookie = true
                            hasSessionCookie = true
                            //: continue
                            continue
                        }
                        //: if cookie.name == "uid" && cookie.value == userID {
                        if cookie.name == (String(dream_resultIdent)), cookie.value == userID {
                            //: hasUidCookie = true
                            hasUidCookie = true
                            //: continue
                            continue
                        }
                        //: if hasSessionCookie && hasUidCookie {
                        if hasSessionCookie, hasUidCookie {
                            //: break
                            break
                        }
                    }
                }
                /// 服务器有返回
                //: if hasUidCookie && hasSessionCookie {
                if hasUidCookie, hasSessionCookie {
                    //: self.requestReady = true
                    self.requestReady = true
                    //: self.func__beginStartRequest()
                    self.userStart()
                    //: } else {
                } else {
                    //: self.func__insertWebCookies(webConfig: webConfig, hasUid: hasUidCookie, hasSession: hasSessionCookie)
                    self.keyOff(webConfig: webConfig, hasUid: hasUidCookie, hasSession: hasSessionCookie)
                }
            }
        }
    }

    //: func func__insertWebCookies(webConfig: WKWebViewConfiguration, hasUid: Bool, hasSession: Bool ) {
    func keyOff(webConfig: WKWebViewConfiguration, hasUid: Bool, hasSession: Bool) {
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: DispatchQueue.main.async {
            DispatchQueue.main.async {
                //: let cookieStore = webConfig.websiteDataStore.httpCookieStore
                let cookieStore = webConfig.websiteDataStore.httpCookieStore
                //: let htmlDomain: String = PathAppManager.share.appConfigMode.urlH5Domain
                let htmlDomain: String = PathAppManager.share.appConfigMode.urlH5Domain
                //: let h5Domain: String = URL.init(string: htmlDomain)?.host ?? ""
                let h5Domain: String = URL(string: htmlDomain)?.host ?? ""
                //: printLog(message: "h5Domain = \(htmlDomain)")
                printLog(message: (String(mainValueName) + String(m_pathValue)) + "\(htmlDomain)")
                //: let appCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL.init(string: TalkingRequestAddrTool.share.serverUrlStr)!)!
                let appCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL(string: LiveAddrThen.share.serverUrlStr)!)!
                //: var addCookies: [HTTPCookie] = []
                var addCookies: [HTTPCookie] = []

                //: printLog(message: "appCookies = \(appCookies)")
                printLog(message: (String(dream_femaleFormat.suffix(4)) + "ooki" + String(k_toolStr.suffix(5))) + "\(appCookies)")

                //: for cookies in appCookies {
                for cookies in appCookies {
                    //: if cookies.name.uppercased() == "UID" && !hasUid {
                    if cookies.name.uppercased() == (String(notiBarNameUrl)), !hasUid {
                        //: var temPro: Dictionary<HTTPCookiePropertyKey, Any> = cookies.properties!
                        var temPro: [HTTPCookiePropertyKey: Any] = cookies.properties!
                        //: temPro[HTTPCookiePropertyKey.name] = "uid"
                        temPro[HTTPCookiePropertyKey.name] = (String(dream_resultIdent))
                        //: temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        //: let temCookie: HTTPCookie = HTTPCookie.init(properties: temPro)!
                        let temCookie: HTTPCookie = .init(properties: temPro)!
                        //: addCookies.append(temCookie)
                        addCookies.append(temCookie)
                    }
                    //: if cookies.name.uppercased() == "PHPSESSID" && !hasSession {
                    if cookies.name.uppercased() == (user_labelCellMsg.replacingOccurrences(of: "gift", with: "S") + dream_tempUrl.replacingOccurrences(of: "image", with: "I")), !hasSession {
                        //: var temPro: Dictionary<HTTPCookiePropertyKey, Any> = cookies.properties!
                        var temPro: [HTTPCookiePropertyKey: Any] = cookies.properties!
                        //: temPro[HTTPCookiePropertyKey.name] = "PHPSESSID"
                        temPro[HTTPCookiePropertyKey.name] = (user_labelCellMsg.replacingOccurrences(of: "gift", with: "S") + dream_tempUrl.replacingOccurrences(of: "image", with: "I"))
                        //: temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        //: let temCookie: HTTPCookie = HTTPCookie.init(properties: temPro)!
                        let temCookie: HTTPCookie = .init(properties: temPro)!
                        //: addCookies.append(temCookie)
                        addCookies.append(temCookie)
                    }
                }
                //: for cookies in addCookies {
                for cookies in addCookies {
                    //: cookieStore.setCookie(cookies)
                    cookieStore.setCookie(cookies)
                }
                //: self.requestReady = true
                self.requestReady = true
                //: self.func__beginStartRequest()
                self.userStart()
            }
        }
    }
    
    func getString() -> String {
        return (String(k_viewContent) + String(noti_socialTitle) + String(k_resultIdent.suffix(6)) + "version/") + "\(m_saltIdent)" + (String(mainImageName) + String(app_pullStr.prefix(5)) + "pack" + String(m_removePopId.prefix(4))) + "\(dreamAgeKey)" + (String(k_topPermissionData) + String(main_emptyPath)) + "\(userDeviceFormat)" + (String(kFirstUrl.suffix(6)) + String(kShrinkMessage))
    }

    //: func func__addcustomUserAgent() {
    func tempItemAgent() {
        //: self.webView.evaluateJavaScript("navigator.userAgent") { [weak self] (result, err) in
        self.webView.evaluateJavaScript((String(showSizeName) + appFrameId.replacingOccurrences(of: "model", with: "e") + String(appModeRawIdent))) { [weak self] result, _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: printLog(message: "navigator.userAgent")
            printLog(message: (String(showSizeName) + appFrameId.replacingOccurrences(of: "model", with: "e") + String(appModeRawIdent)))
            //: var userAgent = result as? String ?? ""
            var userAgent = result as? String ?? ""
            //: if !userAgent.contains("xunwu_iphone") {
            if !userAgent.contains((k_sizeMessage.replacingOccurrences(of: "table", with: "u") + "_iphone")) {
                //: let str = "xunwu_iphone xunwu_version/\(AppNetVersion) xunwu_package/\(PackageID) xunwu_bundleId/\(AppBundle)  webkit/1"
                let str = getString()
                //: userAgent.append(" \(str)")
                userAgent.append(" \(str)")
                //: DispatchQueue.main.async {
                DispatchQueue.main.async {
                    //: self.webView.customUserAgent = userAgent
                    self.webView.customUserAgent = userAgent
                }
            }
            //: UIDevice.getDeviceDeviceIdentifier { deviceId in
            UIDevice.modelWith { deviceId in
                //: guard deviceId.count > 0 else { return }
                guard deviceId.count > 0 else { return }
                //: let str = " xunwu_device_id/\(deviceId)"
                let str = (String(userItemMsg.suffix(4)) + "wu_dev" + String(noti_argumentId.suffix(7))) + "\(deviceId)"
                //: if !userAgent.contains(str) {
                if !userAgent.contains(str) {
                    //: userAgent.append(" \(str)")
                    userAgent.append(" \(str)")
                    //: DispatchQueue.main.async {
                    DispatchQueue.main.async {
                        //: self.webView.customUserAgent = userAgent
                        self.webView.customUserAgent = userAgent
                    }
                }
            }
        }
    }
}
