
//: Declare String Begin

/*: "WEBVIEW_THIRD_REFRESHCOIN_NOTIFICATION" :*/
fileprivate let mInfoPath:[UInt8] = [0x85,0x73,0x70,0x84,0x77,0x73,0x85,0x8d,0x82,0x76,0x77,0x80,0x72,0x8d,0x80,0x73,0x74,0x80,0x73,0x81,0x76,0x71,0x7d,0x77,0x7c,0x8d,0x7c,0x7d,0x82,0x77,0x74,0x77,0x71,0x6f,0x82,0x77,0x7d,0x7c]

fileprivate func viewOf(save num: UInt8) -> UInt8 {
    let value = Int(num) - 46
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "goodsId" :*/
fileprivate let mainErrorKey:String = "join heightgoodsId"

/*: "source" :*/
fileprivate let mUpKey:[Character] = ["s","o","u","r","c","e"]

/*: "type" :*/
fileprivate let m_lastIdent:[Character] = ["t","y","p","e"]

/*: "title" :*/
fileprivate let appFrameGiftMsg:[Character] = ["t","i","t","l","e"]

/*: "url" :*/
fileprivate let k_instanceData:String = "urpath"

/*: "money" :*/
fileprivate let dream_timeCenterValue:String = "mpostey"

/*: "Other" :*/
fileprivate let noti_managerEqualName:[Character] = ["O","t","h","e","r"]

/*: "scene" :*/
fileprivate let k_intimateRowUserTitle:[Character] = ["s","c","e","n","e"]

/*: "show" :*/
fileprivate let kRequestText:String = "sgreetow"

/*: "target" :*/
fileprivate let dreamNameLabelUrl:String = "video"
fileprivate let k_screenColorMessage:String = "sharedget"

/*: "eventName" :*/
fileprivate let dreamStackText:String = "view make make make selfeventName"

/*: "jsonString" :*/
fileprivate let main_pushViewYearValue:[Character] = ["j","s","o","n"]
fileprivate let k_onCenterPathName:[Character] = ["S","t","r","i","n","g"]

/*: "coin" :*/
fileprivate let k_rowEqualId:String = "coview"

/*: "uid" :*/
fileprivate let mainBindIdent:String = "UID"

/*: "未实现的js事件： :*/
fileprivate let mainMakeCalledPath:String = "未\u{5b9e}现的js事件\u{ff1a}"

/*: "PurchaseClick" :*/
fileprivate let show_contentIdent:[Character] = ["P","u","r","c","h","a","s","e","C","l","i","c"]
fileprivate let k_sendPath:String = "refuse"

/*: "Retry After or Go to " :*/
fileprivate let noti_cellKey:String = "let content classRetry A"
fileprivate let app_addIdent:String = "by viewfter "

/*: "Feedback" :*/
fileprivate let show_playerKey:String = "s"
fileprivate let k_soulPath:[Character] = ["e","e","d","b","a","c","k"]

/*: " to contact us" :*/
fileprivate let showIconIndexIdent:[Character] = [" ","t","o"," ","c","o","n","t"]
fileprivate let userTotalIdent:[Character] = ["a","c","t"," ","u","s"]

/*: "Apple" :*/
fileprivate let noti_interactionUpIdent:String = "Appleview title destroy extension normal"

/*: " apple支付充值失败： :*/
fileprivate let showReceiveId:String = "rank return all ap"
fileprivate let mFrameValue:String = "付充值失败："

/*: "payResultCallback();" :*/
fileprivate let showCornerName:[Character] = ["p","a","y","R","e"]
fileprivate let appBindValue:[Character] = ["s","u"]
fileprivate let notiHiddenIdent:[Character] = ["l","t","C","a","l","l","b","a","c","k","(",")",";"]

/*: "USD" :*/
fileprivate let main_colorFormat:[Character] = ["U","S","D"]

/*: "amount" :*/
fileprivate let mPlayerData:[UInt8] = [0xbe,0xb2,0xb0,0xaa,0xb1,0xab]

private func labReply(tap num: UInt8) -> UInt8 {
    return num ^ 223
}

/*: "getSystemNotificationtStatus(true)" :*/
fileprivate let noti_shadowTitle:[UInt8] = [0x9d,0x9b,0xaa,0x89,0xaf,0xa9,0xaa,0x9b,0xa3,0x84,0xa5,0xaa,0x9f,0x9c,0x9f,0x99,0x97,0xaa,0x9f,0xa5,0xa4,0xaa,0x89,0xaa,0x97,0xaa,0xab,0xa9,0x5e,0xaa,0xa8,0xab,0x9b,0x5f]

fileprivate func contentualMatter(row num: UInt8) -> UInt8 {
    let value = Int(num) + 202
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "getSystemNotificationtStatus(false)" :*/
fileprivate let userEmptyName:[UInt8] = [0x1f,0x1d,0x2c,0xb,0x31,0x2b,0x2c,0x1d,0x25,0x6,0x27,0x2c,0x21,0x1e,0x21,0x1b,0x19,0x2c,0x21,0x27,0x26,0x2c,0xb,0x2c,0x19,0x2c,0x2d,0x2b,0xe0,0x1e,0x19,0x24,0x2b,0x1d,0xe1]

fileprivate func screenFrame(talk num: UInt8) -> UInt8 {
    let value = Int(num) + 72
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "mfBean" :*/
fileprivate let main_promptIdent:[UInt8] = [0xac,0xa7,0x83,0xa4,0xa0,0xaf]

private func nutsAndBolts(body num: UInt8) -> UInt8 {
    return num ^ 193
}

/*: "%.2f" :*/
fileprivate let m_appearName:[Character] = ["%",".","2","f"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  EqualMessageHandler+JSEvent.swift
//  AbroadTalking
//
//  Created by young on 2022/9/6.
//

//: import UIKit
import UIKit
//: import WebKit
import WebKit

// 通知三方H5刷新金币
//: public let WEBVIEW_THIRD_REFRESHCOIN_NOTIFICATION = NSNotification.Name(rawValue: "WEBVIEW_THIRD_REFRESHCOIN_NOTIFICATION")
public let dreamStreamIdent = NSNotification.Name(rawValue: String(bytes: mInfoPath.map{viewOf(save: $0)}, encoding: .utf8)!)

//: extension TalkingWebViewController {
extension EqualMessageHandler {
    /// 处理JS事件
    /// - Parameters:
    ///   - message: 消息
    //: func HandleScriptMessage(_ message: WKScriptMessage) {
    func since(_ message: WKScriptMessage) {
        //: let type = WebMethodType(rawValue: message.name)
        let type = SelectionTableConvertible(rawValue: message.name)
        //: let json = JSON(message.body)
        let json = JSON(message.body)
        //: switch type {
        switch type {
        //: case .MethodType_Exit:
        case .MethodType_Exit:
            //: self.func__closeAction(sender: nil)
            self.resourceNose(sender: nil)

        //: case .MethodType_needLogin:
        case .MethodType_needLogin:
            //: needLogin()
            screenUpLogin()

        //: case .MethodType_apPay:
        case .MethodType_apPay:
            //: applePay(productId: json["goodsId"].stringValue, source: json["source"].intValue, payType: .Pay)
            enterArray(productId: json[(String(mainErrorKey.suffix(7)))].stringValue, source: json[(String(mUpKey))].intValue, payType: .Pay)

        //: case .MethodType_apPay_subscribe:
        case .MethodType_apPay_subscribe:
            //: if json["type"].stringValue == "6" {
            if json[(String(m_lastIdent))].stringValue == "6" {
                //: isVideoCallRechargeHalfPage = true
                isVideoCallRechargeHalfPage = true
                //: RECHARGE_CLICK_EVENT = subscribtionRecordButton
                userPackageUrl = k_objectPath
                //: } else if json["type"].stringValue == "7" { // 点击订阅弹窗的订阅按钮
            } else if json[(String(m_lastIdent))].stringValue == "7" { // 点击订阅弹窗的订阅按钮
                //: RECHARGE_CLICK_EVENT = subscribtionRecordButton
                userPackageUrl = k_objectPath
            }
            //: applePay_subscribe(type: json["type"].stringValue, productId: json["goodsId"].stringValue, payType: .Subscribe)
            actionType(type: json[(String(m_lastIdent))].stringValue, productId: json[(String(mainErrorKey.suffix(7)))].stringValue, payType: .Subscribe)

        //: case .MethodType_changeMenuButton:
        case .MethodType_changeMenuButton:
            //: changeRightMenuButton(title: json["title"].stringValue, url: json["url"].stringValue)
            untilJump(title: json[(String(appFrameGiftMsg))].stringValue, url: json[(k_instanceData.replacingOccurrences(of: "path", with: "l"))].stringValue)

        //: case .MethodType_refreshCoin:
        case .MethodType_refreshCoin:
            //: refreshCoin(price: Double(json["money"].stringValue) ?? 0, payMode: "Other", scene: json["scene"].stringValue)
            compareScene(price: Double(json[(dream_timeCenterValue.replacingOccurrences(of: "post", with: "on"))].stringValue) ?? 0, payMode: (String(noti_managerEqualName)), scene: json[(String(k_intimateRowUserTitle))].stringValue)

        //: case .MethodType_appearsOnGiftBroadcast:
        case .MethodType_appearsOnGiftBroadcast:
            //: appearsOnGiftBroadcast(show: json["show"].boolValue)
            financialAid(show: json[(kRequestText.replacingOccurrences(of: "greet", with: "h"))].boolValue)

        //: case.MethodType_ToUrl:
        case .MethodType_ToUrl:
            //: openTourl(url: json.rawString() ?? "")
            storageMedium(url: json.rawString() ?? "")

        //: case .MethodType_customerService:
        case .MethodType_customerService:
            //: NamePushManager.share.func__pushToPriveteChatVC(chatID: FacultyThen.getCustomerServiceID())
            NamePushManager.share.signature(chatID: FacultyThen.withAdd())

        //: case.setRightMenuButton:
        case .setRightMenuButton:
            //: break
            break

        //: case .MethodType_taskCenter:
        case .MethodType_taskCenter:
            //: let target = json["target"].intValue
            let target = json[(dreamNameLabelUrl.replacingOccurrences(of: "video", with: "t") + k_screenColorMessage.replacingOccurrences(of: "shared", with: "ar"))].intValue
            //: self.reloadInputViews()
            self.reloadInputViews()
            //: switch target {
            switch target {
            //: case 1:
            case 1: /// 签到领金币页
                //: NamePushManager.share.func__pushToSubscribePageWebVC()
                NamePushManager.share.phoneVc()

            //: case 2:
            case 2: /// 个人信息编辑页面
                //: let vc = TalkingEditProfilesVC.init()
                let vc = DataProfilesVc()
                //: self.navigationController?.pushViewController(vc, animated: true)
                self.navigationController?.pushViewController(vc, animated: true)

            //: case 3:
            case 3: /// 真人认证页面
                //: verifyBtnClick()
                footing()
            //: case 4:
            case 4: /// 发布动态页面
                //: tabberSelete(type: TabBarItemType.Moment.rawValue)
                forEachDoinge(type: TypeNameConvertible.Moment.rawValue)
                /// 防止动态首页广播还没创建
                //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                    //: NotificationCenter.default.post(name: FREEBTN_UPLOAD_POST_NOTIFICATION,
                    NotificationCenter.default.post(name: mSaltEnterText,
                                                    //: object: nil,
                                                    object: nil,
                                                    //: userInfo: nil)
                                                    userInfo: nil)
                }
            //: case 5:
            case 5: /// 我的页面
                //: tabberSelete(type: TabBarItemType.Account.rawValue)
                forEachDoinge(type: TypeNameConvertible.Account.rawValue)
            //: case 6:
            case 6: /// 首页
                //: tabberSelete(type: TabBarItemType.Social.rawValue)
                forEachDoinge(type: TypeNameConvertible.Social.rawValue)
            //: case 7:
            case 7: /// 消息列表页
                //: tabberSelete(type: TabBarItemType.Message.rawValue)
                forEachDoinge(type: TypeNameConvertible.Message.rawValue)
            //: case 8:
            case 8: /// Greet setting页
                //: let vc = TalkingAutoGreetSettingVC()
                let vc = LogReactiveCompatibleVC()
                //: navigationController?.pushViewController(vc, animated: true)
                navigationController?.pushViewController(vc, animated: true)
            //: case 9:
            case 9: /// 动态列表页
                //: tabberSelete(type: TabBarItemType.Moment.rawValue)
                forEachDoinge(type: TypeNameConvertible.Moment.rawValue)
            //: default:
            default:
                //: break
                break
            }
        //: case .MethodType_systemNotification:
        case .MethodType_systemNotification:
            //: let type = json["type"].intValue
            let type = json[(String(m_lastIdent))].intValue
            //: changeNotifaStatus(type: type)
            statusType(type: type)

        //: case .MethodType_refreshPoint:
        case .MethodType_refreshPoint:
            //: refreshPoint()
            colorPoint()

        //: case .MethodType_onEvent:
        case .MethodType_onEvent:
            //: reportEvent(eventName: json["eventName"].stringValue, jsonStr: json["jsonString"].stringValue)
            elementStr(eventName: json[(String(dreamStackText.suffix(9)))].stringValue, jsonStr: json[(String(main_pushViewYearValue) + String(k_onCenterPathName))].stringValue)

        //: case .MethodType_refreshCoinAll:
        case .MethodType_refreshCoinAll:
            //: req_refreshCoinAll(coin: json["coin"].stringValue)
            nowDoing(coin: json[(k_rowEqualId.replacingOccurrences(of: "view", with: "in"))].stringValue)

        //: case .MethodType_refreshPackage:
        case .MethodType_refreshPackage:
            //: req_refreshPackage()
            contextOfUseAdd()

        //: case .MethodType_vibrate:
        case .MethodType_vibrate:
            //: phoneVibrate()
            generate()

        //: case .MethodType_goFreeVideo:
        case .MethodType_goFreeVideo:
            //: AppManagerRequest.func__requestUserInfo { succeed, result, errorModel in
            PlayerReactiveCompatible.pageLike { _, _, _ in
                //: if PathAppManager.share.loginUserMode.jumpType == 1 {
                if PathAppManager.share.loginUserMode.jumpType == 1 {
                    //: self.func__closeAction(sender: nil)
                    self.resourceNose(sender: nil)
                    //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                    DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                        //: NamePushManager.share.func__pushToRandomVideoVC(isBeginRand: false)
                        NamePushManager.share.noEqual(isBeginRand: false)
                    }
                }
            }

        //: case .MethodType_goMessageDetail:
        case .MethodType_goMessageDetail:
            //: NamePushManager.share.func__pushToPriveteChatVC(chatID: json["uid"].stringValue)
            NamePushManager.share.signature(chatID: json[(mainBindIdent.lowercased())].stringValue)

        //: case .MethodType_goPersonInfo:
        case .MethodType_goPersonInfo:
            //: NamePushManager.share.func__pushToUserDetailVC(uid: json["uid"].stringValue)
            NamePushManager.share.pressAway(uid: json[(mainBindIdent.lowercased())].stringValue)

        //: case .MethodType_showRechargeDialog:
        case .MethodType_showRechargeDialog:
            //: NamePushManager.share.func__pushToHalfWebVC(webViewType: .RechargeHalfPage)
            NamePushManager.share.clickSize(webViewType: .RechargeHalfPage)

        //: case .OpenRetainPopup:
        case .OpenRetainPopup:
            //: self.func__closeAction(sender: nil)
            self.resourceNose(sender: nil)
        //: case .openBrowserWithUrl:
        case .openBrowserWithUrl:
            //: openToOutUrl(url: json.rawString() ?? "")
            jimmyStart(url: json.rawString() ?? "")
        //: case .none:
        case .none:
            //: printLog(message: "未实现的js事件：\(message.name)")
            printLog(message: (mainMakeCalledPath) + "\(message.name)")
        }
    }

    // MARK: - Event

    /// 苹果订阅
    /// - Parameters:
    ///   - type: 订阅入口；1：首页banner，2：全屏充值页，3：半屏充值页，4：领取金币弹窗
    ///   - productId: 商品Id
    ///   - payType: 支付类型
    //: private func applePay_subscribe(type: String, productId: String, payType: ApplePayType) {
    private func actionType(type _: String, productId: String, payType: GiftType) {
        //: applePay(productId: productId, payType: payType)
        enterArray(productId: productId, payType: payType)
    }

    /// 苹果支付/订阅
    /// - Parameters:
    ///   - productId: productId: 商品Id
    ///   - source: 充值来源
    //: private func applePay(productId: String, source: Int = -1, payType: ApplePayType) {
    private func enterArray(productId: String, source: Int = -1, payType: GiftType) {
        //: if PathAppManager.share.loginUid.isEmptyString {
        if PathAppManager.share.loginUid.isEmptyString {
            //: return
            return
        }
        //: FirebaseAnalyticsManager.share.logEvent(name: "PurchaseClick")
        LabSearchionAnalyticsManager.share.crop(name: (String(show_contentIdent) + k_sendPath.replacingOccurrences(of: "refuse", with: "k")))

        //: ProgressHUD.show()
        ShirtButtonThen.distance()
        //: var index = isVideoCallRechargeHalfPage ? 1: 0
        var index = isVideoCallRechargeHalfPage ? 1 : 0
        //: if source != -1 {
        if source != -1 {
            //: index = source
            index = source
        }
        //: AppleIAPManager.shared.iap_startPurchase(productId: productId, payType: payType, source: index) { status, reportMoney, payType in
        SearchRequestDelegate.shared.shouldAdd(productId: productId, payType: payType, source: index) { status, reportMoney, payType in
            //: ProgressHUD.dismiss()
            ShirtButtonThen.allowButton()
            //: DispatchQueue.main.async { [self] in
            DispatchQueue.main.async { [self] in
                //: switch status {
                switch status {
                //: case .verityFail:
                case .verityFail:
                    //: ProgressHUD.toast( "Retry After or Go to \"Feedback\" to contact us".localized)
                    ShirtButtonThen.tableSign((String(noti_cellKey.suffix(7)) + String(app_addIdent.suffix(5)) + "or Go to ") + "\"" + (show_playerKey.replacingOccurrences(of: "s", with: "F") + String(k_soulPath)) + "\"" + (String(showIconIndexIdent) + String(userTotalIdent)).localized)
                //: case .veritySucceed:
                case .veritySucceed:
                    //: appleIAPVeritySucceed(price: reportMoney, payType: payType)
                    underpay(price: reportMoney, payType: payType)
                //: case .renewSucceed:
                case .renewSucceed:
                    //: let scene = (payType == .Subscribe) ? "1":"2"
                    let scene = (payType == .Subscribe) ? "1" : "2"
                    //: refreshCoin(price: reportMoney, payMode: "Apple", scene: scene)
                    compareScene(price: reportMoney, payMode: (String(noti_interactionUpIdent.prefix(5))), scene: scene)
                //: default:
                default:
                    //: printLog(message: " apple支付充值失败：\(status.rawValue)")
                    printLog(message: (String(showReceiveId.suffix(4)) + "ple支" + mFrameValue.capitalized) + "\(status.rawValue)")
                }
            }
        }
    }

    /// 设置导航右侧按钮
    /// - Parameters:
    ///   - title: 标题
    ///   - url: 地址
    //: private func changeRightMenuButton(title: String, url: String) {
    private func untilJump(title: String, url: String) {
        //: let rightBtn = TalkingButton()
        let rightBtn = TelevisionButton()
        //: rightBtn.titleLabel?.font = .systemFont(ofSize: 14)
        rightBtn.titleLabel?.font = .systemFont(ofSize: 14)
        //: rightBtn.setTitle(title, for: .normal)
        rightBtn.setTitle(title, for: .normal)
        //: rightBtn.setTitleColor(.RGBA(51, 51, 51, 1), for: .normal)
        rightBtn.setTitleColor(.futurism(51, 51, 51, 1), for: .normal)
        //: rightBtn.addTarget(self, action: #selector(rightButtonClick), for: .touchUpInside)
        rightBtn.addTarget(self, action: #selector(appropriateDoing), for: .touchUpInside)
        //: rightBtn.sizeToFit()
        rightBtn.sizeToFit()
        //: rightBtn.userInfo = ["url": url]
        rightBtn.userInfo = [(k_instanceData.replacingOccurrences(of: "path", with: "l")): url]
        //: self.navigationItem.rightBarButtonItem = UIBarButtonItem(customView: rightBtn)
        self.navigationItem.rightBarButtonItem = UIBarButtonItem(customView: rightBtn)
    }

    //: @objc func rightButtonClick(button: TalkingButton) {
    @objc func appropriateDoing(button: TelevisionButton) {
        //: guard let urlString = button.userInfo?["url"] as? String else {
        guard let urlString = button.userInfo?[(k_instanceData.replacingOccurrences(of: "path", with: "l"))] as? String else {
            //: return
            return
        }

        //: self.urlStr = urlString
        self.urlStr = urlString
        //: func__startRequest()
        succeedMethod()
    }

    /// 退出登录
    //: private func needLogin() {
    private func screenUpLogin() {
        //: guard Int(PathAppManager.share.loginUid) ?? 0 > 0 else {
        guard Int(PathAppManager.share.loginUid) ?? 0 > 0 else {
            //: self.webView.reload()
            self.webView.reload()
            //: return
            return
        }

        //: NotificationCenter.default.post(name: DID_LOGIN_OUT_SUCCESS_NOTIFICATION,
        NotificationCenter.default.post(name: mDetailId,
                                        //: object: nil,
                                        object: nil,
                                        //: userInfo: nil)
                                        userInfo: nil)
    }

    /// 苹果 支付/订阅 成功验证通过
    /// - Parameters:
    ///   - price: 充值金额
    ///   - payType: 支付类型
    //: func appleIAPVeritySucceed(price: Double, payType: ApplePayType) {
    func underpay(price: Double, payType: GiftType) {
        //: self.webView.evaluateJavaScript("payResultCallback();") { data, error in
        self.webView.evaluateJavaScript((String(showCornerName) + String(appBindValue) + String(notiHiddenIdent))) { _, _ in
        }
        //: let scene = (payType == .Subscribe) ? "1":"2"
        let scene = (payType == .Subscribe) ? "1" : "2"
        //: refreshCoin(price: price, payMode: "Apple", scene: scene)
        compareScene(price: price, payMode: (String(noti_interactionUpIdent.prefix(5))), scene: scene)
    }

    /// 支付/订阅 成功刷新个人信息【Apple/Other】
    /// - Parameters:
    ///   - price: 充值金额
    ///   - payMode: 类型
    ///   - scene: 1：订阅，2：内购
    //: private func refreshCoin(price: Double, payMode: String, scene: String) {
    private func compareScene(price: Double, payMode: String, scene: String) {
        //: NotificationCenter.default.post(name: UPDATE_USER_GETMYINFO_NOTIFICATION, object: nil)
        NotificationCenter.default.post(name: user_positionPath, object: nil)
        //: NotificationCenter.default.post(name: UPDATE_USER_GETINFOCOLUMN_NOTIFICATION, object: nil)
        NotificationCenter.default.post(name: show_limitIdent, object: nil)
        //: NotificationCenter.default.post(name: REFRESH_INDEX_GETCONFIG_NOTIFICATION, object: nil)
        NotificationCenter.default.post(name: m_warningDisplayTitle, object: nil)
        //: NotificationCenter.default.post(name: USER_UPDATE_LOUNGEPLUSINFO_NOTIFICATION, object: nil)
        NotificationCenter.default.post(name: notiInputMsg, object: nil)
        // 成功埋点
        //: if scene == "1" {
        if scene == "1" {
            //: TalkingAdjustManager.share.addSubscribeEvent(price: price, currency: "USD")
            GiftPortAdjustManager.share.freshmanClick(price: price, currency: (String(main_colorFormat)))
            //: } else {
        } else {
            //: TalkingAdjustManager.share.addPurchasedEvent(price: price, currency: "USD")
            GiftPortAdjustManager.share.nighttime(price: price, currency: (String(main_colorFormat)))
        }
        //: FirebaseAnalyticsManager.share.trackPurchase(payType: payMode, price: price, currency: "USD")
        LabSearchionAnalyticsManager.share.priceRemark(payType: payMode, price: price, currency: (String(main_colorFormat)))

        // 半屏充值成功埋点
        //: if RECHARGE_CLICK_EVENT.count > 0 {
        if userPackageUrl.count > 0 {
            //: uploadRecord.uploadRecordEvent(eventID: RECHARGE_CLICK_EVENT, parameterStr: ["amount": price])
            kResolutionText.toStr(eventID: userPackageUrl, parameterStr: [String(bytes: mPlayerData.map{labReply(tap: $0)}, encoding: .utf8)!: price])
        }

        // 支付成功发送通知，调用三方js事件
        //: NotificationCenter.default.post(name: WEBVIEW_THIRD_REFRESHCOIN_NOTIFICATION, object: nil)
        NotificationCenter.default.post(name: dreamStreamIdent, object: nil)
    }

    /// 广播设置
    /// - Parameter show: 是否上广播
    //: private func appearsOnGiftBroadcast(show: Bool) {
    private func financialAid(show _: Bool) {}

    //: private func verifyBtnClick() {
    private func footing() {
        //: if PathAppManager.share.loginUserMode.isTPAuth == TPUserAuth.isSuccessed.rawValue {
        if PathAppManager.share.loginUserMode.isTPAuth == ConfinementScalarLiteral.isSuccessed.rawValue {
            //: return
            return
                //: } else if PathAppManager.share.loginUserMode.isTPAuth == TPUserAuth.unknown.rawValue {
        } else if PathAppManager.share.loginUserMode.isTPAuth == ConfinementScalarLiteral.unknown.rawValue {
            //: NamePushManager.share.func__pushUserVerifyController(toast: nil)
            NamePushManager.share.collectionController(toast: nil)
            //: } else if PathAppManager.share.loginUserMode.isTPAuth == TPUserAuth.isOnGoing.rawValue {
        } else if PathAppManager.share.loginUserMode.isTPAuth == ConfinementScalarLiteral.isOnGoing.rawValue {
            //: let vc = TalkingFinalVerificationVC()
            let vc = RenderRecognizerDelegate()
            //: NamePushManager.share.func__getCurrentActivityVC()?.navigationController?.pushViewController(vc, animated: true)
            NamePushManager.share.toponymVc()?.navigationController?.pushViewController(vc, animated: true)
            //: } else {
        } else {
            //: NamePushManager.share.func__pushUserVerifyController(toast: nil)
            NamePushManager.share.collectionController(toast: nil)
        }
    }

    /// 底部tabbar切换
    //: private func tabberSelete(type: Int) {
    private func forEachDoinge(type: Int) {
        //: guard type < self.tabBarController?.viewControllers?.count ?? 0 else { return }
        guard type < self.tabBarController?.viewControllers?.count ?? 0 else { return }
        //: let vc = self.tabBarController?.viewControllers?[type]
        let vc = self.tabBarController?.viewControllers?[type]
        //: self.tabBarController?.tabBar.isHidden = false
        self.tabBarController?.tabBar.isHidden = false
        //: self.tabBarController?.selectedIndex = type
        self.tabBarController?.selectedIndex = type
        //: self.tabBarController?.selectedViewController = vc
        self.tabBarController?.selectedViewController = vc
        //: self.navigationController?.popViewController(animated: true)
        self.navigationController?.popViewController(animated: true)
    }

    /// 获取系统通知状态
    //: private func changeNotifaStatus(type: Int) {
    private func statusType(type: Int) {
        //: if type == 0 {
        if type == 0 {
            //: if let url = URL(string: UIApplication.openSettingsURLString), UIApplication.shared.canOpenURL(url) {
            if let url = URL(string: UIApplication.openSettingsURLString), UIApplication.shared.canOpenURL(url) {
                //: if #available(iOS 10.0, *) {
                if #available(iOS 10.0, *) {
                    //: UIApplication.shared.open(url, options: [:], completionHandler: nil)
                    UIApplication.shared.open(url, options: [:], completionHandler: nil)
                    //: } else {
                } else {
                    //: if UIApplication.shared.canOpenURL(url) {
                    if UIApplication.shared.canOpenURL(url) {
                        //: UIApplication.shared.openURL(url)
                        UIApplication.shared.openURL(url)
                    }
                }
            }
            //: } else if type == 1 {
        } else if type == 1 {
            //: AppManagerRequest.func__requestUserConfig { succeed, result, errorModel in }
            PlayerReactiveCompatible.enableFrom { _, _, _ in }
            //: AppManagerRequest.func__requestUserInfo { succeed, result, errorModel in }
            PlayerReactiveCompatible.pageLike { _, _, _ in }
            //: TalkingPermissionTool.checkPushNotification { isShow in
            EqualReactiveCompatible.errorEqual { isShow in
                //: DispatchQueue.main.sync {
                DispatchQueue.main.sync {
                    //: let str = isShow ? "getSystemNotificationtStatus(true)" : "getSystemNotificationtStatus(false)"
                    let str = isShow ? String(bytes: noti_shadowTitle.map{contentualMatter(row: $0)}, encoding: .utf8)! : String(bytes: userEmptyName.map{screenFrame(talk: $0)}, encoding: .utf8)!
                    //: self.webView.evaluateJavaScript(str) { data, error in
                    self.webView.evaluateJavaScript(str) { _, _ in
                    }
                }
            }
        }
    }

    //: private func refreshPoint() {
    private func colorPoint() {
        //: AppManagerRequest.func__requestUserInfo { succeed, result, errorModel in
        PlayerReactiveCompatible.pageLike { _, _, _ in
            //: if TalkingLiveManager.shared().isLive {
            if CommentLiveManager.leaveBy().isLive {
                //: NotificationCenter.default.post(name: LIVE_USER_POINT_CHANGE, object: nil, userInfo: ["mfBean": 0.0])
                NotificationCenter.default.post(name: kTabSearchMessage, object: nil, userInfo: [String(bytes: main_promptIdent.map{nutsAndBolts(body: $0)}, encoding: .utf8)!: 0.0])
            }
        }
    }

    /// 刷新金币
    /// - Parameter coin: 传' ', 刷新接口；传其他，进行相加（含负数））
    //: private func req_refreshCoinAll(coin: String) {
    private func nowDoing(coin: String) {
        //: let value: Double = Double(coin) ?? 0
        let value = Double(coin) ?? 0
        //: if value == 0 {
        if value == 0 { // 刷新接口
            //: AppManagerRequest.func__getInfoColumn { succeed, result, errorModel in
            PlayerReactiveCompatible.noneLayer { _, _, _ in
            }
            //: } else {
        } else { // 加减
            //: let newCoin = Double(PathAppManager.share.loginUserMode.mf_coin)! + value
            let newCoin = Double(PathAppManager.share.loginUserMode.mf_coin)! + value
            //: PathAppManager.share.loginUserMode.mf_coin = String(format: "%.2f", newCoin)
            PathAppManager.share.loginUserMode.mf_coin = String(format: "%.2f", newCoin)
        }
    }

    /// 刷新礼物背包
    //: private func req_refreshPackage() {
    private func contextOfUseAdd() {
        //: NotificationCenter.default.post(name: GIFT_REFRESHPACKAGE_NOTIFICATION, object: nil)
        NotificationCenter.default.post(name: kSuccessFormat, object: nil)
    }

    /// 事件上报
    /// - Parameters:
    ///   - eventName: 事件名称
    ///   - jsonStr: 事件参数
    //: private func reportEvent(eventName: String, jsonStr: String) {
    private func elementStr(eventName: String, jsonStr: String) {
        //: uploadRecord.uploadRecordEvent(eventID: eventName, toUid: "", jsonStr: jsonStr)
        kResolutionText.atJson(eventID: eventName, toUid: "", jsonStr: jsonStr)
    }

    /// 手机震动
    //: private func phoneVibrate() {
    private func generate() {
        //: AudioServicesPlaySystemSound(kSystemSoundID_Vibrate)
        AudioServicesPlaySystemSound(kSystemSoundID_Vibrate)
    }
}
