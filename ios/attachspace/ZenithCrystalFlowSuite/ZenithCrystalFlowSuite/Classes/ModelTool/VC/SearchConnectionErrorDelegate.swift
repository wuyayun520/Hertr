
//: Declare String Begin

/*: "#201E50" :*/
fileprivate let dreamInputPath:String = "attention app#201E50"

/*: "#1F1624" :*/
fileprivate let userTableId:String = "#1F162to gift model select"
fileprivate let kModelData:String = "4"

/*: "quick_video_topview" :*/
fileprivate let userTimeRegularUrl:String = "quiconfirmk"
fileprivate let appClearKey:String = "image add var transitioneo_t"

/*: "nav_back_black_nor" :*/
fileprivate let dreamBetweenValue:[Character] = ["n","a","v","_","b","a","c","k","_","b","l","a","c","k","_"]
fileprivate let notiTitleIdent:[Character] = ["n","o","r"]

/*: "Random Video" :*/
fileprivate let kFrameStr:[Character] = ["R","a","n","d","o"]
fileprivate let userConversationTitle:String = "event request attention privatem Video"

/*: "quick_video_explain" :*/
fileprivate let kGiftMessage:[Character] = ["q","u","i","c","k","_","v","i","d","e","o","_","e","x","p","l","a","i","n"]

/*: "get json error" :*/
fileprivate let notiSystemPointFormat:String = "back succeed custom sizeget "
fileprivate let kLastTitlePath:String = "eregardor"

/*: "Searching for perfect matches. please be patient." :*/
fileprivate let user_currentMsg:[UInt8] = [0x8,0x3e,0x3a,0x29,0x38,0x33,0x32,0x35,0x3c,0x7b,0x3d,0x34,0x29,0x7b,0x2b,0x3e,0x29,0x3d,0x3e,0x38,0x2f,0x7b,0x36,0x3a,0x2f,0x38,0x33,0x3e,0x28,0x75,0x7b,0x2b,0x37,0x3e,0x3a,0x28,0x3e,0x7b,0x39,0x3e,0x7b,0x2b,0x3a,0x2f,0x32,0x3e,0x35,0x2f,0x75]

/*: "Matching..." :*/
fileprivate let appTitleStr:String = "Matas model mode"
fileprivate let kPointKey:[Character] = ["c","h","i","n","g",".",".","."]

/*: "matchId" :*/
fileprivate let main_maxId:String = "matchIdany background open"

/*: "Random Video helps locating your matches online quickly. Random Videos are priced at 40 gold coins per minute and are paid for by the party calling the video." :*/
fileprivate let notiToUrl:[UInt8] = []

/*: "OK" :*/
fileprivate let app_effectPath:String = "Olist"

/*: "onVideoMatch" :*/
fileprivate let noti_sectionTitle:String = "value guard string imageonVide"

/*: "retryAfter" :*/
fileprivate let showAtId:[Character] = ["r","e","t","r","y","A","f","t","e"]
fileprivate let noti_labText:String = "manager"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  SearchConnectionErrorDelegate.swift
//  AbroadTalking
//
//  Created by young on 2022/11/4.
//

//: import UIKit
import UIKit

//: class TalkingQuickVideoMatchViewController: TalkingBaseViewController {
class SearchConnectionErrorDelegate: ListRecognizerDelegate {
	var meNumber: Int = 70
	var timeDataTotal: Double = -64.1
	var winnerSCircleDictionary: [AnyHashable: String] = [:]
	var norCount: Int = 78
	var alongSum: Double = 46.6
	var activityDictionary: [AnyHashable: String] = [:]
	var magnitudeInterval: Int = 40
	var magnitudeimateSum: Double = -86.0
	var skiagramDictionary: [AnyHashable: String] = [:]

    //: private var headTimer: Timer?
    private var headTimer: Timer? // 头像展示定时器
    //: private var retryAfterTimer: Timer?
    private var retryAfterTimer: Timer? // 匹配失败重试定时器
    //: private var matchId: Int = 0
    private var matchId: Int = 0 // 速配批次Id

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: req_videoMatchHeadPics()
        betweenPath()
        //: startVideoMatch()
        checkBeauty()
    
            if (bgImgView.viewWithTag(3253) != nil) && (bgImgView.layer.anchorPoint.x != 0.5) {
                //: SWIFT_CUSTOM_DANGER_File_Call
                let modelAddress = StartView(frame: bgImgView.bounds.offsetBy(dx: CGFloat(78.82), dy: CGFloat(0)))
            modelAddress.userQuantity = matchId


            
            modelAddress.offNumber = { [self] changeTotal in
            self.magnitudeInterval = changeTotal
            
            var modelAddress = matchId
                modelAddress -= 1
                if modelAddress <= 0 {
                    modelAddress = modelAddress + 1
                }
            if modelAddress > self.magnitudeInterval {
                self.magnitudeInterval = modelAddress
            }
            
            return self.magnitudeInterval
            }
            modelAddress.sectionLabelSum = { [self] cookingUtensilNumber in
            self.magnitudeimateSum = cookingUtensilNumber
            
            return self.magnitudeimateSum
            }
            modelAddress.miniIdentityDictionary = { [self] detailContainerDictionary in
            self.skiagramDictionary = detailContainerDictionary
            
            guard var value = self.skiagramDictionary as? [String: String] else {
                return nil
            }
            return value
            }
                bgImgView.addSubview(modelAddress)
            }

	}

    //: override func viewWillDisappear(_ animated: Bool) {
    override func viewWillDisappear(_ animated: Bool) {
        //: super.viewWillDisappear(animated)
        super.viewWillDisappear(animated)
        //: stopVideoMatch()
        viewConsort()
    
            if (headImgView.convert(CGRect(x: 0, y: 0, width: 0, height: 0), from: headImgView.superview).size.width == 17.88) && (headImgView.motionEffects.count == 11) {
                //: SWIFT_CUSTOM_DANGER_File_Call
                let snapAcross = StartView()
            snapAcross.userQuantity = matchId


            
            snapAcross.offNumber = { [self] changeTotal in
            self.norCount = changeTotal
            
            var snapAcross = matchId
            snapAcross /= 4
            if snapAcross < self.norCount {
                self.norCount = snapAcross
            }
            
            return self.norCount
            }
            snapAcross.sectionLabelSum = { [self] cookingUtensilNumber in
            self.alongSum = cookingUtensilNumber
            
            return self.alongSum
            }
            snapAcross.miniIdentityDictionary = { [self] detailContainerDictionary in
            self.activityDictionary = detailContainerDictionary
            
            guard var value = self.activityDictionary as? [String: String] else {
                return nil
            }
            return value
            }
                headImgView.addSubview(snapAcross)
            }

	}

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true

        //: setupSubviews()
        setupImageSubviews()
        //: setupSubViewsConstraint()
        quantityerruptByConstraint()
    
            if (bgImgView.convert(CGRect(x: 0, y: 0, width: 0, height: 0), from: bgImgView.superview).size.width == 17.88) && (bgImgView.motionEffects.count == 11) {
                //: SWIFT_CUSTOM_DANGER_File_Call
                let snapAcross = StartView()
            snapAcross.userQuantity = matchId


            
            snapAcross.offNumber = { [self] changeTotal in
            self.meNumber = changeTotal
            
            var snapAcross = matchId
            snapAcross /= 4
            if snapAcross < self.meNumber {
                self.meNumber = snapAcross
            }
            
            return self.meNumber
            }
            snapAcross.sectionLabelSum = { [self] cookingUtensilNumber in
            self.timeDataTotal = cookingUtensilNumber
            
            return self.timeDataTotal
            }
            snapAcross.miniIdentityDictionary = { [self] detailContainerDictionary in
            self.winnerSCircleDictionary = detailContainerDictionary
            
            guard var value = self.winnerSCircleDictionary as? [String: String] else {
                return nil
            }
            return value
            }
                bgImgView.addSubview(snapAcross)
            }

	}

    //: deinit {
    deinit {
        //: stopVideoMatch()
        viewConsort()
    }

    // MARK: - Lazy Load

    //: private lazy var bgImgView: UIImageView = {
    private lazy var bgImgView: UIImageView = {
        //: let view = UIImageView.init()
        let view = UIImageView()
        //: view.image = UIImage.imageGradientTopToBottonColor(colors: [UIColor.init(hex: "#201E50")!.cgColor, UIColor.init(hex: "#1F1624")!.cgColor], size: CGSize(width: ScreenWidth, height: ScreenHeight))
        view.image = UIImage.yearDoing(colors: [UIColor(hex: (String(dreamInputPath.suffix(7))))!.cgColor, UIColor(hex: (String(userTableId.prefix(6)) + kModelData.capitalized))!.cgColor], size: CGSize(width: app_screenFailMarginFormat, height: mainAcceptKey))
        //: view.isUserInteractionEnabled = true
        view.isUserInteractionEnabled = true

        //: let topView = UIImageView.init()
        let topView = UIImageView()
        //: topView.image = UIImage.BundleImageNamed(name: "quick_video_topview")
        topView.image = UIImage.sendCollection(name: (userTimeRegularUrl.replacingOccurrences(of: "confirm", with: "c") + "_vid" + String(appClearKey.suffix(4)) + "opview"))
        //: topView.isUserInteractionEnabled = true
        topView.isUserInteractionEnabled = true
        //: view.addSubview(topView)
        view.addSubview(topView)
        //: topView.snp.makeConstraints { make in
        topView.snp.makeConstraints { make in
            //: make.leading.top.equalTo(0)
            make.leading.top.equalTo(0)
            //: make.width.height.equalTo(actualWidth(w: 218))
            make.width.height.equalTo(actualWidth(w: 218))
        }
        //: return view
        return view
        //: }()
    }()

    //: private lazy var navView: UIView = {
    private lazy var navView: UIView = {
        //: let nav = UIView()
        let nav = UIView()
        //: nav.backgroundColor = .clear
        nav.backgroundColor = .clear
        // 返回
        //: let backBtn = UIButton(type: .custom)
        let backBtn = UIButton(type: .custom)
        //: let backBImg = UIImage.BundleImageNamed(name: "nav_back_black_nor").withRenderingMode(.alwaysTemplate)
        let backBImg = UIImage.sendCollection(name: (String(dreamBetweenValue) + String(notiTitleIdent))).withRenderingMode(.alwaysTemplate)
        //: backBtn.setImage(backBImg, for: .normal)
        backBtn.setImage(backBImg, for: .normal)
        //: backBtn.tintColor = .white
        backBtn.tintColor = .white
        //: backBtn.addTarget(self, action: #selector(clickBackButtonAction), for: .touchUpInside)
        backBtn.addTarget(self, action: #selector(isoclinicAction), for: .touchUpInside)
        //: nav.addSubview(backBtn)
        nav.addSubview(backBtn)
        //: backBtn.snp.makeConstraints { make in
        backBtn.snp.makeConstraints { make in
            //: make.leading.equalTo(0)
            make.leading.equalTo(0)
            //: make.width.equalTo(40)
            make.width.equalTo(40)
            //: make.height.equalTo(NavigationBarHeight)
            make.height.equalTo(main_clickMessage)
            //: make.top.equalTo(StatusBarHeight)
            make.top.equalTo(app_topMsg)
        }
        // 标题
        //: let titleLab = UILabel()
        let titleLab = UILabel()
        //: titleLab.font = UIFont.pingfangMediumFont(fontSize: 17)
        titleLab.font = UIFont.upperClassMove(fontSize: 17)
        //: titleLab.textColor = .white
        titleLab.textColor = .white
        //: titleLab.textAlignment = .center
        titleLab.textAlignment = .center
        //: titleLab.text = "Random Video".localized
        titleLab.text = (String(kFrameStr) + String(userConversationTitle.suffix(7))).localized
        //: nav.addSubview(titleLab)
        nav.addSubview(titleLab)
        //: titleLab.snp.makeConstraints { make in
        titleLab.snp.makeConstraints { make in
            //: make.centerY.equalTo(backBtn)
            make.centerY.equalTo(backBtn)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
        }
        // 提示（审核模式隐藏）
        //: if PathAppManager.share.appStatus == AppSkinStatus.normal.rawValue {
        if PathAppManager.share.appStatus == AssemblageHashable.normal.rawValue {
            //: let matchTipsBtn = UIButton(type: .custom)
            let matchTipsBtn = UIButton(type: .custom)
            //: let matchTipsImg = UIImage.BundleImageNamed(name: "quick_video_explain").withRenderingMode(.alwaysTemplate)
            let matchTipsImg = UIImage.sendCollection(name: (String(kGiftMessage))).withRenderingMode(.alwaysTemplate)
            //: matchTipsBtn.setImage(matchTipsImg, for: .normal)
            matchTipsBtn.setImage(matchTipsImg, for: .normal)
            //: matchTipsBtn.tintColor = .white
            matchTipsBtn.tintColor = .white
            //: matchTipsBtn.addTarget(self, action: #selector(matchTipsButtonClick), for: .touchUpInside)
            matchTipsBtn.addTarget(self, action: #selector(tips), for: .touchUpInside)
            //: nav.addSubview(matchTipsBtn)
            nav.addSubview(matchTipsBtn)
            //: matchTipsBtn.snp.makeConstraints { make in
            matchTipsBtn.snp.makeConstraints { make in
                //: make.trailing.equalTo(0)
                make.trailing.equalTo(0)
                //: make.centerY.equalTo(backBtn)
                make.centerY.equalTo(backBtn)
                //: make.size.equalTo(CGSize(width: 45, height: NavigationBarHeight))
                make.size.equalTo(CGSize(width: 45, height: main_clickMessage))
            }
        }

        //: return nav
        return nav
        //: }()
    }()

    //: lazy var svgaPlayer: SVGAPlayer = {
    lazy var svgaPlayer: SVGAPlayer = {
        //: let player = SVGAPlayer.init()
        let player = SVGAPlayer()
        //: player.loops = 0
        player.loops = 0
        //: player.clearsAfterStop = false
        player.clearsAfterStop = false
        //: player.isUserInteractionEnabled = false
        player.isUserInteractionEnabled = false
        //: player.contentMode = .scaleAspectFill
        player.contentMode = .scaleAspectFill

        //: let url = SVGAEffectTool.default.getZipEffectPath(type: .Quick_Video_BG)
        let url = ReverberationInsideHoeHandleReactiveCompatible.default.pullInConfirmType(type: .Quick_Video_BG)
        //: do {
        do {
            //: let data = try Data(contentsOf: url)
            let data = try Data(contentsOf: url)
            //: let parser = SVGAParser.init()
            let parser = SVGAParser()
            //: parser.parse(with: data, cacheKey: "") { [weak self] videoItem in
            parser.parse(with: data, cacheKey: "") { [weak self] videoItem in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: player.videoItem = videoItem
                player.videoItem = videoItem
                //: player.startAnimation()
                player.startAnimation()
            }
            //: } catch {
        } catch {
            //: printLog(message: "get json error")
            printLog(message: (String(notiSystemPointFormat.suffix(4)) + "json " + kLastTitlePath.replacingOccurrences(of: "regard", with: "rr")))
        }

        //: return player
        return player
        //: }()
    }()

    //: private lazy var tipsLab: UILabel = {
    private lazy var tipsLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.font = UIFont.pingfangRugularFont(fontSize: 15)
        lab.font = UIFont.indicatorSize(fontSize: 15)
        //: lab.textColor = UIColor.init(white: 1, alpha: 0.7)
        lab.textColor = UIColor(white: 1, alpha: 0.7)
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: lab.text = "Searching for perfect matches. please be patient.".localized
        lab.text = String(bytes: user_currentMsg.map{$0^91}, encoding: .utf8)!.localized
        //: lab.numberOfLines = 0
        lab.numberOfLines = 0
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var matchingLab: UILabel = {
    private lazy var matchingLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.font = UIFont.pingfangMediumFont(fontSize: 18)
        lab.font = UIFont.upperClassMove(fontSize: 18)
        //: lab.textColor = .white
        lab.textColor = .white
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: lab.text = "Matching...".localized
        lab.text = (String(appTitleStr.prefix(3)) + String(kPointKey)).localized
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var headImgView: UIImageView = {
    private lazy var headImgView: UIImageView = {
        //: let img = UIImageView()
        let img = UIImageView()
        //: img.contentMode = .scaleAspectFill
        img.contentMode = .scaleAspectFill
        //: img.layer.cornerRadius = actualWidth(w: 90)/2
        img.layer.cornerRadius = actualWidth(w: 90) / 2
        //: img.image = UIImage.placeHolderImage(sex: Gender.female.rawValue)
        img.image = UIImage.indicatorSex(sex: ConnectionClusterLiteral.female.rawValue)
        //: img.layer.masksToBounds = true
        img.layer.masksToBounds = true
        //: return img
        return img
        //: }()
    }()

    //: private lazy var dataArr: [String] = {
    private lazy var dataArr: [String] = //: return Array<String>()
        .init()
    //: }()

    //: private lazy var resultDataArr: [TalkingMatchResultModel] = {
    private lazy var resultDataArr: [FastenerTransformable] = //: return Array<FastenerTransformable>()
        .init()
    //: }()
}

// MARK: - Request & 数据处理

//: extension TalkingQuickVideoMatchViewController {
extension SearchConnectionErrorDelegate {
    /// 速配头像列表（男）
    //: private func req_videoMatchHeadPics() {
    private func betweenPath() {
        //: TalkingQuickVideoManager.req_videoMatchHeadPics { [weak self] succeed, result, errorModel in
        WindowReactiveCompatible.pics { [weak self] succeed, result, _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard succeed == true else { return }
            guard succeed == true else { return }

            //: self.dataArr.removeAll()
            self.dataArr.removeAll()
            //: self.dataArr = result as! [String]
            self.dataArr = result as! [String]

            //: var index = 0
            var index = 0
            //: self.headTimer = Timer.scheduledTimer(withTimeInterval: 5, block: { timer in
            self.headTimer = Timer.scheduledTimer(withTimeInterval: 5, block: { _ in
                // 实现无限循环
                //: if index >= self.dataArr.count { index = 0 }
                if index >= self.dataArr.count { index = 0 }
                // 网络加载图片，实现高斯模糊效果
                //: self.headImgView.setUrlImage(urlStr: self.dataArr[index]) { isSucceed in
                self.headImgView.glycerolizeNeed(urlStr: self.dataArr[index]) { _ in
                    //: guard let image = self.headImgView.image else { return }
                    guard let image = self.headImgView.image else { return }
                    //: self.headImgView.image = UIImage.blur(theImage: image, blurValue: 17)
                    self.headImgView.image = UIImage.searchion(theImage: image, blurValue: 17)
                }
                //: index += 1
                index += 1
                //: }, repeats: true)
            }, repeats: true)
            //: self.headTimer?.fire()
            self.headTimer?.fire()
        }
    }

    /// 开始视频速配（男）
    //: private func req_videoMatchToMatch() {
    private func writeCopy() {
        //: TalkingQuickVideoManager.req_videoMatchToMatch(matchId: self.matchId) { succeed, result, errorModel in
        WindowReactiveCompatible.dismissInfo(matchId: self.matchId) { succeed, result, _ in
            //: guard succeed == true else { return }
            guard succeed == true else { return }

            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())
            //: self.matchId = json["matchId"].intValue
            self.matchId = json[(String(main_maxId.prefix(7)))].intValue
        }
    }
}

// MARK: - Event

//: extension TalkingQuickVideoMatchViewController {
extension SearchConnectionErrorDelegate {
    /// 停止视频速配
    //: private func stopVideoMatch() {
    private func viewConsort() {
        // 销毁定时器
        //: if self.headTimer != nil {
        if self.headTimer != nil {
            //: self.headTimer?.invalidate()
            self.headTimer?.invalidate()
            //: self.headTimer = nil
            self.headTimer = nil
        }

        //: if self.retryAfterTimer != nil {
        if self.retryAfterTimer != nil {
            //: self.retryAfterTimer?.invalidate()
            self.retryAfterTimer?.invalidate()
            //: self.retryAfterTimer = nil
            self.retryAfterTimer = nil
        }

        //: TalkingSocketManager.shared.videoMatchDelegate = nil
        PublishDataSocketDelegate.shared.videoMatchDelegate = nil
        //: TalkingSocketManager.shared.errorDelegate = nil
        PublishDataSocketDelegate.shared.errorDelegate = nil
    }

    /// 开始视频速配
    //: private func startVideoMatch() {
    private func checkBeauty() {
        //: TalkingSocketManager.shared.videoMatchDelegate = self
        PublishDataSocketDelegate.shared.videoMatchDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        PublishDataSocketDelegate.shared.errorDelegate = self
        //: req_videoMatchToMatch()
        writeCopy()
    }

    /// 返回按钮
    //: @objc private func clickBackButtonAction() {
    @objc private func isoclinicAction() {
        //: self.navigationController?.popViewController(animated: true)
        self.navigationController?.popViewController(animated: true)
    }

    /// 打开视频速配说明
    //: @objc private func matchTipsButtonClick() {
    @objc private func tips() {
        //: let message = "Random Video helps locating your matches online quickly. Random Videos are priced at 40 gold coins per minute and are paid for by the party calling the video.".localized
        let message = String(bytes: notiToUrl.reversed(), encoding: .utf8)!.localized
        //: TalkingAlertShow.alert(title: nil, message: message, rightBtnTitle: "OK".localized)
        CourseAlertShow.blockConfig(title: nil, message: message, rightBtnTitle: "OK".localized)
    }
}

// MARK: - TalkingSocketManagerDelegate

//: extension TalkingQuickVideoMatchViewController: SocketManagerVideoMatchDelegate {
extension SearchConnectionErrorDelegate: RansackingObjectProtocol {
    /// 视频速配成功
    //: func socket_match_onVideoMatch(data: [String: Any]) {
    func socket(data: [String: Any]) {
        // 停止速配，跳转速配结果界面
        //: stopVideoMatch()
        viewConsort()
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
            //: guard let topVC = self.navigationController?.topViewController else { return }
            guard let topVC = self.navigationController?.topViewController else { return }
            //: guard !topVC.isKind(of: TalkingMatchResultViewController.self) else { return }
            guard !topVC.isKind(of: KeyRecognizerDelegate.self) else { return }
            //: guard let model = TalkingMatchResultModel.deserialize(from: data) else { return }
            guard let model = FastenerTransformable.deserialize(from: data) else { return }

            // v1.0.6 订阅用户 当前视频速配成功的次数大于等于2，则在其匹配中页面额外展示回放按钮
            //: if PathAppManager.share.loginUserMode.loungePlus == true || PathAppManager.share.loginUserMode.videoPlayback == true {
            if PathAppManager.share.loginUserMode.loungePlus == true || PathAppManager.share.loginUserMode.videoPlayback == true {
                //: if self.resultDataArr.count >= 2 {
                if self.resultDataArr.count >= 2 {
                    //: self.resultDataArr.removeFirst()
                    self.resultDataArr.removeFirst()
                }
                //: self.resultDataArr.append(model)
                self.resultDataArr.append(model)

                //: } else {
            } else { // 非订阅用户只展示一条
                //: self.resultDataArr.removeAll()
                self.resultDataArr.removeAll()
                //: self.resultDataArr.append(model)
                self.resultDataArr.append(model)
            }

            //: let vc = TalkingMatchResultViewController()
            let vc = KeyRecognizerDelegate()
            //: vc.showLastArr = self.resultDataArr
            vc.showLastArr = self.resultDataArr
            //: self.navigationController?.pushViewController(vc, animated: true)
            self.navigationController?.pushViewController(vc, animated: true)
        }
    }

    //: func socket_match_onRequestCall(data: [String: Any]) {}
    func dataOf(data _: [String: Any]) {}
}

// MARK: - DelegateObjectProtocol

//: extension TalkingQuickVideoMatchViewController: TalkingSocketManagerErrorDelegate {
extension SearchConnectionErrorDelegate: DelegateObjectProtocol {
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
    func reverberation(errorNo: Int, cmd: String, msg _: String, data: [String: Any]) {
        //: if cmd == "onVideoMatch" && errorNo == 449 { // 匹配池用完，延时重试
        if cmd == (String(noti_sectionTitle.suffix(6)) + "oMatch") && errorNo == 449 { // 匹配池用完，延时重试
            //: let json = JSON(data)
            let json = JSON(data)
            //: retryAfterTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(json["retryAfter"].intValue), block: { [weak self] timer in
            retryAfterTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(json[(String(showAtId) + noti_labText.replacingOccurrences(of: "manager", with: "r"))].intValue), block: { [weak self] _ in
                //: guard let self = self else { return }
                guard let self = self else { return }

                //: self.matchId = json["matchId"].intValue
                self.matchId = json[(String(main_maxId.prefix(7)))].intValue
                //: self.startVideoMatch()
                self.checkBeauty()
                //: }, repeats: false)
            }, repeats: false)
        }
    }
}

// MARK: - Layout

//: extension TalkingQuickVideoMatchViewController {
extension SearchConnectionErrorDelegate {
    /// 添加视图
    //: private func setupSubviews() {
    private func setupImageSubviews() {
        //: self.view.addSubview(bgImgView)
        self.view.addSubview(bgImgView)
        //: self.view.addSubview(navView)
        self.view.addSubview(navView)
        //: self.view.addSubview(svgaPlayer)
        self.view.addSubview(svgaPlayer)
        //: self.view.addSubview(tipsLab)
        self.view.addSubview(tipsLab)
        //: self.view.addSubview(matchingLab)
        self.view.addSubview(matchingLab)
        //: self.view.addSubview(headImgView)
        self.view.addSubview(headImgView)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func quantityerruptByConstraint() {
        //: bgImgView.snp.makeConstraints { make in
        bgImgView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: navView.snp.makeConstraints { make in
        navView.snp.makeConstraints { make in
            //: make.leading.top.width.equalToSuperview()
            make.leading.top.width.equalToSuperview()
            //: make.height.equalTo(StatusBarNavigationBarHeight)
            make.height.equalTo(dream_systemTitle)
        }
        //: svgaPlayer.snp.makeConstraints { make in
        svgaPlayer.snp.makeConstraints { make in
            //: make.top.equalTo(navView.snp.bottom).offset(actualWidth(w: 50))
            make.top.equalTo(navView.snp.bottom).offset(actualWidth(w: 50))
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.size.equalTo(CGSize(width: ScreenWidth, height: ScreenWidth))
            make.size.equalTo(CGSize(width: app_screenFailMarginFormat, height: app_screenFailMarginFormat))
        }
        //: tipsLab.snp.makeConstraints { make in
        tipsLab.snp.makeConstraints { make in
            //: make.leading.equalTo(30)
            make.leading.equalTo(30)
            //: make.trailing.equalTo(-30)
            make.trailing.equalTo(-30)
            //: make.bottom.equalToSuperview().offset(-88-kDeviceSafeBottomHeight)
            make.bottom.equalToSuperview().offset(-88 - dreamOkTitle)
        }
        //: matchingLab.snp.makeConstraints { make in
        matchingLab.snp.makeConstraints { make in
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.bottom.equalTo(tipsLab.snp.top).offset(-20)
            make.bottom.equalTo(tipsLab.snp.top).offset(-20)
        }
        //: headImgView.snp.makeConstraints { make in
        headImgView.snp.makeConstraints { make in
            //: make.center.equalTo(svgaPlayer)
            make.center.equalTo(svgaPlayer)
            //: make.size.equalTo(CGSize(width: actualWidth(w: 90), height: actualWidth(w: 90)))
            make.size.equalTo(CGSize(width: actualWidth(w: 90), height: actualWidth(w: 90)))
        }
    }
}
