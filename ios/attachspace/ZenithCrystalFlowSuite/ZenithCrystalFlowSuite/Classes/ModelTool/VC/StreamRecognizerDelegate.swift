
//: Declare String Begin

/*: "cmd" :*/
fileprivate let mTurnIdent:[UInt8] = [0xb7,0xc1,0xb8]

fileprivate func byData(send num: UInt8) -> UInt8 {
    let value = Int(num) + 172
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "cancelCall" :*/
fileprivate let dreamFirstMsg:String = "viewanview"

/*: "data" :*/
fileprivate let dream_modeStatusId:[UInt8] = [0x4c,0x49,0x5c,0x49]

private func nurseLog(with num: UInt8) -> UInt8 {
    return num ^ 40
}

/*: "logId" :*/
fileprivate let mainDreamName:String = "logIdto tool reply"

/*: "#322870" :*/
fileprivate let main_labelValue:[Character] = ["#","3","2","2","8"]
fileprivate let main_makeMsg:String = "70"

/*: "Tap to start video chatting" :*/
fileprivate let userTitleId:[Character] = ["T","a","p"," ","t","o"," "]
fileprivate let noti_aspectUrl:String = "view pull current observerstart "
fileprivate let main_appMessage:String = "image to not by chat"

/*: "icon_close_nor" :*/
fileprivate let kPhotoStr:String = "lock coloricon_c"

/*: "icon_views_number" :*/
fileprivate let m_pushPathData:String = "edit selficon_v"
fileprivate let main_textMsg:String = "UMBER"

/*: "%@ chatting" :*/
fileprivate let k_bindMessage:String = "%@ chelse page guard text return"
fileprivate let notiArrayFormat:String = "attiuser"

/*: "icon_lounge" :*/
fileprivate let main_wrapSendFormat:String = "icon_lto nose"
fileprivate let user_toId:String = "otablege"

/*: "icon_rank_coin" :*/
fileprivate let dream_sexCompareIdent:String = "icon_rarange and file"
fileprivate let show_acceptData:String = "nk_coinface make video up"

/*: "vip_btn_top" :*/
fileprivate let appTouchValue:[Character] = ["v","i","p","_","b","t","n"]
fileprivate let noti_sendKey:String = "make label let signal self_top"

/*: "btn_nearby_settings_nor" :*/
fileprivate let kRegularNameMsg:String = "btn_neright else margin icon"
fileprivate let user_videoTitle:[Character] = ["a","r","b"]
fileprivate let mGiftValue:[Character] = ["y","_","s","e","t","t","i","n","g","s","_","n","o","r"]

/*: "%@ Coins/min" :*/
fileprivate let userTrackIdent:[Character] = ["%","@"," ","C","o","i","n","s","/"]
fileprivate let notiStatusId:[Character] = ["m","i","n"]

/*: "get json error" :*/
fileprivate let k_endButtonUrl:String = "get jtable container with block end"
fileprivate let k_afterStr:[Character] = ["s","o","n"]
fileprivate let kFilterStr:[Character] = [" ","e","r","r","o","r"]

/*: "matchId" :*/
fileprivate let mainSharedId:String = "MATCH"
fileprivate let m_interactionEqualStr:String = "Idview array if list info"

/*: "errno" :*/
fileprivate let main_particlePath:[UInt8] = [0x6f,0x6e,0x72,0x72,0x65]

/*: "VIPFee" :*/
fileprivate let dream_appMsg:String = "VIPFeeload make true return let"

/*: "Matching now" :*/
fileprivate let showTextKey:String = "Matchshow normal view size"
fileprivate let k_labelViewIdent:String = "ing nowin ok bring var"

/*: "userTouch" :*/
fileprivate let dream_atContent:String = "userTouchawake text equal live"

/*: "mf/freeCall/getVideoInfo" :*/
fileprivate let user_nextMessage:String = "list timemf/fr"
fileprivate let mMakeIdent:String = "/getVfor center"
fileprivate let dream_pointFormat:String = "ideoInfolive view make"

/*: "fromType" :*/
fileprivate let userLayerMessage:[UInt8] = [0xbb,0xaf,0xb2,0xb0,0x89,0xa4,0xad,0xb8]

private func sightCurrent(hide num: UInt8) -> UInt8 {
    return num ^ 221
}

/*: "You've been barred from receiving calls" :*/
fileprivate let m_addColorId:[UInt8] = [0xa,0x3c,0x26,0x74,0x25,0x36,0x73,0x31,0x36,0x36,0x3d,0x73,0x31,0x32,0x21,0x21,0x36,0x37,0x73,0x35,0x21,0x3c,0x3e,0x73,0x21,0x36,0x30,0x36,0x3a,0x25,0x3a,0x3d,0x34,0x73,0x30,0x32,0x3f,0x3f,0x20]

private func appMake(path num: UInt8) -> UInt8 {
    return num ^ 83
}

/*: "source" :*/
fileprivate let dream_thePriceContent:[UInt8] = [0xe5,0xf9,0xe3,0xe4,0xf5,0xf3]

private func liveEmpty(down num: UInt8) -> UInt8 {
    return num ^ 150
}

/*: "type" :*/
fileprivate let dream_tipStr:[UInt8] = [0x5b,0x56,0x5f,0x4a]

private func demonstrateInfo(gesture num: UInt8) -> UInt8 {
    return num ^ 47
}

/*: "uid" :*/
fileprivate let user_borderContent:[UInt8] = [0x65,0x59,0x54]

fileprivate func ofBullet(cell num: UInt8) -> UInt8 {
    let value = Int(num) - 240
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "fromFreeCall" :*/
fileprivate let main_appearText:String = "FROM"
fileprivate let userBlockData:String = "else since tapCall"

/*: "requestCall" :*/
fileprivate let main_dataPath:String = "rvaluequvalues"
fileprivate let main_dateData:String = "turn view corner true classtCall"

/*: "onVideoMatch" :*/
fileprivate let kVideoFormat:String = "true attach retonVid"

/*: "[Automatic video] Receive video call incoming" :*/
fileprivate let kGroupIdent:[UInt8] = [0xda,0xc0,0xf4,0xf5,0xee,0xec,0xe0,0xf5,0xe8,0xe2,0xa1,0xf7,0xe8,0xe5,0xe4,0xee,0xdc,0xa1,0xd3,0xe4,0xe2,0xe4,0xe8,0xf7,0xe4,0xa1,0xf7,0xe8,0xe5,0xe4,0xee,0xa1,0xe2,0xe0,0xed,0xed,0xa1,0xe8,0xef,0xe2,0xee,0xec,0xe8,0xef,0xe6]

private func backFirst(from num: UInt8) -> UInt8 {
    return num ^ 129
}

/*: "refuseCall" :*/
fileprivate let showInputVideoMsg:[Character] = ["r","e","f","u","s","e","C","a","l","l"]

/*: "onCancelCall" :*/
fileprivate let mainLabMsg:String = "style typeonCan"
fileprivate let mWrapEmptyMessage:[Character] = ["l","l"]

/*: "retryAfter" :*/
fileprivate let kLivePricePath:[Character] = ["r","e","t","r","y"]
fileprivate let notiStyleData:String = "Afterremove view action succeed"

/*: "onRequestCall" :*/
fileprivate let show_colorId:[Character] = ["o","n","R","e","q","u","e","s","t","C","a","l","l"]

/*: "failType" :*/
fileprivate let dreamProductMsg:String = "action make path selffailType"

/*: "callWaitingTime" :*/
fileprivate let showBottomName:String = "caphoto"
fileprivate let m_fatalStr:String = "player viewingTime"

/*: "toUid" :*/
fileprivate let main_builderTitle:String = "cell camera view ontoUid"

/*: "Free 1 min" :*/
fileprivate let mGiftFormat:String = "the contentFree "

/*: "%@/min" :*/
fileprivate let noti_groupData:[Character] = ["%","@","/","m","i","n"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  StreamRecognizerDelegate.swift
//  AbroadTalking
//
//  Created by young on 2023/8/22.
//

//: import UIKit
import UIKit

//: class TalkingRandomVideoViewController: TalkingBaseViewController {
class StreamRecognizerDelegate: ListRecognizerDelegate {
	var playerEnable: Bool = true
	var fileInterval: Double = -56.6
	var awakeArray: [AnyHashable] = []

    //: var fee = 0
    var fee = 0 // 原价
    //: var vipFee = 0
    var vipFee = 0 // VIP价格
    //: var chattingNum = 0
    var chattingNum = 0 // 当前匹配人数
    //: private var matchId: Int = 0
    private var matchId: Int = 0 // 速配批次Id
    //: private var waitingTimer: Timer?
    private var waitingTimer: Timer? // 呼叫等待定时（等待15s）
    //: private var retryAfterTimer: Timer?
    private var retryAfterTimer: Timer? // 匹配失败重试定时器
    //: private var logId = 0
    private var logId = 0 // 通话id
    //: private var isShowFreeVideo = false
    private var isShowFreeVideo = false // 是否已展示免费视频挽留弹窗
    //: var beginTime: TimeInterval?
    var beginTime: TimeInterval?
    //: private var dispatchWorkItem: DispatchWorkItem?
    private var dispatchWorkItem: DispatchWorkItem?
    //: private var closeBtnWorkItem: DispatchWorkItem?
    private var closeBtnWorkItem: DispatchWorkItem?
    //: var isFirstRandow = true
    var isFirstRandow = true
    //: var isBeginRand = false
    var isBeginRand = false

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1.0) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1.0) {
            //: if !self.isFirstRandow {
            if !self.isFirstRandow {
                //: self.refreshVideoCallPrice()
                self.balance()
            }
        }
        //: self.resetCamera()
        self.imageAcross()
    
		if var iconSumReplyValue = videoManager.infoModel?.pairAge { 
	            if (renderView.layer.contains(CGPoint())) && (!renderView.transform.isIdentity) {
	                //: SWIFT_CUSTOM_DANGER_File_Call
	                let safetyMargin = TableWithView(frame: renderView.frame.intersection(CGRect(x: CGFloat(70), y: CGFloat(0), width: CGFloat(0), height: CGFloat(73))))
	            safetyMargin.livelihoodCount = iconSumReplyValue

	            
	            
	            safetyMargin.saddleOff = { [self] rangeAboutClose in
	            self.playerEnable = rangeAboutClose
	            
	            var safetyMargin = isFirstRandow
	            safetyMargin = false
	            if safetyMargin != self.playerEnable {
	                self.playerEnable = safetyMargin
	            }
	            
	                self.playerEnable = false
	                self.playerEnable = !self.playerEnable
	            return self.playerEnable
	            }
	            safetyMargin.titleCount = { [self] counterchangeMagnitude in
	            self.fileInterval = counterchangeMagnitude
	            
	            return self.fileInterval
	            }
	            safetyMargin.getCrackingArray = { [self] visualArray in
	            self.awakeArray = visualArray
	            
	            guard var value = self.awakeArray as? [String] else {
	                return nil
	            }
	            return value
	            }
	                renderView.addSubview(safetyMargin)
	            }
	
		}
	}

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true

        //: setupSubviews()
        subviewsEnable()
        //: setupSubViewsConstraint()
        switchgoing()
        //: bindInteraction()
        appTarget()
        //: videoManager.startPreview(isMatch: true)
        videoManager.collectionMin(isMatch: true)
        //: setPlayer(isRandon: false)
        errorCoffinGolferBlock(isRandon: false)
        //: if isBeginRand {
        if isBeginRand {
            //: start_randomVideo()
            miscalculationVideo()
        }
    }

    //: deinit {
    deinit {
        //: if waitingTimer != nil {
        if waitingTimer != nil {
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": logId]])
            PublishDataSocketDelegate.shared.birthday(info: [String(bytes: mTurnIdent.map{byData(send: $0)}, encoding: .utf8)!: (dreamFirstMsg.replacingOccurrences(of: "view", with: "c") + "elCall"), String(bytes: dream_modeStatusId.map{nurseLog(with: $0)}, encoding: .utf8)!: [(String(mainDreamName.prefix(5))): logId]])
        }
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
        //: destroyWaitingTimer()
        imageLab()
        //: if TalkingSocketManager.shared.isData == false {
        if PublishDataSocketDelegate.shared.isData == false {
            //: TalkingSocketManager.shared.videoMatchDelegate = nil
            PublishDataSocketDelegate.shared.videoMatchDelegate = nil
            //: TalkingSocketManager.shared.callerDelegate = nil
            PublishDataSocketDelegate.shared.callerDelegate = nil
            //: TalkingSocketManager.shared.calledPartyDelegate = nil
            PublishDataSocketDelegate.shared.calledPartyDelegate = nil
            //: TalkingSocketManager.shared.errorDelegate = nil
            PublishDataSocketDelegate.shared.errorDelegate = nil
        }
    }

    // MARK: - Lazy Load

    //: private lazy var videoManager: TalkingVideoChatManager = {
    private lazy var videoManager: LogChatManager = {
        //: let m = TalkingVideoChatManager()
        let m = LogChatManager()
        //: let info = TalkingVideoChatModel()
        let info = WithChatModel()
        //: info.videoStage = .Calling
        info.videoStage = .Calling
        //: info.renderPostion = .selfOnBg
        info.renderPostion = .selfOnBg
        //: m.infoModel = info
        m.infoModel = info
        // 视图传递给manager
        //: m.bgVideoView = self.renderView
        m.bgVideoView = self.renderView
        //: return m
        return m
        //: }()
    }()

    // 视频画面渲染视图
    //: private lazy var renderView: UIView = {
    private lazy var renderView: UIView = {
        //: let v = UIView()
        let v = UIView()
        //: v.backgroundColor = .appTitleColor()
        v.backgroundColor = .statusBy()
        //: return v
        return v
        //: }()
    }()

    //: private lazy var gradientBtn: UIButton = {
    private lazy var gradientBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.addTarget(self, action: #selector(start_randomVideo), for: .touchUpInside)
        btn.addTarget(self, action: #selector(miscalculationVideo), for: .touchUpInside)
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        // 渐变
        //: let layer = CAGradientLayer()
        let layer = CAGradientLayer()
        //: layer.frame = CGRect(x: 0, y: 0, width: ScreenWidth+1, height: ScreenHeight+1)
        layer.frame = CGRect(x: 0, y: 0, width: app_screenFailMarginFormat + 1, height: mainAcceptKey + 1)
        //: layer.startPoint = CGPoint(x: 0.5, y: 0)
        layer.startPoint = CGPoint(x: 0.5, y: 0)
        //: layer.endPoint = CGPoint(x: 0.5, y: 1)
        layer.endPoint = CGPoint(x: 0.5, y: 1)
        //: layer.colors = [UIColor(white: 0, alpha: 0.15).cgColor, UIColor(hex: "#322870")!.cgColor]
        layer.colors = [UIColor(white: 0, alpha: 0.15).cgColor, UIColor(hex: (String(main_labelValue) + main_makeMsg.capitalized))!.cgColor]
        //: layer.locations = [0, 1]
        layer.locations = [0, 1]
        //: btn.layer.addSublayer(layer)
        btn.layer.addSublayer(layer)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var svgaPlayer: SVGAPlayer = {
    private lazy var svgaPlayer: SVGAPlayer = {
        //: let player = SVGAPlayer.init()
        let player = SVGAPlayer()
        //: player.loops = 0
        player.loops = 0
        //: player.clearsAfterStop = false
        player.clearsAfterStop = false
        //: player.isUserInteractionEnabled = false
        player.isUserInteractionEnabled = false
        //: player.contentMode = .scaleAspectFill
        player.contentMode = .scaleAspectFill

        //: return player
        return player
        //: }()
    }()

    //: private lazy var tipsLab: UILabel = {
    private lazy var tipsLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .white
        lab.textColor = .white
        //: lab.font = UIFont.boldSystemFont(ofSize: 20)
        lab.font = UIFont.boldSystemFont(ofSize: 20)
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: lab.text = "Tap to start video chatting".localized
        lab.text = (String(userTitleId) + String(noti_aspectUrl.suffix(6)) + "video" + String(main_appMessage.suffix(5)) + "ting").localized
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var closeBtn: UIButton = {
    private lazy var closeBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_close_nor"), for: .normal)
        btn.setImage(UIImage.sendCollection(name: (String(kPhotoStr.suffix(6)) + "lose_nor")), for: .normal)
        //: btn.addTarget(self, action: #selector(closeButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(dropClick), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var chattingNumBtn: TalkingButton = {
    private lazy var chattingNumBtn: TelevisionButton = {
        //: let btn = TalkingButton()
        let btn = TelevisionButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_views_number"), for: .normal)
        btn.setImage(UIImage.sendCollection(name: (String(m_pushPathData.suffix(6)) + "iews_n" + main_textMsg.lowercased())), for: .normal)
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_views_number"), for: .disabled)
        btn.setImage(UIImage.sendCollection(name: (String(m_pushPathData.suffix(6)) + "iews_n" + main_textMsg.lowercased())), for: .disabled)
        //: btn.spaceBetweenTitleAndImage = 5
        btn.spaceBetweenTitleAndImage = 5
        //: btn.isEnabled = false
        btn.isEnabled = false
        //: btn.setTitleColor(UIColor.white, for: .normal)
        btn.setTitleColor(UIColor.white, for: .normal)
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Regular, fontSize: 15)
        btn.titleLabel?.font = UIFont.bindFor(type: .Regular, fontSize: 15)
        //: btn.setTitle("%@ chatting".localizedArguments(chattingNum), for: .normal)
        btn.setTitle((String(k_bindMessage.prefix(5)) + notiArrayFormat.replacingOccurrences(of: "user", with: "ng")).equalArguments(chattingNum), for: .normal)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var priceStackView: UIStackView = {
    private lazy var priceStackView: UIStackView = {
        //: let v = UIStackView()
        let v = UIStackView()
        //: v.axis = .horizontal
        v.axis = .horizontal
        //: v.alignment = .center
        v.alignment = .center
        //: v.spacing = 4
        v.spacing = 4
        //: v.isUserInteractionEnabled = false
        v.isUserInteractionEnabled = false
        //: return v
        return v
        //: }()
    }()

    //: private lazy var vipIcon: UIImageView = {
    private lazy var vipIcon: UIImageView = {
        //: let img = UIImageView()
        let img = UIImageView()
        //: img.image = UIImage.BundleImageNamed(name: "icon_lounge")
        img.image = UIImage.sendCollection(name: (String(main_wrapSendFormat.prefix(6)) + user_toId.replacingOccurrences(of: "table", with: "un")))
        //: img.contentMode = .scaleAspectFill
        img.contentMode = .scaleAspectFill
        //: return img
        return img
        //: }()
    }()

    //: private lazy var goldCoinIcon: UIImageView = {
    private lazy var goldCoinIcon: UIImageView = {
        //: let img = UIImageView()
        let img = UIImageView()
        //: img.image = UIImage.BundleImageNamed(name: "icon_rank_coin")
        img.image = UIImage.sendCollection(name: (String(dream_sexCompareIdent.prefix(7)) + String(show_acceptData.prefix(7))))
        //: return img
        return img
        //: }()
    }()

    //: private lazy var priceLab: UILabel = {
    private lazy var priceLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .white
        lab.textColor = .white
        //: lab.font = UIFont.pingfangFont(type: .Regular, fontSize: 15)
        lab.font = UIFont.bindFor(type: .Regular, fontSize: 15)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var vipTopImgView: UIImageView = {
    private lazy var vipTopImgView: UIImageView = {
        //: let imgV = UIImageView()
        let imgV = UIImageView()
        //: imgV.isHidden = true
        imgV.isHidden = true
        //: imgV.image  = UIImage.BundleImageNamed(name: "vip_btn_top")
        imgV.image = UIImage.sendCollection(name: (String(appTouchValue) + String(noti_sendKey.suffix(4))))
        //: return imgV
        return imgV
        //: }()
    }()

    // 会员折扣价
    //: private lazy var vipPriceBtn: TalkingButton = {
    private lazy var vipPriceBtn: TelevisionButton = {
        //: let btn = TalkingButton()
        let btn = TelevisionButton()
        //: btn.backgroundColor = UIColor.white.withAlphaComponent(0.1)
        btn.backgroundColor = UIColor.white.withAlphaComponent(0.1)
        //: btn.layer.cornerRadius = actualWidth(w: 27)
        btn.layer.cornerRadius = actualWidth(w: 27)
        //: btn.clipsToBounds = true
        btn.clipsToBounds = true
        //: btn.addTarget(self, action: #selector(maleVipPriceButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(doingGoingClick), for: .touchUpInside)
        //: btn.isHidden = true
        btn.isHidden = true
        //: btn.setBackgroundImage(UIImage.BundleImageNamed(name: "btn_nearby_settings_nor"), for: .normal)
        btn.setBackgroundImage(UIImage.sendCollection(name: (String(kRegularNameMsg.prefix(6)) + String(user_videoTitle) + String(mGiftValue))), for: .normal)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var vipPriceImg: UIImageView = {
    private lazy var vipPriceImg: UIImageView = {
        //: let imgV = UIImageView()
        let imgV = UIImageView()
        //: imgV.image  = UIImage.BundleImageNamed(name: "icon_rank_coin")
        imgV.image = UIImage.sendCollection(name: (String(dream_sexCompareIdent.prefix(7)) + String(show_acceptData.prefix(7))))
        //: return imgV
        return imgV
        //: }()
    }()

    //: private lazy var vipPriceLab: UILabel = {
    private lazy var vipPriceLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .userVipColor()
        lab.textColor = .needEnable()
        //: lab.text = "%@ Coins/min".localizedArguments(vipFee)
        lab.text = (String(userTrackIdent) + String(notiStatusId)).equalArguments(vipFee)
        //: lab.font = UIFont.pingfangFont(type: .Medium, fontSize: 15)
        lab.font = UIFont.bindFor(type: .Medium, fontSize: 15)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var norPriceLab: UILabel = {
    private lazy var norPriceLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: return lab
        return lab
        //: }()
    }()

    // 免费视频挽留弹窗
    //: private lazy var freeVideoAlert: TalkingLiveAlertView = {
    private lazy var freeVideoAlert: SmartView = //: return SmartView(frame: .zero, type: .freeVideo)
        .init(frame: .zero, type: .freeVideo)
    //: }()
}

// MARK: - Request & 数据处理

//: extension TalkingRandomVideoViewController {
extension StreamRecognizerDelegate {
    //: private func setPlayer(isRandon: Bool) {
    private func errorCoffinGolferBlock(isRandon: Bool) {
        //: let url = SVGAEffectTool.default.getZipEffectPath(type: isRandon ? .Random_video_bg : .Random_video_bg_nor )
        let url = ReverberationInsideHoeHandleReactiveCompatible.default.pullInConfirmType(type: isRandon ? .Random_video_bg : .Random_video_bg_nor)
        //: do {
        do {
            //: let data = try Data(contentsOf: url)
            let data = try Data(contentsOf: url)
            //: let parser = SVGAParser.init()
            let parser = SVGAParser()
            //: parser.parse(with: data, cacheKey: "") { [weak self] videoItem in
            parser.parse(with: data, cacheKey: "") { [weak self] videoItem in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: svgaPlayer.videoItem = videoItem
                svgaPlayer.videoItem = videoItem
                //: svgaPlayer.startAnimation()
                svgaPlayer.startAnimation()
            }
            //: } catch {
        } catch {
            //: printLog(message: "get json error")
            printLog(message: (String(k_endButtonUrl.prefix(5)) + String(k_afterStr) + String(kFilterStr)))
        }
    }

    /// 开始随机视频（男）
    /// - Parameters:
    ///   - cmd: 事件名
    ///   - errno: cmd的错误码
    //: private func req_randomVideoToMatch(cmd: String, errno: String) {
    private func toImage(cmd: String, errno: String) {
        //: beginTime = Date().timeIntervalSince1970
        beginTime = Date().timeIntervalSince1970
        //: let params = ["matchId": self.matchId,
        let params = [(mainSharedId.lowercased() + String(m_interactionEqualStr.prefix(2))): self.matchId,
                      //: "cmd": cmd,
                      String(bytes: mTurnIdent.map{byData(send: $0)}, encoding: .utf8)!: cmd,
                      //: "errno": errno] as [String: Any]
                      String(bytes: main_particlePath.reversed(), encoding: .utf8)!: errno] as [String: Any]
        //: TalkingQuickVideoManager.req_randomVideoToMatch(params: params) { succeed, result, errorModel in
        WindowReactiveCompatible.catScan(params: params) { succeed, result, _ in
            //: guard succeed == true else {
            guard succeed == true else {
                //: self.stop_randomVideo()
                self.haphazardWith()
                //: return
                return
            }
            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())
            //: self.matchId = json["matchId"].intValue
            self.matchId = json[(mainSharedId.lowercased() + String(m_interactionEqualStr.prefix(2)))].intValue
        }
    }

    /// 更新VIP价格
    //: @objc private func req_updateVIPFee() {
    @objc private func imageFee() {
        //: TalkingQuickVideoManager.req_videoMatchCheck() { succeed, result, errorModel in
        WindowReactiveCompatible.colorItem { succeed, result, _ in
            //: guard succeed else { return }
            guard succeed else { return }
            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())
            //: self.vipFee = json["VIPFee"].intValue
            self.vipFee = json[(String(dream_appMsg.prefix(6)))].intValue
            //: self.refreshVideoCallPrice()
            self.balance()
        }
    }
}

// MARK: - Event

//: extension TalkingRandomVideoViewController {
extension StreamRecognizerDelegate {
    /// 男性侧vip价格按钮点击事件
    //: @objc private func maleVipPriceButtonClick() {
    @objc private func doingGoingClick() {
        //: NamePushManager.share.func__pushToSubscribeAlert()
        NamePushManager.share.permissionParams()
    }

    /// 关闭按钮点击事件
    //: @objc func closeButtonClick() {
    @objc func dropClick() {
        //: dispatchWorkItem?.cancel()
        dispatchWorkItem?.cancel()
        //: showCloseBtn()
        informationBtn()
        //: uploadRecord.uploadRecordEvent(eventID: ClickRandomvideoCancelNoP)
        kResolutionText.atJson(eventID: dreamFormalKey)

        //: if PathAppManager.share.appConfigMode.displayMatchV4PopUp, PathAppManager.share.loginUserMode.freeCallTimes > 0,
        if PathAppManager.share.appConfigMode.displayMatchV4PopUp, PathAppManager.share.loginUserMode.freeCallTimes > 0,
           //: isShowFreeVideo == false {
           isShowFreeVideo == false
        {
            //: self.freeVideoAlert.show()
            self.freeVideoAlert.tapTo()
            //: isShowFreeVideo = true
            isShowFreeVideo = true
            //: return
            return
        }
        //: currentViewController()?.navigationController?.popViewController(animated: true)
        withTo()?.navigationController?.popViewController(animated: true)
    }

    /// 开始随机视频
    //: @objc private func start_randomVideo() {
    @objc private func miscalculationVideo() {
        //: guard TalkingSocketManager.shared.isData == false else {
        guard PublishDataSocketDelegate.shared.isData == false else {
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.colorGift(showMsg: app_successShowMsg)
            //: return
            return
        }
        //: guard gradientBtn.isEnabled == true else { return }
        guard gradientBtn.isEnabled == true else { return }
        //: afterShowClose()
        scalelikeDoing()
        //: self.matchId = 0
        self.matchId = 0 // 重置匹配池
        //: gradientBtn.isEnabled = false
        gradientBtn.isEnabled = false
        //: tipsLab.text = "Matching now".localized
        tipsLab.text = (String(showTextKey.prefix(5)) + String(k_labelViewIdent.prefix(7))).localized
        //: setPlayer(isRandon: true)
        errorCoffinGolferBlock(isRandon: true)
        //: priceStackView.isHidden = true
        priceStackView.isHidden = true
        //: vipPriceBtn.isHidden = true
        vipPriceBtn.isHidden = true
        //: vipTopImgView.isHidden = true
        vipTopImgView.isHidden = true
        //: if PathAppManager.share.loginUserMode.randomVideoType == 2 {
        if PathAppManager.share.loginUserMode.randomVideoType == 2 {
            //: uploadRecord.uploadRecordEvent(eventID: ClickRobotRandomButton)
            kResolutionText.atJson(eventID: userPathKey)
            //: req_getVideoInfo()
            bring()
            //: } else {
        } else {
            //: uploadRecord.uploadRecordEvent(eventID: ClickRandomvideoCallNoP)
            kResolutionText.atJson(eventID: m_infoMessage)

            //: TalkingSocketManager.shared.videoMatchDelegate = self
            PublishDataSocketDelegate.shared.videoMatchDelegate = self
            //: TalkingSocketManager.shared.callerDelegate = self
            PublishDataSocketDelegate.shared.callerDelegate = self
            //: TalkingSocketManager.shared.calledPartyDelegate = self
            PublishDataSocketDelegate.shared.calledPartyDelegate = self
            //: TalkingSocketManager.shared.errorDelegate = self
            PublishDataSocketDelegate.shared.errorDelegate = self
            //: restart_randomVideo()
            restart()
        }
    }

    /// 重新开始视频速配
    /// - Parameters:
    ///   - cmd: 事件名（默认用户主动点击）
    ///   - errno: cmd的错误码
    //: private func restart_randomVideo(cmd: String = "userTouch", errno: String = "0") {
    private func restart(cmd: String = (String(dream_atContent.prefix(9))), errno: String = "0") {
        // 停止定时器
        //: destroyWaitingTimer()
        imageLab()
        //: req_randomVideoToMatch(cmd: cmd, errno: errno)
        toImage(cmd: cmd, errno: errno)
    }

    /// 停止随机视频
    //: private func stop_randomVideo() {
    private func haphazardWith() {
        // 停止定时器
        //: destroyWaitingTimer()
        imageLab()
        //: showCloseBtn()
        informationBtn()

        //: gradientBtn.isEnabled = true
        gradientBtn.isEnabled = true
        //: tipsLab.text = "Tap to start video chatting".localized
        tipsLab.text = (String(userTitleId) + String(noti_aspectUrl.suffix(6)) + "video" + String(main_appMessage.suffix(5)) + "ting").localized
        //: setPlayer(isRandon: false)
        errorCoffinGolferBlock(isRandon: false)
        //: self.isFirstRandow = false
        self.isFirstRandow = false
        //: if TalkingSocketManager.shared.isData == false {
        if PublishDataSocketDelegate.shared.isData == false {
            //: TalkingSocketManager.shared.videoMatchDelegate = nil
            PublishDataSocketDelegate.shared.videoMatchDelegate = nil
            //: TalkingSocketManager.shared.callerDelegate = nil
            PublishDataSocketDelegate.shared.callerDelegate = nil
            //: TalkingSocketManager.shared.calledPartyDelegate = nil
            PublishDataSocketDelegate.shared.calledPartyDelegate = nil
            //: TalkingSocketManager.shared.errorDelegate = nil
            PublishDataSocketDelegate.shared.errorDelegate = nil
        }
        //: refreshVideoCallPrice()
        balance()
    }

    /// 销毁定时器
    //: private func destroyWaitingTimer() {
    private func imageLab() {
        //: if self.waitingTimer != nil {
        if self.waitingTimer != nil {
            //: self.waitingTimer?.invalidate()
            self.waitingTimer?.invalidate()
            //: self.waitingTimer = nil
            self.waitingTimer = nil
        }

        //: if self.retryAfterTimer != nil {
        if self.retryAfterTimer != nil {
            //: self.retryAfterTimer?.invalidate()
            self.retryAfterTimer?.invalidate()
            //: self.retryAfterTimer = nil
            self.retryAfterTimer = nil
        }
    }

    /// 获取用户主动拨打视频信息
    //: @objc private func req_getVideoInfo() {
    @objc private func bring() {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = PostulationReactiveCompatible()
        //: reqModel.requestPath = "mf/freeCall/getVideoInfo"
        reqModel.requestPath = (String(user_nextMessage.suffix(5)) + "eeCall" + String(mMakeIdent.prefix(5)) + String(dream_pointFormat.prefix(8)))
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: reqModel.params = ["fromType": "1"]
        reqModel.params = [String(bytes: userLayerMessage.map{sightCurrent(hide: $0)}, encoding: .utf8)!: "1"]
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        show_pointBottomPath.ingathering(model: reqModel) { succeed, result, errorModel in

            //: guard succeed == true else {
            guard succeed == true else {
                //: self.stop_randomVideo()
                self.haphazardWith()
                //: if errorModel?.errorCode ?? 1 > 0 {
                if errorModel?.errorCode ?? 1 > 0 {
                    //: PathAppManager.share.loginUserMode.randomVideoType = 1
                    PathAppManager.share.loginUserMode.randomVideoType = 1
                    //: self.start_randomVideo()
                    self.miscalculationVideo()
                    //: return
                    return
                }
                //: self.func__showStatusBarErrorMsg(showMsg: errorModel?.errorMsg ?? "")
                self.colorGift(showMsg: errorModel?.errorMsg ?? "")
                //: return
                return
            }
            //: guard let result = result as? Dictionary<String, Any> else {
            guard let result = result as? [String: Any] else {
                //: return
                return
            }
            //: if let model = TalkingVideoInitivCallModel.deserialize(from: result as? Dictionary) {
            if let model = ResumeTransformable.deserialize(from: result as? Dictionary) {
                /// 预加载
                //: let  player = TalkingVideoPlayerManager.init()
                let player = TincturePlayListener()
                //: player.setMute(bEnable: true)
                player.viewMute(bEnable: true)
                //: player.playerWithUrlAndVideoView(url: model.winVideoUrl, view: UIView.init())
                player.backstop(url: model.winVideoUrl, view: UIView())

                //: TalkingVideoInitivCallManager.shared.videoCallModel = model
                HighThen.shared.videoCallModel = model

                //: TalkingPermissionTool.checkCameraAndMicrophone { [weak self] isOpen in
                EqualReactiveCompatible.searchForMicrophone { [weak self] isOpen in
                    //: guard let self = self else { return }
                    guard let self = self else { return }
                    //: guard isOpen == true else { return }
                    guard isOpen == true else { return }

                    //: self.putThrough()
                    self.goOut()
                }
            }
        }
    }

    /// 接通电话
    //: private func putThrough() {
    private func goOut() {
        //: uploadRecord.uploadRecordEvent(eventID: clickVideoAcceptButton)
        kResolutionText.atJson(eventID: main_informationStr)
        /// 视频通话被禁用
        //: guard TalkingVideoInitivCallManager.shared.videoCallModel.isBan == 0 else {
        guard HighThen.shared.videoCallModel.isBan == 0 else {
            //: self.func__showStatusBarErrorMsg(showMsg: "You've been barred from receiving calls".localized)
            self.colorGift(showMsg: String(bytes: m_addColorId.map{appMake(path: $0)}, encoding: .utf8)!.localized)
            //: return
            return
        }
        //: self.dispatchWorkItem = DispatchWorkItem {
        self.dispatchWorkItem = DispatchWorkItem {
            //: TalkingVideoInitivCallManager.shared.req_userOperation(type: 1) { succeed, result, errorModel in
            HighThen.shared.showCompletion(type: 1) { succeed, _, _ in
                //: guard succeed == true else {
                guard succeed == true else {
                    //: return
                    return
                }
                //: guard succeed == true else {
                guard succeed == true else {
                    //: self.stop_randomVideo()
                    self.haphazardWith()
                    //: return
                    return
                }
                //: let vc = TalkingVideoInitivCallVC()
                let vc = BuildThen()
                //: vc.isRandomVCPush = true
                vc.isRandomVCPush = true
                //: self.currentViewController()?.navigationController?.pushViewController(vc, animated: true)
                self.withTo()?.navigationController?.pushViewController(vc, animated: true)
                //: self.stop_randomVideo()
                self.haphazardWith()
            }
        }
        // 添加延时任务
        //: DispatchQueue.main.asyncAfter(deadline: .now() + 3.0, execute: self.dispatchWorkItem!)
        DispatchQueue.main.asyncAfter(deadline: .now() + 3.0, execute: self.dispatchWorkItem!)
    }

    /// 匹配时x秒显示关闭按钮
    //: private func afterShowClose() {
    private func scalelikeDoing() {
        //: if PathAppManager.share.appUserConfigMode.matchCancelBtnDelayTime > 0 {
        if PathAppManager.share.appUserConfigMode.matchCancelBtnDelayTime > 0 {
            //: self.closeBtn.isHidden = true
            self.closeBtn.isHidden = true
            //: closeBtnWorkItem = DispatchWorkItem { [weak self] in
            closeBtnWorkItem = DispatchWorkItem { [weak self] in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.closeBtn.isHidden = false
                self.closeBtn.isHidden = false
            }
            //: DispatchQueue.main.asyncAfter(deadline: .now() + Double(PathAppManager.share.appUserConfigMode.matchCancelBtnDelayTime), execute: closeBtnWorkItem!)
            DispatchQueue.main.asyncAfter(deadline: .now() + Double(PathAppManager.share.appUserConfigMode.matchCancelBtnDelayTime), execute: closeBtnWorkItem!)
        }
    }

    //: private func showCloseBtn() {
    private func informationBtn() {
        //: closeBtnWorkItem?.cancel()
        closeBtnWorkItem?.cancel()
        //: self.closeBtn.isHidden = false
        self.closeBtn.isHidden = false
    }

    //: private func resetCamera() {
    private func imageAcross() {
        //: guard TalkingSocketManager.shared.isData == false else {
        guard PublishDataSocketDelegate.shared.isData == false else {
            //: return
            return
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 2.0) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 2.0) {
            //: self.videoManager.infoModel?.videoStage = .Waiting
            self.videoManager.infoModel?.videoStage = .Waiting
            //: self.videoManager.config.openCamera = true
            self.videoManager.config.openCamera = true
            //: self.videoManager.isOpenTRTCPreview = false
            self.videoManager.isOpenTRTCPreview = false
            //: self.videoManager.startPreview()
            self.videoManager.collectionMin()
        }
    }
}

// MARK: - RansackingObjectProtocol

//: extension TalkingRandomVideoViewController: SocketManagerVideoMatchDelegate {
extension StreamRecognizerDelegate: RansackingObjectProtocol {
    /// 视频速配成功
    //: func socket_match_onVideoMatch(data: [String: Any]) {
    func socket(data: [String: Any]) {
        // 拨打视频通话
        //: guard let model = TalkingMatchResultModel.deserialize(from: data) else { return }
        guard let model = FastenerTransformable.deserialize(from: data) else { return }
        //: self.matchId = model.matchId
        self.matchId = model.matchId
        //: var callData: [String: Any] = ["matchId": model.matchId,
        var callData: [String: Any] = [(mainSharedId.lowercased() + String(m_interactionEqualStr.prefix(2))): model.matchId,
                                       //: "source": model.source,
                                       String(bytes: dream_thePriceContent.map{liveEmpty(down: $0)}, encoding: .utf8)!: model.source,
                                       //: "type": model.type,
                                       String(bytes: dream_tipStr.map{demonstrateInfo(gesture: $0)}, encoding: .utf8)!: model.type,
                                       //: "uid": model.uid]
                                       String(bytes: user_borderContent.map{ofBullet(cell: $0)}, encoding: .utf8)!: model.uid]
        //: let index = EnableFreeCallType.randow
        let index = LevelMirrorPath.randow
        //: if PathAppManager.share.loginUserMode.freeCallTimes > 0 && PathAppManager.share.appUserConfigMode.enableFreeCallType & index.rawValue == index.rawValue {
        if PathAppManager.share.loginUserMode.freeCallTimes > 0 && PathAppManager.share.appUserConfigMode.enableFreeCallType & index.rawValue == index.rawValue {
            //: callData.updateValue(1, forKey: "fromFreeCall")
            callData.updateValue(1, forKey: (main_appearText.lowercased() + "Free" + String(userBlockData.suffix(4))))
            //: TalkingSocketManager.shared.isFreeCall = true
            PublishDataSocketDelegate.shared.isFreeCall = true
        }
        //: let info: [String: Any] = ["cmd": "requestCall", "data": callData]
        let info: [String: Any] = [String(bytes: mTurnIdent.map{byData(send: $0)}, encoding: .utf8)!: (main_dataPath.replacingOccurrences(of: "value", with: "e") + String(main_dateData.suffix(5))), String(bytes: dream_modeStatusId.map{nurseLog(with: $0)}, encoding: .utf8)!: callData]
        //: TalkingSocketManager.shared.sendMessage(info: info)
        PublishDataSocketDelegate.shared.birthday(info: info)
        // 15s未接听主动挂断
        //: waitingTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(15), block: { [weak self] timer in
        waitingTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(15), block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.logId]])
            PublishDataSocketDelegate.shared.birthday(info: [String(bytes: mTurnIdent.map{byData(send: $0)}, encoding: .utf8)!: (dreamFirstMsg.replacingOccurrences(of: "view", with: "c") + "elCall"), String(bytes: dream_modeStatusId.map{nurseLog(with: $0)}, encoding: .utf8)!: [(String(mainDreamName.prefix(5))): self.logId]])
            //: self.restart_randomVideo(cmd: "onVideoMatch")
            self.restart(cmd: (String(kVideoFormat.suffix(5)) + "eoMatch"))
            //: }, repeats: false)
        }, repeats: false)
    }

    //: func socket_match_onRequestCall(data: [String: Any]) {
    func dataOf(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: self.logId = json["logId"].intValue
        self.logId = json[(String(mainDreamName.prefix(5)))].intValue
    }
}

// MARK: - TelevisionCallerDelegate【视频拨打方处理】

//: extension TalkingRandomVideoViewController: SocketManagerVideoChatCallerDelegate {
extension StreamRecognizerDelegate: TelevisionCallerDelegate {
    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    //: func socket_videoChat_cancelCall(data: [String: Any]) {}
    func mentionExecute(data _: [String: Any]) {}

    /// 对方结束了通话（通话中）
    //: func socket_videoChat_stopCall(data: [String: Any]) {}
    func inner(data _: [String: Any]) {}

    /// 余额不足提醒
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any]) {}
    func surcharge(msg _: String, data _: [String: Any]) {}

    /// 对方接受了来电，建立视频通话
    //: func socket_videoChat_acceptCall(data: [String: Any]) {
    func occurStatus(data: [String: Any]) {
        // 应用在后台，先提醒用户
        //: if UIApplication.shared.applicationState == .background {
        if UIApplication.shared.applicationState == .background {
            //: AppDelegateHelper.pushLocalNotification(body: "[Automatic video] Receive video call incoming".localized)
            ShareDelegateHelper.makeUrl(body: String(bytes: kGroupIdent.map{backFirst(from: $0)}, encoding: .utf8)!.localized)
        }
        //: let model = TalkingMatchResultModel.deserialize(from: data)
        let model = FastenerTransformable.deserialize(from: data)
        //: if let uid = model?.uid {
        if let uid = model?.uid {
            //: uploadSuccessTime(toUid: String(uid))
            exceptError(toUid: String(uid))
        }
        //: stop_randomVideo()
        haphazardWith()
        // 开启视频通话时先销毁当前预览画面
        //: videoManager.shouldReleaseAllResource()
        videoManager.makeSession()
        //: PathAppManager.share.start1v1TalkCall(info: data, situation: .Calling)
        PathAppManager.share.someTo(info: data, situation: .Calling)
    }
}

// MARK: - ValueLevelObjectProtocol【接收到回执代理】

//: extension TalkingRandomVideoViewController: SocketManagerVideoChatCalledPartyDelegate {
extension StreamRecognizerDelegate: ValueLevelObjectProtocol {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    //: func socket_videoChat_refuseCall(data: [String: Any]) {
    func chat(data _: [String: Any]) {
        //: restart_randomVideo(cmd: "refuseCall")
        restart(cmd: (String(showInputVideoMsg)))
    }

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    //: func socket_videoChat_onAcceptCall(data: [String: Any]) {}
    func viewAppear(data _: [String: Any]) {}
}

// MARK: - DelegateObjectProtocol

//: extension TalkingRandomVideoViewController: TalkingSocketManagerErrorDelegate {
extension StreamRecognizerDelegate: DelegateObjectProtocol {
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
    func reverberation(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
        //: if cmd == "onCancelCall" {  // 取消通话异常
        if cmd == (String(mainLabMsg.suffix(5)) + "celCa" + String(mWrapEmptyMessage)) { // 取消通话异常
            //: stop_randomVideo()
            haphazardWith()
            //: return
            return
        }
        //: if cmd == "onVideoMatch", errorNo == 404 { // 匹配池用完
        if cmd == (String(kVideoFormat.suffix(5)) + "eoMatch"), errorNo == 404 { // 匹配池用完
            //: stop_randomVideo()
            haphazardWith()
            //: let json = JSON(data)
            let json = JSON(data)
            //: self.matchId = json["matchId"].intValue
            self.matchId = json[(mainSharedId.lowercased() + String(m_interactionEqualStr.prefix(2)))].intValue
            //: self.func__showStatusBarErrorMsg(showMsg: msg)
            self.colorGift(showMsg: msg)
            //: uploadFailTime(type: 2)
            fail(type: 2)
            //: return
            return
        }
        //: if cmd == "onVideoMatch", errorNo == 449 { // 匹配池用完，延时重试
        if cmd == (String(kVideoFormat.suffix(5)) + "eoMatch"), errorNo == 449 { // 匹配池用完，延时重试
            //: let json = JSON(data)
            let json = JSON(data)
            //: retryAfterTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(json["retryAfter"].intValue), block: { [weak self] timer in
            retryAfterTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(json[(String(kLivePricePath) + String(notiStyleData.prefix(5)))].intValue), block: { [weak self] _ in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.matchId = json["matchId"].intValue
                self.matchId = json[(mainSharedId.lowercased() + String(m_interactionEqualStr.prefix(2)))].intValue
                //: self.restart_randomVideo(cmd: cmd, errno: String(errorNo))
                self.restart(cmd: cmd, errno: String(errorNo))
                //: }, repeats: false)
            }, repeats: false)
            //: return
            return
        }
        //: if cmd == "onRequestCall", errorNo == VideoChatErrorCode.MoneyLack.rawValue { // 余额不足
        if cmd == (String(show_colorId)), errorNo == InsideSignedNumber.MoneyLack.rawValue { // 余额不足
            //: stop_randomVideo()
            haphazardWith()
            //: guard PathAppManager.share.appStatus == AppSkinStatus.normal.rawValue else { return }
            guard PathAppManager.share.appStatus == AssemblageHashable.normal.rawValue else { return }
            //: NamePushManager.share.func__jumpToWebRecharge(clickEvent: clickRandomvideoButton, sufficient: false)
            NamePushManager.share.rechargeParams(clickEvent: mUseName, sufficient: false)
            //: uploadFailTime(type: 1)
            fail(type: 1)
            //: return
            return
        }

        //: restart_randomVideo(cmd: cmd, errno: String(errorNo))
        restart(cmd: cmd, errno: String(errorNo))
    }
}

// MARK: - 埋点

//: extension TalkingRandomVideoViewController {
extension StreamRecognizerDelegate {
    //: func uploadFailTime(type: Int) {
    func fail(type: Int) {
        //: let nowtime = Date().timeIntervalSince1970
        let nowtime = Date().timeIntervalSince1970
        //: let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        //: var parm = Dictionary<String, Any>()
        var parm = [String: Any]()
        //: parm["failType"] = type
        parm[(String(dreamProductMsg.suffix(8)))] = type
        //: parm["callWaitingTime"] = time
        parm[(showBottomName.replacingOccurrences(of: "photo", with: "ll") + "Wait" + String(m_fatalStr.suffix(7)))] = time
        //: uploadRecord.uploadRecordEvent(eventID: RandomvideoFailwaitTime, parameterStr: parm)
        kResolutionText.toStr(eventID: kRecordNetMsg, parameterStr: parm)
    }

    //: func uploadSuccessTime(toUid: String) {
    func exceptError(toUid: String) {
        //: let nowtime = Date().timeIntervalSince1970
        let nowtime = Date().timeIntervalSince1970
        //: let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        //: var parm = Dictionary<String, Any>()
        var parm = [String: Any]()
        //: parm["callWaitingTime"] = time
        parm[(showBottomName.replacingOccurrences(of: "photo", with: "ll") + "Wait" + String(m_fatalStr.suffix(7)))] = time
        //: parm["toUid"] = toUid
        parm[(String(main_builderTitle.suffix(5)))] = toUid
        //: uploadRecord.uploadRecordEvent(eventID: RandomvideowaitTime, parameterStr: parm)
        kResolutionText.toStr(eventID: mFeatureFormat, parameterStr: parm)
    }
}

// MARK: - Layout

//: extension TalkingRandomVideoViewController {
extension StreamRecognizerDelegate {
    /// 刷新视频通话价格
    //: @objc private func refreshVideoCallPrice() {
    @objc private func balance() {
        //: guard PathAppManager.share.appStatus == AppSkinStatus.normal.rawValue else {
        guard PathAppManager.share.appStatus == AssemblageHashable.normal.rawValue else {
            //: priceStackView.isHidden = true
            priceStackView.isHidden = true
            //: vipPriceBtn.isHidden = true
            vipPriceBtn.isHidden = true
            //: vipTopImgView.isHidden = true
            vipTopImgView.isHidden = true
            //: return
            return
        }

        //: vipIcon.isHidden = !PathAppManager.share.loginUserMode.loungePlus
        vipIcon.isHidden = !PathAppManager.share.loginUserMode.loungePlus
        //: priceStackView.isHidden = false
        priceStackView.isHidden = false
        //: let indexType = EnableFreeCallType.randow
        let indexType = LevelMirrorPath.randow
        //: if PathAppManager.share.loginUserMode.freeCallTimes > 0 && PathAppManager.share.appUserConfigMode.enableFreeCallType & indexType.rawValue == indexType.rawValue {
        if PathAppManager.share.loginUserMode.freeCallTimes > 0, PathAppManager.share.appUserConfigMode.enableFreeCallType & indexType.rawValue == indexType.rawValue { // 有免费次数
            //: if PathAppManager.share.appUserConfigMode.callBtnStyle == 1 {
            if PathAppManager.share.appUserConfigMode.callBtnStyle == 1 {
                //: priceLab.text = "Free 1 min".localized
                priceLab.text = (String(mGiftFormat.suffix(5)) + "1 min").localized
                //: } else {
            } else {
                //: priceStackView.isHidden = true
                priceStackView.isHidden = true
            }
            //: vipPriceBtn.isHidden = true
            vipPriceBtn.isHidden = true
            //: vipTopImgView.isHidden = true
            vipTopImgView.isHidden = true

            //: } else {
        } else { // 无免费次数, 展示价格
            //: let price = PathAppManager.share.loginUserMode.loungePlus ? vipFee:fee
            let price = PathAppManager.share.loginUserMode.loungePlus ? vipFee : fee
            //: priceLab.text = "%@/min".localizedArguments(price)
            priceLab.text = (String(noti_groupData)).equalArguments(price)

            //: let attr = NSMutableAttributedString(string: "%@/min".localizedArguments(price), attributes: [.strikethroughStyle : NSUnderlineStyle.single.rawValue, .foregroundColor: UIColor.white.withAlphaComponent(0.5), .font: UIFont.pingfangMediumFont(fontSize: 10)])
            let attr = NSMutableAttributedString(string: (String(noti_groupData)).equalArguments(price), attributes: [.strikethroughStyle: NSUnderlineStyle.single.rawValue, .foregroundColor: UIColor.white.withAlphaComponent(0.5), .font: UIFont.upperClassMove(fontSize: 10)])
            //: norPriceLab.attributedText = attr
            norPriceLab.attributedText = attr
            //: vipPriceBtn.isHidden = PathAppManager.share.loginUserMode.loungePlus
            vipPriceBtn.isHidden = PathAppManager.share.loginUserMode.loungePlus
            //: vipTopImgView.isHidden = PathAppManager.share.loginUserMode.loungePlus
            vipTopImgView.isHidden = PathAppManager.share.loginUserMode.loungePlus

            //: priceStackView.isHidden = !PathAppManager.share.loginUserMode.loungePlus
            priceStackView.isHidden = !PathAppManager.share.loginUserMode.loungePlus
        }
    }

    /// 添加视图
    //: private func setupSubviews() {
    private func subviewsEnable() {
        //: view.addSubview(renderView)
        view.addSubview(renderView)
        //: view.addSubview(gradientBtn)
        view.addSubview(gradientBtn)
        //: view.addSubview(svgaPlayer)
        view.addSubview(svgaPlayer)
        //: view.addSubview(tipsLab)
        view.addSubview(tipsLab)
        //: view.addSubview(chattingNumBtn)
        view.addSubview(chattingNumBtn)
        //: view.addSubview(priceStackView)
        view.addSubview(priceStackView)
        //: priceStackView.addArrangedSubview(vipIcon)
        priceStackView.addArrangedSubview(vipIcon)
        //: priceStackView.addArrangedSubview(goldCoinIcon)
        priceStackView.addArrangedSubview(goldCoinIcon)
        //: priceStackView.addArrangedSubview(priceLab)
        priceStackView.addArrangedSubview(priceLab)
        //: view.addSubview(vipPriceBtn)
        view.addSubview(vipPriceBtn)
        //: view.addSubview(vipTopImgView)
        view.addSubview(vipTopImgView)
        //: vipPriceBtn.addSubview(vipPriceImg)
        vipPriceBtn.addSubview(vipPriceImg)
        //: vipPriceBtn.addSubview(vipPriceLab)
        vipPriceBtn.addSubview(vipPriceLab)
        //: vipPriceBtn.addSubview(norPriceLab)
        vipPriceBtn.addSubview(norPriceLab)

        //: view.addSubview(closeBtn)
        view.addSubview(closeBtn)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func switchgoing() {
        //: renderView.snp.makeConstraints { make in
        renderView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: gradientBtn.snp.makeConstraints { make in
        gradientBtn.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: svgaPlayer.snp.makeConstraints { make in
        svgaPlayer.snp.makeConstraints { make in
            //: make.leading.equalTo(-8)
            make.leading.equalTo(-8)
            //: make.trailing.equalTo(8)
            make.trailing.equalTo(8)
            //: make.centerY.equalToSuperview()
            make.centerY.equalToSuperview()
            //: make.height.equalTo(actualWidth(w: 395))
            make.height.equalTo(actualWidth(w: 395))
        }
        //: tipsLab.snp.makeConstraints { make in
        tipsLab.snp.makeConstraints { make in
            //: make.bottom.equalTo(svgaPlayer.snp.bottom).offset(-40)
            make.bottom.equalTo(svgaPlayer.snp.bottom).offset(-40)
            //: make.width.centerX.equalToSuperview()
            make.width.centerX.equalToSuperview()
        }
        //: chattingNumBtn.snp.makeConstraints { make in
        chattingNumBtn.snp.makeConstraints { make in
            //: make.top.equalTo(tipsLab.snp.bottom).offset(35)
            make.top.equalTo(tipsLab.snp.bottom).offset(35)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
        }

        //: priceStackView.snp.makeConstraints { make in
        priceStackView.snp.makeConstraints { make in
            //: make.top.equalTo(chattingNumBtn.snp.bottom).offset(8)
            make.top.equalTo(chattingNumBtn.snp.bottom).offset(8)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.height.equalTo(31)
            make.height.equalTo(31)
            //: make.trailing.lessThanOrEqualToSuperview()
            make.trailing.lessThanOrEqualToSuperview()
        }
        //: vipIcon.snp.makeConstraints { make in
        vipIcon.snp.makeConstraints { make in
            //: make.width.height.equalTo(15)
            make.width.height.equalTo(15)
        }
        //: goldCoinIcon.snp.makeConstraints { make in
        goldCoinIcon.snp.makeConstraints { make in
            //: make.width.height.equalTo(actualWidth(w: 15))
            make.width.height.equalTo(actualWidth(w: 15))
        }
        //: priceLab.snp.makeConstraints { make in
        priceLab.snp.makeConstraints { make in
            //: make.height.equalToSuperview()
            make.height.equalToSuperview()
        }

        //: vipPriceBtn.snp.makeConstraints { make in
        vipPriceBtn.snp.makeConstraints { make in
            //: make.top.equalTo(priceStackView.snp.bottom).offset(16)
            make.top.equalTo(priceStackView.snp.bottom).offset(16)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.size.equalTo(CGSize(width: actualWidth(w: 240), height: actualWidth(w: 54)))
            make.size.equalTo(CGSize(width: actualWidth(w: 240), height: actualWidth(w: 54)))
        }
        //: vipTopImgView.snp.makeConstraints { make in
        vipTopImgView.snp.makeConstraints { make in
            //: make.top.equalTo(vipPriceBtn.snp.top).offset(-12)
            make.top.equalTo(vipPriceBtn.snp.top).offset(-12)
            //: make.leading.equalTo(vipPriceBtn.snp.leading)
            make.leading.equalTo(vipPriceBtn.snp.leading)
            //: make.size.equalTo(CGSize(width: actualWidth(w: 32), height: actualWidth(w: 24)))
            make.size.equalTo(CGSize(width: actualWidth(w: 32), height: actualWidth(w: 24)))
        }

        //: vipPriceImg.snp.makeConstraints { make in
        vipPriceImg.snp.makeConstraints { make in
            //: make.top.equalToSuperview().offset(actualWidth(w: 12))
            make.top.equalToSuperview().offset(actualWidth(w: 12))
            //: make.width.height.equalTo(15)
            make.width.height.equalTo(15)
            //: make.trailing.equalTo(vipPriceLab.snp.leading).offset(-2)
            make.trailing.equalTo(vipPriceLab.snp.leading).offset(-2)
        }

        //: vipPriceLab.snp.makeConstraints { make in
        vipPriceLab.snp.makeConstraints { make in
            //: make.centerY.equalTo(vipPriceImg)
            make.centerY.equalTo(vipPriceImg)
            //: make.centerX.equalToSuperview().offset(8)
            make.centerX.equalToSuperview().offset(8)
        }
        //: norPriceLab.snp.makeConstraints { make in
        norPriceLab.snp.makeConstraints { make in
            //: make.top.equalTo(vipPriceImg.snp.bottom).offset(5)
            make.top.equalTo(vipPriceImg.snp.bottom).offset(5)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
        }

        //: closeBtn.snp.makeConstraints { make in
        closeBtn.snp.makeConstraints { make in
            //: make.top.equalTo(StatusBarHeight+6)
            make.top.equalTo(app_topMsg + 6)
            //: make.trailing.equalTo(-10)
            make.trailing.equalTo(-10)
            //: make.width.height.equalTo(actualWidth(w: 44))
            make.width.height.equalTo(actualWidth(w: 44))
        }
    }

    /// 绑定事件
    //: private func bindInteraction() {
    private func appTarget() {
        // index/getconfig接口回调刷新UI
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(refreshVideoCallPrice),
                                               selector: #selector(balance),
                                               //: name: UPDATE_INDEX_GETCONFIG_NOTIFICATION,
                                               name: mainClickIconRequestMsg,
                                               //: object: nil)
                                               object: nil)
        // 个人信息更新成功刷新UI
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(req_updateVIPFee),
                                               selector: #selector(imageFee),
                                               //: name: USER_GETMYINFO_SUCCEED_NOTIFICATION,
                                               name: app_failTitle,
                                               //: object: nil)
                                               object: nil)
    }
}
