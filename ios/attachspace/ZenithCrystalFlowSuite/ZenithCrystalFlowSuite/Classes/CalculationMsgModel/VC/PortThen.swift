
//: Declare String Begin

/*: "bg_message_top" :*/
fileprivate let mainManagerId:String = "text modelbg_me"
fileprivate let noti_sizeAttentionTitle:[Character] = ["s","s","a","g","e","_","t","o","p"]

/*: "777777" :*/
fileprivate let notiPerspectiveText:String = "777777"

/*: "Messages" :*/
fileprivate let m_actualTextValue:[Character] = ["M"]
fileprivate let mainPlayUrl:String = "processorssagprocessors"

/*: "Who like me" :*/
fileprivate let dream_recordHiddenKey:String = "Who lilimit let"
fileprivate let showListMsg:String = "ke mefail name app share clear"

/*: "Call" :*/
fileprivate let m_toUrl:String = "Callto remove type succeed"

/*: "#FF2348" :*/
fileprivate let showStartText:String = "#FF2348target image label"

/*: "Current network unavailable" :*/
fileprivate let mWithoutTitle:String = "Currenmode make white voice select"
fileprivate let user_indexIdent:String = "voice custom share icon rangeork "
fileprivate let app_colorText:String = "asharea"
fileprivate let notiLabelCellFormat:[Character] = ["b","l","e"]

/*: "icon_yidu_pre" :*/
fileprivate let m_dataIdent:[Character] = ["i","c","o","n","_","y","i","d","u","_","p","r","e"]

/*: "99+" :*/
fileprivate let showModelName:String = "hiddenhidden+"

/*: "transform.rotation" :*/
fileprivate let user_qualifyFormat:String = "tfirstan"
fileprivate let dreamMainText:[Character] = ["a","t","i","o","n"]

/*: "transform.scale" :*/
fileprivate let app_playerTimeName:[Character] = ["t","r","a","n","s","f","o","r","m",".","s","c","a","l"]
fileprivate let noti_keyValue:String = "round"

/*: "zoom&shake" :*/
fileprivate let user_equalData:String = "zcollectionm"
fileprivate let userCenterValue:[Character] = ["e"]

/*: "yyyy-MM-dd" :*/
fileprivate let noti_marginName:String = "yyyy-MMmake native time corner"
fileprivate let appAppearTitle:String = "fail self content type-dd"

/*: "Allow %@ to send you notifications?" :*/
fileprivate let noti_liveIdent:[UInt8] = [0x11,0x3c,0x3c,0x3f,0x47,0xf0,0xf5,0x10,0xf0,0x44,0x3f,0xf0,0x43,0x35,0x3e,0x34,0xf0,0x49,0x3f,0x45,0xf0,0x3e,0x3f,0x44,0x39,0x36,0x39,0x33,0x31,0x44,0x39,0x3f,0x3e,0x43,0xf]

fileprivate func detailSection(name num: UInt8) -> UInt8 {
    let value = Int(num) + 48
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "Cancel" :*/
fileprivate let dream_faceIdent:[Character] = ["C","a","n","c","e","l"]

/*: "Settings" :*/
fileprivate let dream_likeFormat:[Character] = ["S","e","t","t","i","n","g","s"]

/*: "badNumber" :*/
fileprivate let dream_modelContent:String = "path text to key textbadN"
fileprivate let show_effectTitle:String = "third"

/*: "isConnection" :*/
fileprivate let show_fillMessage:String = "isConnback lab type if var"

/*: "networkStatus" :*/
fileprivate let notiViewTitle:[Character] = ["n","e","t","w"]
fileprivate let user_imageMsg:[Character] = ["o","r","k","S","t","a","t","u","s"]

/*: "unreadMessageNum" :*/
fileprivate let showInformationData:[Character] = ["u","n","r","e","a","d","M","e","s","s","a","g","e"]
fileprivate let k_constraintTitle:[Character] = ["N","u","m"]

/*: "Do you want to mark all messages as read?" :*/
fileprivate let show_errorFormat:[UInt8] = [0x65,0x90,0x41,0x9a,0x90,0x96,0x41,0x98,0x82,0x8f,0x95,0x41,0x95,0x90,0x41,0x8e,0x82,0x93,0x8c,0x41,0x82,0x8d,0x8d,0x41,0x8e,0x86,0x94,0x94,0x82,0x88,0x86,0x94,0x41,0x82,0x94,0x41,0x93,0x86,0x82,0x85,0x60]

fileprivate func somebodyCamera(close num: UInt8) -> UInt8 {
    let value = Int(num) + 223
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "OK" :*/
fileprivate let noti_modelName:[Character] = ["O","K"]

/*: "消息列表一键已读失败：code: :*/
fileprivate let app_tableNameData:String = "消息列\u{8868}一键已"
fileprivate let user_indexUrl:String = "读\u{5931}败"
fileprivate let show_toUrl:String = "if to：code:"

/*: , desc: :*/
fileprivate let mGiftCellKey:String = "result if view section number, des"
fileprivate let appMakeMsg:[Character] = ["c",":"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  PortThen.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/1/20.
//

//: import UIKit
import UIKit

//: class TalkingMessageViewController: TalkingBaseViewController {
class PortThen: ListRecognizerDelegate {
	var loadStyleTotal: Int = 66
	var upVideoMagnitude: Double = 15.5
	var firstDictionary: [AnyHashable: String] = [:]
	var assemblageSum: Int = 46
	var cropSum: Double = -23.9
	var topDictionary: [AnyHashable: String] = [:]

    //: var isNoChangeTop = false
    var isNoChangeTop = false
    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()
    //: private var seleteIndex = 0
    private var seleteIndex = 0

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: setIsShowCleanAmimat()
        setMain()
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
    
            if (badgLB.convert(CGRect(x: 0, y: 0, width: 0, height: 0), from: badgLB.superview).size.width == 17.88) && (badgLB.motionEffects.count == 11) {
                //: SWIFT_CUSTOM_DANGER_File_Call
                let snapAcross = StartView()
            snapAcross.userQuantity = seleteIndex
            snapAcross.bagAccountingCount = slideVC.makeScale

            
            snapAcross.offNumber = { [self] changeTotal in
            self.assemblageSum = changeTotal
            
            var snapAcross = seleteIndex
            snapAcross /= 4
            if snapAcross < self.assemblageSum {
                self.assemblageSum = snapAcross
            }
            
            return self.assemblageSum
            }
            snapAcross.sectionLabelSum = { [self] cookingUtensilNumber in
            self.cropSum = cookingUtensilNumber
            
            var snapAcross = slideVC.topDistance
                snapAcross -= 1
                if snapAcross < 71 {
                    snapAcross = snapAcross + 1
                }
            if snapAcross < self.cropSum {
                self.cropSum = snapAcross
            }
            
            return self.cropSum
            }
            snapAcross.miniIdentityDictionary = { [self] detailContainerDictionary in
            self.topDictionary = detailContainerDictionary
            
            guard var value = self.topDictionary as? [String: String] else {
                return nil
            }
            return value
            }
                badgLB.addSubview(snapAcross)
            }

	}

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true
        //: createUI()
        afterBind()
        //: bindInteraction()
        sumerplay()
        //: func__turnOnSystemNotification()
        squareNotification()
    
            if (colorImageView.convert(CGRect(x: 0, y: 0, width: 0, height: 0), from: colorImageView.superview).size.width == 17.88) && (colorImageView.motionEffects.count == 11) {
                //: SWIFT_CUSTOM_DANGER_File_Call
                let snapAcross = StartView()
            snapAcross.userQuantity = seleteIndex
            snapAcross.bagAccountingCount = slideVC.topDistance

            
            snapAcross.offNumber = { [self] changeTotal in
            self.loadStyleTotal = changeTotal
            
            var snapAcross = seleteIndex
            snapAcross /= 4
            if snapAcross < self.loadStyleTotal {
                self.loadStyleTotal = snapAcross
            }
            
            return self.loadStyleTotal
            }
            snapAcross.sectionLabelSum = { [self] cookingUtensilNumber in
            self.upVideoMagnitude = cookingUtensilNumber
            
            var snapAcross = slideVC.rightSpace
                snapAcross -= 1
                if snapAcross < 71 {
                    snapAcross = snapAcross + 1
                }
            if snapAcross < self.upVideoMagnitude {
                self.upVideoMagnitude = snapAcross
            }
            
            return self.upVideoMagnitude
            }
            snapAcross.miniIdentityDictionary = { [self] detailContainerDictionary in
            self.firstDictionary = detailContainerDictionary
            
            guard var value = self.firstDictionary as? [String: String] else {
                return nil
            }
            return value
            }
                colorImageView.addSubview(snapAcross)
            }

	}

    // MARK: - Lazy load

    // 背景
    //: private lazy var colorImageView: UIImageView = {
    private lazy var colorImageView: UIImageView = {
        //: let colorV = UIImageView.init(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: 100+StatusBarNavigationBarHeight))
        let colorV = UIImageView(frame: CGRect(x: 0, y: 0, width: app_screenFailMarginFormat, height: 100 + dream_systemTitle))
        //: colorV.image = UIImage.BundleImageNamed(name: "bg_message_top")
        colorV.image = UIImage.sendCollection(name: (String(mainManagerId.suffix(5)) + String(noti_sizeAttentionTitle)))
        //: return colorV
        return colorV
        //: }()
    }()

    //: private lazy var noticeView: TalkingNoticeTipView = {
    private lazy var noticeView: TiptoeThen = {
        //: let V = TalkingNoticeTipView()
        let V = TiptoeThen()
        //: V.isHidden = true
        V.isHidden = true
        //: return V
        return V
        //: }()
    }()

    //: private lazy var slideVC: QYSlideNavigationViewController = {
    private lazy var slideVC: LabelViewController = {
        //: let vc = QYSlideNavigationViewController()
        let vc = LabelViewController()
        //: vc.topDistance = 0
        vc.topDistance = 0
        //: vc.controllerItems = controllerItems
        vc.controllerItems = controllerItems
        //: vc.titleItems = titleItems
        vc.titleItems = titleItems
        //: vc.rightSpace = 60
        vc.rightSpace = 60
        //: vc.titleViewBackgroundColor = .clear
        vc.titleViewBackgroundColor = .clear
        //: vc.titleFont = .pingfangFont(type: .Medium, fontSize: 16)
        vc.titleFont = .bindFor(type: .Medium, fontSize: 16)
        //: vc.selectTitleFont = .pingfangFont(type: .Medium, fontSize: 18)
        vc.selectTitleFont = .bindFor(type: .Medium, fontSize: 18)
        //: vc.normalColor = UIColor(hex: "777777")!
        vc.normalColor = UIColor(hex: (notiPerspectiveText.capitalized))!
        //: vc.selectedColor = UIColor.appTitleColor()
        vc.selectedColor = UIColor.statusBy()
        //: vc.hasBttomTabBar = true
        vc.hasBttomTabBar = true
        //: vc.delegate = self
        vc.delegate = self
        //: vc.scrollEnabled = false
        vc.scrollEnabled = false
        //: return vc
        return vc
        //: }()
    }()

    //: private lazy var controllerItems: [TalkingBaseViewController] = {
    private lazy var controllerItems: [ListRecognizerDelegate] = {
        //: var arr = [self.chatListVC, TalkingWhoLikeViewController()]
        var arr = [self.chatListVC, LiveDataSource()]
        //: if PathAppManager.share.loginUserMode.callTabSwitch == 1 {
        if PathAppManager.share.loginUserMode.callTabSwitch == 1 {
            //: arr.insert(TalkingVideoCallVC(), at: arr.count-1)
            arr.insert(DataViewDelegate(), at: arr.count - 1)
        }
        //: return arr
        return arr

        //: }()
    }()

    //: private lazy var chatListVC: TalkingChatListViewController = {
    private lazy var chatListVC: DirectionRecognizerDelegate = {
        //: let vc = TalkingChatListViewController()
        let vc = DirectionRecognizerDelegate()
        //: return vc
        return vc
        //: }()
    }()

    //: private lazy var titleItems: [String] = {
    private lazy var titleItems: [String] = {
        //: var arr = ["Messages".localized, "Who like me".localized]
        var arr = [(String(m_actualTextValue) + mainPlayUrl.replacingOccurrences(of: "processor", with: "e")).localized, (String(dream_recordHiddenKey.prefix(6)) + String(showListMsg.prefix(5))).localized]
        //: if PathAppManager.share.loginUserMode.callTabSwitch == 1 {
        if PathAppManager.share.loginUserMode.callTabSwitch == 1 {
            //: arr.insert("Call".localized, at: arr.count-1)
            arr.insert((String(m_toUrl.prefix(4))).localized, at: arr.count - 1)
        }
        //: return arr
        return arr
        //: }()
    }()

    // 断网提醒
    //: private lazy var netTipLabel: UILabel = {
    private lazy var netTipLabel: UILabel = {
        //: let tipView = UILabel.init(frame: CGRect(x: 0, y: StatusBarNavigationBarHeight, width: ScreenWidth, height: 30))
        let tipView = UILabel(frame: CGRect(x: 0, y: dream_systemTitle, width: app_screenFailMarginFormat, height: 30))
        //: tipView.font = UIFont.pingfangRugularFont(fontSize: 14)
        tipView.font = UIFont.indicatorSize(fontSize: 14)
        //: tipView.textColor = .white
        tipView.textColor = .white
        //: tipView.backgroundColor = UIColor.init(hex: "#FF2348")?.withAlphaComponent(0.8)
        tipView.backgroundColor = UIColor(hex: (String(showStartText.prefix(7))))?.withAlphaComponent(0.8)
        //: tipView.textAlignment = .center
        tipView.textAlignment = .center
        //: tipView.text = "Current network unavailable".localized
        tipView.text = (String(mWithoutTitle.prefix(6)) + "t netw" + String(user_indexIdent.suffix(4)) + "unav" + app_colorText.replacingOccurrences(of: "share", with: "il") + String(notiLabelCellFormat)).localized
        //: tipView.isHidden = true
        tipView.isHidden = true
        //: return tipView
        return tipView
        //: }()
    }()

    // message红点角标
    //: private lazy var msgNumLabel: BadgeLab = {
    private lazy var msgNumLabel: IndoorsView = {
        //: let numbLabel = BadgeLab()
        let numbLabel = IndoorsView()
        //: numbLabel.isHidden = true
        numbLabel.isHidden = true
        //: return numbLabel
        return numbLabel
        //: }()
    }()

    // whoLikeMe红点角标
    //: private lazy var badgLB: BadgeLab = {
    private lazy var badgLB: IndoorsView = {
        //: let numbLabel = BadgeLab()
        let numbLabel = IndoorsView()
        //: numbLabel.isHidden = true
        numbLabel.isHidden = true
        //: return numbLabel
        return numbLabel
        //: }()
    }()

    // 一键清除
    //: private lazy var cleanBtn: UIButton = {
    private lazy var cleanBtn: UIButton = {
        //: let btn = UIButton(type: .custom)
        let btn = UIButton(type: .custom)
        //: btn.backgroundColor = .clear
        btn.backgroundColor = .clear
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_yidu_pre"), for: .normal)
        btn.setImage(UIImage.sendCollection(name: (String(m_dataIdent))), for: .normal)
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_yidu_pre"), for: .highlighted)
        btn.setImage(UIImage.sendCollection(name: (String(m_dataIdent))), for: .highlighted)
        //: return btn
        return btn
        //: }()
    }()
}

// MARK: - TaxRateNavigationDelegate

//: extension TalkingMessageViewController: QYSlideNavigationDelegate {
extension PortThen: TaxRateNavigationDelegate {
    //: func selectedViewController(_ viewController: UIViewController, index: Int) {
    func coordinate(_ viewController: UIViewController, index: Int) {
        //: self.seleteIndex = Int(index)
        self.seleteIndex = Int(index)
        //: if viewController.isKind(of: TalkingChatListViewController.self) {
        if viewController.isKind(of: DirectionRecognizerDelegate.self) {
            //: self.cleanBtn.isHidden = false
            self.cleanBtn.isHidden = false
            //: setIsShowCleanAmimat()
            setMain()

            //: } else if viewController.isKind(of: TalkingWhoLikeViewController.self) {
        } else if viewController.isKind(of: LiveDataSource.self) {
            //: self.cleanBtn.isHidden = true
            self.cleanBtn.isHidden = true
            //: uploadRecord.uploadRecordEvent(eventID: ClickWLikeMeTabNoP)
            kResolutionText.atJson(eventID: appRecordNetScreenName)
        }
    }
}

// MARK: - Event

//: extension TalkingMessageViewController {
extension PortThen {
    //: func setIsTopAll() {
    func appAll() {
        /// 默认选中all
        //: if seleteIndex == 0 {
        if seleteIndex == 0 {
            //: let vc = slideVC.controllerItems.first as? TalkingChatListViewController
            let vc = slideVC.controllerItems.first as? DirectionRecognizerDelegate
            //: vc?.resetToTopItemView()
            vc?.infoDoing()
            // 刷新在线状态
            //: vc?.refreshOnlineStatus(isFirst: true)
            vc?.trifle(isFirst: true)
        }
    }

    /// 无网络提示
    //: func func__checkNetTipState() {
    func buttonMoment() {
        //: if PathAppManager.share.networkStatus != .Unavailable && CharterDataManager.shared.isConnection {
        if PathAppManager.share.networkStatus != .Unavailable, CharterDataManager.shared.isConnection {
            //: netTipLabel.isHidden = true
            netTipLabel.isHidden = true
            //: } else {
        } else {
            //: netTipLabel.isHidden = false
            netTipLabel.isHidden = false
        }
    }

    /// message红点
    //: func func__messagebadgLBState(num: Int) {
    func headTarget(num: Int) {
        //: if num > 0 {
        if num > 0 {
            //: msgNumLabel.isHidden = false
            msgNumLabel.isHidden = false
            //: if num > 99 {
            if num > 99 {
                //: msgNumLabel.text = "99+"
                msgNumLabel.text = "99+"
                //: } else {
            } else {
                //: msgNumLabel.text = String(num)
                msgNumLabel.text = String(num)
            }
            //: } else {
        } else {
            //: msgNumLabel.isHidden = true
            msgNumLabel.isHidden = true
        }
    }

    /// 一键清除动画
    //: func setIsShowCleanAmimat() {
    func setMain() {
        //: let unreadMsgCount = PathAppManager.share.unreadMessageNum
        let unreadMsgCount = PathAppManager.share.unreadMessageNum
        //: let isClean: Bool = UserDefaults.standard.object(forKey: isShowCleanAnimatCacheKey) as? Bool ?? false
        let isClean: Bool = UserDefaults.standard.object(forKey: show_keyRecordTitle) as? Bool ?? false
        //: if unreadMsgCount >= 10 && !isClean {
        if unreadMsgCount >= 10, !isClean {
            //: self.cleanBtnBtnAnimat()
            self.roundTitleLabel()
        }
    }

    //: func cleanBtnBtnAnimat() {
    func roundTitleLabel() {
        //: let degreesToRadians = 180.0 * .pi
        let degreesToRadians = 180.0 * .pi
        //: let angle1 = 0 / degreesToRadians
        let angle1 = 0 / degreesToRadians
        //: let angle2 = 100 / degreesToRadians
        let angle2 = 100 / degreesToRadians
        //: let angle3 = -100 / degreesToRadians
        let angle3 = -100 / degreesToRadians
        //: let angle4 = 100 / degreesToRadians
        let angle4 = 100 / degreesToRadians
        //: let angle5 = -100 / degreesToRadians
        let angle5 = -100 / degreesToRadians
        //: let angle6 = 0 / degreesToRadians
        let angle6 = 0 / degreesToRadians

        //: let du1: NSNumber = 0
        let du1: NSNumber = 0
        //: let du2: NSNumber = 0.13
        let du2: NSNumber = 0.13
        //: let du3: NSNumber = 0.3
        let du3: NSNumber = 0.3
        //: let du4: NSNumber = 0.47
        let du4: NSNumber = 0.47
        //: let du5: NSNumber = 0.8
        let du5: NSNumber = 0.8
        //: let du6: NSNumber = 1.0
        let du6: NSNumber = 1.0

        // 旋转
        //: let roate = CAKeyframeAnimation.init(keyPath: "transform.rotation")
        let roate = CAKeyframeAnimation(keyPath: (user_qualifyFormat.replacingOccurrences(of: "first", with: "r") + "sform.rot" + String(dreamMainText)))
        //: roate.values = [(angle1), (angle2), (angle3), (angle4), (angle5), (angle6)]
        roate.values = [angle1, angle2, angle3, angle4, angle5, angle6]
        //: roate.keyTimes = [(du1), (du2), (du3), (du4), (du5), (du6)]
        roate.keyTimes = [du1, du2, du3, du4, du5, du6]

        // 缩放
        //: let zoom = CAKeyframeAnimation.init(keyPath: "transform.scale")
        let zoom = CAKeyframeAnimation(keyPath: (String(app_playerTimeName) + noti_keyValue.replacingOccurrences(of: "round", with: "e")))
        //: zoom.values = [(1.0), (1.2), (1.0)]
        zoom.values = [1.0, 1.2, 1.0]
        //: zoom.keyTimes = [(0), (0.5), (1.0)]
        zoom.keyTimes = [0, 0.5, 1.0]

        //
        //: let groupAnnimation = CAAnimationGroup.init()
        let groupAnnimation = CAAnimationGroup()
        //: groupAnnimation.autoreverses = false
        groupAnnimation.autoreverses = false
        //: groupAnnimation.animations = [roate, zoom]
        groupAnnimation.animations = [roate, zoom]
        //: groupAnnimation.duration = 1.0
        groupAnnimation.duration = 1.0
        //: groupAnnimation.repeatCount = 2
        groupAnnimation.repeatCount = 2
        //: groupAnnimation.isRemovedOnCompletion = false
        groupAnnimation.isRemovedOnCompletion = false
        //: groupAnnimation.fillMode = .forwards
        groupAnnimation.fillMode = .forwards
        //: cleanBtn.layer.add(groupAnnimation, forKey: "zoom&shake")
        cleanBtn.layer.add(groupAnnimation, forKey: (user_equalData.replacingOccurrences(of: "collection", with: "oo") + "&shak" + String(userCenterValue)))
    }

    /// 检查系统通知是否开启
    //: private func func__turnOnSystemNotification() {
    private func squareNotification() {
        //: TalkingPermissionTool.checkPushNotification { isEnable in
        EqualReactiveCompatible.errorEqual { isEnable in

            //: DispatchQueue.main.async {
            DispatchQueue.main.async {
                //: if isEnable {
                if isEnable {
                    //: self.noticeView.isHidden = true
                    self.noticeView.isHidden = true
                    //: self.func__hideNotificationTipView(hide: true)
                    self.cellFunc(hide: true)
                    //: }else{
                } else {
                    /// 页面顶部提示
                    //: let today = NSDate.getTimeString(date: Date(), dateFormat: "yyyy-MM-dd")
                    let today = NSDate.contentWith(date: Date(), dateFormat: (String(noti_marginName.prefix(7)) + String(appAppearTitle.suffix(3))))
                    //: if let closeDateStr = Defaults.string(forKey: TalkingMessageTabCloseNotificationsKey), today == closeDateStr {
                    if let closeDateStr = dreamBottomMsg.string(forKey: m_displayPathMessage), today == closeDateStr {
                        //: self.func__hideNotificationTipView(hide: true)
                        self.cellFunc(hide: true)
                        //: }else{
                    } else {
                        //: self.func__hideNotificationTipView(hide: false)
                        self.cellFunc(hide: false)
                    }
                    /// 弹框逻辑
                    //: let isShow = Defaults.bool(forKey: TalkingMessageTabTurnOnNotificationsKey)
                    let isShow = dreamBottomMsg.bool(forKey: app_nameUrl)
                    //: if !isShow {
                    if !isShow {
                        //: Defaults.set(true, forKey: TalkingMessageTabTurnOnNotificationsKey)
                        dreamBottomMsg.set(true, forKey: app_nameUrl)
                        //: TalkingAlertShow.alert(title: nil,
                        CourseAlertShow.blockConfig(title: nil,
                                                     //: message: "Allow %@ to send you notifications?".localizedArguments(AppName),
                                                     message: String(bytes: noti_liveIdent.map{detailSection(name: $0)}, encoding: .utf8)!.equalArguments(mVideoIdent),
                                                     //: leftBtnTitle: "Cancel".localized,
                                                     leftBtnTitle: (String(dream_faceIdent)).localized,
                                                     //: rightBtnTitle: "Settings".localized) {
                                                     rightBtnTitle: (String(dream_likeFormat)).localized)
                        {
                            //: TalkingAlertShow.hideAlert()
                            CourseAlertShow.cardWindow()
                            //: return
                            //: } rightBlock: {
                        } rightBlock: {
                            //: if let settingsUrl = URL(string: UIApplication.openSettingsURLString) {
                            if let settingsUrl = URL(string: UIApplication.openSettingsURLString) {
                                //: UIApplication.shared.open(settingsUrl)
                                UIApplication.shared.open(settingsUrl)
                            }
                        }
                    }
                }
            }
        }
    }

    //: private func func__hideNotificationTipView(hide: Bool) {
    private func cellFunc(hide: Bool) {
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: if hide{
            if hide {
                //: self.noticeView.isHidden = true
                self.noticeView.isHidden = true
                //: self.slideVC.view.snp.updateConstraints { make in
                self.slideVC.view.snp.updateConstraints { make in
                    //: make.top.equalToSuperview().offset(StatusBarHeight)
                    make.top.equalToSuperview().offset(app_topMsg)
                }
                //: self.slideVC.viewHeight = ScreenHeight - StatusBarHeight - TabBarViewHeight
                self.slideVC.viewHeight = mainAcceptKey - app_topMsg - mainContentFormat
                //: }else{
            } else {
                //: self.noticeView.isHidden = false
                self.noticeView.isHidden = false
                //: self.slideVC.view.snp.updateConstraints { make in
                self.slideVC.view.snp.updateConstraints { make in
                    //: make.top.equalToSuperview().offset(StatusBarHeight + self.noticeView.viewHeight)
                    make.top.equalToSuperview().offset(app_topMsg + self.noticeView.viewHeight)
                }
                //: self.slideVC.viewHeight = ScreenHeight - self.noticeView.bottom - TabBarViewHeight
                self.slideVC.viewHeight = mainAcceptKey - self.noticeView.bottom - mainContentFormat
            }
        }
    }
}

// MARK: - 通知

//: extension TalkingMessageViewController {
extension PortThen {
    /// who like me 红点通知
    //: @objc func showbadgNumberNotif(notification: NSNotification) {
    @objc func equalAt(notification: NSNotification) {
        //: let userinfo = notification.userInfo as![String: AnyObject]
        let userinfo = notification.userInfo as! [String: AnyObject]
        //: let badNumber: Int  = userinfo["badNumber"] as! Int
        let badNumber: Int = userinfo[(String(dream_modelContent.suffix(4)) + "umbe" + show_effectTitle.replacingOccurrences(of: "third", with: "r"))] as! Int
        //: badgLB.isHidden = false
        badgLB.isHidden = false
        //: if badNumber > 99 {
        if badNumber > 99 {
            //: badgLB.text = "99+"
            badgLB.text = "99+"
            //: } else if badNumber<=0 {
        } else if badNumber <= 0 {
            //: badgLB.isHidden = true
            badgLB.isHidden = true
            //: badgLB.text = "0"
            badgLB.text = "0"
            //: } else {
        } else {
            //: badgLB.text = String(badNumber)
            badgLB.text = String(badNumber)
        }
    }
}

// MARK: - Layout

//: extension TalkingMessageViewController {
extension PortThen {
    /// UI
    //: private func createUI() {
    private func afterBind() {
        //: view.backgroundColor = .white
        view.backgroundColor = .white

        //: view.addSubview(colorImageView)
        view.addSubview(colorImageView)
        //: view.addSubview(noticeView)
        view.addSubview(noticeView)
        //: self.addChild(slideVC)
        self.addChild(slideVC)

        //: self.view.addSubview(slideVC.view)
        self.view.addSubview(slideVC.view)
        //: slideVC.view.backgroundColor = UIColor.clear
        slideVC.view.backgroundColor = UIColor.clear
        //: slideVC.view.snp.makeConstraints { make in
        slideVC.view.snp.makeConstraints { make in
            //: make.top.equalToSuperview().offset(StatusBarHeight)
            make.top.equalToSuperview().offset(app_topMsg)
            //: make.leading.trailing.equalToSuperview()
            make.leading.trailing.equalToSuperview()
            //: make.bottom.equalToSuperview()
            make.bottom.equalToSuperview()
        }

        //: for btn in slideVC.allBtn {
        for btn in slideVC.allBtn {
            //: let curenbrn: UIButton = btn
            let curenbrn: UIButton = btn
            //: if curenbrn.titleLabel!.text == "Who like me".localized {
            if curenbrn.titleLabel!.text == (String(dream_recordHiddenKey.prefix(6)) + String(showListMsg.prefix(5))).localized {
                //: (curenbrn as AnyObject).addSubview(badgLB)
                (curenbrn as AnyObject).addSubview(badgLB)
                //: badgLB.snp.makeConstraints { make in
                badgLB.snp.makeConstraints { make in
                    //: make.leading.equalTo(curenbrn.titleLabel!.snp.trailing).offset(-5)
                    make.leading.equalTo(curenbrn.titleLabel!.snp.trailing).offset(-5)
                    //: make.top.equalTo(curenbrn).offset(7)
                    make.top.equalTo(curenbrn).offset(7)
                    //: make.height.equalTo(16)
                    make.height.equalTo(16)
                    //: make.width.greaterThanOrEqualTo(16)
                    make.width.greaterThanOrEqualTo(16)
                }
                //: badgLB.isHidden = true
                badgLB.isHidden = true
                //: } else if (curenbrn.titleLabel!.text == "Messages".localized) {
            } else if curenbrn.titleLabel!.text == (String(m_actualTextValue) + mainPlayUrl.replacingOccurrences(of: "processor", with: "e")).localized {
                //: (curenbrn as AnyObject).addSubview(msgNumLabel)
                (curenbrn as AnyObject).addSubview(msgNumLabel)
                //: msgNumLabel.snp.makeConstraints { make in
                msgNumLabel.snp.makeConstraints { make in
                    //: make.leading.equalTo(curenbrn.titleLabel!.snp.trailing).offset(-5)
                    make.leading.equalTo(curenbrn.titleLabel!.snp.trailing).offset(-5)
                    //: make.top.equalTo(curenbrn).offset(7)
                    make.top.equalTo(curenbrn).offset(7)
                    //: make.height.equalTo(16)
                    make.height.equalTo(16)
                    //: make.width.greaterThanOrEqualTo(16)
                    make.width.greaterThanOrEqualTo(16)
                }
            }
        }

        //: view.addSubview(cleanBtn)
        view.addSubview(cleanBtn)
        //: cleanBtn.snp.makeConstraints { make in
        cleanBtn.snp.makeConstraints { make in
            //: make.top.equalTo(slideVC.view.snp.top)
            make.top.equalTo(slideVC.view.snp.top)
            //: make.trailing.equalTo(-5)
            make.trailing.equalTo(-5)
            //: make.size.equalTo(CGSize(width: 44, height: 44))
            make.size.equalTo(CGSize(width: 44, height: 44))
        }

        //: view.addSubview(netTipLabel)
        view.addSubview(netTipLabel)
        //: netTipLabel.snp.makeConstraints { make in
        netTipLabel.snp.makeConstraints { make in
            //: make.top.equalTo(slideVC.view.snp.top).offset(44)
            make.top.equalTo(slideVC.view.snp.top).offset(44)
            //: make.leading.trailing.equalToSuperview()
            make.leading.trailing.equalToSuperview()
            //: make.height.equalTo(30)
            make.height.equalTo(30)
        }
    }

    /// 绑定事件
    //: private func bindInteraction() {
    private func sumerplay() {
        //: CharterDataManager.shared.rx
        CharterDataManager.shared.rx
            //: .observeWeakly(Bool.self, "isConnection")
            .observeWeakly(Bool.self, (String(show_fillMessage.prefix(6)) + "ection"))
            //: .subscribe(onNext: { [weak self] (value) in
            .subscribe(onNext: { [weak self] _ in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.func__checkNetTipState()
                self.buttonMoment()
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        //: PathAppManager.share.rx.observeWeakly(Int.self, "networkStatus")
        PathAppManager.share.rx.observeWeakly(Int.self, (String(notiViewTitle) + String(user_imageMsg)))
            //: .subscribe(onNext: { [weak self]  (value) in
            .subscribe(onNext: { [weak self] _ in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.func__checkNetTipState()
                self.buttonMoment()
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        // 展示全部红点
        //: PathAppManager.share.rx.observeWeakly(Int.self, "unreadMessageNum")
        PathAppManager.share.rx.observeWeakly(Int.self, (String(showInformationData) + String(k_constraintTitle)))
            //: .subscribe(onNext: { [weak self] (value) in
            .subscribe(onNext: { [weak self] value in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: let valueInt = value ?? 0
                let valueInt = value ?? 0
                //: self.func__messagebadgLBState(num: valueInt)
                self.headTarget(num: valueInt)
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        //: cleanBtn.rx.controlEvent(.touchUpInside)
        cleanBtn.rx.controlEvent(.touchUpInside)
            //: .subscribe(onNext: {
            .subscribe(onNext: {
                //: let config = ShowAlertConfig()
                let config = KeyCustomAlertConfig()
                //: config.alignment = .center
                config.alignment = .center

                //: TalkingAlertShow.customAlert(message: "Do you want to mark all messages as read?".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "OK".localized, leftBlock: {
                CourseAlertShow.inkpad(message: String(bytes: show_errorFormat.map{somebodyCamera(close: $0)}, encoding: .utf8)!.localized, leftBtnTitle: (String(dream_faceIdent)).localized, rightBtnTitle: "OK".localized, leftBlock: {
                    //: TalkingAlertShow.hideAlert()
                    CourseAlertShow.cardWindow()

                    //: }, rightBlock: {
                }, rightBlock: {
                    //: V2TIMManager.sharedInstance().markAllMessage {
                    V2TIMManager.sharedInstance().markAllMessage {
                        //: UserDefaults.standard.set(true, forKey: isShowCleanAnimatCacheKey)
                        UserDefaults.standard.set(true, forKey: show_keyRecordTitle)
                        //: } fail: { code, desc in
                    } fail: { code, desc in
                        //: UploadLogTool.writeLog(msg: "消息列表一键已读失败：code:\(code), desc:\(String(describing: desc))")
                        LabelLogTool.fillInLine(msg: (app_tableNameData + user_indexUrl + String(show_toUrl.suffix(6))) + "\(code)" + (String(mGiftCellKey.suffix(5)) + String(appMakeMsg)) + "\(String(describing: desc))")
                    }
                    //: }, config: config)
                }, config: config)

                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        // who like me 红点通知
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(showbadgNumberNotif(notification:)),
                                               selector: #selector(equalAt(notification:)),
                                               //: name: WHOLIKEME_BADGENUMBER_NOTIF,
                                               name: notiTurnData,
                                               //: object: nil)
                                               object: nil)

        //: noticeView.closeBtnBlock = { [weak self] ()  -> Void in
        noticeView.closeBtnBlock = { [weak self] () in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: let today = NSDate.getTimeString(date: Date(), dateFormat: "yyyy-MM-dd")
            let today = NSDate.contentWith(date: Date(), dateFormat: (String(noti_marginName.prefix(7)) + String(appAppearTitle.suffix(3))))
            //: Defaults.set(today, forKey: TalkingMessageTabCloseNotificationsKey)
            dreamBottomMsg.set(today, forKey: m_displayPathMessage)
            //: self.func__hideNotificationTipView(hide: true)
            self.cellFunc(hide: true)
        }

        //: NotificationCenter.default.rx.notification( UIApplication.willEnterForegroundNotification)
        NotificationCenter.default.rx.notification(UIApplication.willEnterForegroundNotification)
            //: .subscribe(onNext: { [weak self] (notification) in
            .subscribe(onNext: { [weak self] _ in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.func__turnOnSystemNotification()
                self.squareNotification()
                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)
    }
}
