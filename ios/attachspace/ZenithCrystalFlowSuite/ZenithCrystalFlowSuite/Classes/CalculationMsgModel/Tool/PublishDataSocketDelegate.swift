
//: Declare String Begin

/*: "SOCKET_ADDR_KEY" :*/
fileprivate let user_modelTitle:String = "socke"
fileprivate let showHiddenName:String = "KEuser"

/*: "SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION" :*/
fileprivate let dream_rateMsg:[UInt8] = [0x6f,0x73,0x7f,0x77,0x79,0x68,0x63,0x6a,0x75,0x78,0x79,0x73,0x7f,0x74,0x7d,0x68,0x63,0x6e,0x79,0x6d,0x69,0x79,0x6f,0x68,0x7f,0x7d,0x70,0x70,0x63,0x72,0x73,0x68,0x75,0x7a,0x75,0x7f,0x7d,0x68,0x75,0x73,0x72]

private func premiumView(center num: UInt8) -> UInt8 {
    return num ^ 60
}

/*: "RECIVE_FLOATSCREEN_NOTIFICATION" :*/
fileprivate let dream_theTitle:[UInt8] = [0x4e,0x4f,0x49,0x54,0x41,0x43,0x49,0x46,0x49,0x54,0x4f,0x4e,0x5f,0x4e,0x45,0x45,0x52,0x43,0x53,0x54,0x41,0x4f,0x4c,0x46,0x5f,0x45,0x56,0x49,0x43,0x45,0x52]

/*: "isCalling" :*/
fileprivate let m_fillSendIdent:[Character] = ["i","s","C","a","l","l","i","n","g"]
 
/*: "SOCKET 连接成功." :*/
fileprivate let kSexText:[Character] = ["S","O","C","K","E","T"," ","连","接","成","功","."]

/*: "SOCKET 连接失败, error: :*/
fileprivate let k_lastErrorIndicatorName:String = "socke"
fileprivate let user_pathValue:String = "view time view indexerror:"

/*: ." :*/
fileprivate let user_actualKey:String = "."

/*: "SOCKET 失败 :*/
fileprivate let mCommentColorMessage:String = "let content view selfSOCKET "
fileprivate let mToLabKey:String = "失败"

/*: 次, 切换过Socket域名重新尝试，地址： :*/
fileprivate let m_valueIdent:String = "\u{6b21}, 切\u{6362}过So"
fileprivate let m_holdStr:String = "cket域名\u{91cd}"
fileprivate let show_burnBindLetId:String = "新尝\u{8bd5}，地址："

/*: "SOCKET 已关闭, code: :*/
fileprivate let noti_complaintUrl:String = "socket"
fileprivate let mByValue:String = " 已关\u{95ed}"

/*: , reason: :*/
fileprivate let main_viewTitle:[Character] = [","," ","r","e","a","s"]
fileprivate let m_headName:[Character] = ["o","n",":"]

/*: "ws:// :*/
fileprivate let showModelMessage:[Character] = ["w","s",":","/","/"]

/*: /?sid= :*/
fileprivate let m_labName:String = "color type let/?sid="

/*: &uid= :*/
fileprivate let app_makeImageValue:String = "let var aspect&uid="

/*: &platform=iphone&version= :*/
fileprivate let appErrorUrl:String = "&platscreen class corner self"
fileprivate let kBlockPath:[Character] = ["h","o","n","e"]
fileprivate let appCornerKey:String = "let image shrink return&vers"

/*: &packageId= :*/
fileprivate let main_awakeFormat:String = "color as view&pack"

/*: "1v1server. :*/
fileprivate let showOfPath:String = "surgery language start1v1ser"

/*: .com:7766" :*/
fileprivate let userAMainKey:String = ".cobackground view conversation"
fileprivate let showItemMsg:String = "same equal title namem:7766"

/*: "43.129.185.194:7767" :*/
fileprivate let show_writeToId:String = "model.12"
fileprivate let k_likeRemoteValue:String = "military.19"
fileprivate let notiAtText:[Character] = ["4",":","7","7","6","7"]

/*: "SOCKET 网络reachabilityChanged监听，断网关闭." :*/
fileprivate let main_layerName:[UInt8] = [0xc6,0xc2,0xb6,0xbe,0xb8,0xc7,0x93,0x5a,0x30,0x4,0x5a,0x2e,0xf,0xe5,0xd8,0xd4,0xd6,0xdb,0xd4,0xd5,0xdc,0xdf,0xdc,0xe7,0xec,0xb6,0xdb,0xd4,0xe1,0xda,0xd8,0xd7,0x5a,0xe,0x4,0x58,0x3,0x1f,0x62,0x2f,0xff,0x59,0x9,0x20,0x5a,0x30,0x4,0x58,0xf8,0x26,0x5c,0xa,0x20,0xa1]

fileprivate func meImage(main num: UInt8) -> UInt8 {
    let value = Int(num) + 141
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "cmd" :*/
fileprivate let show_sizeData:String = "cmtrigger"

/*: "ping" :*/
fileprivate let dream_messagePath:String = "playng"

/*: "callHeartbeat" :*/
fileprivate let app_upUrl:String = "var modelcallH"
fileprivate let userMarginTaskPath:String = "eaddt"

/*: "recievedDate" :*/
fileprivate let appDeadlineShowFormat:[Character] = ["r","e","c","i","e","v","e","d","D","a"]
fileprivate let main_targetMessage:[Character] = ["t","e"]

/*: "liveHeartBeat" :*/
fileprivate let mFillUrl:[Character] = ["l","i","v","e","H","e","a","r","t","B"]
fileprivate let userStatusPath:[Character] = ["e","a","t"]

/*: "🔗 关闭 socket" :*/
fileprivate let user_dataInputStr:String = "\u{0001f517} 关闭 "
fileprivate let app_modelStr:String = "SOCKET"

/*: "The user logs out" :*/
fileprivate let appSucceedUrl:[Character] = ["T","h","e"," ","u","s","e"]
fileprivate let dream_ofTitle:String = "distance other container equal leadingr l"
fileprivate let userGiftData:String = "data spec upon deviceogs out"

/*: "SOCKET  :*/
fileprivate let userPathName:String = "SOCmake"
fileprivate let show_nameMessage:String = "T else type a break any"

/*: 重连. 重试次数： :*/
fileprivate let notiEraseLiveIdent:[Character] = ["重","\u{8fde}","."," ","重","试","次","数"]
fileprivate let showGiftKey:String = "："

/*: , 地址： :*/
fileprivate let mStatusValue:[Character] = [","," ","地","\u{5740}","："]

/*: "SOCKET 连接正常 不需要重新连接." :*/
fileprivate let noti_equalFormat:String = "SOCKlabel"
fileprivate let mainPlayerText:[Character] = [" ","连","\u{63a5}","正","常"]
fileprivate let user_nameData:String = " 不\u{9700}要重新\u{8fde}接."

/*: "SOCKET 正在连接 不需要重新连接." :*/
fileprivate let dreamGiftStr:String = "SOCKET frame name"
fileprivate let kTearStr:[Character] = ["正","在","连","接"," ","不","需","要","重","新","连","接","."]

/*: "&resume=1" :*/
fileprivate let mPopVideoIdent:[Character] = ["&","r"]
fileprivate let m_labId:String = "mode topesume=1"

/*: "&lang= :*/
fileprivate let dreamAppText:String = "&"
fileprivate let dreamLevelMessage:[Character] = ["l","a","n","g","="]

/*: "SOCKET 开始连接, 地址： :*/
fileprivate let k_destroyIdent:String = "SOCup"
fileprivate let user_upperId:String = "连\u{63a5}, 地址"
fileprivate let user_positionStr:String = "add"

/*: "onNetDisabled" :*/
fileprivate let mNameLayerId:String = "view event last gender indexonNetDis"
fileprivate let show_instanceData:String = "ablevalue"

/*: "errno" :*/
fileprivate let mTopName:String = "etabletableno"

/*: "data" :*/
fileprivate let appCellSharedUrl:String = "dmodela"

/*: "SOCKET 连接保活cmd: ping." :*/
fileprivate let dreamBackgroundName:String = "clickCKET"
fileprivate let k_collectionSumData:String = "活cmd:name list"

/*: "SOCKET trtc保活cmd: callHeartbeat." :*/
fileprivate let showGenderGiftStr:[UInt8] = [0x9c,0x98,0x8c,0x94,0x8e,0x9d,0x69,0xbd,0xbb,0xbd,0xac,0x2d,0x8,0xe6,0x2f,0xfd,0x4,0xac,0xb6,0xad,0x83,0x69,0xac,0xaa,0xb5,0xb5,0x91,0xae,0xaa,0xbb,0xbd,0xab,0xae,0xaa,0xbd,0x77]

fileprivate func selfGift(top num: UInt8) -> UInt8 {
    let value = Int(num) - 73
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "SOCKET live保活cmd: liveHeartBeat." :*/
fileprivate let dream_yearMessage:[UInt8] = [0x2e,0x74,0x61,0x65,0x42,0x74,0x72,0x61,0x65,0x48,0x65,0x76,0x69,0x6c,0x20,0x3a,0x64,0x6d,0x63,0xbb,0xb4,0xe6,0x9d,0xbf,0xe4,0x65,0x76,0x69,0x6c,0x20,0x54,0x45,0x4b,0x43,0x4f,0x53]

/*: "onP" :*/
fileprivate let mainColorContent:String = "onPerror end dream make let"

/*: "msg" :*/
fileprivate let user_errorMessage:String = "mstitle"

/*: "🔗 处理消息：cmdName: :*/
fileprivate let noti_allId:String = "🔗 处理消\u{606f}："
fileprivate let user_giftId:String = "listm"
fileprivate let show_appUrl:String = "dName:var path"

/*: , errNo: :*/
fileprivate let noti_lengthMessage:[Character] = [","," "]
fileprivate let k_timeMsg:String = "push show viewerrNo:"

/*: , msg: :*/
fileprivate let k_arrayFormat:[Character] = [","," ","m","s","g",":"]

/*: , data: :*/
fileprivate let m_upName:String = ", datatrue let detail path"
fileprivate let noti_colorFormat:[Character] = [":"]

/*: "SOCKET 接收异常消息：cmdName: :*/
fileprivate let userEqualUrl:String = "SOfailK"
fileprivate let main_modelUrl:String = "常消息\u{ff1a}cmdN"
fileprivate let m_voiceMsg:[Character] = ["a","m","e",":"]

/*: "onReceivedGift" :*/
fileprivate let dreamStorageData:String = "onReclet info import leading time"
fileprivate let k_layerSharedPath:String = "subivsub"
fileprivate let user_headIdent:[Character] = ["d","G","i","f","t"]

/*: "onVideoMatch" :*/
fileprivate let noti_userMessage:[Character] = ["o","n","V","i","d","e","o","M"]
fileprivate let userTabData:[Character] = ["a","t","c","h"]

/*: "onRequestCall" :*/
fileprivate let user_appMsg:String = "onRequintimate res"
fileprivate let app_indexFormat:String = "E"
fileprivate let show_cameraUrl:[Character] = ["s","t","C","a","l","l"]

/*: "onCancelCall" :*/
fileprivate let main_pageWithTotalStr:[Character] = ["o","n","C","a","n","c","e","l","C","a","l","l"]

/*: "onRefuseCall" :*/
fileprivate let app_domainKey:String = "onRefufalse can if content"
fileprivate let dreamMarginData:String = "seCallfrom value"

/*: "onAcceptCall" :*/
fileprivate let m_kitMessage:String = "model trueonAcc"

/*: "onStopCall" :*/
fileprivate let dream_errorGroupData:String = "color constraint color conversationonSto"
fileprivate let kLayerKey:[Character] = ["l"]

/*: "freeCallTimes" :*/
fileprivate let appCurrentPath:String = "model selected count viewfreeCa"
fileprivate let showImageIdent:String = "tracks"

/*: "mfCoin" :*/
fileprivate let mPathIndexTitle:String = "field page return formfCoi"
fileprivate let dream_sendTitle:[Character] = ["n"]

/*: "onMissCoin" :*/
fileprivate let mainDataTextUrl:[Character] = ["o","n","M","i","s","s","C","o","i","n"]

/*: "acceptCall" :*/
fileprivate let kMakeText:String = "acceview"
fileprivate let mainAddText:String = "L"

/*: "stopCall" :*/
fileprivate let kDiskInputSucceedText:[Character] = ["s","t","o","p","C","a","l"]
fileprivate let appBottomContent:String = "model"

/*: "requestCall" :*/
fileprivate let userAreaMessage:String = "timee"
fileprivate let m_suiteTitle:String = "quenormal"

/*: "[Invite you to a video call...]" :*/
fileprivate let userWhiteKey:[UInt8] = [0x5d,0x2e,0x2e,0x2e,0x6c,0x6c,0x61,0x63,0x20,0x6f,0x65,0x64,0x69,0x76,0x20,0x61,0x20,0x6f,0x74,0x20,0x75,0x6f,0x79,0x20,0x65,0x74,0x69,0x76,0x6e,0x49,0x5b]

/*: "type" :*/
fileprivate let kViewSourceMsg:[Character] = ["t","y","p","e"]

/*: "[Invite you to a voice call...]" :*/
fileprivate let k_picContent:[UInt8] = [0x28,0x3a,0x1d,0x5,0x1a,0x7,0x16,0x53,0xa,0x1c,0x6,0x53,0x7,0x1c,0x53,0x12,0x53,0x5,0x1c,0x1a,0x10,0x16,0x53,0x10,0x12,0x1f,0x1f,0x5d,0x5d,0x5d,0x2e]

private func replyView(moment num: UInt8) -> UInt8 {
    return num ^ 115
}

/*: "uid" :*/
fileprivate let mainVideoName:String = "uiinside"

/*: "nickname" :*/
fileprivate let userLayerTitle:String = "naddkna"
fileprivate let noti_upUrl:String = "interactione"

/*: "headPic" :*/
fileprivate let noti_kindMessage:String = "top"
fileprivate let k_modeFormat:String = "bottom self state playeadPic"

/*: "onCallResume" :*/
fileprivate let show_becomeRawTitle:String = "onCatext pan with one leading"
fileprivate let m_beautyId:[Character] = ["s","u","m","e"]

/*: "cancelCall" :*/
fileprivate let k_scaleMessage:String = "namenc"
fileprivate let dreamDisplayStr:String = "op"

/*: "refuseCall" :*/
fileprivate let mViewKey:[Character] = ["r","e","f","u","s","e"]
fileprivate let user_gesturePath:String = "Calllabel height"

/*: "onCallMotivation" :*/
fileprivate let k_intervalMessage:String = "one view head atonCallMot"
fileprivate let dream_infoContent:[Character] = ["i","v","a","t","i","o","n"]

/*: "onLiveWarning" :*/
fileprivate let app_requestUrl:[Character] = ["o","n","L","i","v","e","W","a","r","n","i","n","g"]

/*: "reason" :*/
fileprivate let userSizeName:[Character] = ["r","e","a","s","o","n"]

/*: "time" :*/
fileprivate let mAddUrl:String = "readingme"

/*: "onStopLive" :*/
fileprivate let mGiftText:[Character] = ["o","n","S","t","o"]
fileprivate let kNameColorValue:[Character] = ["p","L","i","v","e"]

/*: "onSendProgramGift" :*/
fileprivate let mPathManagerViewMsg:[Character] = ["o","n","S","e","n","d","P"]
fileprivate let kPlayText:[Character] = ["r","o","g","r","a","m","G","i","f","t"]

/*: "onGameFs" :*/
fileprivate let noti_labelEndValue:[Character] = ["o","n","G","a","m","e"]
fileprivate let mainTouchData:[Character] = ["F","s"]

/*: "broadStatus" :*/
fileprivate let mainPartyName:[Character] = ["b","r","o","a","d","S","t","a","t","u"]
fileprivate let main_willInfoFormat:[Character] = ["s"]

/*: "onCallChatMsg" :*/
fileprivate let userScaleTitle:String = "onCallgift video"

/*: "onNotificationPopupWin" :*/
fileprivate let appOfLeadingId:String = "onNoshow selected"
fileprivate let app_rowValue:String = "atiback"
fileprivate let noti_frameListFormat:String = "Winapp text"

/*: "onUserBlock" :*/
fileprivate let k_buttonMsg:[Character] = ["o","n"]
fileprivate let k_somethingFormat:String = "add change viewUserBlock"

/*: "freeCallWin" :*/
fileprivate let show_sharedText:[Character] = ["f","r","e","e","C","a","l"]
fileprivate let notiBindValue:String = "index titlelWin"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  PublishDataSocketDelegate.swift
//  AbroadTalking
//
//  Created by young on 2022/9/13.
//

//: import SocketRocket
import SocketRocket
//: import UIKit
import UIKit

// 保活心跳包ping的时间间隔
//: let SOCKET_PING_INTERVAL = 60.0
let dreamClickData = 60.0
// 视频通话心跳包时间间隔
//: let SOCKET_CALL_INTERVAL = 20.0
let k_showKey = 20.0
// 直播心跳包时间间隔
//: let SOCKET_LIVE_INTERVAL = 20.0
let show_userErrorMessage = 20.0
// 失败重试次数
//: let SOCKET_ADDR_RETRY_COUNT = 3
let app_messageKey = 3
// socket连接超时时间
//: let SOCKET_MANAGER_CONNECT_TIMEOUT = 30.0
let dreamInformationUrl = 30.0
// socket连接成功缓存key
//: let SOCKET_ADDR_KEY = "SOCKET_ADDR_KEY"
let app_nameData = (user_modelTitle.uppercased() + "T_ADDR_" + showHiddenName.replacingOccurrences(of: "user", with: "Y"))

// 错误码
//: let SOCKET_MANAGER_UNCONNECTED_CODE = 5001
let show_bottomDepthTitle = 5001
//: let SOCKET_MANAGER_SELF_BUSY_CODE   = 5002
let showSensePath = 5002
//: let SOCKET_WITHOUT_NET_CODE         = 5003
let mKeyTitle = 5003
//: let SOCKET_MANAGER_USER_LOGOUT_CLOSED_CODE = 1002
let notiInformationMsg = 1002

// 接收方收到音视频通话通知
//: public let SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION = NSNotification.Name(rawValue: "SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION")
public let dreamPartyIdent = NSNotification.Name(rawValue: String(bytes: dream_rateMsg.map{premiumView(center: $0)}, encoding: .utf8)!)
// 游戏飘屏通知
//: public let RECIVE_FLOATSCREEN_NOTIFICATION = NSNotification.Name(rawValue: "RECIVE_FLOATSCREEN_NOTIFICATION")
public let mainScreenIdent = NSNotification.Name(rawValue: String(bytes: dream_theTitle.reversed(), encoding: .utf8)!)

// MARK: - protocol

// MARK: - socket失败代理

/// socket收到失败消息
//: protocol TalkingSocketManagerErrorDelegate: NSObjectProtocol {
protocol DelegateObjectProtocol: NSObjectProtocol {
    /// socket收到失败消息
    /// - Parameters:
    ///   - errorNo: 失败编码
    ///   - cmd: 命令
    ///   - msg: 失败信息
    ///  - data: 数据
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any])
    func reverberation(errorNo: Int, cmd: String, msg: String, data: [String: Any])
}

// MARK: - 视频速配代理

/// 视频速配代理（男）
//: protocol SocketManagerVideoMatchDelegate: NSObjectProtocol {
protocol RansackingObjectProtocol: NSObjectProtocol {
    /// 视频速配成功
    /// - Parameter data: 速配数据
    //: func socket_match_onVideoMatch(data: [String: Any])
    func socket(data: [String: Any])

    /// 匹配成功，可以拨打通话
    /// - Parameter data: 速配数据
    //: func socket_match_onRequestCall(data: [String: Any])
    func dataOf(data: [String: Any])
}

// MARK: - 音视频通话代理

/// 视频通话【拨打方】代理
//: protocol SocketManagerVideoChatCallerDelegate: NSObjectProtocol {
protocol TelevisionCallerDelegate: NSObjectProtocol {
    /// 接收方接受了来电
    /// - Parameter data: 数据
    //: func socket_videoChat_acceptCall(data: [String: Any])
    func occurStatus(data: [String: Any])

    /// 接收方挂断了通话（通话中）
    /// - Parameter data: 数据
    //: func socket_videoChat_stopCall(data: [String: Any])
    func inner(data: [String: Any])

    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_cancelCall(data: [String: Any])
    func mentionExecute(data: [String: Any])

    /// 通话余额不足提醒
    /// - Parameters:
    ///   - msg: 提醒信息
    ///   - data: 数据
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any])
    func surcharge(msg: String, data: [String: Any])
}

/// 视频通话【接收方】代理
//: @objc protocol SocketManagerVideoChatCalledPartyDelegate: NSObjectProtocol {
@objc protocol ValueLevelObjectProtocol: NSObjectProtocol {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_refuseCall(data: [String: Any])
    func chat(data: [String: Any])

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    /// - Parameter data: 数据
    //: func socket_videoChat_onAcceptCall(data: [String: Any])
    func viewAppear(data: [String: Any])

    /// 女性通话积分激励动效展示
    /// - Parameters:
    ///   - data: 数据
    //: @objc optional func socket_videoChat_onCallMotivation(data: [String: Any])
    @objc optional func reportSeek(data: [String: Any])
}

//: class TalkingSocketManager: NSObject {
class PublishDataSocketDelegate: NSObject {
	var toEnable: Bool = true
	var recommendQuantity: Double = -66.9

    //: enum SocketRetryType: String {
    enum WineWaiterType: String {
        //: case AppLogin = "用户登录"
        case AppLogin = "用户登录"
        //: case NetWork = "网络重连"
        case NetWork = "网络重连"
        //: case EnterForeground = "回到APP"
        case EnterForeground = "回到APP"
        //: case SocketFail = "连接失败"
        case SocketFail = "连接失败"
        //: case SocketClose = "连接关闭"
        case SocketClose = "连接关闭"
        //: case SocketSendMsg = "发送消息失败"
        case SocketSendMsg = "发送消息失败"
    }

    /// 音视频最小化
    //: var currentDataVC: AnyObject?
    var currentDataVC: AnyObject? // 当前正在通话的音视频视图
    //: var videoMiniPosition = CGPoint(
    var videoMiniPosition = CGPoint( // 音视频小窗口默认位置
        //: x: LanguageManager.shared.direction == .rightToLeft ? 0:ScreenWidth,
        x: PresentReactiveCompatible.shared.direction == .rightToLeft ? 0 : app_screenFailMarginFormat,
        //: y: StatusBarHeight+190
        y: app_topMsg + 190
    )
    //: var liveMiniPosition = CGPoint(
    var liveMiniPosition = CGPoint( // 直播小窗口默认位置
        //: x: LanguageManager.shared.direction == .rightToLeft ? 0:ScreenWidth,
        x: PresentReactiveCompatible.shared.direction == .rightToLeft ? 0 : app_screenFailMarginFormat,
        //: y: StatusBarHeight+190
        y: app_topMsg + 190
    )
    //: var partyMiniPosition = CGPoint(
    var partyMiniPosition = CGPoint( // 语聊房小窗口默认位置
        //: x: LanguageManager.shared.direction == .rightToLeft ? 0:ScreenWidth,
        x: PresentReactiveCompatible.shared.direction == .rightToLeft ? 0 : app_screenFailMarginFormat,
        //: y: StatusBarHeight+500
        y: app_topMsg + 500
    )
    //: var containerView: UIView?
    var containerView: UIView? // 小窗口容器

    /// delegate
    //: weak var errorDelegate: TalkingSocketManagerErrorDelegate?
    weak var errorDelegate: DelegateObjectProtocol? // error !=0 的异常处理
    //: weak var videoMatchDelegate: SocketManagerVideoMatchDelegate?
    weak var videoMatchDelegate: RansackingObjectProtocol? // 视频通话速配
    //: weak var callerDelegate: SocketManagerVideoChatCallerDelegate?
    weak var callerDelegate: TelevisionCallerDelegate? // 视频通话【拨打方】
    //: weak var calledPartyDelegate: SocketManagerVideoChatCalledPartyDelegate?
    weak var calledPartyDelegate: ValueLevelObjectProtocol? // 视频通话【接收方】

    //: private var webSocket: SRWebSocket?
    private var webSocket: SRWebSocket?
    //: private var keepAliveTimer: Timer?
    private var keepAliveTimer: Timer? // socket保活心跳包
    //: private var connectUrlStr: String?
    private var connectUrlStr: String? // socket连接地址
    //: private var linkIndex = 0
    private var linkIndex = 0 // 当前连接地址索引
    //: private var linkAddrArr: [String]?
    private var linkAddrArr: [String]? // 连接地址数组
    //: private var failureCount = 0
    private var failureCount = 0 // 连接失败次数
    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()

    /// talking
    //: var needReconnect = false
    var needReconnect = false // 断网重连socket
    
    @objc dynamic var isData = false // 是否视频通话中
    //: @objc dynamic var isCalling = false
    @objc dynamic var isCalling = false // 是否呼叫中
    //: private var talkTimer: Timer?
    private var talkTimer: Timer? // 视频通话心跳包
    //: private var liveTimer: Timer?
    private var liveTimer: Timer? // 直播心跳包
    //: private var unHandleCallData: [String: Any]?
    private var unHandleCallData: [String: Any]? // 在后台收到的接听消息
    //: private var cancelCall = false
    private var cancelCall = false // 拨打方是否停止呼叫（还未打通）
    //: var isFreeCall = false
    var isFreeCall = false // 是否发起免费通话

    // singleton
    //: static let shared = TalkingSocketManager()
    static let shared = PublishDataSocketDelegate()
    //: override func copy() -> Any { return self }
    override func copy() -> Any { return self }
    //: override func mutableCopy() -> Any { return self }
    override func mutableCopy() -> Any { 
	if let containerView = containerView {

            if (containerView.layer.contentsRect.size.height != 1) && (containerView.layer.anchorPointZ != 0) {
                //: SWIFT_CUSTOM_DANGER_File_Call
                let listParty = UserView(frame: containerView.bounds)

            
            listParty.styleClose = { [self] labelDoing in
            self.toEnable = labelDoing
            
            var listParty = cancelCall
                listParty = false
                listParty = false
            if listParty != self.toEnable {
                self.toEnable = listParty
            }
            
                self.toEnable = false
                self.toEnable = false
            return self.toEnable
            }
            listParty.geneticMutationTotal = { [self] frameInterval in
            self.recommendQuantity = frameInterval
            
                self.recommendQuantity += 1
                if self.recommendQuantity < 75 {
                    self.recommendQuantity = self.recommendQuantity - 1
                }
            return self.recommendQuantity
            }
                containerView.addSubview(listParty)
            }

	}

		return self }
    //: private override init() {
    override private init() {
        //: super.init()
        super.init()
        // 网络状态监听
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(reachabilityChanged(note:)),
                                               selector: #selector(titleNote(note:)),
                                               //: name: .reachabilityChanged,
                                               name: .reachabilityChanged,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(checkWebsocket),
                                               selector: #selector(makeWebsocket),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        // 音视频呼叫时
        //: self.rx
        self.rx
            //: .observeWeakly(Bool.self, "isCalling")
            .observeWeakly(Bool.self, (String(m_fillSendIdent)))
            //: .subscribe(onNext: {(value) in
            .subscribe(onNext: { value in
                //: if value == true {
                if value == true {
                    // 上报女性用户busy状态
                    //: (TalkingApplication.shared as! TalkingApplication).destroyTimer()
                    (CommentKingfisherCompatible.shared as! CommentKingfisherCompatible).titlePage()
                }

                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)

        // 音视频通话时
        //: self.rx
        self.rx
            //: .observeWeakly(Bool.self, "isTalking")
            .observeWeakly(Bool.self, "isData")
            //: .subscribe(onNext: { (value) in
            .subscribe(onNext: { value in
                //: if value == true {
                if value == true {
                    // 上报女性用户busy状态
                    //: (TalkingApplication.shared as! TalkingApplication).destroyTimer()
                    (CommentKingfisherCompatible.shared as! CommentKingfisherCompatible).titlePage()
                }

                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)
    }

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }
}

// MARK: - SRWebSocketDelegate

//: extension TalkingSocketManager: SRWebSocketDelegate {
extension PublishDataSocketDelegate: SRWebSocketDelegate {
    //: func webSocketDidOpen(_ webSocket: SRWebSocket!) {
    func webSocketDidOpen(_ webSocket: SRWebSocket!) {
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }
        //: UploadLogTool.writeLog(msg: "SOCKET 连接成功.")
        LabelLogTool.fillInLine(msg: (String(kSexText)))
        //: sendPing()
        viewModePing()
        //: NotificationCenter.default.post(name: SOCKET_IS_OPEN_NOTIFICATION,
        NotificationCenter.default.post(name: dreamEnvironmentUrl,
                                        //: object: nil,
                                        object: nil,
                                        //: userInfo: nil)
                                        userInfo: nil)
        // 还原断网重连状态
        //: self.needReconnect = false
        self.needReconnect = false

        //: self.failureCount = 0
        self.failureCount = 0

        // 保存成功连接的记录
        //: if self.linkIndex < self.linkAddrArr?.count ?? 0 {
        if self.linkIndex < self.linkAddrArr?.count ?? 0 {
            //: UserDefaults.standard.set(self.linkAddrArr?[self.linkIndex], forKey: SOCKET_ADDR_KEY)
            UserDefaults.standard.set(self.linkAddrArr?[self.linkIndex], forKey: app_nameData)
        }
        // 创建音视频最小化窗口容器
        //: mini_createContainerView()
        chessPlayerDestroy()
    }

    //: func webSocket(_ webSocket: SRWebSocket!, didFailWithError error: Error!) {
    func webSocket(_ webSocket: SRWebSocket!, didFailWithError error: Error!) {
        //: UploadLogTool.writeLog(msg: "SOCKET 连接失败, error:\(error.localizedDescription).")
        LabelLogTool.fillInLine(msg: (k_lastErrorIndicatorName.uppercased() + "T 连接\u{5931}败, " + String(user_pathValue.suffix(6))) + "\(error.localizedDescription).")
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }

        //: guard let status = PathAppManager.share.reachability?.connection, status != .unavailable else {
        guard let status = PathAppManager.share.reachability?.connection, status != .unavailable else {
            //: return
            return
        }
        //: if self.failureCount == SOCKET_ADDR_RETRY_COUNT {
        if self.failureCount == app_messageKey {
            //: self.failureCount = 0
            self.failureCount = 0
            //: self.linkIndex = (self.linkIndex == 0) ? 1:0
            self.linkIndex = (self.linkIndex == 0) ? 1 : 0
            //: self.connectUrlStr = getWebSocketPath(isRetry: true)
            self.connectUrlStr = ambagesRetry(isRetry: true)
            //: UploadLogTool.writeLog(msg: "SOCKET 失败\(SOCKET_ADDR_RETRY_COUNT)次, 切换过Socket域名重新尝试，地址：\(connectUrlStr ?? "").")
            LabelLogTool.fillInLine(msg: (String(mCommentColorMessage.suffix(7)) + mToLabKey.capitalized) + "\(app_messageKey)" + (m_valueIdent + m_holdStr + show_burnBindLetId) + "\(connectUrlStr ?? "").")
            //: } else {
        } else {
            //: self.failureCount += 1
            self.failureCount += 1
        }
        //: self.reconnectWebSocket(timeInterval: 2, type: .SocketFail)
        self.lineType(timeInterval: 2, type: .SocketFail)
    }

    //: func webSocket(_ webSocket: SRWebSocket!, didReceiveMessage message: Any!) {
    func webSocket(_ webSocket: SRWebSocket!, didReceiveMessage message: Any!) {
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }
        //: self.handleReceiveMessage(message: message as? String)
        self.equalImage(message: message as? String)
    }

    //: func webSocket(_ webSocket: SRWebSocket!, didCloseWithCode code: Int, reason: String!, wasClean: Bool) {
    func webSocket(_ webSocket: SRWebSocket!, didCloseWithCode code: Int, reason: String!, wasClean _: Bool) {
        //: UploadLogTool.writeLog(msg: "SOCKET 已关闭, code:\(code), reason:\(String(describing: reason)).")
        LabelLogTool.fillInLine(msg: (noti_complaintUrl.uppercased() + mByValue + ", code:") + "\(code)" + (String(main_viewTitle) + String(m_headName)) + "\(String(describing: reason)).")
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }

        //: self.closeWebSocket(userLoginOut: false)
        self.comeClose(userLoginOut: false)
        //: if code == SOCKET_MANAGER_USER_LOGOUT_CLOSED_CODE {
        if code == notiInformationMsg {
            //: return
            return
        }
        //: if let status = PathAppManager.share.reachability?.connection, status != .unavailable {
        if let status = PathAppManager.share.reachability?.connection, status != .unavailable {
            //: self.reconnectWebSocket(timeInterval: 5, type: .SocketClose)
            self.lineType(timeInterval: 5, type: .SocketClose)
        }
    }

    /// 获取socket地址
    //: func getWebSocketPath(isRetry: Bool) -> String? {
    func ambagesRetry(isRetry: Bool) -> String? {
        //: var linkAddr = ""
        var linkAddr = ""
        //: self.linkAddrArr = PathAppManager.share.appConfigMode.ws
        self.linkAddrArr = PathAppManager.share.appConfigMode.ws
        //: if self.linkAddrArr == nil || self.linkAddrArr?.count == 0 {
        if self.linkAddrArr == nil || self.linkAddrArr?.count == 0 {
            //: self.linkAddrArr = defaultWSArr()
            self.linkAddrArr = mass()
        }
        //: let lastAddr = UserDefaults.standard.string(forKey: SOCKET_ADDR_KEY)
        let lastAddr = UserDefaults.standard.string(forKey: app_nameData)
        //: if lastAddr == nil || isRetry {
        if lastAddr == nil || isRetry {
            //: if let temLinkAddr = self.linkAddrArr?[safe: self.linkIndex] {
            if let temLinkAddr = self.linkAddrArr?[safe: self.linkIndex] {
                //: linkAddr = temLinkAddr
                linkAddr = temLinkAddr
                //: } else {
            } else {
                //: self.linkIndex = 0
                self.linkIndex = 0
                //: linkAddr = self.linkAddrArr!.first!
                linkAddr = self.linkAddrArr!.first!
            }
            //: } else {
        } else {
            //: if let index = self.linkAddrArr!.firstIndex(of: lastAddr!) {
            if let index = self.linkAddrArr!.firstIndex(of: lastAddr!) {
                //: self.linkIndex = index
                self.linkIndex = index
                //: linkAddr = lastAddr!
                linkAddr = lastAddr!
                //: }else{
            } else {
                //: self.linkIndex = 0
                self.linkIndex = 0
                //: linkAddr = self.linkAddrArr!.first!
                linkAddr = self.linkAddrArr!.first!
            }
        }

        //: let uid = PathAppManager.share.loginUid
        let uid = PathAppManager.share.loginUid
        //: let sid = PathAppManager.share.loginSessionId
        let sid = PathAppManager.share.loginSessionId
        //: let address = "ws://\(linkAddr)/?sid=\(sid)&uid=\(uid)&platform=iphone&version=\(AppNetVersion)&packageId=\(PackageID)"
        let address = "ws://\(linkAddr)" + (String(m_labName.suffix(6))) + "\(sid)" + (String(app_makeImageValue.suffix(5))) + "\(uid)" + (String(appErrorUrl.prefix(5)) + "form=ip" + String(kBlockPath) + String(appCornerKey.suffix(5)) + "ion=") + "\(m_saltIdent)" + (String(main_awakeFormat.suffix(5)) + "ageId=") + "\(dreamAgeKey)"
        //: return address
        return address
    }

    /// 默认地址
    //: func defaultWSArr() -> [String] {
    func mass() -> [String] {
        //: return Environment_Formal ? ["1v1server.\(ReplaceUrlDomain).com:7766"] : ["43.129.185.194:7767"]
        return app_warnKey ? [(String(showOfPath.suffix(6)) + "ver.") + "\(dreamRecordPath)" + (String(userAMainKey.prefix(3)) + String(showItemMsg.suffix(6)))] : [(show_writeToId.replacingOccurrences(of: "model", with: "43") + "9.18" + k_likeRemoteValue.replacingOccurrences(of: "military", with: "5") + String(notiAtText))]
    }
}

// MARK: - NotificationCenter

//: extension TalkingSocketManager {
extension PublishDataSocketDelegate {
    /// 网络监听通知
    //: @objc func reachabilityChanged(note: Notification) {
    @objc func titleNote(note: Notification) {
        //: let reachability = note.object as? Reachability
        let reachability = note.object as? Reachability
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            // 断网, 关闭socket
            //: UploadLogTool.writeLog(msg: "SOCKET 网络reachabilityChanged监听，断网关闭.")
            LabelLogTool.fillInLine(msg: String(bytes: main_layerName.map{meImage(main: $0)}, encoding: .utf8)!)
            //: self.closeWebSocket(userLoginOut: false)
            self.comeClose(userLoginOut: false)
            //: } else {
        } else {
            // 网络原因重连
            //: self.reconnectWebSocket(timeInterval: 2.0, type: .NetWork)
            self.lineType(timeInterval: 2.0, type: .NetWork)
        }
    }

    /// 检测socket链接状态
    //: @objc func checkWebsocket() {
    @objc func makeWebsocket() {
        //: self.reconnectWebSocket(timeInterval: 2.0, type: .EnterForeground)
        self.lineType(timeInterval: 2.0, type: .EnterForeground)
        // 处理通话逻辑
        //: self.handleCallData()
        self.handleTitleData()
    }
}

// MARK: - Event

//: extension TalkingSocketManager {
extension PublishDataSocketDelegate {
    /// socket保活心跳包
    //: private func sendPing() {
    private func viewModePing() {
        //: self.keepAliveTimer = Timer.scheduledTimer(withTimeInterval: SOCKET_PING_INTERVAL, block: { timer in
        self.keepAliveTimer = Timer.scheduledTimer(withTimeInterval: dreamClickData, block: { _ in
            //: self.sendMessage(info: ["cmd": "ping"])
            self.birthday(info: [(show_sizeData.replacingOccurrences(of: "trigger", with: "d")): (dream_messagePath.replacingOccurrences(of: "play", with: "pi"))])
            //: }, repeats: true)
        }, repeats: true)
    }

    /// 开启视频通话心跳包
    //: private func startTalkPing() {
    private func bandyPing() {
        //: endTalkingPing()
        actionSign()

        //: self.talkTimer = Timer.scheduledTimer(withTimeInterval: SOCKET_CALL_INTERVAL, block: {  timer in
        self.talkTimer = Timer.scheduledTimer(withTimeInterval: k_showKey, block: { _ in
            /// 不在通话中，不在音视频通话页，没有开启最小化时
            //: if !self.isData || (self.currentViewController()?.isKind(of: TalkingVideoChatViewController.self) == false  && self.currentViewController()?.isKind(of: TalkingVoiceChatViewController.self) == false ) && (self.currentDataVC?.isKind(of: TalkingVideoChatViewController.self) == false  && self.currentDataVC?.isKind(of: TalkingVoiceChatViewController.self) == false ) {
            if !self.isData || (self.withTo()?.isKind(of: InsideTopReactiveCompatible.self) == false && self.withTo()?.isKind(of: ListObserverDelegate.self) == false) && (self.currentDataVC?.isKind(of: InsideTopReactiveCompatible.self) == false && self.currentDataVC?.isKind(of: ListObserverDelegate.self) == false) {
                //: return
                return
            }
            //: self.sendMessage(info: ["cmd": "callHeartbeat"])
            self.birthday(info: [(show_sizeData.replacingOccurrences(of: "trigger", with: "d")): (String(app_upUrl.suffix(5)) + "eartb" + userMarginTaskPath.replacingOccurrences(of: "add", with: "a"))])
            //: }, repeats: true)
        }, repeats: true)
    }

    /// 结束视频通话心跳包
    //: func endTalkingPing() {
    func actionSign() {
        //: if self.talkTimer != nil {
        if self.talkTimer != nil {
            //: self.talkTimer?.invalidate()
            self.talkTimer?.invalidate()
            //: self.talkTimer = nil
            self.talkTimer = nil
        }
    }

    /// 处理通话逻辑
    //: func handleCallData() {
    func handleTitleData() {
        //: if self.cancelCall == true {
        if self.cancelCall == true { // 拨打方已停止呼叫
            //: self.cancelCall = false
            self.cancelCall = false
            //: self.unHandleCallData = nil
            self.unHandleCallData = nil
            //: return
            return
        }

        //: if self.unHandleCallData != nil {
        if self.unHandleCallData != nil {
            //: let recievedTime = self.unHandleCallData!["recievedDate"] as! Date
            let recievedTime = self.unHandleCallData![(String(appDeadlineShowFormat) + String(main_targetMessage))] as! Date
            //: let currentTime = Date()
            let currentTime = Date()
            //: if currentTime.timeIntervalSince(recievedTime) < VIDEO_1v1_RECIEVED_CALL_TIMEOUT {
            if currentTime.timeIntervalSince(recievedTime) < user_successPath {
                //: PathAppManager.share.recieve1v1TalkRequest(info: self.unHandleCallData!)
                PathAppManager.share.randomInfo(info: self.unHandleCallData!)
            }
            //: self.unHandleCallData = nil
            self.unHandleCallData = nil
        }
    }
}

// MARK: - 直播心跳包

//: extension TalkingSocketManager {
extension PublishDataSocketDelegate {
    /// 开启直播心跳包
    //: func startLivePing() {
    func underDisplay() {
        //: endLivePing()
        attributePing()
        //: self.liveTimer = Timer.scheduledTimer(withTimeInterval: SOCKET_LIVE_INTERVAL, block: { timer in
        self.liveTimer = Timer.scheduledTimer(withTimeInterval: show_userErrorMessage, block: { _ in
            //: self.sendMessage(info: ["cmd": "liveHeartBeat"])
            self.birthday(info: [(show_sizeData.replacingOccurrences(of: "trigger", with: "d")): (String(mFillUrl) + String(userStatusPath))])
            //: }, repeats: true)
        }, repeats: true)
    }

    /// 结束直播心跳包
    //: func endLivePing() {
    func attributePing() {
        //: if self.liveTimer != nil {
        if self.liveTimer != nil {
            //: self.liveTimer?.invalidate()
            self.liveTimer?.invalidate()
            //: self.liveTimer = nil
            self.liveTimer = nil
        }
    }
}

// MARK: - 音视频窗口最小化

//: extension TalkingSocketManager {
extension PublishDataSocketDelegate {
    /// 创建窗口最小化容器
    //: private func mini_createContainerView() {
    private func chessPlayerDestroy() {
        //: guard self.containerView == nil else { return }
        guard self.containerView == nil else { return }

        //: let windowView = TalkingMiniContainerView()
        let windowView = MullionView()
        //: let keyWindow = FacultyThen.getWindow()
        let keyWindow = FacultyThen.titleBack()
        //: windowView.frame = keyWindow.bounds
        windowView.frame = keyWindow.bounds
        //: windowView.backgroundColor = .clear
        windowView.backgroundColor = .clear
        //: keyWindow.addSubview(windowView)
        keyWindow.addSubview(windowView)
        //: self.containerView = windowView
        self.containerView = windowView
        //: self.containerView?.isHidden = !self.isData
        self.containerView?.isHidden = !self.isData
    }

    /// 移除窗口最小化容器
    //: private func mini_removeContainerView() {
    private func pod() {
        //: guard self.containerView != nil else { return }
        guard self.containerView != nil else { return }
        // 防止抢登录时，用户还在直播中/语聊房
        //: guard TalkingLiveManager.shared().isLive == false else { return }
        guard CommentLiveManager.leaveBy().isLive == false else { return }
        //: guard TalkingVoiceRoomManager.shared().isParty == false else { return }
        guard VoxNameReactiveCompatible.listOf().isParty == false else { return }

        //: self.containerView?.removeAllSubviews()
        self.containerView?.removeAllSubviews()
        //: self.containerView?.removeFromSuperview()
        self.containerView?.removeFromSuperview()
        //: self.containerView = nil
        self.containerView = nil
    }
}

// MARK: - Socket

//: extension TalkingSocketManager {
extension PublishDataSocketDelegate {
    /// 关闭socket
    /// - Parameter userLoginOut: 是否是用户退出
    //: func closeWebSocket(userLoginOut: Bool) {
    func comeClose(userLoginOut: Bool) {
        //: printLog(message: "🔗 关闭 socket")
        printLog(message: (user_dataInputStr + app_modelStr.lowercased()))
        //: if self.webSocket != nil {
        if self.webSocket != nil {
            //: self.webSocket?.delegate = nil
            self.webSocket?.delegate = nil
            //: if userLoginOut == true {
            if userLoginOut == true {
                //: self.webSocket?.close(withCode: SOCKET_MANAGER_USER_LOGOUT_CLOSED_CODE, reason: "The user logs out")
                self.webSocket?.close(withCode: notiInformationMsg, reason: (String(appSucceedUrl) + String(dream_ofTitle.suffix(3)) + String(userGiftData.suffix(7))))
                //: } else {
            } else {
                //: self.webSocket?.close()
                self.webSocket?.close()
            }
            //: self.webSocket = nil
            self.webSocket = nil
        }

        //: if self.keepAliveTimer != nil {
        if self.keepAliveTimer != nil {
            //: self.keepAliveTimer?.invalidate()
            self.keepAliveTimer?.invalidate()
            //: self.keepAliveTimer = nil
            self.keepAliveTimer = nil
        }

        //: endTalkingPing()
        actionSign()

        //: if userLoginOut == true {
        if userLoginOut == true {
            //: self.connectUrlStr = nil
            self.connectUrlStr = nil
            // 销毁最小化窗口
            //: mini_removeContainerView()
            pod()
        }
    }

    /// 更新socket连接
    //: func updateWebSocket() {
    func modifyTopSocket() {
        //: self.connectUrlStr = TalkingSocketManager.shared.getWebSocketPath(isRetry: false)
        self.connectUrlStr = PublishDataSocketDelegate.shared.ambagesRetry(isRetry: false)
        //: self.reconnectWebSocket(timeInterval: 0, type: .AppLogin)
        self.lineType(timeInterval: 0, type: .AppLogin)
    }

    /// 重连socket
    //: private func reconnectWebSocket(timeInterval: TimeInterval, type: SocketRetryType) {
    private func lineType(timeInterval: TimeInterval, type: WineWaiterType) {
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + timeInterval) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + timeInterval) {
            //: UploadLogTool.writeLog(msg: "SOCKET \(type.rawValue)重连. 重试次数：\(self.failureCount), 地址：\(self.connectUrlStr ?? "").")
            //: self.connectWebSocket(urlStr: self.connectUrlStr)
            self.viewDoing(urlStr: self.connectUrlStr)
        }
    }

    /// 连接socket
    //: private func connectWebSocket(urlStr: String?) {
    private func viewDoing(urlStr: String?) {
        //: guard urlStr?.isEmpty == false else { return }
        guard urlStr?.isEmpty == false else { return }

        //: if urlStr == self.connectUrlStr && self.webSocket != nil && (self.webSocket?.readyState == .OPEN || self.webSocket?.readyState == .CONNECTING) {
        if urlStr == self.connectUrlStr, self.webSocket != nil, self.webSocket?.readyState == .OPEN || self.webSocket?.readyState == .CONNECTING {
            //: if (self.webSocket?.readyState == .OPEN) {
            if self.webSocket?.readyState == .OPEN {
                //: UploadLogTool.writeLog(msg: "SOCKET 连接正常 不需要重新连接.")
                LabelLogTool.fillInLine(msg: (noti_equalFormat.replacingOccurrences(of: "label", with: "ET") + String(mainPlayerText) + user_nameData))
                //: } else {
            } else {
                //: UploadLogTool.writeLog(msg: "SOCKET 正在连接 不需要重新连接.")
                LabelLogTool.fillInLine(msg: (String(dreamGiftStr.prefix(7)) + String(kTearStr)))
            }
            //: return
            return
        }

        //: self.connectUrlStr = urlStr
        self.connectUrlStr = urlStr

        //: var newStr = urlStr!
        var newStr = urlStr!
        //: if self.isData {
        if self.isData {
            //: newStr = newStr.appending("&resume=1")
            newStr = newStr.appending((String(mPopVideoIdent) + String(m_labId.suffix(7))))
        }
        //: newStr = newStr.appending("&lang=\(TalkingRequestAddrTool.share.interfaceLang)")
        newStr = newStr.appending((dreamAppText.capitalized + String(dreamLevelMessage)) + "\(LiveAddrThen.share.interfaceLang)")
        //: closeWebSocket(userLoginOut: false)
        comeClose(userLoginOut: false)

        //: let url = URL(string: newStr)
        let url = URL(string: newStr)
        //: var request = URLRequest(url: url!)
        var request = URLRequest(url: url!)
        //: request.timeoutInterval = SOCKET_MANAGER_CONNECT_TIMEOUT
        request.timeoutInterval = dreamInformationUrl
        //: self.webSocket = SRWebSocket(urlRequest: request)
        self.webSocket = SRWebSocket(urlRequest: request)
        //: self.webSocket?.delegate = self
        self.webSocket?.delegate = self
        //: self.webSocket?.open()
        self.webSocket?.open()
        //: UploadLogTool.writeLog(msg: "SOCKET 开始连接, 地址：\(newStr).")
        LabelLogTool.fillInLine(msg: (k_destroyIdent.replacingOccurrences(of: "up", with: "K") + "ET 开始" + user_upperId + user_positionStr.replacingOccurrences(of: "add", with: "：")) + "\(newStr).")
    }

    /// 发送消息
    /// - Parameter info: 消息体
    /// - Returns: 发送结果
    //: @discardableResult
    @discardableResult
    //: func sendMessage(info: [String: Any]?) -> Bool {
    func birthday(info: [String: Any]?) -> Bool {
        //: guard info != nil else { return false }
        guard info != nil else { return false }

        // 1. 判断是否有网络连接
        //: if PathAppManager.share.reachability?.connection == nil || PathAppManager.share.reachability?.connection == .unavailable {
        if PathAppManager.share.reachability?.connection == nil || PathAppManager.share.reachability?.connection == .unavailable {
            //: let info: [String: Any] = ["cmd": "onNetDisabled",
            let info: [String: Any] = [(show_sizeData.replacingOccurrences(of: "trigger", with: "d")): (String(mNameLayerId.suffix(8)) + show_instanceData.replacingOccurrences(of: "value", with: "d")),
                                       //: "errno": "\(SOCKET_WITHOUT_NET_CODE)",
                                       (mTopName.replacingOccurrences(of: "table", with: "r")): "\(mKeyTitle)",
                                       //: "data": [:]]
                                       (appCellSharedUrl.replacingOccurrences(of: "model", with: "at")): [:]]
            //: let data = try? JSONSerialization.data(withJSONObject: info, options: [])
            let data = try? JSONSerialization.data(withJSONObject: info, options: [])
            //: let message = String(data: data!, encoding: String.Encoding.utf8)
            let message = String(data: data!, encoding: String.Encoding.utf8)
            //: handleReceiveMessage(message: message)
            equalImage(message: message)
            //: return false
            return false
        }

        // 2. 尝试发送消息
        //: if self.webSocket?.readyState == SRReadyState.OPEN {
        if self.webSocket?.readyState == SRReadyState.OPEN {
            //: let data = try? JSONSerialization.data(withJSONObject: info!, options: [])
            let data = try? JSONSerialization.data(withJSONObject: info!, options: [])
            //: let message = String(data: data!, encoding: String.Encoding.utf8)
            let message = String(data: data!, encoding: String.Encoding.utf8)
            //: if info!["cmd"] as? String == "ping" {
            if info![(show_sizeData.replacingOccurrences(of: "trigger", with: "d"))] as? String == (dream_messagePath.replacingOccurrences(of: "play", with: "pi")) {
                //: UploadLogTool.writeLog(msg: "SOCKET 连接保活cmd: ping.")
                LabelLogTool.fillInLine(msg: (dreamBackgroundName.replacingOccurrences(of: "click", with: "SO") + " \u{8fde}接保" + String(k_collectionSumData.prefix(5)) + " ping."))
                //: } else if info!["cmd"] as? String == "callHeartbeat" {
            } else if info![(show_sizeData.replacingOccurrences(of: "trigger", with: "d"))] as? String == (String(app_upUrl.suffix(5)) + "eartb" + userMarginTaskPath.replacingOccurrences(of: "add", with: "a")) {
                //: UploadLogTool.writeLog(msg: "SOCKET trtc保活cmd: callHeartbeat.")
                LabelLogTool.fillInLine(msg: String(bytes: showGenderGiftStr.map{selfGift(top: $0)}, encoding: .utf8)!)
                //: } else if info!["cmd"] as? String == "liveHeartBeat" {
            } else if info![(show_sizeData.replacingOccurrences(of: "trigger", with: "d"))] as? String == (String(mFillUrl) + String(userStatusPath)) {
                //: UploadLogTool.writeLog(msg: "SOCKET live保活cmd: liveHeartBeat.")
                LabelLogTool.fillInLine(msg: String(bytes: dream_yearMessage.reversed(), encoding: .utf8)!)
            }
            //: self.webSocket?.send(message)
            self.webSocket?.send(message)
            //: return true
            return true

            //: } else {
        } else {
            //: if info!["cmd"] as? String == "ping" {
            if info![(show_sizeData.replacingOccurrences(of: "trigger", with: "d"))] as? String == (dream_messagePath.replacingOccurrences(of: "play", with: "pi")) {
                //: let info: [String: Any] = ["cmd": "onP",
                let info: [String: Any] = [(show_sizeData.replacingOccurrences(of: "trigger", with: "d")): (String(mainColorContent.prefix(3))),
                                           //: "errno": "\(SOCKET_MANAGER_UNCONNECTED_CODE)",
                                           (mTopName.replacingOccurrences(of: "table", with: "r")): "\(show_bottomDepthTitle)",
                                           //: "data": [:]]
                                           (appCellSharedUrl.replacingOccurrences(of: "model", with: "at")): [:]]
                //: let data = try? JSONSerialization.data(withJSONObject: info, options: [])
                let data = try? JSONSerialization.data(withJSONObject: info, options: [])
                //: let message = String(data: data!, encoding: String.Encoding.utf8)
                let message = String(data: data!, encoding: String.Encoding.utf8)
                //: handleReceiveMessage(message: message)
                equalImage(message: message)
            }
            //: self.reconnectWebSocket(timeInterval: 5.0, type: .SocketSendMsg)
            self.lineType(timeInterval: 5.0, type: .SocketSendMsg)
            //: return false
            return false
        }
    }

    /// 处理接收到的消息
    /// - Parameter message: 消息体
    //: private func handleReceiveMessage(message: String?) {
    private func equalImage(message: String?) {
        //: guard message != nil else { return }
        guard message != nil else { return }

        //: let json = JSON(parseJSON: message!)
        let json = JSON(parseJSON: message!)
        //: let cmdName = json["cmd"].stringValue
        let cmdName = json[(show_sizeData.replacingOccurrences(of: "trigger", with: "d"))].stringValue
        //: let errorNo = json["errno"].intValue
        let errorNo = json[(mTopName.replacingOccurrences(of: "table", with: "r"))].intValue
        //: let msg = json["msg"].stringValue
        let msg = json[(user_errorMessage.replacingOccurrences(of: "title", with: "g"))].stringValue
        //: let data = json["data"].dictionaryObject ?? [:]
        let data = json[(appCellSharedUrl.replacingOccurrences(of: "model", with: "at"))].dictionaryObject ?? [:]

        //: printLog(message: "🔗 处理消息：cmdName:\(cmdName), errNo:\(errorNo), msg:\(msg), data:\(data)")
        printLog(message: (noti_allId + user_giftId.replacingOccurrences(of: "list", with: "c") + String(show_appUrl.prefix(6))) + "\(cmdName)" + (String(noti_lengthMessage) + String(k_timeMsg.suffix(6))) + "\(errorNo)" + (String(k_arrayFormat)) + "\(msg)" + (String(m_upName.prefix(6)) + String(noti_colorFormat)) + "\(data)")

        // 检测是否需要恢复直播
        //: check_stopOrRestoreLive(cmdName)
        transitionLive(cmdName)

        // 处理异常
        //: guard errorNo == 0 else {
        guard errorNo == 0 else {
            //: UploadLogTool.writeLog(msg: "SOCKET 接收异常消息：cmdName:\(cmdName), errNo:\(errorNo), msg:\(msg), data:\(data).")
            LabelLogTool.fillInLine(msg: (userEqualUrl.replacingOccurrences(of: "fail", with: "C") + "ET \u{63a5}收异" + main_modelUrl + String(m_voiceMsg)) + "\(cmdName)" + (String(noti_lengthMessage) + String(k_timeMsg.suffix(6))) + "\(errorNo)" + (String(k_arrayFormat)) + "\(msg)" + (String(m_upName.prefix(6)) + String(noti_colorFormat)) + "\(data).")
            //: if errorNo == 444 {
            if errorNo == 444 {
                //: PathAppManager.share.loginUserMode.freeCallTimes = 0
                PathAppManager.share.loginUserMode.freeCallTimes = 0
                //: } else if errorNo == 60003, !PathAppManager.share.loginUserMode.isNaUser {
            } else if errorNo == 60003, !PathAppManager.share.loginUserMode.isNaUser {
                /// 没真人认证时，需要弹窗提示
                //: TalkingPopupWindowManager.shared.faceVerificationPopUpWindow()
                ConfinementWindowManager.shared.executeSize()
                //: return
                return
            }
            //: self.errorDelegate?.socket_didRecieveError(errorNo: errorNo, cmd: cmdName, msg: msg, data: data)
            self.errorDelegate?.reverberation(errorNo: errorNo, cmd: cmdName, msg: msg, data: data)
            //: return
            return
        }

        // 处理正常接收的消息
        //: if cmdName == "onReceivedGift" { // 礼物
        if cmdName == (String(dreamStorageData.prefix(5)) + k_layerSharedPath.replacingOccurrences(of: "sub", with: "e") + String(user_headIdent)) { // 礼物
            //: NotificationCenter.default.post(name: USER_RECIEVED_GIFT_NOTIFICATION, object: data)
            NotificationCenter.default.post(name: app_voiceUserName, object: data)

            //: } else if cmdName == "onVideoMatch" { // 视频速配
        } else if cmdName == (String(noti_userMessage) + String(userTabData)) { // 视频速配
            //: self.videoMatchDelegate?.socket_match_onVideoMatch(data: data)
            self.videoMatchDelegate?.socket(data: data)

            //: } else if cmdName == "onRequestCall" { // 拨打方发出requestCall回执
        } else if cmdName == (String(user_appMsg.prefix(6)) + app_indexFormat.lowercased() + String(show_cameraUrl)) { // 拨打方发出requestCall回执
            //: self.isCalling = true
            self.isCalling = true
            // 视频速配 || 音视频通话
            //: self.videoMatchDelegate?.socket_match_onRequestCall(data: data)
            self.videoMatchDelegate?.dataOf(data: data)

            //: } else if cmdName == "onCancelCall" { // 拨打方发出cancelCall回执，目前无需处理
        } else if cmdName == (String(main_pageWithTotalStr)) { // 拨打方发出cancelCall回执，目前无需处理
            //: } else if cmdName == "onRefuseCall" { // 接收方发送refuseCall回执，目前无需处理
        } else if cmdName == (String(app_domainKey.prefix(6)) + String(dreamMarginData.prefix(6))) { // 接收方发送refuseCall回执，目前无需处理
            //: } else if cmdName == "onAcceptCall" { // 接收方发送acceptCall回执
        } else if cmdName == (String(m_kitMessage.suffix(5)) + "eptCall") { // 接收方发送acceptCall回执
            //: self.isData = true
            self.isData = true
            //: self.isCalling = false
            self.isCalling = false
            //: startTalkPing()
            bandyPing()
            //: self.calledPartyDelegate?.socket_videoChat_onAcceptCall(data: data)
            self.calledPartyDelegate?.viewAppear(data: data)

            //: } else if cmdName == "onStopCall" { // 接收方发送stopCall回执
        } else if cmdName == (String(dream_errorGroupData.suffix(5)) + "pCal" + String(kLayerKey)) { // 接收方发送stopCall回执
            //: self.isData = false
            self.isData = false
            //: self.isCalling = false
            self.isCalling = false
            //: endTalkingPing()
            actionSign()
            //: if let freeCallTimes = data["freeCallTimes"] as? Int {
            if let freeCallTimes = data[(String(appCurrentPath.suffix(6)) + "llTim" + showImageIdent.replacingOccurrences(of: "track", with: "e"))] as? Int {
                //: PathAppManager.share.loginUserMode.freeCallTimes = freeCallTimes
                PathAppManager.share.loginUserMode.freeCallTimes = freeCallTimes
            }
            // 更新余额
            //: PathAppManager.share.loginUserMode.mf_coin = json["data"]["mfCoin"].stringValue
            PathAppManager.share.loginUserMode.mf_coin = json[(appCellSharedUrl.replacingOccurrences(of: "model", with: "at"))][(String(mPathIndexTitle.suffix(5)) + String(dream_sendTitle))].stringValue
            //: NotificationCenter.default.post(name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
            NotificationCenter.default.post(name: user_marginData, object: nil)
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: (TalkingApplication.shared as! TalkingApplication).femaleBusyStatusReport()
            (CommentKingfisherCompatible.shared as! CommentKingfisherCompatible).withTable()
            //: } else if cmdName == "onMissCoin" { // 余额不足提醒
        } else if cmdName == (String(mainDataTextUrl)) { // 余额不足提醒
            //: self.callerDelegate?.socket_videoChat_onMissCoin(msg: msg, data: data)
            self.callerDelegate?.surcharge(msg: msg, data: data)

            //: } else if cmdName == "acceptCall" { // 接收方接受了来电，改变状态为通话中,开始推拉流
        } else if cmdName == (kMakeText.replacingOccurrences(of: "view", with: "p") + "tCal" + mainAddText.lowercased()) { // 接收方接受了来电，改变状态为通话中,开始推拉流
            //: self.isData = true
            self.isData = true
            //: self.isCalling = false
            self.isCalling = false
            //: startTalkPing()
            bandyPing()
            //: self.callerDelegate?.socket_videoChat_acceptCall(data: data)
            self.callerDelegate?.occurStatus(data: data)

            //: } else if cmdName == "stopCall" { // 接收方/拨打方 挂断了通话
        } else if cmdName == (String(kDiskInputSucceedText) + appBottomContent.replacingOccurrences(of: "model", with: "l")) { // 接收方/拨打方 挂断了通话
            //: self.isData = false
            self.isData = false
            //: self.isCalling = false
            self.isCalling = false
            //: endTalkingPing()
            actionSign()
            //: self.callerDelegate?.socket_videoChat_stopCall(data: data)
            self.callerDelegate?.inner(data: data)
            // 更新余额
            //: PathAppManager.share.loginUserMode.mf_coin = json["data"]["mfCoin"].stringValue
            PathAppManager.share.loginUserMode.mf_coin = json[(appCellSharedUrl.replacingOccurrences(of: "model", with: "at"))][(String(mPathIndexTitle.suffix(5)) + String(dream_sendTitle))].stringValue

            //: isShowFreeStopCallPopUpView(data: data)
            am(data: data)
            //: NotificationCenter.default.post(name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
            NotificationCenter.default.post(name: user_marginData, object: nil)
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: (TalkingApplication.shared as! TalkingApplication).femaleBusyStatusReport()
            (CommentKingfisherCompatible.shared as! CommentKingfisherCompatible).withTable()
            //: } else if cmdName == "requestCall" { // 接收方收到了拨打方的申请通话
        } else if cmdName == (userAreaMessage.replacingOccurrences(of: "time", with: "r") + m_suiteTitle.replacingOccurrences(of: "normal", with: "s") + "tCall") { // 接收方收到了拨打方的申请通话
            //: self.isCalling = true
            self.isCalling = true
            // 发送通知，告知外界处理逻辑
            //: NotificationCenter.default.post(name: SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION, object: self)
            NotificationCenter.default.post(name: dreamPartyIdent, object: self)

            //: if UIApplication.shared.applicationState != .background {
            if UIApplication.shared.applicationState != .background { // 应用在前台
                //: PathAppManager.share.recieve1v1TalkRequest(info: data)
                PathAppManager.share.randomInfo(info: data)
                //: } else {
            } else {
                //: self.cancelCall = false
                self.cancelCall = false
                //: self.unHandleCallData = data
                self.unHandleCallData = data
                //: self.unHandleCallData?["recievedDate"] = Date()
                self.unHandleCallData?[(String(appDeadlineShowFormat) + String(main_targetMessage))] = Date()

                // 应用在后台，发送本地推送告知用户
                //: var body = "[Invite you to a video call...]".localized
                var body = String(bytes: userWhiteKey.reversed(), encoding: .utf8)!.localized
                //: if json["data"]["type"].intValue == 1 { // 音频
                if json[(appCellSharedUrl.replacingOccurrences(of: "model", with: "at"))][(String(kViewSourceMsg))].intValue == 1 { // 音频
                    //: body = "[Invite you to a voice call...]".localized
                    body = String(bytes: k_picContent.map{replyView(moment: $0)}, encoding: .utf8)!.localized
                }
                //: AppDelegateHelper.pushLocalNotification(uid: String(json["data"]["uid"].intValue),
                ShareDelegateHelper.makeUrl(uid: String(json[(appCellSharedUrl.replacingOccurrences(of: "model", with: "at"))][(mainVideoName.replacingOccurrences(of: "inside", with: "d"))].intValue),
                                          //: title: data["nickname"] as? String,
                                          title: data[(userLayerTitle.replacingOccurrences(of: "add", with: "ic") + noti_upUrl.replacingOccurrences(of: "interaction", with: "m"))] as? String,
                                          //: body: body,
                                          body: body,
                                          //: imageUrl: data["headPic"] as? String)
                                          imageUrl: data[(noti_kindMessage.replacingOccurrences(of: "top", with: "h") + String(k_modeFormat.suffix(6)))] as? String)
                // 播放来电音乐
                //: AppDelegateHelper.shared.startPlayBGM()
                ShareDelegateHelper.shared.output()
            }

            //: } else if cmdName == "onCallResume" { // 接收方/发送方 恢复通话中的页面，并开始推拉流
        } else if cmdName == (String(show_becomeRawTitle.prefix(4)) + "llRe" + String(m_beautyId)) { // 接收方/发送方 恢复通话中的页面，并开始推拉流
            //: self.isData = true
            self.isData = true
            //: self.isCalling = false
            self.isCalling = false
            //: startTalkPing()
            bandyPing()

            //: var resumeData = data
            var resumeData = data
            //: resumeData["onCallResume"] = "1"
            resumeData[(String(show_becomeRawTitle.prefix(4)) + "llRe" + String(m_beautyId))] = "1"
            //: PathAppManager.share.recieve1v1TalkResumeRequest(info: resumeData)
            PathAppManager.share.doingCell(info: resumeData)
            //: (TalkingApplication.shared as! TalkingApplication).destroyTimer()
            (CommentKingfisherCompatible.shared as! CommentKingfisherCompatible).titlePage()

            //: } else if cmdName == "cancelCall" { // 拨打方拒绝了通话，接收方收到该指令
        } else if cmdName == (k_scaleMessage.replacingOccurrences(of: "name", with: "ca") + "elCal" + dreamDisplayStr.replacingOccurrences(of: "op", with: "l")) { // 拨打方拒绝了通话，接收方收到该指令
            //: self.isData = false
            self.isData = false
            //: self.isCalling = false
            self.isCalling = false
            //: self.cancelCall = true
            self.cancelCall = true
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: endTalkingPing()
            actionSign()
            //: self.callerDelegate?.socket_videoChat_cancelCall(data: data)
            self.callerDelegate?.mentionExecute(data: data)
            // 应用在后台
            //: if UIApplication.shared.applicationState == .background {
            if UIApplication.shared.applicationState == .background {
                // 移除来电本地推送
                //: AppDelegateHelper.local_removeDeliveredNotification(identifier: String(json["data"]["uid"].intValue))
                ShareDelegateHelper.upModelIdentifier(identifier: String(json[(appCellSharedUrl.replacingOccurrences(of: "model", with: "at"))][(mainVideoName.replacingOccurrences(of: "inside", with: "d"))].intValue))
                // 停止播放来电音乐
                //: AppDelegateHelper.shared.stopPlayBGM()
                ShareDelegateHelper.shared.sendIn()
            }

            //: } else if cmdName == "refuseCall" { // 接收方拒绝了通话，拨打方收到该指令
        } else if cmdName == (String(mViewKey) + String(user_gesturePath.prefix(4))) { // 接收方拒绝了通话，拨打方收到该指令
            //: self.isData = false
            self.isData = false
            //: self.isCalling = false
            self.isCalling = false
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: endTalkingPing()
            actionSign()
            //: self.calledPartyDelegate?.socket_videoChat_refuseCall(data: data)
            self.calledPartyDelegate?.chat(data: data)

            //: } else if cmdName == "onCallMotivation" { // 女性通话收到的积分（用于展示赚钱激励动效）
        } else if cmdName == (String(k_intervalMessage.suffix(9)) + String(dream_infoContent)) { // 女性通话收到的积分（用于展示赚钱激励动效）
            //: if PathAppManager.share.loginUserMode.sex == Gender.female.rawValue {
            if PathAppManager.share.loginUserMode.sex == ConnectionClusterLiteral.female.rawValue {
                //: self.calledPartyDelegate?.socket_videoChat_onCallMotivation?(data: data)
                self.calledPartyDelegate?.reportSeek?(data: data)
            }

            //: } else if cmdName == "onLiveWarning" { // 直播违规警告
        } else if cmdName == (String(app_requestUrl)) { // 直播违规警告
            //: TalkingLiveManager.live_showWarningView(content: msg, reason: json["data"]["reason"].stringValue, time: json["data"]["time"].stringValue)
            CommentLiveManager.domiciliateAndTime(content: msg, reason: json[(appCellSharedUrl.replacingOccurrences(of: "model", with: "at"))][(String(userSizeName))].stringValue, time: json[(appCellSharedUrl.replacingOccurrences(of: "model", with: "at"))][(mAddUrl.replacingOccurrences(of: "reading", with: "ti"))].stringValue)

            //: } else if cmdName == "onStopLive" { // 停止直播
        } else if cmdName == (String(mGiftText) + String(kNameColorValue)) { // 停止直播
            //: TalkingLiveManager.shared().live_releaseAllResource()
            CommentLiveManager.leaveBy().define()

            //: } else if cmdName == "onSendProgramGift" { // 视频通话节目送礼成功tost
        } else if cmdName == (String(mPathManagerViewMsg) + String(kPlayText)) { // 视频通话节目送礼成功tost
            //: self.func__showStatusBarSuccessMsg(showMsg: msg)
            self.showMake(showMsg: msg)

            //: } else if cmdName == "onGameFs" { // 游戏中奖飘屏
        } else if cmdName == (String(noti_labelEndValue) + String(mainTouchData)) { // 游戏中奖飘屏
            //: let broadStatus = data["broadStatus"] as? Int ?? -1
            let broadStatus = data[(String(mainPartyName) + String(main_willInfoFormat))] as? Int ?? -1
            //: if PathAppManager.share.appStatus == AppSkinStatus.normal.rawValue {
            if PathAppManager.share.appStatus == AssemblageHashable.normal.rawValue {
                //: if broadStatus == 1 {
                if broadStatus == 1 {
                    //: NotificationCenter.default.post(name: RECIVE_FLOATSCREEN_NOTIFICATION, object: self, userInfo: data)
                    NotificationCenter.default.post(name: mainScreenIdent, object: self, userInfo: data)
                    //: NotificationCenter.default.post(name: USER_RECIEVED_GIFT_NOTIFICATION, object: data)
                    NotificationCenter.default.post(name: app_voiceUserName, object: data)
                    //: } else if broadStatus == -1 {
                } else if broadStatus == -1 {
                    //: NotificationCenter.default.post(name: RECIVE_FLOATSCREEN_NOTIFICATION, object: self, userInfo: data)
                    NotificationCenter.default.post(name: mainScreenIdent, object: self, userInfo: data)
                }
            }

            //: } else if cmdName == "onCallChatMsg" { // 收到视频通话中的聊天消息
        } else if cmdName == (String(userScaleTitle.prefix(6)) + "ChatMsg") { // 收到视频通话中的聊天消息
            //: TalkingVideoDanmuManager.shared().onRecvDanmuNewMsg(msg: data)
            TvThen.signaling().turnABlindEye(msg: data)

            //: } else if cmdName == "onNotificationPopupWin" { // 审核拒绝提示弹窗
        } else if cmdName == (String(appOfLeadingId.prefix(4)) + "tific" + app_rowValue.replacingOccurrences(of: "back", with: "o") + "nPopup" + String(noti_frameListFormat.prefix(3))) { // 审核拒绝提示弹窗
            //: TalkingPopupWindowManager.shared.attestationRefusePopUpWindow(dic: data)
            ConfinementWindowManager.shared.colorRange(dic: data)

            //: } else if cmdName == "onUserBlock" { // 用户被封号
        } else if cmdName == (String(k_buttonMsg) + String(k_somethingFormat.suffix(9))) { // 用户被封号
            //: TalkingLiveManager.shared().live_releaseAllResource()
            CommentLiveManager.leaveBy().define()
            //: TalkingVoiceRoomManager.shared().voiceRoom_releaseAllResource()
            VoxNameReactiveCompatible.listOf().program()
            //: NotificationCenter.default.post(name: DID_LOGIN_OUT_SUCCESS_NOTIFICATION, object: nil, userInfo: nil)
            NotificationCenter.default.post(name: mDetailId, object: nil, userInfo: nil)
        }
    }
}

// MARK: - 处理业务逻辑

//: extension TalkingSocketManager {
extension PublishDataSocketDelegate {
    /// 检测是否需要停止或恢复直播
    /// - Parameter cmdName: socket命令
    //: private func check_stopOrRestoreLive(_ cmdName: String) {
    private func transitionLive(_ cmdName: String) {
        // 【关闭直播】收到来电或主动通话时，关闭直播
        //: if cmdName == "requestCall" || cmdName == "onRequestCall" {
        if cmdName == (userAreaMessage.replacingOccurrences(of: "time", with: "r") + m_suiteTitle.replacingOccurrences(of: "normal", with: "s") + "tCall") || cmdName == (String(user_appMsg.prefix(6)) + app_indexFormat.lowercased() + String(show_cameraUrl)) {
            //: if TalkingLiveManager.shared().isLive == true {
            if CommentLiveManager.leaveBy().isLive == true {
                //: NEED_RESTORE_LIVE = true
                dream_marginName = true
                //: TalkingLiveManager.req_liveStop(type: 3)
                CommentLiveManager.performMessage(type: 3)
                //: TalkingLiveManager.shared().live_releaseAllResource()
                CommentLiveManager.leaveBy().define()
                //: return
                return
            }
        }

        // 【恢复直播】结束通话后，判断是否需要恢复直播
        //: guard NEED_RESTORE_LIVE == true else { return }
        guard dream_marginName == true else { return }
        //: if cmdName == "cancelCall" || cmdName == "onCancelCall" ||
        if cmdName == (k_scaleMessage.replacingOccurrences(of: "name", with: "ca") + "elCal" + dreamDisplayStr.replacingOccurrences(of: "op", with: "l")) || cmdName == (String(main_pageWithTotalStr)) ||
            //: cmdName == "refuseCall" || cmdName == "onRefuseCall" ||
            cmdName == (String(mViewKey) + String(user_gesturePath.prefix(4))) || cmdName == (String(app_domainKey.prefix(6)) + String(dreamMarginData.prefix(6))) ||
            //: cmdName == "stopCall" || cmdName == "onStopCall" {
            cmdName == (String(kDiskInputSucceedText) + appBottomContent.replacingOccurrences(of: "model", with: "l")) || cmdName == (String(dream_errorGroupData.suffix(5)) + "pCal" + String(kLayerKey))
        {
            //: NEED_RESTORE_LIVE = false
            dream_marginName = false
            //: let alert = TalkingLiveAlertView(frame: .zero, type: .turnOnLive)
            let alert = SmartView(frame: .zero, type: .turnOnLive)
            //: alert.rightBlcok = {
            alert.rightBlcok = {
                //: TalkingLiveManager.shared().checkLiveAuthAndTurnOn()
                CommentLiveManager.leaveBy().handlerView()
            }
            //: alert.show()
            alert.tapTo()
        }
    }

    /// 免费1分钟通话结束弹窗
    //: private func isShowFreeStopCallPopUpView(data: [String: Any]) {
    private func am(data: [String: Any]) {
        //: if let freeCallTimes = data["freeCallTimes"] as? Int {
        if let freeCallTimes = data[(String(appCurrentPath.suffix(6)) + "llTim" + showImageIdent.replacingOccurrences(of: "track", with: "e"))] as? Int {
            //: PathAppManager.share.loginUserMode.freeCallTimes = freeCallTimes
            PathAppManager.share.loginUserMode.freeCallTimes = freeCallTimes
        }
        //: let freeCallWin = data["freeCallWin"] as? Bool
        let freeCallWin = data[(String(show_sharedText) + String(notiBindValue.suffix(4)))] as? Bool
        //: if freeCallWin == true && PathAppManager.share.loginUserMode.sex == Gender.male.rawValue {
        if freeCallWin == true, PathAppManager.share.loginUserMode.sex == ConnectionClusterLiteral.male.rawValue {
            //: let view = TalkingFreeStopCallPopUpView.init()
            let view = WillExamineUpView()
            //: view.show()
            view.playShow()
            //: view.setShowData(data: data)
            view.alongShow(data: data)
        }
    }
}
