
//: Declare String Begin

/*: "PARTY_STATUS_CHANGE_NOTIFICATION" :*/
fileprivate let show_makeName:[UInt8] = [0x39,0x28,0x3b,0x3d,0x30,0x36,0x3a,0x3d,0x28,0x3d,0x3c,0x3a,0x36,0x2a,0x21,0x28,0x27,0x2e,0x2c,0x36,0x27,0x26,0x3d,0x20,0x2f,0x20,0x2a,0x28,0x3d,0x20,0x26,0x27]

private func asImage(lab num: UInt8) -> UInt8 {
    return num ^ 105
}

/*: "p- :*/
fileprivate let main_recordPath:[Character] = ["p","-"]

/*: - :*/
fileprivate let showVerticalImageName:String = "-"

/*: "redirectUrl" :*/
fileprivate let k_featurePath:String = "redtapre"
fileprivate let show_viewKitPath:String = "ctUrlmanager equal moment close guard"

/*: "VOICEROOM: 进房模型解析失败。" :*/
fileprivate let app_menuName:[Character] = ["V","O","I","C","E","R","O","O","M"]
fileprivate let mSendContent:String = ": 进房"

/*: "Party room exception" :*/
fileprivate let userTipIdent:String = "title numberPart"
fileprivate let noti_shareData:[Character] = [" "]
fileprivate let dreamSharedErrorPath:[Character] = ["e","x","c","e","p","t","i","o","n"]

/*: _ :*/
fileprivate let dream_screenValue:String = "_"

/*: "male" :*/
fileprivate let app_rankIdent:[UInt8] = [0x65,0x6c,0x61,0x6d]

/*: "female" :*/
fileprivate let appKeyUpMsg:[Character] = ["f","e","m","a","l","e"]

/*: "VOICEROOM: 群聊进房失败。groupId:  :*/
fileprivate let k_containerStr:String = "VOICEtaskO"
fileprivate let mainTextFormat:String = "back videoOM: 群聊"
fileprivate let m_viewId:String = "GROUP"

/*: ." :*/
fileprivate let k_effectMessage:String = "."

/*: "VOICEROOM: 切房模型解析失败。" :*/
fileprivate let show_labValue:String = "VfatalICERfatal"
fileprivate let main_resultImageUrl:[Character] = ["O","M",":"," ","切","房","模","型","\u{89e3}","析","失","败","。"]

/*: "VOICEROOM: 群聊切房失败。groupId:  :*/
fileprivate let show_cellName:String = "VOsizeER"
fileprivate let m_tagToIdent:String = " mouth切房失"
fileprivate let dream_effectKey:String = "launch case raw height colorpId: "

/*: "errCode" :*/
fileprivate let kErrorId:[UInt8] = [0xf8,0x5,0x5,0xd6,0x2,0xf7,0xf8]

fileprivate func photoWith(main num: UInt8) -> UInt8 {
    let value = Int(num) - 147
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "custom_kickout_message" :*/
fileprivate let show_replyObjectContentId:[Character] = ["c","u","s","t","o","m","_","k","i","c","k","o"]
fileprivate let kNameMsg:[Character] = ["u"]
fileprivate let userNameMsg:[Character] = ["t","_","m","e","s","s","a","g","e"]

/*: "closeRoom" :*/
fileprivate let noti_cellBindIdent:String = "cmakeos"

/*: "adminCloseRoom" :*/
fileprivate let notiEnableName:String = "admintimate"
fileprivate let userAllToolKey:[Character] = ["R","o","o","m"]

/*: "The room is closed, please go to another room" :*/
fileprivate let mainStyleValueUrl:[UInt8] = [0x6d,0x6f,0x6f,0x72,0x20,0x72,0x65,0x68,0x74,0x6f,0x6e,0x61,0x20,0x6f,0x74,0x20,0x6f,0x67,0x20,0x65,0x73,0x61,0x65,0x6c,0x70,0x20,0x2c,0x64,0x65,0x73,0x6f,0x6c,0x63,0x20,0x73,0x69,0x20,0x6d,0x6f,0x6f,0x72,0x20,0x65,0x68,0x54]

/*: "OK" :*/
fileprivate let m_lineText:String = "keyK"

/*: "You were kicked out of the room" :*/
fileprivate let show_infoId:[UInt8] = [0xe4,0xd2,0xc8,0x9d,0xca,0xd8,0xcf,0xd8,0x9d,0xd6,0xd4,0xde,0xd6,0xd8,0xd9,0x9d,0xd2,0xc8,0xc9,0x9d,0xd2,0xdb,0x9d,0xc9,0xd5,0xd8,0x9d,0xcf,0xd2,0xd2,0xd0]

private func dominoCan(text num: UInt8) -> UInt8 {
    return num ^ 189
}

/*: "VOICEROOM：进房失败，roomID:  :*/
fileprivate let appSucceedUrl:String = "resultICE"
fileprivate let k_insideToTitle:[Character] = ["败","\u{ff0c}","r","o","o"]
fileprivate let noti_tabTagText:String = "mID: circle user description"

/*: , error:  :*/
fileprivate let noti_moviePath:String = ","
fileprivate let kGiftName:[Character] = [" ","e","r","r","o","r",":"," "]

/*: , reason:  :*/
fileprivate let m_directionFormat:[Character] = [","," ","r","e","a","s","o","n",":"," "]

/*: , extendedData:  :*/
fileprivate let mArrangementStr:[Character] = [","," ","e","x","t","e","n","d","e","d","D","a","t","a",":"," "]

/*: "VOICEROOM: 当前用户推流失败，roomID:  :*/
fileprivate let dreamTipId:String = "voice"
fileprivate let k_connectionIdent:String = "当\u{524d}用户推流"
fileprivate let noti_atUrl:String = "D: pop app find var"

/*: , errorCode:  :*/
fileprivate let k_rawName:String = "type cookie style, erro"
fileprivate let mBlankId:String = "height more status quick titlerCode: "

/*: , streamID:  :*/
fileprivate let main_editKey:String = ", struser main"
fileprivate let k_countIdent:String = "status data self self makeeamID: "

/*: "An error occurred, please try again" :*/
fileprivate let k_mId:[UInt8] = [0xa7,0xd4,0x86,0xcb,0xd8,0xd8,0xd5,0xd8,0x86,0xd5,0xc9,0xc9,0xdb,0xd8,0xd8,0xcb,0xca,0x92,0x86,0xd6,0xd2,0xcb,0xc7,0xd9,0xcb,0x86,0xda,0xd8,0xdf,0x86,0xc7,0xcd,0xc7,0xcf,0xd4]

fileprivate func originType(view num: UInt8) -> UInt8 {
    let value = Int(num) + 154
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "VOICEROOM: 拉流失败，roomID:  :*/
fileprivate let appShowFormat:String = "VOICtimeR"
fileprivate let user_starData:String = " 拉\u{6d41}\u{5931}"
fileprivate let dreamGiftValue:String = "equal pathD: "

/*: "VOICEROOM: 信令解析失败，command:  :*/
fileprivate let dream_textRawUrl:[Character] = ["V","O","I","C","E","R","O","O","M",":"," ","\u{4fe1}","\u{4ee4}","解","析"]
fileprivate let user_arrayName:String = "失败，cload busy floor for"

/*: , roomID:  :*/
fileprivate let m_makeStr:String = "card video let guard let, roo"

/*: "data" :*/
fileprivate let mainRowValue:[Character] = ["d","a","t","a"]

/*: "position" :*/
fileprivate let mainRevenueTitle:String = "POSITI"
fileprivate let notiPreviousPath:[Character] = ["o","n"]

/*: "xxxx--  :*/
fileprivate let app_executeTextContent:String = "xxxx-- break view"

/*: "opType" :*/
fileprivate let mainShareLabelText:[Character] = ["o","p","T","y","p"]
fileprivate let noti_fromMsg:String = "image"

/*: "party:changeMike" :*/
fileprivate let mEqualTitle:[Character] = ["p","a","r","t","y",":","c","h","a","n","g","e"]
fileprivate let mLimitColorValue:String = "player inputMike"

/*: "type" :*/
fileprivate let kInmateId:String = "typfield"

/*: "toPosition" :*/
fileprivate let app_imagePath:[Character] = ["t","o","P","o","s","i","t","i","o","n"]

/*: "party:adminChangeMike" :*/
fileprivate let noti_netMessage:String = "parmenu"
fileprivate let main_infoProfileDetailUrl:[Character] = ["m","i","n","C","h","a","n","g","e","M","i","k","e"]

/*: "You were kicked off the seat" :*/
fileprivate let kModelViewText:String = "color leftYou "
fileprivate let noti_mainStr:String = "icked let corner all"
fileprivate let mWhiteObjectMessage:String = "ttoe"
fileprivate let userAtUrl:String = "case cell enter seat"

/*: "party:changeCoin" :*/
fileprivate let k_centerValue:[Character] = ["p","a","r","t","y",":","c","h","a","n"]
fileprivate let noti_contentLabelStr:String = "geCoinlay edge lab any"

/*: "party:closeStream" :*/
fileprivate let m_menuKey:[Character] = ["p","a","r","t","y",":","c","l","o","s","e","S","t","r","e","a","m"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  VoxNameReactiveCompatible.swift
//  Adjust
//
//  Created by DouXiu on 2024/9/12.
//

//: import ZegoExpressEngine
import ZegoExpressEngine

/// 语聊房状态变更通知
//: public let PARTY_STATUS_CHANGE_NOTIFICATION = NSNotification.Name(rawValue: "PARTY_STATUS_CHANGE_NOTIFICATION")
public let userClickDismissTextValue = NSNotification.Name(rawValue: String(bytes: show_makeName.map{asImage(lab: $0)}, encoding: .utf8)!)
/// 麦位数
//: public let MIKE_SEAT_COUNT = 8
public let show_userUrl = 8

/// 语聊房角色
//: enum VoiceRoomRole: Int {
enum DigitizerSubscriptType: Int {
    //: case host = 1
    case host = 1 // 房主
    //: case audience = 2
    case audience = 2 // 观众
    //: case manager = 4
    case manager = 4 // 管理员
}

//: class TalkingVoiceRoomManager: NSObject {
class VoxNameReactiveCompatible: NSObject {
    /// 语聊房模型
    //: private(set) var partyModel = TalkingVoiceRoomModel()
    private(set) var partyModel = GrantTransformable()
    /// 是否正在语聊房中（状态变更时发送通知）
    //: private(set) var isParty: Bool = false {
    private(set) var isParty: Bool = false {
        //: didSet {
        didSet {
            //: NotificationCenter.default.post(name: PARTY_STATUS_CHANGE_NOTIFICATION, object: self)
            NotificationCenter.default.post(name: userClickDismissTextValue, object: self)
        }
    }

    /// 是否小窗口模式
    //: private(set) var isSmallMode: Bool = false
    private(set) var isSmallMode: Bool = false
    /// singleton
    //: private static var _instance: TalkingVoiceRoomManager?
    private static var _instance: VoxNameReactiveCompatible?
    // 语聊房控制器
    //: private var voiceRoomVC: TalkingVoiceRoomViewController?
    private var voiceRoomVC: VoiceDirectionThen?
    /// 麦位信息字典 [麦位 0~7：麦位模型]
    //: private var mikePositionDict = [Int: TalkingMikeListItemModel]()
    private var mikePositionDict = [Int: PortMeasurable]()
    //: private let mplock = NSLock()
    private let mplock = NSLock()
    /// 正在进房请求中（防止多次调用）
    //: private var isEnterRoomReq = false
    private var isEnterRoomReq = false
    /// 麦克风操作请求中（防止多次调用）
    //: private var isMicReq = false
    private var isMicReq = false

    //: override init() {
    override init() {
        //: super.init()
        super.init()
        //: self.mikePositionDict = initMikePosition()
        self.mikePositionDict = positionKey()
    }

    //: class func shared() -> TalkingVoiceRoomManager {
    class func listOf() -> VoxNameReactiveCompatible {
        //: guard _instance != nil else {
        guard _instance != nil else {
            //: _instance = TalkingVoiceRoomManager()
            _instance = VoxNameReactiveCompatible()
            //: return _instance!
            return _instance!
        }
        //: return _instance!
        return _instance!
    }

    //: class func destroy() {
    class func endDestroy() {
        //: if TalkingVoiceRoomManager._instance != nil {
        if VoxNameReactiveCompatible._instance != nil {
            //: TalkingVoiceRoomManager._instance = nil
            VoxNameReactiveCompatible._instance = nil
        }
    }

    // MARK: - Lazy Load

    // 语聊房小窗口悬浮窗
    //: private lazy var miniView: TalkingVoiceRoomMiniView = {
    private lazy var miniView: GiftView = {
        //: let mini = TalkingVoiceRoomMiniView.buildLiveMiniView()
        let mini = GiftView.statusFor()
        //: mini.isHidden = true
        mini.isHidden = true
        //: mini.tapGestureBlock = { [weak self] in
        mini.tapGestureBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.voiceRoom_goback()
            self.colorModel()
        }
        //: return mini
        return mini
        //: }()
    }()
}

// MARK: - 操作麦位请求

//: extension TalkingVoiceRoomManager {
extension VoxNameReactiveCompatible {
    /// 房主/观众 麦位操作请求
    /// - Parameters:
    ///   - type: 1 上麦，2 下麦，3 切麦，4 开麦，5 闭麦
    ///   - position: 麦位
    ///   - toPosition: 目标麦位（切麦传值）
    //: func voiceRoom_reqChangeMike(type: Int, position: Int, toPosition: Int? = nil) {
    func level(type: Int, position: Int, toPosition: Int? = nil) {
        //: guard isMicReq == false else { return }
        guard isMicReq == false else { return }
        //: isMicReq = true
        isMicReq = true
        //: TalkingVoiceRoomReqTool.req_changeMike(roomId: partyModel.roomId,
        CharterReqTool.pushContent(roomId: partyModel.roomId,
                                            //: uid: Int(PathAppManager.share.loginUid) ?? 0,
                                            uid: Int(PathAppManager.share.loginUid) ?? 0,
                                            //: type: type,
                                            type: type,
                                            //: position: position,
                                            position: position,
                                            //: toPos: toPosition)
                                            toPos: toPosition)
        //: { succeed, result, errorModel in
        { succeed, result, errorModel in
            //: self.isMicReq = false
            self.isMicReq = false
            //: guard succeed else {
            guard succeed else { // 失败提示
                //: if errorModel?.errorCode == 20001 {
                if errorModel?.errorCode == 20001 { // 已在麦位，开始推流，刷新UI
                    //: ZegoExpressEngine.shared().mutePublishStreamAudio(false)
                    ZegoExpressEngine.shared().mutePublishStreamAudio(false)
                    //: let streamID = self.voiceRoom_getStreamID()
                    let streamID = self.finishOff()
                    //: ZegoExpressEngine.shared().startPublishingStream(streamID)
                    ZegoExpressEngine.shared().startPublishingStream(streamID)
                    //: guard let dict = result as? [String: Any] else { return }
                    guard let dict = result as? [String: Any] else { return }
                    //: guard let mikeModel = TalkingMikeListItemModel.deserialize(from: dict) else { return }
                    guard let mikeModel = PortMeasurable.deserialize(from: dict) else { return }
                    //: self.updateMikePosition(position: position, model: mikeModel)
                    self.equality(position: position, model: mikeModel)
                }
                //: return
                return
            }

            // 成功后只做逻辑处理，在接收信令处统一刷新UI
            //: switch type {
            switch type {
            //: case 1:
            case 1: // 上麦成功
                //: ZegoExpressEngine.shared().mutePublishStreamAudio(false)
                ZegoExpressEngine.shared().mutePublishStreamAudio(false)
                //: let streamID = self.voiceRoom_getStreamID()
                let streamID = self.finishOff()
                //: ZegoExpressEngine.shared().startPublishingStream(streamID)
                ZegoExpressEngine.shared().startPublishingStream(streamID)

            //: case 2:
            case 2: // 下麦成功
                //: ZegoExpressEngine.shared().stopPublishingStream()
                ZegoExpressEngine.shared().stopPublishingStream()

            //: case 3:
            case 3: // 切麦成功
                //: break
                break

            //: case 4:
            case 4: // 开麦成功
                //: ZegoExpressEngine.shared().mutePublishStreamAudio(false)
                ZegoExpressEngine.shared().mutePublishStreamAudio(false)

            //: case 5:
            case 5: // 闭麦成功
                //: ZegoExpressEngine.shared().mutePublishStreamAudio(true)
                ZegoExpressEngine.shared().mutePublishStreamAudio(true)

            //: default:
            default:
                //: break
                break
            }
        }
    }

    /// 房主对他人操作请求
    /// - Parameters:
    ///   - uid: 用户Id
    ///   - type: 1 锁麦，2 解锁麦，3 踢下麦，4 开麦，5 闭麦
    ///   - position: 麦位
    //: func voiceRoom_reqAdminChangeMike(uid: Int, type: Int, position: Int) {
    func find(uid: Int, type: Int, position: Int) {
        //: guard isMicReq == false else { return }
        guard isMicReq == false else { return }
        //: isMicReq = true
        isMicReq = true
        //: TalkingVoiceRoomReqTool.req_adminChangeMike(roomId: partyModel.roomId,
        CharterReqTool.messageCompletion(roomId: partyModel.roomId,
                                                  //: uid: uid,
                                                  uid: uid,
                                                  //: type: type,
                                                  type: type,
                                                  //: position: position)
                                                  position: position)
        //: { _, _, _ in
        { _, _, _ in
            //: self.isMicReq = false
            self.isMicReq = false
        }
    }

    /// 获取推流Id
    //: private func voiceRoom_getStreamID() -> String {
    private func finishOff() -> String {
        //: return "p-\(PathAppManager.share.loginUid)-\(partyModel.roomId)"
        return "p-\(PathAppManager.share.loginUid)-\(partyModel.roomId)"
    }
}

// MARK: - 权限检测，开启/进入/切换 语聊房

//: extension TalkingVoiceRoomManager {
extension VoxNameReactiveCompatible {
    /// 检测状态并进入语聊房
    /// - Parameters:
    ///   - roomId: 房间Id（nil：创建房间；有值：进入房间）
    //: func voiceRoom_checkAndTurnOn(roomId: String? = nil) {
    func upwardAt(roomId: String? = nil) {
        //: guard isParty == false else { return }
        guard isParty == false else { return }
        //: guard isEnterRoomReq == false else { return }
        guard isEnterRoomReq == false else { return }
        //: isEnterRoomReq = true
        isEnterRoomReq = true

        // 1. 权限检测
        //: TalkingPermissionTool.func__openRecordServiceWithBlock(true) { isOpen in
        EqualReactiveCompatible.utility(true) { isOpen in
            //: guard isOpen == true else {
            guard isOpen == true else {
                //: self.func__showStatusBarErrorMsg(showMsg: kMessage_permissions_error)
                self.colorGift(showMsg: showMeBarReasonPath)
                //: self.isEnterRoomReq = false
                self.isEnterRoomReq = false
                //: return
                return
            }

            // 2. 接口是否禁播检测
            //: TalkingVoiceRoomReqTool.req_voiceRoomCheck(roomId: roomId) { succeed, result, errorModel in
            CharterReqTool.mic(roomId: roomId) { succeed, result, errorModel in
                //: self.isEnterRoomReq = false
                self.isEnterRoomReq = false
                //: guard succeed else {
                guard succeed else {
                    //: if errorModel?.errorCode == 302 {
                    if errorModel?.errorCode == 302 { // 失败，跳转H5
                        //: if let result = result as? [String: Any] {
                        if let result = result as? [String: Any] {
                            //: NamePushManager.share.func__pushToWebVC(urlStr: result["redirectUrl"] as? String)
                            NamePushManager.share.upwardsConfig(urlStr: result[(k_featurePath.replacingOccurrences(of: "tap", with: "i") + String(show_viewKitPath.prefix(5)))] as? String)
                        }
                        //: } else if errorModel?.errorMsg.count ?? 0 > 0 {
                    } else if errorModel?.errorMsg.count ?? 0 > 0 {
                        //: self.func__showStatusBarErrorMsg(showMsg: errorModel!.errorMsg)
                        self.colorGift(showMsg: errorModel!.errorMsg)
                    }
                    //: return
                    return
                }

                // 3. 是否正在音视频通话中
                //: guard TalkingSocketManager.shared.isData == false else {
                guard PublishDataSocketDelegate.shared.isData == false else {
                    //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
                    self.colorGift(showMsg: app_successShowMsg)
                    //: return
                    return
                }

                // 4. 校验成功，解析模型
                //: guard let model = TalkingVoiceRoomModel.deserialize(from: result as? Dictionary) else {
                guard let model = GrantTransformable.deserialize(from: result as? Dictionary) else {
                    //: UploadLogTool.writeLog(msg: "VOICEROOM: 进房模型解析失败。")
                    LabelLogTool.fillInLine(msg: (String(app_menuName) + mSendContent.capitalized + "模\u{578b}解析\u{5931}败。"))
                    //: self.func__showStatusBarErrorMsg(showMsg: "Party room exception".localized)
                    self.colorGift(showMsg: (String(userTipIdent.suffix(4)) + "y room" + String(noti_shareData) + String(dreamSharedErrorPath)).localized)
                    //: return
                    return
                }
                //: self.partyModel = model
                self.partyModel = model

                // 5. 创建语聊房UI，同时调用sdk创建房间
                //: self.voiceRoomVC = TalkingVoiceRoomViewController()
                self.voiceRoomVC = VoiceDirectionThen()
                //: self.getNavigationController()?.pushViewController(self.voiceRoomVC!, animated: true)
                self.modelButton()?.pushViewController(self.voiceRoomVC!, animated: true)
                //: self.miniView.refreshMiniView()
                self.miniView.equalView()
                //: self.miniView.isHidden = true
                self.miniView.isHidden = true
                //: self.voiceRoom_create()
                self.afterIcon()
                //: UIApplication.shared.isIdleTimerDisabled = true
                UIApplication.shared.isIdleTimerDisabled = true // 防止息屏
                //: self.isParty = true
                self.isParty = true

                // 6. 埋点
                //: uploadRecord.uploadRecordEvent(eventID: "\(click_partyDetail_enter)_\(PathAppManager.share.loginUserMode.sex == Gender.male.rawValue ? "male" : "female")")
                kResolutionText.atJson(eventID: "\(m_tabScreenId)_\(PathAppManager.share.loginUserMode.sex == ConnectionClusterLiteral.male.rawValue ? String(bytes: app_rankIdent.reversed(), encoding: .utf8)! : (String(appKeyUpMsg)))")

                // 7. 初始化弹幕群聊
                //: TalkingDanmuManager.joinLiveRoomDanmu(groupId: self.partyModel.chatGroupId, announcementMsg: self.partyModel.notification) { succeed in
                ScoreThen.abstraction(groupId: self.partyModel.chatGroupId, announcementMsg: self.partyModel.notification) { succeed in
                    //: if succeed == false {
                    if succeed == false {
                        //: UploadLogTool.writeLog(msg: "VOICEROOM: 群聊进房失败。groupId: \(self.partyModel.chatGroupId).")
                        LabelLogTool.fillInLine(msg: (k_containerStr.replacingOccurrences(of: "task", with: "R") + String(mainTextFormat.suffix(6)) + "进房\u{5931}败。" + m_viewId.lowercased() + "Id: ") + "\(self.partyModel.chatGroupId).")
                        //: self.func__showStatusBarErrorMsg(showMsg: "Party room exception".localized)
                        self.colorGift(showMsg: (String(userTipIdent.suffix(4)) + "y room" + String(noti_shareData) + String(dreamSharedErrorPath)).localized)
                        //: self.voiceRoom_releaseAllResource()
                        self.program()
                    }
                }
            }
        }
    }

    /// 切换语聊房
    /// - Parameters:
    ///   - roomId: 当前进入房间Id
    ///   - beforeRoomId: 上一房间Id
    //: func voiceRoom_switch(roomId: String, beforeRoomId: String) {
    func bit(roomId: String, beforeRoomId: String) {
        //: guard isEnterRoomReq == false else { return }
        guard isEnterRoomReq == false else { return }
        //: isEnterRoomReq = true
        isEnterRoomReq = true
        // 1. 调用切房接口
        //: TalkingVoiceRoomReqTool.req_voiceRoomSwitch(roomId: roomId, beforeRoomId: beforeRoomId) { succeed, result, _ in
        CharterReqTool.appFill(roomId: roomId, beforeRoomId: beforeRoomId) { succeed, result, _ in
            //: self.isEnterRoomReq = false
            self.isEnterRoomReq = false
            //: guard succeed else { return }
            guard succeed else { return }
            // 2. 释放上个房间资源 (使用partyModel旧值)
            //: self.voiceRoomVC?.releaseAllResourceAndPop()
            self.voiceRoomVC?.pressurise()

            // 3. 主播切房，需要关闭底部tabbar动效 (使用partyModel旧值)
            //: if String(self.partyModel.streamerInfo.uid) == PathAppManager.share.loginUid {
            if String(self.partyModel.streamerInfo.uid) == PathAppManager.share.loginUid {
                //: self.isParty = false
                self.isParty = false
            }

            // 4. 校验成功，解析模型（给partyModel重新赋值）
            //: guard let model = TalkingVoiceRoomModel.deserialize(from: result as? Dictionary) else {
            guard let model = GrantTransformable.deserialize(from: result as? Dictionary) else {
                //: UploadLogTool.writeLog(msg: "VOICEROOM: 切房模型解析失败。")
                LabelLogTool.fillInLine(msg: (show_labValue.replacingOccurrences(of: "fatal", with: "O") + String(main_resultImageUrl)))
                //: self.func__showStatusBarErrorMsg(showMsg: "Party room exception".localized)
                self.colorGift(showMsg: (String(userTipIdent.suffix(4)) + "y room" + String(noti_shareData) + String(dreamSharedErrorPath)).localized)
                //: return
                return
            }
            //: self.partyModel = model
            self.partyModel = model

            // 5. 创建语聊房UI，同时调用sdk切房
            //: self.voiceRoomVC = TalkingVoiceRoomViewController()
            self.voiceRoomVC = VoiceDirectionThen()
            //: self.getNavigationController()?.pushViewController(self.voiceRoomVC!, animated: true)
            self.modelButton()?.pushViewController(self.voiceRoomVC!, animated: true)
            //: self.miniView.refreshMiniView()
            self.miniView.equalView()
            //: self.miniView.isHidden = true
            self.miniView.isHidden = true
            //: self.voiceRoom_switch(beforeRoomId: beforeRoomId)
            self.dancePalaceTo(beforeRoomId: beforeRoomId)
            //: self.isParty = true
            self.isParty = true
            //: self.isSmallMode = false
            self.isSmallMode = false

            // 6. 埋点
            //: uploadRecord.uploadRecordEvent(eventID: "\(click_partyDetail_enter)_\(PathAppManager.share.loginUserMode.sex == Gender.male.rawValue ? "male" : "female")")
            kResolutionText.atJson(eventID: "\(m_tabScreenId)_\(PathAppManager.share.loginUserMode.sex == ConnectionClusterLiteral.male.rawValue ? String(bytes: app_rankIdent.reversed(), encoding: .utf8)! : (String(appKeyUpMsg)))")

            // 7. 初始化弹幕群聊
            //: TalkingDanmuManager.joinLiveRoomDanmu(groupId: self.partyModel.chatGroupId, announcementMsg: self.partyModel.notification) { succeed in
            ScoreThen.abstraction(groupId: self.partyModel.chatGroupId, announcementMsg: self.partyModel.notification) { succeed in
                //: if succeed == false {
                if succeed == false {
                    //: UploadLogTool.writeLog(msg: "VOICEROOM: 群聊切房失败。groupId: \(self.partyModel.chatGroupId).")
                    LabelLogTool.fillInLine(msg: (show_cellName.replacingOccurrences(of: "size", with: "IC") + "OOM:" + m_tagToIdent.replacingOccurrences(of: "mouth", with: "群聊") + "\u{8d25}。grou" + String(dream_effectKey.suffix(5))) + "\(self.partyModel.chatGroupId).")
                    //: self.func__showStatusBarErrorMsg(showMsg: "Party room exception".localized)
                    self.colorGift(showMsg: (String(userTipIdent.suffix(4)) + "y room" + String(noti_shareData) + String(dreamSharedErrorPath)).localized)
                    //: self.voiceRoom_releaseAllResource()
                    self.program()
                }
            }
        }
    }
}

// MARK: - Public Event

//: extension TalkingVoiceRoomManager {
extension VoxNameReactiveCompatible {
    /// 最小化语聊房
    //: func voiceRoom_mini() {
    func dataConverterSize() {
        //: isSmallMode = true
        isSmallMode = true
        //: miniView.isHidden = false
        miniView.isHidden = false
        //: voiceRoomVC?.popCurrentViewController()
        voiceRoomVC?.subgross()
    }

    /// 返回语聊房
    //: func voiceRoom_goback() {
    func colorModel() {
        //: isSmallMode = false
        isSmallMode = false
        //: miniView.isHidden = true
        miniView.isHidden = true
        //: guard voiceRoomVC != nil else { return }
        guard voiceRoomVC != nil else { return }
        //: guard let currentVC = currentViewController() else { return }
        guard let currentVC = withTo() else { return }
        // 如果顶部视图是私信页，先返回列表，避免多个私信页消息同步问题
        //: if currentVC.isKind(of: TalkingPrivateChatController.self) {
        if currentVC.isKind(of: TotViewController.self) {
            //: if var vcArr = currentVC.navigationController?.viewControllers {
            if var vcArr = currentVC.navigationController?.viewControllers {
                //: vcArr.removeLast()
                vcArr.removeLast()
                //: vcArr.append(voiceRoomVC!)
                vcArr.append(voiceRoomVC!)
                //: currentVC.navigationController?.setViewControllers(vcArr, animated: true)
                currentVC.navigationController?.setViewControllers(vcArr, animated: true)
            }
            //: } else {
        } else {
            //: currentVC.navigationController?.pushViewController(voiceRoomVC!, animated: true)
            currentVC.navigationController?.pushViewController(voiceRoomVC!, animated: true)
        }
    }

    /// 销毁语聊房所有资源
    //: func voiceRoom_releaseAllResource() {
    func program() {
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.isParty = false
            self.isParty = false // 有通知监听，需置为false
            //: UIApplication.shared.isIdleTimerDisabled = false
            UIApplication.shared.isIdleTimerDisabled = false // 恢复息屏
            //: self.miniView.removeFromSuperview()
            self.miniView.removeFromSuperview() // 移除小窗口
            //: self.voiceRoomVC?.releaseAllResourceAndPop()
            self.voiceRoomVC?.pressurise() // 将语聊房视图从栈中移除并释放资源
            //: if self.partyModel.position >= 0 {
            if self.partyModel.position >= 0 { // 退房下麦
                //: self.voiceRoom_reqChangeMike(type: 2, position: self.partyModel.position)
                self.level(type: 2, position: self.partyModel.position)
            }
            //: ZegoExpressEngine.shared().logoutRoom()
            ZegoExpressEngine.shared().logoutRoom() // 退出房间
            //: ZegoExpressEngine.destroy(nil)
            ZegoExpressEngine.destroy(nil) // 销毁引擎
            //: TalkingVoiceRoomManager.destroy()
            VoxNameReactiveCompatible.endDestroy() // 销毁当前单例
        }
    }
}

// MARK: - Private Event

//: extension TalkingVoiceRoomManager {
extension VoxNameReactiveCompatible {
    /// 创建语聊房
    //: private func voiceRoom_create() {
    private func afterIcon() {
        // 创建引擎
        //: let profile = ZegoEngineProfile()
        let profile = ZegoEngineProfile()
        //: profile.appID = FacultyThen.getVoiceRoomAppId()
        profile.appID = FacultyThen.vocalization()
        //: profile.scenario = .standardChatroom
        profile.scenario = .standardChatroom
        //: ZegoExpressEngine.createEngine(with: profile, eventHandler: self)
        ZegoExpressEngine.createEngine(with: profile, eventHandler: self)
        // 进阶配置，进入房间
        //: let config = ZegoRoomConfig()
        let config = ZegoRoomConfig()
        //: config.token = partyModel.token
        config.token = partyModel.token
        //: let user = ZegoUser(userID: PathAppManager.share.loginUid)
        let user = ZegoUser(userID: PathAppManager.share.loginUid)
        //: ZegoExpressEngine.shared().loginRoom(partyModel.roomId, user: user, config: config)
        ZegoExpressEngine.shared().loginRoom(partyModel.roomId, user: user, config: config)
    }

    /// 切换语聊房
    /// - Parameter beforeRoomId: 上一房间Id
    //: private func voiceRoom_switch(beforeRoomId: String) {
    private func dancePalaceTo(beforeRoomId: String) {
        //: let config = ZegoRoomConfig()
        let config = ZegoRoomConfig()
        //: config.token = partyModel.token
        config.token = partyModel.token
        //: ZegoExpressEngine.shared().switchRoom(beforeRoomId, toRoomID: partyModel.roomId, config: config)
        ZegoExpressEngine.shared().switchRoom(beforeRoomId, toRoomID: partyModel.roomId, config: config)
    }
}

// MARK: - ZegoEventHandler

//: extension TalkingVoiceRoomManager: ZegoEventHandler {
extension VoxNameReactiveCompatible: ZegoEventHandler {
    // MARK: - 进房/切房 监听回调

    /// 进房/切房 回调
    //: func onRoomStateChanged(_ reason: ZegoRoomStateChangedReason, errorCode: Int32, extendedData: [AnyHashable: Any], roomID: String) {
    func onRoomStateChanged(_ reason: ZegoRoomStateChangedReason, errorCode: Int32, extendedData: [AnyHashable: Any], roomID: String) {
        //: guard roomID == partyModel.roomId else { return }
        guard roomID == partyModel.roomId else { return }
        //: if errorCode == 0, reason == .logined {
        if errorCode == 0, reason == .logined { // 进房成功
            // 启动声浪等级监控
            //: ZegoExpressEngine.shared().startSoundLevelMonitor()
            ZegoExpressEngine.shared().startSoundLevelMonitor()
            // 开启音量稳定控制
            //: ZegoExpressEngine.shared().enableAEC(true)
            ZegoExpressEngine.shared().enableAEC(true)
            // 获取嘉宾列表接口
            //: TalkingVoiceRoomReqTool.req_getMikeList(roomId: partyModel.roomId) { list in
            CharterReqTool.completion(roomId: partyModel.roomId) { list in
                // 保存麦位列表信息
                //: for (key, value) in list.enumerated() {
                for (key, value) in list.enumerated() {
                    //: self.modifyMikePosition(key: key, model: value)
                    self.passePartout(key: key, model: value)
                }
                // 刷新麦位列表UI
                //: self.voiceRoomVC?.mikePositionView.refreshAllMikeView()
                self.voiceRoomVC?.mikePositionView.oweIndicator()
                // 房主首次创建房间，主动上麦
                //: if self.partyModel.userRole == VoiceRoomRole.host.rawValue, self.partyModel.isCreate == true {
                if self.partyModel.userRole == DigitizerSubscriptType.host.rawValue, self.partyModel.isCreate == true {
                    //: self.voiceRoom_reqChangeMike(type: 1, position: 0)
                    self.level(type: 1, position: 0)
                }
            }

            //: } else if reason == .logining || reason == .reconnectFailed || reason == .reconnecting || reason == .reconnected {
        } else if reason == .logining || reason == .reconnectFailed || reason == .reconnecting || reason == .reconnected {
            //: if errorCode == 0, reason == .reconnected {
            if errorCode == 0, reason == .reconnected { // 重连成功
                //: TalkingVoiceRoomReqTool.req_getMikeList(roomId: partyModel.roomId) { list in
                CharterReqTool.completion(roomId: partyModel.roomId) { list in
                    // 更新麦位列表信息
                    //: for (key, value) in list.enumerated() {
                    for (key, value) in list.enumerated() {
                        //: self.modifyMikePosition(key: key, model: value)
                        self.passePartout(key: key, model: value)
                        //: if String(value.uid) == PathAppManager.share.loginUid {
                        if String(value.uid) == PathAppManager.share.loginUid {
                            //: if value.mikeStatus == 1 {
                            if value.mikeStatus == 1 { // 房主闭麦
                                //: ZegoExpressEngine.shared().stopPublishingStream()
                                ZegoExpressEngine.shared().stopPublishingStream()
                                //: } else {
                            } else {
                                //: let streamID = self.voiceRoom_getStreamID()
                                let streamID = self.finishOff()
                                //: ZegoExpressEngine.shared().startPublishingStream(streamID)
                                ZegoExpressEngine.shared().startPublishingStream(streamID)
                                //: let isMute = value.mikeStatus == 0 ? true : false
                                let isMute = value.mikeStatus == 0 ? true : false
                                //: ZegoExpressEngine.shared().mutePublishStreamAudio(isMute)
                                ZegoExpressEngine.shared().mutePublishStreamAudio(isMute)
                            }
                        }
                    }
                    // 刷新麦位列表UI
                    //: self.voiceRoomVC?.mikePositionView.refreshAllMikeView()
                    self.voiceRoomVC?.mikePositionView.oweIndicator()
                }
                //: } else if errorCode != 0 {
            } else if errorCode != 0 {
                // 进房失败埋点
                //: uploadRecord.uploadRecordEvent(eventID: click_partyTab_creParty_fail, parameterStr: ["errCode": String(errorCode)])
                kResolutionText.toStr(eventID: dreamMicBarMsg, parameterStr: [String(bytes: kErrorId.map{photoWith(main: $0)}, encoding: .utf8)!: String(errorCode)])
            }

            //: } else {
        } else { // 进房失败
            //: if errorCode == 1002055 || errorCode == 1002050 {
            if errorCode == 1_002_055 || errorCode == 1_002_050 { // 用户退出 || 被踢出房间
                //: let json = JSON(extendedData)
                let json = JSON(extendedData)
                //: let value = json["custom_kickout_message"].stringValue
                let value = json[(String(show_replyObjectContentId) + String(kNameMsg) + String(userNameMsg))].stringValue
                //: if value == "closeRoom" || value == "adminCloseRoom" { // 关闭房间
                if value == (noti_cellBindIdent.replacingOccurrences(of: "make", with: "l") + "eRoom") || value == (notiEnableName.replacingOccurrences(of: "intimate", with: "i") + "nClose" + String(userAllToolKey)) { // 关闭房间
                    //: voiceRoom_releaseAllResource()
                    program()
                    //: let config = ShowAlertConfig()
                    let config = KeyCustomAlertConfig()
                    //: config.alignment = .center
                    config.alignment = .center
                    //: TalkingAlertShow.customAlert(message: "The room is closed, please go to another room".localized, rightBtnTitle: "OK".localized, rightBlock: {
                    CourseAlertShow.inkpad(message: String(bytes: mainStyleValueUrl.reversed(), encoding: .utf8)!.localized, rightBtnTitle: "OK".localized, rightBlock: {
                        // 跳转到首页tab-party
                        //: NotificationCenter.default.post(name: SWITCH_TABBAR_HOME_PARTY_NOTIFICATION, object: nil, userInfo: nil)
                        NotificationCenter.default.post(name: appReplaceStr, object: nil, userInfo: nil)
                        //: }, config: config)
                    }, config: config)

                    //: } else {
                } else { // 被踢出房间
                    //: func__showStatusBarErrorMsg(showMsg: "You were kicked out of the room".localized)
                    colorGift(showMsg: String(bytes: show_infoId.map{dominoCan(text: $0)}, encoding: .utf8)!.localized)
                }

                //: } else {
            } else {
                //: UploadLogTool.writeLog(msg: "VOICEROOM：进房失败，roomID: \(roomID), error: \(errorCode), reason: \(reason), extendedData: \(extendedData).")
                LabelLogTool.fillInLine(msg: (appSucceedUrl.replacingOccurrences(of: "result", with: "VO") + "ROOM：进房失" + String(k_insideToTitle) + String(noti_tabTagText.prefix(5))) + "\(roomID)" + (noti_moviePath.capitalized + String(kGiftName)) + "\(errorCode)" + (String(m_directionFormat)) + "\(reason)" + (String(mArrangementStr)) + "\(extendedData).")
                //: func__showStatusBarErrorMsg(showMsg: "Party room exception".localized)
                colorGift(showMsg: (String(userTipIdent.suffix(4)) + "y room" + String(noti_shareData) + String(dreamSharedErrorPath)).localized)
                // 进房失败埋点
                //: uploadRecord.uploadRecordEvent(eventID: click_partyTab_creParty_fail, parameterStr: ["errCode": String(errorCode)])
                kResolutionText.toStr(eventID: dreamMicBarMsg, parameterStr: [String(bytes: kErrorId.map{photoWith(main: $0)}, encoding: .utf8)!: String(errorCode)])
            }
            //: voiceRoom_releaseAllResource()
            program()
        }
    }

    // MARK: - 推流回调

    /// 当前用户推流状态回调
    //: func onPublisherStateUpdate(_ state: ZegoPublisherState, errorCode: Int32, extendedData: [AnyHashable: Any]?, streamID: String) {
    func onPublisherStateUpdate(_: ZegoPublisherState, errorCode: Int32, extendedData _: [AnyHashable: Any]?, streamID: String) {
        //: guard errorCode == 0 || errorCode == 1003020 || errorCode == 1002055 else {
        guard errorCode == 0 || errorCode == 1_003_020 || errorCode == 1_002_055 else { // 推流失败，用户下麦
            //: UploadLogTool.writeLog(msg: "VOICEROOM: 当前用户推流失败，roomID: \(partyModel.roomId), errorCode: \(errorCode), streamID: \(streamID).")
            LabelLogTool.fillInLine(msg: (dreamTipId.uppercased() + "ROOM: " + k_connectionIdent + "失败，roomI" + String(noti_atUrl.prefix(3))) + "\(partyModel.roomId)" + (String(k_rawName.suffix(6)) + String(mBlankId.suffix(7))) + "\(errorCode)" + (String(main_editKey.prefix(5)) + String(k_countIdent.suffix(7))) + "\(streamID).")
            //: func__showStatusBarErrorMsg(showMsg: "An error occurred, please try again".localized)
            colorGift(showMsg: String(bytes: k_mId.map{originType(view: $0)}, encoding: .utf8)!.localized)
            //: let dict = getAllMikePositions()
            let dict = my()
            //: for (position, model) in dict where String(model.uid) == PathAppManager.share.loginUid {
            for (position, model) in dict where String(model.uid) == PathAppManager.share.loginUid {
                //: voiceRoom_reqChangeMike(type: 2, position: position)
                level(type: 2, position: position)
                //: resetMikePosition(position: position, model: model)
                addIcon(position: position, model: model)
            }
            //: if errorCode == 1002078 {
            if errorCode == 1_002_078 { // token过期，关闭房间
                //: voiceRoom_releaseAllResource()
                program()
            }
            //: return
            return
        }
    }

    // MARK: - 拉流回调

    /// 当前用户拉流状态回调
    //: func onPlayerStateUpdate(_ state: ZegoPlayerState, errorCode: Int32, extendedData: [AnyHashable: Any]?, streamID: String) {
    func onPlayerStateUpdate(_: ZegoPlayerState, errorCode: Int32, extendedData _: [AnyHashable: Any]?, streamID: String) {
        //: guard errorCode == 0 else {
        guard errorCode == 0 else { // 拉流失败
            //: UploadLogTool.writeLog(msg: "VOICEROOM: 拉流失败，roomID: \(partyModel.roomId), errorCode: \(errorCode), streamID: \(streamID).")
            LabelLogTool.fillInLine(msg: (appShowFormat.replacingOccurrences(of: "time", with: "E") + "OOM:" + user_starData + "败，roomI" + String(dreamGiftValue.suffix(3))) + "\(partyModel.roomId)" + (String(k_rawName.suffix(6)) + String(mBlankId.suffix(7))) + "\(errorCode)" + (String(main_editKey.prefix(5)) + String(k_countIdent.suffix(7))) + "\(streamID).")
            //: if errorCode == 1002078 {
            if errorCode == 1_002_078 { // token过期，关闭房间
                //: voiceRoom_releaseAllResource()
                program()
            }

            //: return
            return
        }
    }

    // MARK: - 流更新回调

    /// 流更新回调
    //: func onRoomStreamUpdate(_ updateType: ZegoUpdateType, streamList: [ZegoStream], extendedData: [AnyHashable: Any]?, roomID: String) {
    func onRoomStreamUpdate(_ updateType: ZegoUpdateType, streamList: [ZegoStream], extendedData _: [AnyHashable: Any]?, roomID: String) {
        //: guard roomID == partyModel.roomId else { return }
        guard roomID == partyModel.roomId else { return }
        //: switch updateType {
        switch updateType {
        //: case .add:
        case .add:
            //: streamList.forEach { zego in
            streamList.forEach { zego in
                // 开始拉流
                //: ZegoExpressEngine.shared().startPlayingStream(zego.streamID)
                ZegoExpressEngine.shared().startPlayingStream(zego.streamID)
            }

        //: case .delete:
        case .delete:
            //: streamList.forEach { zego in
            streamList.forEach { zego in
                // 停止拉流
                //: ZegoExpressEngine.shared().stopPlayingStream(zego.streamID)
                ZegoExpressEngine.shared().stopPlayingStream(zego.streamID)
            }

        //: @unknown default:
        @unknown default:
            //: fatalError()
            fatalError()
        }
    }

    // MARK: - 声浪等级监听回调

    /// 本地声浪等级
    //: func onCapturedSoundLevelUpdate(_ soundLevel: NSNumber) {
    func onCapturedSoundLevelUpdate(_ soundLevel: NSNumber) {
        //: guard Int(truncating: soundLevel) > 0 else { return }
        guard Int(truncating: soundLevel) > 0 else { return }
        //: voiceRoomVC?.mikePositionView.refreshSoundLevelAnimation(position: partyModel.position, level: soundLevel)
        voiceRoomVC?.mikePositionView.appear(position: partyModel.position, level: soundLevel)
    }

    /// 远端声浪等级
    //: func onRemoteSoundLevelUpdate(_ soundLevels: [String: NSNumber]) {
    func onRemoteSoundLevelUpdate(_ soundLevels: [String: NSNumber]) {
        //: let dict = getAllMikePositions()
        let dict = my()
        //: for (streamID, level) in soundLevels {
        for (streamID, level) in soundLevels {
            //: let components = streamID.split(separator: "-").map(String.init)
            let components = streamID.split(separator: "-").map(String.init)
            //: guard components.count > 2 else { continue }
            guard components.count > 2 else { continue }
            //: if let position = dict.first(where: { String($0.value.uid) == components[1] })?.key {
            if let position = dict.first(where: { String($0.value.uid) == components[1] })?.key {
                //: voiceRoomVC?.mikePositionView.refreshSoundLevelAnimation(position: position, level: level)
                voiceRoomVC?.mikePositionView.appear(position: position, level: level)
            }
        }
    }

    // MARK: - 信令

    /// 上麦、下麦、锁麦、解锁麦、切麦操作会收到后端下发信令
    //: func onIMRecvCustomCommand(_ command: String, from fromUser: ZegoUser, roomID: String) {
    func onIMRecvCustomCommand(_ command: String, from _: ZegoUser, roomID: String) {
        //: guard roomID == partyModel.roomId else { return }
        guard roomID == partyModel.roomId else { return }
        //: guard let decodedStr = command.removingPercentEncoding else {
        guard let decodedStr = command.removingPercentEncoding else {
            //: UploadLogTool.writeLog(msg: "VOICEROOM: 信令解析失败，command: \(command), roomID: \(roomID).")
            LabelLogTool.fillInLine(msg: (String(dream_textRawUrl) + String(user_arrayName.prefix(4)) + "ommand: ") + "\(command)" + (String(m_makeStr.suffix(5)) + "mID: ") + "\(roomID).")
            //: return
            return
        }
        // 更新麦位信息，刷新列表UI
        //: let json = JSON(parseJSON: decodedStr)
        let json = JSON(parseJSON: decodedStr)
        //: let position = json["data"]["position"].intValue
        let position = json[(String(mainRowValue))][(mainRevenueTitle.lowercased() + String(notiPreviousPath))].intValue
        //: guard position < MIKE_SEAT_COUNT else { return }
        guard position < show_userUrl else { return }
        //: guard let mikeModel = TalkingMikeListItemModel.deserialize(from: json["data"].dictionaryObject) else { return }
        guard let mikeModel = PortMeasurable.deserialize(from: json[(String(mainRowValue))].dictionaryObject) else { return }
        //: printLog(message: "xxxx-- \(json.rawValue)")
        printLog(message: (String(app_executeTextContent.prefix(7))) + "\(json.rawValue)")
        //: if json["opType"].stringValue == "party:changeMike" { // 房主/观众 麦位操作信令
        if json[(String(mainShareLabelText) + noti_fromMsg.replacingOccurrences(of: "image", with: "e"))].stringValue == (String(mEqualTitle) + String(mLimitColorValue.suffix(4))) { // 房主/观众 麦位操作信令
            //: let type = json["data"]["type"].intValue
            let type = json[(String(mainRowValue))][(kInmateId.replacingOccurrences(of: "field", with: "e"))].intValue
            //: switch type {
            switch type {
            //: case 1:
            case 1: // 上麦
                //: mikeModel.needGift = true
                mikeModel.needGift = true
                //: updateMikePosition(position: position, model: mikeModel)
                equality(position: position, model: mikeModel)

            //: case 2:
            case 2: // 下麦
                //: if let oldModel = getMikePosition(key: position) {
                if let oldModel = exampleApplication(key: position) {
                    //: resetMikePosition(position: position, model: oldModel)
                    addIcon(position: position, model: oldModel)
                }

            //: case 3:
            case 3: // 切麦
                //: resetMikePosition(position: position, model: mikeModel)
                addIcon(position: position, model: mikeModel)
                //: let toPosition = json["data"]["toPosition"].intValue
                let toPosition = json[(String(mainRowValue))][(String(app_imagePath))].intValue
                //: mikeModel.needGift = true
                mikeModel.needGift = true
                //: updateMikePosition(position: toPosition, model: mikeModel)
                equality(position: toPosition, model: mikeModel)

            //: case 4, 5:
            case 4, 5: // 开麦、闭麦
                //: updateMikePosition(position: position, model: mikeModel)
                equality(position: position, model: mikeModel)

            //: default:
            default:
                //: break
                break
            }
            // 刷新礼物面板头像
            //: if type == 1 || type == 2 || type == 3 {
            if type == 1 || type == 2 || type == 3 {
                //: voiceRoomVC?.bottomView.refreshGiftIcon()
                voiceRoomVC?.bottomView.shouldAcross()
            }

            //: } else if json["opType"].stringValue == "party:adminChangeMike" { // 房主对他人操作信令
        } else if json[(String(mainShareLabelText) + noti_fromMsg.replacingOccurrences(of: "image", with: "e"))].stringValue == (noti_netMessage.replacingOccurrences(of: "menu", with: "t") + "y:ad" + String(main_infoProfileDetailUrl)) { // 房主对他人操作信令
            //: switch json["data"]["type"].intValue {
            switch json[(String(mainRowValue))][(kInmateId.replacingOccurrences(of: "field", with: "e"))].intValue {
            //: case 1, 2:
            case 1, 2: // 锁麦、解锁麦
                //: updateMikePosition(position: position, model: mikeModel)
                equality(position: position, model: mikeModel)

            //: case 3:
            case 3: // 踢下麦
                //: if let oldModel = getMikePosition(key: position) {
                if let oldModel = exampleApplication(key: position) {
                    //: resetMikePosition(position: position, model: oldModel)
                    addIcon(position: position, model: oldModel)
                    // 刷新礼物面板头像
                    //: voiceRoomVC?.bottomView.refreshGiftIcon()
                    voiceRoomVC?.bottomView.shouldAcross()
                    //: guard String(oldModel.uid) == PathAppManager.share.loginUid else { return }
                    guard String(oldModel.uid) == PathAppManager.share.loginUid else { return }
                    //: ZegoExpressEngine.shared().stopPublishingStream()
                    ZegoExpressEngine.shared().stopPublishingStream()
                    //: func__showStatusBarErrorMsg(showMsg: "You were kicked off the seat".localized)
                    colorGift(showMsg: (String(kModelViewText.suffix(4)) + "were k" + String(noti_mainStr.prefix(6)) + "off " + mWhiteObjectMessage.replacingOccurrences(of: "to", with: "h") + String(userAtUrl.suffix(5))).localized)
                }

            //: case 4:
            case 4: // 开麦
                //: updateMikePosition(position: position, model: mikeModel)
                equality(position: position, model: mikeModel)
                //: guard String(mikeModel.uid) == PathAppManager.share.loginUid else { return }
                guard String(mikeModel.uid) == PathAppManager.share.loginUid else { return }
                //: ZegoExpressEngine.shared().mutePublishStreamAudio(false)
                ZegoExpressEngine.shared().mutePublishStreamAudio(false)

            //: case 5:
            case 5: // 闭麦
                //: updateMikePosition(position: position, model: mikeModel)
                equality(position: position, model: mikeModel)
                //: guard String(mikeModel.uid) == PathAppManager.share.loginUid else { return }
                guard String(mikeModel.uid) == PathAppManager.share.loginUid else { return }
                //: ZegoExpressEngine.shared().mutePublishStreamAudio(true)
                ZegoExpressEngine.shared().mutePublishStreamAudio(true)

            //: default:
            default:
                //: break
                break
            }

            //: } else if json["opType"].stringValue == "party:changeCoin" { // 更新用户麦位金币
        } else if json[(String(mainShareLabelText) + noti_fromMsg.replacingOccurrences(of: "image", with: "e"))].stringValue == (String(k_centerValue) + String(noti_contentLabelStr.prefix(6))) { // 更新用户麦位金币
            //: TalkingVoiceRoomManager.shared().getAllMikePositions().forEach { position, model in
            VoxNameReactiveCompatible.listOf().my().forEach { position, model in
                //: if model.uid == mikeModel.uid {
                if model.uid == mikeModel.uid {
                    //: model.coin = mikeModel.coin
                    model.coin = mikeModel.coin
                    //: updateMikePosition(position: position, model: model)
                    equality(position: position, model: model)
                    //: return
                    return
                }
            }

            //: } else if json["opType"].stringValue == "party:closeStream" { // 推流失败
        } else if json[(String(mainShareLabelText) + noti_fromMsg.replacingOccurrences(of: "image", with: "e"))].stringValue == (String(m_menuKey)) { // 推流失败
            //: ZegoExpressEngine.shared().stopPublishingStream()
            ZegoExpressEngine.shared().stopPublishingStream()
        }
    }
}

// MARK: - 信令处理

//: extension TalkingVoiceRoomManager {
extension VoxNameReactiveCompatible {
    /// 更新麦位数据，刷新UI
    /// - Parameters:
    ///   - position: 麦位
    ///   - model: 数据
    //: private func updateMikePosition(position: Int, model: TalkingMikeListItemModel) {
    private func equality(position: Int, model: PortMeasurable) {
        //: modifyMikePosition(key: position, model: model)
        passePartout(key: position, model: model)
        //: voiceRoomVC?.mikePositionView.refreshSingleMikeView(position: position)
        voiceRoomVC?.mikePositionView.mike(position: position)
        //: updateBottomMicStatusIfCurrentUser(uid: model.uid)
        following(uid: model.uid)
    }

    /// 重置麦位数据，刷新UI
    /// - Parameters:
    ///   - position: 麦位
    ///   - model: 数据
    //: private func resetMikePosition(position: Int, model: TalkingMikeListItemModel) {
    private func addIcon(position: Int, model: PortMeasurable) {
        //: modifyMikePosition(key: position, model: TalkingMikeListItemModel())
        passePartout(key: position, model: PortMeasurable())
        //: voiceRoomVC?.mikePositionView.refreshSingleMikeView(position: position)
        voiceRoomVC?.mikePositionView.mike(position: position)
        //: updateBottomMicStatusIfCurrentUser(uid: model.uid)
        following(uid: model.uid)
    }

    /// 更新本人麦位、底部麦克风状态
    /// - Parameter uid: 用户Id
    //: private func updateBottomMicStatusIfCurrentUser(uid: Int) {
    private func following(uid: Int) {
        //: guard String(uid) == PathAppManager.share.loginUid else { return }
        guard String(uid) == PathAppManager.share.loginUid else { return }
        //: let dict = getAllMikePositions()
        let dict = my()
        //: if let position = dict.first(where: { $0.value.uid == uid })?.key {
        if let position = dict.first(where: { $0.value.uid == uid })?.key {
            //: partyModel.position = position
            partyModel.position = position
            //: } else {
        } else {
            //: partyModel.position = -1
            partyModel.position = -1
        }
        //: voiceRoomVC?.bottomView.refreshBottomMicStatus()
        voiceRoomVC?.bottomView.byStatus()
    }
}

// MARK: - 麦位字典安全操作

//: extension TalkingVoiceRoomManager {
extension VoxNameReactiveCompatible {
    /// 获取初始化模型
    //: private func initMikePosition() -> [Int: TalkingMikeListItemModel] {
    private func positionKey() -> [Int: PortMeasurable] {
        //: for index in 0 ..< MIKE_SEAT_COUNT {
        for index in 0 ..< show_userUrl {
            //: modifyMikePosition(key: index, model: TalkingMikeListItemModel())
            passePartout(key: index, model: PortMeasurable())
        }
        //: return mikePositionDict
        return mikePositionDict
    }

    /// 添加/修改 麦位
    //: func modifyMikePosition(key: Int, model: TalkingMikeListItemModel) {
    func passePartout(key: Int, model: PortMeasurable) {
        //: mplock.lock()
        mplock.lock()
        //: defer { mplock.unlock() }
        defer { mplock.unlock() }
        //: mikePositionDict[key] = model
        mikePositionDict[key] = model
    }

    /// 获取麦位
    //: func getMikePosition(key: Int) -> TalkingMikeListItemModel? {
    func exampleApplication(key: Int) -> PortMeasurable? {
        //: mplock.lock()
        mplock.lock()
        //: defer { mplock.unlock() }
        defer { mplock.unlock() }
        //: return mikePositionDict[key]
        return mikePositionDict[key]
    }

    /// 获取所有麦位
    //: func getAllMikePositions() -> [Int: TalkingMikeListItemModel] {
    func my() -> [Int: PortMeasurable] {
        //: mplock.lock()
        mplock.lock()
        //: defer { mplock.unlock() }
        defer { mplock.unlock() }
        //: return mikePositionDict
        return mikePositionDict
    }
}
