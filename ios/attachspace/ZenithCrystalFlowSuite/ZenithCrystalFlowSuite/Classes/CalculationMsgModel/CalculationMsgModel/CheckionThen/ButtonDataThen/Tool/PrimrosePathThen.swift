
//: Declare String Begin

/*: "msgType" :*/
fileprivate let show_backPath:String = "msgTypemake more"

/*: "tips" :*/
fileprivate let show_buttonNameHeightData:[Character] = ["t","i","p","s"]

/*: "extra" :*/
fileprivate let main_elementValue:String = "extrstop"

/*: "msgInfo" :*/
fileprivate let mainRangeData:[Character] = ["m","s","g","I","n","f","o"]

/*: "user" :*/
fileprivate let m_enableMessage:String = "toser"

/*: "uid" :*/
fileprivate let show_titleAvailableName:String = "modelid"

/*: "nickname" :*/
fileprivate let m_resultId:[UInt8] = [0xb7,0xb2,0xac,0xb4,0xb7,0xaa,0xb6,0xae]

fileprivate func sessionCell(touch num: UInt8) -> UInt8 {
    let value = Int(num) - 73
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "headPic" :*/
fileprivate let kTaskIdent:[UInt8] = [0x50,0x4d,0x49,0x4c,0x38,0x51,0x4b]

fileprivate func recordToImage(empty num: UInt8) -> UInt8 {
    let value = Int(num) - 232
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "age" :*/
fileprivate let showInsideEqualTitle:[UInt8] = [0xc4,0xca,0xc8]

fileprivate func quantityHandle(manager num: UInt8) -> UInt8 {
    let value = Int(num) - 99
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "gift" :*/
fileprivate let user_choiceKey:String = "GIFT"

/*: "isGroup" :*/
fileprivate let mainTextPath:String = "isGroupfilter view extension true"

/*: "WantGift" :*/
fileprivate let notiSkinContent:String = "view extra self typeWantGift"

/*: "txt" :*/
fileprivate let m_contentViewFormat:String = "tarrayt"

/*: "img" :*/
fileprivate let noti_sharePath:String = "IMG"

/*: "video" :*/
fileprivate let dream_messagePathTopIdent:[Character] = ["v","i","d","e","o"]

/*: "audio" :*/
fileprivate let showSexKey:[Character] = ["a","u","d","i","o"]

/*: "messageType" :*/
fileprivate let dreamShowText:[Character] = ["m","e","s","s","a"]
fileprivate let dreamShouldIdent:[Character] = ["g","e","T","y","p","e"]

/*: "contentType" :*/
fileprivate let user_genderMessage:[Character] = ["c","o","n","t","e","n","t"]
fileprivate let main_loadText:[Character] = ["T","y","p","e"]

/*: "ImgMsg" :*/
fileprivate let noti_addValue:String = "model"
fileprivate let noti_tipMessage:String = "mgMsgicon self total touch of"

/*: "imageUri" :*/
fileprivate let kCenterLabelFormat:String = "imtable"
fileprivate let main_noData:String = "turn shouldgeUri"

/*: "type" :*/
fileprivate let dreamSearchKey:[Character] = ["t","y","p","e"]

/*: "pic" :*/
fileprivate let main_titleUrl:[Character] = ["p","i","c"]

/*: "isBurn" :*/
fileprivate let main_contentUrl:String = "as type model with rankisBurn"

/*: "giftId" :*/
fileprivate let kMainValue:[Character] = ["g","i","f","t","I","d"]

/*: "params" :*/
fileprivate let show_presentationUrl:[Character] = ["p","a","r","a","m","s"]

/*: "audioLength" :*/
fileprivate let main_modelRecordStr:String = "generate view imageaudioL"

/*: "len" :*/
fileprivate let notiPicMessage:String = "leevaluate"

/*: "audioSandbox" :*/
fileprivate let app_colorText:String = "showudi"
fileprivate let kFileFormat:String = "dbequal"

/*: "senderId" :*/
fileprivate let main_insideIdent:[Character] = ["s","e","n","d","e","r","I","d"]

/*: "AudioMsg" :*/
fileprivate let show_normalMessage:String = "end view out sharedAu"
fileprivate let kExamineNextHeightKey:String = "dioMsgsex app empty"

/*: "content" :*/
fileprivate let appIconMessage:String = "coframeen"
fileprivate let dreamObjectCountData:[Character] = ["t"]

/*: "TxtMsg" :*/
fileprivate let show_tabPath:[Character] = ["T","x","t","M","s","g"]

/*: "message" :*/
fileprivate let noti_sizeTitle:[Character] = ["m","e","s","s","a","g","e"]

/*: "quoteMsgId" :*/
fileprivate let k_conStyleId:String = "fatal"
fileprivate let appViewInsideReturnMessage:String = "uoteMfrom request block head make"

/*: "quoteMsgInfo" :*/
fileprivate let mModelWrapKey:[Character] = ["q","u","o","t","e","M"]
fileprivate let dreamMakeTitle:String = "sgInfocell var background success"

/*: "rYMsgType" :*/
fileprivate let show_requestData:[Character] = ["r","Y","M","s","g","T","y","p","e"]

/*: "GJ:Sess:AutoGreet:TxtCustom" :*/
fileprivate let appImageUrl:String = "gift tap letGJ:Se"
fileprivate let main_scaleValue:String = "add string toss:A"
fileprivate let app_countFormat:String = "view incomereet:"
fileprivate let show_playIdent:String = "equal size let ok caseTxtC"

/*: "changeDirection" :*/
fileprivate let mainEqualMsg:String = "shared bind action mediumchangeD"
fileprivate let kSendPath:[Character] = ["i","r"]
fileprivate let m_sinceTitle:[Character] = ["e","c","t","i","o","n"]

/*: "system" :*/
fileprivate let m_iconValue:[Character] = ["s","y","s","t","e"]
fileprivate let mainModePopValue:String = "add"

/*: "title" :*/
fileprivate let mWithMsg:String = "tstatustle"

/*: "id" :*/
fileprivate let appRemoveMsg:String = "manager"

/*: "icon" :*/
fileprivate let mainAvailableTitle:[Character] = ["i","c","o","n"]

/*: "all" :*/
fileprivate let mTempUrl:String = "ALL"

/*: "icon_chats_pcr" :*/
fileprivate let app_keyMessage:[Character] = ["i","c","o","n","_","c","h","a","t","s","_"]
fileprivate let user_toPath:String = "pdata"

/*: "status" :*/
fileprivate let app_playFormat:String = "sttopus"

/*: "msgIncome" :*/
fileprivate let kShareIdent:String = "list"
fileprivate let showCenterName:[Character] = ["s","g","I","n","c","o","m","e"]

/*: "msgId" :*/
fileprivate let dreamTextStr:String = "msgIdlabel to file note content"

/*: "RestateLabParHashable" :*/
fileprivate let kBirthdayColorValue:String = "content key infoTMsg"
fileprivate let mainBoxMsg:[Character] = ["a"]
fileprivate let notiNameIdent:String = "center table valueteStatus"

/*: "TMsgTranslatedContent" :*/
fileprivate let m_marginValue:[Character] = ["T","M","s","g","T","r","a"]
fileprivate let mainListKey:String = "nsfillat"
fileprivate let user_succeedStr:String = "TENT"

/*: "formatType" :*/
fileprivate let dream_toMessage:String = "formatTshow if point height"
fileprivate let main_shareName:String = "yptap"

/*: "plain" :*/
fileprivate let noti_globalIdent:String = "platextn"

/*: "imageData" :*/
fileprivate let appGiftPushId:String = "imageDaadd let key make return"
fileprivate let user_sourceStr:[Character] = ["t","a"]

/*: "VideoMsg" :*/
fileprivate let noti_cellData:String = "message inside status let ifVideoMsg"

/*: "match" :*/
fileprivate let notiCornerKey:[Character] = ["m","a","t","c","h"]

/*: "mf" :*/
fileprivate let kFailIdent:[Character] = ["m","f"]

/*: "userIntro" :*/
fileprivate let mScreenMessage:String = "listen log case quote appuserI"
fileprivate let kPathMessage:[Character] = ["n","t","r","o"]

/*: "targetId" :*/
fileprivate let dreamLabData:String = "tamake"
fileprivate let mDetailValue:String = "true betweengetId"

/*: "yyyy-MM-dd HH:mm:ss" :*/
fileprivate let dream_bindOkVarPath:String = "picy"
fileprivate let dream_makeValue:[Character] = ["y","-","M","M","-","d","d"," ","H","H",":","m","m",":","s","s"]

/*: "sendTime" :*/
fileprivate let kObjectByStr:String = "sendTimedata touch"

/*: "isRead" :*/
fileprivate let mainMarginFormat:String = "title type letisRead"

/*: "audioData" :*/
fileprivate let k_haveData:String = "to item view return bottomaudioDat"
fileprivate let mComponentData:String = "render"

/*: "audioUri" :*/
fileprivate let show_insertStr:[Character] = ["a","u","d","i","o"]
fileprivate let noti_processMsg:String = "info data string make returnUri"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  PrimrosePathThen.swift

//: import Foundation
import Foundation

// MARK: - 枚举定义

//: @objc public enum TXMessageContentType: UInt {
@objc public enum CommentSourceInsetTarget: UInt {
    //: case TXMessageContentType_text = 1
    case TXMessageContentType_text = 1
    //: case TXMessageContentType_picture
    case TXMessageContentType_picture
    //: case TXMessageContentType_gift
    case TXMessageContentType_gift
    //: case TXMessageContentType_askforGift
    case TXMessageContentType_askforGift
    //: case TXMessageContentType_voice
    case TXMessageContentType_voice
    //: case TXMessageContentType_local
    case TXMessageContentType_local
    //: case TXMessageContentType_tips
    case TXMessageContentType_tips
    //: case TXMessageContentType_userCard
    case TXMessageContentType_userCard
    //: case TXMessageContentType_video
    case TXMessageContentType_video
}

// MARK: - PrimrosePathThen

//: @objcMembers public class TUISocialChatManager: NSObject {
@objcMembers public class PrimrosePathThen: NSObject {
    // MARK: - 公共方法

    /// 获取消息插入的时间戳 毫秒
    //: public class func getMessageInsertTime() -> Int64 {
    public class func replacement() -> Int64 {
        //: let curDate = Date().timeIntervalSince1970
        let curDate = Date().timeIntervalSince1970
        //: let msInterval = Int64(floor(curDate * 1000))
        let msInterval = Int64(floor(curDate * 1000))
        //: var deltaTime = Int64(V2TIMManager.sharedInstance().getServerTime())
        var deltaTime = Int64(V2TIMManager.sharedInstance().getServerTime()) // 秒为单位
        //: if deltaTime < 1 {
        if deltaTime < 1 {
            //: deltaTime = 0
            deltaTime = 0
        }
        //: let timeStamp = msInterval - deltaTime
        let timeStamp = msInterval - deltaTime
        //: return timeStamp
        return timeStamp
    }

    // MARK: - 腾讯IM消息处理

    /// 腾讯IM消息插入
    //: public class func insertTXMessage(withExtral extral: [String: Any], toUid: String, isSender: Bool, isTip: Bool) -> ButtonDataThen? {
    public class func barWrite(withExtral extral: [String: Any], toUid: String, isSender: Bool, isTip: Bool) -> ButtonDataThen? {
        //: let data = handleTXMessageSend(withExtral: extral, isSender: isSender, isTip: isTip)
        let data = weightAdd(withExtral: extral, isSender: isSender, isTip: isTip)
        //: if let data = data {
        if let data = data {
            //: CharterDataManager.shared.func__handleMsg(targetId: toUid, msgCellData: data, isPrivete: true)
            CharterDataManager.shared.checkPrivete(targetId: toUid, msgCellData: data, isPrivete: true)
        }
        //: return data
        return data
    }

    /// 腾讯IM发送消息组装
    //: public class func handleTXMessageSend(withExtral extral: [String: Any], isSender: Bool, isTip: Bool) -> ButtonDataThen? {
    public class func weightAdd(withExtral extral: [String: Any], isSender: Bool, isTip: Bool) -> ButtonDataThen? {
        //: if isTip {
        if isTip {
            //: var extralDict = extral
            var extralDict = extral
            //: extralDict["msgType"] = "tips"
            extralDict[(String(show_backPath.prefix(7)))] = (String(show_buttonNameHeightData))
            //: let extraDict = ["extra": extralDict]
            let extraDict = [(main_elementValue.replacingOccurrences(of: "stop", with: "a")): extralDict]
            //: let extra = parseTXMessageJsonStr(data: extraDict)
            let extra = textPush(data: extraDict)
            //: let data = TalkingChatTipsMsgCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = StyleReactiveCompatible(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extra.data(using: .utf8)
            let cData = extra.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: data.innerMessage.isExcludedFromLastMessage = true
            data.innerMessage.isExcludedFromLastMessage = true
            //: let model = AbTalkingChatMsgBaseModel()
            let model = StageLiveModel()
            //: if let tipModel = try? AbTalkingChatMsgTipsModel(dictionary: extralDict) {
            if let tipModel = try? NameModel(dictionary: extralDict) {
                //: model.tips = tipModel
                model.tips = tipModel
            }
            //: data.msgModel = model
            data.msgModel = model
            //: return data
            return data
        }

        //: var tempDict = extral
        var tempDict = extral
        //: guard let msgInfo = extral["msgInfo"] as? [String: Any] else {
        guard let msgInfo = extral[(String(mainRangeData))] as? [String: Any] else {
            //: return nil
            return nil
        }

        //: tempDict["user"] = [
        tempDict[(m_enableMessage.replacingOccurrences(of: "to", with: "u"))] = [
            //: "uid": PathAppManager.share.loginUserMode.userID,
            (show_titleAvailableName.replacingOccurrences(of: "model", with: "u")): PathAppManager.share.loginUserMode.userID,
            //: "nickname": PathAppManager.share.loginUserMode.nickname,
            String(bytes: m_resultId.map{sessionCell(touch: $0)}, encoding: .utf8)!: PathAppManager.share.loginUserMode.nickname,
            //: "headPic": PathAppManager.share.loginUserMode.headPic,
            String(bytes: kTaskIdent.map{recordToImage(empty: $0)}, encoding: .utf8)!: PathAppManager.share.loginUserMode.headPic,
            //: "age": PathAppManager.share.loginUserMode.age,
            String(bytes: showInsideEqualTitle.map{quantityHandle(manager: $0)}, encoding: .utf8)!: PathAppManager.share.loginUserMode.age,
        ]

        //: let extraDict = ["extra": tempDict]
        let extraDict = [(main_elementValue.replacingOccurrences(of: "stop", with: "a")): tempDict]
        //: let extraString = parseTXMessageJsonStr(data: extraDict)
        let extraString = textPush(data: extraDict)

        // 礼物
        //: if msgInfo["msgType"] as? String == "gift" {
        if msgInfo[(String(show_backPath.prefix(7)))] as? String == (user_choiceKey.lowercased()) {
            //: var data: ButtonDataThen
            var data: ButtonDataThen
            //: if msgInfo["isGroup"] as? String == "1" {  // 群聊
            if msgInfo[(String(mainTextPath.prefix(7)))] as? String == "1" { // 群聊
                //: data = TalkingGroupChatGiftMsgCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                data = WindowCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: } else {
            } else {
                //: data = InsideCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                data = InsideCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            }

            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? StageLiveModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 索要礼物
        //: if msgInfo["msgType"] as? String == "WantGift" {
        if msgInfo[(String(show_backPath.prefix(7)))] as? String == (String(notiSkinContent.suffix(8))) {
            //: let data = IndexVideoReactiveCompatible(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = IndexVideoReactiveCompatible(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? StageLiveModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 文字
        //: if msgInfo["msgType"] as? String == "txt" {
        if msgInfo[(String(show_backPath.prefix(7)))] as? String == (m_contentViewFormat.replacingOccurrences(of: "array", with: "x")) {
            //: var data: ButtonDataThen
            var data: ButtonDataThen
            //: if msgInfo["isGroup"] as? String == "1" {  // 群聊
            if msgInfo[(String(mainTextPath.prefix(7)))] as? String == "1" { // 群聊
                //: data = ConnectionReactiveCompatible(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                data = ConnectionReactiveCompatible(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: } else {
            } else {
                //: data = ButtonCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                data = ButtonCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            }
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? StageLiveModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 图片
        //: if msgInfo["msgType"] as? String == "img" {
        if msgInfo[(String(show_backPath.prefix(7)))] as? String == (noti_sharePath.lowercased()) {
            //: let data = ChatThen(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = ChatThen(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? StageLiveModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 视频
        //: if msgInfo["msgType"] as? String == "video" {
        if msgInfo[(String(show_backPath.prefix(7)))] as? String == (String(dream_messagePathTopIdent)) {
            //: let data = DiskThen(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = DiskThen(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? StageLiveModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 语音
        //: if msgInfo["msgType"] as? String == "audio" {
        if msgInfo[(String(show_backPath.prefix(7)))] as? String == (String(showSexKey)) {
            //: let data = CollectionCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = CollectionCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? StageLiveModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 打招呼
        //: if extral["gift"] != nil && (msgInfo["messageType"] as? Int) == 4 {
        if extral[(user_choiceKey.lowercased())] != nil && (msgInfo[(String(dreamShowText) + String(dreamShouldIdent))] as? Int) == 4 {
            //: let data = ButtonCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = ButtonCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? StageLiveModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        //: return nil
        return nil
    }

    /// 腾讯IM重发消息组装 （只有私聊在用）
    //: public class func handleTXMessageReSend(withData cellData: ButtonDataThen) -> ButtonDataThen? {
    public class func indexAt(withData cellData: ButtonDataThen) -> ButtonDataThen? {
        // 图片
        //: if let reCellData = cellData as? ChatThen {
        if let reCellData = cellData as? ChatThen {
            //: let paths = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true)
            let paths = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true)
            //: var filePath = paths.first!
            var filePath = paths.first!
            //: filePath = (filePath as NSString).appendingPathComponent(FacultyThen.getImageFileName())
            filePath = (filePath as NSString).appendingPathComponent(FacultyThen.tableTop())
            //: filePath = (filePath as NSString).appendingPathComponent((reCellData.imageUrl! as NSString).lastPathComponent)
            filePath = (filePath as NSString).appendingPathComponent((reCellData.imageUrl! as NSString).lastPathComponent)

            //: if !FileManager.default.fileExists(atPath: filePath) {
            if !FileManager.default.fileExists(atPath: filePath) {
                //: return nil
                return nil
            }

            //: var dic: [String: Any] = [:]
            var dic: [String: Any] = [:]
            //: var params: [String: Any] = [:]
            var params: [String: Any] = [:]
            //: var msgInfo: [String: Any] = [:]
            var msgInfo: [String: Any] = [:]
            //: msgInfo["contentType"] = "ImgMsg"
            msgInfo[(String(user_genderMessage) + String(main_loadText))] = (noti_addValue.replacingOccurrences(of: "model", with: "I") + String(noti_tipMessage.prefix(5)))
            //: msgInfo["msgType"] = "img"
            msgInfo[(String(show_backPath.prefix(7)))] = (noti_sharePath.lowercased())
            //: msgInfo["imageUri"] = filePath
            msgInfo[(kCenterLabelFormat.replacingOccurrences(of: "table", with: "a") + String(main_noData.suffix(5)))] = filePath
            //: dic["msgInfo"] = msgInfo
            dic[(String(mainRangeData))] = msgInfo

            //: params["type"] = "ImgMsg"
            params[(String(dreamSearchKey))] = (noti_addValue.replacingOccurrences(of: "model", with: "I") + String(noti_tipMessage.prefix(5)))
            //: params["pic"] = filePath
            params[(String(main_titleUrl))] = filePath

            // 重发私密照片
            //: let isBurn = reCellData.msgModel.msgInfo.customSend.isBurn
            let isBurn = reCellData.msgModel.msgInfo.customSend.isBurn
            //: if !isBurn.isEmpty {
            if !isBurn.isEmpty {
                //: params["isBurn"] = isBurn
                params[(String(main_contentUrl.suffix(6)))] = isBurn
                //: } else {
            } else {
                //: params["isBurn"] = "0"
                params[(String(main_contentUrl.suffix(6)))] = "0"
            }

            //: let giftId = reCellData.msgModel.msgInfo.customSend.giftId
            let giftId = reCellData.msgModel.msgInfo.customSend.giftId
            //: if !giftId.isEmpty {
            if !giftId.isEmpty {
                //: params["giftId"] = giftId
                params[(String(kMainValue))] = giftId
                //: } else {
            } else {
                //: params["giftId"] = "0"
                params[(String(kMainValue))] = "0"
            }

            //: dic["params"] = params
            dic[(String(show_presentationUrl))] = params

            //: let data = handleTXMessageSend(withExtral: dic, isSender: true, isTip: false)
            let data = weightAdd(withExtral: dic, isSender: true, isTip: false)
            //: data?.status = .Msg_Status_Fail
            data?.status = .Msg_Status_Fail
            //: return data
            return data
        }

        // 视频
        //: if let reCellData = cellData as? DiskThen {
        if let reCellData = cellData as? DiskThen {
            //: reCellData.status = .Msg_Status_Fail
            reCellData.status = .Msg_Status_Fail
            //: return reCellData
            return reCellData
        }

        // 语音
        //: if let reCellData = cellData as? CollectionCellData {
        if let reCellData = cellData as? CollectionCellData {
            //: let voiceModel = DBUserInfoManager.cache_getAudioDB(message: reCellData.innerMessage)
            let voiceModel = ModeratenessReactiveCompatible.sourceLine(message: reCellData.innerMessage)
            //: var dic: [String: Any] = [:]
            var dic: [String: Any] = [:]
            //: var msgInfo: [String: Any] = [:]
            var msgInfo: [String: Any] = [:]
            //: var params: [String: Any] = [:]
            var params: [String: Any] = [:]

            //: if !voiceModel.db_audioLength.isEmpty {
            if !voiceModel.db_audioLength.isEmpty {
                //: msgInfo["audioLength"] = voiceModel.db_audioLength
                msgInfo[(String(main_modelRecordStr.suffix(6)) + "ength")] = voiceModel.db_audioLength
                //: params["len"] = voiceModel.db_audioLength
                params[(notiPicMessage.replacingOccurrences(of: "evaluate", with: "n"))] = voiceModel.db_audioLength
            }
            //: if !voiceModel.db_filePath.isEmpty {
            if !voiceModel.db_filePath.isEmpty {
                //: msgInfo["audioSandbox"] = voiceModel.db_filePath
                msgInfo[(app_colorText.replacingOccurrences(of: "show", with: "a") + "oSan" + kFileFormat.replacingOccurrences(of: "equal", with: "ox"))] = voiceModel.db_filePath
                //: params["audioSandbox"] = voiceModel.db_filePath
                params[(app_colorText.replacingOccurrences(of: "show", with: "a") + "oSan" + kFileFormat.replacingOccurrences(of: "equal", with: "ox"))] = voiceModel.db_filePath
            }
            //: msgInfo["msgType"] = "audio"
            msgInfo[(String(show_backPath.prefix(7)))] = (String(showSexKey))
            //: msgInfo["senderId"] = PathAppManager.share.loginUserMode.userID
            msgInfo[(String(main_insideIdent))] = PathAppManager.share.loginUserMode.userID
            //: dic["msgInfo"] = msgInfo
            dic[(String(mainRangeData))] = msgInfo

            //: params["type"] = "AudioMsg"
            params[(String(dreamSearchKey))] = (String(show_normalMessage.suffix(2)) + String(kExamineNextHeightKey.prefix(6)))
            //: dic["params"] = params
            dic[(String(show_presentationUrl))] = params

            //: let data = handleTXMessageSend(withExtral: dic, isSender: true, isTip: false)
            let data = weightAdd(withExtral: dic, isSender: true, isTip: false)
            //: data?.status = .Msg_Status_Fail
            data?.status = .Msg_Status_Fail
            //: return data
            return data
        }

        // 文本
        //: if let reCellData = cellData as? ButtonCellData {
        if let reCellData = cellData as? ButtonCellData {
            //: var dic: [String: Any] = [:]
            var dic: [String: Any] = [:]
            //: var params: [String: Any] = [:]
            var params: [String: Any] = [:]
            //: var msgInfo: [String: Any] = [:]
            var msgInfo: [String: Any] = [:]

            //: msgInfo["content"] = reCellData.contentStr ?? ""
            msgInfo[(appIconMessage.replacingOccurrences(of: "frame", with: "nt") + String(dreamObjectCountData))] = reCellData.contentStr ?? ""
            //: msgInfo["msgType"] = "txt"
            msgInfo[(String(show_backPath.prefix(7)))] = (m_contentViewFormat.replacingOccurrences(of: "array", with: "x"))
            //: dic["msgInfo"] = msgInfo
            dic[(String(mainRangeData))] = msgInfo
            //: params["type"] = "TxtMsg"
            params[(String(dreamSearchKey))] = (String(show_tabPath))
            //: params["message"] = reCellData.contentStr ?? ""
            params[(String(noti_sizeTitle))] = reCellData.contentStr ?? ""

            //: if reCellData.msgModel.quoteMsgInfo.isQuoteMsg() == true {
            if reCellData.msgModel.quoteMsgInfo.table() == true { // 引用消息重发
                //: params["quoteMsgId"] = reCellData.msgModel.quoteMsgInfo.msgId
                params[(k_conStyleId.replacingOccurrences(of: "fatal", with: "q") + String(appViewInsideReturnMessage.prefix(5)) + "sgId")] = reCellData.msgModel.quoteMsgInfo.msgId
                //: dic["quoteMsgInfo"] = reCellData.msgModel.quoteMsgInfo
                dic[(String(mModelWrapKey) + String(dreamMakeTitle.prefix(6)))] = reCellData.msgModel.quoteMsgInfo
                    //: .toDictionary()
                    .toDictionary()
            }

            //: dic["params"] = params
            dic[(String(show_presentationUrl))] = params

            //: let data = handleTXMessageSend(withExtral: dic, isSender: true, isTip: false)
            let data = weightAdd(withExtral: dic, isSender: true, isTip: false)
            //: data?.status = .Msg_Status_Fail
            data?.status = .Msg_Status_Fail
            //: return data
            return data
        }

        //: return nil
        return nil
    }

    /// 腾讯IM接收消息组装
    //: public class func handleTXReceiveMessage(withExtral dic: [String: Any], cusData: Data, tempModel: V2TIMMessage) -> ButtonDataThen? {
    public class func chromosomeMapping(withExtral dic: [String: Any], cusData: Data, tempModel: V2TIMMessage) -> ButtonDataThen? {
        //: var cellData: ButtonDataThen?
        var cellData: ButtonDataThen?
        //: guard let extraDict = dic["extra"] as? [String: Any],
        guard let extraDict = dic[(main_elementValue.replacingOccurrences(of: "stop", with: "a"))] as? [String: Any],
              //: let msgInfoDict = extraDict["msgInfo"] as? [String: Any] else {
              let msgInfoDict = extraDict[(String(mainRangeData))] as? [String: Any]
        else {
            //: return nil
            return nil
        }

        //: let msgInfo = msgInfoDict
        let msgInfo = msgInfoDict
        //: let user = extraDict["user"] as? [String: Any] ?? [:]
        let user = extraDict[(m_enableMessage.replacingOccurrences(of: "to", with: "u"))] as? [String: Any] ?? [:]

        //: guard let model = try? AbTalkingChatMsgBaseModel(dictionary: extraDict) else {
        guard let model = try? StageLiveModel(dictionary: extraDict) else {
            //: return nil
            return nil
        }
        //: var isSelf = (model.user.uid == PathAppManager.share.loginUserMode.userID.intValue)
        var isSelf = (model.user.uid == PathAppManager.share.loginUserMode.userID.intValue)

        // 后台代发送的自动打招呼消息
        //: var isAutoSendMsg = false
        var isAutoSendMsg = false
        //: if model.msgInfo.uid.intValue == PathAppManager.share.loginUserMode.userID.intValue &&
        if model.msgInfo.uid.intValue == PathAppManager.share.loginUserMode.userID.intValue &&
            //: model.msgInfo.messageType.rawValue == 3 {
            model.msgInfo.messageType.rawValue == 3
        {
            //: isSelf = true
            isSelf = true
            //: isAutoSendMsg = true
            isAutoSendMsg = true
        }

        //: if dic["rYMsgType"] as? String == "GJ:Sess:AutoGreet:TxtCustom" {
        if dic[(String(show_requestData))] as? String == (String(appImageUrl.suffix(5)) + String(main_scaleValue.suffix(4)) + "utoG" + String(app_countFormat.suffix(5)) + String(show_playIdent.suffix(4)) + "ustom") {
            //: isSelf = msgInfo["changeDirection"] as? Bool ?? false
            isSelf = msgInfo[(String(mainEqualMsg.suffix(7)) + String(kSendPath) + String(m_sinceTitle))] as? Bool ?? false
        }

        //: let contentType = messageCellType(dic)
        let contentType = stateType(dic)

        // 系统消息
        //: if extraDict["msgType"] as? String == "system" ||
        if extraDict[(String(show_backPath.prefix(7)))] as? String == (String(m_iconValue) + mainModePopValue.replacingOccurrences(of: "add", with: "m")) ||
            //: contentType == .TXMessageContentType_tips {
            contentType == .TXMessageContentType_tips
        {
            //: let model = AbTalkingChatMsgBaseModel()
            let model = StageLiveModel()
            //: let tipModel = try? AbTalkingChatMsgTipsModel(dictionary: extraDict)
            let tipModel = try? NameModel(dictionary: extraDict)
            //: cellData = TalkingChatTipsMsgCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = StyleReactiveCompatible(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: if let content = dic["content"] as? String {
            if let content = dic[(appIconMessage.replacingOccurrences(of: "frame", with: "nt") + String(dreamObjectCountData))] as? String {
                //: tipModel?.content = content
                tipModel?.content = content
            }
            //: if tipModel != nil {
            if tipModel != nil {
                //: model.tips = tipModel!
                model.tips = tipModel!
            }
            //: cellData?.msgModel = model
            cellData?.msgModel = model
            //: return cellData
            return cellData
        }

        // 其他消息类型的创建...
        //: switch contentType {
        switch contentType {
        //: case .TXMessageContentType_picture:
        case .TXMessageContentType_picture:
            //: cellData = ChatThen(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = ChatThen(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)

        //: case .TXMessageContentType_video:
        case .TXMessageContentType_video:
            //: cellData = DiskThen(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = DiskThen(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)

        //: case .TXMessageContentType_gift:
        case .TXMessageContentType_gift:
            //: if let groupID = tempModel.groupID, !groupID.isEmpty {
            if let groupID = tempModel.groupID, !groupID.isEmpty { // 群聊gift
                //: cellData = TalkingGroupChatGiftMsgCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = WindowCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: } else {
            } else { // 私聊gift
                //: cellData = InsideCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = InsideCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            }

        //: case .TXMessageContentType_askforGift:
        case .TXMessageContentType_askforGift: // 索要礼物
            //: let tempDic = parseTXMessageData(data: cusData)
            let tempDic = chorusData(data: cusData)
            //: if let extraData = tempDic["extra"] as? [String: Any],
            if let extraData = tempDic[(main_elementValue.replacingOccurrences(of: "stop", with: "a"))] as? [String: Any],
               //: let msgInfoData = extraData["msgInfo"] as? [String: Any],
               let msgInfoData = extraData[(String(mainRangeData))] as? [String: Any],
               //: !msgInfoData.isEmpty {
               !msgInfoData.isEmpty
            {
                //: if let info = try? AbTalkingChatMsgInfoModel(dictionary: msgInfoData) {
                if let info = try? StatusWithModel(dictionary: msgInfoData) {
                    //: model.msgInfo = info
                    model.msgInfo = info
                }
            }
            //: cellData = IndexVideoReactiveCompatible(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = IndexVideoReactiveCompatible(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: cellData?.msgModel = model
            cellData?.msgModel = model

        //: case .TXMessageContentType_voice:
        case .TXMessageContentType_voice:
            //: cellData = CollectionCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = CollectionCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: if let audioCellData = cellData as? CollectionCellData {
            if let audioCellData = cellData as? CollectionCellData {
                //: if audioCellData.voiceModel.db_voiceCacheWrap.msgId.isEmpty {
                if audioCellData.voiceModel.db_voiceCacheWrap.msgId.isEmpty {
                    //: let mainKey = checkPrimaryKey(withMsgModel: tempModel)
                    let mainKey = pathTo(withMsgModel: tempModel)
                    //: var cacheWrap = WCDBVoiceMsgTable.db_getVoiceMsg(with: mainKey)
                    var cacheWrap = CommentMsgTable.customer(with: mainKey)
                    //: if cacheWrap == nil {
                    if cacheWrap == nil {
                        //: cacheWrap = WCDBVoiceMsgTable.db_inserVoiceMsgToDB(packageDict(withTXMessage: tempModel))
                        cacheWrap = CommentMsgTable.launchNight(stochasticProcess(withTXMessage: tempModel))
                        //: audioCellData.voiceModel.db_voiceCacheWrap = cacheWrap!
                        audioCellData.voiceModel.db_voiceCacheWrap = cacheWrap!
                        //: } else {
                    } else {
                        //: audioCellData.voiceModel.db_voiceCacheWrap = cacheWrap!
                        audioCellData.voiceModel.db_voiceCacheWrap = cacheWrap!
                    }
                }
            }

        //: case .TXMessageContentType_local:
        case .TXMessageContentType_local:
            //: cellData = TalkingChatMatchMsgCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = AppCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)

        //: default:
        default:
            //: let title = extraDict["title"] as? String ?? ""
            let title = extraDict[(mWithMsg.replacingOccurrences(of: "status", with: "i"))] as? String ?? ""
            //: let content = extraDict["content"] as? String ?? ""
            let content = extraDict[(appIconMessage.replacingOccurrences(of: "frame", with: "nt") + String(dreamObjectCountData))] as? String ?? ""
            //: let img = extraDict["img"] as? String ?? ""
            let img = extraDict[(noti_sharePath.lowercased())] as? String ?? ""
            //: if !title.isEmpty || !content.isEmpty || !img.isEmpty {
            if !title.isEmpty || !content.isEmpty || !img.isEmpty { // 系统新消息
                //: cellData = TalkingChatSystemNotifJumpMsgCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = ModelCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: cellData?.extraDict = extraDict
                cellData?.extraDict = extraDict
                //: } else if let groupID = tempModel.groupID, !groupID.isEmpty {
            } else if let groupID = tempModel.groupID, !groupID.isEmpty { // 群聊text
                //: cellData = ConnectionReactiveCompatible(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = ConnectionReactiveCompatible(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: } else {
            } else { // 私聊text
                //: cellData = ButtonCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = ButtonCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            }
        }

        // 设置用户信息
        //: if model.user.nickname.isEmpty {
        if model.user.nickname.isEmpty {
            //: var userDict = user
            var userDict = user
            //: userDict.merge(msgInfo) { (_, new) in new }
            userDict.merge(msgInfo) { _, new in new }
            //: if let userModel = try? AbTalkingChatMsgUserModel(dictionary: userDict) {
            if let userModel = try? ButtonModel(dictionary: userDict) {
                //: if userModel.uid == 0 {
                if userModel.uid == 0 {
                    //: userModel.uid = userDict["id"] as? Int ?? 0
                    userModel.uid = userDict["id"] as? Int ?? 0
                }
                //: if userModel.headPic.isEmpty == true {
                if userModel.headPic.isEmpty == true {
                    //: userModel.headPic = userDict["icon"] as? String ?? ""
                    userModel.headPic = userDict[(String(mainAvailableTitle))] as? String ?? ""
                }

                //: if isAutoSendMsg {
                if isAutoSendMsg {
                    //: userModel.uid = model.msgInfo.toUid.intValue
                    userModel.uid = model.msgInfo.toUid.intValue
                    //: userModel.headPic =
                    userModel.headPic =
                        //: PathAppManager.share.loginUserMode.headPic ?? ""
                        PathAppManager.share.loginUserMode.headPic ?? ""
                }
                //: model.user = userModel
                model.user = userModel
            }
        }

        //: if model.msgInfo == nil {
        if model.msgInfo == nil {
            //: let msgInfoModel = AbTalkingChatMsgInfoModel()
            let msgInfoModel = StatusWithModel()
            //: msgInfoModel.content = dic["content"] as? String ?? ""
            msgInfoModel.content = dic[(appIconMessage.replacingOccurrences(of: "frame", with: "nt") + String(dreamObjectCountData))] as? String ?? ""
            //: msgInfoModel.jumps = extraDict["extra"] as? [Any] ?? []
            msgInfoModel.jumps = extraDict[(main_elementValue.replacingOccurrences(of: "stop", with: "a"))] as? [Any] ?? []
            //: model.msgInfo = msgInfoModel
            model.msgInfo = msgInfoModel
        }

        // 聊天室送礼消息
        //: if contentType == .TXMessageContentType_gift && tempModel.groupID != nil && !tempModel.groupID!.isEmpty {
        if contentType == .TXMessageContentType_gift && tempModel.groupID != nil && !tempModel.groupID!.isEmpty {
            //: if model.toUser.uid.isEmpty {
            if model.toUser.uid.isEmpty { // 送礼所有人
                //: let toUserInfo = AbTalkingChatMsgToUserModel()
                let toUserInfo = StartingModel()
                //: toUserInfo.uid = "all"
                toUserInfo.uid = (mTempUrl.lowercased())
                //: toUserInfo.nickname = model.gift.sendNum
                toUserInfo.nickname = model.gift.sendNum
                //: toUserInfo.headPic = "icon_chats_pcr"
                toUserInfo.headPic = (String(app_keyMessage) + user_toPath.replacingOccurrences(of: "data", with: "cr"))
                //: model.toUser = toUserInfo
                model.toUser = toUserInfo
            }
        }
        //: cellData?.msgModel = model
        cellData?.msgModel = model

        // 本地自定义字段处理
        //: let customStr = String(data: cusData, encoding: .utf8) ?? ""
        let customStr = String(data: cusData, encoding: .utf8) ?? ""
        //: let customDic = NSDictionary.dictionary(withJsonString: customStr) ?? [:]
        let customDic = NSDictionary.textName(withJsonString: customStr) ?? [:]
        //: let status = TMsgStatus(rawValue: UInt(customDic["status"] as? Int ?? 0)) ?? .Msg_Status_Init
        let status = TMsgStatus(rawValue: UInt(customDic[(app_playFormat.replacingOccurrences(of: "top", with: "at"))] as? Int ?? 0)) ?? .Msg_Status_Init
        //: cellData?.status = status
        cellData?.status = status
        //: cellData?.msgIncome = CGFloat(customDic["msgIncome"] as? Float ?? 0)
        cellData?.msgIncome = CGFloat(customDic[(kShareIdent.replacingOccurrences(of: "list", with: "m") + String(showCenterName))] as? Float ?? 0)
        //: cellData?.messageType = msgInfo["messageType"] as? Int ?? 0
        cellData?.messageType = msgInfo[(String(dreamShowText) + String(dreamShouldIdent))] as? Int ?? 0

        //: if let msgId = customDic["msgId"] as? String {
        if let msgId = customDic[(String(dreamTextStr.prefix(5)))] as? String {
            //: cellData?.msgModel.msgInfo.msgId = msgId
            cellData?.msgModel.msgInfo.msgId = msgId
        }

        // 文本翻译
        //: if let translateStatus = customDic["TMsgTranslateStatus"] as? Int {
        if let translateStatus = customDic[(String(kBirthdayColorValue.suffix(4)) + "Transl" + String(mainBoxMsg) + String(notiNameIdent.suffix(8)))] as? Int {
            //: cellData?.translateStatus = TMsgTranslateStatus(rawValue: UInt(translateStatus)) ?? .MsgTranslate_init
            cellData?.translateStatus = RestateLabParHashable(rawValue: UInt(translateStatus)) ?? .MsgTranslate_init
        }
        //: if let translatedContent = customDic["TMsgTranslatedContent"] as? String {
        if let translatedContent = customDic[(String(m_marginValue) + mainListKey.replacingOccurrences(of: "fill", with: "l") + "edCon" + user_succeedStr.lowercased())] as? String {
            //: cellData?.translatedContent = translatedContent
            cellData?.translatedContent = translatedContent
        }

        // 私密照片Cache
        //: if contentType == .TXMessageContentType_picture {
        if contentType == .TXMessageContentType_picture {
            //: let dict = AbTalkingChatMsgIntimatePhotoModel.loadLocalIntimatePhotoDict()
            let dict = StochasticProcessModel.kind()
            //: if let photoData = dict[tempModel.msgID as Any] as? String {
            if let photoData = dict[tempModel.msgID as Any] as? String {
                //: cellData?.msgModel.msgInfo.intimatePhoto = AbTalkingChatMsgIntimatePhotoModel(string: photoData, error: nil)
                cellData?.msgModel.msgInfo.intimatePhoto = StochasticProcessModel(string: photoData, error: nil)
            }
        }

        // 私密视频Cache
        //: if contentType == .TXMessageContentType_video {
        if contentType == .TXMessageContentType_video {
            //: let dict = AbTalkingChatMsgVideoModel.loadLocalIntimateVideoDict()
            let dict = ActualModel.nose()
            //: if let videoData = dict[tempModel.msgID as Any] as? String {
            if let videoData = dict[tempModel.msgID as Any] as? String {
                //: cellData?.msgModel.msgInfo.video = AbTalkingChatMsgVideoModel(string: videoData, error: nil)
                cellData?.msgModel.msgInfo.video = ActualModel(string: videoData, error: nil)
            }
        }

        // 索要礼物Cache
        //: if contentType == .TXMessageContentType_askforGift {
        if contentType == .TXMessageContentType_askforGift {
            //: let arr = WantGiftModel.loadAskForGiftArr()
            let arr = MomentUserModel.sussOut()
            //: if arr.contains(tempModel.msgID as Any) {
            if arr.contains(tempModel.msgID as Any) { // 已发送
                //: cellData?.msgModel.msgInfo.wantGift.sendStatus = 1
                cellData?.msgModel.msgInfo.wantGift.sendStatus = 1
            }
        }

        //: return cellData
        return cellData
    }

    // MARK: - 辅助方法

    /// 根据字典获取消息类型
    //: public class func messageCellType(_ extraDic: [String: Any]) -> TXMessageContentType {
    public class func stateType(_ extraDic: [String: Any]) -> CommentSourceInsetTarget {
        //: guard let dic = extraDic["extra"] as? [String: Any],
        guard let dic = extraDic[(main_elementValue.replacingOccurrences(of: "stop", with: "a"))] as? [String: Any],
              //: let msgInfo = dic["msgInfo"] as? [String: Any],
              let msgInfo = dic[(String(mainRangeData))] as? [String: Any],
              //: let model = try? AbTalkingChatMsgBaseModel(dictionary: dic)
              let model = try? StageLiveModel(dictionary: dic)
        //: else {
        else {
            //: return .TXMessageContentType_text
            return .TXMessageContentType_text
        }

        // 系统消息 官方小密
        //: if dic["formatType"] as? String == "plain" {
        if dic[(String(dream_toMessage.prefix(7)) + main_shareName.replacingOccurrences(of: "tap", with: "e"))] as? String == (noti_globalIdent.replacingOccurrences(of: "text", with: "i")) {
            //: let imageUri = dic["imageUri"] as? String ?? ""
            let imageUri = dic[(kCenterLabelFormat.replacingOccurrences(of: "table", with: "a") + String(main_noData.suffix(5)))] as? String ?? ""
            //: let imageData = dic["imageData"] as? String ?? ""
            let imageData = dic[(String(appGiftPushId.prefix(7)) + String(user_sourceStr))] as? String ?? ""
            //: let isImageStyle = !imageData.isEmpty || !imageUri.isEmpty
            let isImageStyle = !imageData.isEmpty || !imageUri.isEmpty
            //: if isImageStyle {
            if isImageStyle {
                //: return .TXMessageContentType_picture
                return .TXMessageContentType_picture
            }
        }

        // 图片||私密照片
        //: if !model.msgInfo.imageUri.isEmpty || !model.msgInfo.imageData.isEmpty || !model.msgInfo.intimatePhoto.originUrl.isEmpty {
        if !model.msgInfo.imageUri.isEmpty || !model.msgInfo.imageData.isEmpty || !model.msgInfo.intimatePhoto.originUrl.isEmpty {
            //: return .TXMessageContentType_picture
            return .TXMessageContentType_picture
        }

        // 接收的视频消息 || 本人发送的视频消息
        //: if !model.msgInfo.video.videoUrl.isEmpty || model.msgInfo.contentType == "VideoMsg" {
        if !model.msgInfo.video.videoUrl.isEmpty || model.msgInfo.contentType == (String(noti_cellData.suffix(8))) {
            //: return .TXMessageContentType_video
            return .TXMessageContentType_video
        }

        // 礼物，非打招呼消息
        //: if !model.gift.name.isEmpty && model.msgInfo.messageType.rawValue != 4 {
        if !model.gift.name.isEmpty && model.msgInfo.messageType.rawValue != 4 {
            //: return .TXMessageContentType_gift
            return .TXMessageContentType_gift
        }

        //: let isVoiceStyle = checkVoiceMsg(msginfo: msgInfo)
        let isVoiceStyle = makeScript(msginfo: msgInfo)
        //: if isVoiceStyle {
        if isVoiceStyle {
            //: return .TXMessageContentType_voice
            return .TXMessageContentType_voice
        }

        //: if (dic["match"] as? Int) == 1 || (msgInfo["match"] as? Int) == 1 {
        if (dic[(String(notiCornerKey))] as? Int) == 1 || (msgInfo[(String(notiCornerKey))] as? Int) == 1 {
            // 匹配消息
            //: return .TXMessageContentType_local
            return .TXMessageContentType_local
        }

        //: if dic["msgType"] as? String == "tips" || (dic["messageType"] as? Int) == 17 {
        if dic[(String(show_backPath.prefix(7)))] as? String == (String(show_buttonNameHeightData)) || (dic[(String(dreamShowText) + String(dreamShouldIdent))] as? Int) == 17 {
            // 提示消息,包含超时未回复
            //: return .TXMessageContentType_tips
            return .TXMessageContentType_tips
        }

        //: if (dic["formatType"] as? String == "mf") && ((dic["messageType"] as? Int) == 15 || (dic["messageType"] as? Int) == 16) {
        if (dic[(String(dream_toMessage.prefix(7)) + main_shareName.replacingOccurrences(of: "tap", with: "e"))] as? String == "mf") && ((dic[(String(dreamShowText) + String(dreamShouldIdent))] as? Int) == 15 || (dic[(String(dreamShowText) + String(dreamShouldIdent))] as? Int) == 16) {
            // 提示消息，打招呼提示送出神秘礼物,对方设置价格消息提示
            //: return .TXMessageContentType_tips
            return .TXMessageContentType_tips
        }

        //: if dic["msgType"] as? String == "userIntro" {
        if dic[(String(show_backPath.prefix(7)))] as? String == (String(mScreenMessage.suffix(5)) + String(kPathMessage)) {
            //: return .TXMessageContentType_userCard
            return .TXMessageContentType_userCard
        }

        //: if msgInfo["msgType"] as? String == "WantGift" || (msgInfo["messageType"] as? Int) == 25 {
        if msgInfo[(String(show_backPath.prefix(7)))] as? String == (String(notiSkinContent.suffix(8))) || (msgInfo[(String(dreamShowText) + String(dreamShouldIdent))] as? Int) == 25 {
            //: return .TXMessageContentType_askforGift
            return .TXMessageContentType_askforGift
        }

        //: return .TXMessageContentType_text
        return .TXMessageContentType_text
    }

    // MARK: - 语音消息处理

    /// 从TX的model里提取参数
    //: public class func packageDict(withTXMessage message: V2TIMMessage) -> [String: Any] {
    public class func stochasticProcess(withTXMessage message: V2TIMMessage) -> [String: Any] {
        //: let extra = String(data: message.customElem.data, encoding: .utf8) ?? ""
        let extra = String(data: message.customElem.data, encoding: .utf8) ?? ""
        //: let dic = NSDictionary.dictionary(withJsonString: extra) ?? [:]
        let dic = NSDictionary.textName(withJsonString: extra) ?? [:]
        //: let extraData = dic["extra"] as? [String: Any]
        let extraData = dic[(main_elementValue.replacingOccurrences(of: "stop", with: "a"))] as? [String: Any]
        //: let msgInfo = extraData?["msgInfo"] as? [String: Any]
        let msgInfo = extraData?[(String(mainRangeData))] as? [String: Any]
        //: let user = extraData?["user"] as? [String: Any]
        let user = extraData?[(m_enableMessage.replacingOccurrences(of: "to", with: "u"))] as? [String: Any]

        //: let senderId = msgInfo?["senderId"] as? String ?? ""
        let senderId = msgInfo?[(String(main_insideIdent))] as? String ?? ""
        //: var info = msgInfo
        var info = msgInfo

        // 自己发送的和接收的数据结构不一样
        //: if senderId == PathAppManager.share.loginUserMode.userID {
        if senderId == PathAppManager.share.loginUserMode.userID {
            //: info?["senderId"] = senderId
            info?[(String(main_insideIdent))] = senderId
            //: info?["targetId"] = message.userID ?? (user?["targetId"] as? String ?? "")
            info?[(dreamLabData.replacingOccurrences(of: "make", with: "r") + String(mDetailValue.suffix(5)))] = message.userID ?? (user?[(dreamLabData.replacingOccurrences(of: "make", with: "r") + String(mDetailValue.suffix(5)))] as? String ?? "")
            //: } else {
        } else {
            //: info?["senderId"] = "\(msgInfo?["uid"] as? Int ?? 0)"
            info?[(String(main_insideIdent))] = "\(msgInfo?[(show_titleAvailableName.replacingOccurrences(of: "model", with: "u"))] as? Int ?? 0)"
            //: info?["targetId"] = "\(msgInfo?["uid"] as? Int ?? 0)"
            info?[(dreamLabData.replacingOccurrences(of: "make", with: "r") + String(mDetailValue.suffix(5)))] = "\(msgInfo?[(show_titleAvailableName.replacingOccurrences(of: "model", with: "u"))] as? Int ?? 0)"
        }

        //: if msgInfo?.keys.contains("msgId") ?? false {
        if msgInfo?.keys.contains((String(dreamTextStr.prefix(5)))) ?? false {
            //: info?["msgId"] = "\(msgInfo?["msgId"] ?? "")"
            info?[(String(dreamTextStr.prefix(5)))] = "\(msgInfo?[(String(dreamTextStr.prefix(5)))] ?? "")"
            //: } else {
        } else {
            //: let customStr = String(data: message.localCustomData, encoding: .utf8) ?? ""
            let customStr = String(data: message.localCustomData, encoding: .utf8) ?? ""
            //: let customDic = NSDictionary.dictionary(withJsonString: customStr) ?? [:]
            let customDic = NSDictionary.textName(withJsonString: customStr) ?? [:]
            //: if customDic["msgId"] != nil {
            if customDic[(String(dreamTextStr.prefix(5)))] != nil {
                //: info?["msgId"] = "\(customDic["msgId"] ?? "")"
                info?[(String(dreamTextStr.prefix(5)))] = "\(customDic[(String(dreamTextStr.prefix(5)))] ?? "")"
                //: } else {
            } else {
                //: let txMsgId = !message.msgID.isEmpty ? message.msgID : "\(Int64(Date().timeIntervalSince1970))"
                let txMsgId = !message.msgID.isEmpty ? message.msgID : "\(Int64(Date().timeIntervalSince1970))"
                //: info?["msgId"] = txMsgId
                info?[(String(dreamTextStr.prefix(5)))] = txMsgId
            }
        }

        //: if msgInfo?.keys.contains("audioSandbox") ?? false {
        if msgInfo?.keys.contains((app_colorText.replacingOccurrences(of: "show", with: "a") + "oSan" + kFileFormat.replacingOccurrences(of: "equal", with: "ox"))) ?? false {
            //: info?["audioSandbox"] = "\(msgInfo?["audioSandbox"] ?? "")"
            info?[(app_colorText.replacingOccurrences(of: "show", with: "a") + "oSan" + kFileFormat.replacingOccurrences(of: "equal", with: "ox"))] = "\(msgInfo?[(app_colorText.replacingOccurrences(of: "show", with: "a") + "oSan" + kFileFormat.replacingOccurrences(of: "equal", with: "ox"))] ?? "")"
        }
        //: if msgInfo?.keys.contains("audioLength") ?? false {
        if msgInfo?.keys.contains((String(main_modelRecordStr.suffix(6)) + "ength")) ?? false {
            //: info?["audioLength"] = "\(msgInfo?["audioLength"] ?? "")"
            info?[(String(main_modelRecordStr.suffix(6)) + "ength")] = "\(msgInfo?[(String(main_modelRecordStr.suffix(6)) + "ength")] ?? "")"
        }

        //: let formatter = DateFormatter()
        let formatter = DateFormatter()
        //: formatter.dateFormat = "yyyy-MM-dd HH:mm:ss"
        formatter.dateFormat = (dream_bindOkVarPath.replacingOccurrences(of: "pic", with: "yy") + String(dream_makeValue))
        //: info?["sendTime"] = formatter.string(from: message.timestamp ?? Date())
        info?[(String(kObjectByStr.prefix(8)))] = formatter.string(from: message.timestamp ?? Date())
        //: info?["isRead"] = senderId == PathAppManager.share.loginUserMode.userID ? "1" : "0"
        info?[(String(mainMarginFormat.suffix(6)))] = senderId == PathAppManager.share.loginUserMode.userID ? "1" : "0"

        //: return info ?? [:]
        return info ?? [:]
    }

    //: public class func checkPrimaryKey(withMsgModel tempModel: V2TIMMessage) -> String {
    public class func pathTo(withMsgModel tempModel: V2TIMMessage) -> String {
        //: let v2Info = packageDict(withTXMessage: tempModel)
        let v2Info = stochasticProcess(withTXMessage: tempModel)
        //: let key = "\(v2Info["msgId"] ?? "")"
        let key = "\(v2Info[(String(dreamTextStr.prefix(5)))] ?? "")"
        //: print("key = \(key)")
        //: return key
        return key
    }

    // MARK: - JSON处理方法

    //: @objc public class func parseTXMessageData(data: Data) -> [String: Any] {
    @objc public class func chorusData(data: Data) -> [String: Any] {
        //: do {
        do {
            //: let dict = try JSONSerialization.jsonObject(
            let dict = try JSONSerialization.jsonObject(
                //: with: data, options: .mutableContainers)
                with: data, options: .mutableContainers
            )
            //: return dict as? [String: Any] ?? [:]
            return dict as? [String: Any] ?? [:]
            //: } catch {
        } catch {
            //: return [:]
            return [:]
        }
    }

    //: @objc public class func parseTXMessageJsonStr(data: [String: Any]) -> String {
    @objc public class func textPush(data: [String: Any]) -> String {
        //: do {
        do {
            //: let jsonData = try JSONSerialization.data(
            let jsonData = try JSONSerialization.data(
                //: withJSONObject: data, options: [])
                withJSONObject: data, options: []
            )
            //: return String(data: jsonData, encoding: .utf8) ?? ""
            return String(data: jsonData, encoding: .utf8) ?? ""
            //: } catch {
        } catch {
            //: return ""
            return ""
        }
    }

    // MARK: - 语音消息检测

    //: @objc public class func checkVoiceMsg(msginfo: [String: Any]) -> Bool {
    @objc public class func makeScript(msginfo: [String: Any]) -> Bool {
        //: if let msgType = msginfo["msgType"] as? String, msgType == "audio" {
        if let msgType = msginfo[(String(show_backPath.prefix(7)))] as? String, msgType == (String(showSexKey)) {
            //: return true
            return true
        }
        //: if let contentType = msginfo["contentType"] as? String,
        if let contentType = msginfo[(String(user_genderMessage) + String(main_loadText))] as? String,
           //: contentType == "AudioMsg"
           contentType == (String(show_normalMessage.suffix(2)) + String(kExamineNextHeightKey.prefix(6)))
        {
            //: return true
            return true
        }
        //: if let audioData = msginfo["audioData"] as? String, !audioData.isEmpty {
        if let audioData = msginfo[(String(k_haveData.suffix(8)) + mComponentData.replacingOccurrences(of: "render", with: "a"))] as? String, !audioData.isEmpty {
            //: return true
            return true
        }
        //: if let audioUri = msginfo["audioUri"] as? String, !audioUri.isEmpty {
        if let audioUri = msginfo[(String(show_insertStr) + String(noti_processMsg.suffix(3)))] as? String, !audioUri.isEmpty {
            //: return true
            return true
        }
        //: return false
        return false
    }
}
