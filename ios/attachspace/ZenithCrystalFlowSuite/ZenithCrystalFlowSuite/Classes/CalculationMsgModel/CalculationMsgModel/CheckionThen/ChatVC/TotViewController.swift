
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let userViewGiftVideoIdent:[UInt8] = [0xdc,0xdb,0xdc,0xc1,0x9d,0xd6,0xda,0xd1,0xd0,0xc7,0x8f,0x9c,0x95,0xdd,0xd4,0xc6,0x95,0xdb,0xda,0xc1,0x95,0xd7,0xd0,0xd0,0xdb,0x95,0xdc,0xd8,0xc5,0xd9,0xd0,0xd8,0xd0,0xdb,0xc1,0xd0,0xd1]

private func sumTop(manager num: UInt8) -> UInt8 {
    return num ^ 181
}

/*: "F5F5F5" :*/
fileprivate let mainBackgroundTitle:String = "year5year5year5"

/*: "TalkingChatTextMsgCellID" :*/
fileprivate let notiGiftMakeIdent:[Character] = ["T","a","l","k","i","n","g","C","h","a","t","T","e","x","t","M","s","g"]
fileprivate let dreamPlayFormat:[Character] = ["C","e","l","l","I","D"]

/*: "TalkingChatImageMsgCellID" :*/
fileprivate let mTitleFormat:[Character] = ["T","a","l","k","i","n","g","C","h","a"]
fileprivate let m_firstData:[Character] = ["t","I","m","a","g","e","M","s","g"]
fileprivate let showToolName:[Character] = ["C","e","l","l","I","D"]

/*: "TalkingChatVideoMsgCellID" :*/
fileprivate let show_levelName:String = "Talkin second"
fileprivate let k_customStr:String = "tVideopath image let"

/*: "TalkingChatGiftMsgCellID" :*/
fileprivate let kToolListKey:[Character] = ["T","a","l","k","i","n","g","C","h","a","t","G","i","f","t","M","s","g","C","e","l","l","I","D"]

/*: "ChatForConferWithGiftCompartmentViewCell" :*/
fileprivate let main_shareTitle:[Character] = ["T","a","l","k","i","n","g","C","h","a","t","A","s"]
fileprivate let mainNumberTitle:String = "interaction temp view buildkFor"
fileprivate let kMagnitudePath:String = "gCellbutton image gesture error time"

/*: "TalkingChatTipsMsgCellID" :*/
fileprivate let mainFileId:[Character] = ["T","a","l","k","i","n","g","C","h","a","t","T","i","p","s","M","s","g","C","e","l","l","I","D"]

/*: "TalkingChatMatchMsgCellID" :*/
fileprivate let app_itemIdent:String = "t"
fileprivate let main_leadingText:[Character] = ["a","l","k","i","n","g","C","h","a","t","M","a","t","c","h","M","s","g","C","e","l","l","I","D"]

/*: "TalkingChatAudioMsgCellID" :*/
fileprivate let notiSucceedValue:String = "Talkiself let stop position"
fileprivate let mPackageModelMsg:String = "ngChahidden name model error count"
fileprivate let kPerformPhonePath:String = "with size viewioMsg"

/*: "TalkingChatSystemNotifJumpMsgCellID" :*/
fileprivate let m_arrayStr:[UInt8] = [0x6c,0x59,0x54,0x53,0x51,0x56,0x5f,0x7b,0x50,0x59,0x4c,0x6b,0x41,0x4b,0x4c,0x5d,0x55,0x76,0x57,0x4c,0x51,0x5e,0x72,0x4d,0x55,0x48,0x75,0x4b,0x5f,0x7b,0x5d,0x54,0x54,0x71,0x7c]

/*: "TSystemMessageCell_ReuseId" :*/
fileprivate let dream_beautyFormat:String = "guard let conversation equalTSyste"
fileprivate let dreamAcceptMessage:String = "video if equalsageCe"
fileprivate let mainMakeIdent:String = "seIdlevel start at true video"

/*: "#128CFF" :*/
fileprivate let appAddText:String = "#128CFFmodel data inside guard to"

/*: "Feedback" :*/
fileprivate let app_labName:String = "type status name view whiteFeed"

/*: "#8370FF" :*/
fileprivate let k_tingPath:[Character] = ["#","8","3","7","0","F","F"]

/*: "Quick Greeting" :*/
fileprivate let userSpaceMsg:[Character] = ["Q","u","i","c","k"," ","G"]
fileprivate let noti_colorText:String = "robject"
fileprivate let k_cellKey:[Character] = ["e","t","i","n","g"]

/*: "Beauty Photos" :*/
fileprivate let notiBackgroundInputLetTitle:String = "class let normal blockBeauty "
fileprivate let dream_loadText:[Character] = ["P","h","o","t","o","s"]

/*: "url" :*/
fileprivate let mIndexText:String = "urvalue"

/*: "unlockGift" :*/
fileprivate let userManagerPath:String = "current view sexunlockGi"
fileprivate let appSizeText:[Character] = ["f","t"]

/*: "%d" :*/
fileprivate let app_videoUrl:String = "equal self self var%d"

/*: "isBurn" :*/
fileprivate let m_currentCellMsg:[UInt8] = [0xc5,0xcf,0x9e,0xd1,0xce,0xca]

fileprivate func militaryInstallation(to num: UInt8) -> UInt8 {
    let value = Int(num) + 164
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "giftId" :*/
fileprivate let dreamOfSucceedIdent:String = "regular of self of viewgiftId"

/*: "rYMsgType" :*/
fileprivate let noti_colorMsg:[Character] = ["r","Y","M","s","g","T","y","p"]
fileprivate let notiRestoreTitle:[Character] = ["e"]

/*: "GJ:CallCustom" :*/
fileprivate let user_textTitle:[Character] = ["G","J",":","C","a","l","l"]
fileprivate let m_atPath:String = "fatal let title labCustom"

/*: "changeDirection" :*/
fileprivate let appViewFormat:String = "chpicn"
fileprivate let mainKeyLabelId:String = "estrengthion"

/*: "extra" :*/
fileprivate let k_pushIdent:String = "evaluetra"

/*: "msgInfo" :*/
fileprivate let app_sharedTitle:String = "msgInfoany let"

/*: "toUid" :*/
fileprivate let dreamWithMsg:String = "screen app let panel datatoUid"

/*: "wantGift" :*/
fileprivate let userEnableAddMsg:String = "error else top label selfwantG"
fileprivate let showTextUrl:[Character] = ["i","f","t"]

/*: "jumpKey" :*/
fileprivate let m_modelResolutionMessage:String = "equal class view strokejumpKey"

/*: "truePersonAuth" :*/
fileprivate let user_facultyTitle:[Character] = ["t","r","u","e","P","e","r","s"]
fileprivate let m_midViewBlockId:[Character] = ["o","n","A","u","t","h"]

/*: "logType" :*/
fileprivate let appButtonGroupData:String = "mico"
fileprivate let show_awakeFormat:String = "gTypefor resource self"

/*: "msgType" :*/
fileprivate let dream_valuePath:[UInt8] = [0x36,0x28,0x3c,0xf,0x22,0x2b,0x3e]

/*: "txt" :*/
fileprivate let notiWithData:String = "TXT"

/*: "type" :*/
fileprivate let appListPath:String = "typshare"

/*: "QuickMsg" :*/
fileprivate let mainModifyText:[Character] = ["Q","u","i","c","k"]
fileprivate let mainTopKey:String = "Msgtype normal"

/*: "params" :*/
fileprivate let app_backValueName:[UInt8] = [0x82,0x73,0x84,0x73,0x7f,0x85]

fileprivate func layerTitle(live num: UInt8) -> UInt8 {
    let value = Int(num) - 18
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "content" :*/
fileprivate let main_paraTitle:[UInt8] = [0x8f,0x83,0x82,0x98,0x89,0x82,0x98]

/*: "id" :*/
fileprivate let k_userMessage:String = "idirection"

/*: "txtId" :*/
fileprivate let notiAppBackText:[Character] = ["t","x","t","I","d"]

/*: "message" :*/
fileprivate let noti_labelMessage:[UInt8] = [0x32,0x3a,0x2c,0x2c,0x3e,0x38,0x3a]

private func firstM(of num: UInt8) -> UInt8 {
    return num ^ 95
}

/*: "TxtMsg" :*/
fileprivate let m_tempMessage:String = "content action start layer buttonTxtMsg"

/*: "Voice hello download failed. Procedure" :*/
fileprivate let appTodayProductData:[UInt8] = [0x6b,0x84,0x7e,0x78,0x7a,0x35,0x7d,0x7a,0x81,0x81,0x84,0x35,0x79,0x84,0x8c,0x83,0x81,0x84,0x76,0x79,0x35,0x7b,0x76,0x7e,0x81,0x7a,0x79,0x43,0x35,0x65,0x87,0x84,0x78,0x7a,0x79,0x8a,0x87,0x7a]

fileprivate func highlightSum(upper num: UInt8) -> UInt8 {
    let value = Int(num) + 235
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "length" :*/
fileprivate let main_viewData:String = "ledatagt"
fileprivate let mVideoKey:String = "H"

/*: "audio" :*/
fileprivate let user_timePath:String = "autargeto"

/*: "audioId" :*/
fileprivate let main_shareDataFormat:String = "intimate equalaudioId"

/*: "audioLength" :*/
fileprivate let userViewStatusUrl:[UInt8] = [0x66,0x72,0x63,0x6e,0x68,0x4b,0x62,0x69,0x60,0x73,0x6f]

private func letFor(time num: UInt8) -> UInt8 {
    return num ^ 7
}

/*: "audioSandbox" :*/
fileprivate let notiPushIdent:String = "ajumpi"
fileprivate let dream_componentData:String = "dbogame"

/*: "senderId" :*/
fileprivate let userHiddenTitle:[UInt8] = [0x7f,0x71,0x7a,0x70,0x71,0x7e,0x55,0x70]

fileprivate func cornerAccept(sort num: UInt8) -> UInt8 {
    let value = Int(num) - 12
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "AudioMsg" :*/
fileprivate let m_pathMsg:String = "AudioMslet record"
fileprivate let appMaxMakeUrl:String = "G"

/*: "Can't send blank message" :*/
fileprivate let app_toMessage:[Character] = ["C","a","n","\'","t"," ","s","e","n","d"," ","b","l","a","n","k"," "]
fileprivate let notiDayLengthContent:String = "request"
fileprivate let mModelBlockTitle:String = "blackssagblack"

/*: "quoteMsgId" :*/
fileprivate let dream_voiceValue:String = "quosystem"
fileprivate let main_labContent:String = "Idkit in gift to else"

/*: "quoteMsgInfo" :*/
fileprivate let kColorFormat:[Character] = ["q","u","o","t","e","M","s","g","I","n","f","o"]

/*: "contentType" :*/
fileprivate let show_underText:[UInt8] = [0x1b,0x27,0x26,0x2c,0x1d,0x26,0x2c,0xc,0x31,0x28,0x1d]

fileprivate func decisionMaker(range num: UInt8) -> UInt8 {
    let value = Int(num) - 184
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "ImgMsg" :*/
fileprivate let app_colorOfKey:String = "by in load let delayImgMsg"

/*: "img" :*/
fileprivate let app_itemUrl:String = "logmg"

/*: "imageUri" :*/
fileprivate let mSendValue:[UInt8] = [0xcf,0xcb,0xc7,0xc1,0xc3,0xf3,0xd4,0xcf]

private func titleImage(label num: UInt8) -> UInt8 {
    return num ^ 166
}

/*: "customSend" :*/
fileprivate let mainUpperTitle:[UInt8] = [0xd4,0xc2,0xc4,0xc3,0xd8,0xda,0xe4,0xd2,0xd9,0xd3]

private func labKind(info num: UInt8) -> UInt8 {
    return num ^ 183
}

/*: "pic" :*/
fileprivate let user_actionMessage:String = "PIC"

/*: "VideoMsg" :*/
fileprivate let notiFieldId:String = "send"
fileprivate let showEqualAppText:String = "ideoMsgview manager"

/*: "video" :*/
fileprivate let userAnglePath:String = "VIDEO"

/*: "coverImg" :*/
fileprivate let userCornerAgainName:[UInt8] = [0x91,0x9d,0xa4,0x93,0xa0,0x77,0x9b,0x95]

fileprivate func contentTheory(file num: UInt8) -> UInt8 {
    let value = Int(num) + 210
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "videoUrl" :*/
fileprivate let kLockContent:[UInt8] = [0x6a,0x75,0x78,0x79,0x73,0x49,0x6e,0x70]

/*: "duration" :*/
fileprivate let show_liveText:[UInt8] = [0xc0,0xd1,0xd6,0xc5,0xd0,0xcd,0xcb,0xca]

private func beanEffect(make num: UInt8) -> UInt8 {
    return num ^ 164
}

/*: "len" :*/
fileprivate let showFemaleData:[UInt8] = [0x6e,0x65,0x6c]

/*: "Documents/User/voice/" :*/
fileprivate let k_topContent:String = "transform if let succeed countDocum"
fileprivate let main_cellMessage:[Character] = ["e","n","t","s","/"]
fileprivate let app_infoName:[Character] = ["U","s","e","r","/","v","o","i","c","e","/"]

/*: "Documents/User/record/" :*/
fileprivate let notiDataId:[Character] = ["D","o","c","u","m","e","n","t","s","/","U","s","e","r","/","r","e","c","o","r","d","/"]

/*: "msgIncome" :*/
fileprivate let kFromKey:String = "msgIncomodel voice"
fileprivate let k_labelId:String = "ME"

/*: "mfBean" :*/
fileprivate let showToIdent:[UInt8] = [0x6e,0x61,0x65,0x42,0x66,0x6d]

/*: "messageType" :*/
fileprivate let dream_pathTagMessage:String = "mitemss"

/*: "WantGift" :*/
fileprivate let m_hiddenMessage:[Character] = ["W","a","n","t","G","i","f","t"]

/*: _PrivateChatTipsTitle_ :*/
fileprivate let m_userFormat:String = "_Privclass send self fatal"
fileprivate let mainTotalTitleId:String = "image label true cloudatTips"
fileprivate let userViewMsg:String = "equal"

/*: "msgId" :*/
fileprivate let appGroupValue:[Character] = ["m","s","g","I","d"]

/*: "totalMfCoin" :*/
fileprivate let kSuiteTrackMsg:[Character] = ["t","o","t","a","l","M","f"]
fileprivate let main_sizeName:String = "style passCoin"

/*: "attachSysMsg" :*/
fileprivate let user_seatMsg:String = "acontentcontentac"

/*: "intimatePhoto" :*/
fileprivate let mainAddTitlePath:[Character] = ["i","n","t","i","m","a","t","e","P","h","o","t","o"]

/*: _ :*/
fileprivate let appInputFormat:String = "row"

/*: "totalIntimate" :*/
fileprivate let mainListPath:[Character] = ["t","o","t","a","l","I","n","t","i"]
fileprivate let appManagerReportClearPath:[Character] = ["m","a","t","e"]

/*: "uid" :*/
fileprivate let app_giftMessage:[UInt8] = [0x64,0x69,0x75]

/*: "intimate" :*/
fileprivate let dream_hiddenUrl:[UInt8] = [0x13,0x18,0x1e,0x13,0x17,0xb,0x1e,0xf]

fileprivate func miniData(identity num: UInt8) -> UInt8 {
    let value = Int(num) - 170
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "Can't use the function due to your lack of intimacy level." :*/
fileprivate let show_statusDayMakeName:[UInt8] = [0x2e,0x6c,0x65,0x76,0x65,0x6c,0x20,0x79,0x63,0x61,0x6d,0x69,0x74,0x6e,0x69,0x20,0x66,0x6f,0x20,0x6b,0x63,0x61,0x6c,0x20,0x72,0x75,0x6f,0x79,0x20,0x6f,0x74,0x20,0x65,0x75,0x64,0x20,0x6e,0x6f,0x69,0x74,0x63,0x6e,0x75,0x66,0x20,0x65,0x68,0x74,0x20,0x65,0x73,0x75,0x20,0x74,0x27,0x6e,0x61,0x43]

/*: "giftNum" :*/
fileprivate let kRefuseTitle:[Character] = ["g","i","f","t","N","u","m"]

/*: "pkgItemsetId" :*/
fileprivate let main_playerViewKey:[Character] = ["p","k","g","I","t","e"]
fileprivate let m_backgroundRecordUrl:String = "msetIdto self request center"

/*: "from" :*/
fileprivate let m_userMsg:String = "kitrom"

/*: "wantGiftId" :*/
fileprivate let kCountKey:String = "modelnt"

/*: "WantGiftMsg" :*/
fileprivate let kToThenName:String = "selected equalWantG"

/*: "gift" :*/
fileprivate let main_transactionData:String = "gifvalue"

/*: "View info" :*/
fileprivate let userStatusTitle:String = "View infoview user string gift player"

/*: "Notes" :*/
fileprivate let noti_willIdent:[Character] = ["N","o","t","e","s"]

/*: "Report" :*/
fileprivate let show_awakePath:String = "color heightReport"

/*: "View read receipts?" :*/
fileprivate let kImageOkIdent:String = "View rdata at"
fileprivate let show_nameFailTitleValue:String = "ipts?count gift beauty to transform"

/*: "He" :*/
fileprivate let notiNoteMessage:[Character] = ["H","e"]

/*: "She" :*/
fileprivate let dream_videoData:String = "intimate indexShe"

/*: "%@ is using too low a version" :*/
fileprivate let k_tableGiftPath:String = "stage var for%@ i"
fileprivate let main_viewMsg:String = "head forg too lo"
fileprivate let m_addKey:String = "view if partyw a "
fileprivate let appDateMessage:String = "VERSION"

/*: "Shoot video at least 5 seconds" :*/
fileprivate let kPopPath:[Character] = ["S","h","o","o","t"," ","v","i","d","e","o"," ","a","t"," ","l","e","a","s","t"," ","5"," ","s","e"]
fileprivate let showMomentName:String = "equalonds"

/*: "Upload failed" :*/
fileprivate let app_sizeViewMsg:String = "add pop request class inUploa"
fileprivate let app_textScreenMessage:String = "add"

/*: "播放状态: :*/
fileprivate let noti_bindFormat:String = "播放状\u{6001}:"

/*: "c2c_" :*/
fileprivate let dreamEqualLabKey:[Character] = ["c","2","c","_"]

/*: "msg" :*/
fileprivate let dreamWindowFormat:String = "bottomsg"

/*: "tips" :*/
fileprivate let kManagerPath:String = "tistart"

/*: "You can use the video chat feature to give %@ a call" :*/
fileprivate let kModelName:[UInt8] = [0xaa,0x9c,0x86,0xd3,0x90,0x92,0x9d,0xd3,0x86,0x80,0x96,0xd3,0x87,0x9b,0x96,0xd3,0x85,0x9a,0x97,0x96,0x9c,0xd3,0x90,0x9b,0x92,0x87,0xd3,0x95,0x96,0x92,0x87,0x86,0x81,0x96,0xd3,0x87,0x9c,0xd3,0x94,0x9a,0x85,0x96,0xd3,0xd6,0xb3,0xd3,0x92,0xd3,0x90,0x92,0x9f,0x9f]

private func bindVideo(content num: UInt8) -> UInt8 {
    return num ^ 243
}

/*: "cellData" :*/
fileprivate let userCountOrientId:[Character] = ["c","e","l","l","D","a","t"]
fileprivate let m_selectedPath:[Character] = ["a"]

/*: "System notification" :*/
fileprivate let show_failValue:String = "Systemif voice ya"
fileprivate let notiShowPath:String = "FICATION"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  TotViewController.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/9/9.
//

//: import AVFAudio
import AVFAudio
//: import Photos
import Photos
//: import UIKit
import UIKit

/// 私聊入口枚举
//: enum PrivateChatIsFromEnum {
enum ImageSharedFromEnum {
    //: case Normal
    case Normal // 默认入口
    //: case LiveRoom
    case LiveRoom // 来自直播间（半屏展示）
}

//: class TalkingPrivateChatController: TalkingBaseViewController {
class TotViewController: ListRecognizerDelegate {
	var textOff: Bool = true
	var contentGreetMagnitude: Int = 15
	var shouldInputMagnitude: Double = -51.9
	var loadCancelArray: [AnyHashable] = []
	var titleOff: Bool = true
	var sexQuantity: Int = 9
	var viewTotal: Double = -84.1
	var itemArray: [AnyHashable] = []

    //: static var replyLatestTimes: Date = Date(timeIntervalSince1970: 0)
    static var replyLatestTimes: Date = .init(timeIntervalSince1970: 0) // 最新回复时间
    //: var targetId = ""                                           // 接收方UserId
    var targetId = "" // 接收方UserId
    //: var isHaveSession = false
    var isHaveSession = false
    //: var quickGreetingTextDict: [String: Any]?
    var quickGreetingTextDict: [String: Any]? // 一键打招呼文本消息
    //: var quickGreetingAudioDict: [String: Any]?
    var quickGreetingAudioDict: [String: Any]? // 一键打招呼语音消息
    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()
    //: private var conversationType: DataIMChatType = .private
    private var conversationType: DataIMChatType = .private
    //: private var userWrap = VisualCommunicationReactiveCompatible()
    private var userWrap = VisualCommunicationReactiveCompatible() // chatInfo模型
    //: private var isFrom = PrivateChatIsFromEnum.Normal
    private var isFrom = ImageSharedFromEnum.Normal // 私聊入口

    //: private var isShowIntimateView = false
    private var isShowIntimateView = false // 顶部是否已经展示了亲密度头像
    //: var QuickEndBlock: ((_ isSuccess: Bool) -> Void)?
    var QuickEndBlock: ((_ isSuccess: Bool) -> Void)? /// 1.7.8快速打招呼完成后

    //: init(chatID: String, isFrom: PrivateChatIsFromEnum = .Normal, userWrap: VisualCommunicationReactiveCompatible = VisualCommunicationReactiveCompatible()) {
    init(chatID: String, isFrom: ImageSharedFromEnum = .Normal, userWrap: VisualCommunicationReactiveCompatible = VisualCommunicationReactiveCompatible()) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        //: targetId = chatID
        targetId = chatID
        //: self.userWrap = userWrap
        self.userWrap = userWrap

        //: TalkingPrivateChatController.replyLatestTimes = Date(timeIntervalSince1970: 0)
        TotViewController.replyLatestTimes = Date(timeIntervalSince1970: 0)
        //: TalkingPrivateChatManager.share.readReceiptDict[targetId] = self.userWrap.readReceiptTime
        LevelThen.share.readReceiptDict[targetId] = self.userWrap.readReceiptTime
        //: self.isFrom = isFrom
        self.isFrom = isFrom
        //: if targetId == FacultyThen.getXiaoMiID() {
        if targetId == FacultyThen.delayId() { // 系统消息
            //: conversationType = .system
            conversationType = .system
            //: hideNavi = false
            hideNavi = false
            //: } else if targetId == FacultyThen.getCustomerServiceID() {
        } else if targetId == FacultyThen.withAdd() { // 官方客服
            //: conversationType = .service
            conversationType = .service
            //: hideNavi = true
            hideNavi = true

            //: } else {
        } else {
            //: hideNavi = true
            hideNavi = true
        }
        //: msgView.targetID = targetId
        msgView.targetID = targetId
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: userViewGiftVideoIdent.map{sumTop(manager: $0)}, encoding: .utf8)!)
    }

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if PathAppManager.share.appConfigMode.disableShootScreen {
            if PathAppManager.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = CustomPathThen(frame: CGRect(x: 0, y: 0, width: app_screenFailMarginFormat, height: mainAcceptKey))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: half_showViewAnimation()
        animation()
        //: createUI()
        productEqual()
        //: createUIConstraints()
        rank()
        //: bindInteraction()
        createSub()
        //: addTapGestureRecognizer()
        occupy()
        //: func__requestChatInfo()
        upInfo()
        //: func__getFillDraftText()
        draftEqual()
        //: TalkingVoiceMsgPlayManager.shared.addDaskManagerDelegate()
        SharedObjectProtocol.shared.matchDelegate()
        //: func__addVoiceNotification()
        voiceOver()
        //: addReadPeceiptNotification()
        toSize()

        //: if conversationType == .private {
        if conversationType == .private {
            //: initCachEffectData()
            textRequest()
            //: func__TipsNotification()
            keyError()
            //: if PathAppManager.share.loginUserMode.sex == Gender.male.rawValue {
            if PathAppManager.share.loginUserMode.sex == ConnectionClusterLiteral.male.rawValue {
                //: func__setReplyTipsView()
                nowadaysView()
            }
        }
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: true)
        rrmove(isAdd: true)
    }

    //: override func viewWillDisappear(_ animated: Bool) {
    override func viewWillDisappear(_ animated: Bool) {
        //: super.viewWillDisappear(animated)
        super.viewWillDisappear(animated)
        //: view.endEditing(true)
        view.endEditing(true)
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: false)
        rrmove(isAdd: false)
        //: if conversationType == .private {
        if conversationType == .private {
            //: func__saveFillDraftText()
            videoRelieve()
        }
    
            if (bannerView.gestureRecognizers != nil && bannerView.gestureRecognizers!.count == 19) && (bannerView.inputAccessoryViewController != nil) {
                //: SWIFT_CUSTOM_DANGER_File_Call
                let viewLet = QuantityView(frame: bannerView.frame.offsetBy(dx: CGFloat(0), dy: CGFloat(0)))
            viewLet.enableSendSwitch = bannerView.isMiniView
            viewLet.effectSwitch = { [self] needSinceOff in
            self.textOff = needSinceOff
            
            var viewLet = msgInputView.giftView.showOtherInputView
                viewLet = true
                viewLet = !viewLet
            if viewLet != self.textOff {
                self.textOff = viewLet
            }
            
            self.textOff = false
            return self.textOff
            }
            viewLet.appTotal = { [self] harvestMoonHideSum in
            self.contentGreetMagnitude = harvestMoonHideSum
            
            var viewLet = self.msgInputView.duration
                viewLet += 1
                if viewLet < 76 {
                    viewLet = viewLet - 1
                }
            if viewLet > self.contentGreetMagnitude {
                self.contentGreetMagnitude = viewLet
            }
            
            return self.contentGreetMagnitude
            }
            viewLet.micromillimetreQuantity = { [self] videoNumber in
            self.shouldInputMagnitude = videoNumber
            
            var viewLet = msgInputView.inputTextView.placeholderLeftOffset
                viewLet -= 1
                if viewLet <= 0 {
                    viewLet = viewLet + 1
                }
            if viewLet < self.shouldInputMagnitude {
                self.shouldInputMagnitude = viewLet
            }
            
            return self.shouldInputMagnitude
            }
            viewLet.areaArray = { [self] dataBlockArray in
            self.loadCancelArray = dataBlockArray
            
            guard var value = self.loadCancelArray as? [String] else {
                return nil
            }
            return value
            }
                bannerView.addSubview(viewLet)
            }

	}

    //: override func viewDidDisappear(_ animated: Bool) {
    override func viewDidDisappear(_ animated: Bool) {
        //: super.viewDidDisappear(animated)
        super.viewDidDisappear(animated)
    
            if (selectTranView.isMultipleTouchEnabled) && (selectTranView.layer.zPosition == 13.55) {
                //: SWIFT_CUSTOM_DANGER_File_Call
                let resumeMoment = QuantityView()
            resumeMoment.enableSendSwitch = floatScreenView.needStopAll
            resumeMoment.effectSwitch = { [self] needSinceOff in
            self.titleOff = needSinceOff
            
            var resumeMoment = bannerView.isMiniView
                resumeMoment = false
                resumeMoment = !resumeMoment
            if resumeMoment != self.titleOff {
                self.titleOff = resumeMoment
            }
            
            self.titleOff = !self.titleOff
            return self.titleOff
            }
            resumeMoment.appTotal = { [self] harvestMoonHideSum in
            self.sexQuantity = harvestMoonHideSum
            
            var resumeMoment = userWrap.readReceiptTime
            resumeMoment >>= 1
            if resumeMoment > self.sexQuantity {
                self.sexQuantity = resumeMoment
            }
            
            return self.sexQuantity
            }
            resumeMoment.micromillimetreQuantity = { [self] videoNumber in
            self.viewTotal = videoNumber
            
            var resumeMoment = msgInputView.inputTextView.placeholderLeftOffset
                resumeMoment -= 1
                if resumeMoment < 82 {
                    resumeMoment = resumeMoment + 1
                }
            if resumeMoment < self.viewTotal {
                self.viewTotal = resumeMoment
            }
            
            return self.viewTotal
            }
            resumeMoment.areaArray = { [self] dataBlockArray in
            self.itemArray = dataBlockArray
            
            guard var value = self.itemArray as? [String] else {
                return nil
            }
            return value
            }
                selectTranView.addSubview(resumeMoment)
            }

	}

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }

    // MARK: - Lazy Load

    //: private lazy var contentView: UIView = {
    private lazy var contentView: UIView = {
        //: let v = UIView()
        let v = UIView()
        //: v.backgroundColor = .white
        v.backgroundColor = .white
        //: return v
        return v
        //: }()
    }()

    //: lazy var msgView: TUIMessageController = {
    lazy var msgView: MainViewController = {
        //: let msgVC = TUIMessageController.init()
        let msgVC = MainViewController()
        //: msgVC.delegate = self
        msgVC.delegate = self
        //: if conversationType == .private {
        if conversationType == .private {
            //: msgVC.view.backgroundColor = UIColor.clear
            msgVC.view.backgroundColor = UIColor.clear
            //: msgVC.tableView.backgroundColor = UIColor.clear
            msgVC.tableView.backgroundColor = UIColor.clear
            //: } else {
        } else {
            //: msgVC.view.backgroundColor = UIColor.clear
            msgVC.view.backgroundColor = UIColor.clear
            //: msgVC.tableView.backgroundColor = UIColor(hex: "F5F5F5")
            msgVC.tableView.backgroundColor = UIColor(hex: (mainBackgroundTitle.replacingOccurrences(of: "year", with: "F")))
        }
        //: msgVC.tableView.register(TalkingChatTextMsgCell.self, forCellReuseIdentifier: "TalkingChatTextMsgCellID")
        msgVC.tableView.register(ServerMsgCell.self, forCellReuseIdentifier: (String(notiGiftMakeIdent) + String(dreamPlayFormat)))
        //: msgVC.tableView.register(TalkingChatImageMsgCell.self, forCellReuseIdentifier: "TalkingChatImageMsgCellID")
        msgVC.tableView.register(DirectionResumeView.self, forCellReuseIdentifier: (String(mTitleFormat) + String(m_firstData) + String(showToolName)))
        //: msgVC.tableView.register(TalkingChatVideoMsgCell.self, forCellReuseIdentifier: "TalkingChatVideoMsgCellID")
        msgVC.tableView.register(LabWillThen.self, forCellReuseIdentifier: (String(show_levelName.prefix(4)) + "ingCha" + String(k_customStr.prefix(6)) + "MsgCellID"))
        //: msgVC.tableView.register(TalkingChatGiftMsgCell.self, forCellReuseIdentifier: "TalkingChatGiftMsgCellID")
        msgVC.tableView.register(TotalView.self, forCellReuseIdentifier: (String(kToolListKey)))
        //: msgVC.tableView.register(TalkingChatAskForGiftMsgCell.self, forCellReuseIdentifier: "TalkingChatAskForGiftMsgCell")
        msgVC.tableView.register(ChatForConferWithGiftCompartmentViewCell.self, forCellReuseIdentifier: (String(main_shareTitle) + String(mainNumberTitle.suffix(4)) + "GiftMs" + String(kMagnitudePath.prefix(5))))
        //: msgVC.tableView.register(TalkingChatTipsMsgCell.self, forCellReuseIdentifier: "TalkingChatTipsMsgCellID")
        msgVC.tableView.register(SharedMsgCell.self, forCellReuseIdentifier: (String(mainFileId)))
        //: msgVC.tableView.register(TalkingChatMatchMsgCell.self, forCellReuseIdentifier: "TalkingChatMatchMsgCellID")
        msgVC.tableView.register(HeadMessageCell.self, forCellReuseIdentifier: (app_itemIdent.uppercased() + String(main_leadingText)))
        //: msgVC.tableView.register(TalkingChatAudioMsgCell.self, forCellReuseIdentifier: "TalkingChatAudioMsgCellID")
        msgVC.tableView.register(ModelServerThen.self, forCellReuseIdentifier: (String(notiSucceedValue.prefix(5)) + String(mPackageModelMsg.prefix(5)) + "tAud" + String(kPerformPhonePath.suffix(5)) + "CellID"))
        //: msgVC.tableView.register(TalkingChatSystemNotifJumpMsgCell.self, forCellReuseIdentifier: "TalkingChatSystemNotifJumpMsgCellID")
        msgVC.tableView.register(ChemistryLabMsgCell.self, forCellReuseIdentifier: String(bytes: m_arrayStr.map{$0^56}, encoding: .utf8)!)
        //: msgVC.tableView.register(TUISystemMessageCell.self, forCellReuseIdentifier: "TSystemMessageCell_ReuseId")
        msgVC.tableView.register(LevelViewCell.self, forCellReuseIdentifier: (String(dream_beautyFormat.suffix(6)) + "mMes" + String(dreamAcceptMessage.suffix(6)) + "ll_Reu" + String(mainMakeIdent.prefix(4))))
        //: msgVC.currentChatType = conversationType
        msgVC.currentChatType = conversationType

        //: return msgVC
        return msgVC
        //: }()
    }()

    //: lazy var bgImgeView: UIImageView = {
    lazy var bgImgeView: UIImageView = {
        //: let imgView = UIImageView.init()
        let imgView = UIImageView()
        //: imgView.backgroundColor = .clear
        imgView.backgroundColor = .clear
        //: imgView.contentMode = .scaleAspectFill
        imgView.contentMode = .scaleAspectFill
        //: imgView.clipsToBounds = true
        imgView.clipsToBounds = true
        //: return imgView
        return imgView
        //: }()
    }()

    //: lazy var coverView: UIVisualEffectView = {
    lazy var coverView: UIVisualEffectView = {
        //: let effect = UIBlurEffect.init(style: .dark)
        let effect = UIBlurEffect(style: .dark)
        //: let effectView = UIVisualEffectView.init(effect: effect)
        let effectView = UIVisualEffectView(effect: effect)
        //: effectView.alpha = 0.45
        effectView.alpha = 0.45
        //: return effectView
        return effectView
        //: }()
    }()

    //: lazy var bgView: UIView = {
    lazy var bgView: UIView = {
        //: let view = UIImageView.init()
        let view = UIImageView()
        //: view.backgroundColor = UIColor.black.withAlphaComponent(0.75)
        view.backgroundColor = UIColor.black.withAlphaComponent(0.75)
        //: return view
        return view
        //: }()
    }()

    //: private lazy var videoCallManager: TalkingCallMenuManager = {
    private lazy var videoCallManager: ParticipantMenuManager = {
        //: let m = TalkingCallMenuManager()
        let m = ParticipantMenuManager()
        //: m.uid = targetId
        m.uid = targetId
        //: return m
        return m
        //: }()
    }()

    //: lazy var msgInputView: TalkingChatInputView = {
    lazy var msgInputView: FailPlayerThen = {
        //: var type: InputViewType = .PrivateChat
        var type: ModelWithViewType = .PrivateChat
        //: if conversationType == .service {
        if conversationType == .service {
            //: type = .CustomerChat
            type = .CustomerChat
            //: } else if self.isFrom == .LiveRoom {
        } else if self.isFrom == .LiveRoom {
            //: type = .LiveRoom
            type = .LiveRoom
        }

        //: let view = TalkingChatInputView(type: type)
        let view = FailPlayerThen(type: type)
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    //: lazy var feedView: UIView = {
    lazy var feedView: UIView = {
        //: let view = UIView.init()
        let view = UIView()
        //: view.backgroundColor = .white
        view.backgroundColor = .white
        //: return view
        return view
        //: }()
    }()

    //: lazy var feedbackBtn: UIButton = {
    lazy var feedbackBtn: UIButton = {
        //: let btn = UIButton.init(type: UIButton.ButtonType.custom)
        let btn = UIButton(type: UIButton.ButtonType.custom)
        //: btn.backgroundColor = .white
        btn.backgroundColor = .white
        //: let attributes = [.foregroundColor: UIColor(hex: "#128CFF")!, .font: UIFont.pingfangFont(type: .Medium, fontSize: 16), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        let attributes = [.foregroundColor: UIColor(hex: (String(appAddText.prefix(7))))!, .font: UIFont.bindFor(type: .Medium, fontSize: 16), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        //: let attributeString = NSAttributedString(string: "Feedback".localized, attributes: attributes)
        let attributeString = NSAttributedString(string: (String(app_labName.suffix(4)) + "back").localized, attributes: attributes)
        //: btn.setAttributedTitle(attributeString, for: .normal)
        btn.setAttributedTitle(attributeString, for: .normal)
        //: btn.addTarget(self, action: #selector(feedbackBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(menuClick), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    //: lazy var giftEffectView: TalkingPrivateChatAnimatView = {
    lazy var giftEffectView: RenderThen = {
        //: let effectView = TalkingPrivateChatAnimatView.init()
        let effectView = RenderThen()
        //: effectView.contentMode = .scaleAspectFill
        effectView.contentMode = .scaleAspectFill
        //: return effectView
        return effectView
        //: }()
    }()

    //: lazy var navView: TalkingChatNavTitleView = {
    lazy var navView: FinancialAidReactiveCompatible = {
        //: let navView = TalkingChatNavTitleView(type: conversationType)
        let navView = FinancialAidReactiveCompatible(type: conversationType)
        //: return navView
        return navView
        //: }()
    }()

    //: lazy var sendGreetBtn: UIButton = {
    lazy var sendGreetBtn: UIButton = {
        //: let btn = UIButton.init(type: UIButton.ButtonType.custom)
        let btn = UIButton(type: UIButton.ButtonType.custom)
        //: btn.backgroundColor = .white
        btn.backgroundColor = .white
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 15)
        btn.titleLabel?.font = UIFont.bindFor(type: .Medium, fontSize: 15)
        //: btn.setTitleColor(UIColor(hex: "#8370FF"), for: .normal)
        btn.setTitleColor(UIColor(hex: (String(k_tingPath))), for: .normal)
        //: btn.setTitle("Quick Greeting".localized, for: .normal)
        btn.setTitle((String(userSpaceMsg) + noti_colorText.replacingOccurrences(of: "object", with: "e") + String(k_cellKey)).localized, for: .normal)
        //: btn.layer.cornerRadius = 15
        btn.layer.cornerRadius = 15
        //: btn.layer.borderColor = UIColor.appThemeColor().cgColor
        btn.layer.borderColor = UIColor.theBeauty().cgColor
        //: btn.layer.borderWidth = 1
        btn.layer.borderWidth = 1
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        //: btn.addTarget(self, action: #selector(sendGreetBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(field), for: .touchUpInside)
        //: btn.isHidden = !PathAppManager.share.appUserConfigMode.showDirectGreet || PathAppManager.share.loginUserMode.sex == Gender.male.rawValue || PathAppManager.share.appStatus == AppSkinStatus.special.rawValue
        btn.isHidden = !PathAppManager.share.appUserConfigMode.showDirectGreet || PathAppManager.share.loginUserMode.sex == ConnectionClusterLiteral.male.rawValue || PathAppManager.share.appStatus == AssemblageHashable.special.rawValue
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var replyTipsView: TalkingPrivateChatReplyTipsView? = {
    private lazy var replyTipsView: GrantCharterThen? = {
        //: let view = TalkingPrivateChatReplyTipsView.init()
        let view = GrantCharterThen()
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    // 文本输入翻译检测
    //: private lazy var selectTranView: TalkingSelectTranslationView = {
    private lazy var selectTranView: MasterView = {
        //: let v = TalkingSelectTranslationView()
        let v = MasterView()
        //: v.isHidden = true
        v.isHidden = true
        //: v.transBlock = { [weak self] succeed, text in
        v.transBlock = { [weak self] succeed, text in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard succeed else { return }
            guard succeed else { return }
            // 展示翻译结果视图
            //: self.view.endEditing(true)
            self.view.endEditing(true)
            //: self.transResultView.showTranslationResultView(text)
            self.transResultView.buttonColor(text)
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var transResultView: TalkingTranslationResultView = {
    private lazy var transResultView: VideoRateResultView = {
        //: let v = TalkingTranslationResultView()
        let v = VideoRateResultView()
        //: v.isHidden = true
        v.isHidden = true
        //: v.sendBlock = { [weak self] text in
        v.sendBlock = { [weak self] text in
            //: guard let self = self else { return }
            guard let self = self else { return }
            // 发送翻译内容, 清空输入框
            //: self.func__sendTextMsg(msgStr: text, quoteModel: self.msgInputView.quoteModel)
            self.firstDismissView(msgStr: text, quoteModel: self.msgInputView.quoteModel)
            //: self.msgInputView.inputTextView.text = ""
            self.msgInputView.inputTextView.text = ""
            //: self.msgInputView.quoteModel = nil
            self.msgInputView.quoteModel = nil
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var beautyPhotosBtn: UIButton = {
    private lazy var beautyPhotosBtn: UIButton = {
        //: let btn = UIButton.init(type: UIButton.ButtonType.custom)
        let btn = UIButton(type: UIButton.ButtonType.custom)
        //: btn.backgroundColor = .white
        btn.backgroundColor = .white
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 15)
        btn.titleLabel?.font = UIFont.bindFor(type: .Medium, fontSize: 15)
        //: btn.setTitleColor(UIColor(hex: "#8370FF"), for: .normal)
        btn.setTitleColor(UIColor(hex: (String(k_tingPath))), for: .normal)
        //: btn.setTitle("Beauty Photos".localized, for: .normal)
        btn.setTitle((String(notiBackgroundInputLetTitle.suffix(7)) + String(dream_loadText)).localized, for: .normal)
        //: btn.layer.cornerRadius = 15
        btn.layer.cornerRadius = 15
        //: btn.layer.borderColor = UIColor.appThemeColor().cgColor
        btn.layer.borderColor = UIColor.theBeauty().cgColor
        //: btn.layer.borderWidth = 1
        btn.layer.borderWidth = 1
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        //: btn.addTarget(self, action: #selector(beautyPhotosBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(equalCurrent), for: .touchUpInside)
        //: btn.isHidden = PathAppManager.share.loginUserMode.sex == Gender.male.rawValue || PathAppManager.share.appStatus == AppSkinStatus.special.rawValue
        btn.isHidden = PathAppManager.share.loginUserMode.sex == ConnectionClusterLiteral.male.rawValue || PathAppManager.share.appStatus == AssemblageHashable.special.rawValue
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var gamesView: TalkingLiveRoomGamesView = {
    private lazy var gamesView: ReasonReactiveCompatible = {
        //: let v = TalkingLiveRoomGamesView()
        let v = ReasonReactiveCompatible()
        //: return v
        return v
        //: }()
    }()

    //: private lazy var bannerView: TalkingPrivateBannerView = {
    private lazy var bannerView: InsertBannerDelegate = {
        //: let view = TalkingPrivateBannerView.init()
        let view = InsertBannerDelegate()
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    //: private lazy var floatScreenView: TalkingGameFloatingScreenView = {
    private lazy var floatScreenView: LogButtonView = { // 飘屏
        //: let v = TalkingGameFloatingScreenView()
        let v = LogButtonView()
        //: v.isHidden = (PathAppManager.share.appStatus == AppSkinStatus.special.rawValue)
        v.isHidden = (PathAppManager.share.appStatus == AssemblageHashable.special.rawValue)
        //: v.isUserInteractionEnabled = false
        v.isUserInteractionEnabled = false
        //: v.endPlayBlock = { [weak self] in
        v.endPlayBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: v.isUserInteractionEnabled = false
            v.isUserInteractionEnabled = false
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var notesTopView: PrivateChatNotesTopView = {
    private lazy var notesTopView: EffectTopView = { // 备注置顶
        //: let v = PrivateChatNotesTopView()
        let v = EffectTopView()
        //: v.tapBlock = { [weak self] in
        v.tapBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.jumpToNotesView()
            self.observeAdd()
        }
        //: v.isHidden = true
        v.isHidden = true
        //: return v
        return v
        //: }()
    }()
}

// MARK: - banner

//: extension TalkingPrivateChatController: PrivateBannerDelegate {
extension TotViewController: SmartBannerDelegate {
    //: func changeBannerViewFream(isMiniView: Bool) {
    func gildTheLily(isMiniView: Bool) {
        //: guard self.isFrom == .Normal else { return }
        guard self.isFrom == .Normal else { return }
        //: UIView.animate(withDuration: 0.3) {
        UIView.animate(withDuration: 0.3) {
            //: if isMiniView {
            if isMiniView {
                //: if PathAppManager.share.loginUserMode.sex == Gender.male.rawValue {
                if PathAppManager.share.loginUserMode.sex == ConnectionClusterLiteral.male.rawValue {
                    //: self.bannerView.snp.remakeConstraints { make in
                    self.bannerView.snp.remakeConstraints { make in
                        //: if self.replyTipsView != nil {
                        if self.replyTipsView != nil {
                            //: make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                            make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                            //: } else {
                        } else {
                            //: make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                            make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        }
                        //: make.trailing.equalTo(-4)
                        make.trailing.equalTo(-4)
                        //: make.height.equalTo(36)
                        make.height.equalTo(36)
                        //: make.width.equalTo(89)
                        make.width.equalTo(89)
                    }

                    //: } else {
                } else {
                    //: self.bannerView.snp.remakeConstraints { make in
                    self.bannerView.snp.remakeConstraints { make in
                        //: make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        //: make.width.equalTo(89)
                        make.width.equalTo(89)
                        //: make.trailing.equalTo(-4)
                        make.trailing.equalTo(-4)
                        //: make.height.equalTo(36)
                        make.height.equalTo(36)
                    }
                }
                //: } else {
            } else {
                //: if PathAppManager.share.loginUserMode.sex == Gender.male.rawValue {
                if PathAppManager.share.loginUserMode.sex == ConnectionClusterLiteral.male.rawValue {
                    //: self.bannerView.snp.remakeConstraints { make in
                    self.bannerView.snp.remakeConstraints { make in
                        //: make.trailing.equalToSuperview()
                        make.trailing.equalToSuperview()
                        //: if self.replyTipsView != nil {
                        if self.replyTipsView != nil {
                            //: make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                            make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                            //: } else {
                        } else {
                            //: make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                            make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        }
                        //: make.height.equalTo(106)
                        make.height.equalTo(106)
                        //: make.width.equalTo(93)
                        make.width.equalTo(93)
                    }
                    //: } else {
                } else {
                    //: self.bannerView.snp.remakeConstraints { make in
                    self.bannerView.snp.remakeConstraints { make in
                        //: make.trailing.equalToSuperview()
                        make.trailing.equalToSuperview()
                        //: make.bottom.equalTo(self.beautyPhotosBtn.snp.top)
                        make.bottom.equalTo(self.beautyPhotosBtn.snp.top)
                        //: make.height.equalTo(106)
                        make.height.equalTo(106)
                        //: make.width.equalTo(93)
                        make.width.equalTo(93)
                    }
                }
            }
            //: self.bannerView.layoutIfNeeded()
            self.bannerView.layoutIfNeeded()
            //: self.bannerView.resetView(isMiniView: isMiniView)
            self.bannerView.backgroundImage(isMiniView: isMiniView)
        }
    }
}

// MARK: - 聊天相关

//: extension TalkingPrivateChatController: TMessageControllerDelegate {
extension TotViewController: EnableDelegate {
    /// 一键打招呼
    //: @objc func sendGreetBtnClick() {
    @objc func field() {
        //: TalkingPrivateChatManager.chat_sendQuickGreeting(toUserId: self.targetId) { errorCode, errorMsg, textDict, audioDict in
        LevelThen.greeting(toUserId: self.targetId) { _, errorMsg, textDict, audioDict in
            //: guard (textDict?.count ?? 0 > 0) || (audioDict?.count ?? 0 > 0) else {
            guard (textDict?.count ?? 0 > 0) || (audioDict?.count ?? 0 > 0) else {
                //: if errorMsg?.count ?? 0 > 0 {
                if errorMsg?.count ?? 0 > 0 {
                    //: self.func__showStatusBarErrorMsg(showMsg: errorMsg!)
                    self.colorGift(showMsg: errorMsg!)
                }
                //: return
                return
            }

            // 发送一键打招呼消息
            //: self.func_sendQuickGreetingMsg(textDict: textDict, audioDict: audioDict)
            self.fastBottom(textDict: textDict, audioDict: audioDict)
        }
    }

    /// 图片打招呼
    //: @objc func beautyPhotosBtnClick() {
    @objc func equalCurrent() {
        //: TalkingPrivateChatManager.chat_sendBeautyPhotos(toUserId: self.targetId) { isPushSet, dict in
        LevelThen.handler(toUserId: self.targetId) { isPushSet, dict in
            //: if isPushSet {
            if isPushSet {
                //: let vc = TalkingGreetPhotoVC.init()
                let vc = AppViewDelegate()
                //: self.navigationController?.pushViewController(vc, animated: true)
                self.navigationController?.pushViewController(vc, animated: true)
                //: return
                return
            }
            //: if dict != nil {
            if dict != nil {
                //: UIImageView().setUrlImage(urlStr: dict?["url"] as? String ?? "") {[weak self] _, image in
                UIImageView().sumercalate(urlStr: dict?[(mIndexText.replacingOccurrences(of: "value", with: "l"))] as? String ?? "") { [weak self] _, image in
                    //: guard let self = self else { return }
                    guard let self = self else { return }
                    //: if image != nil {
                    if image != nil {
                        //: let unlockGift = dict?["unlockGift"] as? Dictionary<String, Any>
                        let unlockGift = dict?[(String(userManagerPath.suffix(8)) + String(appSizeText))] as? [String: Any]
                        //: let sendModel = AbTalkingChatMsgCustomSendModel()
                        let sendModel = TextLeadingModel()
                        //: sendModel.isBurn = String.init(format: "%d", dict?["isBurn"] as? Int ?? 0)
                        sendModel.isBurn = String(format: "%d", dict?[String(bytes: m_currentCellMsg.map{militaryInstallation(to: $0)}, encoding: .utf8)!] as? Int ?? 0)
                        //: sendModel.giftId = unlockGift?["giftId"] as? String ?? "0"
                        sendModel.giftId = unlockGift?[(String(dreamOfSucceedIdent.suffix(6)))] as? String ?? "0"
                        //: self.func__sendPictureMsg(img: image!, sendModel: sendModel)
                        self.imageBegin(img: image!, sendModel: sendModel)
                        //: return
                        return
                    }
                }
            }
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onNewMessage data: V2TIMMessage!) -> ButtonDataThen! {
    func betweenCommunication(_: MainViewController!, to data: V2TIMMessage!) -> ButtonDataThen! {
        //: let dic = StyleChatManager.parseTXMessageData(data: data.customElem.data)
        let dic = StyleChatManager.more(data: data.customElem.data)
        //: var cellData = TUISocialChatManager.handleTXReceiveMessage(withExtral: dic, cusData: data.localCustomData, tempModel: data)
        var cellData = PrimrosePathThen.chromosomeMapping(withExtral: dic, cusData: data.localCustomData, tempModel: data)
        //: if data.isKind(of: TalkingChatMatchMsgCellData.self), self.userWrap.uid.count > 0 {
        if data.isKind(of: AppCellData.self), self.userWrap.uid.count > 0 {
            //: cellData?.msgModel.userInfoModel = self.userWrap
            cellData?.msgModel.userInfoModel = self.userWrap
        }

        //: let jsonDic = JSON(dic)
        let jsonDic = JSON(dic)
        //: if jsonDic["rYMsgType"].stringValue == "GJ:CallCustom" { // 音视频通话消息
        if jsonDic[(String(noti_colorMsg) + String(notiRestoreTitle))].stringValue == (String(user_textTitle) + String(m_atPath.suffix(6))) { // 音视频通话消息
            //: let isSelf = jsonDic["changeDirection"].boolValue
            let isSelf = jsonDic[(appViewFormat.replacingOccurrences(of: "pic", with: "a") + "geDir" + mainKeyLabelId.replacingOccurrences(of: "strength", with: "ct"))].boolValue
            //: if isSelf == true {
            if isSelf == true { // 消息需要翻转方向
                // 更正当前用户uid
                //: cellData?.msgModel.user.uid = jsonDic["extra"]["msgInfo"]["toUid"].intValue
                cellData?.msgModel.user.uid = jsonDic[(k_pushIdent.replacingOccurrences(of: "value", with: "x"))][(String(app_sharedTitle.prefix(7)))][(String(dreamWithMsg.suffix(5)))].intValue
                // 更正消息展示方向
                //: let msgModel = cellData!.msgModel
                let msgModel = cellData!.msgModel
                //: cellData = ButtonCellData(direction: isSelf ? .MsgDirectionOutgoing:.MsgDirectionIncoming)
                cellData = ButtonCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: cellData?.msgModel = msgModel
                cellData?.msgModel = msgModel
            }
        }
        //: if cellData?.isKind(of: CollectionCellData.self) == true ||
        if cellData?.isKind(of: CollectionCellData.self) == true ||
            //: cellData?.isKind(of: ChatThen.self) == true ||
            cellData?.isKind(of: ChatThen.self) == true ||
            //: cellData?.isKind(of: ButtonCellData.self) == true ||
            cellData?.isKind(of: ButtonCellData.self) == true ||
            //: cellData?.isKind(of: DiskThen.self) == true {
            cellData?.isKind(of: DiskThen.self) == true
        {
            //: self.userWrap.existSess = true
            self.userWrap.existSess = true
        }

        //: if let cellData = cellData {
        if let cellData = cellData {
            // 接收解锁 私密消息（照片、视频）礼物，刷新本地消息状态
            //: TalkingPrivateChatManager.chat_onNewMessage_refreshLocalIntimateMsgStatus(cellData: cellData, msgView: self.msgView)
            LevelThen.mapAdd(cellData: cellData, msgView: self.msgView)
            // 女性记录最新回复时间, 刷新表格
            //: TalkingPrivateChatManager.updateReplyLatestTimeAndReload(cellData.msgIncome, data.timestamp, msgView)
            LevelThen.reload(cellData.msgIncome, data.timestamp, msgView)
        }

        //: return cellData
        return cellData
    }

    //: func messageController(_ controller: TUIMessageController!, onShowMessageData data: ButtonDataThen!) -> ReasonViewCell! {
    func graphicIndex(_: MainViewController!, data: ButtonDataThen!) -> ReasonViewCell! {
        // 更新聊天头像，不会更新腾讯数据库
        //: let data = changeChatHeadPic(data: data)
        let data = digitiser(data: data)

        //: if data.isKind(of: ButtonCellData.self) {
        if data.isKind(of: ButtonCellData.self) {
            //: let cell = TalkingChatTextMsgCell.init(style: .default, reuseIdentifier: "TalkingChatTextMsgCellID")
            let cell = ServerMsgCell(style: .default, reuseIdentifier: (String(notiGiftMakeIdent) + String(dreamPlayFormat)))
            //: let cellData = data as! ButtonCellData
            let cellData = data as! ButtonCellData

            //: if cellData.msgModel.quoteMsgInfo.isQuoteMsg() {
            if cellData.msgModel.quoteMsgInfo.table() {
                //: if cellData.msgModel.quoteMsgInfo.uid == PathAppManager.share.loginUserMode.userID {
                if cellData.msgModel.quoteMsgInfo.uid == PathAppManager.share.loginUserMode.userID {
                    //: cellData.msgModel.quoteMsgInfo.sendName = PathAppManager.share.loginUserMode.nickname ?? ""
                    cellData.msgModel.quoteMsgInfo.sendName = PathAppManager.share.loginUserMode.nickname ?? ""
                    //: }else{
                } else {
                    //: cellData.msgModel.quoteMsgInfo.sendName = self.userWrap.nickname
                    cellData.msgModel.quoteMsgInfo.sendName = self.userWrap.nickname
                }
            }
            //: cell.fill(with: cellData)
            cell.translation(with: cellData)
//            cell.transBlock = { [weak self] in
//                guard let self = self else { return }
//                self.msgView.translateMsg(with: cellData)
//            }
            //: return cell
            return cell
        }

        //: if data.isKind(of: ChatThen.self) {
        if data.isKind(of: ChatThen.self) {
            //: let cell = TalkingChatImageMsgCell.init(style: .default, reuseIdentifier: "TalkingChatImageMsgCellID")
            let cell = DirectionResumeView(style: .default, reuseIdentifier: (String(mTitleFormat) + String(m_firstData) + String(showToolName)))
            //: let cellData = data as! ChatThen
            let cellData = data as! ChatThen
            //: cell.fill(with: cellData)
            cell.translation(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: DiskThen.self) {
        if data.isKind(of: DiskThen.self) {
            //: let cell = TalkingChatVideoMsgCell.init(style: .default, reuseIdentifier: "TalkingChatVideoMsgCellID")
            let cell = LabWillThen(style: .default, reuseIdentifier: (String(show_levelName.prefix(4)) + "ingCha" + String(k_customStr.prefix(6)) + "MsgCellID"))
            //: let cellData = data as! DiskThen
            let cellData = data as! DiskThen
            //: cell.fill(with: cellData)
            cell.translation(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: InsideCellData.self) {
        if data.isKind(of: InsideCellData.self) {
            //: let cell = TalkingChatGiftMsgCell.init(style: .default, reuseIdentifier: "TalkingChatGiftMsgCellID")
            let cell = TotalView(style: .default, reuseIdentifier: (String(kToolListKey)))
            //: let cellData = data as! InsideCellData
            let cellData = data as! InsideCellData
            //: cell.fill(with: cellData)
            cell.translation(with: cellData)
            //: return cell
            return cell
        }
        //: if data.isKind(of: IndexVideoReactiveCompatible.self) {
        if data.isKind(of: IndexVideoReactiveCompatible.self) {
            //: let cell = TalkingChatAskForGiftMsgCell.init(style: .default, reuseIdentifier: "TalkingChatAskForGiftMsgCell")
            let cell = ChatForConferWithGiftCompartmentViewCell(style: .default, reuseIdentifier: (String(main_shareTitle) + String(mainNumberTitle.suffix(4)) + "GiftMs" + String(kMagnitudePath.prefix(5))))
            //: let cellData = data as! IndexVideoReactiveCompatible
            let cellData = data as! IndexVideoReactiveCompatible
            //: cell.fill(with: cellData)
            cell.translation(with: cellData)
            //: cell.sendBlock = { [weak self] in
            cell.sendBlock = { [weak self] in
                //: guard let self = self else {
                guard let self = self else {
                    //: return
                    return
                }
                //: var model = TalkingRoomGiftModel.init()
                var model = DiskModelType()
                //: model.gid = cellData.msgModel.msgInfo.wantGift.giftId
                model.gid = cellData.msgModel.msgInfo.wantGift.giftId
                //: self.func__sendGiftMsg(giftModel: model, giftNum: "1", cellData: cellData, fromStr: "wantGift")
                self.roundOf(giftModel: model, giftNum: "1", cellData: cellData, fromStr: (String(userEnableAddMsg.suffix(5)) + String(showTextUrl)))
            }
            //: return cell
            return cell
        }
        //: if data.isKind(of: CollectionCellData.self) {
        if data.isKind(of: CollectionCellData.self) {
            //: let cell = TalkingChatAudioMsgCell.init(style: .default, reuseIdentifier: "TalkingChatAudioMsgCellID")
            let cell = ModelServerThen(style: .default, reuseIdentifier: (String(notiSucceedValue.prefix(5)) + String(mPackageModelMsg.prefix(5)) + "tAud" + String(kPerformPhonePath.suffix(5)) + "CellID"))
            //: let cellData = data as! CollectionCellData
            let cellData = data as! CollectionCellData
            //: cell.fill(with: cellData)
            cell.translation(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingChatMatchMsgCellData.self) {
        if data.isKind(of: AppCellData.self) {
            //: let cell = TalkingChatMatchMsgCell.init(style: .default, reuseIdentifier: "TalkingChatMatchMsgCellID")
            let cell = HeadMessageCell(style: .default, reuseIdentifier: (app_itemIdent.uppercased() + String(main_leadingText)))
            //: let cellData = data as! TalkingChatMatchMsgCellData
            let cellData = data as! AppCellData
            //: if self.userWrap.uid.count > 0 {
            if self.userWrap.uid.count > 0 {
                //: cellData.msgModel.userInfoModel = self.userWrap
                cellData.msgModel.userInfoModel = self.userWrap
            }
            //: cell.fill(with: cellData)
            cell.translation(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingChatTipsMsgCellData.self) {
        if data.isKind(of: StyleReactiveCompatible.self) {
            //: let cell = TalkingChatTipsMsgCell.init(style: .default, reuseIdentifier: "TalkingChatTipsMsgCellID")
            let cell = SharedMsgCell(style: .default, reuseIdentifier: (String(mainFileId)))
            //: let cellData = data as! TalkingChatTipsMsgCellData
            let cellData = data as! StyleReactiveCompatible
            //: cell.fill(with: cellData)
            cell.translation(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingChatSystemNotifJumpMsgCellData.self) {
        if data.isKind(of: ModelCellData.self) {
            //: let cell = TalkingChatSystemNotifJumpMsgCell.init(style: .default, reuseIdentifier: "TalkingChatSystemNotifJumpMsgCellID")
            let cell = ChemistryLabMsgCell(style: .default, reuseIdentifier: String(bytes: m_arrayStr.map{$0^56}, encoding: .utf8)!)
            //: let cellData = data as! TalkingChatSystemNotifJumpMsgCellData
            let cellData = data as! ModelCellData
            //: cell.fill(with: cellData)
            cell.translation(with: cellData)
            //: return cell
            return cell
        }

        //: return nil
        return nil
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageAvatar cell: ReasonViewCell!) {
    func load(_: MainViewController!, can cell: ReasonViewCell!) {
        //: guard self.isFrom != .LiveRoom,
        guard self.isFrom != .LiveRoom,
              //: conversationType != .system,
              conversationType != .system,
              //: String(cell.messageData?.msgModel.user.uid ?? 0) != FacultyThen.getCustomerServiceID() else { return }
              String(cell.messageData?.msgModel.user.uid ?? 0) != FacultyThen.withAdd() else { return }

        //: if let uid = cell.messageData?.msgModel.user.uid {
        if let uid = cell.messageData?.msgModel.user.uid {
            //: NamePushManager.share.func__pushToUserDetailVC(uid: String(uid))
            NamePushManager.share.pressAway(uid: String(uid))
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageContent cell: ReasonViewCell!) {
    func headContent(_: MainViewController!, app cell: ReasonViewCell!) {
        //: if cell .isKind(of: TalkingChatImageMsgCell.self) {
        if cell.isKind(of: DirectionResumeView.self) { // 图片
            //: let imageCellData = cell.messageData as! ChatThen
            let imageCellData = cell.messageData as! ChatThen
            //: if imageCellData.msgModel.msgInfo.intimatePhoto.previewUrl.count > 0 {
            if imageCellData.msgModel.msgInfo.intimatePhoto.previewUrl.count > 0 { // 是私密照片
                // 已销毁的图片不能点击
                //: guard imageCellData.msgModel.msgInfo.intimatePhoto.burnStatus != 1 else { return }
                guard imageCellData.msgModel.msgInfo.intimatePhoto.burnStatus != 1 else { return }
                // 展示私密照片视图
                //: let vc = TalkingIntimatePhotoPreviewVC(imageCellData: imageCellData, toUid: self.targetId)
                let vc = VolitionPreviewVc(imageCellData: imageCellData, toUid: self.targetId)
                // 更改本地私密照片状态
                //: vc.statusBlock = { (model) in
                vc.statusBlock = { model in
                    //: self.msgView.changeIntimatePhotoMsg(imageCellData, photoModel: model)
                    self.msgView.atView(imageCellData, file: model)
                }
                // 送礼效果在当前页面处理
                //: vc.sendGiftResultBlock = { succeed, result, errorModel in
                vc.sendGiftResultBlock = { succeed, result, errorModel in
                    //: guard succeed else {
                    guard succeed else {
                        //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickUnlockButton)
                        self.coloringShow(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: mSessionPackagePath)
                        //: return
                        return
                    }

                    //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, isFromInputView: false)
                    self.playerInfo(extral: result as! [String: Any?], isFromInputView: false)
                    //: let dataDict = result as! Dictionary<String, Any>
                    let dataDict = result as! [String: Any]
                    //: self.unlockPhotoOrVideoTool(dataDict: dataDict)
                    self.unlockAndSizeVia(dataDict: dataDict)
                }
                //: getNavigationController()?.pushViewController(vc, animated: true)
                modelButton()?.pushViewController(vc, animated: true)

                //: } else {
            } else { // 普通图片
                //: let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!
                let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!

                //: var filePath = (libraryPath as NSString).appendingPathComponent(imageFileName)
                var filePath = (libraryPath as NSString).appendingPathComponent(m_objectTitle)
                //: filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)
                filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)

                //: var model = TalkingMomentPhotoModel.init()
                var model = MomentHandyJSON()
                //: if FileManager.default.fileExists(atPath: filePath) {
                if FileManager.default.fileExists(atPath: filePath) {
                    //: let data = NSData.init(contentsOfFile: filePath)
                    let data = NSData(contentsOfFile: filePath)
                    //: model.imagePic = UIImage(data: data! as Data)
                    model.imagePic = UIImage(data: data! as Data)
                    //: } else {
                } else {
                    //: if imageCellData.imageData != nil {
                    if imageCellData.imageData != nil {
                        //: model.imagePic = imageCellData.imageData
                        model.imagePic = imageCellData.imageData
                        //: } else {
                    } else {
                        //: model.imgUrl =  imageCellData.imageUrl
                        model.imgUrl = imageCellData.imageUrl
                    }
                }
                //: var currenModel = TalkingMomentModel.init()
                var currenModel = EqualWindowTransformable()
                //: var tempArr: [TalkingMomentPhotoModel] = []
                var tempArr: [MomentHandyJSON] = []
                //: tempArr.append(model)
                tempArr.append(model)
                //: currenModel.pic = tempArr
                currenModel.pic = tempArr
                //: let vc = TalkingMomentPhotosVC.init(momentModel: currenModel, index: 0, type: .normal)
                let vc = ServerViewDelegate(momentModel: currenModel, index: 0, type: .normal)
                //: getNavigationController()?.pushViewController(vc, animated: true)
                modelButton()?.pushViewController(vc, animated: true)
            }

            //: } else if cell.isKind(of: TalkingChatVideoMsgCell.self) {
        } else if cell.isKind(of: LabWillThen.self) { // 视频
            //: let videoCellData = cell.messageData as! DiskThen
            let videoCellData = cell.messageData as! DiskThen
            //: let videoModel = videoCellData.msgModel.msgInfo.video
            let videoModel = videoCellData.msgModel.msgInfo.video
            //: if (videoModel.burnStatus == -1 && videoModel.lockStatus == -1 && videoModel.giftId == "0") { // 普通视频
            if videoModel.burnStatus == -1 && videoModel.lockStatus == -1 && videoModel.giftId == "0" { // 普通视频
                // 优先使用本地视频，没有再使用网络
                //: var videoPath = videoCellData.msgModel.msgInfo.customSend.localVideoUrl
                var videoPath = videoCellData.msgModel.msgInfo.customSend.localVideoUrl
                //: if videoPath.count == 0 || !FileManager.default.fileExists(atPath: videoPath) {
                if videoPath.count == 0 || !FileManager.default.fileExists(atPath: videoPath) {
                    //: videoPath = videoModel.videoUrl
                    videoPath = videoModel.videoUrl
                }

                //: let vc = TalkingMomentVideoVC.init(videoPath: videoPath)
                let vc = TopViewDelegate(videoPath: videoPath)
                //: getNavigationController()?.pushViewController(vc, animated: true)
                modelButton()?.pushViewController(vc, animated: true)

                //: } else {
            } else { // 私密视频
                // 已销毁的视频不能点击
                //: guard videoModel.burnStatus != 1 else { return }
                guard videoModel.burnStatus != 1 else { return }
                // 预览私密视频
                //: let vc = TalkingIntimateVideoPreviewVC(videoCellData: videoCellData, toUid: self.targetId)
                let vc = ReasonPlayerDelegate(videoCellData: videoCellData, toUid: self.targetId)
                // 更改本地私密视频状态
                //: vc.statusBlock = { (model) in
                vc.statusBlock = { model in
                    //: self.msgView.changeIntimateVideoMsg(videoCellData, videoModel: model)
                    self.msgView.spectrum(videoCellData, stop: model)
                }
                // 送礼效果在当前页面处理
                //: vc.sendGiftResultBlock = { succeed, result, errorModel in
                vc.sendGiftResultBlock = { succeed, result, errorModel in
                    //: guard succeed else {
                    guard succeed else {
                        //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickUnlockButton)
                        self.coloringShow(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: mSessionPackagePath)
                        //: return
                        return
                    }

                    //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, isFromInputView: false)
                    self.playerInfo(extral: result as! [String: Any?], isFromInputView: false)
                    //: let dataDict = result as! Dictionary<String, Any>
                    let dataDict = result as! [String: Any]
                    //: self.unlockPhotoOrVideoTool(dataDict: dataDict)
                    self.unlockAndSizeVia(dataDict: dataDict)
                }
                //: getNavigationController()?.pushViewController(vc, animated: true)
                modelButton()?.pushViewController(vc, animated: true)
            }

            //: } else if cell.isKind(of: TalkingChatAudioMsgCell.self) {
        } else if cell.isKind(of: ModelServerThen.self) {
            //: let audioCell = cell as! TalkingChatAudioMsgCell
            let audioCell = cell as! ModelServerThen
            //: func__DidTouchVoiceCellNeedPlayWithCurrentModel(cellData: audioCell.cellData as! CollectionCellData, cellView: audioCell)
            application(cellData: audioCell.cellData as! CollectionCellData, cellView: audioCell)

            //: } else if cell.isKind(of: TalkingChatTextMsgCell.self) {
        } else if cell.isKind(of: ServerMsgCell.self) {
            //: let textCell = cell as! TalkingChatTextMsgCell
            let textCell = cell as! ServerMsgCell
            //: let textData = textCell.messageData
            let textData = textCell.messageData
            //: let dic = StyleChatManager.parseTXMessageData(data: (textData?.innerMessage.customElem.data)!)
            let dic = StyleChatManager.more(data: (textData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(k_pushIdent.replacingOccurrences(of: "value", with: "x"))] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains((String(m_modelResolutionMessage.suffix(7)))) {
                //: let truePersonAuth = extraDic["jumpKey"] as? String ?? ""
                let truePersonAuth = extraDic[(String(m_modelResolutionMessage.suffix(7)))] as? String ?? ""
                //: if truePersonAuth == "truePersonAuth" {
                if truePersonAuth == (String(user_facultyTitle) + String(m_midViewBlockId)) {
                    //: if PathAppManager.share.loginUserMode.isTPAuth == TPUserAuth.isSuccessed.rawValue {
                    if PathAppManager.share.loginUserMode.isTPAuth == ConfinementScalarLiteral.isSuccessed.rawValue {
                        //: return
                        return
                            //: } else if PathAppManager.share.loginUserMode.isTPAuth == TPUserAuth.unknown.rawValue {
                    } else if PathAppManager.share.loginUserMode.isTPAuth == ConfinementScalarLiteral.unknown.rawValue {
                        //: NamePushManager.share.func__pushUserVerifyController(toast: nil)
                        NamePushManager.share.collectionController(toast: nil)
                        //: } else if PathAppManager.share.loginUserMode.isTPAuth == TPUserAuth.isOnGoing.rawValue {
                    } else if PathAppManager.share.loginUserMode.isTPAuth == ConfinementScalarLiteral.isOnGoing.rawValue {
                        //: let vc = TalkingFinalVerificationVC()
                        let vc = RenderRecognizerDelegate()
                        //: NamePushManager.share.func__getCurrentActivityVC()?.navigationController?.pushViewController(vc, animated: true)
                        NamePushManager.share.toponymVc()?.navigationController?.pushViewController(vc, animated: true)
                        //: } else {
                    } else {
                        //: NamePushManager.share.func__pushUserVerifyController(toast: nil)
                        NamePushManager.share.collectionController(toast: nil)
                    }
                    //: return
                    return
                }
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<InsideHandyJSON>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: NamePushManager.share.func__actionPushForMessage(jumpModel: jumpModel)
                    NamePushManager.share.constraintScreen(jumpModel: jumpModel)
                }
            }

            //: let extraJson = JSON(extraDic)
            let extraJson = JSON(extraDic)
            //: if  dic["rYMsgType"] as? String == "GJ:CallCustom" {  // 音视频通话消息
            if dic[(String(noti_colorMsg) + String(notiRestoreTitle))] as? String == (String(user_textTitle) + String(m_atPath.suffix(6))) { // 音视频通话消息
                //: if extraJson["msgInfo"]["logType"].intValue == 1 {
                if extraJson[(String(app_sharedTitle.prefix(7)))][(appButtonGroupData.replacingOccurrences(of: "mic", with: "l") + String(show_awakeFormat.prefix(5)))].intValue == 1 {
                    //: checkAndCallVoice()
                    startCheckVideo()
                    //: } else {
                } else {
                    //: checkAndCallVideo()
                    markMake()
                }
            }

            //: } else if cell.isKind(of: TalkingChatTipsMsgCell.self) {
        } else if cell.isKind(of: SharedMsgCell.self) {
            //: let tipCell = cell as! TalkingChatTipsMsgCell
            let tipCell = cell as! SharedMsgCell
            //: let tipData = tipCell.messageData
            let tipData = tipCell.messageData
            //: let dic = StyleChatManager.parseTXMessageData(data: (tipData?.innerMessage.customElem.data)!)
            let dic = StyleChatManager.more(data: (tipData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(k_pushIdent.replacingOccurrences(of: "value", with: "x"))] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains((String(m_modelResolutionMessage.suffix(7)))) {
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<InsideHandyJSON>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: NamePushManager.share.func__actionPushForMessage(jumpModel: jumpModel)
                    NamePushManager.share.constraintScreen(jumpModel: jumpModel)
                }
            }
            //: } else if cell.isKind(of: TalkingChatMatchMsgCell.self) {
        } else if cell.isKind(of: HeadMessageCell.self) {
            //: NamePushManager.share.func__pushToUserDetailVC(uid: targetId)
            NamePushManager.share.pressAway(uid: targetId)
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onRetryMessageContent cell: ReasonViewCell!) {
    func beauty(_: MainViewController!, peeler cell: ReasonViewCell!) {
        //: var cellData: TalkingChatMsgBaseCellData?
        var cellData: DiskTopCellData?
        //: if cell .isKind(of: TalkingChatImageMsgCell.self) {
        if cell.isKind(of: DirectionResumeView.self) {
            //: let imageCell = cell as! TalkingChatImageMsgCell
            let imageCell = cell as! DirectionResumeView
            //: cellData = imageCell.cellData as! ChatThen
            cellData = imageCell.cellData as! ChatThen
            //: } else if cell.isKind(of: TalkingChatVideoMsgCell.self) {
        } else if cell.isKind(of: LabWillThen.self) {
            //: let videoCell = cell as! TalkingChatVideoMsgCell
            let videoCell = cell as! LabWillThen
            //: cellData = videoCell.cellData as! DiskThen
            cellData = videoCell.cellData as! DiskThen
            //: } else if cell.isKind(of: TalkingChatAudioMsgCell.self) {
        } else if cell.isKind(of: ModelServerThen.self) {
            //: let audioCell = cell as! TalkingChatAudioMsgCell
            let audioCell = cell as! ModelServerThen
            //: cellData = audioCell.cellData as! CollectionCellData
            cellData = audioCell.cellData as! CollectionCellData
            //: } else if cell.isKind(of: TalkingChatTextMsgCell.self) {
        } else if cell.isKind(of: ServerMsgCell.self) {
            //: let textCell = cell as! TalkingChatTextMsgCell
            let textCell = cell as! ServerMsgCell
            //: cellData = textCell.cellData as! ButtonCellData
            cellData = textCell.cellData as! ButtonCellData
        }
        //: if cellData != nil {
        if cellData != nil {
            //: let reCellData: ButtonDataThen? = TUISocialChatManager.handleTXMessageReSend(withData: cellData!)
            let reCellData: ButtonDataThen? = PrimrosePathThen.indexAt(withData: cellData!)
            //: if reCellData != nil {
            if reCellData != nil {
                //: cell.messageData?.msgModel = reCellData!.msgModel
                cell.messageData?.msgModel = reCellData!.msgModel
                //: cell.messageData?.status = .Msg_Status_Fail
                cell.messageData?.status = .Msg_Status_Fail
                //: if reCellData!.isKind(of: DiskThen.self) {
                if reCellData!.isKind(of: DiskThen.self) { // 视频重发
                    //: func__sendVideoMsg(msgData: cell.messageData, sendModel: cell.messageData!.msgModel.msgInfo.customSend)
                    funcToModel(msgData: cell.messageData, sendModel: cell.messageData!.msgModel.msgInfo.customSend)
                    //: } else {
                } else {
                    //: func__sendPriveteMsg(msgData: cell.messageData!, reSend: true)
                    signal(msgData: cell.messageData!, reSend: true)
                }
            }
        }
    }

    /// 回复消息
    //: func messageController(_ controller: TUIMessageController!, onReplyMessage cellData: ButtonDataThen!) {
    func view(_: MainViewController!, alongMessage cellData: ButtonDataThen!) {
        //: msgInputView.quoteModel = TalkingPrivateChatManager.handleReplyMsg(cellData: cellData, targetId: userWrap.uid)
        msgInputView.quoteModel = LevelThen.phoneId(cellData: cellData, targetId: userWrap.uid)
    }

    /// 点击“引用”消息
    //: func messageController(_ controller: TUIMessageController!, onClickReplyMessage cellData: ButtonDataThen!) {
    func pause(_: MainViewController!, deviceData cellData: ButtonDataThen!) {
        //: TalkingPrivateChatManager.handleClickReplyMsg(cellData: cellData, targetId: userWrap.uid)
        LevelThen.erase(cellData: cellData, targetId: userWrap.uid)
    }

    /// 实时更新聊天双方头像
    /// - Parameter data: 腾讯数据
    /// - Returns: 腾讯数据
    //: private func changeChatHeadPic(data: ButtonDataThen) -> ButtonDataThen {
    private func digitiser(data: ButtonDataThen) -> ButtonDataThen {
        //: let isSelf = data.msgModel.user.uid == Int(PathAppManager.share.loginUserMode.userID)
        let isSelf = data.msgModel.user.uid == Int(PathAppManager.share.loginUserMode.userID)
        //: if isSelf {
        if isSelf {
            //: if PathAppManager.share.loginUserMode.headPic?.count ?? 0 > 0 {
            if PathAppManager.share.loginUserMode.headPic?.count ?? 0 > 0 {
                //: data.msgModel.user.headPic = PathAppManager.share.loginUserMode.headPic!
                data.msgModel.user.headPic = PathAppManager.share.loginUserMode.headPic!
                //: data.msgModel.user.headPicFrame = PathAppManager.share.loginUserMode.headPicFrame
                data.msgModel.user.headPicFrame = PathAppManager.share.loginUserMode.headPicFrame
            }
            //: } else {
        } else {
            //: if self.userWrap.headPic.count > 0 {
            if self.userWrap.headPic.count > 0 {
                //: data.msgModel.user.headPic = self.userWrap.headPic
                data.msgModel.user.headPic = self.userWrap.headPic
                //: data.msgModel.user.headPicFrame = self.userWrap.headPicFrame
                data.msgModel.user.headPicFrame = self.userWrap.headPicFrame
            }
        }
        //: return data
        return data
    }
}

// MARK: - 发送消息

//: extension TalkingPrivateChatController {
extension TotViewController {
    /// 发送快速打招呼消息
    //: func func_sendQuickGreetingMsg() {
    func utiliserClean() {
        //: msgView.targetID = self.targetId
        msgView.targetID = self.targetId
        //: let msgInfo = ["msgType": "txt"]
        let msgInfo = [String(bytes: dream_valuePath.map{$0^91}, encoding: .utf8)!: (notiWithData.lowercased())]
        //: let paramsDic = ["type": "QuickMsg"]
        let paramsDic = [(appListPath.replacingOccurrences(of: "share", with: "e")): (String(mainModifyText) + String(mainTopKey.prefix(3)))]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(app_sharedTitle.prefix(7))): msgInfo, String(bytes: app_backValueName.map{layerTitle(live: $0)}, encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = PrimrosePathThen.weightAdd(withExtral: dic, isSender: true, isTip: false) {
            //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
            self.signal(msgData: cellData, reSend: false)
        }
    }

    /// 发送一键打招呼消息
    /// - Parameters:
    ///   - textDict: 文本消息
    ///   - audioDict: 语音消息
    //: private func func_sendQuickGreetingMsg(textDict: [String: Any]?, audioDict: [String: Any]?) {
    private func fastBottom(textDict: [String: Any]?, audioDict: [String: Any]?) {
        //: if textDict?.count ?? 0 > 0 {
        if textDict?.count ?? 0 > 0 { // 发送文本消息
            //: let content = textDict?["content"]
            let content = textDict?[String(bytes: main_paraTitle.map{$0^236}, encoding: .utf8)!]
            //: let greetId = textDict?["id"]
            let greetId = textDict?["id"]
            //: let msgInfo = ["content": content, "txtId": greetId, "msgType": "txt"]
            let msgInfo = [String(bytes: main_paraTitle.map{$0^236}, encoding: .utf8)!: content, (String(notiAppBackText)): greetId, String(bytes: dream_valuePath.map{$0^91}, encoding: .utf8)!: (notiWithData.lowercased())]
            //: let paramsDic = ["message": content, "txtId": greetId, "type": "TxtMsg"]
            let paramsDic = [String(bytes: noti_labelMessage.map{firstM(of: $0)}, encoding: .utf8)!: content, (String(notiAppBackText)): greetId, (appListPath.replacingOccurrences(of: "share", with: "e")): (String(m_tempMessage.suffix(6)))]
            //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
            let dic = [(String(app_sharedTitle.prefix(7))): msgInfo, String(bytes: app_backValueName.map{layerTitle(live: $0)}, encoding: .utf8)!: paramsDic]
            //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
            if let cellData = PrimrosePathThen.weightAdd(withExtral: dic, isSender: true, isTip: false) {
                //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
                self.signal(msgData: cellData, reSend: false)
            }
        }

        //: if audioDict?.count ?? 0 > 0 {
        if audioDict?.count ?? 0 > 0 { // 发送语音消息
            //: VoiceDownloadTaskManager.shared.addAudioGreetDownLoadTask(audioDict!) { model in
            InstallmentRateReactiveCompatible.shared.addHandle(audioDict!) { model in
                //: if model.taskTypeId != .oneKeyGreet {
                if model.taskTypeId != .oneKeyGreet {
                    //: return
                    return
                }
                //: if model.taskStatus != .finish {
                if model.taskStatus != .finish {
                    //: DispatchQueue.main.async {
                    DispatchQueue.main.async {
                        //: self.func__showStatusBarErrorMsg(showMsg: "Voice hello download failed. Procedure")
                        self.colorGift(showMsg: String(bytes: appTodayProductData.map{highlightSum(upper: $0)}, encoding: .utf8)!)
                        //: TalkingPrivateChatManager.chat_changeSaveGreetData(toUserId: self.targetId, isHave: false)
                        LevelThen.chatCorner(toUserId: self.targetId, isHave: false)
                    }
                    //: return
                    return
                }
                //: let greetId = audioDict?["id"]
                let greetId = audioDict?["id"]
                //: let length = audioDict?["length"]
                let length = audioDict?[(main_viewData.replacingOccurrences(of: "data", with: "n") + mVideoKey.lowercased())]
                //: let msgInfo = ["msgType": "audio",
                let msgInfo = [String(bytes: dream_valuePath.map{$0^91}, encoding: .utf8)!: (user_timePath.replacingOccurrences(of: "target", with: "di")),
                               //: "audioId": greetId,
                               (String(main_shareDataFormat.suffix(7))): greetId,
                               //: "audioLength": length,
                               String(bytes: userViewStatusUrl.map{letFor(time: $0)}, encoding: .utf8)!: length,
                               //: "audioSandbox": model.unZipFilePath,
                               (notiPushIdent.replacingOccurrences(of: "jump", with: "ud") + "oSan" + dream_componentData.replacingOccurrences(of: "game", with: "x")): model.unZipFilePath,
                               //: "senderId": PathAppManager.share.loginUserMode.userID]
                               String(bytes: userHiddenTitle.map{cornerAccept(sort: $0)}, encoding: .utf8)!: PathAppManager.share.loginUserMode.userID]

                //: let paramsDic = ["audioId": greetId,
                let paramsDic = [(String(main_shareDataFormat.suffix(7))): greetId,
                                 //: "audioLength": length,
                                 String(bytes: userViewStatusUrl.map{letFor(time: $0)}, encoding: .utf8)!: length,
                                 //: "audioSandbox": model.unZipFilePath,
                                 (notiPushIdent.replacingOccurrences(of: "jump", with: "ud") + "oSan" + dream_componentData.replacingOccurrences(of: "game", with: "x")): model.unZipFilePath,
                                 //: "type": "AudioMsg"]
                                 (appListPath.replacingOccurrences(of: "share", with: "e")): (String(m_pathMsg.prefix(7)) + appMaxMakeUrl.lowercased())]
                //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
                let dic = [(String(app_sharedTitle.prefix(7))): msgInfo, String(bytes: app_backValueName.map{layerTitle(live: $0)}, encoding: .utf8)!: paramsDic]
                //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
                if let cellData = PrimrosePathThen.weightAdd(withExtral: dic, isSender: true, isTip: false) {
                    //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
                    self.signal(msgData: cellData, reSend: false)
                }
            }
        }
    }

    /// 发送文本消息
    /// - Parameter msgStr: 文本
    //: internal func func__sendTextMsg(msgStr: String, quoteModel: AbTalkingChatMsgQuoteModel?) {
    func firstDismissView(msgStr: String, quoteModel: EnableScreenModel?) {
        //: let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        //: if temStr.count == 0 {
        if temStr.count == 0 {
            //: func__showStatusBarErrorMsg(showMsg: "Can't send blank message".localized)
            colorGift(showMsg: (String(app_toMessage) + notiDayLengthContent.replacingOccurrences(of: "request", with: "m") + mModelBlockTitle.replacingOccurrences(of: "black", with: "e")).localized)
            //: return
            return
        }

        //: let msgInfo = ["content": temStr, "msgType": "txt"]
        let msgInfo = [String(bytes: main_paraTitle.map{$0^236}, encoding: .utf8)!: temStr, String(bytes: dream_valuePath.map{$0^91}, encoding: .utf8)!: (notiWithData.lowercased())]
        //: var paramsDic = ["message": temStr, "type": "TxtMsg"]
        var paramsDic = [String(bytes: noti_labelMessage.map{firstM(of: $0)}, encoding: .utf8)!: temStr, (appListPath.replacingOccurrences(of: "share", with: "e")): (String(m_tempMessage.suffix(6)))]
        //: var dic:[String: Any] = ["msgInfo": msgInfo]
        var dic: [String: Any] = [(String(app_sharedTitle.prefix(7))): msgInfo]

        //: if let tempQuoteModel = quoteModel{
        if let tempQuoteModel = quoteModel {
            //: paramsDic["quoteMsgId"] = tempQuoteModel.msgId
            paramsDic[(dream_voiceValue.replacingOccurrences(of: "system", with: "t") + "eMsg" + String(main_labContent.prefix(2)))] = tempQuoteModel.msgId
            //: dic["quoteMsgInfo"] = tempQuoteModel.toDictionary()
            dic[(String(kColorFormat))] = tempQuoteModel.toDictionary()
        }
        //: dic["params"] = paramsDic
        dic[String(bytes: app_backValueName.map{layerTitle(live: $0)}, encoding: .utf8)!] = paramsDic

        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = PrimrosePathThen.weightAdd(withExtral: dic, isSender: true, isTip: false) {
            //: cellData.msgModel.quoteMsgInfo = quoteModel ?? AbTalkingChatMsgQuoteModel()
            cellData.msgModel.quoteMsgInfo = quoteModel ?? EnableScreenModel()
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            signal(msgData: cellData, reSend: false)
        }
    }

    /// 发送图片消息
    /// - Parameters:
    ///   - img: 图片
    ///   - sendModel: 本地数据模型
    //: private func func__sendPictureMsg(img: UIImage, sendModel: AbTalkingChatMsgCustomSendModel) {
    private func imageBegin(img: UIImage, sendModel: TextLeadingModel) {
        //: let imagePath = ChatThen.getChatImagePath(image: img)
        let imagePath = ChatThen.noCell(image: img)
        // 缓存本地的消息
        //: let sendDict = sendModel.toDictionary() ?? [:]
        let sendDict = sendModel.toDictionary() ?? [:]
        //: let msgInfo: [String: Any] = ["contentType": "ImgMsg", "msgType": "img", "imageUri": imagePath, "customSend": sendDict]
        let msgInfo: [String: Any] = [String(bytes: show_underText.map{decisionMaker(range: $0)}, encoding: .utf8)!: (String(app_colorOfKey.suffix(6))), String(bytes: dream_valuePath.map{$0^91}, encoding: .utf8)!: (app_itemUrl.replacingOccurrences(of: "log", with: "i")), String(bytes: mSendValue.map{titleImage(label: $0)}, encoding: .utf8)!: imagePath, String(bytes: mainUpperTitle.map{labKind(info: $0)}, encoding: .utf8)!: sendDict]
        // 接口参数
        //: let paramsDic: [String: Any] = ["pic": imagePath,
        let paramsDic: [String: Any] = [(user_actionMessage.lowercased()): imagePath,
                                        //: "type": "ImgMsg",
                                        (appListPath.replacingOccurrences(of: "share", with: "e")): (String(app_colorOfKey.suffix(6))),
                                        //: "isBurn": sendModel.isBurn,
                                        String(bytes: m_currentCellMsg.map{militaryInstallation(to: $0)}, encoding: .utf8)!: sendModel.isBurn,
                                        //: "giftId": sendModel.giftId]
                                        (String(dreamOfSucceedIdent.suffix(6))): sendModel.giftId]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(app_sharedTitle.prefix(7))): msgInfo, String(bytes: app_backValueName.map{layerTitle(live: $0)}, encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = PrimrosePathThen.weightAdd(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            signal(msgData: cellData, reSend: false)
        }
    }

    /// 发送视频消息
    /// - Parameters:
    ///   - msgData: 发送视频cellData；nil：上传流程，有值 ：发送流程
    ///   - sendModel: 视频消息自定义本地模型
    //: private func func__sendVideoMsg(msgData: ButtonDataThen?, sendModel: AbTalkingChatMsgCustomSendModel) {
    private func funcToModel(msgData: ButtonDataThen?, sendModel: TextLeadingModel) {
        // 缓存本地的消息
        //: let videoDict = sendModel.toDictionary() ?? [:]
        let videoDict = sendModel.toDictionary() ?? [:]
        //: let msgInfo: [String: Any] = ["contentType": "VideoMsg", "msgType": "video", "customSend": videoDict]
        let msgInfo: [String: Any] = [String(bytes: show_underText.map{decisionMaker(range: $0)}, encoding: .utf8)!: (notiFieldId.replacingOccurrences(of: "send", with: "V") + String(showEqualAppText.prefix(7))), String(bytes: dream_valuePath.map{$0^91}, encoding: .utf8)!: (userAnglePath.lowercased()), String(bytes: mainUpperTitle.map{labKind(info: $0)}, encoding: .utf8)!: videoDict]
        // 接口参数
        //: let paramsDic: [String: Any] = ["type": "VideoMsg",
        let paramsDic: [String: Any] = [(appListPath.replacingOccurrences(of: "share", with: "e")): (notiFieldId.replacingOccurrences(of: "send", with: "V") + String(showEqualAppText.prefix(7))),
                                        //: "coverImg": sendModel.coverImg,
                                        String(bytes: userCornerAgainName.map{contentTheory(file: $0)}, encoding: .utf8)!: sendModel.coverImg,
                                        //: "videoUrl": sendModel.videoUrl,
                                        String(bytes: kLockContent.map{$0^28}, encoding: .utf8)!: sendModel.videoUrl,
                                        //: "duration": sendModel.duration,
                                        String(bytes: show_liveText.map{beanEffect(make: $0)}, encoding: .utf8)!: sendModel.duration,
                                        //: "isBurn": sendModel.isBurn,
                                        String(bytes: m_currentCellMsg.map{militaryInstallation(to: $0)}, encoding: .utf8)!: sendModel.isBurn,
                                        //: "giftId": sendModel.giftId]
                                        (String(dreamOfSucceedIdent.suffix(6))): sendModel.giftId]
        //: let dict = ["msgInfo": msgInfo, "params": paramsDic]
        let dict = [(String(app_sharedTitle.prefix(7))): msgInfo, String(bytes: app_backValueName.map{layerTitle(live: $0)}, encoding: .utf8)!: paramsDic]

        // 压缩成功 ---> 创建cellData ---> 上传视频
        //: guard let cellData = msgData else {
        guard let cellData = msgData else {
            //: if let uploadCellData = TUISocialChatManager.handleTXMessageSend(withExtral: dict, isSender: true, isTip: false) {
            if let uploadCellData = PrimrosePathThen.weightAdd(withExtral: dict, isSender: true, isTip: false) {
                //: self.msgView.sendCustomMessage(uploadCellData)
                self.msgView.send(uploadCellData)
                //: self.cos_UploadVideo(msgData: uploadCellData, sendModel: sendModel)
                self.anonymUpHypothesis(msgData: uploadCellData, sendModel: sendModel)
            }
            //: return
            return
        }

        // 失败重试，展示加载动画
        //: if cellData.status != .Msg_Status_Sending_2 {
        if cellData.status != .Msg_Status_Sending_2 {
            //: self.msgView.changeCustomMsg(cellData, status: .Msg_Status_Sending_2)
            self.msgView.device(cellData, damageDown: .Msg_Status_Sending_2)
        }

        // 失败重试上传视频流程
        //: guard sendModel.videoUrl.count > 0 else {
        guard sendModel.videoUrl.count > 0 else {
            //: self.cos_UploadVideo(msgData: cellData, sendModel: sendModel)
            self.anonymUpHypothesis(msgData: cellData, sendModel: sendModel)
            //: return
            return
        }

        // 修改本地消息数据，调用发送视频接口
        //: self.msgView.modifyCustomMessageData(withExtral: dict, cellData: cellData)
        self.msgView.bill(ofFare: dict, extralView: cellData)
        //: func__sendPriveteMsg(msgData: cellData, reSend: false)
        signal(msgData: cellData, reSend: false)
    }

    /// 发送语音
    /// - Parameters:
    ///   - voicePath: 语音路径
    ///   - voiceTime: 时间
    //: func func__sendVoiceMsg(voicePath: String, voiceTime: Int) {
    func confinement(voicePath: String, voiceTime: Int) {
        //: let msgInfo = ["msgType": "audio",
        let msgInfo = [String(bytes: dream_valuePath.map{$0^91}, encoding: .utf8)!: (user_timePath.replacingOccurrences(of: "target", with: "di")),
                       //: "audioLength": "\(voiceTime)",
                       String(bytes: userViewStatusUrl.map{letFor(time: $0)}, encoding: .utf8)!: "\(voiceTime)",
                       //: "audioSandbox": voicePath,
                       (notiPushIdent.replacingOccurrences(of: "jump", with: "ud") + "oSan" + dream_componentData.replacingOccurrences(of: "game", with: "x")): voicePath,
                       //: "senderId": PathAppManager.share.loginUserMode.userID]
                       String(bytes: userHiddenTitle.map{cornerAccept(sort: $0)}, encoding: .utf8)!: PathAppManager.share.loginUserMode.userID]

        //: let paramsDic = ["audioSandbox": voicePath,
        let paramsDic = [(notiPushIdent.replacingOccurrences(of: "jump", with: "ud") + "oSan" + dream_componentData.replacingOccurrences(of: "game", with: "x")): voicePath,
                         //: "type": "AudioMsg",
                         (appListPath.replacingOccurrences(of: "share", with: "e")): (String(m_pathMsg.prefix(7)) + appMaxMakeUrl.lowercased()),
                         //: "len": "\(voiceTime)"]
                         String(bytes: showFemaleData.reversed(), encoding: .utf8)!: "\(voiceTime)"]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(app_sharedTitle.prefix(7))): msgInfo, String(bytes: app_backValueName.map{layerTitle(live: $0)}, encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = PrimrosePathThen.weightAdd(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            signal(msgData: cellData, reSend: false)
        }
    }

    /// 发送消息
    /// - Parameters:
    ///   - msgData: 消息数据结构
    ///   - reSend: 是否重发【目前未使用该字段】
    //: private func func__sendPriveteMsg(msgData: ButtonDataThen, reSend: Bool) {
    private func signal(msgData: ButtonDataThen, reSend _: Bool) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            colorGift(showMsg: user_netTitle)
            //: return
            return
        }

        //: var params: Dictionary<String, Any> = msgData.msgModel.params as! Dictionary<String, Any>
        var params: [String: Any] = msgData.msgModel.params as! [String: Any]
        //: params["toUid"] = self.targetId
        params[(String(dreamWithMsg.suffix(5)))] = self.targetId

        // 发送视频消息，索要礼物消息，快速打招呼消息，不在此处插入
        //: if msgData.isKind(of: DiskThen.self) == false && msgData.isKind(of: IndexVideoReactiveCompatible.self) == false && params["type"] as? String != "QuickMsg" {
        if msgData.isKind(of: DiskThen.self) == false, msgData.isKind(of: IndexVideoReactiveCompatible.self) == false, params[(appListPath.replacingOccurrences(of: "share", with: "e"))] as? String != (String(mainModifyText) + String(mainTopKey.prefix(3))) {
            //: self.msgView.sendCustomMessage(msgData)
            self.msgView.send(msgData)
        }
        //: let recordTime = StyleChatManager.getMessageInsertTime()
        let recordTime = StyleChatManager.sizeTime()

        //: if params.keys .contains("pic") { // 图片
        if params.keys.contains((user_actionMessage.lowercased())) { // 图片
            //: let imageStr: String = params["pic"] as! String
            let imageStr: String = params[(user_actionMessage.lowercased())] as! String
            //: do {
            do {
                //: let data: Data = try Data.init(contentsOf: URL.init(fileURLWithPath: imageStr))
                let data: Data = try Data(contentsOf: URL(fileURLWithPath: imageStr))
                //: params["pic"] = data
                params[(user_actionMessage.lowercased())] = data
                //: } catch {
            } catch {
                //: printLog(message: error)
                printLog(message: error)
            }
        }
        //: let type  = params["type"] as! String
        let type = params[(appListPath.replacingOccurrences(of: "share", with: "e"))] as! String
        //: if type == "AudioMsg" { // 语音
        if type == (String(m_pathMsg.prefix(7)) + appMaxMakeUrl.lowercased()) { // 语音
            //: if params.keys.contains("audioSandbox") {
            if params.keys.contains((notiPushIdent.replacingOccurrences(of: "jump", with: "ud") + "oSan" + dream_componentData.replacingOccurrences(of: "game", with: "x"))) {
                //: var path =  params["audioSandbox"] as! String
                var path = params[(notiPushIdent.replacingOccurrences(of: "jump", with: "ud") + "oSan" + dream_componentData.replacingOccurrences(of: "game", with: "x"))] as! String
                //: params.removeValue(forKey: "audioSandbox")
                params.removeValue(forKey: (notiPushIdent.replacingOccurrences(of: "jump", with: "ud") + "oSan" + dream_componentData.replacingOccurrences(of: "game", with: "x")))

                //: let theFileName = (path as NSString).lastPathComponent
                let theFileName = (path as NSString).lastPathComponent

                //: if path.contains("Documents/User/voice/") {
                if path.contains((String(k_topContent.suffix(5)) + String(main_cellMessage) + String(app_infoName))) {
                    //: path = Talking_Chat_voice_SandboxPath + theFileName
                    path = main_methodTitle + theFileName
                    //: } else if(path.contains("Documents/User/record/")) {
                } else if path.contains((String(notiDataId))) {
                    //: path = Talking_Chat_record_SandboxPath + theFileName
                    path = show_indexStatusMessage + theFileName
                }

                //: let audioId = params["audioId"] ?? "0"
                let audioId = params[(String(main_shareDataFormat.suffix(7)))] ?? "0"
                //: if  (audioId as! NSString).integerValue == 0 {
                if (audioId as! NSString).integerValue == 0 {
                    //: do {
                    do {
                        //: let voiceData = try Data.init(contentsOf: URL.init(fileURLWithPath: path))
                        let voiceData = try Data(contentsOf: URL(fileURLWithPath: path))
                        //: params["audio"] = voiceData
                        params[(user_timePath.replacingOccurrences(of: "target", with: "di"))] = voiceData
                        //: } catch {
                    } catch {}
                }
            }
        }

        // 发送消息接口
        //: TalkingChatRequestTool.req_SendPrivete(param: params, completion: { succeed, result, errorModel in
        SourceRequestTool.writeCompletion(param: params, completion: { succeed, result, errorModel in
            //: if !succeed {
            if !succeed {
                //: if type == "QuickMsg" {
                if type == (String(mainModifyText) + String(mainTopKey.prefix(3))) {
                    //: self.QuickEndBlock?(false)
                    self.QuickEndBlock?(false)
                }
                //: self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                self.msgView.outMessage(msgData, photoStatus: nil, length: NSNumber(value: 0), incomeStatus: .Msg_Status_Fail)
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickMessagesendButton)
                self.coloringShow(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: dreamQuantityMsg)
                //: if params.keys.contains("txtId") {
                if params.keys.contains((String(notiAppBackText))) {
                    //: TalkingPrivateChatManager.chat_changeSaveGreetData(toUserId: self.targetId, isHave: false)
                    LevelThen.chatCorner(toUserId: self.targetId, isHave: false)
                }
                /// 打招呼达到设定次数失败时
                //: if errorModel!.errorCode == 10015 {
                if errorModel!.errorCode == 10015 {
                    //: self.msgView.deleteCustomMessage(msgData)
                    self.msgView.basilarArtery(msgData)
                }
                //: return
                return
            }
            //: var updateMsgData = msgData
            var updateMsgData = msgData
            //: let dataDict = result as! Dictionary<String, Any>
            let dataDict = result as! [String: Any]
            //: var msgInfo = dataDict["msgInfo"] as! Dictionary<String, Any>
            var msgInfo = dataDict[(String(app_sharedTitle.prefix(7)))] as! [String: Any]

            //: var msgIncome: NSNumber = NSNumber.init(value: 0)
            var msgIncome: NSNumber = .init(value: 0)
            //: if msgInfo.keys.contains("msgIncome") {
            if msgInfo.keys.contains((String(kFromKey.prefix(7)) + k_labelId.lowercased())) {
                //: msgIncome = msgInfo["msgIncome"] as! NSNumber
                msgIncome = msgInfo[(String(kFromKey.prefix(7)) + k_labelId.lowercased())] as! NSNumber
            }

            // 直播时回复消息，积分更改
            //: if TalkingLiveManager.shared().isLive && msgIncome.floatValue > 0 {
            if CommentLiveManager.leaveBy().isLive && msgIncome.floatValue > 0 {
                //: let mfBean = Double(truncating: msgIncome)
                let mfBean = Double(truncating: msgIncome)
                //: NotificationCenter.default.post(name: LIVE_USER_POINT_CHANGE, object: nil, userInfo: ["mfBean": mfBean])
                NotificationCenter.default.post(name: kTabSearchMessage, object: nil, userInfo: [String(bytes: showToIdent.reversed(), encoding: .utf8)!: mfBean])
            }
            /// 亲密度
            //: self.sendMsgIntimateTool(msgInfo: msgInfo)
            self.observerSex(msgInfo: msgInfo)

            /// 索要礼物，快速打招呼
            //: let messageType = msgInfo["messageType"] as? Int
            let messageType = msgInfo[(dream_pathTagMessage.replacingOccurrences(of: "item", with: "e") + "ageType")] as? Int
            //: if messageType == 25 || type == "QuickMsg" {
            if messageType == 25 || type == (String(mainModifyText) + String(mainTopKey.prefix(3))) {
                //: if messageType == 25 {
                if messageType == 25 {
                    //: msgInfo["msgType"] = "WantGift"
                    msgInfo[String(bytes: dream_valuePath.map{$0^91}, encoding: .utf8)!] = (String(m_hiddenMessage))
                    //: uploadRecord.uploadRecordEvent(eventID: clickMessageDetailGiftPopAskBtnSusType)
                    kResolutionText.atJson(eventID: mVideoUrl)
                    //: } else if type == "QuickMsg" {
                } else if type == (String(mainModifyText) + String(mainTopKey.prefix(3))) {
                    //: msgInfo["msgType"] = "txt"
                    msgInfo[String(bytes: dream_valuePath.map{$0^91}, encoding: .utf8)!] = (notiWithData.lowercased())
                    //: self.QuickEndBlock?(true)
                    self.QuickEndBlock?(true)
                }
                //: let dic = ["msgInfo": msgInfo]
                let dic = [(String(app_sharedTitle.prefix(7))): msgInfo]
                //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
                if let cellData = PrimrosePathThen.weightAdd(withExtral: dic, isSender: true, isTip: false) {
                    //: updateMsgData = cellData
                    updateMsgData = cellData
                    //: self.msgView.sendCustomMessage(updateMsgData)
                    self.msgView.send(updateMsgData)
                }
            }

            // 更新提示文案
            //: self.userWrap.existSess = true
            self.userWrap.existSess = true
            //: let key =  "\(PathAppManager.share.loginUserMode.userID)_PrivateChatTipsTitle_\(self.targetId)"
            let key = "\(PathAppManager.share.loginUserMode.userID)" + (String(m_userFormat.prefix(5)) + "ateCh" + String(mainTotalTitleId.suffix(6)) + "Title" + userViewMsg.replacingOccurrences(of: "equal", with: "_")) + "\(self.targetId)"
            //: UserDefaults.standard.set(true, forKey: key)
            UserDefaults.standard.set(true, forKey: key)
            //: if PathAppManager.share.loginUserMode.sex == Gender.male.rawValue && type != "QuickMsg" {
            if PathAppManager.share.loginUserMode.sex == ConnectionClusterLiteral.male.rawValue, type != (String(mainModifyText) + String(mainTopKey.prefix(3))) {
                //: self.removeReplyTipsView()
                self.removeFromView()
            }
            // 更新本地消息信息
            //: self.msgView.changeCustomMsg(updateMsgData, msgId: msgInfo["msgId"] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)
            self.msgView.outMessage(updateMsgData, photoStatus: msgInfo[(String(appGroupValue))] as? String, length: msgIncome, incomeStatus: .Msg_Status_Succ)

            // 女性记录最新回复时间, 刷新表格
            //: TalkingPrivateChatManager.updateReplyLatestTimeAndReload(CGFloat(msgIncome.floatValue), updateMsgData.innerMessage.timestamp, self.msgView)
            LevelThen.reload(CGFloat(msgIncome.floatValue), updateMsgData.innerMessage.timestamp, self.msgView)

            //: if dataDict.keys.contains("totalMfCoin") {
            if dataDict.keys.contains((String(kSuiteTrackMsg) + String(main_sizeName.suffix(4)))) {
                //: let totalMfCoin: NSNumber = dataDict["totalMfCoin"] as! NSNumber
                let totalMfCoin: NSNumber = dataDict[(String(kSuiteTrackMsg) + String(main_sizeName.suffix(4)))] as! NSNumber
                //: PathAppManager.share.loginUserMode.mf_coin = totalMfCoin.stringValue
                PathAppManager.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            }

            /// 免费消息提示
            //: if dataDict.keys.contains("attachSysMsg") {
            if dataDict.keys.contains((user_seatMsg.replacingOccurrences(of: "content", with: "t") + "hSysMsg")) {
                //: let attachSysMsg: String = dataDict["attachSysMsg"] as? String ?? ""
                let attachSysMsg: String = dataDict[(user_seatMsg.replacingOccurrences(of: "content", with: "t") + "hSysMsg")] as? String ?? ""
                //: if attachSysMsg.count>0 {
                if attachSysMsg.count > 0 {
                    //: self.func__privateAttachSys_insertTipMsg(str: attachSysMsg)
                    self.timeSendStr(str: attachSysMsg)
                }
            }

            //: if msgInfo.keys.contains("intimatePhoto") { // 发送成功私密照片 => model，更新本地消息
            if msgInfo.keys.contains((String(mainAddTitlePath))) { // 发送成功私密照片 => model，更新本地消息
                //: let intimatePhotoDict = msgInfo["intimatePhoto"] as! [String: Any]
                let intimatePhotoDict = msgInfo[(String(mainAddTitlePath))] as! [String: Any]
                //: if let model = try? AbTalkingChatMsgIntimatePhotoModel(dictionary: intimatePhotoDict) {
                if let model = try? StochasticProcessModel(dictionary: intimatePhotoDict) {
                    //: updateMsgData.msgModel.msgInfo.intimatePhoto = model
                    updateMsgData.msgModel.msgInfo.intimatePhoto = model
                    //: self.msgView.changeIntimatePhotoMsg(updateMsgData, photoModel: model)
                    self.msgView.atView(updateMsgData, file: model)
                }

                //: } else if msgInfo.keys.contains("video") { // 发送成功私密视频 => model，更新本地消息
            } else if msgInfo.keys.contains((userAnglePath.lowercased())) { // 发送成功私密视频 => model，更新本地消息
                //: let intimateVideoDict = msgInfo["video"] as! [String: Any]
                let intimateVideoDict = msgInfo[(userAnglePath.lowercased())] as! [String: Any]
                //: if let model = try? AbTalkingChatMsgVideoModel(dictionary: intimateVideoDict) {
                if let model = try? ActualModel(dictionary: intimateVideoDict) {
                    //: updateMsgData.msgModel.msgInfo.video = model
                    updateMsgData.msgModel.msgInfo.video = model
                    //: self.msgView.changeIntimateVideoMsg(updateMsgData, videoModel: model)
                    self.msgView.spectrum(updateMsgData, stop: model)
                }
            }
            //: })
        })

        // 判断是否需要插入音视频提示消息
        //: func__privateChatVideo_insertTipMsg()
        setMsg()

        //: updateSession()
        alongRestore()
    }

    /// 建立会话标识，本地更新数据和会话列表
    //: func updateSession() {
    func alongRestore() {
        //: DBUserInfoManager.cache_updateSessionAndPostNotif(with: targetId)
        ModeratenessReactiveCompatible.finishConstraint(with: targetId)
    }

    /// 处理发送失败情况
    //: private func dealSendMsgError(errorCode: Int, errorStr: String, isGift: Bool, isResend: Bool, clickEvent: String) {
    private func coloringShow(errorCode: Int, errorStr: String, isGift _: Bool, isResend _: Bool, clickEvent: String) {
        //: if errorCode == 50203 {
        if errorCode == 50203 { // 余额不足
            //: guard PathAppManager.share.loginUserMode.status != 1 else {
            guard PathAppManager.share.loginUserMode.status != 1 else {
                //: if !errorStr.isEmptyString {
                if !errorStr.isEmptyString {
                    //: func__showStatusBarErrorMsg(showMsg: errorStr)
                    colorGift(showMsg: errorStr)
                }
                //: return
                return
            }

            //: NamePushManager.share.func__jumpToWebRecharge(clickEvent: clickEvent, sufficient: false)
            NamePushManager.share.rechargeParams(clickEvent: clickEvent, sufficient: false)
            //: msgInputView.giftView.dismissView()
            msgInputView.giftView.kisser()
//            insertTipMsg_Nobalance(tipStr: errorStr)
            //: } else if errorCode == 10888 {
        } else if errorCode == 10888 { // vip订阅弹窗
            //: let view = TalkingLiveVipSubscribePopView.init(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
            let view = LightPopView(frame: CGRect(x: 0, y: 0, width: app_screenFailMarginFormat, height: mainAcceptKey))
            //: view.show()
            view.characterSet()
            //: } else if errorCode == 60003, !PathAppManager.share.loginUserMode.isNaUser {
        } else if errorCode == 60003, !PathAppManager.share.loginUserMode.isNaUser {
            /// 真人认证提示弹窗
            //: TalkingPopupWindowManager.shared.faceVerificationPopUpWindow()
            ConfinementWindowManager.shared.executeSize()
            //: } else {
        } else {
            //: if !errorStr.isEmptyString {
            if !errorStr.isEmptyString {
                //: func__showStatusBarErrorMsg(showMsg: errorStr)
                colorGift(showMsg: errorStr)
            }
        }
    }
}

// MARK: - 亲密度

//: extension TalkingPrivateChatController {
extension TotViewController {
    /// 解锁图片视频送礼亲密度
    //: private func unlockPhotoOrVideoTool(dataDict: Dictionary<String, Any>) {
    private func unlockAndSizeVia(dataDict: [String: Any]) {
        //: let msgInfo = dataDict["msgInfo"] as! Dictionary<String, Any>
        let msgInfo = dataDict[(String(app_sharedTitle.prefix(7)))] as! [String: Any]
        /// 亲密度
        //: self.sendMsgIntimateTool(msgInfo: msgInfo)
        self.observerSex(msgInfo: msgInfo)
    }

    /// 初次展示导航栏的亲密度头像
    //: private func showFirstIntimateView() {
    private func pushDoingTo() {
        //: if self.userWrap.intimate >= PathAppManager.share.appUserConfigMode.intimateLimit.chatTitleEffect && conversationType == .private  {
        if self.userWrap.intimate >= PathAppManager.share.appUserConfigMode.intimateLimit.chatTitleEffect, conversationType == .private {
            //: self.isShowIntimateView = true
            self.isShowIntimateView = true
            //: navView.private_updateIntimateNavView(userModel: userWrap)
            navView.rootModel(userModel: userWrap)
            /// 初次展示动效
            //: let key = "\(PathAppManager.share.loginUserMode.userID)_\(self.targetId)"
            let key = "\(PathAppManager.share.loginUserMode.userID)_\(self.targetId)"
            //: var intimateCacheData = (Defaults.array(forKey: TalkingFirstShowIntimateGuide)) as? [String] ?? []
            var intimateCacheData = (dreamBottomMsg.array(forKey: showLiveTitle)) as? [String] ?? []
            //: if !intimateCacheData.contains(key) {
            if !intimateCacheData.contains(key) {
                //: navView.playAnimation()
                navView.caput()
                //: intimateCacheData.append(key)
                intimateCacheData.append(key)
                //: Defaults.set(intimateCacheData, forKey: TalkingFirstShowIntimateGuide)
                dreamBottomMsg.set(intimateCacheData, forKey: showLiveTitle)
            }
        }
    }

    /// 收到亲密度消息
    //: @objc func newMsgIntimate(notification: NSNotification) {
    @objc func postUp(notification: NSNotification) {
        //: let data = notification.object as! V2TIMMessage
        let data = notification.object as! V2TIMMessage
        //: let dic = StyleChatManager.parseTXMessageData(data: data.customElem.data)
        let dic = StyleChatManager.more(data: data.customElem.data)
        //: let jsonDic = JSON(dic)
        let jsonDic = JSON(dic)
        //: let extra = jsonDic["extra"].dictionary
        let extra = jsonDic[(k_pushIdent.replacingOccurrences(of: "value", with: "x"))].dictionary
        //: let msgInfo = extra?["msgInfo"]?.dictionary
        let msgInfo = extra?[(String(app_sharedTitle.prefix(7)))]?.dictionary
        //: if data.sender == self.targetId {
        if data.sender == self.targetId {
            //: if let totalIntimate = msgInfo?["totalIntimate"]?.int {
            if let totalIntimate = msgInfo?[(String(mainListPath) + String(appManagerReportClearPath))]?.int {
                //: if self.conversationType == .private {
                if self.conversationType == .private {
                    //: let time = data.timestamp.timeIntervalSince1970
                    let time = data.timestamp.timeIntervalSince1970
                    //: self.intimateTool(currenIntimate: totalIntimate, time: Int(time))
                    self.bean(currenIntimate: totalIntimate, time: Int(time))
                }
            }
        }
    }

    /// 发送消息时
    //: private func sendMsgIntimateTool(msgInfo: Dictionary<String, Any>) {
    private func observerSex(msgInfo: [String: Any]) {
        //: if msgInfo.keys.contains("totalIntimate") && self.conversationType == .private {
        if msgInfo.keys.contains((String(mainListPath) + String(appManagerReportClearPath))), self.conversationType == .private {
            //: let totalIntimacy = msgInfo["totalIntimate"] as? Int ?? 0
            let totalIntimacy = msgInfo[(String(mainListPath) + String(appManagerReportClearPath))] as? Int ?? 0
            //: self.intimateTool(currenIntimate: totalIntimacy)
            self.bean(currenIntimate: totalIntimacy)
        }
    }

    /// 亲密度操作
    //: func intimateTool(currenIntimate: Int, time: Int = 0) {
    func bean(currenIntimate: Int, time: Int = 0) {
        //: guard currenIntimate > 0 else { return }
        guard currenIntimate > 0 else { return }
        //: let model = DBUserInfoManager.cache_getCachedUserInfo(targetId: self.targetId)
        let model = ModeratenessReactiveCompatible.drippingPan(targetId: self.targetId)
        //: self.userWrap.intimate = currenIntimate
        self.userWrap.intimate = currenIntimate
        /// 满足最低条件，展示页面
        //: if currenIntimate >= PathAppManager.share.appUserConfigMode.intimateLimit.chatTitleEffect {
        if currenIntimate >= PathAppManager.share.appUserConfigMode.intimateLimit.chatTitleEffect {
            /// 亲密度增加
            //: if !self.isShowIntimateView {
            if !self.isShowIntimateView {
                //: self.isShowIntimateView = true
                self.isShowIntimateView = true
                //: navView.private_updateIntimateNavView(userModel: self.userWrap)
                navView.rootModel(userModel: self.userWrap)
            }
            //: if currenIntimate - (model?.intimate ?? 0) > 0 {
            if currenIntimate - (model?.intimate ?? 0) > 0 {
                //: self.navView.playIntimateAnim(intimate: currenIntimate - (model?.intimate ?? 0))
                self.navView.finishDisplay(intimate: currenIntimate - (model?.intimate ?? 0))
                /// 播放完毕
                //: self.navView.animEndBlock = { [weak self] in
                self.navView.animEndBlock = { [weak self] in
                    //: guard let self = self else { return }
                    guard let self = self else { return }
                    //: self.navView.setAllData(intimate: currenIntimate)
                    self.navView.objectCountimate(intimate: currenIntimate)
                }
                //: } else {
            } else {
                //: self.navView.setAllData(intimate: currenIntimate)
                self.navView.objectCountimate(intimate: currenIntimate)
            }
        }

        //: let currTime = time > 0 ? time: Int(Date().timeIntervalSince1970)
        let currTime = time > 0 ? time : Int(Date().timeIntervalSince1970)
        //: DBUserInfoManager.cache_updateIntimate(with: targetId, intimate: currenIntimate, currTime: currTime)
        ModeratenessReactiveCompatible.messagePath(with: targetId, intimate: currenIntimate, currTime: currTime)
        //: let userInfo = ["uid": self.targetId,
        let userInfo = [String(bytes: app_giftMessage.reversed(), encoding: .utf8)!: self.targetId,
                        //: "intimate": currenIntimate] as [String : Any]
                        String(bytes: dream_hiddenUrl.map{miniData(identity: $0)}, encoding: .utf8)!: currenIntimate] as [String: Any]
        //: NotificationCenter.default.post(name: MSGLIST_UPDATE_INTIMATE_NOTIFICATION,
        NotificationCenter.default.post(name: main_netText,
                                        //: object: nil,
                                        object: nil,
                                        //: userInfo: userInfo)
                                        userInfo: userInfo)
    }
}

// MARK: - 输入框

//: extension TalkingPrivateChatController: ChatInputViewDelegate {
extension TotViewController: FullThen {
    /// 检测是否需要展示翻译入口
    //: func func_inputContentIntervalOneSecond() {
    func beforeOf() {
        //: guard PathAppManager.share.loginUserMode.sex == Gender.female.rawValue else { return }
        guard PathAppManager.share.loginUserMode.sex == ConnectionClusterLiteral.female.rawValue else { return }
        //: let isEnglish = selectTranView.checkInputLanguage(self.msgInputView.inputTextView.text)
        let isEnglish = selectTranView.textAdd(self.msgInputView.inputTextView.text)
        //: self.selectTranView.isHidden = isEnglish
        self.selectTranView.isHidden = isEnglish
    }

    //: func func__selectMentionUser(range: NSRange) {}
    func length(range _: NSRange) {}

    //: func func__selectMoreFunAction(actionType: MoreActionType) {
    func pic(actionType: AssemblageTotalLiteral) {
        //: switch actionType {
        switch actionType {
        //: case .Photo:
        case .Photo:
            //: if self.conversationType == .private {
            if self.conversationType == .private {
                //: if self.userWrap.intimate >= PathAppManager.share.appUserConfigMode.intimateLimit.sendImg {
                if self.userWrap.intimate >= PathAppManager.share.appUserConfigMode.intimateLimit.sendImg {
                    //: func__choicePhotos()
                    statusTo()
                    //: } else {
                } else {
                    //: showIntimateErrorStr()
                    smirch()
                }
                //: } else {
            } else {
                //: func__choicePhotos()
                statusTo()
            }
        //: case .VideoCall:
        case .VideoCall: // 音/视频通话
            //: var videoStr: String?
            var videoStr: String?
            //: var voiceStr: String?
            var voiceStr: String?
            //: if PathAppManager.share.loginUserMode.sex == Gender.female.rawValue {
            if PathAppManager.share.loginUserMode.sex == ConnectionClusterLiteral.female.rawValue {
                //: videoStr = self.userWrap.videoBean
                videoStr = self.userWrap.videoBean
                //: voiceStr = self.userWrap.voiceBean
                voiceStr = self.userWrap.voiceBean
                //: } else {
            } else {
                //: if PathAppManager.share.loginUserMode.loungePlus {
                if PathAppManager.share.loginUserMode.loungePlus { // 男性会员
                    //: videoStr = self.userWrap.videoVIPPrice
                    videoStr = self.userWrap.videoVIPPrice
                    //: voiceStr = self.userWrap.voiceVIPPrice
                    voiceStr = self.userWrap.voiceVIPPrice
                    //: } else {
                } else {
                    //: videoStr = self.userWrap.videoPrice
                    videoStr = self.userWrap.videoPrice
                    //: voiceStr = self.userWrap.voicePrice
                    voiceStr = self.userWrap.voicePrice
                }
            }
            //: if self.userWrap.intimate >= PathAppManager.share.appUserConfigMode.intimateLimit.requestCall {
            if self.userWrap.intimate >= PathAppManager.share.appUserConfigMode.intimateLimit.requestCall {
                //: videoCallManager.showVideoCallMenu(videoPrice: videoStr, voicePrice: voiceStr, vipPrompt: self.userWrap.prompt)
                videoCallManager.strikeModel(videoPrice: videoStr, voicePrice: voiceStr, vipPrompt: self.userWrap.prompt)
                //: } else {
            } else {
                //: showIntimateErrorStr()
                smirch()
            }

        //: case .ShortVideo:
        case .ShortVideo: // 短视频
            //: if self.conversationType == .private {
            if self.conversationType == .private {
                //: if self.userWrap.intimate >= PathAppManager.share.appUserConfigMode.intimateLimit.sendVideo {
                if self.userWrap.intimate >= PathAppManager.share.appUserConfigMode.intimateLimit.sendVideo {
                    //: func__sendShortVideo()
                    translateFile()
                    //: } else {
                } else {
                    //: showIntimateErrorStr()
                    smirch()
                }
                //: } else {
            } else {
                //: func__sendShortVideo()
                translateFile()
            }

        //: case .Gift: break
        case .Gift: break

        //: case .Game:
        case .Game: // 游戏
            //: gamesView.showView(from: .PrivateChat)
            gamesView.motionPictureCamera(from: .PrivateChat)
        }
    }

    //: private func showIntimateErrorStr() {
    private func smirch() {
        //: self.func__showStatusBarErrorMsg(showMsg: "Can't use the function due to your lack of intimacy level.".localized)
        self.colorGift(showMsg: String(bytes: show_statusDayMakeName.reversed(), encoding: .utf8)!.localized)
    }

    //: func func__sendChatRoomGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String, model: TalkingChatRoomMemberModel) {}
    func untilState(giftModel _: DiskModelType, giftNum _: String, model _: ManagerRateThen) {}

    // 聊天面板送礼
    /// - Parameters:
    ///   - giftModel: 礼物模型
    ///   - giftNum: 礼物数量
    //: func func__sendGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {
    func giftWith(giftModel: DiskModelType, giftNum: String) {
        //: func__sendGiftMsg(giftModel: giftModel, giftNum: giftNum, cellData: nil, fromStr: nil)
        roundOf(giftModel: giftModel, giftNum: giftNum, cellData: nil, fromStr: nil)
    }

    //: func func__sendGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String, cellData: IndexVideoReactiveCompatible?, fromStr: String?) {
    func roundOf(giftModel: DiskModelType, giftNum: String, cellData: IndexVideoReactiveCompatible?, fromStr: String?) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            colorGift(showMsg: user_netTitle)
            //: return
            return
        }
        //: var params: Dictionary<String, Any> = [:]
        var params: [String: Any] = [:]
        //: params["toUid"] = self.targetId
        params[(String(dreamWithMsg.suffix(5)))] = self.targetId
        //: params["giftId"] = giftModel.gid
        params[(String(dreamOfSucceedIdent.suffix(6)))] = giftModel.gid
        //: params["giftNum"] = giftNum
        params[(String(kRefuseTitle))] = giftNum
        //: if giftModel.pkgItemsetId != nil {
        if giftModel.pkgItemsetId != nil {
            //: params["pkgItemsetId"] = giftModel.pkgItemsetId
            params[(String(main_playerViewKey) + String(m_backgroundRecordUrl.prefix(6)))] = giftModel.pkgItemsetId
        }
        //: if fromStr != nil {
        if fromStr != nil {
            //: params["from"] = fromStr
            params[(m_userMsg.replacingOccurrences(of: "kit", with: "f"))] = fromStr
        }
        // 送礼接口
        //: TalkingChatRequestTool.req_SendGiftPrivete(param: params, completion: { succeed, result, errorModel in
        SourceRequestTool.holdUp(param: params, completion: { succeed, result, errorModel in
            //: guard succeed else {
            guard succeed else {
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickSendingiftsButton)
                self.coloringShow(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: noti_limitUrl)
                //: return
                return
            }

            // 更新本地礼物背包
            //: if giftModel.pkgItemsetId != nil {
            if giftModel.pkgItemsetId != nil {
                //: self.msgInputView.giftView.bags_removeGiftPackage(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
                self.msgInputView.giftView.fromShared(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
            }
            //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, isFromInputView: true)
            self.playerInfo(extral: result as! [String: Any?], isFromInputView: true)
            //: self.updateSession()
            self.alongRestore()
            /// 亲密度
            //: let extralInfo = result as! Dictionary<String, Any?>
            let extralInfo = result as! [String: Any?]
            //: let msgInfoDic: Dictionary<String, Any> = extralInfo["msgInfo"] as! Dictionary<String, Any>
            let msgInfoDic: [String: Any] = extralInfo[(String(app_sharedTitle.prefix(7)))] as! [String: Any]
            //: if msgInfoDic.keys.contains("totalIntimate") && self.conversationType == .private {
            if msgInfoDic.keys.contains((String(mainListPath) + String(appManagerReportClearPath))), self.conversationType == .private {
                //: let totalIntimacy = msgInfoDic["totalIntimate"] as? Int ?? 0
                let totalIntimacy = msgInfoDic[(String(mainListPath) + String(appManagerReportClearPath))] as? Int ?? 0
                //: self.intimateTool(currenIntimate: totalIntimacy)
                self.bean(currenIntimate: totalIntimacy)
            }
            /// 索要礼物送礼
            //: if cellData != nil {
            if cellData != nil {
                // 修改状态
                //: self.msgView.changeIntimateGiftMsg(cellData)
                self.msgView.gift(cellData)
                //: uploadRecord.uploadRecordEvent(eventID: ClickmessageDetailaskMsgsendBtn)
                kResolutionText.atJson(eventID: app_errBarData)
            }
            //: })
        })
    }

    /// 索要礼物
    //: func func__askforGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {
    func showApp(giftModel: DiskModelType, giftNum _: String) {
        //: uploadRecord.uploadRecordEvent(eventID: ClickMessageDetailGiftPopAskBtnAllType)
        kResolutionText.atJson(eventID: show_userName)
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            colorGift(showMsg: user_netTitle)
            //: return
            return
        }
        //: guard self.userWrap.intimate >= PathAppManager.share.appUserConfigMode.intimateLimit.wantGift else {
        guard self.userWrap.intimate >= PathAppManager.share.appUserConfigMode.intimateLimit.wantGift else {
            //: showIntimateErrorStr()
            smirch()
            //: return
            return
        }
        //: var params: Dictionary<String, Any> = [:]
        var params: [String: Any] = [:]
        //: params["wantGiftId"] = giftModel.gid
        params[(kCountKey.replacingOccurrences(of: "model", with: "wa") + "GiftId")] = giftModel.gid
        //: params["type"] = "WantGiftMsg"
        params[(appListPath.replacingOccurrences(of: "share", with: "e"))] = (String(kToThenName.suffix(5)) + "iftMsg")

        //: let msgInfo = ["msgType": "WantGift"]
        let msgInfo = [String(bytes: dream_valuePath.map{$0^91}, encoding: .utf8)!: (String(m_hiddenMessage))]
        //: let dic = ["msgInfo": msgInfo, "params": params]
        let dic = [(String(app_sharedTitle.prefix(7))): msgInfo, String(bytes: app_backValueName.map{layerTitle(live: $0)}, encoding: .utf8)!: params]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = PrimrosePathThen.weightAdd(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            signal(msgData: cellData, reSend: false)
        }
    }

    //: func heightToBottomChanged(heightToBottom: CGFloat) {
    func acceptBottom(heightToBottom: CGFloat) {
        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: contentView.snp.updateConstraints { make in
            contentView.snp.updateConstraints { make in
                //: make.top.equalTo(HalfViewTopMargin-heightToBottom)
                make.top.equalTo(m_statusIdent - heightToBottom)
            }
        }
        //: msgInputView.snp.updateConstraints { make in
        msgInputView.snp.updateConstraints { make in
            //: make.bottom.equalTo(self.view).offset(-heightToBottom)
            make.bottom.equalTo(self.view).offset(-heightToBottom)
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.active(true)
            self.msgView.active(true)
        }
    }

    //: func inputViewHeightChanged(height: CGFloat) {
    func barrelhouse(height: CGFloat) {
        //: msgInputView.snp.updateConstraints { make in
        msgInputView.snp.updateConstraints { make in
            //: make.height.equalTo(height)
            make.height.equalTo(height)
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.active(true)
            self.msgView.active(true)
        }
    }

    //: @objc override func func__TapGestureRecognizer(_ tap: UITapGestureRecognizer) {
    @objc override func bindAdd(_ tap: UITapGestureRecognizer) {
        //: super.func__TapGestureRecognizer(tap)
        super.bindAdd(tap)
        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: let location = tap.location(ofTouch: 0, in: self.contentView)
            let location = tap.location(ofTouch: 0, in: self.contentView)
            //: if !contentView.point(inside: location, with: nil) {
            if !contentView.point(inside: location, with: nil) {
                // 点击列表以外区域，移除视图（包括聊天列表）
                //: half_dismissViewAnimation(true)
                makeWith(true)
            }
        }
    }

    /// 开始录音操作
    //: func func__starRecordBtnDown() {
    func addToQuit() {
        //: if TalkingVoiceMsgPlayManager.shared.playStatus == .Playing {
        if SharedObjectProtocol.shared.playStatus == .Playing {
            //: TalkingVoiceMsgPlayManager.shared.stopAudioPlayer()
            SharedObjectProtocol.shared.lessen()
        }
    }
}

// MARK: - 动效

//: extension TalkingPrivateChatController {
extension TotViewController {
    /// 插入礼物消息，播放动效
    /// - Parameters:
    ///   - extral: 送礼接口返回结果
    ///   - isFromInputView: 是否来自输入框送礼入口
    //: func func__insertGiftMessageWithExtral(extral: Dictionary<String, Any?>, isFromInputView: Bool) {
    func playerInfo(extral: [String: Any?], isFromInputView: Bool) {
        //: var extralInfo = extral
        var extralInfo = extral
        //: var msgInfoDic: Dictionary<String, Any> = extralInfo["msgInfo"] as! Dictionary<String, Any>
        var msgInfoDic: [String: Any] = extralInfo[(String(app_sharedTitle.prefix(7)))] as! [String: Any]
        //: msgInfoDic["msgType"] = "gift"
        msgInfoDic[String(bytes: dream_valuePath.map{$0^91}, encoding: .utf8)!] = (main_transactionData.replacingOccurrences(of: "value", with: "t"))
        //: extralInfo["msgInfo"] = msgInfoDic
        extralInfo[(String(app_sharedTitle.prefix(7)))] = msgInfoDic

        //: let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        let cellData = PrimrosePathThen.weightAdd(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        //: self.msgView.sendCustomMessage(cellData)
        self.msgView.send(cellData)
        //: var msgIncome: NSNumber = NSNumber.init(floatLiteral: 0)
        var msgIncome = NSNumber(floatLiteral: 0)
        //: if msgInfoDic.keys.contains("msgIncome") {
        if msgInfoDic.keys.contains((String(kFromKey.prefix(7)) + k_labelId.lowercased())) {
            //: msgIncome = msgInfoDic["msgIncome"] as! NSNumber
            msgIncome = msgInfoDic[(String(kFromKey.prefix(7)) + k_labelId.lowercased())] as! NSNumber
        }
        //: self.msgView.changeCustomMsg(cellData, msgId: msgInfoDic["msgId"] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)
        self.msgView.outMessage(cellData, photoStatus: msgInfoDic[(String(appGroupValue))] as? String, length: msgIncome, incomeStatus: .Msg_Status_Succ)
        //: sendGiftByMeToEfffecct(giftDic: extral as [String: Any])
        graduatedTable(giftDic: extral as [String: Any])
        //: if extralInfo.keys.contains("totalMfCoin") {
        if extralInfo.keys.contains((String(kSuiteTrackMsg) + String(main_sizeName.suffix(4)))) {
            //: let totalMfCoin: NSNumber = extralInfo["totalMfCoin"] as! NSNumber
            let totalMfCoin: NSNumber = extralInfo[(String(kSuiteTrackMsg) + String(main_sizeName.suffix(4)))] as! NSNumber
            //: PathAppManager.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            PathAppManager.share.loginUserMode.mf_coin = totalMfCoin.stringValue
        }
        //: if isFromInputView {
        if isFromInputView {
            //: msgInputView.giftView.updateGiftInfo(needReload: false, mf_coin: PathAppManager.share.loginUserMode.mf_coin)
            msgInputView.giftView.pitchCancel(needReload: false, mf_coin: PathAppManager.share.loginUserMode.mf_coin)
        }
    }

    //: func initCachEffectData() {
    func textRequest() {
        //: giftEffectView.initCachAnimatData(tagetID: targetId)
        giftEffectView.auditoryImage(tagetID: targetId)
    }

    //: func sendGiftByMeToEfffecct(giftDic: Dictionary<String, Any>) {
    func graduatedTable(giftDic: [String: Any]) {
        //: giftEffectView.dealGiftAnimatData(dict: giftDic)
        giftEffectView.accumulationDict(dict: giftDic)
    }

    //: @objc func addGiftEffectModelArr(giftArr: Array<Any>) {
    @objc func outline(giftArr: [Any]) {
        //: giftEffectView.addGiftAnimatModelArr(modelArr: giftArr)
        giftEffectView.afterImageArr(modelArr: giftArr)
    }

    //: @objc func getChatVCTargetId() ->String {
    @objc func quantityoPrice() -> String {
        //: return targetId
        return targetId
    }
}

// MARK: - 音视频通话Event

//: extension TalkingPrivateChatController {
extension TotViewController {
    /// 检查权限后发起音频通话
    //: func checkAndCallVoice() {
    func startCheckVideo() {
        //: videoCallManager.checkAndCallVoice {[weak self] succeed in
        videoCallManager.rename { [weak self] succeed in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard succeed else { return }
            guard succeed else { return }
            //: self.updateSession()
            self.alongRestore()
        }
    }

    /// 检测权限后发起视频通话
    //: func checkAndCallVideo() {
    func markMake() {
        //: videoCallManager.checkAndCallVideo {[weak self] succeed in
        videoCallManager.infoPost { [weak self] succeed in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard succeed else { return }
            guard succeed else { return }
            //: self.updateSession()
            self.alongRestore()
        }
    }
}

// MARK: - Event

//: extension TalkingPrivateChatController {
extension TotViewController {
    //: @objc private func feedbackBtnClick() {
    @objc private func menuClick() {
        //: let vc = TalkingFeedbackVC.init()
        let vc = DataingViewController()
        //: getNavigationController()?.pushViewController(vc, animated: true)
        modelButton()?.pushViewController(vc, animated: true)
    }

    /// 更多按钮事件
    //: private func moreBtnClick() {
    private func fly() {
        //: let vc = TalkingMunuPopView.init(frame: self.view.frame)
        let vc = LabelPopView(frame: self.view.frame)
        //: vc.initwithList(cellTitleList: ["View info".localized, "Notes".localized, "Report".localized])
        vc.eldest(cellTitleList: [(String(userStatusTitle.prefix(9))).localized, (String(noti_willIdent)).localized, (String(show_awakePath.suffix(6))).localized])
        //: vc.cancelColor = .appTitleColor()
        vc.cancelColor = .statusBy()
        //: vc.munuBlock = { [weak self] index, str in
        vc.munuBlock = { [weak self] index, _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: if index == 0 {
            if index == 0 {
                //: NamePushManager.share.func__pushToUserDetailVC(uid: self.targetId)
                NamePushManager.share.pressAway(uid: self.targetId)

                //: } else if index == 1 {
            } else if index == 1 { // 备注
                //: jumpToNotesView()
                observeAdd()

                //: } else if index == 2 {
            } else if index == 2 { // 举报
                //: self.msgInputView.showOtherInputView = true
                self.msgInputView.showOtherInputView = true
                //: let reportView = TalkingReportAlertView.init(frame: UIScreen.main.bounds, type: .reportUserType, rId: self.targetId)
                let reportView = RateViewDelegate(frame: UIScreen.main.bounds, type: .reportUserType, rId: self.targetId)
                //: reportView.showReportViewIn(view: nil)
                reportView.toModel(view: nil)
                //: reportView.reportHideBlock = { _ in
                reportView.reportHideBlock = { _ in
                    //: self.msgInputView.showOtherInputView = false
                    self.msgInputView.showOtherInputView = false
                }
            }
        }
    }

    /// 跳转到备注页面
    //: private func jumpToNotesView() {
    private func observeAdd() {
        //: self.msgInputView.showOtherInputView = true
        self.msgInputView.showOtherInputView = true
        //: let view = PrivateChatNotesView()
        let view = PublishView()
        //: view.show(self.userWrap.content, isTop: self.userWrap.top, toUid: self.targetId)
        view.accountingData(self.userWrap.content, isTop: self.userWrap.top, toUid: self.targetId)
        //: view.dismissBlock = { content, isTop in
        view.dismissBlock = { content, isTop in
            //: self.msgInputView.showOtherInputView = false
            self.msgInputView.showOtherInputView = false
            //: self.userWrap.content = content
            self.userWrap.content = content
            //: self.userWrap.top = isTop
            self.userWrap.top = isTop
            //: self.notesTopView.refresh(self.userWrap)
            self.notesTopView.headRecord(self.userWrap)
        }
    }

    /// 返回按钮点击事件
    //: private func backButtonClick() {
    private func buttonData() {
        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: half_dismissViewAnimation(false)
            makeWith(false)
            //: } else {
        } else {
            //: getNavigationController()?.popViewController(animated: true)
            modelButton()?.popViewController(animated: true)
        }
    }

    /// 展示飘屏内容
    //: @objc private func showFloatScreen(notification: NSNotification) {
    @objc private func endList(notification: NSNotification) {
        //: if conversationType == .private {
        if conversationType == .private {
            //: let dataInfo = notification.userInfo as! [String: AnyObject]
            let dataInfo = notification.userInfo as! [String: AnyObject]
            //: if let model = TalkingGameFloatingScreenModel.deserialize(from: dataInfo as? Dictionary) {
            if let model = AppTransformable.deserialize(from: dataInfo as? Dictionary) {
                //: self.floatScreenView.isUserInteractionEnabled = true
                self.floatScreenView.isUserInteractionEnabled = true
                //: self.floatScreenView.didReceive(model)
                self.floatScreenView.turnABlindEyeReceive(model)
            }
        }
    }
}

// MARK: - VIP消息已读回执

//: extension TalkingPrivateChatController {
extension TotViewController {
    /// 添加通知
    //: private func addReadPeceiptNotification() {
    private func toSize() {
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(func__msgReadReceipt(notification:)),
                                               selector: #selector(pairReceive(notification:)),
                                               //: name: CHAT_MSG_READ_RECEIPT_NOTIFICATION,
                                               name: user_showContent,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(func__refreshMsgList),
                                               selector: #selector(dropBack),
                                               //: name: USER_UPDATE_LOUNGEPLUSINFO_NOTIFICATION,
                                               name: notiInputMsg,
                                               //: object: nil)
                                               object: nil)
    }

    /// 收到对端已读消息回执通知 -> 刷新列表
    //: @objc func func__msgReadReceipt(notification: NSNotification) {
    @objc func pairReceive(notification: NSNotification) {
        //: let obj = notification.object as! V2TIMMessageReceipt
        let obj = notification.object as! V2TIMMessageReceipt
        //: guard obj.userID == targetId else { return }
        guard obj.userID == targetId else { return }
        //: guard PathAppManager.share.loginUserMode.loungePlus == true else { return }
        guard PathAppManager.share.loginUserMode.loungePlus == true else { return }
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.msgView.tableView.reloadData()
            self.msgView.tableView.reloadData()
        }
    }

    /// 订阅成功 -> 更改订阅状态 -> 刷新列表
    //: @objc private func func__refreshMsgList() {
    @objc private func dropBack() {
        //: PathAppManager.share.loginUserMode.loungePlus = true
        PathAppManager.share.loginUserMode.loungePlus = true
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.msgView.tableView.reloadData()
            self.msgView.tableView.reloadData()
        }
    }

    /// 判断是否插入已读回执引导充值VIP提示消息
    //: func func__privateVIPReadReceipt_insertTipsMsg() {
    func objectTouch() {
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.4) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.4) {
            //: guard let lastCellData = self.msgView.uiMsgs.lastObject as? ButtonDataThen else { return }
            guard let lastCellData = self.msgView.uiMsgs.lastObject as? ButtonDataThen else { return }
            //: if TalkingPrivateChatManager.needInsertVIPReadReceiptMsg(cellData: lastCellData) {
            if LevelThen.read(cellData: lastCellData) {
                // 只做内存插入展示
                //: self.insertTipMsg_Nobalance(tipStr: "View read receipts?".localized, needInsertLocal: false)
                self.selectName(tipStr: (String(kImageOkIdent.prefix(6)) + "ead rece" + String(show_nameFailTitleValue.prefix(5))).localized, needInsertLocal: false)
            }
        }
    }
}

// MARK: - 选择图片

//: extension TalkingPrivateChatController: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
extension TotViewController: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
    //: private func func__choicePhotos() {
    private func statusTo() {
        //: TalkingPermissionTool.func__openAlbumServiceWithBlock(true) { (isOpen: Bool) in
        EqualReactiveCompatible.upBack(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let vc = TalkingImagePickTool.initImagePickerVc(maxCount: 1, allowPhoto: true, allowVideo: false)
                let vc = RestrictionThen.pass(maxCount: 1, allowPhoto: true, allowVideo: false)
                //: vc.modalPresentationStyle = .fullScreen
                vc.modalPresentationStyle = .fullScreen
                //: self.present(vc, animated: true)
                self.present(vc, animated: true)
                //: vc.didFinishPickingPhotosHandle = { [weak self] ( photos: Array<UIImage>?,
                vc.didFinishPickingPhotosHandle = { [weak self] (photos: [UIImage]?,
                                                                 //: assets: Array<Any>?,
                                                                 _: [Any]?,
                                                                 //: isSelectOriginalPhoto: Bool?)  in
                                                                 _: Bool?) in
                        //: guard let self = self else {return}
                        guard let self = self else { return }
                        //: if photos!.count > 0 {
                        if photos!.count > 0 {
                            //: self.showUsePhoto(img: photos![0])
                            self.put(img: photos![0])
                        }
                }
            }
        }
    }

    //: private func func__camaraPhotos() {
    private func constraintGift() {
        //: TalkingPermissionTool.func__openCaptureDeviceServiceWithBlock(true) { (isOpen: Bool) in
        EqualReactiveCompatible.rangeBlock(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let imagePicker = UIImagePickerController()
                let imagePicker = UIImagePickerController()
                //: imagePicker.sourceType = .camera
                imagePicker.sourceType = .camera
                //: imagePicker.delegate = self
                imagePicker.delegate = self
                //: self.present(imagePicker, animated: true, completion: {})
                self.present(imagePicker, animated: true, completion: {})
            }
        }
    }

    //: func imagePickerController(_ picker: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
    func imagePickerController(_: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
        //: self.dismiss(animated: true) {
        self.dismiss(animated: true) {
            //: if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
            if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
                //: self.showUsePhoto(img: photo)
                self.put(img: photo)
            }
        }
    }

    //: func imagePickerControllerDidCancel(_ picker: UIImagePickerController) {
    func imagePickerControllerDidCancel(_: UIImagePickerController) {
        //: self.dismiss(animated: true, completion: nil)
        self.dismiss(animated: true, completion: nil)
    }

    /// 展示阅后即焚+私密照片选项
    /// - Parameter img: 图片资源
    //: private func showUsePhoto(img: UIImage) {
    private func put(img: UIImage) {
        //: guard conversationType == .private &&
        guard conversationType == .private &&
            //: PathAppManager.share.appStatus == AppSkinStatus.normal.rawValue else {
            PathAppManager.share.appStatus == AssemblageHashable.normal.rawValue
        else { // 非私聊 && 非审核模式
            //: let sendModel = AbTalkingChatMsgCustomSendModel()
            let sendModel = TextLeadingModel()
            //: sendModel.isBurn = "0"
            sendModel.isBurn = "0"
            //: sendModel.giftId = "0"
            sendModel.giftId = "0"
            //: self.func__sendPictureMsg(img: img, sendModel: sendModel)
            self.imageBegin(img: img, sendModel: sendModel)
            //: return
            return
        }

        //: let vc = TalkingMomentPhotosVC(images: [img], index: 0, type: .sendIntimatePhoto)
        let vc = ServerViewDelegate(images: [img], index: 0, type: .sendIntimatePhoto)
        //: vc.sendIntimatePhotoBlock = {[weak self] isBurn, giftId in
        vc.sendIntimatePhotoBlock = { [weak self] isBurn, giftId in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: let sendModel = AbTalkingChatMsgCustomSendModel()
            let sendModel = TextLeadingModel()
            //: sendModel.isBurn = isBurn
            sendModel.isBurn = isBurn
            //: sendModel.giftId = giftId
            sendModel.giftId = giftId
            //: self.func__sendPictureMsg(img: img, sendModel: sendModel)
            self.imageBegin(img: img, sendModel: sendModel)
        }
        //: getNavigationController()?.pushViewController(vc, animated: true)
        modelButton()?.pushViewController(vc, animated: true)
    }
}

// MARK: - 选择视频

//: extension TalkingPrivateChatController {
extension TotViewController {
    /// 发送短视频
    //: private func func__sendShortVideo() {
    private func translateFile() {
        // 检测用户短视频版本是否可用
        //: guard TalkingPrivateChatManager.checkShortVideo(userVersion: self.userWrap.version) else {
        guard LevelThen.sizeColor(userVersion: self.userWrap.version) else {
            //: let gender = self.userWrap.sex == Gender.male.rawValue ? "He".localized:"She".localized
            let gender = self.userWrap.sex == ConnectionClusterLiteral.male.rawValue ? "He".localized : (String(dream_videoData.suffix(3))).localized
            //: self.func__showStatusBarErrorMsg(showMsg: "%@ is using too low a version".localizedArguments(gender))
            self.colorGift(showMsg: (String(k_tableGiftPath.suffix(4)) + "s usin" + String(main_viewMsg.suffix(8)) + String(m_addKey.suffix(4)) + appDateMessage.lowercased()).equalArguments(gender))
            //: return
            return
        }

        //: self.openVideoPicker()
        self.channel()
    }

    /// 打开视频相册
    //: private func openVideoPicker() {
    private func channel() {
        //: let vc = TalkingImagePickTool.initImagePickerVc(maxCount: 1, allowPhoto: false, allowVideo: true)
        let vc = RestrictionThen.pass(maxCount: 1, allowPhoto: false, allowVideo: true)
        //: vc.modalPresentationStyle = .fullScreen
        vc.modalPresentationStyle = .fullScreen
        //: self.present(vc, animated: true)
        self.present(vc, animated: true)
        //: vc.didFinishPickingVideoHandle = { [weak self] ( coverImage: UIImage?, assets: PHAsset?)  in
        vc.didFinishPickingVideoHandle = { [weak self] (_: UIImage?, assets: PHAsset?) in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: TalkingImagePickTool.getVideoPath(asset: assets) { filePath in
            RestrictionThen.sort(asset: assets) { filePath in
                //: guard filePath != nil else { return }
                guard filePath != nil else { return }
                //: let asset = AVURLAsset.init(url: filePath!)
                let asset = AVURLAsset(url: filePath!)
                //: let time = asset.duration
                let time = asset.duration
                //: let seconds = Int(roundf(Float(CMTimeGetSeconds(time))))
                let seconds = Int(roundf(Float(CMTimeGetSeconds(time))))
                //: if seconds >= 5 {
                if seconds >= 5 { // 压缩视频
                    //: self.CompressedVideo(url: filePath!)
                    self.compressed(url: filePath!)
                    //: } else {
                } else {
                    //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                    DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) { // 视频压缩取消
                        //: self.func__showStatusBarErrorMsg(showMsg: "Shoot video at least 5 seconds".localized)
                        self.colorGift(showMsg: (String(kPopPath) + showMomentName.replacingOccurrences(of: "equal", with: "c")).localized)
                    }
                }
            }
        }
    }

    /// 视频压缩
    /// - Parameter url: 本地路径
    //: private func CompressedVideo(url: URL) {
    private func compressed(url: URL) {
        //: let style: TalkingVideoEditorStyle = (conversationType == .private) ? .intimateVideo : .normal
        let style: AppEditorStyle = (conversationType == .private) ? .intimateVideo : .normal
        //: let videoVC = TalkingVideoEditorController(style: style, maxDuration: 60.0, videoURL: url) { videoInfo in
        let videoVC = ConnectEditorController(style: style, maxDuration: 60.0, videoURL: url) { videoInfo in
            //: guard let videoInfo = videoInfo else { return }
            guard let videoInfo = videoInfo else { return }
            // 压缩成功，实例化模型
            //: let sendModel = AbTalkingChatMsgCustomSendModel()
            let sendModel = TextLeadingModel()
            //: sendModel.giftId = videoInfo.giftId
            sendModel.giftId = videoInfo.giftId
            //: sendModel.isBurn = videoInfo.isBurn
            sendModel.isBurn = videoInfo.isBurn
            //: sendModel.localCoverImg = videoInfo.coverPath
            sendModel.localCoverImg = videoInfo.coverPath
            //: sendModel.localVideoUrl = videoInfo.videoPath
            sendModel.localVideoUrl = videoInfo.videoPath
            //: sendModel.duration = Int(roundf(Float(videoInfo.duration)))
            sendModel.duration = Int(roundf(Float(videoInfo.duration)))
            // 发送视频消息
            //: self.func__sendVideoMsg(msgData: nil, sendModel: sendModel)
            self.funcToModel(msgData: nil, sendModel: sendModel)
        }

        //: ProgressHUD.dismiss()
        ShirtButtonThen.allowButton()
        //: getNavigationController()?.pushViewController(videoVC, animated: true)
        modelButton()?.pushViewController(videoVC, animated: true)
    }

    /// 上传视频
    /// - Parameters:
    ///   - msgData: cellData，此处只数据传递
    ///   - sendModel: 消息自定义本地模型
    //: private func cos_UploadVideo(msgData: ButtonDataThen, sendModel: AbTalkingChatMsgCustomSendModel) {
    private func anonymUpHypothesis(msgData: ButtonDataThen, sendModel: TextLeadingModel) {
        //: TalkingMomentVideoManager.shared.cos_uploadVideo(type: 3, coverPath: sendModel.localCoverImg, videoPath: sendModel.localVideoUrl) { coverUrl, videoUrl, succeed in
        DirectionSignatureProvider.shared.when(type: 3, coverPath: sendModel.localCoverImg, videoPath: sendModel.localVideoUrl) { coverUrl, videoUrl, succeed in
            //: guard succeed else {
            guard succeed else {
                //: self.func__showStatusBarErrorMsg(showMsg: "Upload failed".localized)
                self.colorGift(showMsg: (String(app_sizeViewMsg.suffix(5)) + "d faile" + app_textScreenMessage.replacingOccurrences(of: "add", with: "d")).localized)
                // 更改消息状态为失败
                //: self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                self.msgView.outMessage(msgData, photoStatus: nil, length: NSNumber(value: 0), incomeStatus: .Msg_Status_Fail)
                //: return
                return
            }

            /// 上传成功，刷新本地视频消息，调用发消息接口
            //: sendModel.coverImg = coverUrl
            sendModel.coverImg = coverUrl
            //: sendModel.videoUrl = videoUrl
            sendModel.videoUrl = videoUrl
            //: self.func__sendVideoMsg(msgData: msgData, sendModel: sendModel)
            self.funcToModel(msgData: msgData, sendModel: sendModel)
        }
    }
}

// MARK: - 语音消息

//: extension TalkingPrivateChatController {
extension TotViewController {
    //: func func__addVoiceNotification() {
    func voiceOver() {
        //: NotificationCenter.default.addObserver(self, selector: #selector(appDidBecomeActive),
        NotificationCenter.default.addObserver(self, selector: #selector(compartmentTitle),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self, selector: #selector(appDidEnterBack),
        NotificationCenter.default.addObserver(self, selector: #selector(eventIn),
                                               //: name: UIApplication.didEnterBackgroundNotification,
                                               name: UIApplication.didEnterBackgroundNotification,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(audioSessionInterrupted(notification:)),
                                               selector: #selector(exist(notification:)),
                                               //: name: AVAudioSession.interruptionNotification,
                                               name: AVAudioSession.interruptionNotification,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self, selector: #selector(insertGiftMsgNotif(notification:)),
        NotificationCenter.default.addObserver(self, selector: #selector(beyondCorner(notification:)),
                                               //: name: CHAT_ADD_CALL_RECORD_MSG_NOTIFICATION,
                                               name: show_nameClickStr,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(newMsgIntimate(notification:)),
                                               selector: #selector(postUp(notification:)),
                                               //: name: CHAT_NEW_MSG_NOTIFICATION,
                                               name: app_toPath,
                                               //: object: nil)
                                               object: nil)
    }

    //: func func__addOrRrmoveObserverForMsgPlayer(isAdd: Bool) {
    func rrmove(isAdd: Bool) {
        //: if isAdd {
        if isAdd {
            //: TalkingVoiceMsgPlayManager.shared.addDaskManagerDelegate()
            SharedObjectProtocol.shared.matchDelegate()
            //: } else {
        } else {
            //: TalkingVoiceMsgPlayManager.shared.removeDaskManagerDelegate()
            SharedObjectProtocol.shared.blockDelegate()
        }
    }

    //: @objc func appDidBecomeActive() {
    @objc func compartmentTitle() {
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: true)
        rrmove(isAdd: true)
    }

    //: @objc func appDidEnterBack() {
    @objc func eventIn() {
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: false)
        rrmove(isAdd: false)
    }

    //: @objc func audioSessionInterrupted(notification: NSNotification) {
    @objc func exist(notification: NSNotification) {
        //: guard let userInfo = notification.userInfo, let reasonValue = userInfo[AVAudioSessionInterruptionTypeKey] as? UInt else { return }
        guard let userInfo = notification.userInfo, let reasonValue = userInfo[AVAudioSessionInterruptionTypeKey] as? UInt else { return }
        //: switch reasonValue {
        switch reasonValue {
        //: case AVAudioSession.InterruptionType.began.rawValue:
        case AVAudioSession.InterruptionType.began.rawValue: // Began
            //: func__addOrRrmoveObserverForMsgPlayer(isAdd: false)
            rrmove(isAdd: false)
        //: break
        //: case AVAudioSession.InterruptionType.ended.rawValue:
        case AVAudioSession.InterruptionType.ended.rawValue: // End
            //: func__addOrRrmoveObserverForMsgPlayer(isAdd: true)
            rrmove(isAdd: true)
        //: break
        //: default:
        default:
            //: break
            break
        }
    }

    //: func func__DidTouchVoiceCellNeedPlayWithCurrentModel(cellData: CollectionCellData, cellView: TalkingChatAudioMsgCell) {
    func application(cellData: CollectionCellData, cellView: ModelServerThen) {
        //: guard TalkingSocketManager.shared.isData == false else {
        guard PublishDataSocketDelegate.shared.isData == false else {
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.colorGift(showMsg: app_successShowMsg)
            //: return
            return
        }

        //: let message = cellData.innerMessage
        let message = cellData.innerMessage

        //: for indexModel in self.msgView.uiMsgs {
        for indexModel in self.msgView.uiMsgs {
            //: if (indexModel as? ButtonDataThen)!.isKind(of: CollectionCellData.self) == false {
            if (indexModel as? ButtonDataThen)!.isKind(of: CollectionCellData.self) == false {
                //: continue
                continue
            }
            //: let audioModel = indexModel as! CollectionCellData
            let audioModel = indexModel as! CollectionCellData
            //: audioModel.voiceModel.isPlayingStatus = 2
            audioModel.voiceModel.isPlayingStatus = 2
            //: audioModel.voiceModel.activityShowStatus = 2
            audioModel.voiceModel.activityShowStatus = 2
            // 红点提示隐藏
            //: if (audioModel.isEqual(cellData)) {
            if audioModel.isEqual(cellData) {
                //: cellView.voiceTrackPoint.isHidden = true
                cellView.voiceTrackPoint.isHidden = true
            }
        }
        //: if message != nil {
        if message != nil {
            //: cellData.voiceModel.db_voiceCacheWrap = DBUserInfoManager.cache_getAudioDB(message: message!)
            cellData.voiceModel.db_voiceCacheWrap = ModeratenessReactiveCompatible.sourceLine(message: message!)
        }
        //: TalkingVoiceMsgPlayManager.shared.stopAudioPlayer()
        SharedObjectProtocol.shared.lessen()

        //: if TalkingVoiceMsgPlayManager.shared.clickCellId == cellData.voiceModel.db_voiceCacheWrap.msgId {
        if SharedObjectProtocol.shared.clickCellId == cellData.voiceModel.db_voiceCacheWrap.msgId {
            //: printLog(message: "播放状态:\(TalkingVoiceMsgPlayManager.shared.playStatus)")
            printLog(message: (noti_bindFormat) + "\(SharedObjectProtocol.shared.playStatus)")
            //: if TalkingVoiceMsgPlayManager.shared.playStatus != .Unknown {
            if SharedObjectProtocol.shared.playStatus != .Unknown {
                //: TalkingVoiceMsgPlayManager.shared.playStatus = .Unknown
                SharedObjectProtocol.shared.playStatus = .Unknown
                //: return
                return
            }
        }
        // 指定播放的cell
        //: TalkingVoiceMsgPlayManager.shared.clickCellId = cellData.voiceModel.db_voiceCacheWrap.msgId
        SharedObjectProtocol.shared.clickCellId = cellData.voiceModel.db_voiceCacheWrap.msgId
        //: TalkingVoiceMsgPlayManager.shared.playStatus = .Waiting
        SharedObjectProtocol.shared.playStatus = .Waiting
        //: var waitArray = [cellData.voiceModel]
        var waitArray = [cellData.voiceModel]
        //: if cellData.voiceModel.db_voiceCacheWrap.db_isRead == "0" {
        if cellData.voiceModel.db_voiceCacheWrap.db_isRead == "0" {
            //: let currentIndex = self.msgView.uiMsgs.index(of: cellData)
            let currentIndex = self.msgView.uiMsgs.index(of: cellData)
            //: for index in currentIndex+1 ..< self.msgView.uiMsgs.count {
            for index in currentIndex + 1 ..< self.msgView.uiMsgs.count {
                //: let subModel: ButtonDataThen = self.msgView.uiMsgs.object(at: index) as! ButtonDataThen
                let subModel: ButtonDataThen = self.msgView.uiMsgs.object(at: index) as! ButtonDataThen
                //: let subMessage = subModel.innerMessage
                let subMessage = subModel.innerMessage
                //: if subModel.isKind(of: CollectionCellData.self) {
                if subModel.isKind(of: CollectionCellData.self) {
                    //: let audioModel = subModel as! CollectionCellData
                    let audioModel = subModel as! CollectionCellData
                    //: let voiceModel = audioModel.voiceModel
                    let voiceModel = audioModel.voiceModel
                    //: if subMessage != nil {
                    if subMessage != nil {
                        //: voiceModel.db_voiceCacheWrap = DBUserInfoManager.cache_getAudioDB(message: subMessage!)
                        voiceModel.db_voiceCacheWrap = ModeratenessReactiveCompatible.sourceLine(message: subMessage!)
                    }
                    //: if (voiceModel.db_voiceCacheWrap.db_isRead == "0") {
                    if voiceModel.db_voiceCacheWrap.db_isRead == "0" {
                        //: waitArray.append(voiceModel)
                        waitArray.append(voiceModel)
                    }
                    //: cellData.voiceModel.db_voiceCacheWrap.db_isRead = "1"
                    cellData.voiceModel.db_voiceCacheWrap.db_isRead = "1"
                    //: cellView.voiceTrackPoint.isHidden = true
                    cellView.voiceTrackPoint.isHidden = true
                    //: WCDBVoiceMsgTable.db_updateVoiceMsg(cellData.voiceModel.db_voiceCacheWrap)
                    CommentMsgTable.voiceAdd(cellData.voiceModel.db_voiceCacheWrap)
                }
            }
        }
        //: TalkingVoiceMsgPlayManager.shared.starPlayMsg(msgArr: waitArray)
        SharedObjectProtocol.shared.blackRow(msgArr: waitArray)
    }
}

// MARK: - 用户相关

//: extension TalkingPrivateChatController {
extension TotViewController {
    /// 请求聊天用户信息 V1.8.0 提前到进入页面之前，先判断是否封号
    //: func func__requestChatInfo() {
    func upInfo() {
        //: guard conversationType == .private || conversationType == .service else { return }
        guard conversationType == .private || conversationType == .service else { return }

        //: self.func__updateUIWithUserWrap()
        self.compartmentMoveWrap()
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        // 判断是否需要插入一键打招呼消息
        //: if (self.quickGreetingTextDict?.count ?? 0 > 0) || (self.quickGreetingAudioDict?.count ?? 0 > 0) {
        if (self.quickGreetingTextDict?.count ?? 0 > 0) || (self.quickGreetingAudioDict?.count ?? 0 > 0) {
            //: self.func_sendQuickGreetingMsg(textDict: self.quickGreetingTextDict, audioDict: self.quickGreetingAudioDict)
            self.fastBottom(textDict: self.quickGreetingTextDict, audioDict: self.quickGreetingAudioDict)
        }
        //: if self.msgView.uiMsgs.count > 0 && self.userWrap.existSess == false {
        if self.msgView.uiMsgs.count > 0, self.userWrap.existSess == false {
            //: for model in self.msgView.uiMsgs {
            for model in self.msgView.uiMsgs {
                //: let indexModel = model as? ButtonDataThen
                let indexModel = model as? ButtonDataThen
                //: if indexModel?.isKind(of: CollectionCellData.self) == true || indexModel?.isKind(of: ChatThen.self) == true || indexModel?.isKind(of: ButtonCellData.self) == true || indexModel?.isKind(of: DiskThen.self) == true {
                if indexModel?.isKind(of: CollectionCellData.self) == true || indexModel?.isKind(of: ChatThen.self) == true || indexModel?.isKind(of: ButtonCellData.self) == true || indexModel?.isKind(of: DiskThen.self) == true {
                    //: self.userWrap.existSess = true
                    self.userWrap.existSess = true
                    //: break
                    break
                }
            }
        }
        // 判断是否需要插入音视频提示消息
        //: self.func__privateChatVideo_insertTipMsg()
        self.setMsg()
        // 判断是否需要插入官方客服提示消息
        //: self.func__customerServiceCenter_insertTipMsg()
        self.headerThroughTip()
        /// 判断是否插入已读回执引导充值VIP提示消息
        //: self.func__privateVIPReadReceipt_insertTipsMsg()
        self.objectTouch()
    }

    /// 刷新UI
    //: func func__updateUIWithUserWrap() {
    func compartmentMoveWrap() {
        //: guard userWrap.uid.count > 0 else { return }
        guard userWrap.uid.count > 0 else { return }
        //: bgImgeView.setUrlImage(urlStr: userWrap.headPic)
        bgImgeView.glycerolizeNeed(urlStr: userWrap.headPic)
        //: navView.private_updateNavView(userModel: userWrap, isFrom: self.isFrom)
        navView.metadataName(userModel: userWrap, isFrom: self.isFrom)
        /// 亲密度头像
        //: self.showFirstIntimateView()
        self.pushDoingTo()

        //: if sendGreetBtn.isHidden == false {
        if sendGreetBtn.isHidden == false {
            //: sendGreetBtn.isHidden = userWrap.sex == PathAppManager.share.loginUserMode.sex
            sendGreetBtn.isHidden = userWrap.sex == PathAppManager.share.loginUserMode.sex
        }
        //: if beautyPhotosBtn.isHidden == false {
        if beautyPhotosBtn.isHidden == false {
            //: beautyPhotosBtn.isHidden = userWrap.sex == PathAppManager.share.loginUserMode.sex
            beautyPhotosBtn.isHidden = userWrap.sex == PathAppManager.share.loginUserMode.sex
        }
        // 备注置顶
        //: notesTopView.refresh(userWrap)
        notesTopView.headRecord(userWrap)
    }

    //: func func__getFillDraftText() {
    func draftEqual() {
        //: let userID = "c2c_"+targetId
        let userID = (String(dreamEqualLabKey)) + targetId
        //: V2TIMManager.sharedInstance().getConversation(userID) { [weak self] conv in
        V2TIMManager.sharedInstance().getConversation(userID) { [weak self] conv in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                //: let draftText = conv?.draftText ?? ""
                let draftText = conv?.draftText ?? ""
                //: if !draftText.isEmptyString {
                if !draftText.isEmptyString {
                    //: self.msgInputView.inputTextView.text = conv?.draftText
                    self.msgInputView.inputTextView.text = conv?.draftText
                    //: self.msgInputView.inputTextView.becomeFirstResponder()
                    self.msgInputView.inputTextView.becomeFirstResponder()
                    //: self.func_inputContentIntervalOneSecond()
                    self.beforeOf()
                }
            }
            //: } fail: { code, desc in
        } fail: { _, _ in
        }
    }

    //: func func__saveFillDraftText() {
    func videoRelieve() {
        //: let userID = "c2c_"+targetId
        let userID = (String(dreamEqualLabKey)) + targetId
        //: V2TIMManager.sharedInstance().setConversationDraft(userID, draftText: msgInputView.inputTextView.text!) {
        V2TIMManager.sharedInstance().setConversationDraft(userID, draftText: msgInputView.inputTextView.text!) {
            //: } fail: { code, desc in
        } fail: { _, _ in
        }
    }
}

// MARK: - 提示消息

//: extension TalkingPrivateChatController {
extension TotViewController {
    //: func func__TipsNotification() {
    func keyError() {
        //: NotificationCenter.default.addObserver(self, selector: #selector(insertTipsNotif(notification:)),
        NotificationCenter.default.addObserver(self, selector: #selector(cover(notification:)),
                                               //: name: CHAT_TIPS_TEXT_NOTIFICATION,
                                               name: userFileMsg,
                                               //: object: nil)
                                               object: nil)
    }

    //: @objc func insertTipsNotif(notification: NSNotification) -> Void {
    @objc func cover(notification: NSNotification) {
        //: let userinfo = notification.userInfo as! [String: AnyObject]
        let userinfo = notification.userInfo as! [String: AnyObject]
        //: let celldata: TalkingChatTipsMsgCellData  = userinfo["msg"] as! TalkingChatTipsMsgCellData
        let celldata: StyleReactiveCompatible = userinfo[(dreamWindowFormat.replacingOccurrences(of: "bottom", with: "m"))] as! StyleReactiveCompatible

        //: self.msgView.uiMsgs.add(celldata)
        self.msgView.uiMsgs.add(celldata)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.active(true)
        self.msgView.active(true)
    }

    /// 插入提示消息
    /// - Parameters:
    ///   - tipStr: 提示消息文本
    ///   - needInsertLocal: 是否需要插入本地数据库（默认：是）
    //: func insertTipMsg_Nobalance(tipStr: String, needInsertLocal: Bool = true) {
    func selectName(tipStr: String, needInsertLocal: Bool = true) {
        //: let msgInfo: [String: Any] = ["content": tipStr,
        let msgInfo: [String: Any] = [String(bytes: main_paraTitle.map{$0^236}, encoding: .utf8)!: tipStr,
                                      //: "msgType": "tips"]
                                      String(bytes: dream_valuePath.map{$0^91}, encoding: .utf8)!: (kManagerPath.replacingOccurrences(of: "start", with: "ps"))]
        //: var message: ButtonDataThen?
        var message: ButtonDataThen?
        //: if needInsertLocal == true {
        if needInsertLocal == true { // 需要插入本地
            //: message = TUISocialChatManager.insertTXMessage(withExtral: msgInfo, toUid: targetId, isSender: true, isTip: true)
            message = PrimrosePathThen.barWrite(withExtral: msgInfo, toUid: targetId, isSender: true, isTip: true)
            //: } else {
        } else {
            //: message = TUISocialChatManager.handleTXMessageSend(withExtral: msgInfo, isSender: true, isTip: true)
            message = PrimrosePathThen.weightAdd(withExtral: msgInfo, isSender: true, isTip: true)
        }

        //: guard let message = message else { return }
        guard let message = message else { return }
        //: self.msgView.uiMsgs.add(message)
        self.msgView.uiMsgs.add(message)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.active(true)
        self.msgView.active(true)
    }

    /// 官方客服插入系统提示消息
    //: func func__customerServiceCenter_insertTipMsg() {
    func headerThroughTip() {
        //: guard conversationType == .service else { return }
        guard conversationType == .service else { return }
        //: TalkingPrivateChatManager.chat_customerServiceCenter_insertTipMsg { tipsStr in
        LevelThen.anyViewWrite { tipsStr in
            //: guard tipsStr != nil else { return }
            guard tipsStr != nil else { return }
            // 客服提示消息，只做内存插入展示
            //: self.insertTipMsg_Nobalance(tipStr: tipsStr!, needInsertLocal: false)
            self.selectName(tipStr: tipsStr!, needInsertLocal: false)
        }
    }

    /// 判断是否需要插入免费提示消息
    //: func func__privateAttachSys_insertTipMsg(str: String) {
    func timeSendStr(str: String) {
        //: guard conversationType == .private else { return }
        guard conversationType == .private else { return }
        //: self.insertTipMsg_Nobalance(tipStr: str)
        self.selectName(tipStr: str)
    }

    /// 判断是否需要插入音视频通话提示消息
    //: func func__privateChatVideo_insertTipMsg() {
    func setMsg() {
        //: guard conversationType == .private else { return }
        guard conversationType == .private else { return }
        //: TalkingPrivateChatManager.chat_privateChatVideo_insertTipMsg(toUid: self.userWrap.uid, msgView: self.msgView) {
        LevelThen.resourceFinish(toUid: self.userWrap.uid, msgView: self.msgView) {
            // 插入音视频提示消息
            //: let str = self.userWrap.nickname
            let str = self.userWrap.nickname
            //: self.insertTipMsg_Nobalance(tipStr: "You can use the video chat feature to give %@ a call".localizedArguments(str))
            self.selectName(tipStr: String(bytes: kModelName.map{bindVideo(content: $0)}, encoding: .utf8)!.equalArguments(str))
        }
    }

    /// 通话礼物消息
    //: @objc func insertGiftMsgNotif(notification: NSNotification) {
    @objc func beyondCorner(notification: NSNotification) {
        //: let dict = notification.userInfo
        let dict = notification.userInfo
        //: let cellData: ButtonDataThen = dict?["cellData"] as! ButtonDataThen
        let cellData: ButtonDataThen = dict?[(String(userCountOrientId) + String(m_selectedPath))] as! ButtonDataThen
        //: self.msgView.uiMsgs.add(cellData)
        self.msgView.uiMsgs.add(cellData)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.active(true)
        self.msgView.active(true)
    }
}

// MARK: - 回复文案

//: extension TalkingPrivateChatController: PrivateChatReplyTipsProtocol {
extension TotViewController: ToTipsProtocol {
    //: func func__setReplyTipsView() {
    func nowadaysView() {
        //: let key =  "\(PathAppManager.share.loginUserMode.userID)_PrivateChatTipsTitle_\(self.targetId)"
        let key = "\(PathAppManager.share.loginUserMode.userID)" + (String(m_userFormat.prefix(5)) + "ateCh" + String(mainTotalTitleId.suffix(6)) + "Title" + userViewMsg.replacingOccurrences(of: "equal", with: "_")) + "\(self.targetId)"
        //: let isShow: Bool = UserDefaults.standard.object(forKey: key) as? Bool ?? false
        let isShow: Bool = UserDefaults.standard.object(forKey: key) as? Bool ?? false
        //: if !isShow && PathAppManager.share.appStatus == AppSkinStatus.normal.rawValue {
        if !isShow && PathAppManager.share.appStatus == AssemblageHashable.normal.rawValue {
            //: view.addSubview(replyTipsView!)
            view.addSubview(replyTipsView!)
            //: replyTipsView?.snp.makeConstraints { make in
            replyTipsView?.snp.makeConstraints { make in
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.height.equalTo(50)
                make.height.equalTo(50)
            }
            //: msgView.view.snp.remakeConstraints { make in
            msgView.view.snp.remakeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.bottom.equalTo(replyTipsView!.snp.top)
                make.bottom.equalTo(replyTipsView!.snp.top)
            }
            //: self.replyTipsView?.setTipsStrView()
            self.replyTipsView?.pathAcross()
            // 修改banner位置
            //: changeBannerViewFream(isMiniView: self.bannerView.isMiniView)
            gildTheLily(isMiniView: self.bannerView.isMiniView)
            //: } else {
        } else {
            //: removeReplyTipsView()
            removeFromView()
        }
    }

    //: func seleteReplyTipsMessage(str: String) {
    func objectCollectionStr(str: String) {
        //: self.userWrap.existSess = true
        self.userWrap.existSess = true
        //: func__sendTextMsg(msgStr: str, quoteModel: nil)
        firstDismissView(msgStr: str, quoteModel: nil)
        //: removeReplyTipsView()
        removeFromView()
    }

    //: func removeReplyTipsView() {
    func removeFromView() {
        //: if conversationType == .private {
        if conversationType == .private {
            //: if self.replyTipsView != nil {
            if self.replyTipsView != nil {
                //: self.replyTipsView?.removeFromSuperview()
                self.replyTipsView?.removeFromSuperview()
                //: self.replyTipsView = nil
                self.replyTipsView = nil
            }
            //: msgView.view.snp.remakeConstraints { make in
            msgView.view.snp.remakeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
            }
            //: changeBannerViewFream(isMiniView: self.bannerView.isMiniView)
            gildTheLily(isMiniView: self.bannerView.isMiniView)
        }
    }
}

// MARK: - 半屏页面展示/隐藏动画

//: extension TalkingPrivateChatController {
extension TotViewController {
    /// 半屏展示动画
    //: private func half_showViewAnimation() {
    private func animation() {
        //: guard self.isFrom == .LiveRoom else { return }
        guard self.isFrom == .LiveRoom else { return }
        //: NotificationCenter.default.post(name: LIVE_HALF_VIEW_SHOW, object: nil, userInfo: nil)
        NotificationCenter.default.post(name: kNeedData, object: nil, userInfo: nil)
        //: self.view.frame = CGRect(x: 0, y: ScreenHeight-HalfViewTopMargin, width: ScreenWidth, height: ScreenHeight)
        self.view.frame = CGRect(x: 0, y: mainAcceptKey - m_statusIdent, width: app_screenFailMarginFormat, height: mainAcceptKey)
        //: UIView.animate(withDuration: 0.25) {
        UIView.animate(withDuration: 0.25) {
            //: self.view.frame = CGRect(x: 0,
            self.view.frame = CGRect(x: 0,
                                     //: y: 0,
                                     y: 0,
                                     //: width: ScreenWidth,
                                     width: app_screenFailMarginFormat,
                                     //: height: ScreenHeight)
                                     height: mainAcceptKey)
        }
    }

    /// 半屏页消失动画
    /// - Parameter removeChatList: 是否需要移除聊天列表
    //: private func half_dismissViewAnimation(_ removeChatList: Bool) {
    private func makeWith(_ removeChatList: Bool) {
        //: guard self.isFrom == .LiveRoom else { return }
        guard self.isFrom == .LiveRoom else { return }

        //: self.view.endEditing(true)
        self.view.endEditing(true)
        // 移除聊天列表
        //: if removeChatList == true, let currentVC = self.currentViewController() {
        if removeChatList == true, let currentVC = self.withTo() {
            //: if TalkingVoiceRoomManager.shared().isParty == true {
            if VoxNameReactiveCompatible.listOf().isParty == true {
                //: NotificationCenter.default.post(name: LIVE_HALF_VIEW_DISMISS, object: nil, userInfo: nil)
                NotificationCenter.default.post(name: noti_screenId, object: nil, userInfo: nil)
            }
            //: for vc in currentVC.children {
            for vc in currentVC.children {
                //: if vc.isKind(of: TalkingChatListViewController.self) {
                if vc.isKind(of: DirectionRecognizerDelegate.self) {
                    //: vc.removeFromParent()
                    vc.removeFromParent()
                    //: vc.view.removeFromSuperview()
                    vc.view.removeFromSuperview()
                    //: break
                    break
                }
            }
        }

        // 移除私聊页
        //: UIView.animate(withDuration: 0.25) {
        UIView.animate(withDuration: 0.25) {
            //: self.view.frame = CGRect(x: 0,
            self.view.frame = CGRect(x: 0,
                                     //: y: ScreenHeight-HalfViewTopMargin,
                                     y: mainAcceptKey - m_statusIdent,
                                     //: width: ScreenWidth,
                                     width: app_screenFailMarginFormat,
                                     //: height: ScreenHeight)
                                     height: mainAcceptKey)
            //: } completion: { finish in
        } completion: { _ in
            //: self.removeFromParent()
            self.removeFromParent()
            //: self.view.removeFromSuperview()
            self.view.removeFromSuperview()
            //: if TalkingVoiceRoomManager.shared().isParty == false {
            if VoxNameReactiveCompatible.listOf().isParty == false {
                //: NotificationCenter.default.post(name: LIVE_HALF_VIEW_DISMISS, object: nil, userInfo: nil)
                NotificationCenter.default.post(name: noti_screenId, object: nil, userInfo: nil)
            }
        }
    }
}

// MARK: - Layout

//: extension TalkingPrivateChatController {
extension TotViewController {
    /// 创建UI
    //: private func createUI() {
    private func productEqual() {
        //: view.backgroundColor = (self.isFrom == .LiveRoom) ? UIColor.clear:UIColor.white
        view.backgroundColor = (self.isFrom == .LiveRoom) ? UIColor.clear : UIColor.white
        //: view.addSubview(contentView)
        view.addSubview(contentView)

        //: if conversationType == .private {
        if conversationType == .private {
            //: contentView.addSubview(bgImgeView)
            contentView.addSubview(bgImgeView)
            //: contentView.addSubview(bgView)
            contentView.addSubview(bgView)
            //: contentView.addSubview(coverView)
            contentView.addSubview(coverView)
            //: self.addChild(msgView)
            self.addChild(msgView)
            //: contentView.addSubview(msgView.view)
            contentView.addSubview(msgView.view)
            //: contentView.addSubview(beautyPhotosBtn)
            contentView.addSubview(beautyPhotosBtn)
            //: contentView.addSubview(sendGreetBtn)
            contentView.addSubview(sendGreetBtn)
            //: if self.isFrom == .Normal {
            if self.isFrom == .Normal {
                //: contentView.addSubview(bannerView)
                contentView.addSubview(bannerView)
            }
            //: contentView.addSubview(floatScreenView)
            contentView.addSubview(floatScreenView)
            //: contentView.addSubview(giftEffectView)
            contentView.addSubview(giftEffectView)
            //: contentView.addSubview(navView)
            contentView.addSubview(navView)
            //: contentView.addSubview(notesTopView)
            contentView.addSubview(notesTopView)
            //: view.addSubview(msgInputView)
            view.addSubview(msgInputView)
            //: view.addSubview(selectTranView)
            view.addSubview(selectTranView)
            //: view.addSubview(transResultView)
            view.addSubview(transResultView)

            //: } else if conversationType == .service {
        } else if conversationType == .service {
            //: self.addChild(msgView)
            self.addChild(msgView)
            //: contentView.addSubview(msgView.view)
            contentView.addSubview(msgView.view)
            //: contentView.addSubview(navView)
            contentView.addSubview(navView)
            //: view.addSubview(msgInputView)
            view.addSubview(msgInputView)
            //: view.addSubview(selectTranView)
            view.addSubview(selectTranView)
            //: view.addSubview(transResultView)
            view.addSubview(transResultView)

            //: } else {
        } else {
            //: title = "System notification".localized
            title = (String(show_failValue.prefix(6)) + " noti" + notiShowPath.lowercased()).localized
            //: contentView.addSubview(feedView)
            contentView.addSubview(feedView)
            //: feedView.addSubview(feedbackBtn)
            feedView.addSubview(feedbackBtn)
            //: self.addChild(msgView)
            self.addChild(msgView)
            //: contentView.addSubview(msgView.view)
            contentView.addSubview(msgView.view)
        }
    }

    /// 约束
    //: private func createUIConstraints() {
    private func rank() {
        //: contentView.snp.makeConstraints { make in
        contentView.snp.makeConstraints { make in
            //: make.bottom.leading.trailing.equalToSuperview()
            make.bottom.leading.trailing.equalToSuperview()
            //: make.top.equalTo((self.isFrom == .LiveRoom) ? HalfViewTopMargin:0)
            make.top.equalTo((self.isFrom == .LiveRoom) ? m_statusIdent : 0)
        }

        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: contentView.layoutIfNeeded()
            contentView.layoutIfNeeded()
            //: contentView.Corner(width: contentView.width,
            contentView.subEqual(width: contentView.width,
                                 //: height: contentView.height,
                                 height: contentView.height,
                                 //: corners: [.topLeft, .topRight],
                                 corners: [.topLeft, .topRight],
                                 //: cornerRadii: .init(width: 8, height: 8))
                                 cornerRadii: .init(width: 8, height: 8))
        }

        //: if conversationType == .private {
        if conversationType == .private {
            //: bgView.snp.makeConstraints { make in
            bgView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: bgImgeView.snp.makeConstraints { make in
            bgImgeView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: coverView.snp.makeConstraints { make in
            coverView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: navView.snp.makeConstraints { make in
            navView.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalToSuperview()
                make.leading.trailing.top.equalToSuperview()
                //: make.height.equalTo((self.isFrom == .LiveRoom) ? NavigationBarHeight:StatusBarNavigationBarHeight)
                make.height.equalTo((self.isFrom == .LiveRoom) ? main_clickMessage : dream_systemTitle)
            }
            //: notesTopView.snp.makeConstraints { make in
            notesTopView.snp.makeConstraints { make in
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.leading.equalTo(10)
                make.leading.equalTo(10)
                //: make.trailing.equalTo(-10)
                make.trailing.equalTo(-10)
                //: make.height.equalTo(36)
                make.height.equalTo(36)
            }
            //: msgInputView.snp.makeConstraints { make in
            msgInputView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalTo(view)
                make.leading.trailing.bottom.equalTo(view)
                //: make.height.equalTo(msgInputView.BackViewHeight)
                make.height.equalTo(msgInputView.BackViewHeight)
            }
            //: msgView.view.snp.makeConstraints { make in
            msgView.view.snp.makeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: if beautyPhotosBtn.isHidden {
                if beautyPhotosBtn.isHidden {
                    //: make.bottom.equalTo(msgInputView.snp.top)
                    make.bottom.equalTo(msgInputView.snp.top)
                    //: } else {
                } else {
                    //: make.bottom.equalTo(beautyPhotosBtn.snp.top).offset(-5)
                    make.bottom.equalTo(beautyPhotosBtn.snp.top).offset(-5)
                }
            }
            //: giftEffectView.snp.makeConstraints { make in
            giftEffectView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: sendGreetBtn.snp.makeConstraints { make in
            sendGreetBtn.snp.makeConstraints { make in
                //: make.bottom.equalTo(beautyPhotosBtn)
                make.bottom.equalTo(beautyPhotosBtn)
                //: make.leading.equalTo(10)
                make.leading.equalTo(10)
                //: make.size.equalTo(beautyPhotosBtn)
                make.size.equalTo(beautyPhotosBtn)
            }
            //: beautyPhotosBtn.snp.makeConstraints { make in
            beautyPhotosBtn.snp.makeConstraints { make in
                //: make.bottom.equalTo(msgInputView.snp.top).offset(-8)
                make.bottom.equalTo(msgInputView.snp.top).offset(-8)
                //: make.leading.equalTo(sendGreetBtn.snp.trailing).offset(9)
                make.leading.equalTo(sendGreetBtn.snp.trailing).offset(9)
                //: make.width.equalTo(134)
                make.width.equalTo(134)
                //: make.height.equalTo(30)
                make.height.equalTo(30)
            }
            //: selectTranView.snp.makeConstraints { make in
            selectTranView.snp.makeConstraints { make in
                //: make.leading.trailing.equalTo(msgInputView)
                make.leading.trailing.equalTo(msgInputView)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
                //: make.height.equalTo(27)
                make.height.equalTo(27)
            }
            //: transResultView.snp.makeConstraints { make in
            transResultView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalToSuperview()
                make.leading.trailing.bottom.equalToSuperview()
                //: make.height.equalTo(206+kDeviceSafeBottomHeight)
                make.height.equalTo(206 + dreamOkTitle)
            }
            //: floatScreenView.snp.makeConstraints { make in
            floatScreenView.snp.makeConstraints { make in
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.height.equalTo(40)
                make.height.equalTo(40)
            }
            //: if self.isFrom == .Normal {
            if self.isFrom == .Normal {
                //: bannerView.snp.makeConstraints { make in
                bannerView.snp.makeConstraints { make in
                    //: make.trailing.equalToSuperview()
                    make.trailing.equalToSuperview()
                    //: make.bottom.equalTo(beautyPhotosBtn.snp.top)
                    make.bottom.equalTo(beautyPhotosBtn.snp.top)
                    //: make.height.equalTo(106)
                    make.height.equalTo(106)
                    //: make.width.equalTo(93)
                    make.width.equalTo(93)
                }
            }

            //: } else if conversationType == .service {
        } else if conversationType == .service {
            //: navView.snp.makeConstraints { make in
            navView.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalToSuperview()
                make.leading.trailing.top.equalToSuperview()
                //: make.height.equalTo((self.isFrom == .LiveRoom) ? NavigationBarHeight:StatusBarNavigationBarHeight)
                make.height.equalTo((self.isFrom == .LiveRoom) ? main_clickMessage : dream_systemTitle)
            }
            //: msgInputView.snp.makeConstraints { make in
            msgInputView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalTo(view)
                make.leading.trailing.bottom.equalTo(view)
                //: make.height.equalTo(msgInputView.BackViewHeight)
                make.height.equalTo(msgInputView.BackViewHeight)
            }
            //: msgView.view.snp.makeConstraints { make in
            msgView.view.snp.makeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
            }
            //: selectTranView.snp.makeConstraints { make in
            selectTranView.snp.makeConstraints { make in
                //: make.leading.trailing.equalTo(msgInputView)
                make.leading.trailing.equalTo(msgInputView)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
                //: make.height.equalTo(27)
                make.height.equalTo(27)
            }
            //: transResultView.snp.makeConstraints { make in
            transResultView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalToSuperview()
                make.leading.trailing.bottom.equalToSuperview()
                //: make.height.equalTo(206+kDeviceSafeBottomHeight)
                make.height.equalTo(206 + dreamOkTitle)
            }

            //: } else {
        } else {
            //: feedView.snp.makeConstraints { make in
            feedView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalTo((view))
                make.leading.trailing.bottom.equalTo(view)
                //: make.height.equalTo(56+kDeviceSafeBottomHeight)
                make.height.equalTo(56 + dreamOkTitle)
            }
            //: feedbackBtn.snp.makeConstraints { make in
            feedbackBtn.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalTo(feedView)
                make.leading.trailing.top.equalTo(feedView)
                //: make.height.equalTo(56)
                make.height.equalTo(56)
            }
            //: msgView.view.snp.makeConstraints { make in
            msgView.view.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalTo((view))
                make.leading.trailing.top.equalTo(view)
                //: make.bottom.equalTo(feedView.snp.top)
                make.bottom.equalTo(feedView.snp.top)
            }
        }
    }

    /// 事件绑定
    //: func bindInteraction() {
    func createSub() {
        //: navView.backButton.rx.tap
        navView.backButton.rx.tap
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.backButtonClick()
                self.buttonData()
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
        //: navView.moreButton.rx.controlEvent(.touchUpInside)
        navView.moreButton.rx.controlEvent(.touchUpInside)
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.view.endEditing(true)
                self.view.endEditing(true)
                //: self.moreBtnClick()
                self.fly()
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
        //: navView.videoCallBtn.rx.controlEvent(.touchUpInside)
        navView.videoCallBtn.rx.controlEvent(.touchUpInside)
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.view.endEditing(true)
                self.view.endEditing(true)
                //: self.func__selectMoreFunAction(actionType: .VideoCall)
                self.pic(actionType: .VideoCall)
                //: uploadRecord.uploadRecordEvent(eventID: ClickChatVideoButton, toUid: self.userWrap.uid )
                kResolutionText.atJson(eventID: k_videoUrl, toUid: self.userWrap.uid)
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
    }
}
