
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let showMatchLicenseMsg:[UInt8] = [0x64,0x65,0x74,0x6e,0x65,0x6d,0x65,0x6c,0x70,0x6d,0x69,0x20,0x6e,0x65,0x65,0x62,0x20,0x74,0x6f,0x6e,0x20,0x73,0x61,0x68,0x20,0x29,0x3a,0x72,0x65,0x64,0x6f,0x63,0x28,0x74,0x69,0x6e,0x69]

/*: "TalkingGroupChatTextMsgCellID" :*/
fileprivate let appImageUrl:String = "Talkmutual top"
fileprivate let showItemMakeIdent:[Character] = ["o","u","p","C"]
fileprivate let appTabDomainKey:[Character] = ["h","a","t","T","e","x","t","M","s","g","C","e","l","l","I","D"]

/*: "TalkingChatImageMsgCellID" :*/
fileprivate let mEnableFileTitle:String = "Talkicell scale deadline"
fileprivate let noti_topText:String = "icon manager open sizetImag"
fileprivate let appContextData:String = "result input colorllID"

/*: "WithGroupThen" :*/
fileprivate let userSharedKey:[Character] = ["T","a","l","k","i","n","g","G","r","o","u","p","C","h","a","t","G"]
fileprivate let dream_rankKey:String = "live"
fileprivate let showEqualValue:[Character] = ["f","t","M","s","g","C","e","l","l"]

/*: "TalkingChatTipsMsgCellID" :*/
fileprivate let mainBarId:[Character] = ["T","a","l","k","i","n","g","C","h","a","t","T","i"]
fileprivate let kAnnouncementPath:String = "psMsgClet format"
fileprivate let notiShareMsg:[Character] = ["e","l","l","I","D"]

/*: "TSystemMessageCell_ReuseId" :*/
fileprivate let showMakeIconUrl:String = "TSysteequal for"
fileprivate let app_loadValue:String = "self contentageC"
fileprivate let noti_modelMsg:[Character] = ["e"]
fileprivate let mainDimensionName:String = "component name addll_Reu"

/*: "roomId" :*/
fileprivate let app_sizePriceMessage:String = "time video printroomId"

/*: "type" :*/
fileprivate let kMakeId:[UInt8] = [0xf3,0xfe,0xf7,0xe2]

private func sizeAdd(model num: UInt8) -> UInt8 {
    return num ^ 135
}

/*: "extra" :*/
fileprivate let dream_makeIdent:[Character] = ["e","x","t","r","a"]

/*: "jumpKey" :*/
fileprivate let m_streamIdent:[UInt8] = [0x1,0x1e,0x6,0x1b,0x20,0xe,0x12]

private func alongImage(count num: UInt8) -> UInt8 {
    return num ^ 107
}

/*: "Send Gift" :*/
fileprivate let dreamBackgroundIconName:String = "Send Gielse for new"
fileprivate let noti_nameMsg:[Character] = ["f","t"]

/*: "View info" :*/
fileprivate let appHiddenTopStr:[Character] = ["V","i","e","w"," ","i","n","f","o"]

/*: "Report" :*/
fileprivate let showValueName:[Character] = ["R","e","p","o","r"]
fileprivate let k_makeKeyId:String = "up"

/*: "uid" :*/
fileprivate let show_managerUrl:[UInt8] = [0x96,0x8a,0x85]

fileprivate func removeName(host num: UInt8) -> UInt8 {
    let value = Int(num) + 223
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "name" :*/
fileprivate let kRetIdent:[UInt8] = [0x1d,0x12,0x1e,0x16]

private func dataAnd(on num: UInt8) -> UInt8 {
    return num ^ 115
}

/*: "Can't send blank message" :*/
fileprivate let showStyleData:[Character] = ["C","a","n","\'","t"," ","s","e","n","d"," ","b","l","a","n","k"," ","m","e","s"]
fileprivate let noti_contentIdent:String = "SAGE"

/*: "user" :*/
fileprivate let kToId:[Character] = ["u","s","e","r"]

/*: "content" :*/
fileprivate let main_makeTouchId:[UInt8] = [0xd8,0xd4,0xd5,0xcf,0xde,0xd5,0xcf]

/*: "msgType" :*/
fileprivate let show_textData:[Character] = ["m","s","g","T","y"]
fileprivate let notiUpStyleMsg:String = "perror"

/*: "txt" :*/
fileprivate let mainCameraMessage:String = "conversationt"

/*: "isGroup" :*/
fileprivate let userViewContent:String = "isGrougift empty m"
fileprivate let show_imageInfoId:[Character] = ["p"]

/*: "jumps" :*/
fileprivate let userContentFormat:String = "jumsizes"

/*: "message" :*/
fileprivate let main_equalMessage:[UInt8] = [0x65,0x67,0x61,0x73,0x73,0x65,0x6d]

/*: "TxtMsg" :*/
fileprivate let dream_managerMessage:String = "TxtMsguser var let"

/*: "toUid" :*/
fileprivate let m_adKey:String = "toUidguard info"

/*: "msgInfo" :*/
fileprivate let noti_lengthMessage:String = "manager selfmsgInf"
fileprivate let appAspectMediumMsg:String = "action"

/*: "params" :*/
fileprivate let userRequestUrl:[UInt8] = [0x73,0x6d,0x61,0x72,0x61,0x70]

/*: "contentType" :*/
fileprivate let k_errorFormat:[UInt8] = [0xd2,0xde,0xdd,0xe3,0xd4,0xdd,0xe3,0xc3,0xe8,0xdf,0xd4]

fileprivate func layerData(click num: UInt8) -> UInt8 {
    let value = Int(num) - 111
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "ImgMsg" :*/
fileprivate let k_everyId:[Character] = ["I","m","g","M","s","g"]

/*: "img" :*/
fileprivate let app_noUrl:String = "imapp"

/*: "imageUri" :*/
fileprivate let noti_dataValue:[UInt8] = [0x19,0x1d,0x11,0x17,0x15,0x25,0x2,0x19]

private func sizeName(title num: UInt8) -> UInt8 {
    return num ^ 112
}

/*: "pic" :*/
fileprivate let user_hiddenUrl:String = "languagec"

/*: "msgId" :*/
fileprivate let userValueStr:String = "app endmsgId"

/*: "totalMfCoin" :*/
fileprivate let m_noTimeMsg:[Character] = ["t","o","t","a","l"]
fileprivate let showNextIdent:[Character] = ["M","f","C","o","i","n"]

/*: "giftId" :*/
fileprivate let appToText:String = "manager layer self source wrapgiftId"

/*: "giftNum" :*/
fileprivate let k_greetCenterStr:[Character] = ["g","i","f","t","N","u","m"]

/*: "pkgItemsetId" :*/
fileprivate let appPathSucceedName:[Character] = ["p","k","g","I","t","e","m","s","e"]
fileprivate let mStatusPath:String = "value view awake video falsetId"

/*: "msg" :*/
fileprivate let userLikeFormat:String = "mmodel"

/*: "tips" :*/
fileprivate let m_byText:String = "tbeautys"

/*: "gift" :*/
fileprivate let mActualName:[Character] = ["g","i","f","t"]

/*: "nickname" :*/
fileprivate let dream_showData:[Character] = ["n","i","c","k","n","a","m","e"]

/*: "headPic" :*/
fileprivate let k_selectedIdent:[UInt8] = [0x31,0x3c,0x38,0x3d,0x9,0x30,0x3a]

private func failStatus(request num: UInt8) -> UInt8 {
    return num ^ 89
}

/*: "All Numbers" :*/
fileprivate let appClearIdent:String = "All Numedium manager view else in"

/*: "sendNum" :*/
fileprivate let kPicUrl:String = "create post make self ifsendNum"

/*: "toUser" :*/
fileprivate let noti_acceptTitle:String = "toUselocation more data item pic"
fileprivate let app_headFormat:String = "row"

/*: "msgIncome" :*/
fileprivate let showNetFormat:[Character] = ["m","s"]
fileprivate let show_picValue:String = "gIncomedata touch view"

/*: "isConnection" :*/
fileprivate let k_toValueFormat:[Character] = ["i","s","C","o","n","n","e","c","t","i","o","n"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  ProvenanceViewDelegate.swift
//  AbroadTalking
//
//  Created by young on 2023/4/4.
//

//: import UIKit
import UIKit

//: class TalkingGroupChatController: TalkingBaseViewController {
class ProvenanceViewDelegate: ListRecognizerDelegate {
	var bubbleOff: Bool = true
	var playCount: Double = 33.6
	var positionOpen: Bool = true
	var modifyQuantity: Double = 15.2

    //: var infoModel = TalkingChatRoomInfoModel()
    var infoModel = TopLavStyleInfoSimulationHandyJSON() // 群聊信息
    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()
    //: private var tipsTimer: Timer?
    private var tipsTimer: Timer? // 提示消息定时器
    //: private var conversationData = TalkingConversationModel()
    private var conversationData = ComputerSimulationReactiveCompatible() // 会话cellData
    //: private var conversationType: DataIMChatType = .group
    private var conversationType: DataIMChatType = .group // 会话类型
    //: private var atFlag = false
    private var atFlag = false // 记录是否@

    //: init(groupType: String, infoData: [String: Any]) {
    init(groupType: String, infoData: [String: Any]) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        //: hideNavi = true
        hideNavi = true
        //: if let model = TalkingChatRoomInfoModel.deserialize(from: infoData) {
        if let model = TopLavStyleInfoSimulationHandyJSON.deserialize(from: infoData) {
            //: infoModel = model
            infoModel = model
        }
        //: conversationData.groupID = infoModel.roomId
        conversationData.groupID = infoModel.roomId
        //: conversationData.groupType = groupType
        conversationData.groupType = groupType
        //: if groupType == GroupType_Meeting {
        if groupType == GroupType_Meeting {
            //: conversationType = .meeting
            conversationType = .meeting
        }
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: showMatchLicenseMsg.reversed(), encoding: .utf8)!)
    }

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if PathAppManager.share.appConfigMode.disableShootScreen {
            if PathAppManager.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = CustomPathThen(frame: CGRect(x: 0, y: 0, width: app_screenFailMarginFormat, height: mainAcceptKey))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: createUI()
        makeUi()
        //: createUIConstraints()
        suite()
        //: bindInteraction()
        distanceHighVelocityGiftTotalerplay()
        //: addTapGestureRecognizer()
        occupy()
        //: func__chatRoom_insertTipMsg()
        implant()
        //: chatSettings()
        arrangement()

        //: if conversationType == .private {
        if conversationType == .private {
            //: initCachEffectData()
            handleSize()
        }
    
            if (giftTrackView.layer.contentsRect.size.height != 1) && (giftTrackView.layer.anchorPointZ != 0) {
                //: SWIFT_CUSTOM_DANGER_File_Call
                let listParty = UserView(frame: giftTrackView.bounds)
            listParty.presentBlockInterval = msgInputView.inputTextView.placeholderLeftOffset
            
            listParty.styleClose = { [self] labelDoing in
            self.positionOpen = labelDoing
            
            var listParty = infoModel.isOwner
                listParty = false
                listParty = false
            if listParty != self.positionOpen {
                self.positionOpen = listParty
            }
            
                self.positionOpen = false
                self.positionOpen = false
            return self.positionOpen
            }
            listParty.geneticMutationTotal = { [self] frameInterval in
            self.modifyQuantity = frameInterval
            
            var listParty = msgInputView.inputTextView.placeholderLeftOffset
                listParty -= 1
                if listParty <= 0 {
                    listParty = listParty + 1
                }
            if listParty < self.modifyQuantity {
                self.modifyQuantity = listParty
            }
            
                self.modifyQuantity += 1
                if self.modifyQuantity < 75 {
                    self.modifyQuantity = self.modifyQuantity - 1
                }
            return self.modifyQuantity
            }
                giftTrackView.addSubview(listParty)
            }

	}

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: if self.atFlag {
        if self.atFlag {
            //: self.msgInputView.inputTextView.becomeFirstResponder()
            self.msgInputView.inputTextView.becomeFirstResponder()
            //: self.atFlag = false
            self.atFlag = false
        }
    }

    //: override func viewWillDisappear(_ animated: Bool) {
    override func viewWillDisappear(_ animated: Bool) {
        //: super.viewWillDisappear(animated)
        super.viewWillDisappear(animated)
        //: view.endEditing(true)
        view.endEditing(true)
    
            if (!msgInputView.bottomView.autoresizesSubviews) && (msgInputView.bottomView.forLastBaselineLayout.center.y == 28.13) {
                //: SWIFT_CUSTOM_DANGER_File_Call
                let simultaneouslyViewLet = UserView()
            simultaneouslyViewLet.presentBlockInterval = msgInputView.inputTextView.placeholderLeftOffset
            
            simultaneouslyViewLet.styleClose = { [self] labelDoing in
            self.bubbleOff = labelDoing
            
            var simultaneouslyViewLet = animated
                simultaneouslyViewLet = false
                simultaneouslyViewLet = !simultaneouslyViewLet
            if simultaneouslyViewLet != self.bubbleOff {
                self.bubbleOff = simultaneouslyViewLet
            }
            
                self.bubbleOff = true
                self.bubbleOff = false
            return self.bubbleOff
            }
            simultaneouslyViewLet.geneticMutationTotal = { [self] frameInterval in
            self.playCount = frameInterval
            
            var simultaneouslyViewLet = msgInputView.giftView.contentHeight
                simultaneouslyViewLet += 1
                if simultaneouslyViewLet >= 0 {
                    simultaneouslyViewLet = simultaneouslyViewLet - 1
                }
            if simultaneouslyViewLet < self.playCount {
                self.playCount = simultaneouslyViewLet
            }
            
                self.playCount -= 1
                if self.playCount < 77 {
                    self.playCount = self.playCount + 1
                }
            return self.playCount
            }
                msgInputView.bottomView.addSubview(simultaneouslyViewLet)
            }

	}

    //: deinit {
    deinit {
        //: if self.conversationData.groupType == GroupType_Meeting {
        if self.conversationData.groupType == GroupType_Meeting {
            //: V2TIMManager.sharedInstance().quitGroup(self.conversationData.groupID, succ: nil, fail: nil)
            V2TIMManager.sharedInstance().quitGroup(self.conversationData.groupID, succ: nil, fail: nil)
        }
        //: self.func__joinOrQuitRoom(join: false)
        self.coloured(join: false)
        //: self.destroyTipsTimer()
        self.lag()
    }

    // MARK: - Lazy Load

    //: private lazy var msgView: TUIMessageController = {
    private lazy var msgView: MainViewController = {
        //: let msgVC = TUIMessageController.init()
        let msgVC = MainViewController()
        //: msgVC.delegate = self
        msgVC.delegate = self
        //: msgVC.view.backgroundColor = UIColor.clear
        msgVC.view.backgroundColor = UIColor.clear
        //: msgVC.tableView.backgroundColor = .clear
        msgVC.tableView.backgroundColor = .clear
        //: msgVC.tableView.register(TalkingGroupChatTextMsgCell.self, forCellReuseIdentifier: "TalkingGroupChatTextMsgCellID")
        msgVC.tableView.register(GrantMsgCell.self, forCellReuseIdentifier: "GrantMsgCell")
        //: msgVC.tableView.register(TalkingChatImageMsgCell.self, forCellReuseIdentifier: "TalkingChatImageMsgCellID")
        msgVC.tableView.register(DirectionResumeView.self, forCellReuseIdentifier: "DirectionResumeView")
        //: msgVC.tableView.register(TalkingGroupChatGiftMsgCell.self, forCellReuseIdentifier: "TalkingGroupChatGiftMsgCell")
        msgVC.tableView.register(WithGroupThen.self, forCellReuseIdentifier: "WithGroupThen")
        //: msgVC.tableView.register(TalkingChatTipsMsgCell.self, forCellReuseIdentifier: "TalkingChatTipsMsgCellID")
        msgVC.tableView.register(SharedMsgCell.self, forCellReuseIdentifier: "SharedMsgCell")
        //: msgVC.tableView.register(TUISystemMessageCell.self, forCellReuseIdentifier: "TSystemMessageCell_ReuseId")
        msgVC.tableView.register(LevelViewCell.self, forCellReuseIdentifier: "TSystemMessageCell_ReuseId")
        //: msgVC.currentChatType = conversationType
        msgVC.currentChatType = conversationType
        //: return msgVC
        return msgVC
        //: }()
    }()

    //: private lazy var msgInputView: TalkingChatInputView = {
    private lazy var msgInputView: FailPlayerThen = {
        //: let view = TalkingChatInputView(type: .GroupChat)
        let view = FailPlayerThen(type: .GroupChat)
        //: view.chatRoomID = infoModel.roomId
        view.chatRoomID = infoModel.roomId
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    //: private lazy var giftEffectView: TalkingPrivateChatAnimatView = {
    private lazy var giftEffectView: RenderThen = {
        //: let effectView = TalkingPrivateChatAnimatView.init()
        let effectView = RenderThen()
        //: effectView.contentMode = .scaleAspectFill
        effectView.contentMode = .scaleAspectFill
        //: return effectView
        return effectView
        //: }()
    }()

    //: private lazy var giftTrackView: TalkingGiftTrackView = {
    private lazy var giftTrackView: CustomView = {
        //: let trackView = TalkingGiftTrackView.init()
        let trackView = CustomView()
        //: trackView.isUserInteractionEnabled = false
        trackView.isUserInteractionEnabled = false
        //: return trackView
        return trackView
        //: }()
    }()

    //: private lazy var navView: TalkingChatNavTitleView = {
    private lazy var navView: FinancialAidReactiveCompatible = {
        //: let navView = TalkingChatNavTitleView(type: conversationType)
        let navView = FinancialAidReactiveCompatible(type: conversationType)
        //: navView.titleLabel.text = infoModel.name
        navView.titleLabel.text = infoModel.name
        //: return navView
        return navView
        //: }()
    }()

    //: private lazy var someoneView: TalkingSomeoneAtMeView = {
    private lazy var someoneView: WillReactiveCompatible = {
        //: let v = TalkingSomeoneAtMeView()
        let v = WillReactiveCompatible()
        //: v.isHidden = true
        v.isHidden = true
        //: return v
        return v
        //: }()
    }()

    //: private lazy var mentionInfoArr: [[String: Any]] = {
    private lazy var mentionInfoArr: [[String: Any]] = //: return [[String: Any]]()
        .init()
    //: }()
}

// MARK: - Load Data

//: extension TalkingGroupChatController {
extension ProvenanceViewDelegate {
    /// 群聊设置
    //: private func chatSettings() {
    private func arrangement() {
        //: TalkingGroupChatManager.share.chatRoomVC = self
        ActinideHeadChatManager.share.chatRoomVC = self

        //: func__joinOrQuitRoom(join: true)
        coloured(join: true)
    }

    /// 加入或退出聊天室
    /// - Parameter join: 是否加入
    //: private func func__joinOrQuitRoom(join: Bool) {
    private func coloured(join: Bool) {
        //: let params = ["roomId": self.conversationData.groupID, "type": (join == true ? 1:2)] as [String: Any]
        let params = [(String(app_sizePriceMessage.suffix(6))): self.conversationData.groupID, String(bytes: kMakeId.map{sizeAdd(model: $0)}, encoding: .utf8)!: join == true ? 1 : 2] as [String: Any]
        //: TalkingChatRequestTool.req_chatRoomJoinOrQuit(params: params) { succeed, result, errorModel in
        SourceRequestTool.requestDoing(params: params) { succeed, _, _ in
            //: guard succeed else { return }
            guard succeed else { return }
//            guard let data = result as? [String: Any] else {
//                return
//            }
//
//            if join {
//
//            }
        }
    }
}

// MARK: - EnableDelegate

//: extension TalkingGroupChatController: TMessageControllerDelegate {
extension ProvenanceViewDelegate: EnableDelegate {
    //: func messageController(_ controller: TUIMessageController!, onNewMessage data: V2TIMMessage!) -> ButtonDataThen! {
    func betweenCommunication(_: MainViewController!, to data: V2TIMMessage!) -> ButtonDataThen! {
        //: let dic = StyleChatManager.parseTXMessageData(data: data.customElem.data)
        let dic = StyleChatManager.more(data: data.customElem.data)
        //: let cellData = TUISocialChatManager.handleTXReceiveMessage(withExtral: dic, cusData: data.localCustomData, tempModel: data)
        let cellData = PrimrosePathThen.chromosomeMapping(withExtral: dic, cusData: data.localCustomData, tempModel: data)

        //: return cellData
        return cellData
    }

    //: func messageController(_ controller: TUIMessageController!, onShowMessageData data: ButtonDataThen!) -> ReasonViewCell! {
    func graphicIndex(_: MainViewController!, data: ButtonDataThen!) -> ReasonViewCell! {
        //: if data.isKind(of: ConnectionReactiveCompatible.self) {
        if data.isKind(of: ConnectionReactiveCompatible.self) {
            //: let cell = TalkingGroupChatTextMsgCell.init(style: .default, reuseIdentifier: "TalkingGroupChatTextMsgCellID")
            let cell = GrantMsgCell(style: .default, reuseIdentifier: "GrantMsgCell")
            //: let cellData = data as! ConnectionReactiveCompatible
            let cellData = data as! ConnectionReactiveCompatible
            //: cell.fill(with: cellData)
            cell.translation(with: cellData)
            //: cellData.atBlock = { [weak self] uid in
            cellData.atBlock = { [weak self] uid in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.menu_goToUserDetailController(uid: uid)
                self.utilizer(uid: uid)
            }
            //: cell.transBlock = { [weak self] in
            cell.transBlock = { [weak self] in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.msgView.translateMsg(with: cellData)
                self.msgView.objectTitle(cellData)
            }

            //: return cell
            return cell
        }

        //: if data.isKind(of: ChatThen.self) {
        if data.isKind(of: ChatThen.self) {
            //: let cell = TalkingChatImageMsgCell.init(style: .default, reuseIdentifier: "TalkingChatImageMsgCellID")
            let cell = DirectionResumeView(style: .default, reuseIdentifier: "DirectionResumeView")
            //: let cellData = data as! ChatThen
            let cellData = data as! ChatThen
            //: cell.fill(with: cellData)
            cell.translation(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingGroupChatGiftMsgCellData.self) {
        if data.isKind(of: WindowCellData.self) {
            //: let cell = TalkingGroupChatGiftMsgCell.init(style: .default, reuseIdentifier: "TalkingGroupChatGiftMsgCell")
            let cell = WithGroupThen(style: .default, reuseIdentifier: "WithGroupThen")
            //: let cellData = data as! TalkingGroupChatGiftMsgCellData
            let cellData = data as! WindowCellData
            //: cell.fill(with: cellData)
            cell.translation(with: cellData)

            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingChatTipsMsgCellData.self) {
        if data.isKind(of: StyleReactiveCompatible.self) {
            //: let cell = TalkingChatTipsMsgCell.init(style: .default, reuseIdentifier: "TalkingChatTipsMsgCellID")
            let cell = SharedMsgCell(style: .default, reuseIdentifier: "SharedMsgCell")
            //: let cellData = data as! TalkingChatTipsMsgCellData
            let cellData = data as! StyleReactiveCompatible
            //: cell.fill(with: cellData)
            cell.translation(with: cellData)
            //: return cell
            return cell
        }

        //: return nil
        return nil
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageAvatar cell: ReasonViewCell!) {
    func load(_: MainViewController!, can cell: ReasonViewCell!) {
        //: if let userModel = cell.messageData?.msgModel.user {
        if let userModel = cell.messageData?.msgModel.user {
            //: if userModel.uid == Int(PathAppManager.share.loginUid) {
            if userModel.uid == Int(PathAppManager.share.loginUid) {
                //: menu_goToUserDetailController(uid: userModel.uid )
                utilizer(uid: userModel.uid)
                //: }else{
            } else {
                //: menu_showMunuPopView(user: userModel)
                confirm(user: userModel)
            }
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageContent cell: ReasonViewCell!) {
    func headContent(_: MainViewController!, app cell: ReasonViewCell!) {
        //: if cell .isKind(of: TalkingChatImageMsgCell.self) {
        if cell.isKind(of: DirectionResumeView.self) { // 图片
            //: let imageCellData = cell.messageData as! ChatThen
            let imageCellData = cell.messageData as! ChatThen
            //: let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!
            let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!
            //: var filePath = (libraryPath as NSString).appendingPathComponent(imageFileName)
            var filePath = (libraryPath as NSString).appendingPathComponent(m_objectTitle)
            //: filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)
            filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)

            //: var model = TalkingMomentPhotoModel.init()
            var model = MomentHandyJSON()
            //: if FileManager.default.fileExists(atPath: filePath) {
            if FileManager.default.fileExists(atPath: filePath) {
                //: let data = NSData.init(contentsOfFile: filePath)
                let data = NSData(contentsOfFile: filePath)
                //: model.imagePic = UIImage(data: data! as Data)
                model.imagePic = UIImage(data: data! as Data)
                //: } else {
            } else {
                //: if imageCellData.imageData != nil {
                if imageCellData.imageData != nil {
                    //: model.imagePic = imageCellData.imageData
                    model.imagePic = imageCellData.imageData
                    //: } else {
                } else {
                    //: model.imgUrl =  imageCellData.imageUrl
                    model.imgUrl = imageCellData.imageUrl
                }
            }
            //: var currenModel = TalkingMomentModel.init()
            var currenModel = EqualWindowTransformable()
            //: var tempArr: [TalkingMomentPhotoModel] = []
            var tempArr: [MomentHandyJSON] = []
            //: tempArr.append(model)
            tempArr.append(model)
            //: currenModel.pic = tempArr
            currenModel.pic = tempArr
            //: let vc = TalkingMomentPhotosVC.init(momentModel: currenModel, index: 0, type: .normal)
            let vc = ServerViewDelegate(momentModel: currenModel, index: 0, type: .normal)
            //: self.currentViewController()?.navigationController?.pushViewController(vc, animated: true)
            self.withTo()?.navigationController?.pushViewController(vc, animated: true)

            //: } else if cell.isKind(of: TalkingGroupChatTextMsgCell.self) {
        } else if cell.isKind(of: GrantMsgCell.self) {
            //: let textCell = cell as! TalkingGroupChatTextMsgCell
            let textCell = cell as! GrantMsgCell
            //: let textData = textCell.messageData
            let textData = textCell.messageData
            //: let dic = StyleChatManager.parseTXMessageData(data: (textData?.innerMessage.customElem.data)!)
            let dic = StyleChatManager.more(data: (textData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(String(dream_makeIdent))] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains(String(bytes: m_streamIdent.map{alongImage(count: $0)}, encoding: .utf8)!) {
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<InsideHandyJSON>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: NamePushManager.share.func__actionPushForMessage(jumpModel: jumpModel)
                    NamePushManager.share.constraintScreen(jumpModel: jumpModel)
                }
            }

            //: } else if cell.isKind(of: TalkingChatTipsMsgCell.self) {
        } else if cell.isKind(of: SharedMsgCell.self) {
            //: let tipCell = cell as! TalkingChatTipsMsgCell
            let tipCell = cell as! SharedMsgCell
            //: let tipData = tipCell.messageData
            let tipData = tipCell.messageData
            //: let dic = StyleChatManager.parseTXMessageData(data: (tipData?.innerMessage.customElem.data)!)
            let dic = StyleChatManager.more(data: (tipData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(String(dream_makeIdent))] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains(String(bytes: m_streamIdent.map{alongImage(count: $0)}, encoding: .utf8)!) {
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<InsideHandyJSON>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: NamePushManager.share.func__actionPushForMessage(jumpModel: jumpModel)
                    NamePushManager.share.constraintScreen(jumpModel: jumpModel)
                }
            }
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onRetryMessageContent cell: ReasonViewCell!) {
    func beauty(_: MainViewController!, peeler cell: ReasonViewCell!) {
        //: func__sendPriveteMsg(msgData: cell.messageData!, reSend: true)
        permission(msgData: cell.messageData!, reSend: true)
    }
}

// MARK: - Event

//: extension TalkingGroupChatController {
extension ProvenanceViewDelegate {
    /// 销毁倒计时
    //: private func destroyTipsTimer() {
    private func lag() {
        //: if tipsTimer != nil {
        if tipsTimer != nil {
            //: tipsTimer?.invalidate()
            tipsTimer?.invalidate()
            //: tipsTimer = nil
            tipsTimer = nil
        }
    }

    /// 点击用户头像，展示操作面板
    /// - Parameter user: 用户模型
    //: private func menu_showMunuPopView(user: AbTalkingChatMsgUserModel) {
    private func confirm(user: ButtonModel) {
        //: view.endEditing(true)
        view.endEditing(true)
        //: let vc = TalkingMunuPopView.init(frame: self.view.frame)
        let vc = LabelPopView(frame: self.view.frame)
        //: vc.title = user.nickname
        vc.title = user.nickname
        //: vc.initwithList(cellTitleList: ["Send Gift".localized, "@", "View info".localized, "Report".localized])
        vc.eldest(cellTitleList: [(String(dreamBackgroundIconName.prefix(7)) + String(noti_nameMsg)).localized, "@", (String(appHiddenTopStr)).localized, (String(showValueName) + k_makeKeyId.replacingOccurrences(of: "up", with: "t")).localized])
        //: vc.munuBlock = { [weak self] index, str in
        vc.munuBlock = { [weak self] index, _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: if index == 0 {
            if index == 0 {
                //: self.menu_sentGiftAction(user: user)
                self.companyName(user: user)
                //: } else if index == 1 {
            } else if index == 1 {
                //: self.menu_atTa(user: user)
                self.deadline(user: user)
                //: } else if index == 2 {
            } else if index == 2 {
                //: self.menu_goToUserDetailController(uid: user.uid)
                self.utilizer(uid: user.uid)
                //: } else if index == 3 {
            } else if index == 3 {
                //: self.menu_reportAction(uid: String(user.uid))
                self.bankManager(uid: String(user.uid))
            }
        }
    }

    /// 送礼
    /// - Parameter user: 用户模型
    //: private func menu_sentGiftAction(user: AbTalkingChatMsgUserModel) {
    private func companyName(user: ButtonModel) {
        //: self.msgInputView.clickgiftBtn()
        self.msgInputView.gutBtn()
        //: let model = TalkingChatRoomMemberModel()
        let model = ManagerRateThen()
        //: model.uid = "\(user.uid)"
        model.uid = "\(user.uid)"
        //: model.nickname = user.nickname
        model.nickname = user.nickname
        //: model.headPic = user.headPic
        model.headPic = user.headPic
        //: model.sex = user.sex
        model.sex = user.sex
        //: model.age = Int(user.age) ?? 0
        model.age = Int(user.age) ?? 0
        //: self.msgInputView.giftView.didClickSelectedRow(model: model)
        self.msgInputView.giftView.addMake(model: model)
    }

    /// @Ta
    /// - Parameter user: 用户模型
    //: private func menu_atTa(user: AbTalkingChatMsgUserModel) {
    private func deadline(user: ButtonModel) {
        //: let dict: [String: Any] = ["uid": String(user.uid), "name": user.nickname]
        let dict: [String: Any] = [String(bytes: show_managerUrl.map{removeName(host: $0)}, encoding: .utf8)!: String(user.uid), String(bytes: kRetIdent.map{dataAnd(on: $0)}, encoding: .utf8)!: user.nickname]
        //: self.msgInputView.receiveAtInfo(range: nil, dict: dict)
        self.msgInputView.startTo(range: nil, dict: dict)
        //: self.msgInputView.inputTextView.becomeFirstResponder()
        self.msgInputView.inputTextView.becomeFirstResponder()
    }

    /// 访问用户详情
    /// - Parameter uid: 用户Id
    //: private func menu_goToUserDetailController(uid: Int) {
    private func utilizer(uid: Int) {
        //: NamePushManager.share.func__pushToUserDetailVC(uid: String(uid))
        NamePushManager.share.pressAway(uid: String(uid))
    }

    /// 举报
    /// - Parameter uid: 用户Id
    //: private func menu_reportAction(uid: String) {
    private func bankManager(uid: String) {
        //: self.msgInputView.showOtherInputView = true
        self.msgInputView.showOtherInputView = true
        //: let reportView = TalkingReportAlertView.init(frame: UIScreen.main.bounds, type: .reportUserType, rId: uid)
        let reportView = RateViewDelegate(frame: UIScreen.main.bounds, type: .reportUserType, rId: uid)
        //: reportView.showReportViewIn(view: nil)
        reportView.toModel(view: nil)
        //: reportView.reportHideBlock = { [weak self] _ in
        reportView.reportHideBlock = { [weak self] _ in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: self.msgInputView.showOtherInputView = false
            self.msgInputView.showOtherInputView = false
            //: return
        }
    }
}

// MARK: - 发送消息

//: extension TalkingGroupChatController {
extension ProvenanceViewDelegate {
    /// 发送文本消息
    /// - Parameter msgStr: 文本

    //: internal func func__sendTextMsg(msgStr: String, quoteModel: AbTalkingChatMsgQuoteModel?) {
    func firstDismissView(msgStr: String, quoteModel _: EnableScreenModel?) {
        //: let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        //: if temStr.count == 0 {
        if temStr.count == 0 {
            //: func__showStatusBarErrorMsg(showMsg: "Can't send blank message".localized)
            colorGift(showMsg: (String(showStyleData) + noti_contentIdent.lowercased()).localized)
            //: return
            return
        }

        //: let uidStr = NSMutableString()
        let uidStr = NSMutableString()
        //: var jumps = [[String: String]]()
        var jumps = [[String: String]]()
        //: if msgInputView.atArr.count > 0 {
        if msgInputView.atArr.count > 0 { // 有@ 消息
            //: msgInputView.atArr.forEach { uid in
            msgInputView.atArr.forEach { uid in
                //: let dict = ["jumpKey": "user", "uid": uid]
                let dict = [String(bytes: m_streamIdent.map{alongImage(count: $0)}, encoding: .utf8)!: (String(kToId)), String(bytes: show_managerUrl.map{removeName(host: $0)}, encoding: .utf8)!: uid]
                //: uidStr.append(uid+",")
                uidStr.append(uid + ",")
                //: jumps.append(dict)
                jumps.append(dict)
            }
            //: uidStr.deleteCharacters(in: NSRange(location: uidStr.length-1, length: 1))
            uidStr.deleteCharacters(in: NSRange(location: uidStr.length - 1, length: 1))
        }

        //: var msgInfo: [String: Any] = ["content": temStr, "msgType": "txt", "isGroup": "1"]
        var msgInfo: [String: Any] = [String(bytes: main_makeTouchId.map{$0^187}, encoding: .utf8)!: temStr, (String(show_textData) + notiUpStyleMsg.replacingOccurrences(of: "error", with: "e")): (mainCameraMessage.replacingOccurrences(of: "conversation", with: "tx")), (String(userViewContent.prefix(6)) + String(show_imageInfoId)): "1"]
        //: if jumps.count > 0 {
        if jumps.count > 0 {
            //: msgInfo["jumps"] = jumps
            msgInfo[(userContentFormat.replacingOccurrences(of: "size", with: "p"))] = jumps
        }

        // 接口参数
        //: var paramsDic: [String: Any] = ["message": temStr,
        var paramsDic: [String: Any] = [String(bytes: main_equalMessage.reversed(), encoding: .utf8)!: temStr,
                                        //: "type": "TxtMsg",
                                        String(bytes: kMakeId.map{sizeAdd(model: $0)}, encoding: .utf8)!: (String(dream_managerMessage.prefix(6))),
                                        //: "roomId": self.infoModel.roomId]
                                        (String(app_sizePriceMessage.suffix(6))): self.infoModel.roomId]
        //: if uidStr.length > 0 {
        if uidStr.length > 0 {
            //: paramsDic["toUid"] = uidStr
            paramsDic[(String(m_adKey.prefix(5)))] = uidStr
        }

        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(noti_lengthMessage.suffix(6)) + appAspectMediumMsg.replacingOccurrences(of: "action", with: "o")): msgInfo, String(bytes: userRequestUrl.reversed(), encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = PrimrosePathThen.weightAdd(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            permission(msgData: cellData, reSend: false)
        }
    }

    /// 发送图片消息
    /// - Parameter img: 图片
    //: private func func__sendPictureMsg(img: UIImage) {
    private func funcAwakeOf(img: UIImage) {
        //: let imagePath = ChatThen.getChatImagePath(image: img)
        let imagePath = ChatThen.noCell(image: img)
        //: let msgInfo: [String: Any] = ["contentType": "ImgMsg", "msgType": "img", "imageUri": imagePath]
        let msgInfo: [String: Any] = [String(bytes: k_errorFormat.map{layerData(click: $0)}, encoding: .utf8)!: (String(k_everyId)), (String(show_textData) + notiUpStyleMsg.replacingOccurrences(of: "error", with: "e")): (app_noUrl.replacingOccurrences(of: "app", with: "g")), String(bytes: noti_dataValue.map{sizeName(title: $0)}, encoding: .utf8)!: imagePath]
        // 接口参数
        //: let paramsDic: [String: Any] = ["pic": imagePath,
        let paramsDic: [String: Any] = [(user_hiddenUrl.replacingOccurrences(of: "language", with: "pi")): imagePath,
                                        //: "type": "ImgMsg",
                                        String(bytes: kMakeId.map{sizeAdd(model: $0)}, encoding: .utf8)!: (String(k_everyId)),
                                        //: "roomId": self.infoModel.roomId]
                                        (String(app_sizePriceMessage.suffix(6))): self.infoModel.roomId]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(noti_lengthMessage.suffix(6)) + appAspectMediumMsg.replacingOccurrences(of: "action", with: "o")): msgInfo, String(bytes: userRequestUrl.reversed(), encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = PrimrosePathThen.weightAdd(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            permission(msgData: cellData, reSend: false)
        }
    }

    /// 发送消息
    /// - Parameters:
    ///   - msgData: 消息数据结构
    ///   - reSend: 是否重发【目前未使用该字段】
    //: private func func__sendPriveteMsg(msgData: ButtonDataThen, reSend: Bool) {
    private func permission(msgData: ButtonDataThen, reSend _: Bool) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            colorGift(showMsg: user_netTitle)
            //: return
            return
        }
        // 插入本地
        //: self.msgView.sendCustomMessage(msgData)
        self.msgView.send(msgData)
        // 处理请求参数
        //: var params: Dictionary<String, Any> = msgData.msgModel.params as! Dictionary<String, Any>
        var params: [String: Any] = msgData.msgModel.params as! [String: Any]
//        params["toUid"] = self.targetId
        //: if params.keys.contains("pic") { // 图片
        if params.keys.contains((user_hiddenUrl.replacingOccurrences(of: "language", with: "pi"))) { // 图片
            //: let imageStr: String = params["pic"] as! String
            let imageStr: String = params[(user_hiddenUrl.replacingOccurrences(of: "language", with: "pi"))] as! String
            //: do {
            do {
                //: let data: Data = try Data.init(contentsOf: URL.init(fileURLWithPath: imageStr))
                let data: Data = try Data(contentsOf: URL(fileURLWithPath: imageStr))
                //: params["pic"] = data
                params[(user_hiddenUrl.replacingOccurrences(of: "language", with: "pi"))] = data
                //: } catch {
            } catch {
                //: printLog(message: error)
                printLog(message: error)
            }
        }

        // 发送消息接口
        //: TalkingChatRequestTool.req_SendRoomMsg(param: params, completion: { succeed, result, errorModel in
        SourceRequestTool.mainCompletion(param: params, completion: { succeed, result, errorModel in
            //: if !succeed {
            if !succeed {
                //: self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                self.msgView.outMessage(msgData, photoStatus: nil, length: NSNumber(value: 0), incomeStatus: .Msg_Status_Fail)
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                self.customMiscalculation(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                //: return
                return
            }

            //: let dataDict = result as! [String: Any]
            let dataDict = result as! [String: Any]
            //: let msgInfo = dataDict["msgInfo"] as! [String: Any]
            let msgInfo = dataDict[(String(noti_lengthMessage.suffix(6)) + appAspectMediumMsg.replacingOccurrences(of: "action", with: "o"))] as! [String: Any]

            // 更新本地消息信息
            //: self.msgView.changeCustomMsg(msgData, msgId: msgInfo["msgId"] as? String, msgIncome: NSNumber(value: 0), status: .Msg_Status_Succ)
            self.msgView.outMessage(msgData, photoStatus: msgInfo[(String(userValueStr.suffix(5)))] as? String, length: NSNumber(value: 0), incomeStatus: .Msg_Status_Succ)

            //: if dataDict.keys.contains("totalMfCoin") {
            if dataDict.keys.contains((String(m_noTimeMsg) + String(showNextIdent))) {
                //: let totalMfCoin: NSNumber = dataDict["totalMfCoin"] as! NSNumber
                let totalMfCoin: NSNumber = dataDict[(String(m_noTimeMsg) + String(showNextIdent))] as! NSNumber
                //: PathAppManager.share.loginUserMode.mf_coin = totalMfCoin.stringValue
                PathAppManager.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            }
            //: })
        })
    }

    /// 处理发送失败情况
    //: private func dealSendMsgError(errorCode: Int, errorStr: String, isGift: Bool, isResend: Bool) {
    private func customMiscalculation(errorCode: Int, errorStr: String, isGift _: Bool, isResend _: Bool) {
        //: if errorCode == 50203 {
        if errorCode == 50203 {
            //: guard PathAppManager.share.loginUserMode.status != 1 else {
            guard PathAppManager.share.loginUserMode.status != 1 else {
                //: if !errorStr.isEmptyString {
                if !errorStr.isEmptyString {
                    //: func__showStatusBarErrorMsg(showMsg: errorStr)
                    colorGift(showMsg: errorStr)
                }
                //: return
                return
            }

            //: NamePushManager.share.func__jumpToWebRecharge(clickEvent: clickSendingiftsButton, sufficient: false)
            NamePushManager.share.rechargeParams(clickEvent: noti_limitUrl, sufficient: false)
            //: msgInputView.giftView.dismissView()
            msgInputView.giftView.kisser()
            //: insertTipMsg_Nobalance(tipStr: errorStr)
            noBlockLocal(tipStr: errorStr)
            //: }else if errorCode == 10888 {
        } else if errorCode == 10888 {
            //: let view = TalkingLiveVipSubscribePopView.init(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
            let view = LightPopView(frame: CGRect(x: 0, y: 0, width: app_screenFailMarginFormat, height: mainAcceptKey))
            //: view.show()
            view.characterSet()
            //: } else {
        } else {
            //: if !errorStr.isEmptyString {
            if !errorStr.isEmptyString {
                //: func__showStatusBarErrorMsg(showMsg: errorStr)
                colorGift(showMsg: errorStr)
            }
        }
    }
}

// MARK: - 输入框

//: extension TalkingGroupChatController: ChatInputViewDelegate {
extension ProvenanceViewDelegate: FullThen {
    //: func func__askforGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {
    func showApp(giftModel _: DiskModelType, giftNum _: String) {}

    /// 输入框输入内容停顿超过1秒
    //: func func_inputContentIntervalOneSecond() {}
    func beforeOf() {}

    /// 输入框@ 在线列表用户
    //: func func__selectMentionUser(range: NSRange) {
    func length(range: NSRange) {
        //: let vc = TalkingChatRoomOnlineListController()
        let vc = LabIndexRecognizerDelegate()
        //: vc.roomID = self.infoModel.roomId
        vc.roomID = self.infoModel.roomId
        //: vc.titleType = .callNumber
        vc.titleType = .callNumber
        //: vc.selectedBlock = {[weak self] dict in
        vc.selectedBlock = { [weak self] dict in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.atFlag = true
            self.atFlag = true
            //: self.msgInputView.receiveAtInfo(range: range, dict: dict)
            self.msgInputView.startTo(range: range, dict: dict)
            //: self.navigationController?.popViewController(animated: true)
            self.navigationController?.popViewController(animated: true)
        }
        //: self.navigationController?.pushViewController(vc, animated: true)
        self.navigationController?.pushViewController(vc, animated: true)
    }

    //: func func__selectMoreFunAction(actionType: MoreActionType) {
    func pic(actionType: AssemblageTotalLiteral) {
        //: switch actionType {
        switch actionType {
        //: case .Photo:
        case .Photo:
            //: func__choicePhotos()
            photos()

        //: default:
        default:
            //: break
            break
        }
    }

    //: func func__sendGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {}
    func giftWith(giftModel _: DiskModelType, giftNum _: String) {}

    //: func func__sendChatRoomGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String, model: TalkingChatRoomMemberModel) {
    func untilState(giftModel: DiskModelType, giftNum: String, model: ManagerRateThen) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            colorGift(showMsg: user_netTitle)
            //: return
            return
        }
        //: var params: Dictionary<String, Any> = [:]
        var params: [String: Any] = [:]
        //: params["toUid"] =  model.uid
        params[(String(m_adKey.prefix(5)))] = model.uid
        //: params["giftId"] = giftModel.gid
        params[(String(appToText.suffix(6)))] = giftModel.gid
        //: params["giftNum"] = giftNum
        params[(String(k_greetCenterStr))] = giftNum
        //: params["roomId"] = infoModel.roomId
        params[(String(app_sizePriceMessage.suffix(6)))] = infoModel.roomId
        //: if giftModel.pkgItemsetId != nil {
        if giftModel.pkgItemsetId != nil {
            //: params["pkgItemsetId"] = giftModel.pkgItemsetId
            params[(String(appPathSucceedName) + String(mStatusPath.suffix(3)))] = giftModel.pkgItemsetId
        }

        // 送礼接口
        //: TalkingChatRequestTool.req_SendGiftChatRoom(param: params, completion: { succeed, result, errorModel in
        SourceRequestTool.largesseView(param: params, completion: { succeed, result, errorModel in
            //: guard succeed else {
            guard succeed else {
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                self.customMiscalculation(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                //: return
                return
            }

            // 更新本地礼物背包
            //: if giftModel.pkgItemsetId != nil {
            if giftModel.pkgItemsetId != nil {
                //: self.msgInputView.giftView.bags_removeGiftPackage(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
                self.msgInputView.giftView.fromShared(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
            }
            //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, model: model, isFromInputView: true)
            self.theMakeView(extral: result as! [String: Any?], model: model, isFromInputView: true)
            //: })
        })
    }

    //: func heightToBottomChanged(heightToBottom: CGFloat) {
    func acceptBottom(heightToBottom: CGFloat) {
        //: msgInputView.snp.updateConstraints { make in
        msgInputView.snp.updateConstraints { make in
            //: make.bottom.equalTo(self.view).offset(-heightToBottom)
            make.bottom.equalTo(self.view).offset(-heightToBottom)
        }
        //: someoneView.snp.updateConstraints { make in
        someoneView.snp.updateConstraints { make in
            //: make.bottom.equalTo(msgInputView.snp.top).offset(-103)
            make.bottom.equalTo(msgInputView.snp.top).offset(-103)
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.active(true)
            self.msgView.active(true)
        }
    }

    //: func inputViewHeightChanged(height: CGFloat) {
    func barrelhouse(height: CGFloat) {
        //: self.msgInputView.snp.updateConstraints { make in
        self.msgInputView.snp.updateConstraints { make in
            //: make.height.equalTo(height)
            make.height.equalTo(height)
        }
        //: self.view.needsUpdateConstraints()
        self.view.needsUpdateConstraints()
        //: self.view.updateConstraintsIfNeeded()
        self.view.updateConstraintsIfNeeded()
        //: UIView.animate(withDuration: 0.3, animations: {
        UIView.animate(withDuration: 0.3, animations: {
            //: self.view.layoutIfNeeded()
            self.view.layoutIfNeeded()
            //: })
        })
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.active(true)
            self.msgView.active(true)
        }
    }

    //: @objc override func func__TapGestureRecognizer(_ tap: UITapGestureRecognizer) {
    @objc override func bindAdd(_ tap: UITapGestureRecognizer) {
        //: super.func__TapGestureRecognizer(tap)
        super.bindAdd(tap)
    }

    //: func func__starRecordBtnDown() {}
    func addToQuit() {}

    //: func func__sendVoiceMsg(voicePath: String, voiceTime: Int) {}
    func confinement(voicePath _: String, voiceTime _: Int) {}
}

// MARK: - 选择图片

//: extension TalkingGroupChatController: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
extension ProvenanceViewDelegate: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
    //: private func func__choicePhotos() {
    private func photos() {
        //: TalkingPermissionTool.func__openAlbumServiceWithBlock(true) { (isOpen: Bool) in
        EqualReactiveCompatible.upBack(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let vc = TalkingImagePickTool.initImagePickerVc(maxCount: 1, allowPhoto: true, allowVideo: false)
                let vc = RestrictionThen.pass(maxCount: 1, allowPhoto: true, allowVideo: false)
                //: vc.modalPresentationStyle = .fullScreen
                vc.modalPresentationStyle = .fullScreen
                //: NamePushManager.share.func__getCurrentActivityVC()?.present(vc, animated: true)
                NamePushManager.share.toponymVc()?.present(vc, animated: true)
                //: vc.didFinishPickingPhotosHandle = { [weak self] ( photos: Array<UIImage>?,
                vc.didFinishPickingPhotosHandle = { [weak self] (photos: [UIImage]?,
                                                                 //: assets: Array<Any>?,
                                                                 _: [Any]?,
                                                                 //: isSelectOriginalPhoto: Bool?)  in
                                                                 _: Bool?) in
                        //: guard let self = self else {return}
                        guard let self = self else { return }
                        //: if photos!.count > 0 {
                        if photos!.count > 0 {
                            //: self.func__sendPictureMsg(img: photos![0])
                            self.funcAwakeOf(img: photos![0])
                        }
                }
            }
        }
    }

    //: private func func__camaraPhotos() {
    private func lineStatus() {
        //: TalkingPermissionTool.func__openCaptureDeviceServiceWithBlock(true) { (isOpen: Bool) in
        EqualReactiveCompatible.rangeBlock(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let imagePicker = UIImagePickerController()
                let imagePicker = UIImagePickerController()
                //: imagePicker.sourceType = .camera
                imagePicker.sourceType = .camera
                //: imagePicker.delegate = self
                imagePicker.delegate = self
                //: self.present(imagePicker, animated: true, completion: {})
                self.present(imagePicker, animated: true, completion: {})
            }
        }
    }

    //: func imagePickerController(_ picker: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
    func imagePickerController(_: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
        //: self.dismiss(animated: true) {
        self.dismiss(animated: true) {
            //: if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
            if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
                //: self.func__sendPictureMsg(img: photo)
                self.funcAwakeOf(img: photo)
            }
        }
    }

    //: func imagePickerControllerDidCancel(_ picker: UIImagePickerController) {
    func imagePickerControllerDidCancel(_: UIImagePickerController) {
        //: self.dismiss(animated: true, completion: nil)
        self.dismiss(animated: true, completion: nil)
    }
}

// MARK: - 处理@ 消息

//: extension TalkingGroupChatController {
extension ProvenanceViewDelegate {
    /// 接收到群聊@ 消息
    /// - Parameter info: @消息数据
    //: func handleMentionMsg(info: [String: Any]) {
    func handleWithinProgramme(info: [String: Any]) {
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: guard info["roomId"] as! String == self.conversationData.groupID else { return }
            guard info[(String(app_sizePriceMessage.suffix(6)))] as! String == self.conversationData.groupID else { return }
            //: if self.mentionInfoArr.count >= 5 {
            if self.mentionInfoArr.count >= 5 { // 只保留最新5条@ 消息
                //: self.mentionInfoArr.removeFirst()
                self.mentionInfoArr.removeFirst()
            }
            //: self.mentionInfoArr.append(info)
            self.mentionInfoArr.append(info)
            //: self.someoneView.isHidden = self.mentionInfoArr.count > 0 ? false:true
            self.someoneView.isHidden = self.mentionInfoArr.count > 0 ? false : true
        }
    }
}

// MARK: - 提示消息

//: extension TalkingGroupChatController {
extension ProvenanceViewDelegate {
    //: func func__TipsNotification() {
    func dowry() {
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(insertTipsNotif(notification:)),
                                               selector: #selector(cognomen(notification:)),
                                               //: name: CHAT_TIPS_TEXT_NOTIFICATION,
                                               name: userFileMsg,
                                               //: object: nil)
                                               object: nil)
    }

    //: @objc func insertTipsNotif(notification: NSNotification) {
    @objc func cognomen(notification: NSNotification) {
        //: let userinfo = notification.userInfo as! [String: AnyObject]
        let userinfo = notification.userInfo as! [String: AnyObject]
        //: let celldata: TalkingChatTipsMsgCellData  = userinfo["msg"] as! TalkingChatTipsMsgCellData
        let celldata: StyleReactiveCompatible = userinfo[(userLikeFormat.replacingOccurrences(of: "model", with: "sg"))] as! StyleReactiveCompatible

        //: self.msgView.uiMsgs.add(celldata)
        self.msgView.uiMsgs.add(celldata)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.active(true)
        self.msgView.active(true)
    }

    /// 插入提示消息
    /// - Parameters:
    ///   - tipStr: 提示消息文本
    ///   - needInsertLocal: 是否需要插入本地数据库（默认：是）
    //: func insertTipMsg_Nobalance(tipStr: String, needInsertLocal: Bool = true) {
    func noBlockLocal(tipStr: String, needInsertLocal: Bool = true) {
        //: let msgInfo = ["content": tipStr, "msgType": "tips"]
        let msgInfo = [String(bytes: main_makeTouchId.map{$0^187}, encoding: .utf8)!: tipStr, (String(show_textData) + notiUpStyleMsg.replacingOccurrences(of: "error", with: "e")): (m_byText.replacingOccurrences(of: "beauty", with: "ip"))]
        //: var message: ButtonDataThen?
        var message: ButtonDataThen?
        //: if needInsertLocal == true {
        if needInsertLocal == true { // 需要插入本地
            //: } else {
        } else {
            //: message = TUISocialChatManager.handleTXMessageSend(withExtral: msgInfo, isSender: true, isTip: true)
            message = PrimrosePathThen.weightAdd(withExtral: msgInfo, isSender: true, isTip: true)
        }

        //: guard let message = message else { return }
        guard let message = message else { return }
        //: self.msgView.uiMsgs.add(message)
        self.msgView.uiMsgs.add(message)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.active(true)
        self.msgView.active(true)
    }

    /// 公共聊天室插入系统提示消息
    //: func func__chatRoom_insertTipMsg() {
    func implant() {
        // 延时，内存插入
        //: tipsTimer = Timer.scheduledTimer(withTimeInterval: 5, repeats: false, block: {[weak self] time in
        tipsTimer = Timer.scheduledTimer(withTimeInterval: 5, repeats: false, block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard PathAppManager.share.appUserConfigMode.rTips.count > 0 else { return }
            guard PathAppManager.share.appUserConfigMode.rTips.count > 0 else { return }
            //: self.insertTipMsg_Nobalance(tipStr: PathAppManager.share.appUserConfigMode.rTips, needInsertLocal: false)
            self.noBlockLocal(tipStr: PathAppManager.share.appUserConfigMode.rTips, needInsertLocal: false)
            //: })
        })
    }
}

// MARK: - 动效

//: extension TalkingGroupChatController {
extension ProvenanceViewDelegate {
    /// 插入礼物消息，播放动效
    /// - Parameters:
    ///   - extral: 送礼接口返回结果
    ///   - isFromInputView: 是否来自输入框送礼入口
    //: func func__insertGiftMessageWithExtral(extral: Dictionary<String, Any?>, model: TalkingChatRoomMemberModel, isFromInputView: Bool) {
    func theMakeView(extral: [String: Any?], model: ManagerRateThen, isFromInputView: Bool) {
        //: var extralInfo = extral
        var extralInfo = extral
        //: var msgInfoDic: Dictionary<String, Any> = extralInfo["msgInfo"] as! Dictionary<String, Any>
        var msgInfoDic: [String: Any] = extralInfo[(String(noti_lengthMessage.suffix(6)) + appAspectMediumMsg.replacingOccurrences(of: "action", with: "o"))] as! [String: Any]
        /// 用于本地判断celldata类型
        //: msgInfoDic["isGroup"] = "1"
        msgInfoDic[(String(userViewContent.prefix(6)) + String(show_imageInfoId))] = "1"
        //: msgInfoDic["msgType"] = "gift"
        msgInfoDic[(String(show_textData) + notiUpStyleMsg.replacingOccurrences(of: "error", with: "e"))] = (String(mActualName))
        //: extralInfo["msgInfo"] = msgInfoDic
        extralInfo[(String(noti_lengthMessage.suffix(6)) + appAspectMediumMsg.replacingOccurrences(of: "action", with: "o"))] = msgInfoDic

        //: var toUser = ["uid": model.uid,
        var toUser = [String(bytes: show_managerUrl.map{removeName(host: $0)}, encoding: .utf8)!: model.uid,
                      //: "nickname": model.nickname,
                      (String(dream_showData)): model.nickname,
                      //: "headPic": model.headPic]
                      String(bytes: k_selectedIdent.map{failStatus(request: $0)}, encoding: .utf8)!: model.headPic]

        //: if model.nickname == "All Numbers".localized {
        if model.nickname == (String(appClearIdent.prefix(6)) + "mbers").localized {
            //: if extralInfo.keys.contains("gift") != false {
            if extralInfo.keys.contains((String(mActualName))) != false {
                //: let giftdic = extralInfo["gift"] as! Dictionary<String, Any>
                let giftdic = extralInfo[(String(mActualName))] as! [String: Any]
                //: toUser["nickname"] = "\(giftdic["sendNum"] ?? "1")"
                toUser[(String(dream_showData))] = "\(giftdic[(String(kPicUrl.suffix(7)))] ?? "1")"
            }
        }
        //: extralInfo["toUser"] = toUser
        extralInfo[(String(noti_acceptTitle.prefix(5)) + app_headFormat.replacingOccurrences(of: "row", with: "r"))] = toUser

        //: let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        let cellData = PrimrosePathThen.weightAdd(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        //: self.msgView.sendCustomMessage(cellData)
        self.msgView.send(cellData)
        //: var msgIncome: NSNumber = NSNumber.init(floatLiteral: 0)
        var msgIncome = NSNumber(floatLiteral: 0)
        //: if msgInfoDic.keys.contains("msgIncome") {
        if msgInfoDic.keys.contains((String(showNetFormat) + String(show_picValue.prefix(7)))) {
            //: msgIncome = msgInfoDic["msgIncome"] as! NSNumber
            msgIncome = msgInfoDic[(String(showNetFormat) + String(show_picValue.prefix(7)))] as! NSNumber
        }
        //: self.msgView.changeCustomMsg(cellData, msgId: msgInfoDic["msgId"] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)
        self.msgView.outMessage(cellData, photoStatus: msgInfoDic[(String(userValueStr.suffix(5)))] as? String, length: msgIncome, incomeStatus: .Msg_Status_Succ)
        //: sendGiftByMeToEfffecct(giftDic: extral as [String: Any], model: model)
        lope(giftDic: extral as [String: Any], model: model)
        //: if extralInfo.keys.contains("totalMfCoin") {
        if extralInfo.keys.contains((String(m_noTimeMsg) + String(showNextIdent))) {
            //: let totalMfCoin: NSNumber = extralInfo["totalMfCoin"] as! NSNumber
            let totalMfCoin: NSNumber = extralInfo[(String(m_noTimeMsg) + String(showNextIdent))] as! NSNumber
            //: PathAppManager.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            PathAppManager.share.loginUserMode.mf_coin = totalMfCoin.stringValue
        }
        //: if isFromInputView {
        if isFromInputView {
            //: msgInputView.giftView.updateGiftInfo(needReload: false, mf_coin: PathAppManager.share.loginUserMode.mf_coin)
            msgInputView.giftView.pitchCancel(needReload: false, mf_coin: PathAppManager.share.loginUserMode.mf_coin)
        }
        //: msgInputView.giftView.saveRecentMembers()
        msgInputView.giftView.theSave()
    }

    //: func initCachEffectData() {
    func handleSize() {
        //: giftEffectView.initCachAnimatData(tagetID: infoModel.roomId)
        giftEffectView.auditoryImage(tagetID: infoModel.roomId)
    }

    //: func sendGiftByMeToEfffecct(giftDic: Dictionary<String, Any>, model: TalkingChatRoomMemberModel) {
    func lope(giftDic: [String: Any], model: ManagerRateThen) {
        //: guard let giftModel = giftEffectView.chatRoomDisposeReceiveGiftMsg(giftMessageDic: (giftDic as NSDictionary) as! Dictionary<String, Any>, model: model) else { return }
        guard let giftModel = giftEffectView.backgroundModel(giftMessageDic: (giftDic as NSDictionary) as! [String: Any], model: model) else { return }
        //: if giftTrackView.giftSendItem.count == 0 {
        if giftTrackView.giftSendItem.count == 0 {
            //: let arrM = NSMutableArray.init()
            let arrM = NSMutableArray()
            //: for model in TalkingChatGiftManager.share.getSendingItems() {
            for model in AdministratorReactiveCompatible.share.userConstraint() {
                //: arrM.add(model)
                arrM.add(model)
            }
            //: giftTrackView.giftSendItem = arrM as! [TalkingGiftNumArrModel]
            giftTrackView.giftSendItem = arrM as! [TelecastingMeasurable]
        }
        //: giftTrackView.func__didReceiveGiftMsgModel(model: giftModel)
        giftTrackView.thenFunc(model: giftModel)

        //: giftEffectView.dealGiftAnimatData(dict: giftDic)
        giftEffectView.accumulationDict(dict: giftDic)
    }

    //: @objc func addGiftEffectModelArr(giftArr: Array<Any>) {
    @objc func addReason(giftArr: [Any]) {
        //: if giftTrackView.giftSendItem.count == 0 {
        if giftTrackView.giftSendItem.count == 0 {
            //: let arrM = NSMutableArray.init()
            let arrM = NSMutableArray()
            //: for model in TalkingChatGiftManager.share.getSendingItems() {
            for model in AdministratorReactiveCompatible.share.userConstraint() {
                //: arrM.add(model)
                arrM.add(model)
            }
            //: giftTrackView.giftSendItem = arrM as! [TalkingGiftNumArrModel]
            giftTrackView.giftSendItem = arrM as! [TelecastingMeasurable]
        }
        //: for i in 0..<giftArr.count {
        for i in 0 ..< giftArr.count {
            //: let giftModel = giftArr[i]
            let giftModel = giftArr[i]
            //: giftTrackView.func__didReceiveGiftMsgModel(model: giftModel as! TalkingGiftMsgAnimatModel)
            giftTrackView.thenFunc(model: giftModel as! LabelMeasurable)
        }

        //: giftEffectView.addGiftAnimatModelArr(modelArr: giftArr)
        giftEffectView.afterImageArr(modelArr: giftArr)
    }
}

// MARK: - Layout

//: extension TalkingGroupChatController {
extension ProvenanceViewDelegate {
    /// 创建UI
    //: private func createUI() {
    private func makeUi() {
        //: self.addChild(msgView)
        self.addChild(msgView)
        //: view.addSubview(msgView.view)
        view.addSubview(msgView.view)
        //: view.addSubview(someoneView)
        view.addSubview(someoneView)
        //: view.addSubview(giftEffectView)
        view.addSubview(giftEffectView)
        //: view.insertSubview(giftTrackView, belowSubview: giftEffectView)
        view.insertSubview(giftTrackView, belowSubview: giftEffectView)
        //: view.addSubview(msgInputView)
        view.addSubview(msgInputView)
        //: view.addSubview(navView)
        view.addSubview(navView)
        //: msgView.targetID = self.conversationData.groupID
        msgView.targetID = self.conversationData.groupID
    }

    /// 约束
    //: private func createUIConstraints() {
    private func suite() {
        //: navView.snp.makeConstraints { make in
        navView.snp.makeConstraints { make in
            //: make.leading.trailing.top.equalTo(view)
            make.leading.trailing.top.equalTo(view)
            //: make.height.equalTo(StatusBarNavigationBarHeight)
            make.height.equalTo(dream_systemTitle)
        }
        //: msgInputView.snp.makeConstraints { make in
        msgInputView.snp.makeConstraints { make in
            //: make.leading.trailing.bottom.equalTo(view)
            make.leading.trailing.bottom.equalTo(view)
            //: make.height.equalTo(msgInputView.BackViewHeight)
            make.height.equalTo(msgInputView.BackViewHeight)
        }
        //: msgView.view.snp.makeConstraints { make in
        msgView.view.snp.makeConstraints { make in
            //: make.leading.trailing.equalTo(view)
            make.leading.trailing.equalTo(view)
            //: make.top.equalTo(StatusBarNavigationBarHeight)
            make.top.equalTo(dream_systemTitle)
            //: make.bottom.equalTo(msgInputView.snp.top)
            make.bottom.equalTo(msgInputView.snp.top)
        }
        //: someoneView.snp.makeConstraints { make in
        someoneView.snp.makeConstraints { make in
            //: make.bottom.equalTo(msgInputView.snp.top).offset(-103)
            make.bottom.equalTo(msgInputView.snp.top).offset(-103)
            //: make.trailing.equalToSuperview()
            make.trailing.equalToSuperview()
            //: make.size.equalTo(CGSize(width: 130, height: 37))
            make.size.equalTo(CGSize(width: 130, height: 37))
        }
        //: giftEffectView.snp.makeConstraints { make in
        giftEffectView.snp.makeConstraints { make in
            //: make.leading.trailing.top.bottom.equalTo(view)
            make.leading.trailing.top.bottom.equalTo(view)
        }
        //: giftTrackView.snp.makeConstraints { make in
        giftTrackView.snp.makeConstraints { make in
            //: make.leading.equalTo(view)
            make.leading.equalTo(view)
            //: make.height.equalTo(180)
            make.height.equalTo(180)
            //: make.bottom.equalTo(-actualHeight(h: 220)-kDeviceSafeBottomHeight)
            make.bottom.equalTo(-actualHeight(h: 220) - dreamOkTitle)
        }
    }

    /// 事件绑定
    //: func bindInteraction() {
    func distanceHighVelocityGiftTotalerplay() {
        // 返回
        //: navView.backButton.rx.tap.subscribe(onNext: { [weak self] in
        navView.backButton.rx.tap.subscribe(onNext: { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.navigationController?.popViewController(animated: true)
            self.navigationController?.popViewController(animated: true)
            //: }).disposed(by: disposeBag)
        }).disposed(by: disposeBag)

        // 在线列表
        //: navView.onlineListBtn.rx.tap.subscribe(onNext: { [weak self] in
        navView.onlineListBtn.rx.tap.subscribe(onNext: { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: let vc = TalkingChatRoomOnlineListController()
            let vc = LabIndexRecognizerDelegate()
            //: vc.roomID = self.infoModel.roomId
            vc.roomID = self.infoModel.roomId
            //: vc.titleType = .normal
            vc.titleType = .normal
            //: self.navigationController?.pushViewController(vc, animated: true)
            self.navigationController?.pushViewController(vc, animated: true)
            //: }).disposed(by: disposeBag)
        }).disposed(by: disposeBag)

        // 群聊someone@ me 点击事件
        //: someoneView.mentionBtn.rx.tap
        someoneView.mentionBtn.rx.tap
            //: .throttle(.milliseconds(1000), scheduler: MainScheduler.instance)
            .throttle(.milliseconds(1000), scheduler: MainScheduler.instance)
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else { return }
                guard let self = self else { return }
                // 找到当前消息，滚动到指定位置
                //: guard let dict = self.mentionInfoArr.first else { return }
                guard let dict = self.mentionInfoArr.first else { return }
                //: for (index, cellData) in self.msgView.uiMsgs.enumerated() {
                for (index, cellData) in self.msgView.uiMsgs.enumerated() {
                    //: if (cellData as! ButtonDataThen).isKind(of: ConnectionReactiveCompatible.self) {
                    if (cellData as! ButtonDataThen).isKind(of: ConnectionReactiveCompatible.self) {
                        //: let groupCellData = cellData as! ConnectionReactiveCompatible
                        let groupCellData = cellData as! ConnectionReactiveCompatible
                        //: if groupCellData.msgID == dict["msgId"] as! String {
                        if groupCellData.msgID == dict[(String(userValueStr.suffix(5)))] as! String {
                            //: self.msgView.tableView.scroll(toRow: UInt(index), inSection: 0, at: .bottom, animated: true)
                            self.msgView.tableView.scroll(toRow: UInt(index), inSection: 0, at: .bottom, animated: true)
                            //: self.mentionInfoArr.removeFirst()
                            self.mentionInfoArr.removeFirst()
                            //: self.someoneView.isHidden = self.mentionInfoArr.count > 0 ? false:true
                            self.someoneView.isHidden = self.mentionInfoArr.count > 0 ? false : true
                        }
                    }
                }
                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)

        // 被挤下线时，退出群聊
        //: CharterDataManager.shared.rx
        CharterDataManager.shared.rx
            //: .observeWeakly(Bool.self, "isConnection")
            .observeWeakly(Bool.self, (String(k_toValueFormat)))
            //: .subscribe(onNext: { [weak self] (value) in
            .subscribe(onNext: { [weak self] value in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: if value == false {
                if value == false {
                    //: self.navigationController?.popViewController(animated: true)
                    self.navigationController?.popViewController(animated: true)
                }
                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)
    }
}
