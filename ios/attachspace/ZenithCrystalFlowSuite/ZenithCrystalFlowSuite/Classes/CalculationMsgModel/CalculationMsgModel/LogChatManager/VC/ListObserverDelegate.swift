
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let userManagerFrameContent:[UInt8] = [0x3f,0x38,0x3f,0x22,0x7e,0x35,0x39,0x32,0x33,0x24,0x6c,0x7f,0x76,0x3e,0x37,0x25,0x76,0x38,0x39,0x22,0x76,0x34,0x33,0x33,0x38,0x76,0x3f,0x3b,0x26,0x3a,0x33,0x3b,0x33,0x38,0x22,0x33,0x32]

/*: "uid" :*/
fileprivate let dream_analysisMsg:[UInt8] = [0x45,0x59,0x54]

private func searchView(app num: UInt8) -> UInt8 {
    return num ^ 48
}

/*: "headPic" :*/
fileprivate let show_rowPath:String = "view else playheadPic"

/*: "nickname" :*/
fileprivate let app_momentText:String = "onickona"
fileprivate let app_giftTitle:String = "toole"

/*: "birthday" :*/
fileprivate let dream_appPathMessage:[Character] = ["b","i","r","t","h","d","a","y"]

/*: "videoUrl" :*/
fileprivate let user_noToMsg:String = "vidok"

/*: "location" :*/
fileprivate let k_evidenceId:[Character] = ["l","o","c"]
fileprivate let notiHandleId:String = "ATION"

/*: "cmd" :*/
fileprivate let main_tableName:[UInt8] = [0x49,0x47,0x4e]

/*: "refuseCall" :*/
fileprivate let dreamToName:[Character] = ["r","e","f","u"]
fileprivate let app_firstMessage:String = "seCallsend app"

/*: "data" :*/
fileprivate let appImageContextTitle:[UInt8] = [0x21,0x24,0x31,0x24]

private func inTo(of num: UInt8) -> UInt8 {
    return num ^ 69
}

/*: "Call interruption" :*/
fileprivate let dream_giftPath:[Character] = ["C","a","l","l"," ","i","n","t","e","r"]
fileprivate let main_colorTitle:[Character] = ["r","u","p","t","i","o","n"]

/*: "stopCall" :*/
fileprivate let k_eventId:String = "rank model noticestopCal"
fileprivate let showAddIdent:String = "equal"

/*: "logId" :*/
fileprivate let k_indicatorMsg:[UInt8] = [0xfc,0xff,0xf7,0xd9,0xf4]

fileprivate func atSquareKit(conversation num: UInt8) -> UInt8 {
    let value = Int(num) - 144
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "cancelCall" :*/
fileprivate let app_topWindowId:String = "cancformat"

/*: "No answer" :*/
fileprivate let notiLabIconSendFormat:[Character] = ["N","o"," "]
fileprivate let appIndexSourceTitle:[Character] = ["a","n","s","w","e","r"]

/*: "duration" :*/
fileprivate let showDreamStr:String = "DURATION"

/*: "She has hung up" :*/
fileprivate let k_successSizeWithValue:String = "She limit till self"
fileprivate let dream_giftValue:[Character] = ["u","p"]

/*: "cancelSex" :*/
fileprivate let notiAttributeData:String = "sizenc"

/*: "He has hung up" :*/
fileprivate let show_blockUrl:[Character] = ["H","e"," ","h","a","s"," ","h","u","n"]
fileprivate let appUserMessage:[Character] = ["g"," ","u","p"]

/*: "endType" :*/
fileprivate let dreamAppMessage:String = "endTypemake icon center number"

/*: "stopSex" :*/
fileprivate let dream_captureCenterInfoStr:[Character] = ["s","t","o","p","S","e","x"]

/*: "Insufficient gold coins, call has ended" :*/
fileprivate let mNowEqualName:[UInt8] = [0x42,0x67,0x6c,0x6e,0x5f,0x5f,0x62,0x5c,0x62,0x5e,0x67,0x6d,0x19,0x60,0x68,0x65,0x5d,0x19,0x5c,0x68,0x62,0x67,0x6c,0x25,0x19,0x5c,0x5a,0x65,0x65,0x19,0x61,0x5a,0x6c,0x19,0x5e,0x67,0x5d,0x5e,0x5d]

fileprivate func transactionTo(argument num: UInt8) -> UInt8 {
    let value = Int(num) - 249
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "The other hung up the phone" :*/
fileprivate let app_logData:String = "let view let returnThe ot"
fileprivate let mUserValue:String = "view title if count drafthung "
fileprivate let mainModePath:String = "skip path backup the "
fileprivate let mainTitlePath:String = "phcolore"

/*: "type" :*/
fileprivate let kUtilityUrl:String = "deadlineype"

/*: "jumpUrl" :*/
fileprivate let app_reasonData:[Character] = ["j","u","m","p","U","r","l"]

/*: "Cancel" :*/
fileprivate let userVideoDefineMessage:String = "Cancelimage content file"

/*: "Get gold coins" :*/
fileprivate let user_tableNameMessage:[Character] = ["G","e","t"," ","g","o","l"]
fileprivate let app_messageContent:String = "view size knowd coins"

/*: "addBean" :*/
fileprivate let dream_viewMakeEngineFormat:String = "button appaddBean"

/*: "refuseSex" :*/
fileprivate let m_tableIdent:String = "videoefuse"
fileprivate let dream_bottomIdent:String = "label custom self pathSex"

/*: "onAcceptCall" :*/
fileprivate let dream_startStr:String = "label"
fileprivate let mainSizeSendData:String = "of malenAccep"

/*: "acceptCall" :*/
fileprivate let app_rangeText:String = "ACCE"

/*: "Calls are cancelled" :*/
fileprivate let main_colorIdent:String = "Callshare tap head allow to"
fileprivate let appViewName:String = "CANC"

/*: "The call has ended" :*/
fileprivate let dream_equalMsg:String = "The cmake self file block"
fileprivate let main_objectFormat:[Character] = ["a","l","l"," ","h","a","s"," ","e","n","d","e","d"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  ListObserverDelegate.swift
//  AbroadTalking
//
//  Created by young on 2022/11/11.
//

//: import CallKit.CXCallObserver
import CallKit.CXCallObserver
//: import UIKit
import UIKit

// 通话监听(必须为全局属性，否则无法接听)
//: let voiceCallHelper = CXCallObserver()
let app_screenUrl = CXCallObserver()
//: class TalkingVoiceChatViewController: TalkingBaseViewController {
class ListObserverDelegate: ListRecognizerDelegate {
	var itemArray: [AnyHashable] = []
	var listQuantity: Int = 20
	var homeQuantity: Double = -47.8
	var fromContent: String = "bottom"
	var indexTableArray: [AnyHashable] = []
	var dataDictionary: [AnyHashable: String] = [:]
	var awakeViewArray: [AnyHashable] = []

    //: var chatModel: TalkingVideoChatModel?
    var chatModel: WithChatModel? // 语音通话模型
    //: private var player: TalkingCallBGMPlayer?
    private var player: ReasonWindowPlayer? // 拨打中背景音乐
    //: private var timeOutTimer: Timer?
    private var timeOutTimer: Timer? // 未接听超时定时器
    //: private var expiredDate = Date()
    private var expiredDate = Date() // 失效时间
    //: private var miniView: TalkingVoiceMiniView?
    private var miniView: ConnectionMiniView? // 音频通话小窗口

    //: override init(nibName nibNameOrNil: String?, bundle nibBundleOrNil: Bundle?) {
    override init(nibName _: String?, bundle _: Bundle?) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        // 提前绑定delegate
        //: TalkingSocketManager.shared.callerDelegate = self
        PublishDataSocketDelegate.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        PublishDataSocketDelegate.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        PublishDataSocketDelegate.shared.errorDelegate = self
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: userManagerFrameContent.map{$0^86}, encoding: .utf8)!)
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: let nav = self.navigationController as! TalkingNavigationController
        let nav = self.navigationController as! StreetSmartNavigationController
        //: nav.protectedPushVc = nil
        nav.protectedPushVc = nil

        //: UIApplication.shared.isIdleTimerDisabled = true
        UIApplication.shared.isIdleTimerDisabled = true // 设备不会进入休眠状态
        //: self.startBGM()
        self.showPhoto()
        //: if self.chatModel?.videoStage == .Recieved {
        if self.chatModel?.videoStage == .Recieved { // 接收方验证权限
            //: checkVoiceAuth()
            addVocalizationAuth()
        }
    
		if var conversationValue = chatModel?.pairHeadPic { 
			if var stepValue = miniView?.pointAnimView.isPlayAnimation { 
		            if (self.children.count == 19) && (self.edgesForExtendedLayout == .left) {
		                //: SWIFT_CUSTOM_DANGER_File_Call
		                let colorRound = NonsolidColorController()
		            colorRound.titleOpen = stepValue
		            colorRound.appellationContent = conversationValue
		            colorRound.giftCount = { [self] textTotal in
		            self.listQuantity = textTotal
		            
		            var colorRound = interactionView.giftView.keyborHeight
		                colorRound -= 1
		                if Int(colorRound) > -35 {
		                    colorRound = colorRound + 1
		                }
		            if colorRound > self.listQuantity {
		                self.listQuantity = colorRound
		            }
		            
		            return self.listQuantity
		            }
		            colorRound.moreMagnitude = { [self] orderedSeriesSum in
		            self.homeQuantity = orderedSeriesSum
		            
		            var colorRound = interactionView.giftView.contentHeight
		                colorRound += 1
		                if colorRound != 53 {
		                    colorRound = colorRound - 1
		                }
		            if colorRound < self.homeQuantity {
		                self.homeQuantity = colorRound
		            }
		            
		            return self.homeQuantity
		            }
		            colorRound.textTitle = { [self] sendEffectViewName in
		            self.fromContent = sendEffectViewName
		            
		            var colorRound = interactionView.giftView.chatRoomID
		            colorRound = String(colorRound.sorted(by:<))
		            if colorRound.contains(self.fromContent) {
		                self.fromContent = colorRound
		            }
		            
		            return self.fromContent
		            }
		            colorRound.distanceTapViewArray = { [self] detailArray in
		            self.indexTableArray = detailArray
		            
		            guard var value = self.indexTableArray as? [String] else {
		                return nil
		            }
		            return value
		            }
		            colorRound.compareBindDictionary = { [self] statusListDictionary in
		            self.dataDictionary = statusListDictionary
		            
		            guard var value = self.dataDictionary as? [String: String] else {
		                return nil
		            }
		            return value
		            }
		                self.navigationController?.pushViewController(colorRound.self, animated: true)
		            }
		
			}
		}
	}

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: if self.miniView != nil {
        if self.miniView != nil { // 移除小窗口
            //: self.miniView?.removeFromSuperview()
            self.miniView?.removeFromSuperview()
            //: self.miniView = nil
            self.miniView = nil
            //: TalkingSocketManager.shared.currentDataVC = nil
            PublishDataSocketDelegate.shared.currentDataVC = nil
        }
        //: interactionView.initCachEffectData()
        interactionView.fileData()
    
	if let miniView = miniView {

            if (miniView.pointAnimView.gestureRecognizers != nil && miniView.pointAnimView.gestureRecognizers!.count == 11) && (miniView.pointAnimView.layer.isDoubleSided != true) {
                //: SWIFT_CUSTOM_DANGER_File_Call
                let messageMatchLet = NonsolidColorView()
            messageMatchLet.withInterval = interactionView.giftView.contentHeight
            
            
            messageMatchLet.needArray = { [self] detailArray in
            self.itemArray = detailArray
            
            guard var value = self.itemArray as? [String] else {
                return nil
            }
            return value
            }
                miniView.pointAnimView.addSubview(messageMatchLet)
            }

	}

	}

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true
        //: setupSubviews()
        host()
        //: setupSubViewsConstraint()
        setupOkConstraint()
        //: initNotificationObserver()
        highGroundFirst()
        //: req_callGetUserInfo()
        textInfo()
        //: timeoutRules()
        timeout()
        //: resumeTalk()
        holdForth()
        //: setUpBGMPlayer()
        random()
    
            if (!interactionView.autoresizesSubviews) && (interactionView.constraints.count == 45) {
                //: SWIFT_CUSTOM_DANGER_File_Call
                let countCell = NonsolidColorView(frame: interactionView.bounds.offsetBy(dx: CGFloat(197.81), dy: CGFloat(0)))
            countCell.withInterval = interactionView.giftView.contentHeight
            
            
            countCell.needArray = { [self] detailArray in
            self.awakeViewArray = detailArray
            
            guard var value = self.awakeViewArray as? [String] else {
                return nil
            }
            return value
            }
                interactionView.addSubview(countCell)
            }

	}

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if PathAppManager.share.appConfigMode.disableShootScreen {
            if PathAppManager.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = CustomPathThen(frame: CGRect(x: 0, y: 0, width: app_screenFailMarginFormat, height: mainAcceptKey))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
    }

    //: deinit {
    deinit {
        //: UIApplication.shared.isIdleTimerDisabled = false
        UIApplication.shared.isIdleTimerDisabled = false
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
        //: TalkingSocketManager.shared.isData = false
        PublishDataSocketDelegate.shared.isData = false
        //: self.stopVCAllActivties()
        self.targetActivties()
    }

    // MARK: - Lazy Load

    /// 音频通话操作按钮
    //: private lazy var interactionView: Talking1v1VoiceInteractionView = {
    private lazy var interactionView: PathSumeractionView = {
        //: let v = Talking1v1VoiceInteractionView(frame: .zero, chatModel: self.chatModel)
        let v = PathSumeractionView(frame: .zero, chatModel: self.chatModel)
        //: v.delegate = self
        v.delegate = self
        //: return v
        return v
        //: }()
    }()

    //: private lazy var voiceManager: TalkingVoiceChatManager = {
    private lazy var voiceManager: PathChatManager = {
        //: let m = TalkingVoiceChatManager()
        let m = PathChatManager()
        //: m.handsfreeBtn = self.interactionView.handsfreeBtn
        m.handsfreeBtn = self.interactionView.handsfreeBtn
        //: return m
        return m
        //: }()
    }()
}

// MARK: - Request & 数据处理

//: extension TalkingVoiceChatViewController {
extension ListObserverDelegate {
    /// 查询接收者信息
    //: func req_callGetUserInfo() {
    func textInfo() {
        //: guard let pairUid = chatModel?.pairUid else { return }
        guard let pairUid = chatModel?.pairUid else { return }
        //: TalkingVideoChatManager.req_callGetUserInfo(uid: pairUid) { [weak self] succeed, result, errorModel in
        LogChatManager.sufficient(uid: pairUid) { [weak self] succeed, result, _ in
            //: guard succeed == true else { return }
            guard succeed == true else { return }
            //: guard let self = self else { return }
            guard let self = self else { return }

            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())

            //: self.chatModel?.pairUid = json["uid"].stringValue
            self.chatModel?.pairUid = json[String(bytes: dream_analysisMsg.map{searchView(app: $0)}, encoding: .utf8)!].stringValue
            //: self.chatModel?.pairHeadPic = json["headPic"].stringValue
            self.chatModel?.pairHeadPic = json[(String(show_rowPath.suffix(7)))].stringValue
            //: self.chatModel?.pairNickName = json["nickname"].stringValue
            self.chatModel?.pairNickName = json[(app_momentText.replacingOccurrences(of: "on", with: "n") + app_giftTitle.replacingOccurrences(of: "tool", with: "m"))].stringValue
            //: self.chatModel?.pairBirthday = json["birthday"].stringValue
            self.chatModel?.pairBirthday = json[(String(dream_appPathMessage))].stringValue
            //: self.chatModel?.videoUrl = json["videoUrl"].stringValue
            self.chatModel?.videoUrl = json[(user_noToMsg.replacingOccurrences(of: "ok", with: "e") + "oUrl")].stringValue
            //: self.chatModel?.location = json["location"].stringValue
            self.chatModel?.location = json[(String(k_evidenceId) + notiHandleId.lowercased())].stringValue

            // 刷新通话视图
            //: self.interactionView.afterGetInfo()
            self.interactionView.errorCell()
        }
    }
}

// MARK: - Event

//: extension TalkingVoiceChatViewController {
extension ListObserverDelegate {
    /// 权限校验，播放BGM
    //: private func checkVoiceAuth() {
    private func addVocalizationAuth() {
        //: TalkingPermissionTool.func__openRecordServiceWithBlock(true) { isOpen in
        EqualReactiveCompatible.utility(true) { isOpen in
            //: guard isOpen == true else {
            guard isOpen == true else {
                //: self.func__showStatusBarErrorMsg(showMsg: kMessage_permissions_error)
                self.colorGift(showMsg: showMeBarReasonPath)
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                PublishDataSocketDelegate.shared.birthday(info: [String(bytes: main_tableName.map{$0^42}, encoding: .utf8)!: (String(dreamToName) + String(app_firstMessage.prefix(6))), String(bytes: appImageContextTitle.map{inTo(of: $0)}, encoding: .utf8)!: [String(bytes: dream_analysisMsg.map{searchView(app: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
                //: self.popCurrentViewController()
                self.writeAt()
                //: return
                return
            }

            //: self.stopBGM()
            self.nameBgm()
            //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: .Response)
            self.player = ReasonWindowPlayer.componentTo(type: .Response)
            //: self.startBGM()
            self.showPhoto()
        }
    }

    /// 释放当前页面所有资源
    //: private func stopVCAllActivties() {
    private func targetActivties() {
        //: if self.timeOutTimer != nil {
        if self.timeOutTimer != nil {
            //: self.timeOutTimer?.invalidate()
            self.timeOutTimer?.invalidate()
            //: self.timeOutTimer = nil
            self.timeOutTimer = nil
        }

        //: self.stopBGM()
        self.nameBgm()
        //: TalkingSocketManager.shared.callerDelegate = nil
        PublishDataSocketDelegate.shared.callerDelegate = nil
        //: TalkingSocketManager.shared.calledPartyDelegate = nil
        PublishDataSocketDelegate.shared.calledPartyDelegate = nil
        //: TalkingSocketManager.shared.errorDelegate = nil
        PublishDataSocketDelegate.shared.errorDelegate = nil
    }

    // 将当前视图从栈中移除
    //: private func popCurrentViewController(animated: Bool = true) {
    private func writeAt(animated: Bool = true) {
        //: self.stopBGM()
        self.nameBgm()

        //: if self.navigationController?.topViewController == self {
        if self.navigationController?.topViewController == self {
            //: self.navigationController?.popViewController(animated: animated)
            self.navigationController?.popViewController(animated: animated)
            //: } else {
        } else {
            //: if var vcArr = self.navigationController?.viewControllers {
            if var vcArr = self.navigationController?.viewControllers {
                //: if let index = vcArr.firstIndex(of: self) {
                if let index = vcArr.firstIndex(of: self) {
                    //: vcArr.remove(at: index)
                    vcArr.remove(at: index)
                    //: self.navigationController?.setViewControllers(vcArr, animated: false)
                    self.navigationController?.setViewControllers(vcArr, animated: false)
                }
            }
        }
    }

    /// 创建音频通话房间
    //: private func createTalkRoom() {
    private func fine() {
        //: self.voiceManager.createRoomAndStartPublish(roomId: self.chatModel!.logId)
        self.voiceManager.remark(roomId: self.chatModel!.logId)
    }

    /// 通话重连
    //: private func resumeTalk() {
    private func holdForth() {
        //: if self.chatModel?.resumeTalk == true {
        if self.chatModel?.resumeTalk == true {
            //: createTalkRoom()
            fine()
        }
    }
}

// MARK: - 通知

//: extension TalkingVoiceChatViewController {
extension ListObserverDelegate {
    /// 初始化通知
    //: private func initNotificationObserver() {
    private func highGroundFirst() {
        // 监听 App 从后台切前台
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(appWillEnterForeground(notif:)),
                                               selector: #selector(determine(notif:)),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        // socket断网重连监听
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(reachabilityChanged(note:)),
                                               selector: #selector(realize(note:)),
                                               //: name: SOCKET_IS_OPEN_NOTIFICATION,
                                               name: dreamEnvironmentUrl,
                                               //: object: nil)
                                               object: nil)

        //: voiceCallHelper.setDelegate(self, queue: DispatchQueue.main)
        app_screenUrl.setDelegate(self, queue: DispatchQueue.main)
    }

    /// app进入前台
    //: @objc private func appWillEnterForeground(notif: Notification) {
    @objc private func determine(notif _: Notification) {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: let date = Date()
        let date = Date()
        //: let interval = date.timeIntervalSince(self.expiredDate)
        let interval = date.timeIntervalSince(self.expiredDate)
        //: if interval >= 0 {
        if interval >= 0 {
            //: popCurrentViewController()
            writeAt()
        }
    }

    /// 断网重连socket
    //: @objc private func reachabilityChanged(note: Notification) {
    @objc private func realize(note _: Notification) {
        //: guard TalkingSocketManager.shared.needReconnect == true else { return }
        guard PublishDataSocketDelegate.shared.needReconnect == true else { return }
        //: TalkingSocketManager.shared.callerDelegate = self
        PublishDataSocketDelegate.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        PublishDataSocketDelegate.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        PublishDataSocketDelegate.shared.errorDelegate = self
        //: self.voiceManager.restartTalkWhenDisconnectedAndNetAvailable(roomId: self.chatModel?.logId ?? 0)
        self.voiceManager.equalBy(roomId: self.chatModel?.logId ?? 0)
    }
}

// MARK: - CXCallObserverDelegate【通话监听】

//: extension TalkingVoiceChatViewController: CXCallObserverDelegate {
extension ListObserverDelegate: CXCallObserverDelegate {
    //: func callObserver(_ callObserver: CXCallObserver, callChanged call: CXCall) {
    func callObserver(_: CXCallObserver, callChanged call: CXCall) {
        //: if call.hasConnected == true {
        if call.hasConnected == true { // 通话中
            //: self.func__showStatusBarErrorMsg(showMsg: "Call interruption".localized)
            self.colorGift(showMsg: (String(dream_giftPath) + String(main_colorTitle)).localized)
            //: switch(self.chatModel?.videoStage) {
            switch self.chatModel?.videoStage {
            //: case .Calling:
            case .Calling:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
                PublishDataSocketDelegate.shared.birthday(info: [String(bytes: main_tableName.map{$0^42}, encoding: .utf8)!: (String(k_eventId.suffix(7)) + showAddIdent.replacingOccurrences(of: "equal", with: "l")), String(bytes: appImageContextTitle.map{inTo(of: $0)}, encoding: .utf8)!: [String(bytes: k_indicatorMsg.map{atSquareKit(conversation: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Waiting:
            case .Waiting:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
                PublishDataSocketDelegate.shared.birthday(info: [String(bytes: main_tableName.map{$0^42}, encoding: .utf8)!: (app_topWindowId.replacingOccurrences(of: "format", with: "el") + "Call"), String(bytes: appImageContextTitle.map{inTo(of: $0)}, encoding: .utf8)!: [String(bytes: k_indicatorMsg.map{atSquareKit(conversation: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Recieved:
            case .Recieved:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                PublishDataSocketDelegate.shared.birthday(info: [String(bytes: main_tableName.map{$0^42}, encoding: .utf8)!: (String(dreamToName) + String(app_firstMessage.prefix(6))), String(bytes: appImageContextTitle.map{inTo(of: $0)}, encoding: .utf8)!: [String(bytes: dream_analysisMsg.map{searchView(app: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
            //: case .none:
            case .none:
                //: break
                break
            }

            //: if miniView != nil {
            if miniView != nil {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.modelEnd()
                //: TalkingSocketManager.shared.currentDataVC = nil
                PublishDataSocketDelegate.shared.currentDataVC = nil
            }
            //: popCurrentViewController()
            writeAt()

            //: } else if call.hasEnded == true || call.isOnHold == true {
        } else if call.hasEnded == true || call.isOnHold == true { // 电话挂断 || 无人接听挂断
            //: self.voiceManager.enableAudioModule(enable: true)
            self.voiceManager.viewPing(enable: true)
        }
    }
}

// MARK: - 通话音乐

//: extension TalkingVoiceChatViewController {
extension ListObserverDelegate {
    /// 初始化音频通话背景音乐
    //: private func setUpBGMPlayer() {
    private func random() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let type = self.chatModel!.isCaller == true ? CallBGMPlayerType.Call : CallBGMPlayerType.Response
        let type = self.chatModel!.isCaller == true ? CommonHashableRepresentation.Call : CommonHashableRepresentation.Response
        //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: type)
        self.player = ReasonWindowPlayer.componentTo(type: type)
    }

    /// 开始播放背景音乐
    //: private func startBGM() {
    private func showPhoto() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: guard self.chatModel?.resumeTalk != true else { return }
        guard self.chatModel?.resumeTalk != true else { return }

        //: self.player?.play()
        self.player?.fromCurrent()
    }

    /// 停止播放背景音乐
    //: private func stopBGM() {
    private func nameBgm() {
        //: guard self.player != nil else { return }
        guard self.player != nil else { return }
        //: self.player?.stop()
        self.player?.upwardStop()
        //: self.player = nil
        self.player = nil
    }
}

// MARK: - 定时器

//: extension TalkingVoiceChatViewController {
extension ListObserverDelegate {
    /// 超时定时器
    //: private func timeoutRules() {
    private func timeout() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let timeout = self.chatModel?.isCaller == true ? VIDEO_1v1_CALL_TIMEOUT : self.chatModel?.remainAnswerCallTime()
        let timeout = self.chatModel?.isCaller == true ? appTimeName : self.chatModel?.indicatorCommit()
        //: if timeout! <= 0 {
        if timeout! <= 0 {
            //: buildTalkTimeOutAction()
            frameworkUpwards()
        }
        //: self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] timer in
        self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.buildTalkTimeOutAction()
            self.frameworkUpwards()
            //: }, repeats: false)
        }, repeats: false)

        //: self.expiredDate = Date().addingTimeInterval(60)
        self.expiredDate = Date().addingTimeInterval(60)
    }

    /// 倒计时超时逻辑处理
    //: private func buildTalkTimeOutAction() {
    private func frameworkUpwards() {
        //: guard !TalkingSocketManager.shared.isData else { return }
        guard !PublishDataSocketDelegate.shared.isData else { return }

        //: var socketDict: [String: Any]
        var socketDict: [String: Any]
        //: if self.chatModel?.isCaller == true {
        if self.chatModel?.isCaller == true { // 拨打方
            //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
            self.colorGift(showMsg: (String(notiLabIconSendFormat) + String(appIndexSourceTitle)).localized)
            //: socketDict = ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]]
            socketDict = [String(bytes: main_tableName.map{$0^42}, encoding: .utf8)!: (app_topWindowId.replacingOccurrences(of: "format", with: "el") + "Call"), String(bytes: appImageContextTitle.map{inTo(of: $0)}, encoding: .utf8)!: [String(bytes: k_indicatorMsg.map{atSquareKit(conversation: $0)}, encoding: .utf8)!: self.chatModel?.logId]]
            //: } else {
        } else { // 接收方
            //: socketDict = ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]]
            socketDict = [String(bytes: main_tableName.map{$0^42}, encoding: .utf8)!: (String(dreamToName) + String(app_firstMessage.prefix(6))), String(bytes: appImageContextTitle.map{inTo(of: $0)}, encoding: .utf8)!: [String(bytes: dream_analysisMsg.map{searchView(app: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]]
        }
        //: TalkingSocketManager.shared.sendMessage(info: socketDict)
        PublishDataSocketDelegate.shared.birthday(info: socketDict)
        //: popCurrentViewController()
        writeAt()
    }
}

// MARK: - resume talk【恢复通话】

//: extension TalkingVoiceChatViewController {
extension ListObserverDelegate {
    /// 恢复音频通话，更新chatModel
    /// - Parameter resumeData: 数据
    //: func updateInfoViaResumeTalkData(resumeData: [String: Any]) {
    func bitData(resumeData: [String: Any]) {
        //: let json = JSON(resumeData)
        let json = JSON(resumeData)
        //: let logId = json["logId"].intValue
        let logId = json[String(bytes: k_indicatorMsg.map{atSquareKit(conversation: $0)}, encoding: .utf8)!].intValue
        //: let duration = json["duration"].intValue
        let duration = json[(showDreamStr.lowercased())].intValue
        //: let interval = Date().timeIntervalSince1970
        let interval = Date().timeIntervalSince1970
        //: let startTalkTime = floor(interval) - Double(duration)
        let startTalkTime = floor(interval) - Double(duration)
        //: self.chatModel?.startTime = startTalkTime
        self.chatModel?.startTime = startTalkTime
        //: self.interactionView.updateStartTime(startTime: startTalkTime)
        self.interactionView.topPush(startTime: startTalkTime)

        //: if self.chatModel?.videoStage != .Calling {
        if self.chatModel?.videoStage != .Calling {
            //: self.chatModel?.logId = logId
            self.chatModel?.logId = logId
            //: self.chatModel?.videoStage = .Calling
            self.chatModel?.videoStage = .Calling
            //: self.interactionView.showViewsViaStage()
            self.interactionView.stage()
            //: createTalkRoom()
            fine()
            //: self.stopBGM()
            self.nameBgm()
        }
    }
}

// MARK: - TelevisionCallerDelegate【拨打方】

//: extension TalkingVoiceChatViewController: SocketManagerVideoChatCallerDelegate {
extension ListObserverDelegate: TelevisionCallerDelegate {
    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    //: func socket_videoChat_cancelCall(data: [String: Any]) {
    func mentionExecute(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(k_successSizeWithValue.prefix(4)) + "has hung " + String(dream_giftValue)).localized
            //: if json["cancelSex"].stringValue == Gender.male.rawValue {
            if json[(notiAttributeData.replacingOccurrences(of: "size", with: "ca") + "elSex")].stringValue == ConnectionClusterLiteral.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(show_blockUrl) + String(appUserMessage)).localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.colorGift(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.modelEnd()
            //: TalkingSocketManager.shared.currentDataVC = nil
            PublishDataSocketDelegate.shared.currentDataVC = nil
        }

        //: popCurrentViewController()
        writeAt()
    }

    /// 对方接受了来电，建立音频通话
    //: func socket_videoChat_acceptCall(data: [String: Any]) {
    func occurStatus(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)

        //: self.chatModel?.logId = json["logId"].intValue
        self.chatModel?.logId = json[String(bytes: k_indicatorMsg.map{atSquareKit(conversation: $0)}, encoding: .utf8)!].intValue
        //: self.chatModel?.videoStage = .Calling
        self.chatModel?.videoStage = .Calling
        //: self.chatModel?.renderPostion = .SelfOnWindow
        self.chatModel?.renderPostion = .SelfOnWindow

        //: self.stopBGM()
        self.nameBgm()
        //: self.interactionView.showViewsViaStage()
        self.interactionView.stage()
        //: self.createTalkRoom()
        self.fine()
    }

    /// 对方结束了通话（通话中）
    //: func socket_videoChat_stopCall(data: [String: Any]) {
    func inner(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)

        //: let endType = json["endType"].intValue
        let endType = json[(String(dreamAppMessage.prefix(7)))].intValue
        //: var typeStr = "She has hung up".localized
        var typeStr = (String(k_successSizeWithValue.prefix(4)) + "has hung " + String(dream_giftValue)).localized
        //: if json["stopSex"].stringValue == Gender.male.rawValue {
        if json[(String(dream_captureCenterInfoStr))].stringValue == ConnectionClusterLiteral.male.rawValue {
            //: typeStr = "He has hung up".localized
            typeStr = (String(show_blockUrl) + String(appUserMessage)).localized
        }

        //: if endType == 1 {
        if endType == 1 { // 付费方余额不足
            //: if PathAppManager.share.appStatus == AppSkinStatus.normal.rawValue {
            if PathAppManager.share.appStatus == AssemblageHashable.normal.rawValue {
                //: typeStr = "Insufficient gold coins, call has ended".localized
                typeStr = String(bytes: mNowEqualName.map{transactionTo(argument: $0)}, encoding: .utf8)!.localized
                //: } else {
            } else {
                //: typeStr = "The other hung up the phone".localized
                typeStr = (String(app_logData.suffix(6)) + "her " + String(mUserValue.suffix(5)) + String(mainModePath.suffix(7)) + mainTitlePath.replacingOccurrences(of: "color", with: "on")).localized
            }
        }
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.colorGift(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.modelEnd()
            //: TalkingSocketManager.shared.currentDataVC = nil
            PublishDataSocketDelegate.shared.currentDataVC = nil
        }

        //: self.interactionView.func_giftViewDismiss()
        self.interactionView.pointInTime()
        //: self.popCurrentViewController()
        self.writeAt()
        //: NotificationCenter.default.post(name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
        NotificationCenter.default.post(name: user_marginData, object: nil)
    }

    /// 余额不足提醒
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any]) {
    func surcharge(msg: String, data: [String: Any]) {
        //: guard PathAppManager.share.appStatus == AppSkinStatus.normal.rawValue else { return }
        guard PathAppManager.share.appStatus == AssemblageHashable.normal.rawValue else { return }

        //: let index = data["type"] as? String ?? ""
        let index = data[(kUtilityUrl.replacingOccurrences(of: "deadline", with: "t"))] as? String ?? ""
        //: if index == "2" {
        if index == "2" {
            //: guard let jumpUrl = data["jumpUrl"] as? String  else { return }
            guard let jumpUrl = data[(String(app_reasonData))] as? String else { return }
            //: let httpsUrl = PathAppManager.share.appConfigMode.urlH5Domain+jumpUrl
            let httpsUrl = PathAppManager.share.appConfigMode.urlH5Domain + jumpUrl
            //: var config = TalkingWebConfig.init()
            var config = WillConfig()
            //: config.isDiscountsView = true
            config.isDiscountsView = true
            //: NamePushManager.share.func__pushToWebVC(urlStr: httpsUrl, webConfig: config)
            NamePushManager.share.upwardsConfig(urlStr: httpsUrl, webConfig: config)
            //: } else {
        } else {
            //: TalkingAlertShow.alert(title: nil,
            CourseAlertShow.blockConfig(title: nil,
                                         //: message: msg,
                                         message: msg,
                                         //: leftBtnTitle: "Cancel".localized,
                                         leftBtnTitle: (String(userVideoDefineMessage.prefix(6))).localized,
                                         //: rightBtnTitle: "Get gold coins".localized) {
                                         rightBtnTitle: (String(user_tableNameMessage) + String(app_messageContent.suffix(7))).localized)
            {
                //: TalkingAlertShow.hideAlert()
                CourseAlertShow.cardWindow()
                //: } rightBlock: {
            } rightBlock: {
                //: NamePushManager.share.func__jumpToWebRecharge(clickEvent: clickLess1minButton, sufficient: false)
                NamePushManager.share.rechargeParams(clickEvent: user_styleUrl, sufficient: false)
            }
        }
    }

    /// 女性通话积分激励动效展示
    //: func socket_videoChat_onCallMotivation(data: [String: Any]) {
    func reportSeek(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: let addBean = json["addBean"].stringValue
        let addBean = json[(String(dream_viewMakeEngineFormat.suffix(7)))].stringValue
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: interactionView.showPointAnimaData(point: addBean)
            interactionView.twentyFourHourPeriod(point: addBean)
            //: } else {
        } else {
            //: self.miniView?.showPointAnimaData(point: addBean)
            self.miniView?.allButton(point: addBean)
        }
    }
}

// MARK: - ValueLevelObjectProtocol【接收方】

//: extension TalkingVoiceChatViewController: SocketManagerVideoChatCalledPartyDelegate {
extension ListObserverDelegate: ValueLevelObjectProtocol {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_refuseCall(data: [String: Any]) {
    func chat(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(k_successSizeWithValue.prefix(4)) + "has hung " + String(dream_giftValue)).localized
            //: if json["refuseSex"].stringValue == Gender.male.rawValue {
            if json[(m_tableIdent.replacingOccurrences(of: "video", with: "r") + String(dream_bottomIdent.suffix(3)))].stringValue == ConnectionClusterLiteral.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(show_blockUrl) + String(appUserMessage)).localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.colorGift(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.modelEnd()
            //: TalkingSocketManager.shared.currentDataVC = nil
            PublishDataSocketDelegate.shared.currentDataVC = nil
        }
        //: popCurrentViewController()
        writeAt()
    }

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    //: func socket_videoChat_onAcceptCall(data: [String: Any]) {
    func viewAppear(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: self.chatModel?.logId = json["logId"].intValue
        self.chatModel?.logId = json[String(bytes: k_indicatorMsg.map{atSquareKit(conversation: $0)}, encoding: .utf8)!].intValue
        //: self.chatModel?.videoStage = .Calling
        self.chatModel?.videoStage = .Calling
        //: self.chatModel?.renderPostion = .SelfOnWindow
        self.chatModel?.renderPostion = .SelfOnWindow
        //: self.stopBGM()
        self.nameBgm()
        //: self.interactionView.showViewsViaStage()
        self.interactionView.stage()
        //: self.createTalkRoom()
        self.fine()
    }
}

// MARK: - DelegateObjectProtocol【异常处理】

//: extension TalkingVoiceChatViewController: TalkingSocketManagerErrorDelegate {
extension ListObserverDelegate: DelegateObjectProtocol {
    // 处理error情况
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
    func reverberation(errorNo: Int, cmd: String, msg: String, data _: [String: Any]) {
        //: if cmd == "refuseCall" { // 对方拒绝了通话
        if cmd == (String(dreamToName) + String(app_firstMessage.prefix(6))) { // 对方拒绝了通话
            //: if self.miniView == nil {
            if self.miniView == nil {
                //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
                self.colorGift(showMsg: (String(notiLabIconSendFormat) + String(appIndexSourceTitle)).localized)
                //: } else {
            } else {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.modelEnd()
                //: TalkingSocketManager.shared.currentDataVC = nil
                PublishDataSocketDelegate.shared.currentDataVC = nil
            }
            //: self.popCurrentViewController()
            self.writeAt()

            //: } else if cmd == "onAcceptCall" && errorNo == 101 { // 接听通话时，余额不足
        } else if cmd == (dream_startStr.replacingOccurrences(of: "label", with: "o") + String(mainSizeSendData.suffix(6)) + "tCall") && errorNo == 101 { // 接听通话时，余额不足
            //: guard PathAppManager.share.appStatus == AppSkinStatus.normal.rawValue else { return }
            guard PathAppManager.share.appStatus == AssemblageHashable.normal.rawValue else { return }
            //: NamePushManager.share.func__jumpToWebRecharge(sufficient: false)
            NamePushManager.share.rechargeParams(sufficient: false)

            //: } else if cmd == "onAcceptCall" && errorNo == 102 { // 对方已取消通话
        } else if cmd == (dream_startStr.replacingOccurrences(of: "label", with: "o") + String(mainSizeSendData.suffix(6)) + "tCall") && errorNo == 102 { // 对方已取消通话
            //: self.func__showStatusBarErrorMsg(showMsg: msg)
            self.colorGift(showMsg: msg)
            //: self.popCurrentViewController()
            self.writeAt()
        }
    }
}

// MARK: - ContentFailObjectProtocol【通话按钮操作事件】

//: extension TalkingVoiceChatViewController: Talking1v1VoiceInteractionViewDelegate {
extension ListObserverDelegate: ContentFailObjectProtocol {
    /// 接听音频通话
    //: func interactionView_shouldAcceptCall() {
    func tableStartFile() {
        //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "acceptCall", "data": ["uid": self.chatModel?.pairUid]])
        PublishDataSocketDelegate.shared.birthday(info: [String(bytes: main_tableName.map{$0^42}, encoding: .utf8)!: (app_rangeText.lowercased() + "ptCall"), String(bytes: appImageContextTitle.map{inTo(of: $0)}, encoding: .utf8)!: [String(bytes: dream_analysisMsg.map{searchView(app: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
    }

    /// 拨打方结束通话
    //: func interactionView_shouldEndCall() {
    func includeBlock() {
        //: if self.chatModel?.videoStage == .Waiting {
        if self.chatModel?.videoStage == .Waiting { // 拨打方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.colorGift(showMsg: (String(main_colorIdent.prefix(4)) + "s are " + appViewName.lowercased() + "elled").localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
            PublishDataSocketDelegate.shared.birthday(info: [String(bytes: main_tableName.map{$0^42}, encoding: .utf8)!: (app_topWindowId.replacingOccurrences(of: "format", with: "el") + "Call"), String(bytes: appImageContextTitle.map{inTo(of: $0)}, encoding: .utf8)!: [String(bytes: k_indicatorMsg.map{atSquareKit(conversation: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: } else if self.chatModel?.videoStage == .Recieved {
        } else if self.chatModel?.videoStage == .Recieved { // 接收方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.colorGift(showMsg: (String(main_colorIdent.prefix(4)) + "s are " + appViewName.lowercased() + "elled").localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
            PublishDataSocketDelegate.shared.birthday(info: [String(bytes: main_tableName.map{$0^42}, encoding: .utf8)!: (String(dreamToName) + String(app_firstMessage.prefix(6))), String(bytes: appImageContextTitle.map{inTo(of: $0)}, encoding: .utf8)!: [String(bytes: dream_analysisMsg.map{searchView(app: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
            //: } else {
        } else { // 【通话中】
            //: self.func__showStatusBarErrorMsg(showMsg: "The call has ended".localized)
            self.colorGift(showMsg: (String(dream_equalMsg.prefix(5)) + String(main_objectFormat)).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
            PublishDataSocketDelegate.shared.birthday(info: [String(bytes: main_tableName.map{$0^42}, encoding: .utf8)!: (String(k_eventId.suffix(7)) + showAddIdent.replacingOccurrences(of: "equal", with: "l")), String(bytes: appImageContextTitle.map{inTo(of: $0)}, encoding: .utf8)!: [String(bytes: k_indicatorMsg.map{atSquareKit(conversation: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
        }

        //: self.interactionView.func_giftViewDismiss()
        self.interactionView.pointInTime()
        //: self.popCurrentViewController()
        self.writeAt()
    }

    /// 是否静音
    //: func interactionView_shouldChangeMute(isMute: Bool) {
    func weight(isMute: Bool) {
        //: self.voiceManager.setMute(isMute: isMute)
        self.voiceManager.enableDisplayMute(isMute: isMute)
    }

    /// 是否免提
    //: func interactionView_shouldChangeSpeaker(isHandsfree: Bool) {
    func handsfree(isHandsfree: Bool) {
        //: self.voiceManager.setHandsfree(isHandsfree: isHandsfree)
        self.voiceManager.setToMessage(isHandsfree: isHandsfree)
    }

    /// 展示语音通话小窗口
    //: func interactionView_shouldDisplayMiniWindow() {
    func filterApp() {
        //: TalkingSocketManager.shared.currentDataVC = self
        PublishDataSocketDelegate.shared.currentDataVC = self
        //: self.miniView = TalkingVoiceMiniView.buildVoiceMiniView()
        self.miniView = ConnectionMiniView.miniskirtView()
        //: self.miniView?.tapGestureBlock = { [weak self] in
        self.miniView?.tapGestureBlock = { [weak self] in // 还原回通话界面
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.currentViewController()?.navigationController?.pushViewController(self, animated: true)
            self.withTo()?.navigationController?.pushViewController(self, animated: true)
        }
        //: let talkingTime = self.interactionView.getCurrentTalkingTime()
        let talkingTime = self.interactionView.holderTime()
        //: self.miniView?.updateTalkingTime(talkingTime)
        self.miniView?.beautyCell(talkingTime)
        //: popCurrentViewController()
        writeAt()
    }

    /// 更新小窗口通话时间
    //: func interactionView_shouldUpdateTime(time: Double) {
    func caseTask(time: Double) {
        //: guard self.miniView != nil else { return }
        guard self.miniView != nil else { return }
        //: self.miniView?.updateTalkingTime(time)
        self.miniView?.beautyCell(time)
    }

    /// 收到礼物消息展示
    //: @objc func receiveGiftPlay(giftArr: Array<Any>) {
    @objc func pony(giftArr: [Any]) {
        //: interactionView.addGiftEffectModelArr(giftArr: giftArr)
        interactionView.mark(giftArr: giftArr)
    }
}

// MARK: - Layout

//: extension TalkingVoiceChatViewController {
extension ListObserverDelegate {
    /// 添加视图
    //: private func setupSubviews() {
    private func host() {
        //: self.view.addSubview(interactionView)
        self.view.addSubview(interactionView)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func setupOkConstraint() {
        //: interactionView.snp.makeConstraints { make in
        interactionView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
    }
}
