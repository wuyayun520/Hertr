
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let notiDataValue:[UInt8] = [0x56,0x5b,0x56,0x61,0x15,0x50,0x5c,0x51,0x52,0x5f,0x27,0x16,0xd,0x55,0x4e,0x60,0xd,0x5b,0x5c,0x61,0xd,0x4f,0x52,0x52,0x5b,0xd,0x56,0x5a,0x5d,0x59,0x52,0x5a,0x52,0x5b,0x61,0x52,0x51]

fileprivate func backFemale(to num: UInt8) -> UInt8 {
    let value = Int(num) - 237
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "icon_home_position" :*/
fileprivate let showCameraStr:String = "icon_temp type regular"
fileprivate let noti_nameFormat:String = "H"
fileprivate let dreamTaskPath:[Character] = ["o","m","e","_","p","o","s","i","t","i","o","n"]

/*: "btn_video_minimize" :*/
fileprivate let notiViewRangeId:String = "extension var andbtn_"
fileprivate let user_saveText:String = "dreaminidreamize"

/*: "btn_video_drop_nor" :*/
fileprivate let show_limitRenderData:[Character] = ["b","t","n","_","v","i","d","e","o","_","d"]
fileprivate let dreamTillVideoButtonPath:[Character] = ["r","o","p","_","n","o","r"]

/*: "btn_video_call_nor" :*/
fileprivate let showEqualMsg:[Character] = ["b","t","n","_","v","i","d","e","o","_","c","a","l","l"]
fileprivate let dreamReadKey:[Character] = ["_","n","o","r"]

/*: "transform.scale" :*/
fileprivate let mainInputId:[Character] = ["t","r","a","n","s","f"]
fileprivate let k_managerIdent:String = "orm.sfloat type info"

/*: "animation" :*/
fileprivate let dreamAddMsg:[Character] = ["a","n","i"]
fileprivate let noti_socialValue:[Character] = ["m","a","t","i","o","n"]

/*: "icon_videocall_female_answer_bg" :*/
fileprivate let m_screenName:[UInt8] = [0x67,0x62,0x5f,0x72,0x65,0x77,0x73,0x6e,0x61,0x5f,0x65,0x6c,0x61,0x6d,0x65,0x66,0x5f,0x6c,0x6c,0x61,0x63,0x6f,0x65,0x64,0x69,0x76,0x5f,0x6e,0x6f,0x63,0x69]

/*: "icon_videocall_female_answer" :*/
fileprivate let userReportMessage:[Character] = ["i","c","o","n","_","v","i","d","e","o","c","a","l","l","_","f","e","m","a","l","e","_","a","n","s","w","e","r"]

/*: "icon_calltalk_points" :*/
fileprivate let k_reasonKey:String = "icto"
fileprivate let kViewMsg:String = "alk_plet white"
fileprivate let main_modelTitle:String = "topints"

/*: "get json error" :*/
fileprivate let k_priceName:String = "get jump size"
fileprivate let main_videoKey:String = " errorshe back else self"

/*: "btn_video_turn_nor" :*/
fileprivate let app_rawUrl:[Character] = ["b","t","n","_","v","i","d","e","o","_","t","u","r"]
fileprivate let dreamViewStr:String = "n_norshow make let else guard"

/*: "btn_video_turn_pre" :*/
fileprivate let appLabelStr:String = "btn_vidto right model return view"
fileprivate let m_bagName:String = "color"
fileprivate let dreamBackFlushMessage:String = "o_tustring public equal fail"

/*: "btn_video_start_nor" :*/
fileprivate let mManagerPath:[Character] = ["b","t","n","_","v","i","d","e","o","_","s","t","a","r","t","_","n","o","r"]

/*: "btn_video_start_pre" :*/
fileprivate let user_streetFileMessage:String = "btn_else line action leading"
fileprivate let dreamCommentTextMsg:[Character] = ["a","r","t","_","p","r","e"]

/*: "icon_complaint" :*/
fileprivate let main_formatName:String = "itableon"
fileprivate let mainManagerTitle:String = "lalabelt"

/*: "showBullet" :*/
fileprivate let kSendId:String = "size skin checkshowBul"
fileprivate let noti_expectedMessage:String = "leapp"

/*: "icon_videoCall_coin_nor" :*/
fileprivate let kModelFormat:String = "voice imageicon_"
fileprivate let userColorName:String = "Call_coreturn object send self user"
fileprivate let notiSelectEqualData:String = "margin data guardin_nor"

/*: "btn_video_gift_nor" :*/
fileprivate let user_styleModelMsg:String = "btn_vicase path"
fileprivate let k_confirmData:String = "theor"

/*: "   " :*/
fileprivate let userCenterManagerMsg:String = "toptoptop"

/*: "Say something...     " :*/
fileprivate let mSelectedName:[Character] = ["S"]
fileprivate let m_needData:[Character] = ["a","y"," ","s","o","m","e","t","h","i","n","g",".",".","."," "," "," "," "," "]

/*: "waiting for the other party to accept the video invitation..." :*/
fileprivate let mainBlockId:[UInt8] = [0x6b,0x55,0x5d,0x68,0x5d,0x62,0x5b,0x14,0x5a,0x63,0x66,0x14,0x68,0x5c,0x59,0x14,0x63,0x68,0x5c,0x59,0x66,0x14,0x64,0x55,0x66,0x68,0x6d,0x14,0x68,0x63,0x14,0x55,0x57,0x57,0x59,0x64,0x68,0x14,0x68,0x5c,0x59,0x14,0x6a,0x5d,0x58,0x59,0x63,0x14,0x5d,0x62,0x6a,0x5d,0x68,0x55,0x68,0x5d,0x63,0x62,0x22,0x22,0x22]

fileprivate func contentStatus(view num: UInt8) -> UInt8 {
    let value = Int(num) - 244
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "Invite you to a video call..." :*/
fileprivate let appLineLabData:[Character] = ["I","n","v","i","t","e"," ","y","o","u"," ","t","o"," ","a"," ","v","i","d","e"]
fileprivate let k_toFormat:[Character] = ["o"," "]
fileprivate let dream_infoId:[Character] = ["c","a","l","l",".",".","."]

/*: "yyyy-MM-dd" :*/
fileprivate let k_mName:[Character] = ["y","y","y","y","-","M","M","-","d"]
fileprivate let app_addText:String = "video"

/*: "00:00" :*/
fileprivate let dreamReasonChangeToName:[Character] = ["0","0",":","0","0"]

/*: " +" :*/
fileprivate let k_successfullyBottomTitle:String = " current"

/*: "%@ points/min" :*/
fileprivate let dreamAutomaticallyHiddenMsg:String = "empty size%@ po"
fileprivate let dream_methodStr:[Character] = ["m","i","n"]

/*: "Camera must be on" :*/
fileprivate let showLeadingValue:String = "to live in makeCamera"
fileprivate let dream_locationMessage:String = "t local false"
fileprivate let dreamPointName:String = "content else message wrapbe on"

/*: "msgId" :*/
fileprivate let k_leadingMessage:[UInt8] = [0x64,0x49,0x67,0x73,0x6d]

/*: "senderUid" :*/
fileprivate let main_normalNetStr:[UInt8] = [0xa3,0xb5,0xbe,0xb4,0xb5,0xa2,0x85,0xb9,0xb4]

private func analogDigitalConverter(signature num: UInt8) -> UInt8 {
    return num ^ 208
}

/*: "toUid" :*/
fileprivate let main_runningIdent:String = "toUidlet image to"

/*: "nickname" :*/
fileprivate let noti_contentTitle:[UInt8] = [0x45,0x42,0x48,0x40,0x45,0x4a,0x46,0x4e]

/*: "content" :*/
fileprivate let app_sharedId:[UInt8] = [0x1f,0x2b,0x2a,0x30,0x21,0x2a,0x30]

fileprivate func layerMatch(count num: UInt8) -> UInt8 {
    let value = Int(num) + 68
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "isMySender" :*/
fileprivate let app_picId:[UInt8] = [0xf6,0x0,0xda,0x6,0xe0,0xf2,0xfb,0xf1,0xf2,0xff]

fileprivate func photoStyle(status num: UInt8) -> UInt8 {
    let value = Int(num) - 141
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "hasFace" :*/
fileprivate let mBurnLabelValue:String = "hcell"
fileprivate let mainTingKey:String = "table then return about selfsFace"

/*: "Automatically answer for you in %@s" :*/
fileprivate let mainManagerNameId:[UInt8] = [0x63,0x97,0x96,0x91,0x8f,0x83,0x96,0x8b,0x85,0x83,0x8e,0x8e,0x9b,0x42,0x83,0x90,0x95,0x99,0x87,0x94,0x42,0x88,0x91,0x94,0x42,0x9b,0x91,0x97,0x42,0x8b,0x90,0x42,0x47,0x62,0x95]

fileprivate func toThe(result num: UInt8) -> UInt8 {
    let value = Int(num) - 34
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "giftId" :*/
fileprivate let notiInfoMessage:[Character] = ["g","i","f","t","I","d"]

/*: "giftNum" :*/
fileprivate let mDataLogKey:String = "giftNunormal lab path target pic"
fileprivate let showLoadName:String = "M"

/*: "from" :*/
fileprivate let userQuantityKey:String = "fshowom"

/*: "videoCall" :*/
fileprivate let kLogId:String = "videindex"

/*: "programSort" :*/
fileprivate let user_succeedValue:[Character] = ["p","r","o","g","r","a"]
fileprivate let m_videoUrl:String = "if attributemSort"

/*: "pkgItemsetId" :*/
fileprivate let app_rankData:String = "pkgItwith with view view response"

/*: "msgInfo" :*/
fileprivate let kFirstIndexTitle:String = "tool gift name if tomsgInfo"

/*: "msgType" :*/
fileprivate let userEditNumberValue:[Character] = ["m"]
fileprivate let dreamControlKey:[Character] = ["s","g","T","y","p","e"]

/*: "gift" :*/
fileprivate let showWithTitle:String = "GIFT"

/*: "totalMfCoin" :*/
fileprivate let dreamShareUrl:String = "totalMview service view answer"
fileprivate let notiBackgroundUrl:[Character] = ["f","C","o","i","n"]

/*: "cellData" :*/
fileprivate let dreamShowImageStr:[UInt8] = [0x29,0x2f,0x26,0x26,0xe,0x2b,0x3e,0x2b]

private func equalFile(nose num: UInt8) -> UInt8 {
    return num ^ 74
}

/*: "%02i:%02i" :*/
fileprivate let mElementMsg:String = "top user%02i"

/*: "..." :*/
fileprivate let showPlayerName:String = "buttonbuttonbutton"

/*: ".." :*/
fileprivate let showCollectionUrl:String = ".."

/*: "FaceActionEvent_Interval_12_frame" :*/
fileprivate let noti_viewContent:[UInt8] = [0xe7,0xc0,0xc2,0xc4,0xe0,0xc2,0xd5,0xc8,0xce,0xcf,0xe4,0xd7,0xc4,0xcf,0xd5,0xfe,0xe8,0xcf,0xd5,0xc4,0xd3,0xd7,0xc0,0xcd,0xfe,0x90,0x93,0xfe,0xc7,0xd3,0xc0,0xcc,0xc4]

private func sizeAccountPin(user num: UInt8) -> UInt8 {
    return num ^ 161
}

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  ValueGrantViewDelegate.swift
//  AbroadTalking
//
//  Created by young on 2022/11/8.
//

//: import UIKit
import UIKit

//: protocol Talking1v1VideoInteractionViewDelegate: NSObjectProtocol {
protocol CannulateViewDelegate: NSObjectProtocol {
    /// 发送方结束通话
    //: func interactionView_shouldEndCall()
    func errorBy()

    /// 接听视频通话
    //: func interactionView_shouldAcceptCall()
    func nearTakeABow()

    /// 旋转摄像头
    /// - Parameter isFront: 是否前置（默认前置）
    //: func interactionView_shouldChangeCamera(isFront: Bool)
    func loadUp(isFront: Bool)

    /// 禁用/开启摄像头
    /// - Parameter forbidden: 是否禁用摄像头
    //: func interactionView_shouldForbiddenCamera(forbidden: Bool)
    func buttonKey(forbidden: Bool)

    /// 展示视频通话小窗口
    //: func interactionView_shouldDisplayMiniWindow()
    func displayForText()

    /// 投诉成功
    //: func interactionView_reportSucceed()
    func nameLoad()
}

//: class Talking1v1VideoInteractionView: UIView {
class ValueGrantViewDelegate: UIView {
    //: let place = (ScreenWidth-actualWidth(w: 45)*5)/6
    let place = (app_screenFailMarginFormat - actualWidth(w: 45) * 5) / 6
    //: weak var delegate: Talking1v1VideoInteractionViewDelegate?
    weak var delegate: CannulateViewDelegate?
    //: private var chatModel = TalkingVideoChatModel()
    private var chatModel = WithChatModel() // 引用类型，整个视频通话共用同一个model
    //: private var waitTimer: Timer?
    private var waitTimer: Timer? // 等待接听动画定时器
    //: private var startTime: TimeInterval?
    private var startTime: TimeInterval? // 开始的时间戳记 since1970
    //: private var countTimer: Timer?
    private var countTimer: Timer? // 通话时间定时器
    //: private var freeCountTimer: Timer?
    private var freeCountTimer: Timer? // 免费通话剩余时间定时器
//    var isShowRecharge  = false                         // 是否展示充值页中
    //: var isShowInput  = false
    var isShowInput = false // 是否在聊天中
    //: private var autoAcceptTimer: DispatchSourceTimer?
    private var autoAcceptTimer: DispatchSourceTimer? // 自动接听定时器

    //: init(frame: CGRect, chatModel: TalkingVideoChatModel?) {
    init(frame: CGRect, chatModel: WithChatModel?) {
        //: super.init(frame: frame)
        super.init(frame: frame)
        //: setupSubviews()
        equalSave()
        //: setupSubViewsConstraint()
        third()
        //: if chatModel != nil {
        if chatModel != nil {
            //: self.chatModel = chatModel!
            self.chatModel = chatModel!
            //: self.startTime = chatModel!.startTime
            self.startTime = chatModel!.startTime
        }
        //: showViewsViaStage()
        sendTarget()
        //: bindInteraction()
        holdFast()
        //: initNotificationObserver()
        meCancel()
        //: initCachEffectData()
        dataIconTap()
        //: isShowHangupBtn()
        putDoing()

        //: TalkingVideoDanmuManager.shared().delegate = self
        TvThen.signaling().delegate = self
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: notiDataValue.map{backFemale(to: $0)}, encoding: .utf8)!)
    }

    //: deinit {
    deinit {
        //: if waitTimer != nil {
        if waitTimer != nil {
            //: waitTimer?.invalidate()
            waitTimer?.invalidate()
            //: waitTimer = nil
            waitTimer = nil
        }
        //: if countTimer != nil {
        if countTimer != nil {
            //: countTimer?.invalidate()
            countTimer?.invalidate()
            //: countTimer = nil
            countTimer = nil
        }
        //: if freeCountTimer != nil {
        if freeCountTimer != nil {
            //: freeCountTimer?.invalidate()
            freeCountTimer?.invalidate()
            //: freeCountTimer = nil
            freeCountTimer = nil
        }
        //: cancelAutoAcceptTimer()
        activity()
        //: player.stopPlay()
        player.unblock()
        //: invalidateIdleTimer()
        care()
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }

    // MARK: - Lazy Load

    // 点击展示/隐藏交互按钮
    //: private lazy var touchHidenBtn: UIButton = {
    private lazy var touchHidenBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.addTarget(self, action: #selector(touchHidenBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(exceptFinish), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    // 接收方头像背景
    //: private lazy var pairInfoBg: UIView = {
    private lazy var pairInfoBg: UIView = {
        //: let v = UIView()
        let v = UIView()
        //: v.backgroundColor = UIColor(white: 0, alpha: 0.4)
        v.backgroundColor = UIColor(white: 0, alpha: 0.4)
        //: v.layer.cornerRadius = actualWidth(w: 50)/2
        v.layer.cornerRadius = actualWidth(w: 50) / 2
        //: v.layer.masksToBounds = true
        v.layer.masksToBounds = true
        //: return v
        return v
        //: }()
    }()

    //: private lazy var locationBtn: TalkingButton = {
    private lazy var locationBtn: TelevisionButton = {
        //: let btn = TalkingButton()
        let btn = TelevisionButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_home_position"), for: .normal)
        btn.setImage(UIImage.sendCollection(name: (String(showCameraStr.prefix(5)) + noti_nameFormat.lowercased() + String(dreamTaskPath))), for: .normal)
        //: btn.spaceBetweenTitleAndImage = 2
        btn.spaceBetweenTitleAndImage = 2
        //: btn.setTitleColor(.white, for: .normal)
        btn.setTitleColor(.white, for: .normal)
        //: btn.titleLabel?.font = UIFont.pingfangRugularFont(fontSize: 12)
        btn.titleLabel?.font = UIFont.indicatorSize(fontSize: 12)
        //: btn.contentHorizontalAlignment = LanguageManager.shared.direction == .rightToLeft ? .right:.left
        btn.contentHorizontalAlignment = PresentReactiveCompatible.shared.direction == .rightToLeft ? .right : .left
        //: return btn
        return btn
        //: }()
    }()

    // 接收方头像
    //: private lazy var pairPicV: UIImageView = {
    private lazy var pairPicV: UIImageView = {
        //: let v = UIImageView()
        let v = UIImageView()
        //: v.contentMode = .scaleAspectFill
        v.contentMode = .scaleAspectFill
        //: v.isUserInteractionEnabled = true
        v.isUserInteractionEnabled = true
        //: v.layer.cornerRadius = actualWidth(w: 40)/2
        v.layer.cornerRadius = actualWidth(w: 40) / 2
        //: v.layer.masksToBounds = true
        v.layer.masksToBounds = true
        //: v.layer.borderWidth = 1
        v.layer.borderWidth = 1
        //: v.layer.borderColor = UIColor.white.cgColor
        v.layer.borderColor = UIColor.white.cgColor
        //: return v
        return v
        //: }()
    }()

    // 接收方昵称
    //: private lazy var pairNameLab: UILabel = {
    private lazy var pairNameLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .white
        lab.textColor = .white
        //: lab.font = UIFont.pingfangMediumFont(fontSize: 17)
        lab.font = UIFont.upperClassMove(fontSize: 17)
        //: return lab
        return lab
        //: }()
    }()

    // 呼叫中描述
    //: private lazy var desLab: UILabel = {
    private lazy var desLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .white
        lab.textColor = .white
        //: lab.font = UIFont.pingfangRugularFont(fontSize: 17)
        lab.font = UIFont.indicatorSize(fontSize: 17)
        //: lab.numberOfLines = 0
        lab.numberOfLines = 0
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: return lab
        return lab
        //: }()
    }()

    // 最小化按钮
    //: private lazy var minimizeBtn: UIButton = {
    private lazy var minimizeBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "btn_video_minimize"), for: .normal)
        btn.setImage(UIImage.sendCollection(name: (String(notiViewRangeId.suffix(4)) + "video_" + user_saveText.replacingOccurrences(of: "dream", with: "m"))), for: .normal)
        //: btn.addTarget(self, action: #selector(minimizeButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(completeDelete), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    // 挂断/取消 按钮
    //: private lazy var hangupBtn: UIButton = {
    private lazy var hangupBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "btn_video_drop_nor"), for: .normal)
        btn.setImage(UIImage.sendCollection(name: (String(show_limitRenderData) + String(dreamTillVideoButtonPath))), for: .normal)
        //: btn.addTarget(self, action: #selector(hangupButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(pieceOfWriting), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    // 接听按钮
    //: private lazy var acceptBtn: UIButton = {
    private lazy var acceptBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "btn_video_call_nor"), for: .normal)
        btn.setImage(UIImage.sendCollection(name: (String(showEqualMsg) + String(dreamReadKey))), for: .normal)
        //: btn.addTarget(self, action: #selector(acceptButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(itemView), for: .touchUpInside)

        // 添加关键帧动画
        //: let keyAnimation = CAKeyframeAnimation()
        let keyAnimation = CAKeyframeAnimation()
        //: keyAnimation.keyPath = "transform.scale"
        keyAnimation.keyPath = (String(mainInputId) + String(k_managerIdent.prefix(5)) + "cale")
        //: keyAnimation.values = [NSValue(cgAffineTransform: CGAffineTransform(scaleX: 1, y: 1)),
        keyAnimation.values = [NSValue(cgAffineTransform: CGAffineTransform(scaleX: 1, y: 1)),
                               //: NSValue(cgAffineTransform: CGAffineTransform(scaleX: actualWidth(w: 80/70), y: actualWidth(w: 80/70))),
                               NSValue(cgAffineTransform: CGAffineTransform(scaleX: actualWidth(w: 80 / 70), y: actualWidth(w: 80 / 70))),
                               //: NSValue(cgAffineTransform: CGAffineTransform(scaleX: 1, y: 1))]
                               NSValue(cgAffineTransform: CGAffineTransform(scaleX: 1, y: 1))]
        //: keyAnimation.duration = 1.2
        keyAnimation.duration = 1.2
        //: keyAnimation.repeatCount = MAXFLOAT
        keyAnimation.repeatCount = MAXFLOAT
        //: keyAnimation.isRemovedOnCompletion = false
        keyAnimation.isRemovedOnCompletion = false
        //: keyAnimation.fillMode = .both
        keyAnimation.fillMode = .both
        //: keyAnimation.calculationMode = .cubicPaced
        keyAnimation.calculationMode = .cubicPaced
        //: btn.layer.add(keyAnimation, forKey: "animation")
        btn.layer.add(keyAnimation, forKey: (String(dreamAddMsg) + String(noti_socialValue)))

        //: return btn
        return btn
        //: }()
    }()

    // 女性接听按钮
    //: private lazy var femaleAcceptBGBtn: UIButton = {
    private lazy var femaleAcceptBGBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setBackgroundImage(UIImage.BundleImageNamed(name: "icon_videocall_female_answer_bg"), for: .normal)
        btn.setBackgroundImage(UIImage.sendCollection(name: String(bytes: m_screenName.reversed(), encoding: .utf8)!), for: .normal)
        // 添加关键帧动画
        //: let keyAnimation = CAKeyframeAnimation()
        let keyAnimation = CAKeyframeAnimation()
        //: keyAnimation.keyPath = "transform.scale"
        keyAnimation.keyPath = (String(mainInputId) + String(k_managerIdent.prefix(5)) + "cale")
        //: keyAnimation.values = [NSValue(cgAffineTransform: CGAffineTransform(scaleX: 1, y: 1)),
        keyAnimation.values = [NSValue(cgAffineTransform: CGAffineTransform(scaleX: 1, y: 1)),
                               //: NSValue(cgAffineTransform: CGAffineTransform(scaleX: 1.02, y: 1.02)),
                               NSValue(cgAffineTransform: CGAffineTransform(scaleX: 1.02, y: 1.02)),
                               //: NSValue(cgAffineTransform: CGAffineTransform(scaleX: 0.95, y: 0.95))]
                               NSValue(cgAffineTransform: CGAffineTransform(scaleX: 0.95, y: 0.95))]
        //: keyAnimation.duration = 0.6
        keyAnimation.duration = 0.6
        //: keyAnimation.repeatCount = MAXFLOAT
        keyAnimation.repeatCount = MAXFLOAT
        //: keyAnimation.isRemovedOnCompletion = false
        keyAnimation.isRemovedOnCompletion = false
        //: keyAnimation.fillMode = .both
        keyAnimation.fillMode = .both
        //: keyAnimation.calculationMode = .cubicPaced
        keyAnimation.calculationMode = .cubicPaced
        //: btn.layer.add(keyAnimation, forKey: "animation")
        btn.layer.add(keyAnimation, forKey: (String(dreamAddMsg) + String(noti_socialValue)))
        //: return btn
        return btn
        //: }()
    }()

    // 女性接听按钮
    //: private lazy var femaleAcceptBtn: UIButton = {
    private lazy var femaleAcceptBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_videocall_female_answer"), for: .normal)
        btn.setImage(UIImage.sendCollection(name: (String(userReportMessage))), for: .normal)
        //: btn.addTarget(self, action: #selector(acceptButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(itemView), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    // 女性接听积分提示
    //: private lazy var femaleAcceptTipsBtn: TalkingButton = {
    private lazy var femaleAcceptTipsBtn: TelevisionButton = {
        //: let btn = TalkingButton()
        let btn = TelevisionButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_calltalk_points"), for: .normal)
        btn.setImage(UIImage.sendCollection(name: (k_reasonKey.replacingOccurrences(of: "to", with: "on") + "_callt" + String(kViewMsg.prefix(5)) + main_modelTitle.replacingOccurrences(of: "top", with: "o"))), for: .normal)
        //: btn.setTitleColor(.white, for: .normal)
        btn.setTitleColor(.white, for: .normal)
        //: btn.titleLabel?.font = UIFont.pingfangRugularFont(fontSize: 15)
        btn.titleLabel?.font = UIFont.indicatorSize(fontSize: 15)
        //: btn.isUserInteractionEnabled = false
        btn.isUserInteractionEnabled = false
        //: return btn
        return btn
        //: }()
    }()

    //: lazy var acceptPlayer: SVGAPlayer = {
    lazy var acceptPlayer: SVGAPlayer = {
        //: let player = SVGAPlayer.init()
        let player = SVGAPlayer()
        //: player.loops = 0
        player.loops = 0
        //: player.clearsAfterStop = false
        player.clearsAfterStop = false
        //: player.isUserInteractionEnabled = false
        player.isUserInteractionEnabled = false
        //: player.contentMode = .scaleAspectFill
        player.contentMode = .scaleAspectFill
        //: let url = SVGAEffectTool.default.getZipEffectPath(type: .CallTalk_Accept)
        let url = ReverberationInsideHoeHandleReactiveCompatible.default.pullInConfirmType(type: .CallTalk_Accept)
        //: do {
        do {
            //: let data = try Data(contentsOf: url)
            let data = try Data(contentsOf: url)
            //: let parser = SVGAParser.init()
            let parser = SVGAParser()
            //: parser.parse(with: data, cacheKey: "") { [weak self] videoItem in
            parser.parse(with: data, cacheKey: "") { [weak self] videoItem in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: player.videoItem = videoItem
                player.videoItem = videoItem
                //: player.startAnimation()
                player.startAnimation()
            }
            //: } catch {
        } catch {
            //: printLog(message: "get json error")
            printLog(message: (String(k_priceName.prefix(4)) + "json" + String(main_videoKey.prefix(6))))
        }
        //: if LanguageManager.shared.direction == .rightToLeft {
        if PresentReactiveCompatible.shared.direction == .rightToLeft {
            //: player.transform = CGAffineTransform(scaleX: -1, y: 1)
            player.transform = CGAffineTransform(scaleX: -1, y: 1)
        }
        //: return player
        return player
        //: }()
    }()

    // 旋转摄像头按钮
    //: private lazy var rotateCameraBtn: UIButton = {
    private lazy var rotateCameraBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "btn_video_turn_nor"), for: .normal)
        btn.setImage(UIImage.sendCollection(name: (String(app_rawUrl) + String(dreamViewStr.prefix(5)))), for: .normal)
        //: btn.setImage(UIImage.BundleImageNamed(name: "btn_video_turn_pre"), for: .selected)
        btn.setImage(UIImage.sendCollection(name: (String(appLabelStr.prefix(7)) + m_bagName.replacingOccurrences(of: "color", with: "e") + String(dreamBackFlushMessage.prefix(4)) + "rn_pre")), for: .selected)
        //: btn.addTarget(self, action: #selector(rotateCameraButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(betweenCandidCamera), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    // 开启/关闭摄像头按钮
    //: private lazy var openCameraBtn: UIButton = {
    private lazy var openCameraBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "btn_video_start_nor"), for: .normal)
        btn.setImage(UIImage.sendCollection(name: (String(mManagerPath))), for: .normal)
        //: btn.setImage(UIImage.BundleImageNamed(name: "btn_video_start_pre"), for: .selected)
        btn.setImage(UIImage.sendCollection(name: (String(user_streetFileMessage.prefix(4)) + "video_st" + String(dreamCommentTextMsg))), for: .selected)
        //: btn.addTarget(self, action: #selector(openCameraButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(afterFile), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    // 投诉按钮（男性）
    //: private lazy var reportBtn: UIButton = {
    private lazy var reportBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_complaint"), for: .normal)
        btn.setImage(UIImage.sendCollection(name: (main_formatName.replacingOccurrences(of: "table", with: "c") + "_comp" + mainManagerTitle.replacingOccurrences(of: "label", with: "in"))), for: .normal)
        //: btn.addTarget(self, action: #selector(reportButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(informInsideClick), for: .touchUpInside)
        //: let data = JSON(PathAppManager.share.appConfigMode.videoReport)
        let data = JSON(PathAppManager.share.appConfigMode.videoReport)
        //: if data["showBullet"].boolValue == true {
        if data[(String(kSendId.suffix(7)) + noti_expectedMessage.replacingOccurrences(of: "app", with: "t"))].boolValue == true {
            //: btn.isEnabled = true
            btn.isEnabled = true
            //: } else {
        } else {
            //: btn.isEnabled = false
            btn.isEnabled = false
        }
        //: return btn
        return btn
        //: }()
    }()

    // 充值按钮
    //: private lazy var rechargeBtn: UIButton = {
    private lazy var rechargeBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_videoCall_coin_nor"), for: .normal)
        btn.setImage(UIImage.sendCollection(name: (String(kModelFormat.suffix(5)) + "video" + String(userColorName.prefix(7)) + String(notiSelectEqualData.suffix(6)))), for: .normal)
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_videoCall_coin_nor"), for: .selected)
        btn.setImage(UIImage.sendCollection(name: (String(kModelFormat.suffix(5)) + "video" + String(userColorName.prefix(7)) + String(notiSelectEqualData.suffix(6)))), for: .selected)
        //: btn.addTarget(self, action: #selector(rechargeBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(imageNeed), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    // 送礼按钮
    //: private lazy var sendGiftBtn: UIButton = {
    private lazy var sendGiftBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "btn_video_gift_nor"), for: .normal)
        btn.setImage(UIImage.sendCollection(name: (String(user_styleModelMsg.prefix(6)) + "deo_gift_" + k_confirmData.replacingOccurrences(of: "the", with: "n"))), for: .normal)
        //: btn.setImage(UIImage.BundleImageNamed(name: "btn_video_gift_nor"), for: .selected)
        btn.setImage(UIImage.sendCollection(name: (String(user_styleModelMsg.prefix(6)) + "deo_gift_" + k_confirmData.replacingOccurrences(of: "the", with: "n"))), for: .selected)
        //: btn.addTarget(self, action: #selector(sendGiftButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(wishTurn), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var timeLab: UILabel = {
    private lazy var timeLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .white
        lab.textColor = .white
        //: lab.font = UIFont.pingfangMediumFont(fontSize: 18)
        lab.font = UIFont.upperClassMove(fontSize: 18)
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var freeTimeView: TalkingFreeCallRechargeTipsView = {
    private lazy var freeTimeView: RenderTipsView = {
        //: let view = TalkingFreeCallRechargeTipsView()
        let view = RenderTipsView()
        //: view.backgroundColor = .clear
        view.backgroundColor = .clear
        //: view.touchRechargeBtnBlock = { [weak self] in
        view.touchRechargeBtnBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
//            self.isShowRecharge = true
        }
        //: return view
        return view
        //: }()
    }()

    //: lazy var giftView: TalkingChatGiftView = {
    lazy var giftView: SunnaThen = {
        //: let giftV = TalkingChatGiftView.init(style: .call)
        let giftV = SunnaThen(style: .call)
        //: giftV.frame = CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight)
        giftV.frame = CGRect(x: 0, y: 0, width: app_screenFailMarginFormat, height: mainAcceptKey)
        //: return giftV
        return giftV
        //: }()
    }()

    //: lazy var giftEffectView: TalkingPrivateChatAnimatView = {
    lazy var giftEffectView: RenderThen = {
        //: let effectView = TalkingPrivateChatAnimatView()
        let effectView = RenderThen()
        //: effectView.contentMode = .scaleAspectFill
        effectView.contentMode = .scaleAspectFill
        //: return effectView
        return effectView
        //: }()
    }()

    //: lazy var giftTrackView: TalkingGiftTrackView = {
    lazy var giftTrackView: CustomView = {
        //: let trackView = TalkingGiftTrackView()
        let trackView = CustomView()
        //: trackView.isUserInteractionEnabled = false
        trackView.isUserInteractionEnabled = false
        //: return trackView
        return trackView
        //: }()
    }()

    //: lazy var pointAnimView: TalkingPointAnimationView = {
    lazy var pointAnimView: LevelReactiveCompatible = {
        //: let pointView = TalkingPointAnimationView()
        let pointView = LevelReactiveCompatible()
        //: pointView.isUserInteractionEnabled = false
        pointView.isUserInteractionEnabled = false
        //: return pointView
        return pointView
        //: }()
    }()

    // 视频播放器
    //: private lazy var player: TalkingVideoPlayerManager = {
    private lazy var player: TincturePlayListener = {
        //: let player = TalkingVideoPlayerManager()
        let player = TincturePlayListener()
        //: player.setMute(bEnable: true)
        player.viewMute(bEnable: true)
        //: return player
        return player
        //: }()
    }()

    // 视频封面
    //: private lazy var coverImg: UIImageView = {
    private lazy var coverImg: UIImageView = {
        //: let v = UIImageView()
        let v = UIImageView()
        //: v.contentMode = .scaleAspectFill
        v.contentMode = .scaleAspectFill
        //: v.isUserInteractionEnabled = true
        v.isUserInteractionEnabled = true
        //: v.isHidden = true
        v.isHidden = true
        //: return v
        return v
        //: }()
    }()

    /// 1.7.2 聊天消息
    //: private lazy var chatBtn: UIButton = {
    private lazy var chatBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.backgroundColor = UIColor.init(white: 0, alpha: 0.25)
        btn.backgroundColor = UIColor(white: 0, alpha: 0.25)
        //: btn.layer.cornerRadius = 17
        btn.layer.cornerRadius = 17
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        //: btn.setTitle("   "+"Say something...     ".localized, for: .normal)
        btn.setTitle("   " + (String(mSelectedName) + String(m_needData)).localized, for: .normal)
        //: btn.titleLabel?.font = UIFont.pingfangRugularFont(fontSize: 14)
        btn.titleLabel?.font = UIFont.indicatorSize(fontSize: 14)
        //: btn.setTitleColor(UIColor.init(white: 1, alpha: 0.8), for: .normal)
        btn.setTitleColor(UIColor(white: 1, alpha: 0.8), for: .normal)
        //: btn.contentHorizontalAlignment = LanguageManager.shared.direction == .rightToLeft ? .right:.left
        btn.contentHorizontalAlignment = PresentReactiveCompatible.shared.direction == .rightToLeft ? .right : .left
        //: btn.addTarget(self, action: #selector(commentBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(inside), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var danmuView: TalkingVideoDanmuMsgListTableView = {
    private lazy var danmuView: DiskTableView = {
        //: let view = TalkingVideoDanmuMsgListTableView.init()
        let view = DiskTableView()
        //: view.touchTableGesblock = { [weak self] in
        view.touchTableGesblock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.touchHidenBtnClick()
            self.exceptFinish()
        }
        //: return view
        return view
        //: }()
    }()

    //: private lazy var commInputView: TalkingVideoCallInputView = {
    private lazy var commInputView: ListModelReactiveCompatible = {
        //: let text = TalkingVideoCallInputView.init()
        let text = ListModelReactiveCompatible()
        //: text.delegate = self
        text.delegate = self
        //: text.isHidden = true
        text.isHidden = true
        //: return text
        return text
        //: }()
    }()

    // 自动接听文案描述（女性）
    //: private lazy var autoAcceptDesLab: UILabel = {
    private lazy var autoAcceptDesLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .white
        lab.textColor = .white
        //: lab.font = UIFont.pingfangRugularFont(fontSize: 17)
        lab.font = UIFont.indicatorSize(fontSize: 17)
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: return lab
        return lab
        //: }()
    }()
}

// MARK: - Event

//: extension Talking1v1VideoInteractionView {
extension ValueGrantViewDelegate {
    /// 展示视图状态
    //: func showViewsViaStage() {
    func sendTarget() {
        //: switch chatModel.videoStage {
        switch chatModel.videoStage {
        //: case .Waiting:
        case .Waiting:
            //: desLab.text = "waiting for the other party to accept the video invitation...".localized
            desLab.text = String(bytes: mainBlockId.map{contentStatus(view: $0)}, encoding: .utf8)!.localized
            //: desLab.isHidden = false
            desLab.isHidden = false
            //: pairInfoBg.isHidden = false
            pairInfoBg.isHidden = false
            //: minimizeBtn.isHidden = true
            minimizeBtn.isHidden = true
            //: hangupBtn.isHidden = false
            hangupBtn.isHidden = false
            //: acceptBtn.isHidden = true
            acceptBtn.isHidden = true
            //: femaleAcceptBtn.isHidden = true
            femaleAcceptBtn.isHidden = true
            //: femaleAcceptTipsBtn.isHidden = true
            femaleAcceptTipsBtn.isHidden = true
            //: acceptPlayer.isHidden = true
            acceptPlayer.isHidden = true
            //: femaleAcceptBGBtn.isHidden = true
            femaleAcceptBGBtn.isHidden = true
            //: if PathAppManager.share.loginUserMode.sex == Gender.male.rawValue {
            if PathAppManager.share.loginUserMode.sex == ConnectionClusterLiteral.male.rawValue {
                //: rotateCameraBtn.isHidden = true
                rotateCameraBtn.isHidden = true
                //: openCameraBtn.isHidden = true
                openCameraBtn.isHidden = true
            }
            //: reportBtn.isHidden = true
            reportBtn.isHidden = true
            //: sendGiftBtn.isHidden = true
            sendGiftBtn.isHidden = true
            //: rechargeBtn.isHidden = true
            rechargeBtn.isHidden = true
            //: touchHidenBtn.isEnabled = false
            touchHidenBtn.isEnabled = false
            //: timeLab.isHidden = true
            timeLab.isHidden = true
            //: coverImg.isHidden = false
            coverImg.isHidden = false
            //: chatBtn.isHidden = true
            chatBtn.isHidden = true
            //: danmuView.isHidden = true
            danmuView.isHidden = true
            //: autoAcceptDesLab.isHidden = true
            autoAcceptDesLab.isHidden = true
        //: case .Recieved:
        case .Recieved:
            //: desLab.text = "Invite you to a video call...".localized
            desLab.text = (String(appLineLabData) + String(k_toFormat) + String(dream_infoId)).localized
            //: desLab.isHidden = false
            desLab.isHidden = false
            //: pairInfoBg.isHidden = false
            pairInfoBg.isHidden = false
            //: minimizeBtn.isHidden = true
            minimizeBtn.isHidden = true
            //: hangupBtn.isHidden = false
            hangupBtn.isHidden = false
            //: autoAcceptDesLab.isHidden = true
            autoAcceptDesLab.isHidden = true
            //: if PathAppManager.share.loginUserMode.sex == Gender.male.rawValue {
            if PathAppManager.share.loginUserMode.sex == ConnectionClusterLiteral.male.rawValue {
                //: acceptBtn.isHidden = false
                acceptBtn.isHidden = false
                //: femaleAcceptBtn.isHidden = true
                femaleAcceptBtn.isHidden = true
                //: femaleAcceptTipsBtn.isHidden = true
                femaleAcceptTipsBtn.isHidden = true
                //: acceptPlayer.isHidden = true
                acceptPlayer.isHidden = true
                //: femaleAcceptBGBtn.isHidden = true
                femaleAcceptBGBtn.isHidden = true
                //: rotateCameraBtn.isHidden = true
                rotateCameraBtn.isHidden = true
                //: openCameraBtn.isHidden = true
                openCameraBtn.isHidden = true
                //: } else {
            } else {
                //: acceptBtn.isHidden = true
                acceptBtn.isHidden = true
                //: femaleAcceptBtn.isHidden = false
                femaleAcceptBtn.isHidden = false
                //: femaleAcceptTipsBtn.isHidden = false
                femaleAcceptTipsBtn.isHidden = false
                //: femaleAcceptBGBtn.isHidden = false
                femaleAcceptBGBtn.isHidden = false
                //: let date = Defaults.object(forKey: TalkingCallShowTag)
                let date = dreamBottomMsg.object(forKey: app_clickTitle)
                //: let today = NSDate.getTimeString(date: Date(), dateFormat: "yyyy-MM-dd")
                let today = NSDate.contentWith(date: Date(), dateFormat: (String(k_mName) + app_addText.replacingOccurrences(of: "video", with: "d")))
                //: if date == nil || (date as! String) != today {
                if date == nil || (date as! String) != today {
                    //: Defaults.set(today, forKey: TalkingCallShowTag)
                    dreamBottomMsg.set(today, forKey: app_clickTitle)
                    //: acceptPlayer.isHidden = false
                    acceptPlayer.isHidden = false
                    //: } else {
                } else {
                    //: acceptPlayer.isHidden = true
                    acceptPlayer.isHidden = true
                }
                //: autoAcceptDesLab.isHidden = false
                autoAcceptDesLab.isHidden = false
                // 直播时自动接听
                //: if PathAppManager.share.appConfigMode.autoAnswer == 2 &&
                if PathAppManager.share.appConfigMode.autoAnswer == 2 &&
                    //: NEED_RESTORE_LIVE == true {
                    dream_marginName == true
                {
                    //: showOrHidenAutoRecieved(autoAccept: true)
                    makeAccept(autoAccept: true)
                    //: } else {
                } else {
                    //: showOrHidenAutoRecieved()
                    makeAccept()
                }
            }
            //: reportBtn.isHidden = true
            reportBtn.isHidden = true
            //: sendGiftBtn.isHidden = true
            sendGiftBtn.isHidden = true
            //: rechargeBtn.isHidden = true
            rechargeBtn.isHidden = true
            //: touchHidenBtn.isEnabled = false
            touchHidenBtn.isEnabled = false
            //: timeLab.isHidden = true
            timeLab.isHidden = true
            //: coverImg.isHidden = false
            coverImg.isHidden = false
            //: chatBtn.isHidden = true
            chatBtn.isHidden = true
            //: danmuView.isHidden = true
            danmuView.isHidden = true
        //: case .Calling:
        case .Calling:
            //: desLab.text = nil
            desLab.text = nil
            //: desLab.isHidden = true
            desLab.isHidden = true
            //: pairInfoBg.isHidden = true
            pairInfoBg.isHidden = true
            //: minimizeBtn.isHidden = false
            minimizeBtn.isHidden = false
            //: hangupBtn.isHidden = false
            hangupBtn.isHidden = false
            //: acceptBtn.isHidden = true
            acceptBtn.isHidden = true
            //: femaleAcceptBtn.isHidden = true
            femaleAcceptBtn.isHidden = true
            //: femaleAcceptTipsBtn.isHidden = true
            femaleAcceptTipsBtn.isHidden = true
            //: acceptPlayer.isHidden = true
            acceptPlayer.isHidden = true
            //: femaleAcceptBGBtn.isHidden = true
            femaleAcceptBGBtn.isHidden = true
            //: rotateCameraBtn.isHidden = false
            rotateCameraBtn.isHidden = false
            //: openCameraBtn.isHidden = false
            openCameraBtn.isHidden = false
            //: reportBtn.isHidden = !reportBtn.isEnabled || PathAppManager.share.loginUserMode.sex == Gender.female.rawValue
            reportBtn.isHidden = !reportBtn.isEnabled || PathAppManager.share.loginUserMode.sex == ConnectionClusterLiteral.female.rawValue
            //: sendGiftBtn.isHidden = false
            sendGiftBtn.isHidden = false
            //: rechargeBtn.isHidden = false
            rechargeBtn.isHidden = false
            //: touchHidenBtn.isEnabled = true
            touchHidenBtn.isEnabled = true
            //: timeLab.isHidden = false
            timeLab.isHidden = false
            //: chatBtn.isHidden = false
            chatBtn.isHidden = false
            //: danmuView.isHidden = false
            danmuView.isHidden = false
            //: autoAcceptDesLab.isHidden = true
            autoAcceptDesLab.isHidden = true
            //: if startTime != nil {
            if startTime != nil {
                //: changeTalkTime()
                doTitle()
                //: } else {
            } else {
                //: timeLab.text = "00:00"
                timeLab.text = (String(dreamReasonChangeToName))
            }
            //: startTalkCount()
            ptolemaicSystem()
            //: touchHiddenTimer()
            suspension()
            //: if TalkingSocketManager.shared.isFreeCall {
            if PublishDataSocketDelegate.shared.isFreeCall {
                //: setFreeView()
                showAttention()
                //: setFreeCountTimer()
                halfClick()
            }
            //: if reportBtn.isHidden {
            if reportBtn.isHidden {
                //: sendGiftBtn.snp.remakeConstraints { make in
                sendGiftBtn.snp.remakeConstraints { make in
                    //: make.trailing.equalToSuperview().offset(-10)
                    make.trailing.equalToSuperview().offset(-10)
                    //: make.bottom.equalTo(reportBtn)
                    make.bottom.equalTo(reportBtn)
                    //: make.size.equalTo(reportBtn)
                    make.size.equalTo(reportBtn)
                }
            }
        }

        //: updateLayout()
        comeAbout()
        //: afterGetInfo()
        dataItem()
    }

    /// 刷新通话视图数据
    //: func afterGetInfo() {
    func dataItem() {
        //: pairNameLab.text = chatModel.pairNickName
        pairNameLab.text = chatModel.pairNickName
        //: locationBtn.isHidden = chatModel.location.count <= 0
        locationBtn.isHidden = chatModel.location.count <= 0
        //: locationBtn.setTitle("\(chatModel.location)", for: .normal)
        locationBtn.setTitle("\(chatModel.location)", for: .normal)
        //: femaleAcceptTipsBtn.setTitle(" +" + "%@ points/min".localizedArguments(chatModel.points), for: .normal)
        femaleAcceptTipsBtn.setTitle(" +" + (String(dreamAutomaticallyHiddenMsg.suffix(5)) + "ints/" + String(dream_methodStr)).equalArguments(chatModel.points), for: .normal)
        //: let optionsInfo = [KingfisherOptionsInfoItem.processor(RoundCornerImageProcessor(cornerRadius: actualWidth(w: 20)))]
        let optionsInfo = [KingfisherOptionsInfoItem.processor(RoundCornerImageProcessor(cornerRadius: actualWidth(w: 20)))]
        //: pairPicV.setUrlImage(urlStr: chatModel.pairHeadPic, options: optionsInfo)
        pairPicV.sumercalate(urlStr: chatModel.pairHeadPic, options: optionsInfo)

        // 男性展示视频封面 【非通话中】
        //: if PathAppManager.share.loginUserMode.sex == Gender.male.rawValue {
        if PathAppManager.share.loginUserMode.sex == ConnectionClusterLiteral.male.rawValue {
            //: coverImg.isHidden = false
            coverImg.isHidden = false
            //: coverImg.setUrlImage(urlStr: chatModel.pairHeadPic)
            coverImg.glycerolizeNeed(urlStr: chatModel.pairHeadPic)
            //: if chatModel.videoStage != .Calling, chatModel.videoUrl.count > 0 {
            if chatModel.videoStage != .Calling, chatModel.videoUrl.count > 0 {
                //: player.delegate = self
                player.delegate = self
                //: player.playerWithUrlAndVideoView(url: chatModel.videoUrl, view: self)
                player.backstop(url: chatModel.videoUrl, view: self)
            }
        }
        //: if chatModel.videoStage == .Calling {
        if chatModel.videoStage == .Calling {
            //: coverImg.isHidden = true
            coverImg.isHidden = true
            //: player.stopPlay()
            player.unblock()
            //: player.removeVideoWidget()
            player.toColor()
        }
    }

    /// 最小化按钮事件
    //: @objc private func minimizeButtonClick() {
    @objc private func completeDelete() {
        //: delegate?.interactionView_shouldDisplayMiniWindow()
        delegate?.displayForText()
    }

    /// 添加视频通话画面窗口
    /// - Parameter view: 视频画面窗口
    //: func addWindowView(view: UIView) {
    func viewApp(view: UIView) {
        //: insertSubview(view, belowSubview: pairPicV)
        insertSubview(view, belowSubview: pairPicV)
    }

    /// 挂断按钮事件
    //: @objc private func hangupButtonClick() {
    @objc private func pieceOfWriting() {
        //: delegate?.interactionView_shouldEndCall()
        delegate?.errorBy()
    }

    /// 接听按钮事件
    //: @objc private func acceptButtonClick() {
    @objc private func itemView() {
        //: self.cancelAutoAcceptTimer()
        self.activity()
        //: delegate?.interactionView_shouldAcceptCall()
        delegate?.nearTakeABow()
    }

    /// 旋转摄像头事件
    //: @objc private func rotateCameraButtonClick() {
    @objc private func betweenCandidCamera() {
        //: if openCameraBtn.isSelected == true {
        if openCameraBtn.isSelected == true { // 腾讯美颜 && 摄像头关闭时
            //: func__showStatusBarErrorMsg(showMsg: "Camera must be on".localized)
            colorGift(showMsg: (String(showLeadingValue.suffix(6)) + " mus" + String(dream_locationMessage.prefix(2)) + String(dreamPointName.suffix(5))).localized)
            //: return
            return
        }
        //: rotateCameraBtn.isSelected = !rotateCameraBtn.isSelected
        rotateCameraBtn.isSelected = !rotateCameraBtn.isSelected
        //: delegate?.interactionView_shouldChangeCamera(isFront: !rotateCameraBtn.isSelected)
        delegate?.loadUp(isFront: !rotateCameraBtn.isSelected)
    }

    /// 是否禁用摄像头事件
    //: @objc private func openCameraButtonClick() {
    @objc private func afterFile() {
        //: openCameraBtn.isSelected = !openCameraBtn.isSelected
        openCameraBtn.isSelected = !openCameraBtn.isSelected
        //: delegate?.interactionView_shouldForbiddenCamera(forbidden: openCameraBtn.isSelected)
        delegate?.buttonKey(forbidden: openCameraBtn.isSelected)
    }

    /// 举报按钮点击事件（男性）
    //: @objc private func reportButtonClick() {
    @objc private func informInsideClick() {
        //: let reportView = TalkingReportAlertView(frame: UIScreen.main.bounds, type: .reportVideo, rId: String(chatModel.logId))
        let reportView = RateViewDelegate(frame: UIScreen.main.bounds, type: .reportVideo, rId: String(chatModel.logId))
        //: reportView.showReportViewIn(view: nil)
        reportView.toModel(view: nil)
        //: reportView.reportHideBlock = { [weak self] succeed in
        reportView.reportHideBlock = { [weak self] succeed in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard succeed == true else { return }
            guard succeed == true else { return }
            // 投诉成功
            //: self.reportBtn.isEnabled = false
            self.reportBtn.isEnabled = false
            //: self.reportBtn.isHidden = true
            self.reportBtn.isHidden = true
            //: self.delegate?.interactionView_reportSucceed()
            self.delegate?.nameLoad()
        }
    }

    /// 送礼按钮事件
    //: @objc private func sendGiftButtonClick() {
    @objc private func wishTurn() {
        //: func__sendGift()
        giftQuery()
    }

    /// 充值按钮事件
    //: @objc private func rechargeBtnClick() {
    @objc private func imageNeed() {
//        self.isShowRecharge = true
        //: NamePushManager.share.func__pushToHalfWebVC(webViewType: .RechargeHalfPage)
        NamePushManager.share.clickSize(webViewType: .RechargeHalfPage)
    }

    //: @objc private func touchHidenBtnClick() {
    @objc private func exceptFinish() {
        //: changeInteractionShowState()
        presentState()
        //: if isShowInput {
        if isShowInput {
            //: dismissClick()
            everyShow()
        }
    }

    /// 点击展示/隐藏通话中的按钮
    //: @objc private func changeInteractionShowState() {
    @objc private func presentState() {
        //: let isHidden = !rotateCameraBtn.isHidden
        let isHidden = !rotateCameraBtn.isHidden
        //: minimizeBtn.isHidden = isHidden
        minimizeBtn.isHidden = isHidden
        //: rotateCameraBtn.isHidden = isHidden
        rotateCameraBtn.isHidden = isHidden
        //: openCameraBtn.isHidden = isHidden
        openCameraBtn.isHidden = isHidden
        //: reportBtn.isHidden = (reportBtn.isEnabled ? isHidden:true) ||  PathAppManager.share.loginUserMode.sex == Gender.female.rawValue
        reportBtn.isHidden = (reportBtn.isEnabled ? isHidden : true) || PathAppManager.share.loginUserMode.sex == ConnectionClusterLiteral.female.rawValue
        //: sendGiftBtn.isHidden = isHidden
        sendGiftBtn.isHidden = isHidden
        //: rechargeBtn.isHidden = isHidden
        rechargeBtn.isHidden = isHidden
        //: hangupBtn.isHidden = isHidden
        hangupBtn.isHidden = isHidden
        //: timeLab.isHidden = isHidden
        timeLab.isHidden = isHidden
        //: if TalkingSocketManager.shared.isFreeCall {
        if PublishDataSocketDelegate.shared.isFreeCall {
            //: self.freeTimeView.isHidden = isHidden
            self.freeTimeView.isHidden = isHidden
        }
        //: if isHidden {
        if isHidden {
            //: invalidateIdleTimer()
            care()
            //: } else {
        } else {
            //: touchHiddenTimer()
            suspension()
        }
    }

    /// 去电，x秒后才显示挂断按钮
    //: private func isShowHangupBtn() {
    private func putDoing() {
        //: if chatModel.isCaller && chatModel.videoStage == .Waiting && PathAppManager.share.appUserConfigMode.callCancelBtnDelayTime > 0 && PathAppManager.share.loginUserMode.sex == Gender.male.rawValue {
        if chatModel.isCaller, chatModel.videoStage == .Waiting, PathAppManager.share.appUserConfigMode.callCancelBtnDelayTime > 0, PathAppManager.share.loginUserMode.sex == ConnectionClusterLiteral.male.rawValue {
            //: hangupBtn.isHidden = true
            hangupBtn.isHidden = true
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now()+Double(PathAppManager.share.appUserConfigMode.callCancelBtnDelayTime)) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + Double(PathAppManager.share.appUserConfigMode.callCancelBtnDelayTime)) {
                //: self.hangupBtn.isHidden = false
                self.hangupBtn.isHidden = false
            }
        }
    }

    /// 取消GCD自动接听定时器
    //: private func cancelAutoAcceptTimer() {
    private func activity() {
        //: if autoAcceptTimer != nil {
        if autoAcceptTimer != nil {
            //: autoAcceptTimer?.cancel()
            autoAcceptTimer?.cancel()
            //: autoAcceptTimer = nil
            autoAcceptTimer = nil
        }
    }
}

// MARK: - TvThen

//: extension Talking1v1VideoInteractionView: TalkingVideoDanmuManagerDelegate {
extension ValueGrantViewDelegate: DataManagerDelegate {
    //: func func__DanmuRecvNewModel(Msg: TalkingVideoCallDanmuModel) {
    func styleIn(Msg: KibbitzModelType) {
        //: danmuView.addNewMsg(msgModel: Msg)
        danmuView.utiliser(msgModel: Msg)
    }

    //: @objc private func commentBtnClick() {
    @objc private func inside() {
        /// 拉起弹幕评论
        //: self.bringSubviewToFront(commInputView)
        self.bringSubviewToFront(commInputView)
        //: commInputView.updatePlaceholder()
        commInputView.dataPlaceholder()
    }
}

// MARK: - 发表弹幕代理

//: extension Talking1v1VideoInteractionView: TalkingVideoCallInputViewDelegate {
extension ValueGrantViewDelegate: VideoViewDelegate {
    //: @objc func dismissClick() {
    @objc func everyShow() {
        //: self.isShowInput = false
        self.isShowInput = false
//        commInputView.inputTextView.text = ""
        //: commInputView.resignkeyBoard()
        commInputView.federalReserveBoard()
    }

    //: func func__sendTextMsg(msgStr: String) {
    func baseStr(msgStr: String) {
        //: self.isShowInput = false
        self.isShowInput = false
        //: sendToTextMsg(msgStr: msgStr)
        recordThen(msgStr: msgStr)
    }

    //: func heightToBottomChanged(heightToBottom: CGFloat) {
    func service(heightToBottom: CGFloat) {
        //: if heightToBottom > 0 {
        if heightToBottom > 0 {
            //: self.isShowInput = true
            self.isShowInput = true
        }
        //: commInputView.snp.updateConstraints { make in
        commInputView.snp.updateConstraints { make in
            //: make.bottom.equalTo(self).offset(-heightToBottom)
            make.bottom.equalTo(self).offset(-heightToBottom)
        }
        //: danmuView.snp.updateConstraints { make in
        danmuView.snp.updateConstraints { make in
            //: let height = heightToBottom == 0 ? -10 : -(heightToBottom-chatBtn.height+commInputView.height-kDeviceSafeBottomHeight+10)
            let height = heightToBottom == 0 ? -10 : -(heightToBottom - chatBtn.height + commInputView.height - dreamOkTitle + 10)
            //: make.bottom.equalTo(chatBtn.snp.top).offset(height)
            make.bottom.equalTo(chatBtn.snp.top).offset(height)
        }
    }

    //: func inputViewHeightChanged(height: CGFloat) {
    func signalingUponHighness(height: CGFloat) {
        //: commInputView.snp.updateConstraints { make in
        commInputView.snp.updateConstraints { make in
            //: make.height.equalTo(height)
            make.height.equalTo(height)
        }

        //: self.needsUpdateConstraints()
        self.needsUpdateConstraints()
        //: self.updateConstraintsIfNeeded()
        self.updateConstraintsIfNeeded()
        //: UIView.animate(withDuration: 0.3, animations: {
        UIView.animate(withDuration: 0.3, animations: {
            //: self.layoutIfNeeded()
            self.layoutIfNeeded()
            //: })
        })
    }

    /// 发送文本消息
    //: func sendToTextMsg(msgStr: String) {
    func recordThen(msgStr: String) {
        //: TalkingVideoDanmuManager.uploadToTextMsg(logId: "\(chatModel.logId)", content: msgStr) { succeed, result, errorModel in
        TvThen.kindCompletion(logId: "\(chatModel.logId)", content: msgStr) { succeed, result, _ in
            //: if succeed {
            if succeed {
                //: let json = JSON(result!)
                let json = JSON(result!)
                //: let msgId = json["msgId"].string
                let msgId = json[String(bytes: k_leadingMessage.reversed(), encoding: .utf8)!].string
                //: var toUid: String?
                var toUid: String?
                //: if self.chatModel.iUid == PathAppManager.share.loginUserMode.userID {
                if self.chatModel.iUid == PathAppManager.share.loginUserMode.userID {
                    //: toUid = self.chatModel.pairUid
                    toUid = self.chatModel.pairUid
                    //: } else {
                } else {
                    //: toUid = self.chatModel.iUid
                    toUid = self.chatModel.iUid
                }
                //: let data = ["msgId": msgId, "senderUid": PathAppManager.share.loginUserMode.userID, "toUid": toUid, "nickname": PathAppManager.share.loginUserMode.nickname, "content": msgStr, "isMySender": "1"]
                let data = [String(bytes: k_leadingMessage.reversed(), encoding: .utf8)!: msgId, String(bytes: main_normalNetStr.map{analogDigitalConverter(signature: $0)}, encoding: .utf8)!: PathAppManager.share.loginUserMode.userID, (String(main_runningIdent.prefix(5))): toUid, String(bytes: noti_contentTitle.map{$0^43}, encoding: .utf8)!: PathAppManager.share.loginUserMode.nickname, String(bytes: app_sharedId.map{layerMatch(count: $0)}, encoding: .utf8)!: msgStr, String(bytes: app_picId.map{photoStyle(status: $0)}, encoding: .utf8)!: "1"]
                //: TalkingVideoDanmuManager.shared().onRecvDanmuNewMsg(msg: data as [String: Any])
                TvThen.signaling().turnABlindEye(msg: data as [String: Any])
                //: self.rotateCameraBtn.isHidden = true
                self.rotateCameraBtn.isHidden = true
                //: self.changeInteractionShowState()
                self.presentState()
                //: self.commInputView.inputTextView.attributedText = nil
                self.commInputView.inputTextView.attributedText = nil
            }
        }
    }
}

// MARK: - ToImageThen【视频播放器】

//: extension Talking1v1VideoInteractionView: TalkingVideoPlayerDelegate {
extension ValueGrantViewDelegate: ToImageThen {
    //: func func__playerStatus(player: TalkingVideoPlayerManager, status: VideoPlayerStatus) {
    func funcDismissSocialStation(player _: TincturePlayListener, status: ModelPlayerStatus) {
        //: if status == .Playing {
        if status == .Playing {
            //: self.player.setRenderMode(renderMode: .FILL_SCREEN)
            self.player.transformMode(renderMode: .FILL_SCREEN)
            //: self.coverImg.isHidden = true
            self.coverImg.isHidden = true
        }
    }

    //: func func__playerDuration(player: TalkingVideoPlayerManager, duration: Int, currentTime: Int) {}
    func upTo(player _: TincturePlayListener, duration _: Int, currentTime _: Int) {}

    //: func func__playerProgress(player: TalkingVideoPlayerManager, progress: CGFloat) {}
    func lineList(player _: TincturePlayListener, progress _: CGFloat) {}
}

// MARK: - 通知事件

//: extension Talking1v1VideoInteractionView {
extension ValueGrantViewDelegate {
    /// 商汤美颜是否捕获到人脸通知（女性）
    //: @objc private func catchFaceResultNotification(notification: NSNotification) {
    @objc private func browDeal(notification: NSNotification) {
        // 女性 && 等待接听
        //: guard PathAppManager.share.loginUserMode.sex == Gender.female.rawValue else { return }
        guard PathAppManager.share.loginUserMode.sex == ConnectionClusterLiteral.female.rawValue else { return }
        //: guard self.chatModel.videoStage == .Recieved else { return }
        guard self.chatModel.videoStage == .Recieved else { return }

        //: let userInfo = notification.userInfo as! [String: AnyObject]
        let userInfo = notification.userInfo as! [String: AnyObject]
        //: let hasFace = (userInfo["hasFace"] as! NSNumber).description.boolValue
        let hasFace = (userInfo[(mBurnLabelValue.replacingOccurrences(of: "cell", with: "a") + String(mainTingKey.suffix(5)))] as! NSNumber).description.boolValue
        //: showOrHidenAutoRecieved(autoAccept: hasFace)
        makeAccept(autoAccept: hasFace)
    }

    /// 是否展示自动接听按钮
    /// - Parameter autoAccept: 是否自动接听（默认：false）
    //: private func showOrHidenAutoRecieved(autoAccept: Bool = false) {
    private func makeAccept(autoAccept: Bool = false) {
        //: var isActive = false
        var isActive = false

        //: if PathAppManager.share.appConfigMode.autoAnswer == 1 {
        if PathAppManager.share.appConfigMode.autoAnswer == 1 {
            //: let timeDifference = Date().timeIntervalSince1970 - (TalkingApplication.shared as! TalkingApplication).lastEventTime
            let timeDifference = Date().timeIntervalSince1970 - (CommentKingfisherCompatible.shared as! CommentKingfisherCompatible).lastEventTime
            //: if timeDifference < 5 {
            if timeDifference < 5 {
                //: isActive = true
                isActive = true
            }
        }

        //: DispatchQueue.main.async { [weak self] in
        DispatchQueue.main.async { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: if autoAccept || isActive {
            if autoAccept || isActive { // 自动接听
                //: self.autoAcceptDesLab.isHidden = false
                self.autoAcceptDesLab.isHidden = false

                //: var countdown = PathAppManager.share.appConfigMode.autoAnswerCountdownTime
                var countdown = PathAppManager.share.appConfigMode.autoAnswerCountdownTime
                // 防止多次初始化
                //: guard self.autoAcceptTimer == nil else { return }
                guard self.autoAcceptTimer == nil else { return }
                //: self.autoAcceptTimer = DispatchSource.makeTimerSource(queue: DispatchQueue.global())
                self.autoAcceptTimer = DispatchSource.makeTimerSource(queue: DispatchQueue.global())
                //: self.autoAcceptTimer?.schedule(deadline: .now(), repeating: .seconds(1))
                self.autoAcceptTimer?.schedule(deadline: .now(), repeating: .seconds(1))
                //: self.autoAcceptTimer?.setEventHandler {
                self.autoAcceptTimer?.setEventHandler {
                    //: DispatchQueue.main.async {
                    DispatchQueue.main.async {
                        //: guard countdown > 0 else {
                        guard countdown > 0 else {
                            //: if self.chatModel.videoStage == .Recieved {
                            if self.chatModel.videoStage == .Recieved {
                                //: self.acceptButtonClick()
                                self.itemView()
                            }
                            //: return
                            return
                        }
                        //: self.autoAcceptDesLab.text = "Automatically answer for you in %@s".localizedArguments(countdown)
                        self.autoAcceptDesLab.text = String(bytes: mainManagerNameId.map{toThe(result: $0)}, encoding: .utf8)!.equalArguments(countdown)
                        //: countdown -= 1
                        countdown -= 1
                    }
                }
                //: self.autoAcceptTimer?.resume()
                self.autoAcceptTimer?.resume()
            }
        }
    }

    /// 半屏充值页退下
    //: @objc private func changeRechargeStatus() {
    @objc private func modelShared() {}
}

// MARK: -

//: extension Talking1v1VideoInteractionView {
extension ValueGrantViewDelegate {
    //: func showPointAnimaData(point: String) {
    func withMessage(point: String) {
        //: addSubview(pointAnimView)
        addSubview(pointAnimView)
        //: pointAnimView.snp.remakeConstraints { make in
        pointAnimView.snp.remakeConstraints { make in
            //: make.center.equalToSuperview()
            make.center.equalToSuperview()
            //: make.size.equalTo(CGSize(width: actualWidth(w: 100), height: actualWidth(w: 100)))
            make.size.equalTo(CGSize(width: actualWidth(w: 100), height: actualWidth(w: 100)))
        }
        //: pointAnimView.setPointData(point: point)
        pointAnimView.writeStr(point: point)
    }
}

// MARK: - 送礼

//: extension Talking1v1VideoInteractionView {
extension ValueGrantViewDelegate {
    //: func func__sendGift() {
    func giftQuery() {
        //: TalkingChatGiftManager.share.func__sendGiftEvent {
        AdministratorReactiveCompatible.share.simpleFraction {
            //: self.func__showGiftChoiceView()
            self.overGift()
        }
    }

    //: func func__showGiftChoiceView() {
    func overGift() {
        //: giftView.updateGiftInfo(needReload: true, mf_coin: PathAppManager.share.loginUserMode.mf_coin)
        giftView.pitchCancel(needReload: true, mf_coin: PathAppManager.share.loginUserMode.mf_coin)
        //: window?.addSubview(giftView)
        window?.addSubview(giftView)
        //: giftView.showView()
        giftView.tableOfTitles()
        //: giftView.sendActionBlock = { [weak self] (_ giftModel: TalkingRoomGiftModel, _ num: String) in
        giftView.sendActionBlock = { [weak self] (_ giftModel: DiskModelType, _ num: String) in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.func__sendGiftMsg(giftId: giftModel.gid, giftNum: num, pkgItemsetId: giftModel.pkgItemsetId, programSort: nil)
            self.sendRow(giftId: giftModel.gid, giftNum: num, pkgItemsetId: giftModel.pkgItemsetId, programSort: nil)
        }
    }

    //: func func_giftViewDismiss() {
    func lessDismiss() {
        //: giftView.dismissView()
        giftView.kisser()
    }

    //: func func__sendGiftMsg(giftId: String, giftNum: String, pkgItemsetId: String?, programSort: String?) {
    func sendRow(giftId: String, giftNum: String, pkgItemsetId: String?, programSort: String?) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            colorGift(showMsg: user_netTitle)
            //: return
            return
        }
        //: var params: [String: Any] = [:]
        var params: [String: Any] = [:]
        //: if chatModel.iUid == PathAppManager.share.loginUserMode.userID {
        if chatModel.iUid == PathAppManager.share.loginUserMode.userID {
            //: params["toUid"] = chatModel.pairUid
            params[(String(main_runningIdent.prefix(5)))] = chatModel.pairUid
            //: } else {
        } else {
            //: params["toUid"] = chatModel.iUid
            params[(String(main_runningIdent.prefix(5)))] = chatModel.iUid
        }
        //: params["giftId"] = giftId
        params[(String(notiInfoMessage))] = giftId
        //: params["giftNum"] = giftNum
        params[(String(mDataLogKey.prefix(6)) + showLoadName.lowercased())] = giftNum
        //: params["from"] = "videoCall"
        params[(userQuantityKey.replacingOccurrences(of: "show", with: "r"))] = (kLogId.replacingOccurrences(of: "index", with: "o") + "Call")
        //: if programSort != nil {
        if programSort != nil {
            //: params["programSort"] = programSort
            params[(String(user_succeedValue) + String(m_videoUrl.suffix(5)))] = programSort
        }
        //: if pkgItemsetId != nil {
        if pkgItemsetId != nil {
            //: params["pkgItemsetId"] = pkgItemsetId
            params[(String(app_rankData.prefix(5)) + "emsetId")] = pkgItemsetId
        }

        //: TalkingChatRequestTool.req_SendGiftPrivete(param: params, completion: { succeed, result, errorModel in
        SourceRequestTool.holdUp(param: params, completion: { succeed, result, errorModel in
            //: guard succeed else {
            guard succeed else {
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                self.windowDeal(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                //: return
                return
            }

            // 更新本地礼物背包
            //: if pkgItemsetId != nil {
            if pkgItemsetId != nil {
                //: self.giftView.bags_removeGiftPackage(pkgItemsetId: pkgItemsetId!, num: Int(giftNum) ?? 0)
                self.giftView.fromShared(pkgItemsetId: pkgItemsetId!, num: Int(giftNum) ?? 0)
            }
            //: self.func__insertGiftMessageWithExtral(extral: result as! [String: Any?])
            self.extral(extral: result as! [String: Any?])
            //: })
        })
    }

    //: func func__insertGiftMessageWithExtral(extral: [String: Any?]) {
    func extral(extral: [String: Any?]) {
        //: var extralInfo = extral
        var extralInfo = extral
        //: var msgInfoDic: [String: Any] = extralInfo["msgInfo"] as! [String: Any]
        var msgInfoDic: [String: Any] = extralInfo[(String(kFirstIndexTitle.suffix(7)))] as! [String: Any]
        //: msgInfoDic["msgType"] = "gift"
        msgInfoDic[(String(userEditNumberValue) + String(dreamControlKey))] = (showWithTitle.lowercased())
        //: extralInfo["msgInfo"] = msgInfoDic
        extralInfo[(String(kFirstIndexTitle.suffix(7)))] = msgInfoDic

        //: sendGiftByMeToEfffecct(giftDic: extral as [String: Any])
        enrichmentMake(giftDic: extral as [String: Any])
        //: if extralInfo.keys.contains("totalMfCoin") {
        if extralInfo.keys.contains((String(dreamShareUrl.prefix(6)) + String(notiBackgroundUrl))) {
            //: let totalMfCoin: NSNumber = extralInfo["totalMfCoin"] as! NSNumber
            let totalMfCoin: NSNumber = extralInfo[(String(dreamShareUrl.prefix(6)) + String(notiBackgroundUrl))] as! NSNumber
            //: PathAppManager.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            PathAppManager.share.loginUserMode.mf_coin = totalMfCoin.stringValue
        }
        //: giftView.updateGiftInfo(needReload: false, mf_coin: PathAppManager.share.loginUserMode.mf_coin)
        giftView.pitchCancel(needReload: false, mf_coin: PathAppManager.share.loginUserMode.mf_coin)

        //: var toUid = ""
        var toUid = ""
        //: if chatModel.iUid == PathAppManager.share.loginUserMode.userID {
        if chatModel.iUid == PathAppManager.share.loginUserMode.userID {
            //: toUid = chatModel.pairUid
            toUid = chatModel.pairUid
            //: } else {
        } else {
            //: toUid = chatModel.iUid
            toUid = chatModel.iUid
        }

        //: let cellData = TUISocialChatManager.insertTXMessage(withExtral: extralInfo, toUid: toUid, isSender: true, isTip: false)
        let cellData = PrimrosePathThen.barWrite(withExtral: extralInfo, toUid: toUid, isSender: true, isTip: false)

        //: NotificationCenter.default.post(name: CHAT_ADD_CALL_RECORD_MSG_NOTIFICATION, object: self, userInfo: ["cellData": cellData])
        NotificationCenter.default.post(name: show_nameClickStr, object: self, userInfo: [String(bytes: dreamShowImageStr.map{equalFile(nose: $0)}, encoding: .utf8)!: cellData])
    }

    //: func dealSendMsgError(errorCode: Int, errorStr: String, isGift: Bool, isResend: Bool) {
    func windowDeal(errorCode: Int, errorStr: String, isGift _: Bool, isResend _: Bool) {
        //: if errorCode == 50203 {
        if errorCode == 50203 {
            //: guard PathAppManager.share.loginUserMode.status != 1 else {
            guard PathAppManager.share.loginUserMode.status != 1 else {
                //: if !errorStr.isEmptyString {
                if !errorStr.isEmptyString {
                    //: func__showStatusBarErrorMsg(showMsg: errorStr)
                    colorGift(showMsg: errorStr)
                }
                //: return
                return
            }
            //: NamePushManager.share.func__jumpToWebRecharge(clickEvent: clickSendingiftsButton, sufficient: false)
            NamePushManager.share.rechargeParams(clickEvent: noti_limitUrl, sufficient: false)
            //: giftView.dismissView()
            giftView.kisser()
            //: } else if errorCode == 10888 {
        } else if errorCode == 10888 {
            //: let view = TalkingLiveVipSubscribePopView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
            let view = LightPopView(frame: CGRect(x: 0, y: 0, width: app_screenFailMarginFormat, height: mainAcceptKey))
            //: view.show()
            view.characterSet()
            //: } else {
        } else {
            //: if !errorStr.isEmptyString {
            if !errorStr.isEmptyString {
                //: func__showStatusBarErrorMsg(showMsg: errorStr)
                colorGift(showMsg: errorStr)
            }
        }
    }

    //: func initCachEffectData() {
    func dataIconTap() {
        //: var targetId = ""
        var targetId = ""
        //: if chatModel.iUid == PathAppManager.share.loginUserMode.userID {
        if chatModel.iUid == PathAppManager.share.loginUserMode.userID {
            //: targetId = chatModel.pairUid
            targetId = chatModel.pairUid
            //: } else {
        } else {
            //: targetId = chatModel.iUid
            targetId = chatModel.iUid
        }
        //: giftEffectView.initCachAnimatData(tagetID: targetId)
        giftEffectView.auditoryImage(tagetID: targetId)
    }

    //: func sendGiftByMeToEfffecct(giftDic: [String: Any]) {
    func enrichmentMake(giftDic: [String: Any]) {
        //: let giftModel = giftEffectView.changeGiftToModel(dict: giftDic) ?? TalkingGiftMsgAnimatModel()
        let giftModel = giftEffectView.towardSumimate(dict: giftDic) ?? LabelMeasurable()
        //: if giftTrackView.giftSendItem.count == 0 {
        if giftTrackView.giftSendItem.count == 0 {
            //: let arrM = NSMutableArray()
            let arrM = NSMutableArray()
            //: for model in TalkingChatGiftManager.share.getSendingItems() {
            for model in AdministratorReactiveCompatible.share.userConstraint() {
                //: arrM.add(model)
                arrM.add(model)
            }
            //: giftTrackView.giftSendItem = arrM as! [TalkingGiftNumArrModel]
            giftTrackView.giftSendItem = arrM as! [TelecastingMeasurable]
        }
        //: giftTrackView.func__didReceiveGiftMsgModel(model: giftModel)
        giftTrackView.thenFunc(model: giftModel)

        //: giftEffectView.dealGiftAnimatData(dict: giftDic)
        giftEffectView.accumulationDict(dict: giftDic)
    }

    //: func addGiftEffectModelArr(giftArr: [Any]) {
    func writtenRecord(giftArr: [Any]) {
        //: if giftTrackView.giftSendItem.count == 0 {
        if giftTrackView.giftSendItem.count == 0 {
            //: let arrM = NSMutableArray()
            let arrM = NSMutableArray()
            //: for model in TalkingChatGiftManager.share.getSendingItems() {
            for model in AdministratorReactiveCompatible.share.userConstraint() {
                //: arrM.add(model)
                arrM.add(model)
            }
            //: giftTrackView.giftSendItem = arrM as! [TalkingGiftNumArrModel]
            giftTrackView.giftSendItem = arrM as! [TelecastingMeasurable]
        }
        //: for i in 0..<giftArr.count {
        for i in 0 ..< giftArr.count {
            //: let giftModel = giftArr[i]
            let giftModel = giftArr[i]
            //: giftTrackView.func__didReceiveGiftMsgModel(model: giftModel as! TalkingGiftMsgAnimatModel)
            giftTrackView.thenFunc(model: giftModel as! LabelMeasurable)
        }

        //: giftEffectView.addGiftAnimatModelArr(modelArr: giftArr)
        giftEffectView.afterImageArr(modelArr: giftArr)
    }
}

// MARK: - 通话计时器

//: extension Talking1v1VideoInteractionView {
extension ValueGrantViewDelegate {
    /// 开始通话计时
    //: private func startTalkCount() {
    private func ptolemaicSystem() {
        //: guard countTimer == nil else { return }
        guard countTimer == nil else { return }

        //: if startTime == nil {
        if startTime == nil {
            //: let interval = Date().timeIntervalSince1970
            let interval = Date().timeIntervalSince1970
            //: startTime = interval
            startTime = interval
            //: chatModel.startTime = startTime
            chatModel.startTime = startTime
        }
        //: countTimer = Timer.scheduledTimer(withTimeInterval: 1, block: { [weak self] _ in
        countTimer = Timer.scheduledTimer(withTimeInterval: 1, block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.changeTalkTime()
            self.doTitle()
            //: }, repeats: true)
        }, repeats: true)
    }

    /// 免费视频剩余时间倒计时
    //: private func setFreeCountTimer() {
    private func halfClick() {
        //: guard freeCountTimer == nil else { return }
        guard freeCountTimer == nil else { return }
        //: var  index = 60
        var index = 60
        //: freeCountTimer = Timer.scheduledTimer(withTimeInterval: 1, block: { [weak self] _ in
        freeCountTimer = Timer.scheduledTimer(withTimeInterval: 1, block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: index -= 1
            index -= 1
            //: self.freeTimeView.setTipsMessage(count: index)
            self.freeTimeView.exaggerate(count: index)
            //: }, repeats: true)
        }, repeats: true)
    }

    // 展示通话时间
    //: private func changeTalkTime() {
    private func doTitle() {
        //: let duration = getCurrentTalkingTime()
        let duration = choiceToTime()
        //: let min = floor(duration/60)
        let min = floor(duration / 60)
        //: let sec = floor(duration.truncatingRemainder(dividingBy: 60))
        let sec = floor(duration.truncatingRemainder(dividingBy: 60))
        //: timeLab.text = String(format: "%02i:%02i", Int(min), Int(sec))
        timeLab.text = String(format: "%02i:%02i", Int(min), Int(sec))
    }

    /// 恢复通话，更新通话时间
    /// - Parameter startTime: 通话时间
    //: func updateStartTime(startTime: TimeInterval) {
    func getMovingDown(startTime: TimeInterval) {
        //: self.startTime = startTime
        self.startTime = startTime
        //: changeTalkTime()
        doTitle()
    }

    /// 获取当前通话时间
    /// - Returns: 秒
    //: func getCurrentTalkingTime() -> Double {
    func choiceToTime() -> Double {
        //: guard chatModel.videoStage == .Calling else { return 0 }
        guard chatModel.videoStage == .Calling else { return 0 }
        //: let interval = Date().timeIntervalSince1970
        let interval = Date().timeIntervalSince1970
        //: let duration = floor(interval-(startTime ?? 0))
        let duration = floor(interval - (startTime ?? 0))
        //: return duration
        return duration
    }
}

// MARK: - 【通话中】触屏隐藏交互按钮逻辑

//: extension Talking1v1VideoInteractionView {
extension ValueGrantViewDelegate {
    /// 触摸隐藏开启延时
    //: private func touchHiddenTimer() {
    private func suspension() {
        //: invalidateIdleTimer()
        care()
        //: perform(#selector(hideInteractionView), afterDelay: 10)
        perform(#selector(valueView), afterDelay: 10)
    }

    /// 销毁延迟
    //: private func invalidateIdleTimer() {
    private func care() {
        //: NSObject.cancelPreviousPerformRequests(withTarget: self, selector: #selector(hideInteractionView), object: nil)
        NSObject.cancelPreviousPerformRequests(withTarget: self, selector: #selector(valueView), object: nil)
    }

    /// 延时到时隐藏视图
    //: @objc private func hideInteractionView() {
    @objc private func valueView() {
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.changeInteractionShowState()
            self.presentState()
        }
    }
}

// MARK: - Layout

//: extension Talking1v1VideoInteractionView {
extension ValueGrantViewDelegate {
    /// 添加视图
    //: private func setupSubviews() {
    private func equalSave() {
        //: addSubview(giftEffectView)
        addSubview(giftEffectView)
        //: insertSubview(giftTrackView, belowSubview: giftEffectView)
        insertSubview(giftTrackView, belowSubview: giftEffectView)
        //: addSubview(coverImg)
        addSubview(coverImg)
        //: addSubview(touchHidenBtn)
        addSubview(touchHidenBtn)
        //: addSubview(pairInfoBg)
        addSubview(pairInfoBg)
        //: pairInfoBg.addSubview(pairPicV)
        pairInfoBg.addSubview(pairPicV)
        //: pairInfoBg.addSubview(pairNameLab)
        pairInfoBg.addSubview(pairNameLab)
        //: pairInfoBg.addSubview(locationBtn)
        pairInfoBg.addSubview(locationBtn)
        //: addSubview(desLab)
        addSubview(desLab)
        //: addSubview(minimizeBtn)
        addSubview(minimizeBtn)
        //: addSubview(hangupBtn)
        addSubview(hangupBtn)
        //: addSubview(rotateCameraBtn)
        addSubview(rotateCameraBtn)
        //: addSubview(openCameraBtn)
        addSubview(openCameraBtn)
        //: addSubview(sendGiftBtn)
        addSubview(sendGiftBtn)
        //: addSubview(rechargeBtn)
        addSubview(rechargeBtn)
        //: addSubview(timeLab)
        addSubview(timeLab)
        //: addSubview(reportBtn)
        addSubview(reportBtn)
        //: addSubview(acceptBtn)
        addSubview(acceptBtn)
        //: addSubview(chatBtn)
        addSubview(chatBtn)
        //: addSubview(danmuView)
        addSubview(danmuView)
        //: addSubview(commInputView)
        addSubview(commInputView)
        //: addSubview(femaleAcceptBGBtn)
        addSubview(femaleAcceptBGBtn)
        //: addSubview(femaleAcceptBtn)
        addSubview(femaleAcceptBtn)
        //: femaleAcceptBtn.addSubview(femaleAcceptTipsBtn)
        femaleAcceptBtn.addSubview(femaleAcceptTipsBtn)
        //: addSubview(acceptPlayer)
        addSubview(acceptPlayer)
        //: addSubview(autoAcceptDesLab)
        addSubview(autoAcceptDesLab)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func third() {
        //: giftEffectView.snp.makeConstraints { make in
        giftEffectView.snp.makeConstraints { make in
            //: make.leading.trailing.top.bottom.equalTo(self)
            make.leading.trailing.top.bottom.equalTo(self)
        }
        //: giftTrackView.snp.makeConstraints { make in
        giftTrackView.snp.makeConstraints { make in
            //: make.leading.equalTo(self)
            make.leading.equalTo(self)
            //: make.height.equalTo(180)
            make.height.equalTo(180)
            //: make.bottom.equalTo(-actualHeight(h: 220)-kDeviceSafeBottomHeight)
            make.bottom.equalTo(-actualHeight(h: 220) - dreamOkTitle)
        }
        //: coverImg.snp.makeConstraints { make in
        coverImg.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: touchHidenBtn.snp.makeConstraints { make in
        touchHidenBtn.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: pairInfoBg.snp.makeConstraints { make in
        pairInfoBg.snp.makeConstraints { make in
            //: make.top.equalTo(StatusBarHeight+16)
            make.top.equalTo(app_topMsg + 16)
            //: make.leading.equalTo(15)
            make.leading.equalTo(15)
            //: make.size.equalTo(CGSize(width: actualWidth(w: 128), height: actualWidth(w: 50)))
            make.size.equalTo(CGSize(width: actualWidth(w: 128), height: actualWidth(w: 50)))
        }
        //: pairPicV.snp.makeConstraints { make in
        pairPicV.snp.makeConstraints { make in
            //: make.leading.equalTo(actualWidth(w: 5))
            make.leading.equalTo(actualWidth(w: 5))
            //: make.centerY.equalToSuperview()
            make.centerY.equalToSuperview()
            //: make.size.equalTo(CGSize(width: actualWidth(w: 40), height: actualWidth(w: 40)))
            make.size.equalTo(CGSize(width: actualWidth(w: 40), height: actualWidth(w: 40)))
        }
        //: pairNameLab.snp.makeConstraints { make in
        pairNameLab.snp.makeConstraints { make in
            //: make.leading.equalTo(pairPicV.snp.trailing).offset(8)
            make.leading.equalTo(pairPicV.snp.trailing).offset(8)
            //: make.top.equalTo(7)
            make.top.equalTo(7)
            //: make.height.equalTo(17)
            make.height.equalTo(17)
            //: make.trailing.equalToSuperview().offset(-7)
            make.trailing.equalToSuperview().offset(-7)
        }
        //: locationBtn.snp.makeConstraints { make in
        locationBtn.snp.makeConstraints { make in
            //: make.leading.equalTo(pairNameLab)
            make.leading.equalTo(pairNameLab)
            //: make.top.equalTo(pairNameLab.snp.bottom).offset(4)
            make.top.equalTo(pairNameLab.snp.bottom).offset(4)
            //: make.trailing.equalToSuperview().offset(-10)
            make.trailing.equalToSuperview().offset(-10)
        }
        //: desLab.snp.makeConstraints { make in
        desLab.snp.makeConstraints { make in
            //: make.top.equalToSuperview().offset(StatusBarHeight+86)
            make.top.equalToSuperview().offset(app_topMsg + 86)
            //: make.leading.equalTo(30)
            make.leading.equalTo(30)
            //: make.trailing.equalTo(-30)
            make.trailing.equalTo(-30)
        }
        //: minimizeBtn.snp.makeConstraints { make in
        minimizeBtn.snp.makeConstraints { make in
            //: make.top.equalTo(StatusBarHeight+10)
            make.top.equalTo(app_topMsg + 10)
            //: make.leading.equalTo(10)
            make.leading.equalTo(10)
            //: make.width.height.equalTo(actualWidth(w: 42))
            make.width.height.equalTo(actualWidth(w: 42))
        }

        //: acceptBtn.snp.makeConstraints { make in
        acceptBtn.snp.makeConstraints { make in
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.bottom.equalTo(hangupBtn)
            make.bottom.equalTo(hangupBtn)
            //: make.size.equalTo(hangupBtn)
            make.size.equalTo(hangupBtn)
        }
        //: femaleAcceptBtn.snp.makeConstraints { make in
        femaleAcceptBtn.snp.makeConstraints { make in
            //: make.edges.equalTo(hangupBtn)
            make.edges.equalTo(hangupBtn)
        }
        //: femaleAcceptTipsBtn.snp.makeConstraints { make in
        femaleAcceptTipsBtn.snp.makeConstraints { make in
            //: make.bottom.equalTo(-9)
            make.bottom.equalTo(-9)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
        }
        //: timeLab.snp.makeConstraints { make in
        timeLab.snp.makeConstraints { make in
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.top.equalTo(StatusBarHeight+20)
            make.top.equalTo(app_topMsg + 20)
        }
        //: autoAcceptDesLab.snp.makeConstraints { make in
        autoAcceptDesLab.snp.makeConstraints { make in
            //: make.center.width.equalToSuperview()
            make.center.width.equalToSuperview()
        }
        //: chatBtn.snp.makeConstraints { make in
        chatBtn.snp.makeConstraints { make in
            //: make.bottom.equalTo(-kDeviceSafeBottomHeight)
            make.bottom.equalTo(-dreamOkTitle)
            //: make.leading.equalTo(10)
            make.leading.equalTo(10)
            //: make.width.equalTo(actualWidth(w: 157))
            make.width.equalTo(actualWidth(w: 157))
            //: make.height.equalTo(actualWidth(w: 34))
            make.height.equalTo(actualWidth(w: 34))
        }
        //: danmuView.snp.makeConstraints { make in
        danmuView.snp.makeConstraints { make in
            //: make.bottom.equalTo(chatBtn.snp.top).offset(-10)
            make.bottom.equalTo(chatBtn.snp.top).offset(-10)
            //: make.leading.equalTo(10)
            make.leading.equalTo(10)
            //: make.height.equalTo(VideoMsgTableViewHeight)
            make.height.equalTo(m_errStr)
            //: make.width.equalTo(VideoMsgTableViewWidth)
            make.width.equalTo(mainBarName)
        }
        //: reportBtn.snp.makeConstraints { make in
        reportBtn.snp.makeConstraints { make in
            //: make.trailing.equalToSuperview().offset(-10)
            make.trailing.equalToSuperview().offset(-10)
            //: make.centerY.equalTo(chatBtn)
            make.centerY.equalTo(chatBtn)
            //: make.size.equalTo(actualWidth(w: 33))
            make.size.equalTo(actualWidth(w: 33))
        }
        //: sendGiftBtn.snp.makeConstraints { make in
        sendGiftBtn.snp.makeConstraints { make in
            //: make.trailing.equalTo(reportBtn.snp.leading).offset(-10)
            make.trailing.equalTo(reportBtn.snp.leading).offset(-10)
            //: make.bottom.equalTo(reportBtn)
            make.bottom.equalTo(reportBtn)
            //: make.size.equalTo(reportBtn)
            make.size.equalTo(reportBtn)
        }
        //: rechargeBtn.snp.makeConstraints { make in
        rechargeBtn.snp.makeConstraints { make in
            //: make.trailing.equalTo(sendGiftBtn.snp.leading).offset(-10)
            make.trailing.equalTo(sendGiftBtn.snp.leading).offset(-10)
            //: make.bottom.equalTo(reportBtn)
            make.bottom.equalTo(reportBtn)
            //: make.size.equalTo(reportBtn)
            make.size.equalTo(reportBtn)
        }

        //: hangupBtn.snp.makeConstraints { make in
        hangupBtn.snp.makeConstraints { make in
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.bottom.equalTo(-kDeviceSafeBottomHeight-161)
            make.bottom.equalTo(-dreamOkTitle - 161)
            //: make.size.equalTo(actualWidth(w: 70))
            make.size.equalTo(actualWidth(w: 70))
        }
        //: commInputView.snp.makeConstraints { make in
        commInputView.snp.makeConstraints { make in
            //: make.bottom.equalTo(self)
            make.bottom.equalTo(self)
            //: make.leading.trailing.equalTo(0)
            make.leading.trailing.equalTo(0)
            //: make.height.equalTo(actualWidth(w: 55))
            make.height.equalTo(actualWidth(w: 55))
        }
    }

    /// 更新约束
    //: private func updateLayout() {
    private func comeAbout() {
        //: if chatModel.videoStage == .Recieved {
        if chatModel.videoStage == .Recieved {
            //: if PathAppManager.share.loginUserMode.sex == Gender.male.rawValue {
            if PathAppManager.share.loginUserMode.sex == ConnectionClusterLiteral.male.rawValue {
                //: hangupBtn.snp.remakeConstraints { make in
                hangupBtn.snp.remakeConstraints { make in
                    //: make.leading.equalTo(ScreenWidth/2-42-70)
                    make.leading.equalTo(app_screenFailMarginFormat / 2 - 42 - 70)
                    //: make.bottom.equalTo(-kDeviceSafeBottomHeight-161)
                    make.bottom.equalTo(-dreamOkTitle - 161)
                    //: make.size.equalTo(70)
                    make.size.equalTo(70)
                }
                //: acceptBtn.snp.remakeConstraints { make in
                acceptBtn.snp.remakeConstraints { make in
                    //: make.leading.equalTo(ScreenWidth/2+42)
                    make.leading.equalTo(app_screenFailMarginFormat / 2 + 42)
                    //: make.bottom.equalTo(hangupBtn)
                    make.bottom.equalTo(hangupBtn)
                    //: make.size.equalTo(hangupBtn)
                    make.size.equalTo(hangupBtn)
                }
                //: } else {
            } else {
                //: hangupBtn.snp.remakeConstraints { make in
                hangupBtn.snp.remakeConstraints { make in
                    //: make.leading.equalTo(34)
                    make.leading.equalTo(34)
                    //: make.bottom.equalTo(-kDeviceSafeBottomHeight-161)
                    make.bottom.equalTo(-dreamOkTitle - 161)
                    //: make.size.equalTo(70)
                    make.size.equalTo(70)
                }
                //: femaleAcceptBGBtn.snp.remakeConstraints { make in
                femaleAcceptBGBtn.snp.remakeConstraints { make in
                    //: make.trailing.equalTo(-33)
                    make.trailing.equalTo(-33)
                    //: make.bottom.equalTo(-kDeviceSafeBottomHeight-147)
                    make.bottom.equalTo(-dreamOkTitle - 147)
                    //: make.size.equalTo(CGSize.init(width: 208, height: 99))
                    make.size.equalTo(CGSize(width: 208, height: 99))
                }
                //: femaleAcceptBtn.snp.remakeConstraints { make in
                femaleAcceptBtn.snp.remakeConstraints { make in
                    //: make.centerX.equalTo(femaleAcceptBGBtn)
                    make.centerX.equalTo(femaleAcceptBGBtn)
                    //: make.bottom.equalTo(femaleAcceptBGBtn.snp.bottom).offset(-15)
                    make.bottom.equalTo(femaleAcceptBGBtn.snp.bottom).offset(-15)
                    //: make.size.equalTo(CGSize.init(width: 180, height: 70))
                    make.size.equalTo(CGSize(width: 180, height: 70))
                }
                //: acceptPlayer.snp.remakeConstraints { make in
                acceptPlayer.snp.remakeConstraints { make in
                    //: make.trailing.equalTo(-8)
                    make.trailing.equalTo(-8)
                    //: make.bottom.equalTo(femaleAcceptBtn.snp.bottom).offset(10)
                    make.bottom.equalTo(femaleAcceptBtn.snp.bottom).offset(10)
                    //: make.size.equalTo(85)
                    make.size.equalTo(85)
                }
                //: openCameraBtn.snp.makeConstraints { make in
                openCameraBtn.snp.makeConstraints { make in
                    //: make.leading.equalTo(ScreenWidth/2+12)
                    make.leading.equalTo(app_screenFailMarginFormat / 2 + 12)
                    //: make.bottom.equalTo(-kDeviceSafeBottomHeight-15)
                    make.bottom.equalTo(-dreamOkTitle - 15)
                    //: make.size.equalTo(actualWidth(w: 45))
                    make.size.equalTo(actualWidth(w: 45))
                }
                //: rotateCameraBtn.snp.makeConstraints { make in
                rotateCameraBtn.snp.makeConstraints { make in
                    //: make.trailing.equalTo(openCameraBtn.snp.leading).offset(-25)
                    make.trailing.equalTo(openCameraBtn.snp.leading).offset(-25)
                    //: make.centerY.equalTo(openCameraBtn)
                    make.centerY.equalTo(openCameraBtn)
                    //: make.size.equalTo(openCameraBtn)
                    make.size.equalTo(openCameraBtn)
                }
            }
            //: } else if chatModel.videoStage == .Calling {
        } else if chatModel.videoStage == .Calling {
            //: hangupBtn.snp.remakeConstraints { make in
            hangupBtn.snp.remakeConstraints { make in
                //: make.trailing.equalTo(rotateCameraBtn.snp.leading).offset(-10)
                make.trailing.equalTo(rotateCameraBtn.snp.leading).offset(-10)
                //: make.centerY.equalTo(openCameraBtn)
                make.centerY.equalTo(openCameraBtn)
                //: make.size.equalTo(openCameraBtn)
                make.size.equalTo(openCameraBtn)
            }
            //: openCameraBtn.snp.remakeConstraints { make in
            openCameraBtn.snp.remakeConstraints { make in
                //: make.top.equalTo(StatusBarHeight+actualHeight(h: 25)+actualHeight(h: 170)+26+35+9)
                make.top.equalTo(app_topMsg + actualHeight(h: 25) + actualHeight(h: 170) + 26 + 35 + 9)
                //: make.trailing.equalToSuperview().offset(-10)
                make.trailing.equalToSuperview().offset(-10)
                //: make.size.equalTo(actualWidth(w: 33))
                make.size.equalTo(actualWidth(w: 33))
            }
            //: rotateCameraBtn.snp.remakeConstraints { make in
            rotateCameraBtn.snp.remakeConstraints { make in
                //: make.trailing.equalTo(openCameraBtn.snp.leading).offset(-10)
                make.trailing.equalTo(openCameraBtn.snp.leading).offset(-10)
                //: make.centerY.equalTo(openCameraBtn)
                make.centerY.equalTo(openCameraBtn)
                //: make.size.equalTo(openCameraBtn)
                make.size.equalTo(openCameraBtn)
            }
            //: } else {
        } else {
            //: hangupBtn.snp.makeConstraints { make in
            hangupBtn.snp.makeConstraints { make in
                //: make.centerX.equalToSuperview()
                make.centerX.equalToSuperview()
                //: make.bottom.equalTo(-kDeviceSafeBottomHeight-161)
                make.bottom.equalTo(-dreamOkTitle - 161)
                //: make.size.equalTo(actualWidth(w: 70))
                make.size.equalTo(actualWidth(w: 70))
            }
            //: openCameraBtn.snp.makeConstraints { make in
            openCameraBtn.snp.makeConstraints { make in
                //: make.leading.equalTo(ScreenWidth/2+12)
                make.leading.equalTo(app_screenFailMarginFormat / 2 + 12)
                //: make.bottom.equalTo(-kDeviceSafeBottomHeight-15)
                make.bottom.equalTo(-dreamOkTitle - 15)
                //: make.size.equalTo(actualWidth(w: 45))
                make.size.equalTo(actualWidth(w: 45))
            }
            //: rotateCameraBtn.snp.makeConstraints { make in
            rotateCameraBtn.snp.makeConstraints { make in
                //: make.trailing.equalTo(openCameraBtn.snp.leading).offset(-25)
                make.trailing.equalTo(openCameraBtn.snp.leading).offset(-25)
                //: make.centerY.equalTo(openCameraBtn)
                make.centerY.equalTo(openCameraBtn)
                //: make.size.equalTo(openCameraBtn)
                make.size.equalTo(openCameraBtn)
            }
        }
    }

    /// 添加事件
    //: private func bindInteraction() {
    private func holdFast() {
        // 视频等待...动画
        //: waitTimer = Timer.scheduledTimer(withTimeInterval: 0.5, block: { [weak self] _ in
        waitTimer = Timer.scheduledTimer(withTimeInterval: 0.5, block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard var text = self.desLab.text else { return }
            guard var text = self.desLab.text else { return }

            //: if text.hasSuffix("...") {
            if text.hasSuffix("...") {
                //: text = text.replacingOccurrences(of: "...", with: ".")
                text = text.replacingOccurrences(of: "...", with: ".")
                //: } else if text.hasSuffix("..") {
            } else if text.hasSuffix("..") {
                //: text = text.replacingOccurrences(of: "..", with: "...")
                text = text.replacingOccurrences(of: "..", with: "...")
                //: } else {
            } else {
                //: text = text.replacingOccurrences(of: ".", with: "..")
                text = text.replacingOccurrences(of: ".", with: "..")
            }
            //: self.desLab.text = text
            self.desLab.text = text

            //: }, repeats: true)
        }, repeats: true)
        //: waitTimer?.fire()
        waitTimer?.fire()
    }

    //: private func setFreeView() {
    private func showAttention() {
        //: addSubview(freeTimeView)
        addSubview(freeTimeView)
        //: freeTimeView.snp.remakeConstraints { make in
        freeTimeView.snp.remakeConstraints { make in
            //: make.trailing.equalToSuperview()
            make.trailing.equalToSuperview()
            //: make.top.equalTo(timeLab.snp.bottom).offset(actualHeight(h: 299))
            make.top.equalTo(timeLab.snp.bottom).offset(actualHeight(h: 299))
            //: make.height.equalTo(111)
            make.height.equalTo(111)
            //: make.width.equalTo(126)
            make.width.equalTo(126)
        }
    }

    /// 添加通知
    //: private func initNotificationObserver() {
    private func meCancel() {
        // 女性用户 && 使用商汤 && 人脸检测自动接听
        //: if PathAppManager.share.loginUserMode.sex == Gender.female.rawValue &&
        if PathAppManager.share.loginUserMode.sex == ConnectionClusterLiteral.female.rawValue,
           //: SenseTime_Use == true &&
           show_giftId == true,
           //: PathAppManager.share.appConfigMode.autoAnswer == 1 {
           PathAppManager.share.appConfigMode.autoAnswer == 1
        {
            /// 监听 是否捕获到人脸
            //: NotificationCenter.default.addObserver(self,
            NotificationCenter.default.addObserver(self,
                                                   //: selector: #selector(catchFaceResultNotification(notification:)),
                                                   selector: #selector(browDeal(notification:)),
                                                   //: name: NSNotification.Name(rawValue: "FaceActionEvent_Interval_12_frame"),
                                                   name: NSNotification.Name(rawValue: String(bytes: noti_viewContent.map{sizeAccountPin(user: $0)}, encoding: .utf8)!),
                                                   //: object: nil)
                                                   object: nil)
        }

        //: NotificationCenter.default.addObserver(self, selector: #selector(changeRechargeStatus), name: RECHARGEHALFPAGE_END_CLOSE_NOTIFICATION, object: nil)
        NotificationCenter.default.addObserver(self, selector: #selector(modelShared), name: userSenseKey, object: nil)
    }
}
