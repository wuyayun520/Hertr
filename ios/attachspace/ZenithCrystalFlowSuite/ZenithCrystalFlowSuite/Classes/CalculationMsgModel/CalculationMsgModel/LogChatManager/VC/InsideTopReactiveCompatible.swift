
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let user_giftTitle:[UInt8] = [0xb0,0xb7,0xb0,0xad,0xf1,0xba,0xb6,0xbd,0xbc,0xab,0xe3,0xf0,0xf9,0xb1,0xb8,0xaa,0xf9,0xb7,0xb6,0xad,0xf9,0xbb,0xbc,0xbc,0xb7,0xf9,0xb0,0xb4,0xa9,0xb5,0xbc,0xb4,0xbc,0xb7,0xad,0xbc,0xbd]

private func lengthPackage(margin num: UInt8) -> UInt8 {
    return num ^ 217
}

/*: "uid" :*/
fileprivate let k_makeId:[UInt8] = [0x90,0x84,0x7f]

fileprivate func playerFill(view num: UInt8) -> UInt8 {
    let value = Int(num) - 27
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "headPic" :*/
fileprivate let show_styleContent:String = "at buttonheadPic"

/*: "nickname" :*/
fileprivate let userTrackId:String = "niframek"

/*: "birthday" :*/
fileprivate let showAgentName:String = "birthtopy"

/*: "videoUrl" :*/
fileprivate let userShowDataText:String = "VIDE"

/*: "cover" :*/
fileprivate let userEventTextTitle:[Character] = ["c","o","v","e","r"]

/*: "location" :*/
fileprivate let showPicToDoingicialMessage:String = "locfemalet"
fileprivate let main_lessUrl:[Character] = ["i","o","n"]

/*: "cmd" :*/
fileprivate let dreamItemStr:[UInt8] = [0x31,0x3f,0x36]

private func titleVar(table num: UInt8) -> UInt8 {
    return num ^ 82
}

/*: "refuseCall" :*/
fileprivate let mStyleMessage:[Character] = ["r","e","f","u","s","e","C","a","l"]
fileprivate let dreamLogId:[Character] = ["l"]

/*: "data" :*/
fileprivate let m_coverName:[UInt8] = [0x61,0x74,0x61,0x64]

/*: "Call interruption" :*/
fileprivate let k_bottomId:[Character] = ["C","a","l","l"," ","i","n","t","e","r","r","u"]
fileprivate let notiInputData:String = "ptioimage"

/*: "stopCall" :*/
fileprivate let dream_makeTopName:String = "style"
fileprivate let dream_videoBeautyData:String = "topCallopen request button if"

/*: "logId" :*/
fileprivate let userDoingKey:[UInt8] = [0x64,0x49,0x67,0x6f,0x6c]

/*: "cancelCall" :*/
fileprivate let noti_originText:String = "CANCEL"
fileprivate let noti_modelMainData:String = "for manager normal bottomCall"

/*: "No answer" :*/
fileprivate let user_devicePath:[Character] = ["N","o"," ","a","n"]
fileprivate let dream_toolTalkStr:String = "sweresult"

/*: "duration" :*/
fileprivate let mainPageTitle:String = "durarray"

/*: "She has hung up" :*/
fileprivate let app_logStr:String = "status false color shared shadowShe h"
fileprivate let appTextName:[Character] = ["a","s"," ","h","u","n","g"," ","u","p"]

/*: "cancelSex" :*/
fileprivate let user_norQuantityLogId:[Character] = ["c","a"]
fileprivate let noti_lengthFormat:String = "ncelSexkit model to info return"

/*: "He has hung up" :*/
fileprivate let noti_somethingMsg:String = "equal manager read commonHe h"
fileprivate let dreamLabelTitle:String = "false share type imageas h"

/*: "endType" :*/
fileprivate let noti_sizeId:String = "color screen customendType"

/*: "stopSex" :*/
fileprivate let kTranslationKey:String = "size collection player ifstopSex"

/*: "Insufficient gold coins, call has ended" :*/
fileprivate let userTopMsg:[UInt8] = [0x60,0x47,0x5a,0x5c,0x4f,0x4f,0x40,0x4a,0x40,0x4c,0x47,0x5d,0x9,0x4e,0x46,0x45,0x4d,0x9,0x4a,0x46,0x40,0x47,0x5a,0x5,0x9,0x4a,0x48,0x45,0x45,0x9,0x41,0x48,0x5a,0x9,0x4c,0x47,0x4d,0x4c,0x4d]

private func centerRowResistance(title num: UInt8) -> UInt8 {
    return num ^ 41
}

/*: "The other hung up the phone" :*/
fileprivate let k_byAppKey:String = "The otransaction color send label"
fileprivate let k_coverStr:String = "pic model falsehung "
fileprivate let m_nameKey:String = "view for sub log appup the"

/*: "type" :*/
fileprivate let showFormatMessage:String = "tymodel"

/*: "jumpUrl" :*/
fileprivate let dream_finishPath:[Character] = ["j","u","m","p","U"]
fileprivate let dreamSizeGroupUrl:String = "RL"

/*: "Cancel" :*/
fileprivate let kPhotoFormat:String = "Canceview wrap by self"
fileprivate let m_sessionName:String = "L"

/*: "Get gold coins" :*/
fileprivate let k_makeIdent:String = "Get gmake self manager"
fileprivate let user_mPath:String = "fill else state baseold "

/*: "addBean" :*/
fileprivate let show_errorPath:String = "addBeanafter lab play log"

/*: "refuseSex" :*/
fileprivate let dreamMoreId:String = "REFUS"
fileprivate let kTalkAddUrl:String = "self verticaleSex"

/*: "onAcceptCall" :*/
fileprivate let dream_appearIdent:String = "app main if photoonAcce"
fileprivate let dream_barPath:String = "ptCallmake zone after"

/*: "acceptCall" :*/
fileprivate let noti_countIdent:String = "view if message frame targetacceptC"
fileprivate let notiBackData:[Character] = ["a","l","l"]

/*: "Calls are cancelled" :*/
fileprivate let showAppName:[Character] = ["C","a","l","l","s"," ","a","r","e"," ","c","a","n","c","e","l","l","e","d"]

/*: "The call has ended" :*/
fileprivate let m_labelFacePath:String = "The caappearance true"
fileprivate let user_modelIdent:[Character] = ["s"," ","e","n","d","e","d"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  InsideTopReactiveCompatible.swift
//  AbroadTalking
//
//  Created by young on 2022/11/8.
//

//: import CallKit.CXCallObserver
import CallKit.CXCallObserver
//: import UIKit
import UIKit

// 通话监听(必须为全局属性，否则无法接听)
//: let videoCallHelper = CXCallObserver()
let userThirdData = CXCallObserver()
//: class TalkingVideoChatViewController: TalkingBaseViewController {
class InsideTopReactiveCompatible: ListRecognizerDelegate {
	var styleNameMagnitude: Double = 8.7
	var ticketArray: [AnyHashable] = []
	var enableDictionary: [AnyHashable: String] = [:]

    //: var chatModel: TalkingVideoChatModel?
    var chatModel: WithChatModel? // 视频通话模型
    //: private var player: TalkingCallBGMPlayer?
    private var player: ReasonWindowPlayer? // 拨打中背景音乐
    //: private var timeOutTimer: Timer?
    private var timeOutTimer: Timer? // 未接听超时定时器
    //: private var expiredDate = Date()
    private var expiredDate = Date() // 失效时间
    //: private var miniView: TalkingVideoMiniView?
    private var miniView: SharedThen? // 视频通话小窗口
    //: private var isFromMiniView = false
    private var isFromMiniView = false // 是否点击小窗口进入
    //: private var reportView: TalkingVideoFloatScreenView?
    private var reportView: ValueThen? // 视频举报弹窗

    //: override init(nibName nibNameOrNil: String?, bundle nibBundleOrNil: Bundle?) {
    override init(nibName _: String?, bundle _: Bundle?) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        // 提前绑定delegate
        //: TalkingSocketManager.shared.callerDelegate = self
        PublishDataSocketDelegate.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        PublishDataSocketDelegate.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        PublishDataSocketDelegate.shared.errorDelegate = self
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: user_giftTitle.map{lengthPackage(margin: $0)}, encoding: .utf8)!)
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: let nav = self.navigationController as! TalkingNavigationController
        let nav = self.navigationController as! StreetSmartNavigationController
        //: nav.protectedPushVc = nil
        nav.protectedPushVc = nil

        //: UIApplication.shared.isIdleTimerDisabled = true
        UIApplication.shared.isIdleTimerDisabled = true // 设备不会进入休眠状态
        //: self.startBGM()
        self.anViewBgm()
        //: if self.chatModel?.videoStage == .Recieved {
        if self.chatModel?.videoStage == .Recieved { // 接收方验证权限
            //: checkVideoAuth()
            buildAuth()
            //: } else if self.chatModel?.videoStage == .Calling {
        } else if self.chatModel?.videoStage == .Calling { // 直接创建通话中的界面（包括重连）
            // 通话小窗口进入无需重复创建
            //: guard isFromMiniView == false else { return }
            guard isFromMiniView == false else { return }
            //: createCallingRoom(logId: self.chatModel?.logId)
            talkNeed(logId: self.chatModel?.logId)
        }
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: if self.miniView != nil {
        if self.miniView != nil { // 移除小窗口
            //: self.miniView?.removeFromSuperview()
            self.miniView?.removeFromSuperview()
            //: self.miniView = nil
            self.miniView = nil
            //: TalkingSocketManager.shared.currentDataVC = nil
            PublishDataSocketDelegate.shared.currentDataVC = nil
        }
        //: interactionView.initCachEffectData()
        interactionView.dataIconTap()
    
		if var showValue = self.chatModel?.fee { 
	            if (interactionView.giftEffectView.giftAnimatView.clearsContextBeforeDrawing) && (interactionView.giftEffectView.giftAnimatView.forLastBaselineLayout.center.y == 46.79) {
	                //: SWIFT_CUSTOM_DANGER_File_Call
	                let searchPlus = TouchView()
	            searchPlus.mainTotal = showValue
	            searchPlus.redDownTotal = interactionView.giftView.contentHeight
	            
	            searchPlus.signQuantity = { [self] rowNumber in
	            self.styleNameMagnitude = rowNumber
	            
	            var searchPlus = interactionView.giftView.contentHeight
	                searchPlus -= 1
	                if searchPlus < 52 {
	                    searchPlus = searchPlus + 1
	                }
	            if searchPlus < self.styleNameMagnitude {
	                self.styleNameMagnitude = searchPlus
	            }
	            
	                self.styleNameMagnitude -= 1
	                if Int(self.styleNameMagnitude) > -86 {
	                    self.styleNameMagnitude = self.styleNameMagnitude + 1
	                }
	            return self.styleNameMagnitude
	            }
	            searchPlus.cellArray = { [self] layArray in
	            self.ticketArray = layArray
	            
	            guard var value = self.ticketArray as? [String] else {
	                return nil
	            }
	            return value
	            }
	            searchPlus.rubricDictionary = { [self] boundDictionary in
	            self.enableDictionary = boundDictionary
	            
	            guard var value = self.enableDictionary as? [String: String] else {
	                return nil
	            }
	            return value
	            }
	                interactionView.giftEffectView.giftAnimatView.addSubview(searchPlus)
	            }
	
		}
	}

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true
        //: setupSubviews()
        recordSize()
        //: setupSubViewsConstraint()
        upClear()
        //: initNotificationObserver()
        exceptAdd()
        //: req_callGetUserInfo()
        visualCommunication()
        //: timeoutRules()
        audience()
        //: setUpBGMPlayer()
        toMake()
        //: if self.chatModel?.videoStage != .Recieved {
        if self.chatModel?.videoStage != .Recieved {
            //: self.startPreview()
            self.textEqualPreview()
        }
    }

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if PathAppManager.share.appConfigMode.disableShootScreen {
            if PathAppManager.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = CustomPathThen(frame: CGRect(x: 0, y: 0, width: app_screenFailMarginFormat, height: mainAcceptKey))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
    }

    //: deinit {
    deinit {
        //: UIApplication.shared.isIdleTimerDisabled = false
        UIApplication.shared.isIdleTimerDisabled = false
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
        //: TalkingSocketManager.shared.isData = false
        PublishDataSocketDelegate.shared.isData = false
        //: self.stopVCAllActivties()
        self.withValue()
    }

    // MARK: - Lazy Load

    /// 视频通话背景
    //: private lazy var bgVideoView: TalkingBGVideoView = {
    private lazy var bgVideoView: VisualCommunicationThen = {
        //: let v = TalkingBGVideoView()
        let v = VisualCommunicationThen()
        //: v.showCoverView(show: self.chatModel?.videoStage != .Calling)
        v.effectBringHomeShared(show: self.chatModel?.videoStage != .Calling)
        //: return v
        return v
        //: }()
    }()

    /// 视频通话操作按钮
    //: private lazy var interactionView: Talking1v1VideoInteractionView = {
    private lazy var interactionView: ValueGrantViewDelegate = {
        //: let v = Talking1v1VideoInteractionView(frame: .zero, chatModel: self.chatModel)
        let v = ValueGrantViewDelegate(frame: .zero, chatModel: self.chatModel)
        //: v.delegate = self
        v.delegate = self
        //: return v
        return v
        //: }()
    }()

    // 视频通话小窗口
    //: private lazy var windowView: TalkingVideoWindowView = {
    private lazy var windowView: ShirtButtonReactiveCompatible = {
        //: let v = TalkingVideoWindowView()
        let v = ShirtButtonReactiveCompatible()
        //: var x = ScreenWidth-15-actualWidth(w: 125)
        var x = app_screenFailMarginFormat - 15 - actualWidth(w: 125)
        //: if LanguageManager.shared.direction == .rightToLeft {
        if PresentReactiveCompatible.shared.direction == .rightToLeft {
            //: x = 15
            x = 15
        }
        //: v.frame = CGRect(x: x, y: StatusBarHeight+actualHeight(h: 25)+26+35, width: actualWidth(w: 125), height: actualHeight(h: 170))
        v.frame = CGRect(x: x, y: app_topMsg + actualHeight(h: 25) + 26 + 35, width: actualWidth(w: 125), height: actualHeight(h: 170))
        //: v.isHidden = self.chatModel?.videoStage != .Calling
        v.isHidden = self.chatModel?.videoStage != .Calling
        //: v.block = { [weak self] in
        v.block = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.changeRenderView()
            self.light()
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var videoManager: TalkingVideoChatManager = {
    private lazy var videoManager: LogChatManager = {
        //: let m = TalkingVideoChatManager()
        let m = LogChatManager()
        //: m.infoModel = self.chatModel
        m.infoModel = self.chatModel
        // 视图传递给manager
        //: m.bgVideoView = self.bgVideoView.renderView
        m.bgVideoView = self.bgVideoView.renderView
        //: m.bgView = self.bgVideoView.bgView
        m.bgView = self.bgVideoView.bgView
        //: m.windowVideoView = self.windowView.renderView
        m.windowVideoView = self.windowView.renderView
        //: return m
        return m
        //: }()
    }()
}

// MARK: - Request && 刷新UI

//: extension TalkingVideoChatViewController {
extension InsideTopReactiveCompatible {
    /// 查询接收者信息
    //: func req_callGetUserInfo() {
    func visualCommunication() {
        //: guard let pairUid = chatModel?.pairUid else { return }
        guard let pairUid = chatModel?.pairUid else { return }
        //: TalkingVideoChatManager.req_callGetUserInfo(uid: pairUid) { [weak self] succeed, result, errorModel in
        LogChatManager.sufficient(uid: pairUid) { [weak self] succeed, result, _ in
            //: guard succeed == true else { return }
            guard succeed == true else { return }
            //: guard let self = self else { return }
            guard let self = self else { return }

            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())

            //: self.chatModel?.pairUid = json["uid"].stringValue
            self.chatModel?.pairUid = json[String(bytes: k_makeId.map{playerFill(view: $0)}, encoding: .utf8)!].stringValue
            //: self.chatModel?.pairHeadPic = json["headPic"].stringValue
            self.chatModel?.pairHeadPic = json[(String(show_styleContent.suffix(7)))].stringValue
            //: self.chatModel?.pairNickName = json["nickname"].stringValue
            self.chatModel?.pairNickName = json[(userTrackId.replacingOccurrences(of: "frame", with: "c") + "name")].stringValue
            //: self.chatModel?.pairBirthday = json["birthday"].stringValue
            self.chatModel?.pairBirthday = json[(showAgentName.replacingOccurrences(of: "top", with: "da"))].stringValue
            //: self.chatModel?.videoUrl = json["videoUrl"].stringValue
            self.chatModel?.videoUrl = json[(userShowDataText.lowercased() + "oUrl")].stringValue
            //: self.chatModel?.cover = json["cover"].stringValue
            self.chatModel?.cover = json[(String(userEventTextTitle))].stringValue
            //: self.chatModel?.location = json["location"].stringValue
            self.chatModel?.location = json[(showPicToDoingicialMessage.replacingOccurrences(of: "female", with: "a") + String(main_lessUrl))].stringValue

            // 刷新通话视图
            //: self.interactionView.afterGetInfo()
            self.interactionView.dataItem()
            //: self.videoBG_refreshViewWithCameraStatus()
            self.equalTab()
        }
    }

    /// 根据摄像头是否开启状态，刷新视频通话背景
    //: private func videoBG_refreshViewWithCameraStatus() {
    private func equalTab() {
        //: var bgHeadPic: String?
        var bgHeadPic: String?
        //: var bgShowName: String?
        var bgShowName: String?
        //: var bgOpenCamera = VideoCameraStatus.Open
        var bgOpenCamera = ShareCameraStatus.Open
        //: var windowHeadPic: String?
        var windowHeadPic: String?
        //: var windowOpenCamera = VideoCameraStatus.Open
        var windowOpenCamera = ShareCameraStatus.Open

        //: if self.chatModel?.renderPostion == .SelfOnWindow {
        if self.chatModel?.renderPostion == .SelfOnWindow { // 自己在窗口
            //: bgHeadPic = self.chatModel?.pairHeadPic
            bgHeadPic = self.chatModel?.pairHeadPic
            //: bgShowName = self.chatModel?.pairNickName
            bgShowName = self.chatModel?.pairNickName
            //: bgOpenCamera = self.videoManager.config.remoteCameraStatus
            bgOpenCamera = self.videoManager.config.remoteCameraStatus
            //: windowHeadPic = self.chatModel?.iHeadPic
            windowHeadPic = self.chatModel?.iHeadPic
            //: windowOpenCamera = self.videoManager.config.openCamera ? .Open:.Closed
            windowOpenCamera = self.videoManager.config.openCamera ? .Open : .Closed

            //: } else {
        } else { // 自己在大背景
            //: bgHeadPic = self.chatModel?.iHeadPic
            bgHeadPic = self.chatModel?.iHeadPic
            //: bgShowName = self.chatModel?.iNickName
            bgShowName = self.chatModel?.iNickName
            //: bgOpenCamera = self.videoManager.config.openCamera ? .Open:.Closed
            bgOpenCamera = self.videoManager.config.openCamera ? .Open : .Closed
            //: windowHeadPic = self.chatModel?.pairHeadPic
            windowHeadPic = self.chatModel?.pairHeadPic
            //: windowOpenCamera = self.videoManager.config.remoteCameraStatus
            windowOpenCamera = self.videoManager.config.remoteCameraStatus
        }

        //: self.bgVideoView.refreshBgView(urlStr: bgHeadPic, showName: bgShowName, isOpenCamera: bgOpenCamera)
        self.bgVideoView.withCamera(urlStr: bgHeadPic, showName: bgShowName, isOpenCamera: bgOpenCamera)
        //: self.windowView.refreshBgView(urlStr: windowHeadPic, isOpenCamera: windowOpenCamera)
        self.windowView.restoreCamera(urlStr: windowHeadPic, isOpenCamera: windowOpenCamera)
    }
}

// MARK: - Event

//: extension TalkingVideoChatViewController {
extension InsideTopReactiveCompatible {
    /// 权限校验，播放BGM
    //: private func checkVideoAuth() {
    private func buildAuth() {
        //: TalkingPermissionTool.checkCameraAndMicrophone { isOpen in
        EqualReactiveCompatible.searchForMicrophone { isOpen in
            //: guard isOpen == true else {
            guard isOpen == true else {
                //: self.func__showStatusBarErrorMsg(showMsg: kMessage_permissions_error)
                self.colorGift(showMsg: showMeBarReasonPath)
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                PublishDataSocketDelegate.shared.birthday(info: [String(bytes: dreamItemStr.map{titleVar(table: $0)}, encoding: .utf8)!: (String(mStyleMessage) + String(dreamLogId)), String(bytes: m_coverName.reversed(), encoding: .utf8)!: [String(bytes: k_makeId.map{playerFill(view: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
                //: self.popCurrentViewController()
                self.periodicTable()
                //: return
                return
            }

            //: self.startPreview()
            self.textEqualPreview()
            //: self.stopBGM()
            self.arrayUp()
            //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: .Response)
            self.player = ReasonWindowPlayer.componentTo(type: .Response)
            //: self.startBGM()
            self.anViewBgm()
        }
    }

    /// 预览视频画面
    //: private func startPreview() {
    private func textEqualPreview() {
        //: self.videoManager.startPreview()
        self.videoManager.collectionMin()
    }

    /// 释放当前页面所有资源
    //: private func stopVCAllActivties() {
    private func withValue() {
        //: if self.timeOutTimer != nil {
        if self.timeOutTimer != nil {
            //: self.timeOutTimer?.invalidate()
            self.timeOutTimer?.invalidate()
            //: self.timeOutTimer = nil
            self.timeOutTimer = nil
        }

        //: self.stopBGM()
        self.arrayUp()
        //: TalkingVideoDanmuManager.danmu_releaseAllResource()
        TvThen.subOff()
        //: TalkingSocketManager.shared.callerDelegate = nil
        PublishDataSocketDelegate.shared.callerDelegate = nil
        //: TalkingSocketManager.shared.calledPartyDelegate = nil
        PublishDataSocketDelegate.shared.calledPartyDelegate = nil
        //: TalkingSocketManager.shared.errorDelegate = nil
        PublishDataSocketDelegate.shared.errorDelegate = nil
    }

    // 将当前视图从栈中移除
    //: private func popCurrentViewController(animated: Bool = true) {
    private func periodicTable(animated: Bool = true) {
        //: self.stopBGM()
        self.arrayUp()

        //: if self.navigationController?.topViewController == self {
        if self.navigationController?.topViewController == self {
            //: self.navigationController?.popViewController(animated: animated)
            self.navigationController?.popViewController(animated: animated)
            //: } else {
        } else {
            //: if var vcArr = self.navigationController?.viewControllers {
            if var vcArr = self.navigationController?.viewControllers {
                //: if let index = vcArr.firstIndex(of: self) {
                if let index = vcArr.firstIndex(of: self) {
                    //: vcArr.remove(at: index)
                    vcArr.remove(at: index)
                    //: self.navigationController?.setViewControllers(vcArr, animated: false)
                    self.navigationController?.setViewControllers(vcArr, animated: false)
                }
            }
        }
    }

    /// 视频通话双方窗口转换
    //: private func changeRenderView() {
    private func light() {
        //: if self.chatModel?.renderPostion == .selfOnBg {
        if self.chatModel?.renderPostion == .selfOnBg {
            //: self.chatModel?.renderPostion = .SelfOnWindow
            self.chatModel?.renderPostion = .SelfOnWindow
            //: } else if self.chatModel?.renderPostion == .SelfOnWindow {
        } else if self.chatModel?.renderPostion == .SelfOnWindow {
            //: self.chatModel?.renderPostion = .selfOnBg
            self.chatModel?.renderPostion = .selfOnBg
        }

        //: self.videoManager.changeRenderView()
        self.videoManager.original()
        //: self.videoBG_refreshViewWithCameraStatus()
        self.equalTab()
    }

    /// 创建视频通话房间
    //: private func createTalkRoom() {
    private func seek() {
        //: self.windowView.isHidden = self.chatModel?.videoStage != .Calling
        self.windowView.isHidden = self.chatModel?.videoStage != .Calling
        //: self.videoManager.createRoomAndStartPublish()
        self.videoManager.render()
        //: self.videoBG_refreshViewWithCameraStatus()
        self.equalTab()
        // 展示投诉说明飘屏（男性）
        //: self.show_videoFloatScreenAnimation()
        self.decease()
    }

    /// 展示投诉说明飘屏（男性）
    //: private func show_videoFloatScreenAnimation() {
    private func decease() {
        //: guard PathAppManager.share.loginUserMode.sex == Gender.male.rawValue else { return }
        guard PathAppManager.share.loginUserMode.sex == ConnectionClusterLiteral.male.rawValue else { return }
        //: guard reportView == nil else { return }
        guard reportView == nil else { return }

        //: reportView = TalkingVideoFloatScreenView()
        reportView = ValueThen()
        //: view.addSubview(reportView!)
        view.addSubview(reportView!)
        //: reportView?.show_animation()
        reportView?.logAnimation()
    }
}

// MARK: - 通知

//: extension TalkingVideoChatViewController {
extension InsideTopReactiveCompatible {
    /// 初始化通知
    //: private func initNotificationObserver() {
    private func exceptAdd() {
        // 监听 App 从后台切前台
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(appWillEnterForeground(notif:)),
                                               selector: #selector(use(notif:)),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        // 监听 对方摄像头是否可用
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(updateCameraStatus(notification:)),
                                               selector: #selector(cameraNotification(notification:)),
                                               //: name: VIDEO_CHAT_OTHER_CAMERA_AVAILABLE_NOTIFICATION,
                                               name: kPartyData,
                                               //: object: nil)
                                               object: nil)

        // socket断网重连监听
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(reachabilityChanged(note:)),
                                               selector: #selector(towardPlayNote(note:)),
                                               //: name: SOCKET_IS_OPEN_NOTIFICATION,
                                               name: dreamEnvironmentUrl,
                                               //: object: nil)
                                               object: nil)

        //: videoCallHelper.setDelegate(self, queue: DispatchQueue.main)
        userThirdData.setDelegate(self, queue: DispatchQueue.main)
    }

    /// app进入前台
    //: @objc private func appWillEnterForeground(notif: Notification) {
    @objc private func use(notif _: Notification) {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: let date = Date()
        let date = Date()
        //: let interval = date.timeIntervalSince(self.expiredDate)
        let interval = date.timeIntervalSince(self.expiredDate)
        //: if interval >= 0 {
        if interval >= 0 {
            //: popCurrentViewController()
            periodicTable()
        }
    }

    /// 断网重连socket
    //: @objc private func reachabilityChanged(note: Notification) {
    @objc private func towardPlayNote(note _: Notification) {
        //: guard TalkingSocketManager.shared.needReconnect == true else { return }
        guard PublishDataSocketDelegate.shared.needReconnect == true else { return }
        //: TalkingSocketManager.shared.callerDelegate = self
        PublishDataSocketDelegate.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        PublishDataSocketDelegate.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        PublishDataSocketDelegate.shared.errorDelegate = self
        //: self.videoManager.restartTalkWhenDisconnectedAndNetAvailable()
        self.videoManager.statusOf()
    }

    /// 根据对方摄像头状态更新UI
    /// - Parameter notification: 通知
    //: @objc private func updateCameraStatus(notification: NSNotification) {
    @objc private func cameraNotification(notification _: NSNotification) {
        //: self.videoBG_refreshViewWithCameraStatus()
        self.equalTab()
    }
}

// MARK: - CXCallObserverDelegate【通话监听】

//: extension TalkingVideoChatViewController: CXCallObserverDelegate {
extension InsideTopReactiveCompatible: CXCallObserverDelegate {
    //: func callObserver(_ callObserver: CXCallObserver, callChanged call: CXCall) {
    func callObserver(_: CXCallObserver, callChanged call: CXCall) {
        //: if call.hasConnected == true {
        if call.hasConnected == true { // 通话中
            //: self.func__showStatusBarErrorMsg(showMsg: "Call interruption")
            self.colorGift(showMsg: (String(k_bottomId) + notiInputData.replacingOccurrences(of: "image", with: "n")))
            //: switch(self.chatModel?.videoStage) {
            switch self.chatModel?.videoStage {
            //: case .Calling:
            case .Calling:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
                PublishDataSocketDelegate.shared.birthday(info: [String(bytes: dreamItemStr.map{titleVar(table: $0)}, encoding: .utf8)!: (dream_makeTopName.replacingOccurrences(of: "style", with: "s") + String(dream_videoBeautyData.prefix(7))), String(bytes: m_coverName.reversed(), encoding: .utf8)!: [String(bytes: userDoingKey.reversed(), encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Waiting:
            case .Waiting:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
                PublishDataSocketDelegate.shared.birthday(info: [String(bytes: dreamItemStr.map{titleVar(table: $0)}, encoding: .utf8)!: (noti_originText.lowercased() + String(noti_modelMainData.suffix(4))), String(bytes: m_coverName.reversed(), encoding: .utf8)!: [String(bytes: userDoingKey.reversed(), encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Recieved:
            case .Recieved:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                PublishDataSocketDelegate.shared.birthday(info: [String(bytes: dreamItemStr.map{titleVar(table: $0)}, encoding: .utf8)!: (String(mStyleMessage) + String(dreamLogId)), String(bytes: m_coverName.reversed(), encoding: .utf8)!: [String(bytes: k_makeId.map{playerFill(view: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
            //: case .none:
            case .none:
                //: break
                break
            }

            //: if miniView != nil {
            if miniView != nil {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.modelEnd()
                //: TalkingSocketManager.shared.currentDataVC = nil
                PublishDataSocketDelegate.shared.currentDataVC = nil
            }
            //: popCurrentViewController()
            periodicTable()
        }
    }
}

// MARK: - 通话音乐

//: extension TalkingVideoChatViewController {
extension InsideTopReactiveCompatible {
    /// 初始化视频通话背景音乐
    //: private func setUpBGMPlayer() {
    private func toMake() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let type = self.chatModel!.isCaller == true ? CallBGMPlayerType.Call : CallBGMPlayerType.Response
        let type = self.chatModel!.isCaller == true ? CommonHashableRepresentation.Call : CommonHashableRepresentation.Response
        //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: type)
        self.player = ReasonWindowPlayer.componentTo(type: type)
    }

    /// 开始播放背景音乐
    //: private func startBGM() {
    private func anViewBgm() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: guard self.chatModel?.resumeTalk != true else { return }
        guard self.chatModel?.resumeTalk != true else { return }

        //: self.player?.play()
        self.player?.fromCurrent()
    }

    /// 停止播放背景音乐
    //: private func stopBGM() {
    private func arrayUp() {
        //: guard self.player != nil else { return }
        guard self.player != nil else { return }
        //: self.player?.stop()
        self.player?.upwardStop()
        //: self.player = nil
        self.player = nil
    }
}

// MARK: - 定时器

//: extension TalkingVideoChatViewController {
extension InsideTopReactiveCompatible {
    /// 超时定时器
    //: private func timeoutRules() {
    private func audience() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let timeout = self.chatModel?.isCaller == true ? VIDEO_1v1_CALL_TIMEOUT : self.chatModel?.remainAnswerCallTime()
        let timeout = self.chatModel?.isCaller == true ? appTimeName : self.chatModel?.indicatorCommit()
        //: if timeout! <= 0 {
        if timeout! <= 0 {
            //: buildTalkTimeOutAction()
            contentAdd()
        }
        //: self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] timer in
        self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.buildTalkTimeOutAction()
            self.contentAdd()
            //: }, repeats: false)
        }, repeats: false)

        //: self.expiredDate = Date().addingTimeInterval(60)
        self.expiredDate = Date().addingTimeInterval(60)
    }

    /// 倒计时超时逻辑处理
    //: private func buildTalkTimeOutAction() {
    private func contentAdd() {
        //: guard !TalkingSocketManager.shared.isData else { return }
        guard !PublishDataSocketDelegate.shared.isData else { return }

        //: var socketDict: [String: Any]
        var socketDict: [String: Any]
        //: if self.chatModel?.isCaller == true {
        if self.chatModel?.isCaller == true { // 拨打方
            //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
            self.colorGift(showMsg: (String(user_devicePath) + dream_toolTalkStr.replacingOccurrences(of: "result", with: "r")).localized)
            //: socketDict = ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]]
            socketDict = [String(bytes: dreamItemStr.map{titleVar(table: $0)}, encoding: .utf8)!: (noti_originText.lowercased() + String(noti_modelMainData.suffix(4))), String(bytes: m_coverName.reversed(), encoding: .utf8)!: [String(bytes: userDoingKey.reversed(), encoding: .utf8)!: self.chatModel?.logId]]
            //: } else {
        } else { // 接收方
            //: socketDict = ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]]
            socketDict = [String(bytes: dreamItemStr.map{titleVar(table: $0)}, encoding: .utf8)!: (String(mStyleMessage) + String(dreamLogId)), String(bytes: m_coverName.reversed(), encoding: .utf8)!: [String(bytes: k_makeId.map{playerFill(view: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]]
        }
        //: TalkingSocketManager.shared.sendMessage(info: socketDict)
        PublishDataSocketDelegate.shared.birthday(info: socketDict)
        //: popCurrentViewController()
        periodicTable()
    }
}

// MARK: - resume talk【恢复通话】

//: extension TalkingVideoChatViewController {
extension InsideTopReactiveCompatible {
    /// 恢复视频通话，更新chatModel
    /// - Parameter resumeData: 数据
    //: func updateInfoViaResumeTalkData(resumeData: [String: Any]) {
    func noFile(resumeData: [String: Any]) {
        //: let json = JSON(resumeData)
        let json = JSON(resumeData)
        //: let logId = json["logId"].intValue
        let logId = json[String(bytes: userDoingKey.reversed(), encoding: .utf8)!].intValue
        //: let duration = json["duration"].intValue
        let duration = json[(mainPageTitle.replacingOccurrences(of: "array", with: "a") + "tion")].intValue
        //: let interval = Date().timeIntervalSince1970
        let interval = Date().timeIntervalSince1970
        //: let startTalkTime = floor(interval) - Double(duration)
        let startTalkTime = floor(interval) - Double(duration)
        //: self.chatModel?.startTime = startTalkTime
        self.chatModel?.startTime = startTalkTime
        //: self.interactionView.updateStartTime(startTime: startTalkTime)
        self.interactionView.getMovingDown(startTime: startTalkTime)

        //: if self.chatModel?.videoStage != .Calling {
        if self.chatModel?.videoStage != .Calling {
            //: self.chatModel?.logId = logId
            self.chatModel?.logId = logId
            //: self.chatModel?.videoStage = .Calling
            self.chatModel?.videoStage = .Calling
            //: self.interactionView.showViewsViaStage()
            self.interactionView.sendTarget()
            //: createTalkRoom()
            seek()
            //: self.stopBGM()
            self.arrayUp()
        }
    }

    /// 创建通话中房间
    /// - Parameter logId: 通话记录Id
    //: private func createCallingRoom(logId: Int?) {
    private func talkNeed(logId: Int?) {
        //: guard let logId = logId else { return }
        guard let logId = logId else { return }

        //: self.chatModel?.logId = logId
        self.chatModel?.logId = logId
        //: self.chatModel?.videoStage = .Calling
        self.chatModel?.videoStage = .Calling
        //: self.chatModel?.renderPostion = .SelfOnWindow
        self.chatModel?.renderPostion = .SelfOnWindow

        //: self.videoManager.changeRenderView()
        self.videoManager.original()
        //: self.bgVideoView.showCoverView(show: false)
        self.bgVideoView.effectBringHomeShared(show: false)
        //: self.stopBGM()
        self.arrayUp()
        //: self.interactionView.showViewsViaStage()
        self.interactionView.sendTarget()
        //: self.createTalkRoom()
        self.seek()
    }
}

// MARK: - TelevisionCallerDelegate【拨打方】

//: extension TalkingVideoChatViewController: SocketManagerVideoChatCallerDelegate {
extension InsideTopReactiveCompatible: TelevisionCallerDelegate {
    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    //: func socket_videoChat_cancelCall(data: [String: Any]) {
    func mentionExecute(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(app_logStr.suffix(5)) + String(appTextName)).localized
            //: if json["cancelSex"].stringValue == Gender.male.rawValue {
            if json[(String(user_norQuantityLogId) + String(noti_lengthFormat.prefix(7)))].stringValue == ConnectionClusterLiteral.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(noti_somethingMsg.suffix(4)) + String(dreamLabelTitle.suffix(4)) + "ung up").localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.colorGift(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.modelEnd()
            //: TalkingSocketManager.shared.currentDataVC = nil
            PublishDataSocketDelegate.shared.currentDataVC = nil
        }

        //: popCurrentViewController()
        periodicTable()
    }

    /// 对方接受了来电，建立视频通话
    //: func socket_videoChat_acceptCall(data: [String: Any]) {
    func occurStatus(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: createCallingRoom(logId: json["logId"].intValue)
        talkNeed(logId: json[String(bytes: userDoingKey.reversed(), encoding: .utf8)!].intValue)
    }

    /// 对方结束了通话（通话中）
    //: func socket_videoChat_stopCall(data: [String: Any]) {
    func inner(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)

        //: let endType = json["endType"].intValue
        let endType = json[(String(noti_sizeId.suffix(7)))].intValue
        //: var typeStr = "She has hung up".localized
        var typeStr = (String(app_logStr.suffix(5)) + String(appTextName)).localized
        //: if json["stopSex"].stringValue == Gender.male.rawValue {
        if json[(String(kTranslationKey.suffix(7)))].stringValue == ConnectionClusterLiteral.male.rawValue {
            //: typeStr = "He has hung up".localized
            typeStr = (String(noti_somethingMsg.suffix(4)) + String(dreamLabelTitle.suffix(4)) + "ung up").localized
        }

        //: if endType == 1 {
        if endType == 1 { // 付费方余额不足
            //: if PathAppManager.share.appStatus == AppSkinStatus.normal.rawValue {
            if PathAppManager.share.appStatus == AssemblageHashable.normal.rawValue {
                //: typeStr = "Insufficient gold coins, call has ended".localized
                typeStr = String(bytes: userTopMsg.map{centerRowResistance(title: $0)}, encoding: .utf8)!.localized
                //: } else {
            } else {
                //: typeStr = "The other hung up the phone".localized
                typeStr = (String(k_byAppKey.prefix(5)) + "ther " + String(k_coverStr.suffix(5)) + String(m_nameKey.suffix(6)) + " phone").localized
            }
        }
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.colorGift(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.modelEnd()
            //: TalkingSocketManager.shared.currentDataVC = nil
            PublishDataSocketDelegate.shared.currentDataVC = nil
        }

        //: self.interactionView.func_giftViewDismiss()
        self.interactionView.lessDismiss()
        //: popCurrentViewController()
        periodicTable()
    }

    /// 余额不足提醒
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any]) {
    func surcharge(msg: String, data: [String: Any]) {
        //: guard PathAppManager.share.appStatus == AppSkinStatus.normal.rawValue else { return }
        guard PathAppManager.share.appStatus == AssemblageHashable.normal.rawValue else { return }

        //: let index = data["type"] as? String ?? ""
        let index = data[(showFormatMessage.replacingOccurrences(of: "model", with: "pe"))] as? String ?? ""
        //: if index == "2" {
        if index == "2" {
//            if self.interactionView.isShowRecharge {
//                return
//            }
            //: guard let jumpUrl = data["jumpUrl"] as? String  else { return }
            guard let jumpUrl = data[(String(dream_finishPath) + dreamSizeGroupUrl.lowercased())] as? String else { return }
            //: let httpsUrl = PathAppManager.share.appConfigMode.urlH5Domain+jumpUrl
            let httpsUrl = PathAppManager.share.appConfigMode.urlH5Domain + jumpUrl
            //: var config = TalkingWebConfig.init()
            var config = WillConfig()
            //: config.isDiscountsView = true
            config.isDiscountsView = true
            //: NamePushManager.share.func__pushToWebVC(urlStr: httpsUrl, webConfig: config)
            NamePushManager.share.upwardsConfig(urlStr: httpsUrl, webConfig: config)
            //: } else {
        } else {
            //: TalkingAlertShow.alert(title: nil,
            CourseAlertShow.blockConfig(title: nil,
                                         //: message: msg,
                                         message: msg,
                                         //: leftBtnTitle: "Cancel".localized,
                                         leftBtnTitle: (String(kPhotoFormat.prefix(5)) + m_sessionName.lowercased()).localized,
                                         //: rightBtnTitle: "Get gold coins".localized) {
                                         rightBtnTitle: (String(k_makeIdent.prefix(5)) + String(user_mPath.suffix(4)) + "coins").localized)
            {
                //: TalkingAlertShow.hideAlert()
                CourseAlertShow.cardWindow()
                //: } rightBlock: {
            } rightBlock: {
                //: NamePushManager.share.func__jumpToWebRecharge(clickEvent: clickLess1minButton, sufficient: false)
                NamePushManager.share.rechargeParams(clickEvent: user_styleUrl, sufficient: false)
            }
        }
    }

    /// 收到礼物消息展示
    //: @objc func receiveGiftPlay(giftArr: Array<Any>) {
    @objc func giftControl(giftArr: [Any]) {
        //: interactionView.addGiftEffectModelArr(giftArr: giftArr)
        interactionView.writtenRecord(giftArr: giftArr)
    }

    /// 女性通话积分激励动效展示
    //: func socket_videoChat_onCallMotivation(data: [String: Any]) {
    func reportSeek(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: let addBean = json["addBean"].stringValue
        let addBean = json[(String(show_errorPath.prefix(7)))].stringValue
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: interactionView.showPointAnimaData(point: addBean)
            interactionView.withMessage(point: addBean)
            //: } else {
        } else {
            //: self.miniView?.showPointAnimaData(point: addBean)
            self.miniView?.pointAdd(point: addBean)
        }
    }
}

// MARK: - ValueLevelObjectProtocol【接收方】

//: extension TalkingVideoChatViewController: SocketManagerVideoChatCalledPartyDelegate {
extension InsideTopReactiveCompatible: ValueLevelObjectProtocol {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_refuseCall(data: [String: Any]) {
    func chat(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(app_logStr.suffix(5)) + String(appTextName)).localized
            //: if json["refuseSex"].stringValue == Gender.male.rawValue {
            if json[(dreamMoreId.lowercased() + String(kTalkAddUrl.suffix(4)))].stringValue == ConnectionClusterLiteral.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(noti_somethingMsg.suffix(4)) + String(dreamLabelTitle.suffix(4)) + "ung up").localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.colorGift(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.modelEnd()
            //: TalkingSocketManager.shared.currentDataVC = nil
            PublishDataSocketDelegate.shared.currentDataVC = nil
        }

        //: popCurrentViewController()
        periodicTable()
    }

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    //: func socket_videoChat_onAcceptCall(data: [String: Any]) {
    func viewAppear(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: createCallingRoom(logId: json["logId"].intValue)
        talkNeed(logId: json[String(bytes: userDoingKey.reversed(), encoding: .utf8)!].intValue)
    }
}

// MARK: - DelegateObjectProtocol【异常处理】

//: extension TalkingVideoChatViewController: TalkingSocketManagerErrorDelegate {
extension InsideTopReactiveCompatible: DelegateObjectProtocol {
    // 处理error情况
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
    func reverberation(errorNo: Int, cmd: String, msg: String, data _: [String: Any]) {
        //: if cmd == "refuseCall" { // 对方拒绝了通话
        if cmd == (String(mStyleMessage) + String(dreamLogId)) { // 对方拒绝了通话
            //: if self.miniView == nil {
            if self.miniView == nil {
                //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
                self.colorGift(showMsg: (String(user_devicePath) + dream_toolTalkStr.replacingOccurrences(of: "result", with: "r")).localized)
                //: } else {
            } else {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.modelEnd()
                //: TalkingSocketManager.shared.currentDataVC = nil
                PublishDataSocketDelegate.shared.currentDataVC = nil
            }
            //: popCurrentViewController()
            periodicTable()

            //: } else if cmd == "onAcceptCall" && errorNo == 101 { // 接听通话时，余额不足
        } else if cmd == (String(dream_appearIdent.suffix(6)) + String(dream_barPath.prefix(6))) && errorNo == 101 { // 接听通话时，余额不足
            //: guard PathAppManager.share.appStatus == AppSkinStatus.normal.rawValue else { return }
            guard PathAppManager.share.appStatus == AssemblageHashable.normal.rawValue else { return }
            //: NamePushManager.share.func__jumpToWebRecharge(sufficient: false)
            NamePushManager.share.rechargeParams(sufficient: false)

            //: } else if cmd == "onAcceptCall" && errorNo == 102 { // 对方已取消通话
        } else if cmd == (String(dream_appearIdent.suffix(6)) + String(dream_barPath.prefix(6))) && errorNo == 102 { // 对方已取消通话
            //: self.func__showStatusBarErrorMsg(showMsg: msg)
            self.colorGift(showMsg: msg)
            //: popCurrentViewController()
            periodicTable()
        }
    }
}

// MARK: - CannulateViewDelegate【通话按钮操作事件】

//: extension TalkingVideoChatViewController: Talking1v1VideoInteractionViewDelegate {
extension InsideTopReactiveCompatible: CannulateViewDelegate {
    /// 接听视频通话
    //: func interactionView_shouldAcceptCall() {
    func nearTakeABow() {
        //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "acceptCall", "data": ["uid": self.chatModel?.pairUid]])
        PublishDataSocketDelegate.shared.birthday(info: [String(bytes: dreamItemStr.map{titleVar(table: $0)}, encoding: .utf8)!: (String(noti_countIdent.suffix(7)) + String(notiBackData)), String(bytes: m_coverName.reversed(), encoding: .utf8)!: [String(bytes: k_makeId.map{playerFill(view: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
    }

    /// 拨打方结束通话
    //: func interactionView_shouldEndCall() {
    func errorBy() {
        //: if self.chatModel?.videoStage == .Waiting {
        if self.chatModel?.videoStage == .Waiting { // 拨打方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.colorGift(showMsg: (String(showAppName)).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
            PublishDataSocketDelegate.shared.birthday(info: [String(bytes: dreamItemStr.map{titleVar(table: $0)}, encoding: .utf8)!: (noti_originText.lowercased() + String(noti_modelMainData.suffix(4))), String(bytes: m_coverName.reversed(), encoding: .utf8)!: [String(bytes: userDoingKey.reversed(), encoding: .utf8)!: self.chatModel?.logId]])
            //: } else if self.chatModel?.videoStage == .Recieved {
        } else if self.chatModel?.videoStage == .Recieved { // 接收方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.colorGift(showMsg: (String(showAppName)).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
            PublishDataSocketDelegate.shared.birthday(info: [String(bytes: dreamItemStr.map{titleVar(table: $0)}, encoding: .utf8)!: (String(mStyleMessage) + String(dreamLogId)), String(bytes: m_coverName.reversed(), encoding: .utf8)!: [String(bytes: k_makeId.map{playerFill(view: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
            //: } else {
        } else { // 【通话中】
            //: self.func__showStatusBarErrorMsg(showMsg: "The call has ended".localized)
            self.colorGift(showMsg: (String(m_labelFacePath.prefix(6)) + "ll ha" + String(user_modelIdent)).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
            PublishDataSocketDelegate.shared.birthday(info: [String(bytes: dreamItemStr.map{titleVar(table: $0)}, encoding: .utf8)!: (dream_makeTopName.replacingOccurrences(of: "style", with: "s") + String(dream_videoBeautyData.prefix(7))), String(bytes: m_coverName.reversed(), encoding: .utf8)!: [String(bytes: userDoingKey.reversed(), encoding: .utf8)!: self.chatModel?.logId]])
        }

        //: popCurrentViewController()
        periodicTable()
    }

    /// 旋转摄像头
    //: func interactionView_shouldChangeCamera(isFront: Bool) {
    func loadUp(isFront: Bool) {
        //: self.videoManager.useFrontCamera(isFront)
        self.videoManager.useShared(isFront)
    }

    /// 开启/关闭摄像头
    //: func interactionView_shouldForbiddenCamera(forbidden: Bool) {
    func buttonKey(forbidden: Bool) {
        //: if self.chatModel?.videoStage == .Recieved {
        if self.chatModel?.videoStage == .Recieved {
            //: self.bgVideoView.showCoverView(show: !forbidden)
            self.bgVideoView.effectBringHomeShared(show: !forbidden)
        }
        //: self.videoManager.useCamera(!forbidden)
        self.videoManager.useRequest(!forbidden)
        //: self.videoBG_refreshViewWithCameraStatus()
        self.equalTab()
    }

    /// 展示视频通话小窗口
    //: func interactionView_shouldDisplayMiniWindow() {
    func displayForText() {
        //: TalkingSocketManager.shared.currentDataVC = self
        PublishDataSocketDelegate.shared.currentDataVC = self
        //: self.miniView = TalkingVideoMiniView.buildVideoMiniView()
        self.miniView = SharedThen.command()
        //: self.videoManager.smallRenderView = self.miniView?.renderView
        self.videoManager.smallRenderView = self.miniView?.renderView
        //: self.videoManager.enterSmallRenderMode()
        self.videoManager.appGift()
        //: self.miniView?.tapGestureBlock = { [weak self] in
        self.miniView?.tapGestureBlock = { [weak self] in // 还原回通话界面
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.videoManager.exitSmallRenderMode()
            self.videoManager.wayOut()
            //: self.isFromMiniView = true
            self.isFromMiniView = true
            //: self.currentViewController()?.navigationController?.pushViewController(self, animated: true)
            self.withTo()?.navigationController?.pushViewController(self, animated: true)
        }
        //: popCurrentViewController()
        periodicTable()
    }

    /// 投诉成功
    //: func interactionView_reportSucceed() {
    func nameLoad() {
        //: if reportView?.superview != nil {
        if reportView?.superview != nil {
            //: reportView?.removeFromSuperview()
            reportView?.removeFromSuperview()
        }
    }
}

// MARK: - Layout

//: extension TalkingVideoChatViewController {
extension InsideTopReactiveCompatible {
    /// 添加视图
    //: private func setupSubviews() {
    private func recordSize() {
        //: self.view.layer.masksToBounds = true
        self.view.layer.masksToBounds = true
        //: self.view.addSubview(bgVideoView)
        self.view.addSubview(bgVideoView)
        //: self.view.addSubview(interactionView)
        self.view.addSubview(interactionView)
        //: self.interactionView.addWindowView(view: self.windowView)
        self.interactionView.viewApp(view: self.windowView)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func upClear() {
        //: bgVideoView.snp.makeConstraints { make in
        bgVideoView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: interactionView.snp.makeConstraints { make in
        interactionView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
    }
}
